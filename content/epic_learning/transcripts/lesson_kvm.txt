One of the key components of any 2D game is animation, and Paper 2D provides us with a flipbook asset in order to create and control 2D animations. In this video we'll create our first animation using the Character Animation sheet we imported in the previous module. Once that's done, we'll walk through the relevant options and configure the flipbook to get our character animation looking its best. We'll begin by creating a new sprite asset from our previously imported textures. That will allow us then use some built-in Paper 2D utilities to quickly extract each frame of animation and build a flipbook. As always, we should try our best to keep things organized from the very start, so let's go ahead and create a new folder to store our sprite assets. In the content browser, navigate to the My Content folder and create a new folder called Sprites. Open the Sprites folder and create a new folder within it called Character. Finally, open this character folder and create folders below that for each of the animations we'll need. This means we'll create a folder called Walk, Run, Jump, Idle Stand, Full and Air Trans. With the folders created, navigate to Content, My Content, Textures and locate the T underscore UE4 robot underscore Walk underscore D texture. Right-click this asset and select Sprite Actions, Extract Sprites. This will open the Extract Sprites window. Looking in the settings roll out of the Details panel, we can see the Sprite Extract mode defaults to Auto. Looking over on the left-hand side in the viewport, we can see the yellow boxes indicating what that would look like. While to be fair, it's done a pretty good job. We can clearly see the size of the box changes for some of the frames, which would cause a lot of problems later on. What we want to do here is define a constant size that matches the intended size of each frame when the artist created the asset. In this instance, each frame is intended to be 64 pixels wide and 64 pixels tall. Returning to the settings roll out in the Details panel, change the Sprite Extract mode from Auto to Grid. Selecting this option will add a new Grid roll out in the Details panel. The Cell Width and Height options have defaulted to the size of the entire texture, and we can see that represented by the yellow box surrounding the entire image. Now change the Cell Width and Cell Height to 64. Note that we now have a perfect square around each frame of the animation. And with that change made, we're now ready to extract the individual Sprites, and we can do this by hitting the Extract button on the bottom right of the window. Unwell Engine will now extract each individual Sprite, putting an increasing number at the end of the file name. We can see that this Walk animation has 16 frames. Don't forget that the count starts from 0. Keeping all these Sprites selected, open the Sources panel in the Content Browser and locate our Sprites character Walk folder, and then drag all of the selected Sprites into that folder. Now that we have extracted the Sprites, all we need to do is combine them into a single Flipbook asset. Navigate to the My Content folder and create a new folder called Flipbooks, and then create Character and Walk folders similar to our Sprites folder structure. Use the Content Browser to return to our Walk Sprites. Select all of the Sprites from the Walk texture we created earlier, right click on any of them and select Create Flipbook in the Sprite Action section of the menu. This will create a new Flipbook asset and prompt you to rename it. I suggest something like Flipbook, underscore UE4, underscore robot, underscore walk. Use the Content Browser to move this new Flipbook into the correct Walk folder. Move to the folder in the Content Browser and double click the asset so that we can take a quick look at the Flipbook editor. Along the bottom we can see the timeline. We can also see how each individual frame has been added in the correct order using the naming convention. We also have Play and Pause controls. We can also scrub through the animation by selecting the red playhead and tracking it backwards and forwards. Note that we could also adjust the playback frame rate by adjusting the frames per second value in the Sprite rollout within the Details panel. However, we do not need to make any changes here. These assets have been specifically designed to look good with the standard frame rate. All we need to do now is repeat this process for each of the character animations, making sure to create a folder for each new collection of Sprites and individual flipbooks. When you've completed the process for all the flipbooks, the following folder structure should be filled like this. In this video we created our very first Paper2D animation asset, the Flipbook. We began by creating a series of Sprites, extracting each frame of animation from our previously imported Counter-Sheet texture. We then finished up by using these Sprite assets to create the Flipbook, combining each individual frame into a single animation. We kept things organized by using a folder structure that kept our source texture, extracted Sprites and Flipbooks separated.