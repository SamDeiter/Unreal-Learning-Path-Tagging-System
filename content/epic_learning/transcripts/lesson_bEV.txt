With the Blueprint Data Table functionality working, we can now see the similar approach when taken in C++. We'll be doing this in the Data Actor Table class, so go ahead and get that open and ready. Inside of the header, you'll notice that I've declared the function, post-edit-change-property, which is an override function. This allows us to place our logic here, and then whenever anything is changed through the editor, we'll see those results in real time. You could implement the logic on the BeginPlay or Custom function, but this allows our C++ class to work in a similar way to the functionality that we've placed in the construction script in our Blueprint class. Below this, we're going to want to create a new variable, which will be exposed to the editor. This will be a pointer of type U Data Table, named Item Data Table. I'll set the U property to be Edit anywhere, Blueprint read-only, and put it in the category named ItemInfo. This will allow us to swap our Data Table that were about to accessing code through the editor if we wanted to test different tables, just again for that added flexibility. Before moving over to the code file, I'm just going to return to the Data Actor Base header and add a new protected variable for both of our inherited classes to use. This will be a Boolean named BRun code, and I'll default this to be false. I'll copy one of the U properties from above and paste that just above our new Boolean. This is another optional step, but I'd recommend taking this one as well. It's going to make things easier to switch between our Blueprint tests and our C++ test in both our asset and table data actors. Moving back over to the Data Actor Table Code file, we can start implementing our first bit of logic to get our information from the database in C++. Inside of the post-edit change property function, making sure that we put this below the supercool, so that the other editor changes are accounted for even if something here doesn't happen. We're going to want to place some conditional safety checks to avoid crashes as we're going to be making a lot of these changes live in the editor based on the information we're hoping that we have access to. So first of all, we want to ensure that our logic is only run if all of the potential variables holding the data that we want to access are valid. This is going to be quite simple. We'll just say that we will, first of all, if BRun code is false or if isn't BRun code and then add an OR condition for the item type is equal to the E-item type none, then if either of these are true, then we're just going to return out of the function entirely. We're then going to want a second conditional if check just below, add check for a valid item data table. If we put this above in our check to return, if we have a valid data table in this slot, that is then cleared in the editor. We're likely to get past this returning of the function and then enter into an engine crash. So this is why we're just going to put it in a new statement here. All of our logic is going to be placed inside of this F statement as we're now certain that we've chosen to run the code. We have a valid item type to pull information and a valid item data table to pull that information from. So the first thing that we need is a new F name variable, which is going to be the thing our search query will be based on to find a valid row. Similar to our blueprint approach, we're going to leverage the enum to avoid any user error, but this will require a couple of extra steps similar again to blueprint. So the first things that we're going to create a new const F name named Ronin. This will be the result of a conversion using a new F name and passing in the Uenum type. And from this using the default function named get display value is text and this is going to be based on our item type and then converting the resulting name to a string again to clean that information. So that means that we now have our name to search for or the roe named search for. So we're going to create a new F item information table pointer named row. And this will be the result of the find row function on our item data table inside of the angled brackets. We want to provide the type of F item information table and we'll be looking for the row name and I'll set the context string to item details. So this is basically going to attempt to return the row information based on the name of the item that we're passing in from the F information table. The context string can be left completely blank by passing in an empty set of quotation marks. This is only really used if something goes wrong to generate warnings. So this will help give you more details about where the error is happening. So you can make this as for both as necessary. Then at this point we have a valid set of details stored in our row as long as we've provided a valid name to a valid data table, which we know we have all of that information or else we wouldn't be inside of this if check. So with that information available we now want to, before setting any of the variables, we're going to use another F check to ensure this. This is going to be quite simply F row. Then we can take all of the information from our temporary row and store this somewhere. Now this is going to be a little bit repetitive, but what we want to do is we're going to set every piece of information in our generic item info to be from the struct that we've just declared in our parent class to be the result of the information we've just stored in a temporary row. So in order I'm just going to go through and say that the generic item info dot data is equal to the new variable. So for instance dot item type and then dot mesh is equal to row item type and row mesh using the arrow operator to access the data in the row. So if we repeat this process for the item cost item image item info item name the curve float and the rotation duration you should have something that ends up looking like this. And like with the blueprint the final step is going to be that visual finish. So we're going to also set we're going to take this information and we're going to set the mesh components static mesh to be the mesh in the generic item info dot mesh. Now that we have that stored we can use the information there. You could also use the row mesh either will work fine here. And that is our C++ logic implemented and ready to go and work with our data table. So again be sure to save compile in head back on over to the editor when that's finished. With the compilation complete back in the BP underscore data item table construction script just going to make a couple of quick changes based on our new variables declared in our C++ parent. First allow the branch to check and only run if the run code is equal to false or isn't true. And then rather than manually setting the data table type here we can now use the item data table that we've exposed to the editor. Then back in our map we can test the new BP or C++ code at a simple switch of our run code boolean with this set to true we know that we're using the C++ logic and set to false we're going to be using our blueprint logic be sure to also set the item data table to our item info table drop the generic item info to check that the data is being updated correctly. And of course if we switch the item around to the different types we can test the results. We can also make sure this is still working with our widget so I'm going to come in our fly round and interact with some of the items again and just check that the correct information is still being passed to the widget and it seems as though that is all working as expected. So they're the basics of working with data tables in both blueprint and C++. Hopefully some of those additional steps have provided some kind of extra ideas of how you can add flexibility to your classes when sharing or testing joint functionality or just to see what's happening with features you may not be too familiar with.