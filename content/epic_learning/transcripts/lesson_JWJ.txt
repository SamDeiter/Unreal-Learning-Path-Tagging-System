So, schematic overview. This is the same as we have seen in the intro, but more in depth. We can see the real built tool here, the real automation tool, and the source code. I'm going to switch to the image. All the base types that we're going to be using to create our code, the gameplay framework I talk about, memory management, replication, and then all those stages compile build cook back and the bugging when needed. So let's start with the first part, that is how we structure our code in different modules, plugins or platforms. A module that will be the pillar of our code, a module is a group of files that organize the code in one big tank of, let's say, one big tank of features. So for example, I have two modules in our example project. One is gain, that will be the main one, that is including combat. So for example, the good thing is when you have different modules and you separate functionality among modules, what you can do is easily get this, for example, this combat module and use it in a different project. In a very transparent, if you do it properly, it's going to be very transparent. And then you have a combat system that you can use in several different projects because you divided this in separate modules. But also it's good for compiling times. If you only change something in one module, you only will have to compile that module. In the same sense, we have plugins that are even more decoupled from the code. Plugins can leave either in the engine or in our project. And the idea behind plugins is something that you can activate and deactivate and the gain or the engine should be running in the same way but just with less functionality. I usually say that for example, plugins to decide if that plugin should go into the engine or into the project, if it's something that you are going to be using in different projects and you are going to share the same engine among those projects, among different teams, then that plugin should go into the engine plugin folder. The platform is something new that we are still working on. Platforms, it's going to be different folders for each platform and it's all the code that that platform needs. Typically we had that inside the engine folder, like the online subsystems and sets and it was deeply imbebbed in the engine. But now we are trying to decouple it from the engine and have different folders where we can find all the source code that we need for that platform separately. So the Unreal build tool will look at all the modules that we have, all the plugins and create the files and the project that we need to handle the source code. So let's have a look at the base types that we have in the engine and in our C++. That is not purely basic C++ because as we are going to see we have a lot of different new types and we have a layer of code on top of C++ to make us our life much easier and to provide visibility from our code to the editor. So those base types I'm going to start with the most basic ones. We have our own NUM system to create new data types. Let's have a look. For example, Inter soluchinal, current break, thing I should... For example, I can create my own NUMs and as you can see it's built on top of the regular C++ NUM definition we create a layer that will give us metadata to work with. We have several different types of strings. We have F string, F name and F text. F string will be the most basic one. F name will be AIM for unique names of actors for example. And the F text is localization ready strings. So that means that it's then that we create an F text. We can then in the localization system gather all the F text that we have in Blubrins, in Actors, in Code. We will have a list of all those and we can easily localize them. We have Structs, three types, different types of collections, maps, arrays, sets. One thing to note about our arrays is that we usually... We can't have a race of arrays. We have to do workarounds to work that restriction. And the classic transform types like vectors, rotations, quadrions and transform rotators will be using Euler angles. But the most important ones are U object and actor. A U object is an object that is going to be handled by our garbage collector system. So any kind of, for example, an asset will be another U object, an actor is an U object. So anything that has to be handled automatically by the memory system should inherit from U object. And an actor is something that can be placed in a level. So anything that can have a transform is going to be an actor. And complementing actors, we have components. Components will add functionality to actors. They are kind of... They inherit from object also. So they also manage, but the garbage collector. And like other engines, what we have is that both actor and components can provide functionality. So it's a mix approach to the component paradigm. And we can either have children of actor that have a particular feature of functionality. And we can then add components to add functionality to them. So for example, in our project, if I go to the group prints, and I come to the main character, as I can see, the parent class of the main character is a combat character. Instead of an actor, it's this combat character. And this combat character inherits from character that it's inheriting from actor. So it has some functionality without having a component at that. But at the same time, the character has also a damatible component, mesh components, capsule components, to add all the functionality that that actor doesn't have. And that functionality that we want to share with any kind of actor. So then we have a life cycle for actors and eObjects. That life cycle is going to be dependent if that actor has been placed in the level in the editor, or that actor is being spawned directly from code or from Blueprint. And also, we have several ways of handling. We have a smart pointer to handle references to actors or even to components. And depending on those smart pointers, the garbage collector will be collecting or not a reference that has been lost. There are also different ways of creating and distracting actors and objects. Usually an actor will be spawned. It will be destroyed. But an object instead will be created with new object. But we shouldn't be trying to destroy automatically, like specifically an object. The object should be destroyed by the garbage collector once we have no reference to that object. So as we said also, we have a layer of functionality over our C++. That is all these macros that can be seen, you function, you property, you class, generated body. And inside these macros, we have specifiers. This specifiers will add a layer of functionality to any function or variable that we are using them in. So for example, in this property, we're saying that this property can be edited in the editor, that can be read only from Blueprint and can be written. This one, for example, can be written. And then we are setting categories for the sake of sorting them. And also, with function, we can do a lot of different things. For example, this can be called from Blueprint, so it will be a Blueprint node and sets. So specifiers are really, really important to know the most important ones. It's really important to know them because it will have an expose functionality for designers and for artists, for example, to work with them in the editor. So for example, if I go to Blueprint, I can call this function that is Blueprint Colable. From I can create, for example, Blueprint Colable, HasTargetLog. I can red-click HasTargetLog. If I remove the contact sensitive, I can find this function that is only for combat characters. But also, in the basic data types, we have some data objects that are really useful for gameplay purposes and also for designers to be able to access this kind of data. We have curves, data that we can reference from Blueprint or even from C++ to change, I don't know, the interpolation movement between actors and sets. Any thing that you can imagine, data sets that are purely data that we can reference from code. For example, data tables are specified by code. I can specify what we will put in that table. For example, here I am specifying what a role of that table is going to contain. Base Damage, Base Health, Site Distance. I am creating table that uses those raw information. From Blueprint, we are reading this to spawn these kinds of different minions and enemies. We have a lot of tools also to help gameplay. What else? The best types, then let's move to the gameplay framework, I think. This will be the lifecycle of actors, begin play and play construction. In between, we initialize all components so we can hook all those events and we can place code in any of those events. Even Penning Kill means that when we set one actor to be destroyed, it will be marked as Penning Kill. It is usually a common error to try to access actors that have been set as Penning Kill. Memory Management. As I said, we have a garbage collector. The garbage collector will automatically look for references that have no reference at all and will mark them and will gather that memory when it sits fit. We have different ways of dealing with this and dealing with assets. Let's say that we want to have an asset and reference it from code. So that asset, depending on how we do it, will be loaded as soon as the code is loaded. Or we can have a reference to that asset. And we can ask to the streaming system to load that asset synchronously. So we don't have to have everything loaded from the beginning. And the same thing happens with actors. We can control the spawning and the strain of actors. Except if they were placing them in the map, if they were placing them up and we unload them up, they will be all automatically garbage collected. What else? Gameplay and Fair Work. So the Gameplay and Fair Work is something that comes with the engine when you download it. And it provides a general system to create any kind of games. It provides a way of controlling your game flow. It provides a way of having characters in your game. And it provides a way of control for the player to control any kind of interaction in your game. So the main pillar will be the game mode. The game mode controls the flow of the game. So it decides when the game has finished, when it has to change. And usually contains all the information that should be handled and to change an estate in the game. It also contains which other classes are set as a false right? Yes, yes, yes. The game mode leaves in the level. So it's time we change level. We are changing the game mode. So that's the reason for the game instant to exist. Game instance is going to persist any level change that we do. And the game mode is going to leave and this is related to replication, but the game mode is going to exist in the server. So no client can access to the game mode. But we can share information we got in the game state, in the game state. So it, for example, we have the game mode, will like the state of the game will change if we reach some certain point. For example, in this case, when we're playing, we have an enemy count here, that information has to live in the game mode. But if we want to share that along several players, we have to place that in the game state and say and mark it as replicate. And as part of that, I mean, all of this is replicated as publication support. So that's not a feature of the framework. Yeah. Yeah. It's everything here in the game play framework and usually everything in the engine is replication ready. Yeah. And also the same happens with the player controller. The player controller is going to be the brain of the character. And with that, what I mean is that we can have a brain that controls, let's say a puppet, the character is going to be kind of a puppet and any decision that to any decision of action for that puppet is going to be taken by the player controller. And the player controller can be driven by, well, in this case, player controller is going to be driven by the input of the player, but we can have other kind of controller that are the AI controller. And those AI controller be taking decision depending on on behavior trees or any kind of AI that we want to supply to the controller. And if you have a group of units and you want to switch between units, there's only one player controller, but there's four characters or ponds. Yeah. That's how you, for example, we have a game where we can process different kind of units. We will have a player controller that will be processing and amposes in different characters. But if, for example, we have an RTS and we have a top down vision and we control several characters at the same time, we only will have a player controller that is not going to process anyone, but will be reading the input, like the mouse actions and sets, and we'll be making decisions and actions for all those characters. So you can process only one character, but you also can control. A-I controller. And AI controller. And I guess I say you just tell them go there and go there. Yeah. All those will have AI controllers to say, this is the action that the player controller told us to do. And in the same fashion as the game mode and game state, the player controller, only the client will replicate to the only the only the only client will own, we will have the player controller in the game. The any other client won't only have the characters, won't have the player controller. And the player controller, if we want to share some information between clients, we have to use the player state, like for example, score and lives and that kind of things. And then you have the input component. And the input component is very particular component that is attached to the player component. You're going to show how it's used in the game private controllers. You decompel this is our play control controller. And we are using the input component. We are setting up saying what actions are we going to be reading from? And those actions are transformed into functions. So if we go to the input, the break settings, let's have a look just quickly at the input. Here input. We see how different inputs are transformed into actions or into access values. So that is the information that the player controller is reading and transforming into different decisions making. And those decisions are going to be transformed into actions for the character. And again, the point and the character is their physical representations in the wall for anything that has a behavior or should be making decisions. And as part of this framework, this additional classes we haven't covered here, but this is the bulk of it, but just things like damage. I will give you as part of it. So there's damage system building. Movement component, for example, to move characters and such. Like the framework is huge, but I'm only talking about the pillars of it. So also in this framework, we should be taking care about ticking and subsystems ticking. It's mainly the way we have to say to update things. So ticking will happen every frame or as you want to set every couple of frames or every group of frames. And it will it can update functionality at that rate. And subsystems are kind of an easy way to, how to say to expand the functionality of gaming instance or game states or these different blocks in the game in the gameplay framework. So you don't have to really know what is happening like when you're narrated from them, you just create a new subsystem and it will have the same life cycle as they have. I'm going to explain the network system anyway. Very quickly. Yeah, it's based on ownership. It can replicate both actors or properties. The replication is based on reliability. You can set the reliability of how things are going to replicate. And you also can generate your own messages. So you can decide what things you want to update when they're changed, but also you can say I can send messages messages. And you can also hook to events when something has changed to do any kind of action. And then well, the compile building and cook processing and and package was already addressed in the engine structure class, but briefly say that that is lead by the real automation tool. And that you have several ways of, for example, cooking. You can select what to cook or you can let the engine decide cook by the fly. So you don't have to cook everything if you, for example, are testing something. You can do cook and apply and it will gather the assets as it's if it's. And all that hooks into the bargain profiling tools that we have. There are a lot of them. Just naming some of them stats. You can check, for example, in the command line, stat, FPS, and you can see how it's performing. But there are a lot of different stats. Console commands the same. For example, if you want to disable something, you can use the console command show. For example, we change a lot of things like you can disable a inclusion and that kind of processes. We also have session session front end where we have a profiler, but we have included the real insights. Again, we have covered that in the engine structure session class. Memory port. It's used for for memory profiling. And LLM is low level memory. That will give us a very accurate vision of the things that are loaded in memory and how we can, like, even the amount of elements that are in memory and the class. So we can track. So the LLM was just a command, right? Was it start LLM? Yeah, but you have to enable it in your build. I see. Yes. But the memory port was the root. The memory, memory of report is it's a common line and it dumps all the memory, like all the actors that are in memory, all the objects, and you can have a look again, but the LLM it's low level and it will be much more accurate than the other one. Profile GPU, we already have seen that. It's, it's a command and will give you. Profile GPU or, you always mix up GPU profile or profile GPU. I think it's profile GPU. And it's not in the suggestions. So profile. Space GPU, I think. Or one. No, maybe not. Let's try. Yeah, profile GPU. One word. One word. Yeah, altogether. So here you can see what's happening in GPU. That's more for the rendering side. And a gauntlet will, once you have a build, you will be able to auto test your, your, your, your build and, and see. Yeah, for example, if the achievements are being assigned, or not, and sets. And I think that's quite all. Just a word about asynchronous, you can create easily. From C++, create new tasks to, to, for example, save the game. So you don't have to do it in the main thread in the game thread. And, yeah, and you can hook delegates and events. The engine has a huge system of delegates and events that also hooks into blueprints. So you can access both from C++ and blueprints to those events. There we cover a life plus plus. That's all I told you. We're. Down, little bit down, not there. Yeah, very much. Oh, yeah. Lifflas Plus is, it's in the engine already. And what you can do with Lifflas Plus is to compile. While you're running, you can do changes. And it will create generate a patch. And it will recompile everything. And you don't have to stop running the editor to make a change in, in your code. And in your code.