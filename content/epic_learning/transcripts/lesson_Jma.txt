We are going to be learning how to broadcast and subscribe in C++ directly to events that are both available in Blueprint and C++. So what's the use case here? Well, in the BPQuestMager we have a complete quest event. And at the moment we are calling it in Blueprints because we didn't know how to do that. But it is possible to move this into the C++ implementation. It's also possible to move the subscription to an event such as the one in QuestMarker on BeginPlay, where we subscribe to the quest updated and refresh the visibility of the back of that. That is also possible in C++. So we're going to do both of them in this module. So first of all, let's do the broadcasting. So we go to questmager.h. We already have our dynamic delegate setup on this class. So we're going to go over to the C++ and in a complete quest implementation, basically we want to be doing what we're doing in Blueprint, the call to complete quest. And that's pretty straightforward. What we do is we have the completed quest dynamic delegate, and we can do a dot operator on this and then just call the broadcast function, which will take just one parameter, which is going to be our quest index. So we don't have to even do that double lookup of the quest index. So it really is just that simple over in the C++ to do broadcasting. Now how about subscribing to events that's slightly more complicated? So we're going to go to the questmarker.h. And what we're going to have to do is we have a look at the Blueprint for the idea of how this works. We're going to have to do the subscription in the BeginPlay function. We're going to have to bind. But what we're binding, we're having to bind a function, something here that is a custom event. But over in C++, that is going to be a function. So we're going to create one, we'll go over to the header file. And we're going to make it private because nothing else needs to have access to this function. So it is going to be what return value, it's going to be a void return value. And we know this because it has to match the signature of our dynamic delegate. And it's going to have to take in the index. So we're going to call it as we have called it over here in the questmarker, the quest updated. And as we said, it is going to take in an int32, which is going to be called the index. And it doesn't matter whether the name matches in this case, because we just need the signature to match. And then we can go ahead and copy the signature to the clipboard and paste it into the bottom of our questmarker CPP. Now that we're here, we need to subscribe to that delegate. And we need to actually call the refresh at the appropriate times. So we've removed our BeginPlay here. So what I'm going to do is I'm going to copy the signature from our quest manager, which is the same signature for that. So it's going to be protected, BeginPlay, we'll go over to questmarker.h. I'm going to put it just above our private section, protected BeginPlay. And I'm going to copy that over into the C++ just above the refresh visibility function. Then in BeginPlay, we are going to call refresh visibility because that's something we want to do right off the bat just to make sure that the visibility is in sync with our quest manager. We're also going to call refresh visibility down here. And finally, we need to subscribe. So how do we do that? Well, first of all, we need to get hold of our quest manager. We can do that with the getquest manager function. Then we've got the arrow key. And because it's public, we have access to that dynamic delegate completed quest. And then on that, we can do add dynamic. Now you notice the auto complete is not entirely correct here. It wants to complete to internal dynamic. But that's because there is actually a macro here in between called ad dynamic that expands and does some extra stuff. Now it would just be add if you weren't using a dynamic delegate and the signature is ever so slightly different, but just you might have to undo that auto complete. Now what you have to do here, what you have to pass as a signature is the object that we are going to call the delegate on. So this and then the actual reference to that function. So it's going to have to be the fully qualified reference, the a quest marker colon colon quest updated. And we can get the reference using the reference of operator, the ampersand like so. And that's going to hook up the delegate to this instance of the object and that particular method. And it's doing that on begin play pretty straightforward, really. And it should be quite familiar to anyone who has used this kind of delegate system in C++ before it's quite standard. And as I said, the only difference between this and a non dynamic delegate is that you're going to be using ad instead of ad dynamic. Let's have a go and compile and see what happens. Now the compilation succeeds, but if I go over to my main map and play, we'll notice that actually it is not succeeding in being subscribed because we don't refresh the visibility. We do update the UI in the top left, but obviously the broadcasting is working and the subscription is not. Now the reason for this is not entirely obvious, but because we're using a dynamic delegate, that means that the function we're calling has to be dynamic. i.e. needs to be a U function. And that is what we're going to do. If we add the U function macro, we don't have to add any kind of accessibility to it, but if you add the U function macro and go ahead and compile, it should work. In some versions of Unreal, this would have given an error when compiling or while playing, but here it does not seem to be doing that. If we go ahead and play now that we have compiled with the U function and go up to this marker, you can see that the marker's visibility has indeed properly refreshed. So things are working as we would expect. Now we can go and remove the redundant code. We no longer need to be subscribing to the event in begin place. Let's remove that and compile and save. Let's also remove the call to completed quest that we do not need in a BP quest manager. Just simplifying that right down. In fact, we don't even need to be overriding a complete quest function here in Blueprint anymore because the C++ version does everything that we might need. So that's it. We have moved the entirety of that event over in to C++. We've been able to create it and declare it. We've been able to subscribe to it and we've been able to broadcast to it entirely in C++. So now you know how to do that.