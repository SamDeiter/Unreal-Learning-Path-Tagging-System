In this video we'll improve our existing camera system by using a player character velocity to drive the movement of the camera component. To make the camera move based upon the player character's velocity, we'll need to modify the existing update camera function. So let's go ahead and do that now. Using the content browser, navigate to content, 2D side scroller BP, Blueprints and open my 2D player character in the Blueprint Editor by double-clicking it. If it's not open already, double-click the update camera function in the functions rollout of the my Blueprint panel on the left hand side. For now, we need to disconnect this existing logic and move it out of the way. We'll reconnect it up later once we're done. All left click on the execution pin of the update camera function to disconnect the wire. Use a marquee selection to select the other nodes and move them out of the way. Before we start adding new logic, we need to create a few new variables. Make the first new variable called movement camera ratio. Make this a vector variable and add it to the camera category of variables. As the name suggests, this variable we use to control the ratio between the velocity of the character and its impact on the movement of the camera. We need to set the default values for this variable, so hit the compile button to compile the Blueprint. Starting over to the details panel on the right hand side, set the default values to 0.15, 0 and 0.1 respectively. This means the ratio will take some input from the x-axis, none from the y-axis, and a little bit from the z-axis. We'll also need another vector variable to store the result of this new calculated position. Create another vector variable called movement camera offset and add it to the camera category as before. We can leave this variable with the default values of 0, 0 and 0. Next, we need to define some sensible maximum values for this offset variable we've created. Create a vector 2D variable and name it movement camera max offset and add it to the camera category. Once again, we need to change the default values so compile the Blueprint. Returning to the details panel and the default value rollout, set the values to x-100 y-50. With our variable setup, it's now time to start generating values using Blueprint logic. We'll begin by generating a value using the character velocity and the movement camera ratio. Select click in an empty space in the graph and locate the get velocity function. Drag from the return value of this function and locate the vector multiplied by vector function. Now we can control left click and drag the movement camera ratio variable into the event graph to get a reference to it. With that done, connect this variable to the other input of the vector multiply node. We're going to need to modify the specific contents of this vector variable so we'll need to break it open to look inside. Drag from the output of the vector multiply node and locate break vector. The next step is to make sure that these values don't exceed our defined offset value in either x or y. Let's start with the x value first. We're looking to turn this x value into another value between our defined minimum and maximum and luckily we have a clamp function for just that purpose. Drag from the x output of the break vector and locate clamp float. We can see that this clamp function requires a minimum and a maximum. So we need to go ahead and generate those values. Control left click drag the movement camera max offset variable into the event graph. Drag from the output and locate break vector 2d. Drag from the x value of this node and locate the float multiplied by float function. For the second input of this new function, make the value minus 1. Now we need to quickly reposition the nodes and then connect the output of this to the min input of the clamp float. I suggest double clicking somewhere on this execution wire to add a re-root node to keep things tidy. With that done, we have the minimum value for our clamp function but we still need to supply the maximum. The max value is simply the raw input from the break vector node in x. So drag another wire from the x output of the break vector 2d function and connect it to the max input of the clamp function. Once again, I suggest using a re-root node to prevent the wires from overlapping and keep everything readable. So that takes care of x. Now we need to do something simpler for y. Make sure we have enough space and then mark you select the nodes we've been working on and press Ctrl C and Ctrl V to copy them as we will need exactly the same functions again. Position this collection of nodes somewhere underneath the previous ones and connect them to the z output of the break vector. The primary change we need to make is instead of using the x output of the break vector 2d, we want to use the y output of this vector. We can save some time and not have to re-rear any execution wires by holding down Ctrl and left-click dragging the wire into the y input. Feel free to adjust your re-root nodes to keep everything neat and tidy. Now we need to re-create a new vector with this derived data. Drag from the return value of the clamp for the x vector and locate make vector. Note that by doing it this way, it's automatically connected to the x input of the make vector. Now we just need to do the same thing for the z vector. Connect the return value from the second clamp float into the z input of the make vector. And with that, we're almost there. This new vector defines where we'd like the camera to be. However, if we just fed this new value in directly, the camera would snap awkwardly to that position and that would look and more importantly feel bad. So to fix that, we use a LUP node to gradually move to this new position. First, we'll need a reference to the camera's current position movement offset. Luckily we already have that stored in the movement camera offset variable. So let's drag that variable from the my blueprint panel into the event graph by control left click dragging it. Drag from the output of this node and locate LUP vector. Note that by doing it in this manner, it's automatically connected to the a input, which is what we want. Now move the nodes around a little so the wires aren't going to overlap and connect the return value from our make vector earlier to the B input of the LUP vector. Now we just need to provide the alpha value to drive the rate of change. We'll do this using game time and our existing player camera LUP speed variable. Drag a reference to the player camera LUP speed variable into the event graph by control left click dragging it from the my blueprint panel. Drag from this variable and locate float multiplied by float right click on an empty spot on the graph near this and locate get world delta seconds. Connect the return value of this function to the secondary input of the multiply function. Now we can connect the output of this multiply node to the alpha input of the LUP node. Now the return value of this LUP will give us the final movement camera offset variable. To do this, we need to use a set node and we can do this by alt left click dragging the movement camera offset variable into the event graph from the my blueprint panel. Connect the return value from the LUP function to the input node of the set function. Now this new part of the logic is complete. However, we still need to make it work with the existing logic we disconnected at the start. Let's return to our existing logic for the position of the camera. All this does is takes the current position and adds the player camera offset vector. This vector is generated by player input on the stick and the keyboard and nothing else. So we've already done all the hard work in the previous blueprint logic. All we need to do now is add the movement camera offset vector as well. Click the plus sign next to the add pin on the add node. Control left click drag the movement camera offset into the graph. Connect this to the new input of the add node. There's one last thing that's pretty important. Currently none of this logic is connected to the execution pin for the function, so it'll never actually fire. Oops. So let's take care of that. We want to fire our new logic and the existing logic in a sequence, and our real engine has a node for that. Locate the update camera function execution pin and hold S and left click in some empty space nearby in the graph. This will add a sequence node. First, connect the execution pin from update camera to the input execution of the sequence node. I suggest zooming out a little and using a marquee select to reposition our blocks of code. This will allow us to neatly connect to the sequence node. Let's handle our new logic first. We want to connect the execution pin from the set movement camera offset node, so let's move that up a little higher and connect it to the first output of the sequence node. I prefer to have all of my gameplay logic to the left of the function node, but that's up to you. Now we need to do is connect the second block of code to the second output execution pin of the sequence. And with that, we're done. Compile and save the blueprint before closing the blueprint editor window and returning to the main editor window. Let's review the impact of the changes we've just made in a play and editor session. Hit the play button from the toolbar to start a new play and editor session. Note that we can still manipulate the camera manually as before, either by using the right thumbstick on a gamepad or jk and l on the keyboard. But also, if we let the camera settle into the default position and move the character left and right, we can see the character move a little bit further ahead in the facing direction by itself. Pretty cool. Note that we can also do both. We can manually manipulate the camera, and if we move the player as well, both of those vectors combine to give a nice smooth output. That's all we need to see for now, so we can end the play and editor session and return to the editor. In this video, we added additional blueprint logic to the player character to enhance the look and feel of the camera by altering its position dependent upon the current velocity of the character. We used the player movement component to determine this velocity and applied a specific offset to the camera in the appropriate direction as the character moved around. We finished up by reviewing the results of these changes in a play and editor session.