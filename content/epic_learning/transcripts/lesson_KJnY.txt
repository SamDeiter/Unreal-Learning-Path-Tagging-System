With our images ready to go, let's configure our project settings to support the targeted hardware and software that we have built to. In this module, we're going to set up the required platform that our app supports as well as the version it was tested on. We'll also modify a setting that allows the app to run on non-dev locked devices. Open the HoloLens settings inside of project settings. During at the top, the OS that we are targeting is Windows.Holographic, and we need to indicate which versions are supported. Because we built and tested this app for Windows SDK 10.0.18362.0, let's set that to both the minimum supported and the max tested. If you have other SDKs inside your machine, make sure this is set properly. Next, we don't want to build for the emulator and only for the device. Under the package settings, we need to recreate a new signing certificate that contains our package identity publisher name found inside of our account in the Microsoft Partner Center. To use this info, scroll back up to the description settings. When we first set up the project, we set the company distinguished name to CN equals HoloLens. Now, because we want to package our app for uploading to the store, we need to add our Windows publisher ID. Change the company distinguished name to the value provided in product identity. It should begin with CN equals. Back in the HoloLens settings, generate a new certificate and give it a good password. Scroll down to localized resources. When we are preparing our app for submission, the system will allow us to upload different information based on the language. If your app is built to be localized in additional languages, set that information here. It will also be added to the manifest and used during the submission process. At the bottom of the package settings, open the advanced options. Setting use retail Windows store environment to true will allow non-dev locked devices to run the app. Let's set that to true. In this module, we configured our project settings to specify the minimum and maximum-supported SDKs that we are building to. We prepared the project to be runnable on a retail Windows store environment and modified our company details to fit the requirements of the store. In the next module, we will set the final package settings for our build targeting the HoloLens 2 and run the final app through an automated test allowing us to upload it to the Microsoft store.