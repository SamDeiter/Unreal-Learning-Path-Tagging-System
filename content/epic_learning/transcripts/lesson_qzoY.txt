We've seen a few examples of working with static mesh class to gather and analyze information regarding LEDs and primitive counts. Once a static mesh is added to a level, a static mesh actor is created. Each static mesh actor is a long list of attributes that will be modified in editor, allowing for a significant amount of variety. In fact, you can even change the static mesh associated with the actor. In this module, we're going to take our investigation of static mesh as one step further by considering how many times each asset is instance in our test level. An asset may have a high triangle count, but if it only occurs a few times in a level, it may not have as much overall impact on performance as a simpler asset with many instances. If an optimization pass is required, it can be helpful to know which assets to focus on. We will start by getting the number of instances of each static mesh in the level, then we will multiply this number by the assets median triangle count. We're going to begin by revisiting the documentation for unreal.editorActorSubsystem. This is a class we looked at earlier in this course. What it's going to do is give us some common functionality associated with actors, and we'll be using the getAllLevelActors method, which will return an array of all the actors in the level. We'll be looking for a specific kind of actor, the static mesh actor, so we can use get class on object base to filter by class. Once we have the static mesh actor, we can access the static mesh component. If we go down to the bottom of the documentation on static mesh actor, there is a property that will return the static mesh component. Setic mesh component is associated with all of the information that you would find in the details panel with a static mesh selected. And once we've got the static mesh component, we can find which static mesh is affiliated with the static mesh actor by using this static mesh property. And from there, we're just going to tap into the code that we already wrote, and the last module to gather the polycounts. Once we have the polycounts, we can go ahead and multiply that by the number of instances, and we'll have the information that we're looking for. Let's go ahead and start with a little code here. I've got a function already started called getStaticMeshInstanceCounts. The first thing I want to do is get all of my level actors. So we'll head over to editor actor subsystem. And I only need this code to run once. So I am not going to instance this class, or I'm not going to make one instance to reuse. So for now, I'm just going to grab the class from each level actor and print it. And print it. We will refer back to object base. We can forget class. We'll save this. Head over to the editor. We'll reload. And the printout here is going to give us the class. We can see those static mesh actors. I mean, I'm sure all kinds of other things here, but mostly static mesh actors. But this isn't quite as easy to sort through. Fortunately, if we just add. Get name to the back of that. It'll give us exactly what we're looking for. So we're we're trying to track down everything that's a static mesh actor. We'll set up a logic here. So we filtered for our static mesh actors. The next thing that we need is the component. So we can see there is a property. Static mesh component. And once we've got the static mesh component. We can get the static mesh. So we'll just use the property here. And just to make sure this makes sense, let's go ahead and print out our static mesh. And we'll just use the property here. All right, that all looks pretty good to me. Now we need a way to keep track of how many times each actor occurs in the scene. Something to create an empty list called static mesh actors. And then I'll just add the name of the static mesh actor as I come to it into this list. Let's take a look at a way to figure out how many times each actor occurs in our static mesh actors list here. So essentially all we're doing here is we're going through and for every static mesh actor. We're going to print the name of the static mesh actor and then the number of times that item occurs in this list. And I don't really want to see multiples here. So I'm going to make another list up here. So this way we should only see that each static mesh actor should only occur once here in our final printout. So we're working pretty well. So we'll go ahead and package this data up into a table. So we'll add a new empty list up here to hang on to these tuples. Let's store this in a variable. Go ahead and append that data to our static mesh actor counts list. And then once again, just to confirm this all makes sense, we'll print it out. So the next step in this process is going to be to sort this list by the second element of this to do that. I'm going to grab a little bit of code here that you can just copy if you'd like. So this is just going to sort by index one, the second element of the two player. And then it's going to reverse it. So it's basically going to put the highest value at the top of the list. We can take a look at what the output is going to be here. So down here at the bottom, we can see everything that's only got one instance. And then we open up and up and finally we have the items that are contributing the most instances, not necessarily high poly objects. This shape cube is probably just some default thing, but you know this mesh here might have a few polygons to consider. And there are 182 of them so could adequately. So this function here get static mesh instance counts. The one that we just wrote is going to give us the number of times every static mesh occurs in our scene. And then this function up here get static mesh, LOD data is going to give us the try counts per LED for each static mesh. So essentially what we're going to do is just combine these these two functions here to get the information that we're looking for, which is again the overall contribution of triangles per static mesh for the level. So the first thing I need to do is essentially return the static mesh try count data. And I probably need to figure out a way to find an average value that I can use that makes sense based on the level. So I'm going to hop over to love a real quick. And some of the factors that you might take an consideration are this is sort of a medium distance layout here. So presumably some of these things might be in a lower LOD and some of these things might be in a higher LOD. If we were in a wide open space of value or something, there might be a lot more items that would be at the lower end of the LOD scale. And if we were in an interior space, like a shop or something, we might expect to see a lot more stuff that was a higher end. Let's take a look one more time at the the output of this function so that we can remind ourselves what it looks like. So this middle list here is what we're looking at. So for this asset, this grass asset LOD zero is 24 triangles and then the last is four triangles. So what what we're trying to figure out is like based on what the scene looks like, what's the right number to use in here. So we can actually hop over to a visualizer here called level of detail coloration. And if we turn it on, what you'll see is we've got three or four different colors here. White means it's an LOD zero. So there's no LOD happening in terms of optimization. I think red is LOD one and if actually we just just sort of look at the stairs here. So we're up close to it. It's going to be white and then as we kind of back away, it switches to red and then to green and eventually we go to blue. So things that are blue, you can see over here are being pretty heavily optimized and that's great, but there's still a lot of stuff in here that doesn't have any LEDs at all. So this might actually have been a good thing to have on when I flip the switch on to get static mesh data. I didn't actually save those changes to the static mesh, which was just adding an LOD preset, this large prop preset. But let's go ahead and do that and we'll just see if there's a noticeable change here. There's a lot of things here that are still in LOD zero and we probably just double check one just to confirm that it is actually picked up the LOD setting. So that's just the whatever the value is for the LOD and we're going to we're still a little bit, but what we can see here is a lot of stuff is still in LOD zero. And then maybe 50 50 split between LOD one and two and then there's a few things here that are like three. So we're just going to go ahead and use LOD one. I think that's that's probably a reasonable approximation. And again, the reason that we have to do this is because we don't know necessarily where in the level the player is going to be standing and looking around. So we can't just say everything from this perspective. That's the values that we're going to use. So we have to sort of find a general solution for that. Back over and we're going to comment some of this stuff out. We don't really need to print anything from this function anymore, but we do need to pass out some data regarding the static mesh try count. So what I'm going to do is we'll just package this up into a nice little tuple. And then I need to wait a few track of it. And then I need to wait a few track of it. So again, we're just creating a tuple here that's going to have the static mesh name. And then the try count of LOD one. And then we are appending that tuple into a list. And at the end of this whole process, once you've gathered all the data for each static mesh, we're going to return static mesh LED data. So just to make sure that this is all working as it should. Let's go ahead and print this out. So I'm going to keep this print statement here. I'll just add a new one. All right, we'll do a reload. And we are getting an error. This is an index out of range error. It's unhappy that we're speaking to the first element in this list. So let's just do a very quick little debug here. And we'll see if we can figure out which one of these is causing the problem. We're just going to print the static mesh name. This is the line that it's unhappy about. It says that this is out of range. So we'll just run that again. And we can see we've got this asset here. We just punch that in. Take a look. That's got LEDs. We'll look at this one here. That's not a thing. There we go. All right. So this asset doesn't actually have any LEDs. And we can just confirm that it does have an LED group. Right. So small prop is the LED group. So we didn't apply our preset to it. And apparently, in this case, it's only got one LED. So we are going to have to make this very simple. Obviously, you would probably want a more robust solution. But this will work for now. Let's try that. And turn off the warnings, definitely with the messages and ears. And where we have all of our data, nicely organized. So we've got the asset name here. The number of instances. And then we've also got down here, the number of triangles for LED one for all the things that have an LED one. So we go ahead and put this all together. First thing we'll do is comment out. These two prints. We're going to make a nested for loop. So we're going to make a nested for loop. So let's go on here is I'm iterating over everything in the static my sector, councilist. And then I'm iterating over everything in LED data. And I'm comparing the first element in each one, which is the static my name. Once I can find the match, I can go ahead and multiply the values in the second element there. I think I'm actually going to make this into a tool that has the name of the static mesh. As well as the overall aggregate try count for its LED one across the level. And then we can go ahead and create an empty list to store this data and sorted. And we'll grab our sorting code here. And then we'll take a look at the output. All right, and if we scroll to the very top, this list, what we can learn is this asset, this Nordic B truck is contributing 100,000 triangles. And this other, that looks like another cliff piece is just a little bit less than that. And so on down the line here. So if you needed to optimize, you could take a look at these assets here, kind of focus your efforts at the top of this list. And you could expect to get the biggest bang for your bug overall. If you can work across static meshes, static mesh actors and static mesh components, you can access quite a bit of valuable information. And this module we combined information on LED triangle counts with static mesh actor counts to generate actionable optimization data. And the next module will take a look at a simple method to track down inherited functionality that may not be included in a class's documentation.