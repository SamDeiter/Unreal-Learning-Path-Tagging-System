In this video, we'll give the enemy AI the ability to fight back with the projectile attack. We'll start by creating the projectile blueprint and then work towards giving the AI the ability to detect the player and fire the projectile at the appropriate time. The first step will be to create a new blueprint for the projectile itself. In the Content Browser, navigate to Content 2D Size Crawler BP, Blueprints, Enemies. Select click in the Content Browser and in the Create Basic Assets section, select Blueprint class. From the Common Classes roll out, select Actor. Name this newly created Blueprint Enemy Projectile. Double-click on a Blueprint to open the Blueprint Editor. Let's begin by adding a Collision Volume component. As that's the object we want to use as the parent to attach our visual objects to. In Components tab on the left-hand side, hit the Add Component button and locate Box Collision. We can leave the default name of Box. In the Hierarchy, drag this new box over the default scene root to replace it as the parent or root object. With the box still selected, hit Add Component again and locate Paper Flipbook. Once again, the default name of Paper Flipbook is fine so we can press Enter. With the Flipbook selected, move over to the Details panel on the right-hand side and locate the Sprite rollout. For the Source Flipbook, use the drop-down and locate Flipbook underscore Energy Bolt. With the visual now in place, we can adjust our Collision Box to match. First, let's adjust our Viewport to make it easier to visualize for a 2D perspective. Use the Viewport Control to select the right Viewport. Once that's done, select the Unlit View Mode. We can now zoom in using the mouse wheel on the Viewport. In the Components panel, select the Box Collision. Moving over to the Details panel in the right-hand side, locate the Shape Rollout and then locate the Box Extent Options. Set these values to X15Y10Z6. We can see that our Box Collision now matches the visual. To get started, we can make use of some of the built-in logic for Projectile Movement, much like the Character Movement component. This is helpfully called the Projectile Movement class. Return to the Components panel and press the Add Component button and locate Projectile Movement. Press Enter to accept the default name of Projectile Movement. This component can do a lot of fancy things, but for our simple demonstration, we only need to make one change, Disabling Graffiti. With the Projectile Movement component still selected, locate Projectile Graffiti in the Details panel and change the value from 1 to 0. With all the components now set up, it's time to start working on the logic. Let's begin with the two main ways the Projectile will be destroyed. Those are after a certain amount of time alive, and secondly, if the Projectile collides with the Player Character. Let's start with the overall time to live. Open the Event Graph and drag from the Event Begin Play node and locate Set Timer by Event. Let's clear ourselves some space and marquee select the other two nodes and delete them as we don't need them for now. Let's work our way through the requirements for the Set Timer by Event function. Let's begin by creating the event that will occur when this time are complete. Drag from the Event node on the function and locate Custom Event. Main this custom function on Time Expired. Drag from the execution pin of this new custom event and locate Destroy Actor. Note that by default the target here is set to Self. To when this event is fired, the Actor will destroy itself. Next, let's configure a Time Value for this timer event. Drag from the Time Input of the Set Timer by Event function and select Promote to Variable. Name this variable Time Alive. We need to set a default value for this variable, so compile the Blueprint so that we can do that. Moving over to the Details panel on the right-hand side, set the default value of Time Alive to 3. That's everything we need for the basics here. But it's worth taking the time to move around the nodes and use re-root nodes when necessary to keep everything neat and tidy. Now let's tackle collision with the Player Character. We'll need to use the Event Actor Begin Overlap to check for this. So right-click in an empty space in the Event Graph and locate that event. The first thing we want to do with this Event Overlap is check if the thing we've overlap with is the Player Character. To do that, we'll use a Cast. Drag from the other Actor Output of the Event and locate Cast to My 2D Player Character. Now that we're sure we're dealing with the Player, we want to do a few things. Send some damage and destroy the projectile as before. Drag from the main execution pin of the Cast node and locate Apply Damage. Let's work our way through the Inputs for the Apply Damage function. We want the Player Character to be the one receiving this damage. So drag from the As My 2D Player Character Output node of the Cast and connect it to the Damage Actor Input of the Apply Damage function. While we're here, let's set up a Damage Value for the projectile that we can use later while we work on receiving the damage from the Player Character's perspective. Drag from the base Damage Input and locate Promote to Variable. Name this new variable Damage. Compile the Blueprint and set the default value of this variable to 25. Now let's handle the Instigator and Damage Corsa. If we hover over the Input for the Instigator, we can see that it's looking for the controller that was responsible for this damage. Luckily, Unreal Engine makes it easy for us to get to this with just two nodes. First, right-click in an empty space and locate Get Instigator. Drag from the Return Value of this node and locate Get Controller. Connect the Return Value of this Get Controller node to the Event Instigator Input. Next, the Damage Corsa Input, which, if we look at the Tooltip, refers to the Actor that calls the Damage. Drag another wire from the Get Instigator Return Value and connect it directly to the Damage Corsa. I suggest using some re-root nodes to keep these paths readable. Finally, we have the option to select a Damage Type class. However, that's beyond the scope of this project, so we can just leave it blank. With the Damage correctly applied to our Player Character, there's only one thing left to do. Destroy the projectile. We can do that in exactly the same manner as before by dragging from the execution pin of the Applied Damage function and locating Destroy Actor. The only thing left to do is tidy up the nodes a little bit so everything is nice and easy to read. And with that, we're done with the core functionality of the projectile itself. We can compile, save, and close the Blueprint Editor. Before making the AI actually fire the projectile, let's handle detection of the Player Character. Primarily, we want to know if the Player Character is in front of the AI and within a certain distance. Let's create a collision volume, attach the AI Character. We can use to check that. In the Content Browser, navigate to Content, 2D sidescroller BP, Blueprints, Enemies, and open the Enemy Character Blueprint. Select the Viewport tab so that we can visualize these new components as we add them. Select the Capture Component from the Components panel on the left-hand side. With the Capture Component still selected in the Component hierarchy, click Add Component and locate Box Collision. Name this new Box Collision Detection Range. With this Collision Component still selected, move over to the Details panel on the right-hand side and locate the Box Extents section within the shape rollout. Set these values to X700Y32Z32. Next, move to the Transform rollout and set the location to X180Y0Z0. Finally, adjust the scale to X0.25Y1.0Z1.0. Now, if we take a quick look at the Viewport, we can see the volume extending out in front of the AI Character. Later, we'll use this volume to drive our Blueprint logic to decide if we should attack or not. While Unreal Engine has a robust set of AI sensing systems, these aren't the focus of the course, and have far more functionality than we actually require for a simple 2D Action Platformer. However, if there is something that interests you, I suggest taking a look at the Introduction to AI with Blueprint's course, which deals with the sensing systems in much greater detail. For this project, though, we'll keep it simple and use Overlapse when the Collision volume you've just created to drive a new Event Dispatcher containing the relevant information. Let's begin by creating Events for the Overlapse with this new volume. Move to the Event Graph tab, and with the Detection component still selected, locate the Events rollout in the Details panel on the right-hand side, and click the plus icon next to On Component Begin Overlap and On Component End Overlap to create these events in the graph. When either of these events occur, we need to know if the Overlaped Actor was the Player Character, and we can do that easily using a Cast. Drag from the Other Actor Output of the On Component Begin Overlap event and locate Cast to My 2D Player Character. If we can successfully cast to the Player Character, that means the Overlap event contained the Player, and we'll need to pass this message on as a sort of basic Detection event. We'll manage this by using a new Event Dispatcher. In the My Blueprint panel on the left-hand side, click the plus in the Event Dispatcher section to create a new Event Dispatcher. Name this new Dispatcher on Enemy Sense Change. Now, let's configure the relevant information this Event Dispatcher will need to pass on. First, let's add a reference to our Detected Player Character. With the Event Dispatcher still selected, move to the Details panel on the right-hand side, and press the plus icon next to the Inputs rollout. From the Type drop-down, locate My 2D Player Character and name the Input Player. We also need a variable to store if the player has actually been detected or not. That is to say, have they just entered our Detection Area or have they just left the Detection Area? We'll use a Boolean variable to store this. Hit the plus icon in the Inputs rollout again and set the type to Boolean. Name this Input, Detected. And with that, a new Event Dispatcher is ready to use. So let's bring it into the graph. Drag the On Enemy Sense Change Event Dispatcher from the My Blueprint panel into the graph and select Call. Reposition the node and connect the execution pin from the Cast node into the Event Dispatcher. As this particular logic refers to the Begin Overlapped Event, it means that the Player Character has just entered the Detection Volume. So check the box next to the Detected Input to set the Boolean to true. To save ourselves a little time, we can copy and paste these two nodes for the End Overlapped Event. Connect the other Actor Output to the Object Input of the Cast node. Make sure to also connect the execution pin. Moving over to the Dispatcher Call, change the Detected Boolean from true to false. The last thing we need to do for both of these paths is connect the Output from the Cast node. Connect the Output from the Cast node to the Player Input on the Event Dispatcher. And with that, we've created our basic sensing volume for the AI. The next step is to have the AI controller decide what to do with this information. But for now, we can return to the main Editor window. In this video, we took our first steps to what's having the AI Character identified a player as a valid target and fire a projectile at them. We began with the creation of a new Blueprint Actor to store the logic of the projectile itself. Adding several components to manage visual and collision volumes. We also added a projectile movement component and disabled the gravity. We then moved on to creating a basic Sense System for the AI Character using a combination of a specifically shaped Box Collision component and an Event Dispatcher capable of passing on relevant information from the overlap events generated. We finished up by configuring specific calls of our new Event Dispatcher ready to distribute this key information to any Actor bound to these events.