In this lesson, we'll review arrays in Unreal Engine. We'll observe the properties of an array and show a couple of examples of how arrays are accessed, especially in our block game. And we'll look at areas where arrays can make programming more simple or easier and less error prone. We'll demonstrate the way that we used an array in the block game and explain how arrays are indexed and very importantly, what an out-of-bounds access to an array means. Let's define what an array is. An array is a container for things or variables or items. And it's on a way to refer to multiple items with the same name, but use an index into the array to get or access each one of the items. The index starts at zero and ends at the number of items in the array minus one or the length of the array minus one. I'm going to demonstrate arrays using a block I built in the block game specifically for that purpose. What this block does is it adds a child of each of the blocks in the all blocks array to the new array and displays the index of that item above the block. So let's put some terms on the array. This array starts at index zero with a BP base block in that slot. The index is then incremented by one for each new item added to the array. And on the last item, the index is six. When we talk about this array, we say it's starting at the zero with index and ends at the sixth index and the length of this array is length seven. So the last valid index is the length minus one, which would be six. If we add an item to this array, we then have eight items in the array zero through seven. And the last index is now seven. On Unreal Engine, arrays can be expanded or contracted. You can add items to them or take items away. This is not the case in all computer languages. Java, for example, has arrays that are set length when they're declared or instantiated. Java does have an array list, which can work just like the arrays in Unreal Engine. In general terms, all of the items in the array have to be the same type. So for example, if we had an array of ints, we could only put integers into that array. However, if you're using inheritance, all of the things in the array can have the same parent class, but they can be different child classes. And that's how we're using this with the BP base block. Referring back to our example, we have the BP base block as the parent class for all of these child classes. So we can store any of the child classes in the array. There are numerous nodes to work with arrays in Unreal. The most common is to get an item from an array. And for that, you use the get node. You provide the index of the item into the array. And Unreal Engine returns a copy of that item from the array. Another version of the get node returns a reference to the item in the array. In this case, you can change the item by using one of the set nodes for that particular item type. If you want to change the array at runtime, you can use the add node. The add node takes the array that you're adding an item to, the item that you're adding, and then it returns the index of the newly added item. The new item is added at the end of the array. To set something in an array, you provide the array, the index that you want to set, and the item that you want to set it to. This will replace the item at that index. If the index is specified past the end of the array, or past the end of the length of the array, you can check the size to fit option and expand the array. If you don't have the size to fit option checked, it won't expand the array and adding the item will fail. You'll also get an error in the output log. To remove an item from an array, you can either use the remove at a specified index node, or you can furnish the item that you want to remove and it will be removed from the array. It's important to note that there are side effects from removing an item from an array. To demonstrate the side effects of removing an item from an array, we use our previous example. I'm going to remove the third item from the array, or actually the fourth, but the item with the third index, with the index of three. What's going to happen is all of the items past that index will move down one index. They won't visually move to a new position, but their indexes will change. I'm going to right-click on the item at index three and you'll see that index three was removed and the one just passed it, the item just passed it, became index three. The one passed that became four, five, and six. Now our array is now size seven or length seven. If we remove the index, the item at index three, our array shrinks again. All of the items move down one to fill in the gap. That doesn't happen visually here because these blocks aren't moving, but it does remove the item and the index, and it doesn't leave a hole in the array, if you will. It moves all of the items down. That's a very important side effect to keep in mind if you remove something from an array. Now it's really important to note that the shifting of these indexes can cause us to refer to the wrong block in our case. For example, this purple block was previously index five, but the result of removing the two blocks before it has moved it down to index three. Now if we were still referring to index five, we would be getting the teal block instead. Another example of the result of this side effect of removing an item from inside an array. A very common operation on arrays is to iterate through the array or do something to each item in the array. For that we use a four each loop. A four each loop takes the array as its parameter and then it has a loop body. In the loop body, it gets each of the elements of the array one after the other. It also tells you what index that item is in the array. In this code that's demonstrated here, we just print out the index and the item value using the print string node. After the loop body runs, the completed node runs and it prints finished. In the block game, we use an array to keep track of all of the actors that are a child class of the BP base block. By using an array like this, we can keep track of the current block by using an index into that array. We don't need separate variables to represent every possible type of block. This makes adding blocks to the game very easy and it also makes changing the block straight forward as we can use addition and subtraction to keep track of the current index. Let's take a look at that code. Let's look at the all blocks array first. The all blocks array has a BP base block as the type. More accurately, it's a BP base block class reference. What that means is that all classes that extend BP base block can now be placed into the all blocks array and we have several of them here. In the begin play event, we access the all blocks array and call the last index node and save that last index to the variable last block index. We use that when we change the current block that we're working with. Let's take a look at that code. We have two actions. Select previous block and select the next block. The select previous block takes the current block index, which is by default set to zero and subtracts one from it. Let's say we just started the game and we did the input actions select previous block. We reduce current block index by one, which makes its value negative one. We check the value is negative one greater than or equal to zero. It is not. So the false condition is run. We get that value from the last block index and we set the current block index to that. We then retrieve from the all blocks array, the current block index, which is now the last block index and we set the current block to that, which is the last block in the all blocks array. Then we set the preview block. Now, let's take a look at what would happen. If say, for example, we've got 11 blocks in our all blocks array, the last index is 10 and the current block index were equal to 10. We select the previous block. 10 minus one is nine. One is greater than or equal to zero. So the true wire runs. We access the current block index, which is now nine and we retrieve the block right before that and set the current block to that. And again, we would go backwards, select if we were selecting the previous block to 8, 7, 6, 5, 4, 3, 2, 1, and so on. Finally when we got to zero, if we tried to select the previous block again, we would access the last block because this code would change the value of the current block index to the valid last block index. And it's important that we do that because we avoid doing it out of bounds access on the all blocks array. There is no index at negative one. Let's take a quick look at how the select next block works, which is very similar to this, but going in the opposite direction. Let's say we start the game, current block index is set to zero and we fire the select next block input action. We add one to current block index. We then ask is one less than or equal to the last block index, which in our cases 10 and in fact, it is. So the true wire runs. We get the current block index, which is now one and we retrieve the block at the first index from the all blocks array, set the current block and then set the preview block. This continues if we keep selecting the next block, adding one to it. When down through the list, we get to 10. Finally we add one to the current block index. It now equals 11. 11 is not less than or equal to the last block index. So the false pin runs and the current block index is wrapped around and set to zero. Again, avoiding an out of bounds access to this array because there is no index at 11. Now, if you do have an out of bounds access in Unreal Engine, your game will not crash or stop working, but you will get an error in your output log. Other languages, Java, for example, will throw an exception when you have an out of bounds access and it's never good for them to have an out of bounds array access, even if the language you're using does not force your program to crash. It's not good programming. These are the three main documentation links for array use in Unreal Engine. I suggest you take a look at them because there's many more nodes available for arrays and a lot of information on these documentation pages. In this module, we discuss the properties of an array and we showed an example of how to access an array. We identified an area where arrays can simplify programming, such as with our all blocks array. And by doing that, we demonstrated the use of the all blocks array in the block game. We also talked about what an out of bounds access would mean and how to avoid them.