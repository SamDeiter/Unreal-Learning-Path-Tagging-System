We see a lot of users populate in their Unreal Engine scenes with photogrammetry models. The Quixel Asset Library alone contains more than 16,000 realistic 3D photogrammetry assets that can quickly help to bring 3D environments to life. User-generated photogrammetry assets moving from reality capture to Unreal Engine is a popular pipeline path. Navigate to the zip file contents once more and locate the .fbx file and the two JPEG images. We'll start off using the default import process in Unreal Engine to pull our photogrammetry mesh in. Click on the Add button. Navigate to Import to Fortslash Game. Locate the fbx file and click Open. Once the fbx import options window opens, click on the Reset to Defaults button at the top right hand portion of the import window. Then click the Import All button. If we look in our content browser, we will see that the fbx file has been brought in as a textured static mesh actor. The two textures are present and you should see one material. If you plan to import large-scale photogrammetry models comprised of many segmented files, I'd recommend checking out the Blueprint utility widget photogrammetry importer. We have linked to this on the course module page and it provides users with a way to help simplify the process of importing and preparing large-scale photogrammetry imports for Unreal Engine 5 projects. It's built using a combination of Blueprints and Python code and can be used as a starting point to develop your own pipeline import automations. Back to the content browser, select the static mesh actor and drag it into the 3D viewport, then locate it in the Outliner. Switch over to the Details tab and set its position to 0000 on each axis. With the photogrammetry sample in our scene, we can start to look at what we can do to clean up some of the problematic areas on the mesh. In many situations, users will use a range of tools to clean and, if necessary, retapologize photogrammetry for use in real-time engines. With the arrival of the modeling tools editor mode, Unreal Engine users have a plethora of tools at their disposal to carry out many of the cleaning and optimization operations that would typically be done in external packages. We will now quickly explore a few parts of the modeling tools editor mode. When you start a project with some templates, the model in tools editor mode will be active. Navigate to the plugins window, type in Modeling with 1L and ensure that the modeling tools editor mode plugin is enabled. Activate it if it isn't, restart the project, and you should now see the modeling mode option in the top left-hand drop-down list. Switch now from the selection mode to the modeling mode. If we navigate around the mesh, we can see that the photogrammetry representation of foliage on this sample is blocky and doesn't fit in well with the architectural mesh. Let's now look at cleaning this up, firstly create a box from the modeling mode shapes section. This new box can snap onto the normal orientations of existing meshes within your scene. Left-click to snap the new box onto the edge of the building, then click accept. If the box selected activates the poly-ed tool in the poly model section. We can now select individual faces on the box and manipulate the position, rotation and scale of each polygon with the coloured helpers. Here I'm selecting individual polygons and extruding these faces to build up a mesh volume to encapsulate areas of foliage to remove. Once the shape covers the foliage we want to remove, click the accept button. At this point save the scene. We can now select the photogrammetry mesh, followed by the new mesh we've just created. The order in which we select the meshes will be important in the next step. Click on the mesh ball tool in the poly model section. In the mesh ball options make sure that the boolean operation is set to different A-B. A-B in the first mesh selected, B referring to the second mesh. Click accept and the resulting mesh should appear with the unwanted areas removed. The mesh cut tool can also be used in a similar way, except here we can specify to keep both meshes, allowing users to split photogrammetry meshes up into smaller pieces. Here's a quickly assembled example scene that comprises of four separated cuts from the original photogrammetry sample mesh. There are also a few quicksle assets in the scene for context. This configuration took approximately 15 minutes to assemble, and this could act as a good starting position to tailor a more detailed scene with higher resolution assets, decals, particle effects and more. The patent tool was used to replicate multiple building levels. Here we can set offsets for the end translations of our reference model, the number that we want to replicate by, and here we are able to specify that we want instant static meshes saved as separate actors in the scene. For the inverted side, use the mirror tool, and specify that new geometry should be created. The same patent approach has been taken for the stairs and the connecting geometry here. Modulizing your own photogrammetry meshes in a similar way allows for the creation of building blocks that can rapidly be configured together to form more complex arrangements, whilst also reducing the resource overheads as we are reusing single assets multiple times as instances. This concludes our photogrammetry module. Next up, a quick summary of what we've learnt and what we could do next.