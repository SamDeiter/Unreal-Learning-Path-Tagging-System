The SaveGameObject is a custom object inside of Unreal Engine, and it allows us to easily store and load data, and we can run some custom Blueprint code if we'd like to. Let's go ahead and take a look at that SaveGameObject and how we might start working with it. The first thing we're going to want to do is create a SaveGameObject. So inside of our Content Browser, we can right-click and choose Blueprint class. Now we're going to want the SaveGame as the parent class so we can search for it under all classes here, and we can choose SaveGame as the parent class. I'm going to select it, and I'll name our SaveGameObject SG underscore basic, and this is our SaveGame that we're going to be working with. Let's go ahead and double-click to open it up, and you'll notice the basically out of Blueprint because this is a Blueprint. The SaveGameObject, the SaveGameBlueprint, is a special class that allows us to easily save and load variables that we have assigned and set up inside of here. So I can go ahead and create a new variable, and I can name this PlayerHealth, for example. And if I want to save out this SaveGameObject, it's going to save out the current value that PlayerHealth is set to. Now I might want to change this to something else. You can see here we have our basic types, and that's something important to note. I could change this to Integer, and my PlayerHealth would save out an Integer, maybe it's set to 50, for example. But if I was to change this to something else, let's say for example I looked for Character, and I wanted to save this out as a reference to a Character I have in my level, maybe my main Character, and then store all of his information. It's not going to work. That's going to be an Object Reference, it's going to be an InstanceObject, or something that physically exists inside of our world, and it's not going to be able to save that out. We can only save out our basic types. We can't save out things like sets and maps in arrays, as long as they contain those basic types, but we can't save out actual instances of objects, and that's important to note. Save out the data, not the actual item itself. Now, because this is a Blueprint, we have the ability to work with Blueprints inside of it. For example, we could create custom events, or there are nodes specifically designed to work with Blueprints. Let's take a look at some of those. If I search for SaveGame in my Blueprint menu, we can find we have a couple of nodes here. We have the ability to create a SaveGame using the Create SaveGameObject node. After it's created, once we're working with it and we're done and we want to save it, we can save the game to slot. Or if we want to load it up, once we've started our project, we have the load game from slot. Now, we might want to get rid of it later. We can delete the game in slot, or we can check and see if it does save game exist. Now you notice I've been using the word slot. The slot is basically going to be the file name that stores this save game onto the local storage of your device. On iPhones and Windows, MacOS, Linux, all of our different platforms, it saves it in the appropriate directory. Now there are two other nodes, the async load game from slot and the async save game to slot. These are asynchronous nodes. That means they're not going to stop your project from running when it's saving or loading. The normal save and load game from slot nodes will. It'll basically stop processing and finish that node loading or saving and then continue. If you don't want to see a stutter or a hitch when you're saving and loading, maybe we have a lot of data in the middle of a game, use your async reddest versions and we will look at them when we get to that point. Now that we've seen the basics, let's go ahead and put some of that into use.