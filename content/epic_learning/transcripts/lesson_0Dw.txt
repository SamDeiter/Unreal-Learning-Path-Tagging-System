The simplest form of control that we can give a user of the scatter tool that we want to build is a checkbox. The brilliant thing being that while simply nature, they are a control that can affect our blueprint operation in far reaching in complex ways. So for instance here, we will lay the groundwork for two important options that we want to add to our scatter tool. These being the clustering and scale file options that we mentioned in our introductory overview. Selecting the Blueprints actor inside the level then, let's press Delete. And then whilst inside the Content Browser's Blueprints folder, let's right-click and create a new Blueprint class. Selecting Actor from the menu that we see. After naming it BP, underscore ADV, underscore scatter, we can press the return key to lock that into place. And then double-click to open up the Actor viewer. Inside the My Blueprints section, which is where we can begin laying out the framework for the Blueprints tool that we are creating, we can click the plus button inside the variables roll out. Now let's call this first variable, scale, far-left. And then after pressing the return key, change the variable type option to Boolean from inside the detail section if needed that is. Now we can think of a Boolean as being essentially a true or false, yes or no, on or off statement in our Blueprint that will allow us to create toggles such as in our case here. So if we set the value to true, we have a scale file of applied, but if we set it to false, then we won't. Now we can add a second variable. Now this time we want to call it cluster around center. And after pressing return, we want to click the Make Public button, which is the small i icon next to each of the variables. Now we have made these variables public as it will allow the user, in our case, us, to adjust the variable in a level without having to come back inside the Blueprints itself. If we go ahead and hit the compile button now, we can see if we look inside the details section with one of the variables selected that we can adjust its state. Now we'll be doing this for each of the variables as and when needed as we go along. Next up, I want to select each of the newly created Booleans, and then set them to be true by default by simply putting a check in the default value section of the details area. Now this is just a personal preference as I like the user to see that this function exists as soon as they drop the tool into the scene as opposed to them missing it because it isn't active that is. Now as mentioned the variables that we have created here will ultimately control two aspects of our tool. So firstly, whether or not to scatter static mesh actors and cluster them around the center of the detailed spawn zone. Secondly, whether or not they will have a scale file off applied as they move away from the center of that area functionality that will hopefully allow artists to make use of the scatter tool in a wider variety of circumstances.