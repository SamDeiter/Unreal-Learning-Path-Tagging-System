Make sure to download the project files from the course page. I've already done some work based on the previous course that will serve as a base for this course. In this module, we will create our base target blueprint. We will create a target with randomized behavior and settings. This is an example showing that you don't always need to use child blueprints for randomization and different behavior. Welcome to the project. Let's create our target actor. We'll go to the blueprints folder, create a new folder called it target. And inside of there, we'll create a new blueprint class actor and call that pp underscore target. Open it up. Now we need to add our static mesh to this. So we'll go to static mesh, call that SM underscore target, static mesh target. And we'll set it as guess what a target. And there we go. We have our beautiful target. We'll keep it as a child of the default scene root because that will make it easier to change the location of it down the line using the relative location. Now I would like to add a random scale on construction script. So when it's constructed, it will have a random scale. Go ahead and grab a reference to the target mesh by holding down control and dragging it out. And we'll do set world skill 3D. We won't connect it yet because we want to set the randomized value first. We'll create a new variable. I'm going to call that actor scale. Make sure that's a float, not a vector, because we'll use a random float for this. Set this and drag off from here and look for random float in range. Don't go with the random value in a certain range. Now by default, the target is already pretty big. So we only want to go smaller. I'm going to go with 0.25 and a maximum scale of one. So the default skill, connect the actor scale and set world skill 3D. We'll go ahead and split this struct pin. And now we have three floats that we can all connect to our actor scale float to make sure it has a uniform scale. Next up, we'll want to go ahead and add randomized behavior. Something that we can use for this is an enumerator. And then we have to go outside of the blueprint for a second to create that enumerator. An enumerator is a list of named values. So we'll be able to create a list filled with names and then use these names to execute logic. Go ahead and press right mouse button. Go to blueprints and there we'll see enumeration. We'll name it e-on-score target behavior. Open it up and you will see this. A lot of information in here. There's a button with new. We'll press that four times. So we'll have four values. One of our behaviors will be nothing. The target will just stand there and wait to be shot. Then we'll have move or resound to Lee. And we'll have move vertically. And as the last one, we will add a just scale. So one of the one of the behaviors will be a constantly changing skill of the target making it harder to hit. Going back to BP target, we can now add our enumerator as a variable. So create a new variable. We'll look for e target behavior. And we'll call this target behavior. We can now do set this after we set our world skill 3D. And assign a random behavior. We'll have to convert an integer to a byte. And then convert that byte to that enumerator. But it's pretty easy to do. All we need to do is get a random integer in range. And the range is 0, 2, 3. Because 0 is 1. And then 0, 1, 2, 3. Like I said, we need to convert this to a byte. Otherwise, it won't take it. And we can connect the byte straight into here. And it will add the second conversion that is needed. Now, when we go back to our level and we'll drag in a target, you can already see it's scaling on construction scripts. So you can actually check out the skill. Are you happy with the way that the skills done at the moment? The smallest, maybe too small. Is the biggest, maybe too big. And you can change your values. I'm going back to our BP target. And I'll go into the event graph where we'll add random behavior on the game play. I'm going to delete the actor, begin overlap, and the tick. We don't need those. And I'm going to get the target behavior. What we can do here is now add a switch, switch on e target behavior. And we'll get this. A note with multiple exit pins, basically, depending on the value of our enumerator. So here we can now determine what we want to change, depending on the value of the e target behavior. On nothing, guess what? We'll want to do nothing, on move horizontally, and move vertically. We want to set a random value for the location that they'll move to. We only need one factor variable for that. We're going to create a new factor variable. And we're going to call it destination location, set it to a vector. And basically get a set twice. So copy paste it. One will be set on move vertically. And one will be set on move horizontally. We will split the stroke pins for both of them. And I will set a random behavior for both of them. But what do we need to change for horizontally? You can go to the viewport, click the mesh, and check how do if horizontally, what do we need to change? Oh, clearly that is the x value. Great. Now we know that. We can do a random float in range again. So we'll get a random value. And I think at minimum, we should move it 100 units and at maximum 250 units. And I think we can use the same random float in range for the horizontal and the vertical movement. So now we've set up our destination locations. We'll need to create the actual timeline that makes sure that things are being moved. So dragging off from either of those, we'll go with add timeline. And we'll call this the underscore move. Open it up. Double click it. A length of one second, which is always nice to have because it's super easy to tweak down the road. We'll make sure that it loops because we want it to be constantly playing. We don't just want the targets to move once. I'll make sure to add it floats and call that alpha. Add our tree values. So one at time one value zero, one at time zero value zero. And then one at a halfway point with the value of one where our alpha will be one. Go ahead and smooth these out. Compile safe. Now I'm back here. Instead of play, I'll reconnect to play from start. You can reconnect pins by holding down control and then clicking the left mouse button. So holding the left mouse button, you can reconnect it anywhere, which is particularly nice. When you have a lot of wires going into one node that you then all want to connect to somewhere else. I'm going to do the same here. And we're going to do the same for a just scale because we do want to adjust the scale. Personally, I don't want to add another random value for the scale. So we'll connect it straight into the timeline and we'll move on to the logic next. Now on this side of the timeline, after update, we'll use another switch on target behavior. Connect our target behavior. On nothing, we still want to do nothing on both move horizontally and move vertically. We want to update the relative location of our targets. So you can do set relative location. We can connect both the horizontal and vertical to the same node. And dragging off our new location, we can do a Lurb vector. Now the default location of our target, as you can see, is 0000. But this is why it's nice to have the target as a child of the default scene route. We only have to worry about relative location so we can leave this at 0000 and just connect the destination location over here. Connect the alpha as well. And now when this behavior is selected, it will move to its destination location and we'll do that on a loop. For our target, it's going to be very similar to what we did on a construction script. We'll do set world scale 3D. Connect the adjust scale. You can also copy paste this. This might look a little bit cleaner to disconnect one of the connection wires. You can also hold alt and click. As you can see, that will disconnect the connection between the two. We can do a Lurb vector. Now the A value is going to be the scale of the actor at the start of things. And we know that because we stored that in a variable called actor scale. We can connect to these. And what would we want the modification to be? I'm going to go with one value, not a random value. It's going to be times 1.5. So flow times floats. And give that a value of 1.5. Like I said before, you could use a random float for this. But if you do a random floats, make sure to promote this to a variable and then set the random float value that variable here with a random floating range. So now we have our target set up with random behavior and we can already have a look and test if that all works. We'll grab a range of random targets. I'll rotate them 90 degrees. Hold Alt and drag. Just drag them out, drag them out. Here we go. One or two more. And when we press play, you can see there's some random behavior. Now there's a few things that I notice that I don't really like. I don't like nothing. I don't want them to just stand there. I'm not a particularly big fan of that. So we will remove that functionality and we can just do that by changing the random integer and range to one. Because now it will start at one, which is move horizontally and nothing will be skipped. I already liked that a lot better. And they all moved the same. So we want to create some randomization in the way that our timeline behaves as well. And this is why it's nice that we chose one second because now it's very easy for us to change that value. Let's select everything over here and drag it out a little bit further. Let's get a reference to our timeline. Get the move. I will probably have to scroll down. Here you can see a reference to our timeline. And you can do set play rate. Now we want to reconnect all of these, which is what I explained before. Hold down control, left click. Connect them all here. Just in one go. That's really nice. And here on the new rate, what we can do is float, defied by floats. Now we can defied one by our desired amount of seconds. I'm going to use a random float in range. Personally, I felt that they were all moving too fast. So I'll change this to at minimum. It should take 1.5 seconds. And at maximum, it should take three seconds. I think that will give us a nice result. Compile save. Let's go back to level, press play. And now they all have randomized behavior. We've got scaling, moving horizontally, moving vertically. This is a nice, this already looks like a lot more of a challenge, a nice challenge to face. And I think that's what will wrap up our work for the target for now. We just created and tested our base target blueprint with a lot of randomized behavior. First, I'd recommend you to clean up your code, make sure it's commented, maybe add some redirection notes. And then in the next module, we'll go on to creating the target spawner.