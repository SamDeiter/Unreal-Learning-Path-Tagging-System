In this video, we will need to create our Capture's Collision Radius and then get it to spawn inside our user defined scatter area. We will also see how we can test what we have in the level in order to see if it is working as intended. As well as a Radius setting for the Collision Detection System, we will of course also need both Start and Endpoints. Fortunately for us, you may remember that for each loop of this process that we run through, we are storing the position of our object in the spawn point variable. And so, let's drag out another instance of this variable. Only this time selecting the get option when prompted. Next, we can search far and add a getActileOcationNode and then drag from its output and create an addVectorToVectorNode. After which, we can pipe the spawn point vector into the second input. To explain, what we have done here is take the current word location of the spawn area itself and then added the random position within that area to it, meaning that no matter where we place our spawn area in the scene, the mesh position option will automatically take that into account. Earlier on, we created two addNodes for our makeVectorNodes and so let's now pipe the new vector that we have just created into the second input of each of these addNodes. Again, we have done this so that we always have a collision capsule that is placed roughly wherever our meshes will be spawning. This is also traveling straight down in the scene within a confined radius, so it was then to allow us to see whether it intersects with other mesh actors in the scene or not. To show this in action, let's drag from the setSparmPoint node and then search far and add a sphere trace by channel, placing it at the end of our flow. Our topmost add node will be the start input then, whilst our second will be the end input and we will use the first of our divide nodes for the radius option. Let's make sure that the trace complex option is checked as this will give us better detection and we also want to make sure that the ignoreCelf option is checked, otherwise the system will collide with itself and not spawn any meshes. If we still have the spawn zone in the scene, we can change the Dra-D book type to Fargeration, come to the start of the flow and hook up our setBoxExtent node to the first far loop node. And then after compiling and saving, if we come to our 3D view, let's take a look at our collision detection in action. Now although we haven't as of yet input any mesh types, the system by default, just because of how we have set it up, will act as if there is a mesh to be spawned, which hopefully means that we can see our system at work. If we change the spawn area radius inside the details tab now, we should see our collisions update, with the same being true if we also change the number of meshes. In fact, if we push our spawn area down into the floor, we should be able to see when the system detects our static mesh actor. Let's just reset those back to defaults and also turn off the debug visuals for the system so as to not slow the viewport down later on. As our collision detection system is clearly working as intended, we can now move confidently onto creating the last bit of logic for this particular pad of our blueprint.