In the previous video, we finally brought our new player character up to the same standard as the one included with the Paper 2T Start project, but with an additional understanding of how each element fits together. Now it's time to move beyond that foundation, address some of the limitations of the current implementation as well as add some new abilities. We'll start this process by adding the ability for the player character to move at both a run and walk speed using the movement component. We'll finish up by adding some logic to our blueprint to change the flip-put display pending upon whether the character is running or walking. Let's start by giving the player character the ability to run. The first thing we'll need to do is create a new input for running, so let's open the input section of the project settings window. Navigate to edit project settings in the main editor window and locate input on the left-hand side. Locate and unroll the action mappings roll out. As we can see so far, we only have one action mapping, the jump mapping. Let's create a new mapping by pressing the plus icon and name this new mapping run. We can quickly assign a key value to this mapping by pressing the keyboard icon to the left of the mapping drop down. Once we've clicked the icon, press the key you'd like to use. A come-on run button on the keyboard is left shift, so I suggest using that. Now that we've added the run input mapping, we've completed that setup and we can close the project settings window to return to the main editor window. The next stage is now to use this mapping with our player character Blueprint to alter a Boolean variable. In the content browser, navigate to content, my content Blueprints and double-click my2D player character to open the Blueprint editor. Make sure to select the event graph tab. Right-click within the graph and search for input action run to add the event. Much like we did before, for the input axis move right, we're going to use the input action run to modify a Boolean variable. We'll then later use this Boolean variable to modify other Blueprint logic to decide what we'll do when we're running. Let's start by creating a new variable by pressing the plus icon in the variable section of the myBlooperInPanel. Make sure that the new variable is of the Boolean type and name the variable is running. With the variable now created, hold down Alt and left-click drag the is-running variable into the graph to create a set node. We'll need two copies of this node, so use the copy-paste functionality to create another one. Now, connect the execution wire for both of these nodes to the pressed and released execution pin of the run-imper action. When we press the run button, we want to be running. So therefore, for the set node, make sure is running is checked. When we release the run key, we want the stop running, so ensure that is running is not checked in the set node. Now, let's create a new event that will use this Boolean to alter the max walk speed of the character. Right-click on the graph and type custom event into the search box and add a new custom event. Name this new custom event, update walk speed. This event will use the value of the is-running variable we created to decide which walk speed should be used. This means the first step will be to get the value of the is-running variable. Control left-click the is-running bull into the graph. Connect the execution pin from the update walk speed event into the branch. This branch gives us two execution pathways we can use to alter the walk speed, and now we'll go through the logic to adjust the value using the character movement component. Control left-click drag the character movement component from the components panel to get the component into the event graph. Drag from the output pin of the component and search for set max walk speed. Let's start by setting the walk speed. The speed the player character will move when the run button is not held down. While we could just add a number directly into the set node here and hard code the value, it's good practice to make numbers like these variables on the blueprint so we don't have to go hunting for them later if we change our mind. Drag from the max walk speed node and search for promote to variable. This will automatically create a new variable of the correct type which in this instance is a float and prompts us to name the variable. Let's name this new variable walk speed. This value should match the walk speed value we set in our movement component in the previous module. However, before we can set the variable, our new engine is telling us that we need to compile the blueprint so let's do that now. Now over in the details panel we can set the walk speed to 78, exactly the same value as we added to the movement component previously. And with that this particular set node is now configured. All that's left to do is connect it to the false execution pin of the branch. Now, let's configure our run speed. We can save a bit of time by using copy paste on our previous set node. Mark you select the nodes and copy paste them above. Delete the walk speed variable reference as we'll create another variable for the run speed. Connect the new set node to the true execution pin of the branch. Add a re-root node by double clicking on the execution wire. We position the wire so that it's out of the way for the other nodes. Note that I personally like to add a re-root node to the other branch as well so that things look the same. This makes it easier for me to understand what this branch is doing and read it quickly. Now let's create our new variable. Drag from the max walk speed pin of the new node and search once more for promote to variable. This time name the variable run speed. Once we've compiled the blueprint set the default value for run speed to 250. Let's quickly go over what we've implemented. If the Boolean is running is set to true, then we'll use the character movement component to set the max walk speed of the character to our local run speed variable. If the is running ball is set to false, then we'll do the same thing, but set the max walk speed to our local variable for the walk speed. The simple piece of logic is all that is required for the event, and now all we need to do is actually call it from somewhere. We want to call this update as frequently as all of our existing logic, so let's execute it as part of the tick update. Locate our tick event, right click on the graph somewhere nearby, and search for our update walk speed event and select it. Insert this new node after the update animation in the tick event execution path, and make sure that all the subsequent nodes are still connected. And that's everything we need to do in the event graph for now. So let's save, compile, and close the blueprint editor window. Let's start a new play in editor session and note how the character speed changes as we press and release the run input button. The next logical step will be to display the corresponding run and walk flipbook animations at the appropriate time. Let's reopen the player character blueprint and locate our handle animation event. The basic version that we've implemented so far has served us pretty well. It just checks if the player character has any velocity at all and picks between two flip books, idle and walk. What we need to do now is extend this by adding another select node to pick between running and walking. Drag from the true pin of the current selector and type select until the selector node appears. This time we'll use the Is Running Ball as the index for the new selector. Control left click the Is Running Ball from the My Blueprint panel and drag it into the event graph. Drag the output pin from this ball into the index of the new selector. Now we can use the dropdowns of the selector to choose our walk and run flipbook animations. For false, so when we are not running, select the walk flipbook. This is flipbook underscore UE4 robot underscore walk. For the true selector select the run flipbook. So that's a flipbook underscore UE4 robot underscore run. That's all we need to do for now, so compile and save the blueprint and return to the main editor window. Let's start a new playing editor session and see what difference that has made. We can see that the animation changes as we press and release the run button. And with that, we've implemented the running functionality both mechanically and visually. In this video, we added the ability for the player character to run. We began this process by adding a new run input mapping to our game. We then used this new mapping to adjust the max orc speed variable of the character between two speeds. One for running and another for walking. We then used the same logic to set a ball variable to indicate if the character is currently running. We then used this variable to decide which flipbook animations are displayed as the character moves.