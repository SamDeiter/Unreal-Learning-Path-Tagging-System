When organizing our content browser, we have a few things available to us, and one of them is the ability to filter out our content by asset type. In your content browser, near the top, next to your search bar is the filters option. When we click on this, we're presented with a bunch of different asset types, from basic common used ones at the top to everything else at the bottom. Now, if we click on one of them, let's say static mesh in this case, it's going to filter out the results of the folder we have selected based on that asset type. If you have multiple asset types, it will go ahead and combine them. Now, let's go to look at this. Right now, I have a Blueprints folder selected, static mesh as the filter, and we can say I have two results, a sky box and some arrows. If we click on our main content folder at the top, now we see more static meshes. If we were to look through these, we'll find that these are not all inside of one folder. By default, our filters are going to look through whatever folder you've selected and recursively go through all the folders below that and find any of the children. Now, if you don't want that, under view options, you can disable the filter recursively option. When you do that, it's only going to look in the individual folder you've selected and not any of the children folders. I'm going to go ahead and re-enable filter recursively, so it's back on, and we'll look at it further. Now, in this case, I'm going to go back to my content folder. I'm going to go to filters and we go ahead and check texture. Now we have static mesh and texture selected, and inside of our results, you can see we have both textures and static meshes. Now this isn't an AND. We don't want static meshes AND textures, so something that matches. We're collecting everything that matches the filter we have enabled. If you don't want an individual filter turned on anymore, you can click on it. In this case, I'm going to disable the texture filter on or off by clicking on it. You can right-click on any of these filters we've selected and remove just that one or enable only that one, as well as enabling or removing all filters at once. Now inside of our filter menus, one last thing, the check box, next to them, will select individual assets or the entire parent and child below it. So in this case, if I go to sounds and I click the check box, next to sounds, you'll notice all the children are now selected and everything the engine considers a sound asset is now selected. So we have things such as sound waves, sound cues and classes. And of course, if I wanted to filter out something individual like maybe just the sound waves, I could find the sound wave asset type itself inside of here. Right-click, enable only that one and everything else would be diselected. And when I'm done, I can right-click and remove all or I can go back to the filters menu and click reset filters at the top. Filters are going to be unique per content browser, so keep that in mind.