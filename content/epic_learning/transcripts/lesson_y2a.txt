We have viewed the extension of our BP toolbox and the process of creating and maintaining local persistent anchors in our scene. In this module, we will do a full walk through to explore how it was created. Let's get started. We'll start inside of BP toolbox. I've gone through the event graph of BP toolbox and moved things around to their own events. This top section is everything that we made together. And this bottom section is the local anchor logic. Pan to the Prepare anchor tab. This has been split into two sections. For the top, we have the Delete button bound to an event that will call an event inside of BPPON named Delete Local Anchor. We'll provide only the name of the anchor we want to delete. We'll explore BPPON's events and how it handles these inputs shortly. Pan over to the next button. We again bind this to an event and when pressed, call Cycle Anchors. To be certain that the text is set right at the start, we will call Cycle Anchors after binding the next button. Double click Cycle Anchors to go to this event. We can see that we are retrieving a map or a dictionary variable from BPPON. Getting the length of the variable and if it has less than or equal to zero indices, set the text to no anchors loaded. If the variable contains more than zero, cycle through the current local anchor and get the key at the int value of current local anchor. Current local anchor is programmatically set by the length minus one of loaded AR pins. This means that the range of current local anchor is zero to max amount of loaded AR pins. So it will never request an index that doesn't exist. After we get the requested index, we will retrieve the debug name and set the text to its value. Pan up a little. When ad new is pressed, we call ad local anchor, which we can see is also defined inside of our BPPON. See that we have provided none as the anchor name. This will signal BP anchor to generate a random name for the anchor. We'll go over how this is done in a moment, but if your project requires specific names, here is where to append the logic to set that name. Besides the name, we also provide it with the selection of the type of placement the user wants. Zero for manual and one for tap to place. After adding the new anchor, we close the toolbox. For the load all button, we'll again call an event inside of BPPON named load local anchors. We will provide the bool value of whether to spawn BP anchor at each loaded anchor. Then with these loaded, we cycle the anchors. Lastly, when delete all is pressed, we call delete all anchors inside of BPPON. Let's move on to BP anchor to understand what happens when it is added to the map. Navigate to world builder, blueprints, and open BP anchor. Starting at begin play, our anchors are given a name for us to identify them throughout the world. See that there is a string variable named anchor name. It is instance editable and exposed on spawn, so when it is spawned, it should immediately contain its name. If the name is none or is empty, it will generate a new one by appending these two string arrays with a dash as a separator. One has seven color strings and the other has ten numbers written out, giving us 70 named options. Keep in mind that despite 70 options, the chances of creating a duplicate name is possible and will increase with each created anchor. If two anchors contain the same name, the delete button in BP toolbox will end up deleting both. Also, if any actor is searching for a specific anchor, it may attach to the wrong one. An easy way around this would be to create more values and appends to use for the generated name. Creating options like color-number is much easier to remember for later reference or debugging. Tangerine-4, for example, is a bit more memorable of a debug name than the 32 character globally unique ID that will be associated with this anchor. Anyway, after the name is set, we want to bind the delete button that is in this blueprint. When it is pressed, call the Remove Anchor Event. This will call Delete Local Anchor inside of BPPON and see that it provides self for the input of Remove BP Anchor. This will save BPPON the trouble of needing to search for it by name before destroying it. Pan down a little to the EventTick. The EventTick is telling the rendered text and the delete button to always face our camera. So, no matter where it is around us, we can identify it and remove it if needed, assuming that it is close enough. Moving on to when the anchors are spawned. If we want to add interaction to them, add interaction component gets called and is provided an int. If the int is zero, it will add a generic manipulator component and bind an event to on and grab. Select the generic manipulator component and see that we are only allowing translation with two hand transform modes as we want to keep the anchor to always stay the same scale and keep the vertical orientation. When the user stops moving the anchor, it will call Save Anchor Transform. If Add Interaction Component is called with a one for the placement type, it will instead add a Tap to Place component and again, when placed, call Save Anchor Transform. Active Anchor Transform will call the Save Local Anchor Event inside of BP-Pon, providing the scene component named Anchor as the component to pin and the anchor name as the debug name. Open the Viewport and set the View Mode to Wireframe. The component bound control is the component that we are actually interacting with using our two means of interaction. The location of its pivot is very important for anchoring to a surface, but because this one's pivot is off-center, we will offer the anchor scene component for the pivot's world location to save as the anchor location. Back in the Event Graph, bound control is destroyed as soon as the anchor is saved. This will prevent the user from moving the anchor after it has been placed. The main player in our system is our BP-Pon. So let's go over how all of this logic comes together there. Open BP-Pon. In the Event Graph, this entire bottom-right section is what is needed for this project, but as mentioned at the beginning, this is the requirement for this project. The amount of needed blueprint nodes to get a basic system working is much smaller. Let's start at the top and work our way down. In BP toolbox calls Add Local Anchor, our blueprint will first check if the AR-Pin local store is supported and if AR-Pin local store is ready. If not, nothing will happen. It is recommended to add this branch and provide an error to your users if it is not ready. Here I am simply printing to the log and the screen that the local store is not ready. If it is ready, call Spawn Anchor for placement. BP Anchor will get spawned 60 centimeters in front of us. We will call Add Interaction Component and pass on the interaction type that we saw earlier. Next Save Local Anchor is called when the user is finished moving the new BP anchor. This part is important when setting up and saving local anchors. We know that the scene component Anchor is coming from BP Anchor and will be the component to pin and provide its world transform. We will also provide the anchor's name to the debug name. This will add a pin and place it in the world transform of the component. I have added a small delay after setting the pin to make sure all is ready for the next step. Lastly and very importantly, we need to save this pin in the local store. That is all that is needed to save the anchors in the scene. Our system expands on that by adding a new pin to our dictionary variable. When load local anchors is called, the important part is this load AR pins from local store node. This will recall all the saved pins and make them available to us. Expanding on this, we will cache this return value, clearing it before loading and setting it immediately after load. This will prevent any loaded duplicates. After caching these new values into our variable, we can cycle through the available anchors inside of BP toolbox. If the user has set to display the anchors on load, we call spawn anchor meshes, which will get all the available anchors and spawn a BP anchor for each one, set its name and call attach anchor to pin. Notice that add interaction component is not called. This is to prevent the user from moving an already placed anchor. Then attach actor to pin is called, so we only have one event responsible for this action. We will provide the anchor scene component inside of BP anchor as the point to attach, as well as the name. We will use this attach actor to pin event for other blueprints or actors in the scene that we want to make sure they are located in the correct location. This will become an important part of using the local anchors for our project. To pin your actors to the anchor, all that is needed is the pin component to AR pin node. If the user makes an attempt to pin something to an already used pin, the first actor would be un-pinned, but here I am checking if the pin is being used already, and if so, don't pin the new object to it. And instead, attach it directly to the object currently on the pin. This way we can use the single pin for multiple actors if needed. The next event is delete local anchor. The only node needed for this to work is right at the beginning. Remove AR pin from local store. This will be enough to remove a single pin from the store. We have further expanded on it to keep all the other pieces of our system up to date. If the delete button on the BP anchor was pressed, is valid, will receive a valid self and destroy what was provided. If the delete button was pressed from BP toolbox, we need to search for all the anchors in the scene and remove the one that has the same name. Be careful here with anchors with the same name because if there are multiples in the scene, they will all be destroyed. After destroying the no longer needed anchor, we update our loaded AR pins variable and cycle through the anchors in our toolbox. Here is another good example as to why an is valid node is important. Double-click cycle current anchor in toolbox. Deleting a single anchor can happen inside of BP toolbox as well as on the anchor itself. If the user pressed delete on the anchor itself, there is a possibility that BP toolbox is not in the scene. Without this is valid node, we would receive an error. Right on top of this is delete all anchors. The only node we need is the one immediately following. Remove all AR pins from local store. This will do exactly as it says. When a user presses delete all, the store is emptied. All BP anchors are destroyed and the list of anchors is updated, resulting in the text displaying no anchors loaded. I know this was a lot and I hope it was okay that we didn't go through each step together. Before we move on to the next module, let's do a quick review. To add a persistent local anchor, check if AR pin local store is ready. Spawn a mesh or blueprint to pin to a component for a visual representation and provide a unique name and save the AR pin to the local store. To add them, call load AR pins from local store. Either cache them for later use or use a 4-H loop to spawn a visual representation for each value. To remove a single one, provide remove AR pin from local store. The name of the pin you want removed. And to remove all of them, call remove all AR pins from local store. One quick comment about the way I have laid out the blueprints here, especially BP pawn. This is not best practice, as BP pawn has become much heavier than it should be. Know that the reason this has been done this way is for the benefit of the learner. Instead of breaking this up between multiple blueprints, such as one responsible for UI, one for anchor management, one for hand interactions, one for low level data management, and so on, I have placed it all here so the flow can be easily seen and read through. In a later course, we will cover principles for project optimization and good code layout, but until then we will leave this blueprint as is. In this module, we demonstrated the system that was built to handle adding new anchors to our map using two means of interaction. When the user places the anchor in the location it is needed, it will save to our local AR pin store. We saw how the anchors can be loaded across sessions and deleted individually or all at once. After seeing the system in action, we took some time to go through the various blueprints to see what happens behind the scenes that makes this all work. In the next module, we will explain the difference between using the stage frame of reference for smaller scale experiences and using spatial anchors to build larger world scale experiences.