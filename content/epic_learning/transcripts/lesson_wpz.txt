Parallelax and bump offset are two useful tools for pushing the depth of your materials in textures and engine without adding additional geometry to the mesh. Let's go over how they work and talk about setting up and importing a height map, using bump offset in a material, and using parallax in a material. To start, let's ensure we have all the proper textures we need to create and use both bump offset and parallax exclusion mapping in engine. We need to create and import a height map. The height map is a grayscale texture, and it's just like the roughness or metalness as it ranges from 0 to 1 in value. The 0 depicting parts of the texture that are recessed or pushed back and the white values depicting the parts of the texture that are coming forward or pushed out. We can pack this texture just as we did with our other grayscale maps. I'm going to go in here and to Photoshop, and I'm just going to go through my textures that I have set up so far. So I have my base color, my normal. I have my textures that I'm going to channel pack. Now before we use the green channel for the metalness, but since I don't have any metal in this particular material, I'm going to go ahead and substitute the height map instead for that green channel. So I'm going to go in here and I'm going to go ahead and move this to the top of the stack so we can see as we go. So I have my AO. I'm going to set this to blue channel. I'm going to set the roughness to red channel. You can see here, and I turn on my displacement. We have this texture set up so that we can see the white areas or the more areas that are coming forward and the black areas are more pushed back in the actual material that will be displaying. So we're going to go ahead and channel pack this one into our green channel. And now we're all set up for export. Now we're going to go ahead and back into Unreal to see what the results are going to be after you apply this to our material. Now in Unreal, I have the three textures that I just exported, and I'll bring them into a new material. So let's go ahead and create a material. So here are the three textures that I have. I've insured that this texture is set to mask mode. And I'm going to go ahead and divide materials. And I'm just going to create a new material. I'm just going to name this depth test, 01A. And I'm going to bring those textures into this material. So just as before, we're going to set it up in the same way for the most part minus a few changes that will be adding as well. So we have our base color here. We're going to go ahead and hook that to our base color, our normal that will hook into our normal, our roughness that will hook into our roughness and our ambient occlusion that will hook into our ambient occlusion. Now the change is because we don't have a metallic, and by default, the metallic is set to 0. We don't need to hook anything up here. But now we'll add as a bump offset node, so if I type in bump offset, this will create this node here. And this node is actually going to ramp into the UV values of each of your textures. Now something to note is going to say missing height input. We need to create the height texture that will be inputted here. And while we are using it on this side, we can't ramp this back into here because it's what we call a re-intering expression, meaning it's using itself to direct itself. And so it gets a little confused by that, and it doesn't like that particular part of the process. So all we need to do is fix this. It's a copy, this particular texture sample. Move it over here, and then ramp this into the height value. Now that we have this set up in the height, we can come in here and we can add a texture coordinate. And we'll also create one constant value to be used as what's called our height ratio input. And this is going to use to be used to dictate how heavy or how strong the effect of this bump offsets is actually going to be on our material. So we're going to convert this to a parameter, and we're going to call this height ratio. This is set to 0.05 by default in the bump offset. So we're just going to set the same thing here so that we can start from this point. Now with this done, we go ahead and save this, browse to the material, and I'm going to create a material instance. So name this to the mind. Bump offset, go in A. And I'm just going to drag this here onto this material. So let's go ahead and actually add a switch so we can more visually see what's actually happening here as we switch back and forth between bump using bump offset and not using bump offset. So I'm just going to add a switch parameter node. And I'm going to say use bump offset, question mark. And I'm just going to wrap these in here. And when I want to use it, I'm going to set it to true. And when I don't want to use it, I'm just going to simply use the regular texture coordinates here. So I'm going to go ahead and hit apply, save this, come back into our material instance here. And you can see right now, it doesn't have the bump offset turned on. And it's still, apparently, good just with the normal map itself. But let's really see what it looks like with the bump offset turned on. So I'm going to go ahead and turn this on. You can see here, now it's getting a lot more features that are showing that it looks to be a fo3d kind of view of this by how it's manipulating the pixel information here. And you can see it more as I increase or decrease this amount here. Now keep in mind, this is a foe kind of process that isn't really displacing the mesh. There's no geometry being added to this mesh. The mesh is still the same far to look in here into wireframe mode. You can see here, it's still the same mesh as it was. There's no change there. The silhouette is not actually changing. It's just an effect to give the idea of depth. So you can see here, there is a limit to how much you can push this before you start seeing some kind of tearing or ripping up the pixels. The best case used for this, I would suggest, is if you're using this is to make sure that the height map has softer or more rounded values. Having a more contrast value in your height map will actually provide for more ripping or tearing of the pixels more quickly because it's more of a dramatic change from high to low values. So having softer transitions between the high to low values will provide a smoother transition for this bump offset to work. Now let's look at another similar process, which is called parallax occlusion mapping. This is similar to bump offset and that it doesn't add any geometry to the mesh, but instead tricks the eye into seeing a perceived depth based on pixel manipulation of the material. To create this effect, let's first add a parallax occlusion mapping node back into our node network. So I'm just going to go back here into our master material and I'm going to right click and I'm going to create a parallax occlusion mapping node. This node has many more inputs and outputs than the bump offset node does. We'll keep it fairly simple for now and you'll only use the inputs and outputs to achieve the basic result. So I'm just going to drag this up here. We're actually just going to replace this. So instead of use bump offset, we're going to say use palm or parallax occlusion mapping. And if it's true, we're going to ramp it into the parallax occlusion UVs. We can still use many of the nodes that we've already previously set up the bump offset, but we'll need to create a few more as well. So I'm going to go ahead and drag these up here and I'm going to go ahead and convert this texture right click to a texture object. This is because this particular input needs a texture 2D to use as the height map information. So I'm going to ramp this into the height map texture here. It also still requires that height ratio input. So I'm going to reuse that same height ratio input that we had previously. It also wants a UVs or texture cord if we want to use that as well. And it's also going to need a few extra inputs to make this work properly. So the first it's going to need is to know what channel on the texture that you've inputed here is actually using the height information. So remember it's in the green channel. So I'm going to create a constant for vector. And I'm going to specify to this material to look to the green channel by adding a one value in the green channel here. Now it knows to look to the green channel to find the height map information of this particular texture. The next thing I need to do is create a max and min steps. So I'm going to duplicate this constant and I'm going to input these here to max and min steps. Think about max and min steps as the minimum steps in what you want to have the parallax occlusion mapping happen. So basically the littlest effect or the cheapest effect version of the effect that we could possibly get. And then max steps you want to think about is what's the most you're willing to spend to get the effect to look good. So I'm going to go in here and I'm going to rename this to max steps. And I'm going to set this to start at 32. And here I'm going to set the min steps and I'm going to set this to eight. Now again, you can roll over here and you can see what the default values are. So they have eight and 32. So I've matched the same default values here so that I know I've set up properly at least to start off. And then I can kind of tweak the effect from there. Now with this setup, I can go ahead and hit apply. And I can come back in here and I can look at see what our node looks like. So you can see here now the material and offset looks much more reasonable. And there's very, very little tearing. It actually looks much more believable than the bump offset. So here if I increase the height ratio, you can see here I can push it fairly far and avoid any kind of tearing or ripping because I now have more steps that I can use to simulate the effect. If I increase this to a higher number, you can see here the fidelity even gets increased. So it actually looks even better. But be aware of the higher you make the max steps, the more expense this will cause. So if I put this at 128 again, the effect is much higher and the results are much crisper. But the overall expense for this material to render and cost for on frame rate may increase. Both bump offset and parallax occlusion mapping are fairly reasonable in terms of cost of render and material expense. Of course, bump offset is much cheaper than parallax occlusion mapping, but both are substantially more affordable in terms of rendering resources compared to fully displacing or testulating your mesh. So use these nodes when you want to show depth, but want to have performance in mind.