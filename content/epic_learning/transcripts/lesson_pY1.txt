The first thing that we need to allow us to work with any of our external data types is a structure defined somewhere within our project that will match the parameters that we'll be working with. The parameters in this case are going to be the details I mentioned a little bit earlier in a previous video, the things like the data, about the items, the names, the cost, and so on. And just a quick note here before we start looking at this stuff. When I begin jumping into working with the code in this project you'll notice that I'm using the IDE rider. But if you wanted to follow along with any other IDE of your choice depending on your platform, VS Code Visual Studio or Eclipse depending on the environment that you're working, any of those will work perfectly fine as we go through the course. I've provided a template project for this course that I'd recommend downloading to help you follow along with the content. The template project comes with the more generic steps implemented so that we can focus just on the course specific topics. So let's take a quick tour of the project to see what's already provided and get a better idea of what will be implementing throughout the project. First we have the assets folder with a selection of meshes, materials, and textures. The meshes will be the visual representation of the different items that will be allowing our data classes to define through the code and in a similar way inside of the textures folder we have a few images of the items which will be automatically set in a widget that will see in a moment. And inside of the Blueprints folder we have two data item classes, BP underscore data item asset and BP underscore data item table. These are the child classes of a C++ class that I've also provided. These will be used to demonstrate the data asset and the data table functions to pull information via code and use that to populate the classes. Now in this scenario we will be using both of these to essentially represent the same information that's not something you'd see in most projects. This is going to give us some really nice comparisons of what a data asset will do and what a data table will do as well as being able to just focus on the code and the calls to get that working. Then finally we have the WBP underscore item data class which is provided purely as a visual representation of the information being pulled from our data assets. It provides a simple widget to visualize the details that we create about our items allowing us to confirm that the correct details are being pulled in code and displayed to the player. As you can see here we'll need to return to this widget class a little bit later to hook up some of the details when we've started creating some of our custom classes and structs. For the code classes that I've provided the data actor base is the parent of all of the data item classes that we'll be creating and provides the most generic components, the sphere collider and the mesh that both of the examples of using the data tables and data assets we'll use. Then the data actor asset and data actor table are child classes of this and are left mostly empty for now. We'll be filling these with the code specific to using either data tables and of course data assets in their respective classes. Now that we've seen the project and what's already included you hopefully have this downloaded and ready to go. If we return to the editor as we'll need to start by creating a new class to hold a struct and an enum to represent our item information. We could do this in any number of ways but for this example I'm going to choose to create a new C++ Blueprint function library. The main thing is that we just want this struct and enum to be accessible anywhere in our other classes. I'll place this new class into a folder named Data. This will cause a small compilation issue in a moment but we'll fix that when it arises and it will help us keep the project that little bit tidier. And then of course we need to give this new class a name so I'll call this one Data Library. When that's finished generating you'll likely see this error message which is due to placing the classes in the different folder structure as I've just mentioned. But as I said this is a simple fix and does that last to keep the project well structured. So let's go over to our IDE of choice. I'm using RIDER for Unreal here. If we navigate to our source folder and then the new data folder that we've created. We want to head to the data library.cpp, drop down the includes and quite simply remove the data and the forward slash from the include here. If we recompile this night that solved the compilation issue that we had and we can then continue as normal. So in the data library header file we'll start by creating a new enum named eItemType. This will be used to allow us to easily and safely select the type of item that we want to pull the information from inside of our data type whether that be a table or the asset option. Remember in our meshes folder we have the three different item representations so I'll now create four new definitions. One, two default to none just in case we don't have any of the other three selected and then one for the other three items which are going to be the radio, trophy and spaceship. Each of these will have the same U-Meta definition which will simply be their display name and that will match exactly their given definition. So in this case that's going to be none, radio, trophy and spaceship. So this step will actually optional to the entire process of working with data types or data assets but I find that this makes things a little bit easier to track if we are going to start kind of adding a lot more items to our project and also when we're searching through the data it reduces the likelihood of typos compared to searching by string. And then to wrap up the topic we're going to create a couple of structs that will be compatible with all of our project requirement meaning that this needs to account for data tables and data assets. They both work in slightly different ways. So for this reason there are going to be a couple of extra steps but I'll cover the traditional approach and the one more tailored to keep this course content as flexible as possible and reduce the repetitive code. The first thing that we'll need is a standard struct named f generic item info. You could name this anything but for me that is going to tell me that this is the generic structure which anything can compare against whether it's an asset or a table and you'll see a little bit later they're all going to be using the same kind of structure information. And as I said this isn't a standard process but because this course we want to cover all of the different data types I wanted to show the comparative way that we would set up the same structure for different types. So I'll be giving this a u struct and provide the blueprint type specify as we'll also want to access this in our blueprint examples I'll show you again how we can access this in c++ and blueprint so you can see the best of both. And this will hold all of our information of our different items so we'll implement in here a new e item type and I'm going to name this one item type. Then we're going to want three f strings one named item name one named item cost and the final one item info. For the visual representations we're going to want a u static mesh named mesh and a u texture 2d named image and these are going to be used to generate the mesh at runtime and the image to add the widget when we overlap the slider with the player. So that's something else that I've not mentioned here we're not going to be manually setting up any of these items so we're going to have a single item class which could potentially be the radio, the trophy or the spaceship. And all of those will be generated at runtime depending on which item we've defined it to be and it will pull everything including how it should be visually represented so what static mesh it should be using. All of that will be loaded at runtime depending on the information we pass in. So that means that this also saves us from doing things like creating hundreds of individual items with their own variables and information within them. We're going to let all of that be generated from our data assets. So finally we want to add a u float curve named curve float. And a standard float named rotation duration that I'm just going to set to a default of 5. These are just here to allow us to provide the potential of each item to use their own unique float curve and rotation duration to rotate for a different amount of time using different animations. Purely visual but it's another way that we can as well as using all of the data types, the data assets and tables we can also have a look at the float curves and it's all kind of been one pretty flexible kind of item example that we'll be working with. And then to finish these details off you can see here that I'm just giving them all a u property. Again they're all going to share the same simple u property specifiers. We'll make these edit anywhere blueprint read right and we'll put them all in a category named item info. This is just so that we can find them easily and we're going to make these editable anywhere and also blueprint read rightable again just for simplicity of testing them so that we can see that are updating in different places and also we will be looking at the blueprint functionality so we want these to be blueprint editable as well as viewable but again in other situations if you know this is purely going to be C++ based you may want to make this blueprint read only and maybe only visible but for testing purposes and simplicity to get these kind of new topics or points across we'll pretty much expose these in their entirety. So at this point we have all of the information that we'll be using for any of the different items in the project in a single struct. However at the moment this wouldn't allow us to create a data table or data asset from it and to keep the concepts and topics as clear as possible we'll focus just on one data asset type at a time so we'll start with the data tables for now but do keep in mind that we'll need to return here a little bit later make this nice and reusable for our learning opportunities and to avoid the potential code repetition that I've mentioned when we come back to add our data asset version as well. So for the data tables we need a specific type of struct for this I'll copy and paste the generic item info struct below rename it to f item information table to remind us that this will be the struct specific to our data table example and then after the name of the struct for data tables we need to include a colon followed by public and f table row base to specify that we're using this as part of a data table without this bit when we create the data table assets we wouldn't see this struct as an option to pull information from also note this requires the include of the engine forward slash data table dot h. So this is a little bit of code repetition here that you wouldn't necessarily usually see in a lot of projects but again this is going to allow us to easily pass information around between the various examples in our data tables our data assets the widget and everything in blueprints so we're going to do this kind of larger code repetition here in the c++ header or our codefile header and this then means that we can very easily pass around and work with the different types as we go through our project. The important takeaway here is that you need to remember to include the public f table row base in a struct that you wish to use or implement for data table classes. The struct above is going to be used purely for all of our blueprint references so to compare the difference information between the data table that we'll be working with and we'll see something very similar a little bit later in the course when we come back and go through data assets and with all of that implemented remember to build the the code and I make sure that you have no compilation errors and that will get us ready for the next video where we go back over to the editor to make use of this new data structure also note that when you're back in the editor you may want to hit the compile there as well just to make sure that everything has synced up.