We are at a new level of reality altering experiences in our project. The power of mixed reality is demonstrated by the immersion we can provide our users. In this module, we will prepare our project to handle generated mesh data from our environment and interact with it through our holograms. The HoloLens 2 can create mesh data that will be built from scans of our environment and we can use this mesh to create a scene for us and our holograms to interact with. When choosing and preparing your space for creating a good spatial map, be sure to have plenty of good lighting and, if possible, avoid dark or translucent surfaces. The accuracy and quality of the mesh data depends heavily on the user and the space that they are in and wish to scan. In your spatial mapping projects, try to include a method to inform the user to walk slowly around their space and look in every direction. The fuller the scan, the more dependable it will be. Even repeating a walkthrough of their space multiple times is recommended for a complete scan. Of course, the threshold for what complete means depends a lot on your project's needs. Some common errors that occur, such as holes in the mesh or bias within perfectly aligned surfaces, can be fixed if another pass of scanning is done. Inside of our project, we're going to create UI to begin and end scanning. This is a heavy process and if the requirements do not demand constant scanning, the user should be able to turn it off to maintain the target frame rate of 60 frames per second. With actors rendered in the scene, such as the scanned surface and this performance is hit for too long, our project will be poorly perceived and can induce motion sickness. We'll need to find that balance of features and performance. Let's see all of this in action. Open the spatial mapping map inside of world builder, maps by course, spatial mapping, and open spatial mapping. To enable spatial mapping on the HoloLens 2, open our project settings. Scroll down to the HoloLens plugin and under the capabilities category, set spatial perception to true. This will communicate to the HoloLens that we intend to use the scanned spatial data, but the setting alone will not start the scan. Navigate to our AR session config asset located in the world builder assets folder. Open AR session config. Under world mapping, set generate mesh data from tracked geometry, as well as generate collision for mesh data to true. We'll return to these settings a little later. Press save and open our BP pawn. BP pawn has long been prepared to start the AR session at begin play. Starting and pausing this session is how we will control the state of the scanning. Similar to starting the AR session, we can pause it and stop it. Pausing the session will pause it, but keep the current state. Stopping this session would stop it completely and clear the state. See that we use the stop AR session node at event end play. This is to clear the state of our session when we stop playing. In your project, you can also take advantage of the node, get AR session status, to update the user if an unexpected result occurs. This can be used to inform the user that something is not as expected. Perhaps in error with permissions or with the project itself, but knowing this update, they can decide what steps to take. What we want to do is add a way to be informed when a new world object has been scanned and is available for us to use. For this, we need to add a new component. Search for AR trackable notify. This component will listen for updates provided by the HoloLens and we can utilize various events to update the experience. There are lots of events we can use here, but for now, we'll only prepare logic for on update tracked geometry. So let's add an on update tracked geometry event. From tracked geometry, get the underlying mesh. What this event is providing is a reference of the mesh or meshes that it has scanned and added to its list of scanned meshes. We need to be careful here because not all the mesh updates will be valid for our use. So from return value, create an is valid node and from is valid, create a new branch. The AR session is prepared to inform us of the current status and we really want to proceed from here if all is working as it should. Right click and search for get AR session status. Break this struct and from return value status, create an equals to enum node. Open the list of enums here. Based on the returned status, we can inform our end users to take various steps depending on what the problem might be. If we wanted to, we can also use a switch node from the return value status and that will allow us to do unique things for each possible path. For our purpose, we'll use the equals to node. So it is either working or not. So let's set the enum to running. Create a new variable named tracked geometry. Set it to type MR mesh component and change it to an array. We'll use this array to cache all the valid meshes that our system finds. From true on our branch, create an add unique node from tracked geometry. This node will ensure that we are only adding new elements to the array. Plug in the return value from get underlying mesh into the input. We know that we will want to be able to pause and start scanning as well as toggle the material for the mesh. If, for example, we wanted to render the mesh, we need to allow this for newly scanned meshes as soon as they are added to this array. Create a new bool variable named spatial mapping render mesh and connect to a branch. We'll return to this in a moment. Create a new custom event named spatial mapping set visibility. One bool input named visible. From visible set spatial mapping render mesh. Then from our tracked geometry array, create a for each loop. As the scan continues, the meshes can be changed making some of them invalid. So from array element, create and is valid node. From is not valid, remove this element from the array. From is valid, connect array element to a set material node. Pull backwards on material and create a select node. Using the spatial mapping render mesh bool, if true, set the material to M-emissive wire orange. We can leave false with nothing so that it will simply remove the material. Notice that the material we are wanting to add is a material instance of M-emissive wire. This is located in the materials folder under World Builder. If you open M-emissive wire, you can see that under the rendering tab, we have wire frames set to true. Using this material will always render in wire frame, which will help us visualize the scan to see around us. Return to BP-Pon. Create another custom event and name it toggle AR session. Also give this one single bool input named active. Connect to a branch. From true, add a start AR session node and set the session config to our AR session config. From false, add a pause AR session node. All right, we have the nodes we want to call. We only need to modify our BP toolbox by adding a tab to allow us to call these events. Open BP toolbox. If you have been following along from previous courses, you'll see that I have added a way to allow the user to change tabs. Know that I have done nothing new here, and I'll do a quick run through so we can start scanning our world. Originally, BP toolbox was set up as a single vertical menu with no tabs. I expanded on this to add a few buttons, tabs, and a separator. The separator here is simply a scaled cube to allow a visual separation of tabs switching buttons on the left and the settings on the right. The two buttons, meshes, and spatial mapping are set up the same way we built the spawn button on the meshes tab. And the meshes tab has had only one change to its logic. It will still spawn a sphere or a cube, but it will not destroy itself after spawning, as it originally did. That logic has been moved to this close button. Remember that to open this menu after it has been closed, a user must have their microphone on and say, world builder. If I move over the tab spawn component, we can see the options for spatial mapping. We have two switches here, which again have been set up the same way that cube and sphere are set up with the exception that I made no speech mappings for these buttons. Notice too that scan world and display mesh are not contingent on the value of the other. They can both be on or off respectively. I'll move the spawn options back to where they were, and let's finish up the event graph. We know that we have two buttons that need to communicate their value to our BP pawn when changed. The two, of course, are toggle display mesh and toggle spatial mesh. We have already prepared the bound events for both of them waiting for a button press. We'll start with on toggle spatial mapping scan. Right click and get the player pawn. Cast to BP pawn and convert the cast to a pure cast. Call our new event toggle AR session, and plug is checked into active. Copy the get player pawn and pure cast node. Pan a bit over and paste it here. Now call spatial mapping set visibility. OK, return to our spatial mapping map. Inside of the world settings tab, set override world gravity to true, and set the global gravity Z value to minus 50. Because we have scaled this whole scene down, the standard model for gravity must also be scaled down, otherwise our physics objects will simply shoot down rapidly. All right, let's scan our environment for use and test the collision using a spawned sphere. Press preview in VR. I'm going to do this part a little fast to show the pre-scanned environment, world builder. OK, our toolbox is showing and I'll choose sphere and press spawn. I'll close the toolbox and grab the sphere with my left hand. When I let go, it falls through the floor. Remember a few courses back. We set up our hand gestures to interact with blueprints that are using our blueprint interface. In the case of the cube and sphere that we can spawn, when they receive this call, they will simulate physics if gestured by our left hand and turn the simulation off if gestured with our right hand. Now that we've been in here for a bit, I'll repeat the process, world builder. And again, I'll spawn a sphere, grab it with my left hand and let go. It has hit the floor. Let's turn on the mesh to see what it hit exactly. I'll switch to spatial mapping and set display mesh to true. We can now see all the wire framing that our scan has created. If we feel happy with the scan and don't think we need to spend the resources to continue, we can pause the session and the scanning will stop. Let's do one more thing before we end this module. Return to our AR session config. Set use mesh data for occlusion to true. Now, when our environment is scanned, holograms that are located behind the scanned mesh will be hidden. The amount of detail that you'd like to scan can be found in the HoloLens plug-in settings. Increasing max triangles per cubic meter will allow for more detailed meshes, but with a larger performance cost. Spatial meshing volume size defaults to 20 meters. I've set mine down to three as that will work great for this room and any additional space is unneeded. Before we preview again, I'll double the max triangles to 1000 to see the scene in twice the detail. Press preview and VR. I've walked around the scene to get a good scan. I'm gonna lay my palm flat in front of me for the object spawner UI to display. I'm gonna put a floor about here. See what else? Put a wall, put a wall here, and yeah, I think that's enough. Okay, if we stand somewhere with an object between us and the hologram, we can see that it has been occluded by the object further immersing the user into the experience. In this module, we prepared our HoloLens to scan spatial data around us and prepared our project to handle starting and pausing the scans as well as whether or not to render them. We modified a method to allow users to set these options with an easy tabbed interface. After scanning was underway and the mesh representing the floor was in our scene with generated collision, our spawned spheres were dropped onto the floor and rolled around as expected. Finally, we instructed our AR session to use the scanned spatial data to occlude any hologram located behind it from the perspective of our camera. In the next module, we will review a system to create and manage persistent local anchors.