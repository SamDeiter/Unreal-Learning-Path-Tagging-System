Welcome back, we are continuing by taking a look at the resturization process and the G-Buffer. The resturization process is the conversion of 3D data to pixels, so basically render out the image of the models that we know exist in the space in front of us. The G-Buffer are the different images we are rendering out. We are not actually rendering one image. We are rendering a set of images that we can then use for compositing later on. Okay, so in order to render out the information, we basically have a grid of pixels, right? PixelSos2, which means the entire screen, the entire image, is a grid. And that means that if you render a polygon, say the blue line here is a polygon, we have to figure out which pixels, which squares behind that triangle are going to be colored or not because of that triangle. So the matter to do this is call to resturization process. This is done draw call per draw call. Doesn't impact you. There's not much you can do there either. Just explaining this happens in the background. But what you can do is something about it's over shading and that's something that's close to related to it and that does have an impact. A pixel can only ever be shown once. So before we get to over shading, make sure you understand this. This is a very interesting one. A pixel will only ever show one polygon at the same time. Maybe a part of the polygon or maybe the entire polygon, but never more than one polygon in one pixel. That has huge implications for over shading. So we know with absolute certainty that one pixel never represents more than one polygon. Here we have three triangles seen from very far away and zoomed in. OK, so there's one triangle here, which is the orange brown one here, which is essentially its triangle. There's a triangle in front of it triangle in front of it triangles. So it's three triangles overlapping each other. If you look at this from very, very far away, you can see it doesn't blend together. You might expect the colors to blend together where a pixel shows just the blend of these different colors from these different polymers. Doesn't happen. They stay very strongly divided. Very clearly divided between which pixel belong to which triangle. That means that if you have a hundred thousand polygon objects seen from so far away that it would be one pixel big, you're actually only seeing one polygon. But you're still processing one hundred thousand polygon. So performance wise, this makes zero difference. But it's still fun to point out, I think, that you're actually only seeing one polygon. Now when it renders out these pixels, it uses a pixel shader to calculate the precise shading. We're going to get back to what a pixel shader is in the next video. But for now, just move on. Let's focus on the over shading that comes with it. In order to do that, in order to shade it and shading means we have to tint it. Give it a color. Is what kind of texture is on top of it? All of that is affecting the color, the material and so on. Therefore, that polygon has to tell us what kind of texture is on it? Any information from the shading, from the vertexes, for example, or is it smooth at edges, those kind of things, vertex colors, any information we need has to be brought over. Now, because of hardware design, it always does this in a two by two pixel quad for processing. So we're not processing pixel by pixel. And we're asking pixel one. Are you actually on top of this triangle? If yes, can you ask the triangle for information on the texture, the color and so on? So you can color yourself accordingly. We're not doing that question pixel by pixel. We're doing it two by two pixels. That means that if a polygon is very small or very thin, it might shade four pixels while only one pixel is actually visibly filled. That is over shading in first stage. So here we have a grid of pixels. So the gray lines are the pixels. The orange lines are the quads. So these four pixels are calculated together. These four, these four, these four, these four, these four, these four, is grouped together in groups of four. Then we have a triangle here. So there would be a triangle here. And if you would color the triangle, these pixels would be filled up because the color of the triangle is covering these pixels. And in fact, this is slightly wrong drawn. It's not correct, but let's me point out what it does. Normally, it figures out if the middle point of a pixel is covered by the triangle, the pixel as a whole will be colored. So actually this one, the middle point is just outside a triangle and here the middle point is outside a triangle too. In reality, only this pixel would get covered. But let's just say, all three of them get a color for the sake of example. And what would actually have happened is that all of these would have calculators. Because when it figures out this pixel, it automatically also has the question about this one, this one and this one. So all of these pixels are processed. That's overshading. The fact you shade more than you should be shading. Now we got a second triangle coming in next to it. Right? I'll skip that slide for a second so I can talk on. So that means that basically these five pixels would be expected to be colored. This is what you would visually see again. Here it doesn't actually cross the middle, but ignore that. But this would result in these being calculated again. Because the moment we draw in this triangle, this is calculated again. All of these four and these four. This is overshading in second stage. So first stage, just the fact that you do more pixels than are visible, second stage, the fact that other polygons and other models that are covering the same areas, the same quad of four pixels are going to reprocess those pixels again. Okay? So here we have five pixels that are visibly filled with color. But we actually calculated one, two, three, four, five groups of four, twenty pixels, five pixels visible, twenty pixels calculated. This is four times too much. That's overshading. This is what it means in reality. Let's switch to Unreal. There is a view mode called Quatt overdraw. It shows you this. Go inside. You can see blue is good. Black is even better. I mean the colors here, that's totally good. Usually the color cannot go here. You only get a color that goes beyond four for things that are transparent. That's why the grass goes white. But as long as it's not transparent, as is the case inside where nothing is transparent, it stays in the first couple of colors. The further something is away, the denser the color gets. That's the thing. What this means is that objects seen from very far away are actually slower to render because there is more polygons covering a smaller number of pixels. Look, we have a sphere here, right? Remove the sphere away. If I'm right in front of the sphere, it looks blue. But if you move away, the sphere starts to go green. When green is the maximum, we can accomplish without transparency. Green is slow. So it means that the expense goes up if the object is seen from further away. There are more expensive to render if they're in gray density. When seen at a distance, the density automatically increases because you have the same number of polygons for that sphere, but fewer pixels to display the same number of polygons. The density of polygons per pixel is increasing simply when seen from a distance. You're supposed to use polygon accounts, reduce the polygon count at a distance using loading or culling to overcome this. You can clearly see the switch is statue. Outside, if you're up close, it's fairly dense. It has a lot of the base lot. But as you move away, it switches to the second lot and gets low poly there for also the overshading gets less. So loading helps with that. The more complex initial pixel shader passes, the more expensive overshading is. So forward rendering has a more expensive initial pixel shader pass. Therefore, we'll have a bigger hit from overshading than the third. So in the third rendering, the overshading is not the biggest problem. It's not extremely slow. It has a cost, but the cost it has is not huge. And there's plenty of developers and people who make entire games who simply didn't even look at overshading and they were fine. They survived. Performance was perfectly acceptable and so on. So this is not the most critical thing there is. But if you're going to use forward rendering and you really need every bit of performance you can get, for example, you're going to use forward rendering and a VR application, you probably do want to look at this. It will have an impact. That also means that very thin triangles affect overshading because they pass through many two by two cross. Here's your worst case scenario. You know, when I started with game development many, many years ago in the 90s and early 2000s, I remember everyone always told me in the beginning, please don't make polymers like this. If you have a cylinder, for example, make the polymers nicely evenly spread out over the circular space. Now, whenever told me why, the reason why is because of overshading. This is a worst case scenario. You have many very thin triangles and they all come to the exact same point. That means your overshading is going to happen along the entire length of polygons, along the entire length of all of them. And in the middle it will be massive overshading because there's so much overlap. Okay, so this is a reason why sometimes people say try to make your triangles more uniform. But again, nowadays we have so much hardware power that this will not make any difference unless you really need every bit of performance. Either way, as it has renderized it, it will now render out to the images. The images are called the G-buffer. The G-buffer is used for compositing in anything ranging from materials to lighting, fog and so on. So basically everything we do from this point onwards is based on a G-buffer not on the actual geometry anymore. We're not using the geometry to calculate things. We're simply using a picture where if you want to put it very bluntly, we're just photoshopped filtering from this point onwards. Instead of showing them in the slides, let me show it directly in render. Doc, because that one will show you the actual images themselves. So let's go to the bottom of the list here. So we get to finish the image. You can see here on the right the output this is the G-buffer. You have seen color deferred, which is dark. If you have a brightness slider here, if you change the brightness, you see what it actually is. This is the image itself with lighting. We'll get back to why the lighting is already here, but it's a static lighting only because it has basically textures. We'll get back to that. You have the world normal. The world normal allows us to record information on which direction a model was pointing in when we converted it to an image. The red axis shows you which pixels would point that way. Or if you invert it, we'd point you all the way around. Invert button somewhere, which I forgot where the invert button is. I can't find back. You can invert the view, but if you invert it, you basically have the same information, but from the left. Likewise there. Likewise there. So you simply store the information in all the different axes together and it becomes world normal. Then you will this one, which is a combination of metallic. We have a mask image. This is everything in the environment that is metallic. This is the specular. These are the specular values that you've entered in the material editor, rendered out separately from everything else. This is roughness. These are the roughness values from everything. So now we're basically building a images that track how shiny something is, how shiny should this pixel be and how shiny should that pixel be. And later on we can use them for further calculations. This is the result without any lighting. Then we have a couple of special ones. We got this one, which doesn't really help as much, but these spheres on the side of the screen, they have all kinds of different things like hair, rendering, subsurface, scattering and so on. So different materials as for the sake of demo. And this identifies them by color and gives some information on it. Likewise here, these are special cases it's used for identifying which pixels and which areas should behave differently. And finally we have the depth buffer, which is not technically part of the G buffer, but you know, you don't really see if you want to keep this simple, you can just say it's all part of the same thing because it's the same principle. It's an image that remembers the depth, right? So basically this looks for you. Now you can rescale this at will because it's just number at the end of the day and it remembers how far away each pixel is. So basically now we know the material properties per pixel, we know the color from the pixel, we know the direction the pixel is facing in and we know the distance the pixel is from the camera. That's the information we've stored in images. And using that information we can do all further rendering. These are the things I've just shown. There's one more thing which is called custom depth, custom depth ties further into this by letting you separate an object from other objects. So let's say we take, see what we take here, perhaps the tree over here, you can do this on any model. There's a property called custom depth, you can enable custom depth and that will include it in a separate render target, a separate G buffer. And that can then be used for all kinds of effects, outline effects and so on. And the easiest way to visualize it is just to use a higher resolution screenshot tool, which has a simple button here that says use custom depth as mask and you see what it does. It's not the only way of doing it, but you see you can green screen these objects out of the environment, including lighting and everything. If you then save it, you could press this, it will take a screenshot and it will dump it at a higher resolution than you have. In fact, you can include the buffer visualization targets, which would include all of the things just mentioned as separate images. So if you want to compose a video by rendering it in Unreal and then dumping the renderings as separate images frame by frame, including the entire G buffer, you can do that. You can then put the G buffer into a compositing program like Nuke and do further magic with it there because you have the information there. In fact, those buffer, the buffer is also visible here, buffer visualization. This shows all of them and a few additional ones or special ones, but this is very much similar to what you just saw in render doc. There is nothing you can do about any of this. It just happens. You can't impact it. It does impact performance, but on a very stable way, I mean, it always takes up the exact same amount of performance and you simply come to anything about this. The G buffer does take up a lot of memory. So if you ever get to a point where you feel you want to extend the engine to have more G buffers, so you have more information to do some effect with later on, do take into account there is a limit to this simply because of the memory. What we're basically doing is we're rendering up to 10 different frames per frame. You got 60 frames per second. We're basically rendering 600 frames per second, not 60, because you're rendering X number of additional G buffer images for each and every frame you do. And those are all uncompressed and they have to be transferred through the hardware of the GPU. So we're talking a huge amount of bandwidth to transport 600 uncompressed images per second continuously at 60 frames per second, roughly. And that wraps up this. And we'll move on. In the next video, to take a look at textures and afterwards materials and shaders.