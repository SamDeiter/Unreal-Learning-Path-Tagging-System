At certain times during gameplay, the player may need to be directed where to go to complete certain objectives. A level designer may be able to show them by placing actors in the world that mark their intended destination. Let's look at creating an objective marker to accomplish this. So I'm going to go ahead and start by creating a new actor class here in the Content Browser. Right click here in the Blueprints folder and just choose Blueprint class and Actors find for what we need. Let's call this BP underscore objective marker and we'll open this up. Okay, so we need a few different things here. We need some visuals that appear in Editor to make this useful to us that we can predict how it's going to be used and we can see accurately where it's placed in the world. So we're going to add some Billboard components for that. Beyond that, we also need to add a function to toggle the enabled state of the objective marker that can be called from the level blueprint or from other actors. We also need the functionality within the actor itself to disable the objective marker when the player reaches it. So we're going to add some collision and some collision related events to it for that. And lastly, we need an event to spatch her for when the player reaches the objective marker so that we can drive events in other actors based on that event. So right here we're going to change... This is the non-selectable billboard that appears when you create a new actor. Typically I want to replace this with something custom that can base the level designer utility of what I want. This is a blank scene that I just added. I'm going to rename it root and just drag that there so we have nothing in the world. Now I'm going to add my own billboard that will show me in the editor viewport where this actor is placed. So here's our new billboard. I'm going to change this to something more descriptive. The default on real waypoint texture. And I'm just going to go and move this up a little. I'll set it at... Let's see. Raise it to 50. And then I'm going to add another billboard. And this next billboard is going to use a custom texture that I've included in the project files. And this will represent the sort of diamond like waypoint shape of the objective marker. This will be grabbed by the highlighter for the exact world location. This doesn't need to be connected. All right. And we'll just type in a waypoint again. And that's our diamond shape. It's a little big. Let's just shrink it down. Point five and we'll raise it up to 200. Okay. And I'm actually going to name this something more descriptive. So there... We know it's just not another billboard. It's actually the location in the world of our objective marker. So let's name it that. Okay. So now we're going to add some box collision. Then I'll kind of as a trigger volume for the player arriving at this destination. Okay. So first, you can go to left view so you can see it from the side. I'm going to raise the box collision up to 200. And then for editor utility reasons, I'm going to go in the construction script and allow us to decide once this is placed in the world, it does need to be child of that. Once this is placed in the world, whether or not we want to use the trigger volume, it's included here. So to that end, I'm going to make a new variable. Call it use trigger. We'll expose it so we can actually change that in the instance. And I'll drag it here. And whenever I add a new variable, I like to add a category to it with the name of generally speaking with the name of the actor that it's part of. So if we have to access this actor from maybe the looper in script somewhere else, we know which variables are actually part of this actor. And they don't get lost in the shuffle of like all the variables in non-real. So I'm going to take the category here and just name this objective marker. So we'll drag all of our other variables under that category right here. So if I use trigger is false, what we're going to do is destroy this component, just remove it so it doesn't confuse anything. This might be if this actor is manually scripted instead of using the logic within the actor itself. So it just makes it clear. Also if we are going to use the trigger, we're going to set the box extends i.e. the size of the box with another exposed variable. I'm going to promote this to a variable here and drag it under the same category, expose it and by default, let's call this something more descriptive than box extend. Let's call it trigger extent or just how about size trigger size instead. And yeah, so let's set, let's make the defaults of this, let me compile and save it so we can set our default values of the trigger size. And we'll make it 200, 200, 200. And we'll go ahead and set use trigger to true by default also. So next, let's just test our construction script quickly here in editor. I'll just grab my new actor, drag it in the world and we can ignore that. And okay, it's a little, okay. Yeah, the trigger volume is actually too big. Let's find out what that is. Looks like this was doubled in scale somehow. We don't want that. You know, we'll compile and save that. And so now it actually sits on the ground, aligns with this icon in the world. You make your own kind of utilities like this that's useful to keep track of those things. You don't drive yourself crazy and you can predict how you can use these things. It's good. Let's test the properties here. So we have objective marker used trigger. You can see if we click it to false, the trigger goes away. And then we can change the size of it here by dragging our typing and value. So that's useful there. So let's go back and add some more functionality to this. So here in the event graph, just delete these items there. We're going to add a collision event here on component begin overlap for the box. And because the player, there might be some gameplay where the player walks through this trigger volume, but we don't want it to trigger anything yet. Maybe the objective is not enabled yet. So we're going to add a new variable here called enabled. Drag it into the same category. And the events will only fire from this event here if it's enabled. So we'll do branch check on this. And from here, we will just do a do once node. Typically speaking once it's triggered from the player reaching it, that's all we need to know. I always like to add event dispatchers for all of my actors. This is no exception. We'll just call this on objective marker reached. And then we want a few different things happening here. So let's do a checklist. And add another branch check here. And I'm going to do some more Boolean conditions. One is if it's enabled and if a new variable is true, we're going to call this disable on reached. Because what we want to happen here, if this is true, let's see if it's, we'll make it true by default. And enabled will link true by default. These are both true. We want to disable this turn off pretty much the subjective marker. So I'm going to make a new function that's going to run right here after this branch check. We're going to call this enable objective marker. And in this function, a few things are going to happen. We're going to pass some variables through the inputs. One just enable. That's true. And the second one, disable on reached. Because this is likely going to be called, or be able to be called also, I should say, from outside this actor. We'll set this true. Not to true, I'm sorry. We'll set the variable based on that function enabled to disable on reached. Then we're going to add another function, a call to the event graph, which is possibility to reset the functionality of this actor. So I'm going to add a custom event here that resets this two ones node. Just call this reset on enabled. Okay, so here in our enable objective marker function, we're going to add a call to that custom event to reset on enabled. Compile and save this. And we're going to add, well, first let's like add this to the right category. This is duplicate categories. Maybe you hit a space wrong or that kind of thing. We can clean that up later. But we're going to add some more functionality here that will enable us to chain objective markers together to enable one sort of as a series of markers that you'll see in the world. So I'm creating a new variable here that will be the objective marker class. And we're going to make this an exposed variable so we can choose a hard reference to it in the level. So we're just going to make that exposed in the instance and drag it up into the right category there. And I'll compile and save again. And here in the game world, let's have a look at some of our properties here. I'm just going to drag this by make a copy by dragging here and move it over to say another location in the level. And I'm going to link these two together. Now we won't be able to see the functionality yet here for the icons appearing in the world because we haven't done the work on the player HUD to reference these things. But yeah, it looks like we need, yeah, let's clean up the categories a little bit here. Just dragging these into the same category. So hopefully it groups them properly. So if I go back and I look, looks good, it looks like some of them aren't exposed. It should be. So I'll go in and I'll expose enabled and disable unreached. Those are options we want on each instance. I'll just reorder this a little bit. Whatever you make your own tools, it helps a lot to sort of organize all your variables. The whole structure of the actor class, just for usability. Now that we have our enable objective marker function complete, we need to go back and add it to the event graph. On the true results of enabled and disabled on reach, we want to call this. We want to disable the objective marker and disable unreached, we'll disable that as well. And we also want to do a check here. Our next waypoint to enable if this has been supplied with a hard reference in the level, we're going to check to see if it's valid. If it's valid, it means someone's a user has chosen this as the next objective marker to enable. So if that's true, if it is valid, we will just call enable objective marker on this next one. And it will work sort of like a breadcrumb throughout the level. Yeah, so in other words, we could chain these. This is kind of a test to show how it works. Not a test of functionality, but a test to show like how you use these in the editor. This one we wouldn't want enabled to start with. We want it to be enabled once the player reached this earlier objective marker. So if we pick with the eyedropper, we can pick the second one. Ah, but there's a problem. I can't pick this. This is important. When you use multiple sub levels like I'm doing here, you always got to watch which sub level your actor is placed in. So the first vector marker was placed in the milltown LD layer. Sorry, sub level. But the second one I played in the persistence, so I can't actually select it as a hard reference. You want to make sure to move it to the milltown LD sub level. Then that way you can actually select a hard reference with the eyedropper like this. So the first objective marker enables, it calls that enable function in the second one. We'll get an example of that working once we have the HUD fully working. And now that we have an actor to mark destinations for the player in the level, we will next need to set up the HUD to draw them as icons in the world.