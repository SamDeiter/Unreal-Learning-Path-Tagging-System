When working in the editor, you might have noticed some text under some of the nodes that say development only. Our print string node is one of them. This is a special flag on the node that tells the editor when we finalize our project to not run these nodes and it's a great way to use only parts of our code when we are testing or debugging. In addition, we have the ability to flag a node as disabled, which is a great way to isolate the code that is being executed to focus only on the parts we want when testing. Now by default, this is disabled and there are no hot keys set up for it. So let's look at how to take care of that. These are editor keyboard shortcuts. So let's open up the editor preferences, edit editor preferences and we're looking for keyboard shortcuts. Now we're looking for disabled nodes, enabled nodes and development only. Now we can search for those or we can go directly to the section. I've gone ahead and done a search for disabled nodes to show you this is under general keyboard shortcuts in the graph editor section. You'll notice there is no binding. You can set this to whatever you want. I'm going to go ahead and set a few shortcuts for these. We also have enable nodes. We have three options for enable nodes. We have enable nodes, enable nodes always and enable nodes development only. With the introduction of development only enabling, we have the ability to always or development only nodes. So the enable nodes hotkey itself is non functional. You want to set up always and development only. With those shortcuts set up, let's go ahead and look at how we can use them. Inside of my custom example here, I have a very simple setup like we've seen before. When the character jumps, it's going to slow them down and then for five seconds afterwards, they will walk slower. And if we test it, we can verify it works as expected. Now we've gotten some feedback that this is annoying and someone would like to test it without the slowdown. Now without the ability to enable and disable nodes, you would have to disconnect the nodes that you do not want and then reconnect the wires. With the ability to enable and disable nodes, we can temporarily or permanently disable them as needed. So I'm going to use the hotkey for disabling and you'll notice the wires coming in as well as the node itself has become a darker gray. These nodes are now disabled and will not fire. And if we test, our character no longer slows down. We can of course verify this with execution watching. When I jump, you'll notice it stops and it does not continue on. Now one thing I want to show you was even though you didn't see the execute wire going to the awesome jump event, it was still enabled. I've added a print string at the end after the awesome jump effect and we still have the set node disabled and when I jump, you can see it does execute and our text shows up in the top left. So when you enable or disable nodes, it only affects these selected items. Now I can go ahead and re-enable these by using my hotkey and you'll notice our wires come back on and the nodes are now enabled to get. Now for development, this generally only works on functions or things we can execute, not really setters and getters. I will go ahead and select my set max walks feed and use the hotkey for development mode and you'll notice nothing really happened. We can do the same thing with some of our getters down here for our peers and you'll notice nothing is happening. But if we do it on our custom events like let's say our awesome jump event, you'll now say now say development only. You can also toggle between development only and normal by setting it back to enable nodes always and it will even work on something like this print string node. So this gives you ability to alter the flow without disconnecting or reconnecting a bunch of wires and the ability to have things in development mode which is very similar to the way the print string works. Now there is one other way rather than the hotkey but it is an experimental option. We'll go back to our editor preferences, go to our blueprint editor settings, bare mental, advanced, you have the option here, allow explicit imp here node disabling and we'll enable it. We can go right back into our blueprints and if we right click now you'll notice we have the options down here for disable, do not compile or enable always and development and you can see the hotkeys that we've set up. So we can go ahead and set these back to where they were before using the right click option so enable always and that's how you have the option to have right click toggling rather than just the shortcut. This was a fundamental look at the concept of debugging and blueprints and some of the tools that the Unreal Engine Editor gives us when working with the Editor. When things go weird in the future you should now have a good start unidentifying what is going wrong and how to find ways to fix the issues.