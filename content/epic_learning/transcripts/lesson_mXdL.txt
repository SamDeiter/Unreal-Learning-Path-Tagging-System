Now that we have a way together all the static meshes in the project, we can take a deeper look at accessing class-specific information and using editor properties to modify object data. Let's start by taking a look at the static mesh class documentation. We're going to have a description of the class at the top and then a long list of editor properties. So what exactly are editor properties? Let's take a look at this first one here, allow CPU access to hop over to the editor. Look up the meshes folder and grab a static mesh at random. And then here in the details panel, I'm going to begin typing allow CPU access and you can see it's just going to give me the ability to modify the value associated with the allow CPU access checkbox here. So I'm not really worried about what that specifically is, but it should illustrate what all of these things are here. This is all of the data that you can modify regarding your static mesh and you have access to all that stuff here via the editor properties. So you'll see this a lot in the documentation, see get editor property and set editor property. So these two methods exist on a class that is inherited by many, many objects in Unreal called underscore object base. There are lots of things in here that I use all the time, get name is very useful and then there's going to be get editor property, which takes as its only argument the name and then down a little bit lower, you're going to see set editor property and a set editor properties method as well. And what this takes is the name of the editor property that you want to modify and then the value you want to set it to and then there's an optional argument here that we're not going to worry about too much, but that's where these live in case you're curious. So we can take a look at a very useful editor property called asset import data. But this one is going to return is an asset import data object. We'll take a look at how to access information on this here momentarily, but for now I want to go ahead and just write a little bit of code that returns to us the asset import data object associated with each set of mesh in the project. So I have created a function here called get static mesh data and I have commented out my little print statement here from the get asset class function that we wrote in the last module. I'm going to go ahead and add a line here that will return all of these static meshes in the project. So now that I have all the static meshes I want to iterate over each static mesh. So at this point it's useful to mention again that what static mesh is here is actually the static mesh object itself. It's not the path, it's not the name, it's the object itself. And because it's the object itself, I can call methods directly from the static mesh. And what I'm looking for here is get editor property. So you'll feed the editor property in as a string. And I'll just copy it here from the documentation directly. And we'll print it just to make sure it's working. We don't need this anymore. We'll reload. We'll call our function again. And here we see a long list of objects that are of type fpx static mesh in port data. So what is a static mesh in port data? What this is going to give us is the file path to the fpx associated with each static mesh in the project. So let's go ahead and do a quick Google search on asset import data. The docs between 427 and 426 are not different. There's no change in this in this class 5.0 maybe. So take a look at that once it's available. But for now we can go to be docs for 427 and we see three methods. And the one that we're interested in here is going to be xDract file names, going to return an array of type string. So I'm going to go ahead and copy this and lie back over to the code. Save it. And run our function again. And what we can now see is the path to each fpx associated with every static mesh in the game. So you can see a lot of these are exported from from a desktop location. And for a personal project that is totally fine, but where you working with source control, you probably would not want fpx is being exported into the game from someone's desktop. So it's just a good way to confirm that everything is being exported into the game properly. That can be very handy. The next thing I want to look at here. In our static mesh editor properties is. LOD group. LOD group is going to contain information that Unreal will use to auto generate LODs. Take a quick look here at a sample. We'll just grab this one again. So what we can see is in the LOD group for the static mesh, we don't actually have a preset already set up. But we can we can choose one before we do. I want to go to LOD and you can see we've only got one LOD that's LOD zero. But if I select one of these other ones, large prop. Let's see. So what does this saying is if we change this, it's going to go and regenerate LODs for us, which is, which is okay. So it'll think for just a moment and now if we go back to LOD, there we go. Now we have a variety of LODs here that will be swapped in depending on whatever LOD scheme we've picked. It can either be a distance to camera or screen space. So this is a significant optimization and fairly easy considering you can just set the value here in the details and Unreal will kind of take care of the rest of it. So let's go ahead and write a little more code here to figure out what the LOD group settings are for each static mesh. And if we need to, we can go ahead and set that value as well. So we no longer need to worry about the asset input, do the stuff, but I'll be right here. We'll go ahead and save this at a print statement. Head back over to the editor. The reload on our function again and here we can see that some of the assets have a preset and some of them don't. Now let's work mentioning that just because it says none here doesn't mean it doesn't have LODs necessarily. There's another editor property that we can use to determine that information or maybe a method, I'll double check in seconds. So if the user has created their own LODs and some other DCC application like Maya or something, then you would not necessarily want to do what we're about to do. But it's pretty easy to write some logic to go ahead and say if you had like a generic LOD configuration that you were comfortable applying to anything that didn't already have something. Just a little bit of a scary thing to do project wide, but it's easy enough here to demonstrate we're going to go ahead and write some logic. So before I add in the next part, this is review the documentation for set editor property. It's going to be looking for the name of the property that we want to modify and the value that we want to set it to. So we'll just take a look here at an example, we've got large prop capital L capital P. So this will actually fail because none is normally reserved keyword and you would not need to put it in quotes, but in this case, because we're actually clearing the return data from our get it a property, which is going to return this value as a string. We need to go ahead and set these quotes here so that this evaluates properly. Now we're to run this, it would actually go through every single static mesh asset in the project and begin generating LODs for anything that's missing an LOD group preset. But we don't necessarily want to do that as I said before in the case of a scenario where the user may have generated LODs in an external application. So we want to do one more check before we actually run this code here and we'll just take a quick glance back at the documentation for static mesh. I wasn't sure a moment ago if the number of LODs was an editor property or a method, it turns out it's going to be a method. So we've got this get num lots here. So before I use get num lots, I want to make sure that the data I'm getting back from it makes sense for the logic that I write for it. I want to comment these two lines out because I don't want to get any additional information than what I'm asking for here and what I'm going to ask for is the name of any static mesh that doesn't have an LOD preset. And then I'm going to ask for the number of LODs. So this get name method is on the underscore object base class, which is inherited by lots of things and unreal. And you can see right here get names is going to return the name of the instance calling this method. So let over to the editor for a moment. So quick reload. So here you can see that a lot of these have only one LOD. But certainly not all of them as we get close to the top here, you can see some of these have lots of LEDs already, but they don't have a preset LOD group. So what this means is the LEDs are created someplace else. So we want to preserve these. This is all looks like mega scan stuff. So that's probably a lot better than the quality we would get from something like using a large prop preset. But it's that's going to work fine for for everything else here. So let's go ahead and we'll just comment these out. And now I do actually want to get this information. Go ahead and add one more line of code here to see how many LEDs we have. I don't really do the stuff anymore get rid of it. What we're saying is if there's no preset LOD group and there's only one LOD, then go ahead and modify the LOD group without static mesh to the large prop preset. And I'm going to leave this active. So we'll print the first pass and then I'll run it again and we'll see that there aren't any extra stragglers there. So say that to reload. So we have the first run through where we're getting lots of nuns here in some mornings. And then if I run it again. You can see now everything that was a nun is now set to large prop. So we can assume that there is at least some LOD optimization going on with the static mesh is associated with this project. We've used editor properties to gather and modify important data regarding the static mesh objects in this project. We looked at ways to collect export file paths and review and modify LOD reduction settings. And the next module will continue our evaluation of static mesh objects by examining tribal counts per LOD.