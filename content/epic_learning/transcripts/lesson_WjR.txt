up until this point, we have been using C++ functions from Blueprint. Now we want to go the other way. We want to be able to call some sort of implementation that exists in Blueprint from C++. As I have alluded to earlier in this course, that is indeed possible, as long as the definition is included in C++, so C++ knows what it's calling to, and essentially we have a virtual method. So how is this going to work? Well, we are going to use a new keyword called a Blueprint Implementable Event, and let's have a look at the scenario that we're wanting to enact. So we've got our grabber here, and at the end of grab, we have this notifyquestActors function. And this notifyquestActors, I actually do not want to move this into C++ at any stage at all, because it's kind of a little bit hacky, and I probably don't want to cement it in there. I want to leave the designers open to doing this in a different way if they want to. So this wants to stay in Blueprint, but how do we get the execution to come here from C++? Because eventually I want to move grab and release into C++. How can I call to a Blueprint function in that way? One of the ways of doing that is using the Blueprint Implementable Event, and the way this works is you go over to your header file in grabber.h, and you can create a new U function. Always has to be U function if we're going to be calling it, or having anything to do with Blueprint. Instead of using BlueprintColable, which, as you understand, goes the other way, we want Blueprint Implementable Event. And this basically means that we aren't going to have any function body here in C++. That's all going to go on the Blueprint side, but we do have a signature here in C++, which allows us to call it if we want to. Now in this case, Notice my Quest Actor has got an argument, which is the Actor, that we need to notify, but it does not have a return value, so it's going to be avoid, notify Quest Actor, and it's going to take the A Actor star as its argument, and this is going to be just Actor. And we don't need to create a body. In fact, we should not create a body for this Implementable Event, because Blueprint is going to essentially create a null implementation for us. Notice we also don't have to mark this function as virtual, which might seem a little odd for those C++ nuts among you, because that's essentially what we're doing, is we're kind of using it like a virtual function, but don't worry, all the pre-process of stuff is handling that automatically for us, and creating a implementation for us to call in there. So let's go ahead and compile this now. And if we go over into Unreal, we'll see that because we've used the same name, we're now getting an error when it comes to this Quest Actor here. But what we should also see is if we go over to our Event Graph, and right-click in the Event Graph, we should be able to type in Notify Quest Actor. So that's interesting. What we've created here isn't so much a function in Blueprint terminology, as it is an event in the Event Graph. So now what we can do is this Notify Quest Actor that's giving us errors, we can probably just rename that, or so you'd hope, but because it's overwriting something that already exists, we can't now rename it, which is a little bit frustrating. We had to do the rename before we compiled our code. You go ahead and compile the Blueprint, you can actually see why it's giving us an error. You can see that it's actually saying that the access specifier's not compatible with the parent function, and we can see that because the access specifier is set to private here. So maybe if we said that to protect it, things would work. Let's try compiling now. It's saying that it's already in use, so I could go over to the Event Graph, and I could remove this event and compile now. But not finally, it does go back down to this one error, saying that the function name cannot be used. So basically, we should have renamed it before, but what we can do is we can move the functionality, just cut it out, and put it in the Event Graph instead. So that's what I'm going to do. Create a new notify quest actor in here, paste in my Blueprint Graph, and hook it up to that Event, and hook in the Actor as well that we're passing along here. Now, if we go ahead and delete the function from Blueprint, we should be able to see that in the Grab function, the Notify Quest Actor is still set up, calling essentially that function from C++. If we compile now, everything should be working the same. The only difference now is that the definition of Notify Quest Actor is in C++. The implementation is still in Blueprint. Now, it has moved over to the Event Graph, and it's in the Event Graph because it is a Blueprint Implementable event with no return type. Please note that in certain circumstances, which will go over, an event will not be created, and instead a function will be created here, and there are a few different situations in which this happens, but we aren't going to go into the details right here. Generally speaking, we do want Blueprint Implementable events to be functions in the Event Graph.