Tracking our hands using the HoloLens 2 was as easy as using the data that was already being collected. In this module, we're going to add new events to our BPPON that will take that tracked data and apply it to a posable mesh component for both hands enabling us to see them rendered. When all is set up, we'll preview the scene in VR and test the collisions. Let's get started in the hands map and press simulate. Select any of the puzzle meshes around the scene. Most of them are already simulating physics which would allow us to move them around dynamically, even them knocking them into the void if we so choose. If you have not experimented with physics simulation in Unreal Engine, it's very simple to get started. Grab any of the puzzle pieces and move them around. It will instantly interact with any other simulating mesh that collides with it. This puzzle piece alone on the island, however, is not yet simulating physics and is set to static. Stop the scene and set simulate physics to true. Notice that the mobility has automatically been set to movable. Press simulate again and see that all it took was a checkbox. While the physics options available to us can become intensely complicated, getting started and achieving the results most projects need can be done with a few parameters here. Now, let's prepare our pawn to render our hands and allow us to interact with these physics objects. There's lots to do, so let's begin. Open our BP pawn, add two posable mesh components. Name one right-hand and the other left-hand. Set both skeletal mesh to our new input simulation hands. While we'll need to fully solve this problem shortly, set the Y scale on the left-hand to minus one so we can differentiate them in the viewport. Under the rendering category of both, increase the bound scale to around 10. I'll need to increase mine even higher to make sure they don't disappear while I'm demoing for you. OK, we're going to build from our already created Track Hands custom event. Delete the debug draw node at the end as this is where we will pose our meshes. From the break XR motion controller data node, drag from hand index and create a switch node. Plug it into the loop body after the 4-H-E hand key point. We'll need to update both left and right every frame to make sure they are tracked properly. Because this is a resource-heavy mechanic, let's not waste any more than we need to. Before the switch, create a branch. We'll return to it shortly, but plug true into the switch. Let's begin with the easier hand, which would be the one that the mesh was designed for. The right. Some right on the switch node create a new set bone transform by name node with the right hand being the target. Go ahead and move this over a bit. Our first input is bone name, and we can use the enum-e hand key point returned from our loop to identify which bone is being referenced. The problem though is that the node is expecting the exact name of the bone, including spaces, or lack of spaces. If we open the skeletal mesh and look at the skeleton tree, we will find no spaces. So we need to make a quick solution for this. Let's create that as a macro. Name it remove spaces. One input named enum-e hand key point and set it as that type. And one output named return value of type name. From enum-e hand key point, convert the enumerator to a string. Be sure to select enum-to-string and not to string, as that will result in the byte data returning only a number converted to a string. Now dragging back from the output, convert a string to name. From string, still going backwards, create a select string node. Drag back from pick-a and create a contains node. Put in our converted enum-e into search-in. In substring, simply put a single space. Now the select string will choose a if there is a space in it. So pull from the enum-to string to create a replace node. In from, add a single space and in to keep empty. If there was no space, just plug the enum-to string directly into B. So all we're doing here is removing the spaces found in the enumeration. Drag a remove spaces macro into the editor and plug it into the returned enum value. Move the return value into bone name. We'll also use this macro on our branch to help save any resources. From right hand, create a does socket exist and plug its return value into the branch condition. Plug in the return value on our remove spaces macro into the in socket name. This node will return true only if the bone exists on this mesh. Next on the set bone transform by name is the in transform. We already have that data and we just need to make a transform from it. From the get nodes, make a transform, but keep the scale as 1-1-1. Plug the return value into in transform. For the right hand, we'll keep the bone space in world space. Alright, that wasn't too bad. Let's move on to the left hand. Create a new function named convert transform. It will need one input named tracked data of type transform and one output named adjusted transform, also of type transform. Before we open and build the function, let's finish the event. Plug in a reference of convert transform and plug it into left. From the make transform we made earlier for the right hand, plug into tracked data. Replicate the set bone transform by name node for the left hand using the adjusted transform output. Also set bone space to component space. Lastly, add a set world location with the left hand as the target, keeping this new location at 0-0-0-0. Alright, I'm aware how strange this looks, but it is to make sure we can spawn our pawn anywhere in the map and our left hand stays with us. Because we are manipulating the mesh in component space and not to world space, we have to take that extra step to move it back. Alright, let's open the convert transform function. From tracked data, we're going to compose the transform with the world location of the left hand poseable mesh so that we can localize it to its component space. Split the result struct. From location, make a transform. And also plug the rotation in. From scale, multiply it by 1-1-1 and plug the result into scale. Lastly, plug the return into our output. Before we test, let's add a static mesh to our index finger. Select the right hand component and add a sphere component. Fill it down to 0.02 and attach it to the index tip bone. Alright, be sure to compile and save, and let's try this. We can see that both hands are now being tracked and rendered, and there is the small sphere at the tip of our index finger on our right hand. The motion is nice and fluid. While the Skeleton mesh has no collision on it, our sphere does. I can use it to interact with the puzzle pieces around our scene. Even the sphere alone on the island that we made to simulate physics. In this module, we used the assets from MRTK to modify our BP-PON to render both of our hands which required a different solution for each. We created a new macro to assist us and a new function to convert to transform data. We placed a sphere at the tip of our index finger and interacted with our virtual environment. We got lots done. In the next module, we'll add hand interaction actors to our PON so we can pick various meshes up and move them around.