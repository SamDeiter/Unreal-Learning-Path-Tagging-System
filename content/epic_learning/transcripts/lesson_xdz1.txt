Let's start implementing these controls in the rick graph using various regnodes or rick units. I'll be referring to them as regnodes throughout this course. The type of node and their behavior depends on what kind of event chain you're using them in. As we saw in the earlier module that there are a few event chains in control rig and you can switch between them using this event mode tool up here in the toolbar. Now each of these options is associated with an event node in the rick graph. You can right click on the rick graph and search for events. This will show all the event nodes available. In this module we'll be focusing on the forward solve and backward solve. By default the event mode is set to forward solve and there is an associated forward solve event node in the rick graph as well. Forward solve is a traditional functionality of a rig created for animation where the animator can manipulate the controls to drive the bones. Now backward solve does exactly the opposite where you can drive the controls with bones. This allows you to make the bone data from a mocap animation onto controls for further manipulation using forward solve. I'll go more in depth on using the backward solve later in the course. But now let's set up a reg logic for all the controls that we created using the forward solve. To set up the forward solve event chain I'll start with the root and go down the spine chain till head. Then I'll follow it up with an iK setup for lens and finally we'll finish this by adding nodes for finger controls. For the spine chain or simple FK based control rig we'll use a combination of get and set node. Since and forward solve controls will drive the bones so we'll need to fetch the info from controls using the get node and use it as an input for bones using the set node. To create a get node just drag the root control from the rig hierarchy tab and drop it onto the rig graph. A contextual menu will pop up with the different options select get control. So this creates a get node in the rig graph with some default data prefil for the selected rig element. As the root control has a control type of Euler transform the name of the node denotes the same. The name is context sensitive as it changes based on the type of item you select under this drop down. Similarly based on the item type we can select any reg element of that type using the name drop down. In this case we can choose any control listed under the rig hierarchy tab. Then we can switch between global and local space. And the final bull can be used to switch between initial and current transform based on the selected input parameters we get a transform output from the node. Next we need to drive a root bone using the output transform for this get node. To create a set node we can either drag our bone from the rig hierarchy tab and select set bone or I can right click on the rig graph and look for set bone. The context sensitive menu will show the viable options select set transform under transform category and this adds a set transform bone on the graph. Now click on the name drop down and search for root and select that. Next connect the out transform from the get node to the input transform of the set node. Finally I'll connect the execute pin with the forward solve event chain. Now you can move your character in the viewport using the root control. Since auto compile is on by default you'll see the outcome of the rig graph in real time. One thing to note here in the set node is the bullion at the bottom here called propagate to children. By default this is set to true. Now this decides if the children of the affected item which is the root bone in this case will follow it or not. To better visualize it I'll toggle the bone visibility in the viewport. So click on character, bones and you can either select all hierarchy or you can hold shift and select this option to pin it on the viewport. This is a useful tip for all of you in case you didn't know about it. Now I'll go ahead and pin selected only option as well and then I'll toggle back to all hierarchy and you can see all the bones following the root bone. If I toggle off propagate to children then this just affects the root bone. So I'll switch back to my default options. Now I'll go ahead and use the same technique for all the joints in the spine chain till the head. Now that we have the entire spine chain all set up we can try and move a few of these controls in the viewport to check if everything is working as intended. Also it's better to organize your nodes for better readability so I'll go ahead and add comments and group these nodes. To create a comment you can just select the node and hit see on the keyboard. Next for limbs I'll use a basic IK node. I can keep connecting the nodes one after the other and make it like a one long train of nodes going across the graph which will get too long to keep scrolling so I won't do that. Instead I'll go back to the forward solve node, hold S on the keyboard and left click to create a sequence node. Now this helps us manage the nodes for different body parts into different sections. For the clavicles I'll go ahead and use the same combination of get and set node. And for the hands right click and look for basic IK. Select it and it adds this tall node to the graph with so many parameters. It might look a bit daunting but it's quite straightforward. This is the most basic form of IK with a chain of three join. Hence the name basic IK. Starting from the top there are three items to basically define the chain. These are item A and B followed by an effector item. So I'll select the bone names for each of them. For item A I'll select upper arm L as the first bone in the chain followed by lower arm L for item B and finally the hand bone will drive the entire IK chain. That's why hand L for the effector item. Then we need to provide a transform input to drive the effector bone. For that we've created a hand control. So I'll drag that and select get control. Then there are two important parameters where most users get confused. These are primary and secondary access to define the orientation of the poll vector. Talking about the poll vector let's first connect the poll vector input here and it is asking for a position vector. So let's drag the poll vector control and select get control. This time we don't need the entire transform we just need the location. So expand the transform pen and there is a translation pen. I'll connect this to the poll vector input pen. Now that the poll vector is done let's define these two up here. First is the primary access. This is the access aligned along the item. Now what item are we referring to here? In a three bone chain like this poll vector is where the middle bone in the chain points to which is the lower arm as assigned under item B. So we need to check the orientation of this bone. To do that you can go to view options in the viewport and turn on this option called display access on selection. This will display the access on selecting any regular limit. So if I select the lower arm L and press F to zoom in a bit we can see that X is pointing along the length of the bone. So I'll put one in X and leave other two as zero under primary. Similarly the second re-axis is the one aligned in the direction of the poll vector. We can see that Y axis is pointing opposite to the poll vector. So I'll put minus one in Y and leave other two as zero. Finally make sure that propagate to children is ticked. Almost done with the left hand as we are just left with the twist bones. For that we just need to set rotation. So I'll select both the upper arm and lower arm twist joints on the left hand and drag it into the paragraph and select set rotation. Likewise I'll select the twist controls and get control. Let's arrange these a little and now expand the transform pin on both the get nodes and connect the rotation pin with the input rotation pin of the set node. With that I'm done with the IK setup for the left hand. So I'll just add a comment for this entire group of nodes and then I'll copy paste the same for the right hand. To quickly update the values in these nodes I'll use a very handy function called search and replace. With all the nodes selected, right click on any node and select search and replace or mirror. In the search put underscore ill and in the replace put underscore R and hit OK. So this updates all the values like bone names and control names. Now I can just update the access value and I'm done. Next we'll setup our leg IK using the same techniques as the hand IK except for a few different nodes. So I've already added these nodes here in the graph. I'll just connect those and then we'll go through the setup. Since both the legs are using the same logic just like the hands I'll just go over the left leg setup. To start with we have the basic IK node where we are setting the three bone chain to be affected by IK starting from thigh, calf and then finally setting foot as the effector. Then we are passing on the pole vector transform and set the primary and secondary access values based on the calf bone orientation. The only difference is this new node called project to new parent that I'm using to set the affected transform. This node basically creates a parent constraint while maintaining an offset. So if I were to use just the get transform node from the foot control and connected as the input, then the ball control isn't valid anymore. And even if I do a set rotation on the ball joint using this control, it will just rotate the ball joint and doesn't affect the foot anymore. So let's switch back to the previous node where the foot joint is parent constrained to the ball control and we get the resulting transform as the output. Next I'm using a rotation constraint node to constrain the ball joints rotation to toe pivot control. This allows the toe control to drive the rotation for both the foot and the ball joint. Now all these extra nodes are used to provide better control to the animator while dealing with ground contact points. The animators can use just the toe pivot control to adjust the foot rotation with respect to the ground and use ball control to adjust the toe roll like this. After the foot IK setup I'm just setting the bone rotation for both the twist controls similar to the hand. That concludes the setup for both the legs as right leg uses the same setup. Next we'll setup the control logic for all the fingers. Instead of using a get set node for all the finger joints, we'll use a very useful node called per each node. It basically loops through a collection of area of elements and performed the desired operation on each element. To create this node right click and I'll type for each in the search and select it. Now it's asking for an area of collection of items to iterate over. Now there are different ways to create an area of items in control rig. I'll go through one using the get children node right leg on the graph and look for get children. This node outputs an area of all the children under the specified parent in the input. So I'll select the left hand bone as the parent. I don't want to include the parent as I just want the finger joints. Next I'll take the recursive option as I want all the successive bones and finally under type to search as specified bone. This helps me filter out all the different children if there are any. Similarly I'll create a duplicate node using control plus D and change the parent to the right hand bone. Now we have two arrays with all the finger joints. So I'll combine these using the append node. This gives me a single array that I can connect as an input for the for each node. Since we have all the bone elements, I'll just drag the element and search for set rotation. Now we need to provide an input rotation from the controls. So I'll just expand the element type and we have the name pin which contains the name of the bones. Since I named all my finger controls similar to the bone with suffix of CTRL for control. So I can add that suffix in front of the bone names to get the control. To do that, I'll drag out of the name pin and search for something in a common language like combine as I want to combine the suffix with the bone names. You'll notice that Unreal is smart enough to determine the type as name and show a corresponding function that does the same thing. But with the name, so under the name category, you'll see an option called concat. This helps you combine two strings into one with the bone name connected into a type underscore control in B. And the output result is now control names for all the fingers. I'll just drag out the result pin and look for get transform. Expand the transform pin and connect the output result to the set bone rotation input pin. That's it for the fingers. You can see how easy it was to cover a repeated functionality for so many finger joints with just a few nodes. Let's try and move a few of these finger controls to see if everything works fine. Finally, I have two more controls. These are of type bull. I've created these to control the visibility of extra controls as you don't always need controls for twist and fingers to be visible the whole time, especially when you're working on the primary body pose. This helps you avoid the clutter of controls in the viewport while animating. So for finger controls, I'll look for set control visibility node. I'll drag my control for finger visibility and connect the output to the node above. Now for the twist controls, I can either drag each control and use the set visibility node individually. Or I can use another technique to create an array or collection of regaliments. In the reg hierarchy tab, I'll search for twist and then click on this option button to hide nulls and imported bones. Also, I'll use hide parents when filtering. This gives me a list of just the twist controls in the reg hierarchy tab. I'll select all except for the bull visibility control and drag all of these into the reg graph. I'll select the last option that says create item array. So this creates an array with all the selected controls. In total, there are eight items, so the index goes from 0 to 7. The output is an array of controls, so I'll run a forage on this. Finally, create a set control visibility node and connect that twist visibility control output into it. Now that I've shown you how to use for each and different ways of creating an array item, you can use this process for any repeated task. For example, if we go back to our spine chain, you'll see the same logic being used multiple times and I can easily replace it with fewer nodes. So I'll switch back to my default options on the reg hierarchy tab. Select all the joints in the spine chain till the head, even the clavicle joints and drag them into the reg graph. Select create item array, run a forage on it. Use a set transform node for bones. Use concat to get control names. Create a get transform on the controls. Connect get control transform to set bone transform and that's it. Control drag the input pin and connect it to the new implementation. Now I can delete the clavicle implementation under the hand section. And if I move these controls in the viewport, they work just fine but with fewer nodes. So there are many possibilities with control rig and it totally depends on how you want to set up your reg logic. Definitely try to do a single thing in multiple ways to get a better understanding. With that, we are done setting up the control rig functionality using some of the reg nodes or reg units in the forward solve event chain. In the next module, we'll take a look at some of the methods to debug this.