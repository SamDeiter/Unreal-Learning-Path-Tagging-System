Welcome to procedural geometry based clouds. I'm Paul Neal for Epic Games. With this module, we'll be exploring shading models for the purpose of creating translucent, subsurface scattering clouds created with a standard sphere and displacements using animated procedural noise. As we proceed, we will gain knowledge in creating many of the standard nodes that are used, as well as learning some of the more complex nodes, such as Frenel, Depth Fade, Absolute World Position, Vertex Normals, and how to use time node to create procedural animation. To get started, we'll need to create a new material. Go to the materials folder and right-click and choose material. We'll rename this new material, M underscore for our prefix for all our materials, and we'll call this simple cloud. Now that we have the new material, we'll double-click on it to open the material editor. To get started, we'll need to make some changes to the base shader that we're working with. We're going to be looking for having some opacity, and you can see that the opacity channel isn't active right now. We'll also want to have the subsurface channel active, as well as being able to work with world displacement and the Tessilation multiplier. To make these changes, we'll go over to the Details panel, and you can see we have the Blend mode currently set as opaque. This is for our solid surfaces. We'll want to open that up and change it to Translucent so that we can be able to control opacity on the surface. The next one's going to be the Shading model. The default lit needs to be changed to Sub-Surface. Now Sub-Surface scattering refers to light when it passes through a surface. You can see this in, say, a wax candle or human skin when you put a light behind your hand or behind an ear. The light passes through the surface, scatters inside the surface, and comes back out as colors picked up within the surface. The next change we'll want to make is for the displacement. If we scroll down, we'll be able to find the Tessilation settings. Now Tessilation means that it's going to rebuild the surface of the asset forest by testulating each triangle into more triangles. There are two choices in the Tessilation, the Flat Tessilation, and PN triangles. Flat Tessilation will testulate the model by adding more triangles to every triangle that exists in the original asset. PN triangles will do the same, but it'll also try and smooth those new triangles together so that you get a smoother surface. We'll want PN triangles for our clouds because we want a nice smooth rounded surface. The next step we'll want to add a color node to be able to control the base color and the sub-surface color of our material. To do this, easiest ways to either find the Constant Vector 3 in the right-hand palette, or we can hold down 3 on the keyboard and left-click, that'll create us a Constant Vector 3 for us that we can plug in. I want to take this out into the base color and we'll change this color to maybe a subtle pinky hue by double clicking on it, and change the color maybe into the pinks, let's say, just a subtle pink color. Now, I could also take this base color into the sub-surface color as well, but what I'm going to do is duplicate this and use the second one so that we can control them individually. The easiest way to duplicate a node is to select the node and then press Ctrl and W, and underneath your mouse a copy of that node will be created. I'll take the output of this one into the sub-surface color and they'll both be the same color and get us started. For the next step, we'll control the Tessellation multiplier. We'll need a Constant for this, so one on the keyboard and left-click, and take the output into the Tessellation multiplier. The Tessellation multiplier controls how many new edges and new vertices are being generated in the mesh when it's being tessellated. We'll start this value, say, around a value of 1 so that we're not overdoing it too much. We can adjust this later as we're starting to fine-tune the shape of the clouds. Next we'll create the shape of our cloud with the World Displacement channel by using a Noise node. This node creates a three-dimensional cloud of noise in the material. This is in World Space, so we're going to take the output of this into the World Displacement channel, and you can see that the result is kind of a rough edge. We need to be able to control the amount of the noise, and to do that we'll multiply it by some factor. Create a Multiply node by holding down M, and then we'll create a Constant, and we'll control the factor with the Constant. We'll start with, say, a value of 10. You can see the result is that the vertices of the asset are all being pushed along the XYZ channel all the same time in the same direction. This isn't what we're going for. We need to be able to push the vertices of the asset out along our surface normals. To do this, we're going to create another node, and that will be the Vertex Normal WS node that's Vertex Normal World Space. This node retrieves the Vertex normals of the asset, pointing out perpendicular to the surface of the asset. We'll need another Multiply node, and we'll multiply the Vertex normals by our noise, and then multiply the result of those by our factor to get the amount of displacement we're looking for. You can now see that the noise is being pushed out on the surface of the sphere. We'll need a way to control the scale of the noise. If we select the noise node, you can see that there's a scale input value. However, there's no input directly into the noise. It's actually controlled through the position of the noise, and we want to scale that value up and down. We'll need a constant first to be able to control that scale value, and the scale values actually needs to be really small to be able to get the result we're after. We're going to say .005, for instance, to get started. We want to be able to take this value and control that position. This has to be in World Space. We want to right-click and type in World Position and create an absolute World Position node. Next, we'll create a Multiply node and we'll multiply those two together. Then take the output of the Multiply into the position. You can see that the noise has become much rounder, much larger in this case, because we've reduced the scale of the noise. Now we'll make a change to the type of noise that we're using. Select the Noise node, and in the function you'll see we're currently using this Simplex Texture Based. This is a high-quality noise type. We wouldn't make sure that our clouds are as fast as possible in our real-time environment. We're going to change that down to be the value computational, which is a low-quality solution. It uses less instructions per level to be able to generate the noise. You can see that the shape has changed, but we'll be adjusting this in the final map when we create the cloud with a sphere, and are able to adjust all of these values to be able to reflect the type of cloud we're looking for. So we'd like to have our cloud animating. Kind of unilating as you see clouds do in the sky and changing shape from one animal to another. To do this, we're going to use a Time node, and Time ticks over as the level is playing. Now our world position and our noise values are 0.3 values, or vector 3 values, that have an x, y, z channel to them. So we need to reproduce that with our Time, which is just a scalar value. We're going to have our noise only change on the z axis as it moves up through the cloud. To do this, we'll create an append vector node. Now, append vector allows us to take two inputs and output a value that would have, in this case, an x, y channel. I'll make a copy of that with Ctrl W and output the first one into the second. And now we have an x, y, z, or u, v, w, or an r, g, b, output coming out of the second append vector. Our time will go into the b channel of the second append, so that we're controlling what's the z value. For the x and y, we're going to make a constant, and just set those at a value of 0, because they don't need to change. We want to add the result of our Time output to the position of the noise. So I'll create an ad node by holding down a on the keyboard, and add the two values together, and then take the output of the ad into the position of the noise. You can now see that the cloud is changing shape over time. It might be too fast, it could be too slow, depending on the size, or the type of cloud we're creating. So we need some way of being able to control the time, the speed of the cloud changing. We'll create another multiply holding down m, and we'll multiply the time itself by some factor again, with a constant value. We'll set the default of this constant to a value of 1 to get us started. We need to be able to control the opacity of our cloud, so that it's denser in the middle, and more translucent on the edges. To do this, we're going to use a Frenel node. A Frenel node allows us to be able to control a fall off across the surface of the object. Faces that are facing towards the camera can be given a different value, as ones that are perpendicular to the camera. We can see how that works in this graphic. We have the direction to the camera from the pixel that's being drawn on screen. We also have the surface normal of the deformed cloud. If it's pointing directly in the same direction as the camera, it receives a value of 0 from the Frenel node. If it's perpendicular to the camera, it receives a value of 1. We can think of those values as black and white in this case. We can take the output of the Frenel node and put it into the input of the opacity. You can see the effect is the exact opposite of what we're looking for. We're looking for softer edges and a more dense center. We need to invert the value of the Frenel node by using a 1-node. The 1-node simply takes the value 1 and subtracts the input value from it. You can now see the edges are softer and the center has more density to it. We can control this fall off with the exponent input of the Frenel node. We want to be able to control that with a constant value again. Press 1 and left click and input it into the exponent in. And set the value to maybe a default value of 1 to get us started. You can see that's producing an even softer cloud now with much softer edges to it. There's one more step in controlling the opacity to our cloud. With an object interacts with another one and its transparent, you tend to see a clipping happening as one of the polygons crashes into the other. We want to be able to control the opacity as one object gets closer to another one. Let's say your clouds are clipping with the top of mountains, for instance. We want to be able to make them a little more transparent where that clipping happens and hide that edge from being seen. There's this node called depth fade. We'll create one of those. And we're going to use the depth fade node to control that fall off between the objects as it's happening. Depth fade node has an opacity and a fade distance value inputs. We want to be able to control both of those inputs with constants and have those input in. As default values, we might set the opacity to 1 and say about 1000 maybe to start with. This will again depend on the scale of your scene, how big your objects are, how big the clouds are, and that final look that you're after. We then want to mix the inverted frontal node with our depth fade parameter. Now, there's all kinds of ways you can blend these values together. The best one in our case is going to be one of the blend nodes and we'll use the blend overlay in this case to blend these two together. We'll take the output of the frontal into the base and the output of the depth fade into the blend and then take the output of the blend into the opacity. This will complete the nodes for our cloud material. Let's take a look at the results of our cloud so far. From the modes panel under basic, we're going to use a standard sphere. Drag and drop that into our map. Just move it up a bit and drag our m simple cloud material either directly onto the asset or into the materials channel. We can then scale it up and make it the size we're looking for so it better fits the scale of our environment. We need to be able to change and control some of the settings that we've set up within our material to be able to shape the cloud the way we'd like. To do that, the best thing to do is create a material instance that we can adjust in real time to see our results. To create a material instance, all we need to do is select material we're working with, right click on it and choose create material instance. We'll change the name from the default that's been given and use the prefix of MI underscore for material instance so we can easily search and find it. Once we have that material instance created, we can then replace the material that's on the object by either dragging and dropping it directly on the asset or into the material slot in the details panel. We can double click on the material instance and see the parameters that are exposed to it. The parent parameters, the one that actually holds the instance to the original material that we've been creating, double clicking on it allows us to be able to open it and now we can go about the process of exposing more parameters to the instance so that we can adjust and shape our cloud as we need. To expose parameters to the instance, we can simply select ones like our constant input for our funnel, right click on it and choose convert parameter. Now that it's a parameter, we can name them and in this case we'll call this one opacity. Then we can also set some default properties for it in the details panel. There's the default value, a min and max slider and those can be set if you'd like to be able to clamp the value in between different values. There's also a group that you can set and setting the group will allow us to be able to control a grouping of like parameters. In this case, we're going to group all of the parameters that control opacity of our clouds. We can then choose both of the inputs for the depth fade, right click on them and choose convert to parameter. Our first one will be called opacity depth and our second one will be called fade depth. We can select both again and then from the group, choose the group that we previously named called opacity. I'll go ahead and create the rest of the parameters for our material that will show up in the material instance. I've now created parameters for the noise size, cloud speed, the displacement amount, the testellation amount, as well as both of the color inputs that we can adjust the base color and the subsurface color of the clouds. These have all then been grouped together into several groups. If we choose the material instance, we can see the results. We have our cloud shape where the speed and sizes, our color parameters, displacement values for the displacement amount and testellation amount, and then the opacity with the fade depth, opacity and opacity depth. We can now make adjustments for the parameters to better shape our cloud into our environment. I'll take the tab for the material instance and just put it off to the side here. When the checkbox is off, the parameter is using its default value in the original material. I'll take the noise size, maybe we'll take this down even smaller to 0.001 to make these sort of the changes in the shape a little more subtle. We'll take the displacement amount and we can increase that much more just by dragging it up and seeing the result that we get. You can also see now that the speed is just way too fast and needs to come way down to be able to get that slow undulation of a cloud shape that we're looking for. We can turn on opacity and work with it so we can make it more translucent so that we can see through it more around the edges. There's also the depth fade and the opacity depth. What these are doing then is the fade depth is handling how close it gets to another object. As long as it's within this distance, it'll begin to fade out as it touches other objects. This will get rid of that hard edge that you see when a transparent object passes into another. Then you can of course also control that soft edge as it starts to pass into other objects and how translucent it becomes at that point. This allows you to be able to control it with the colors and get the exact kind of cloud that we're looking for. We've learned how to create procedural geometry-based clouds by setting up complex shading parameters to control translocency, subsurface colors, and displacements with desolation, as well as how to use a frenell and depth fade to control the opacity of assets and how to display them with animated procedural noise using the time node.