Hi everyone, I'm Deguri and I will be your instructor for this gameplay ability system lecture. This is the 9th lecture of the series and today we will build a growth system based on the experience attribute. Here are today's learning objectives. Lone, how to use global data tables to define and apply the initial stats for a character? Lone, how to data drive your experience systems using separated data tables? Lone, how to build a scalable data driven level of system driven by experience point? Before we begin, let me show you the research video of today's example. In this lecture, we'll create a simple prototype by placing three NPCs with different levels. And the player will grow by battling against lower level NPCs. These are the core components we'll cover in this lecture. This session is designed to reuse the elements you will already learned. In this engine exercise, we'll use gameplay effects, attributes, and gameplay cues together. Through this lecture, we'll finally complete the full cycle of gas. With this cycle, you will be able to establish the foundation of simple RPG system where characters level up. Now, let's see how a level of system is designed in gas. As you saw in the previous lecture, both gameplay abilities and gameplay effects provide a level parameter. Based on the input level value, gameplay abilities can perform extended actions, such as combo attacks. Similarly, gameplay effects can apply different stats according to level. These level based variations are systematically managed through data sets like Excel and Unreal Engine provides a feature called Data Tables to handle them. The image on the right shows a data table asset. Data tables allow you to manage level data across the whole lane effectively through tables or graphs. With these data tables, you can create a level of system where characters grow. There are two main approaches to implement this. The first is to use gameplay effects. In gameplay effect settings, you can link modifiers to data tables. If you pass the level value into the apply gameplay effect node, the corresponding data for the level is automatically applied. This way, you can implement different step values for character level without coding, simply through settings. However, if you have many types of stats to handle, manually assigning them one by one can be tedious. To address this, gas allows you to define rules within a data table asset. In the project roles, these rules are applied to set all stats values at once. In this engine session, we'll use this second method to implement a level of growth system. Now let's move on and implement this through a practice project. I have prepared the example A static file that we will use for this exercise. When unzipping, I will rename the folder to example 8. Now let's proceed with the basic setup for the exercise. Right-click the project file, while holding SHIFT, and select the Generate Visual Studio project files menu. Generate the solution file. Double-click to open the solution. Make sure the starter project is set to our arena-battery gas. Then proceed with the build. Once the build is finished, run the editor. For this lecture, I duplicate the level from the previous session and create the new label called step 8, setting it as the default level. The step 8 level is slightly different from the previous one. I placed three types of NPCs so we can test the character growth. Before building the level of system, let's first look at some additional assets prepared for this project. The first asset we'll look at is the gameplay queue asset. I prepare an actor and a gameplay queue so that a powerful flame effect will trigger upon leveling up. Let's first take a look at the actor. The actor is set to play a flame particle effect as soon as it begins. And it is configured with a lifespan value so it disappears automatically after 3 seconds. The gameplay queue, the spawns this actor, is named according to the same rule I explained in the previous lecture so that the gameplay queue tag is automatically assigned. I have already created the matching gameplay queue, that actor, the level of gameplay queue tag in the project. If we look at the implemented own execute function, it spawns the actor I showed earlier. Then it attach the actor so that it follows after being triggered. As the lifespan is set, it will follow around and disappear after 3 seconds. If you find any part of gameplay queue unfamiliar, I recommend reviewing the previous lecture. This time, a new folder called Data Table has been added and it contains 2 data tables. The CT character set default asset is a data table that contains the character attribute values by level. As the level increases, the max health and health values increase. The CT combat set default asset is a data table that contains combat related attribute values by level. Since the level increases, the attack rate value increases. However, for simplicity, the attack range value is kept fixed. If you would like this value to also change depending on level, you can freely edit it. We will leave that as optional practice for later, but in this hand-junk session, we'll use the provided values to build the level of system. First, let's assign starting level to the player and place the characters. For this, let's go to Visual Studio and declare an integer variable called start level. Add the Add it to anywhere keyword so it can be set from anywhere. In blueprint, this will be read only. Then go back to the constructor and set the default value to 1. Once complete, compile through live coding. When compilation is complete, assign starting level to each NPC. For sign level 1 to the 1, on the file left, 10 to the middle, and 100 to the 1 on the right. With these assigned values, we now need to initialize the stats for each character. There is a way to do this using gameplay effects, but in this hand-junk, we'll use the ability system global, which manages gas-global settings to perform a bulk update. To do this, let's first go to project settings. Go to the gameplay-ability system settings section. Register the two data tables I showed earlier into the middle, global attribute set default table field. To use a global table, there are some special rules. If you open the character attribute data table again, you will see the column names start with the prefix AB character. This prefix is set to distinguish it from other data, while the rest, character attribute set is the class name. And max health is the attribute name, which is a class member variable. Some names in the data table must follow this rule. By structuring the data table this way, in C++, you can use the AB character prefix to feel all related attribute sets and properties for the current character at the specified level in one goal. To apply these changes, the editor must be restarted, so close the editor. Now, let's work on the code. First, include the headers needed to fetch global data table information. First, include the headers needed to fetch global data table information. In the post-initialized component function, there was a logic that manually sets base values. We move this, instead add a function that fetchs the default values from the data table. First, get the module. From the module, access the ability system global object. From this object, use attribute to set initter and code init attribute to set default function. For the parameters, provide the prefix AB character, and the start level value we set earlier. Finally, pass in true to initialize the attribute values. Compile the code and restart the editor. Before starting, go into the NPC. We previously fixed, it just starting to take power to 50. Remove this setting. Now, press play to check the wizard. You can see that each NPC has a different health value assigned. And moving to the right, you will notice that the attack power also becomes stronger. With the level system applied correctly, next, we create a system where characters grow according to experience point. First, go to the data table folder and create an experience table. Name it CTEXP set default. Your class name for the attribute to set will create its EXP attribute to set. And here, we will specify the amount of experience each character drops. Once the table is created, let's now create a new attribute to set. Go to the new C++ class menu. Create a class named ABEXP attribute to set with attribute to set as the parent. Once the class is generated, move over to Visual Studio. Press the reload all button to refresh the project. Go to the ABEXP attribute to set head of file. First, let's include ability system component.h. Declarate a constructor. Declarate the EXP gained attribute, which stored the Torah experience accumulated so far. In Blueprint, it will be read only. Also, declare the EXP level attribute, which stores the level calculated from this experience. Add the max attributes for each of these. Finally, add the drop EXP attribute, which stores the experience dropped by your defeated NPC. Now, use attribute to access the macros to declare functions for each attribute. To validate the values, bring in the pre-attribute to change function from the parent class. To synchronize level changes, whenever experience changes, also bring in the post gameplay effect execute function. When experience is accumulated and levels increase, several related tasks are needed. Stats must be reset, and the UI must be updated. To handle this, declare a delegate that sends both the old and new level values. Use declare, dynamic, multicast, delegate, two paroms to declare a type named app-level change. Set its parameters to old EXP level and new EXP level. Then, declare a delegate variable named own level of this type. Use the Blueprint or sign up keyword so it can be bound in both C++ and Blueprint functions. This completes the work needed in the header. Next, let's implement each function in the CPV file. To implement the post gameplay effect execute, you first need to include gameplay effect extension header. Now, we'll write the definitions for each function. First, in the constructor, limit the maximum level and experience to 1000 and 9999 zero respectively. Set the default EXP level value to 1 and the rest to zero. After setting the initial values, in pre-attribute to change, add logic to ensure values are always valid. Start with handling the EXP level attribute. Climb it so it doesn't exceed the max value. Since the level data will always be handled as integers, also truncate any decimal values. Similarly, ensure that EXP gain doesn't exceed its max value. This time, the level should change as experience accumulate. Now, let's implement post gameplay effect execute. When EXP is gained, we need to calculate the level from it. For this, I prepared a very simple formula. The formula divides total experience gained by 10, then adds 1 with the result being the level. First, fetch the current level value and store it in old EXP level. Then, we calculate the level from the updated experience. If the new level is greater than the old one, update the EXP level value. Broadcast to any functions, subscribe to the delegate. Now, let's move to the base character declaration. Here, below the combat attribute to set, add the EXP attribute to set as well. Then, declare a function to link to the EXP attribute to set delegate. Create on level of CPP. And move to its definition. In the constructor, like the other attribute to set, create the EXP attribute set object and register it with ASC. Add the header. Then, add it to the ability system component as well. Now, the attribute to set object has been initialized. Then, bind the own level of CPP function to the own level of delegate. While attribute manage level as floats, abilities and order system use integers, so set the start level value from the editor as the initial level. Since the value might change during setup, reload and convert it accordingly. Now, let's implement the level of logic. When leveling up, fetch the new level values from the global default table and fill in all stats. Convenient, isn't it? Then, compile, build and launch the editor. Next, when the NPC dies, it should pass it to drop EXP value to the player. Set the gained EXP attribute and assign the drop EXP attribute to it. To do this, go to the GEP folder and create a new gameplay effect called BPGEdropEXP. Set the gained EXP attribute and assign the drop EXP attribute to it. Then, return to the NPC and create an applied gameplay effect to the target node. Connect the target from the instigator, which holds the player actor who attacked. And, assign the BPGEdropEXP effect. Here, we need to set the level value, make some space, fetch the EXP gain attribute to set, and connect it to the level input. Retrieve the EXP level value, but specifically the current base value. Next, get the ability system component of the instigator and connect it to the target. With this, we've added the logic to grant the experience to the player. Then, go to the player character. Here, when the level of event is triggered, we'll also trigger a visual effect. Add another execution pin to the sequence node. Use the ASC's get attribute to send node to fetch the EXP attribute to set. Create an event called onLabelOpp bound to the onLabelOppDeligate. In this event, use the execute gameplay queue on actor node to play the level of gameplay queue with so earlier. Finally, let's sync the level value to the hot UI. In the WBP hot widget, just like in the previous lecture, create a custom event called bindEXP attributes with EXP attribute to set as a parameter. Similarly, bind an onLabelOpp event to the onLabelOppDeligate. When the level of occurs, update the current level value. Then, code a pre-made refresh level function. Compile and save. Back in the player character, code bindEXP attributes we just made and connect EXP attribute set as the parameter. Press play to test it. When you defeat the left NPC, use the player grow to level 11. Defeating the middle NPC will grow the player to level 111. Finally, in the battle with the level 100 NPC, since our level is now higher, we can defeat it, and our level rises to 611. And that's how we built a level of system that scales with data tables. After this lecture, try modifying the data tables to create your own immersive level systems. That concludes this hand-john session. Let's summarize what we've learned in this lecture. With the level system provided by GAS, you can design the character growth structure. In addition, gameplay ability global allow you to broadly extend the functionality of GAS. Finally, by structuring data tables according to GAS rules, you can efficiently handle largest scale data. Try using the provided step A level to practice implementing different effects based on the gameplay effect. That concludes today's lecture. Please look forward to the next session as well. This has been Duku-V. Thank you.