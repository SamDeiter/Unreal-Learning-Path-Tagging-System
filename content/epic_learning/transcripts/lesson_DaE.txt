Let's move on to setting up our object rotation now, and also add some slight adjustments to each of the mesh actors positions that we can better control or fine tune the end result. We can start by dragging out another random seed instance variable, and from its output, add a random float in range from stream node. As this will control our random rotation, let's set the max value to 360, so that our objects can essentially be in any state of rotation at all. Again, this could be another variable that we expose so that the end user could choose to scatter more artificial mesh actors. So, if they wanted boxes in a warehouse, all facing the same direction, for example, they would be able to do this. For now though, we will keep it simple as we know we are scattering organic mesh actors that have an organized chaos when it comes to rotation. We will drag out from the return value of our random float in range node, add a make rotator node, and then while it's holding down the control key, change the input over to the ZRZ your input. Just above this new set of nodes, let's set up our static mesh actors position by dragging out a new spawn point variable instance, and choosing the get option. We can next add the ability to make some fine tune adjustments to the Z spawn point if needed, and so after dragging from its output, we can add a vector minus vector node. In the Z field, let's punch in a value of 10 to adjust the position of our spawned meshes. We can of course come back and adjust this when our blueprint is complete. We could even once again make a new variable to control this value and make it public if we wanted access to this at all times in our level. We will drag out from our new vector minus vector node, and locate and add a make transform node. We can then pipe our make rotator node into the rotation input, and the make vector node into the scale input. And with that, we have set up the position, rotation and scale controls of our mesh objects, as well as having added clustering and scale file off into the mix as well, which means we're getting close now to finishing off our scatter tool. Though, there is still the task of selecting which meshes are to be spawned that needs taking care of, and also the need to add a random seed option to that very selection.