In this video, we use the tile map we created in a previous module as a basis for a playable level file ahead of creating our player character. We'll create a new level file, add our previously created tile map and finish up by adding a player start point to the level. Let's get started by creating a new folder to store our level file. Using the content browser, navigate to content, my content and create a new folder called levels. Open the new levels folder, right click and select level from the create basic asset section. Name this new level file, my tile map level. Double click the new level file to open it. Now in the central viewport, we can see our blank level file. Let's bring our tile map into this new level. Create to content, my content, tile maps and drag the tile map we created in the previous module directly into the main viewport. Next, let's head over to the details panel with the tile map still selected and find the location settings in the transform rollout. Set the x, y and z values to zero. Even though our final game will be viewed from an orthographic 2D perspective, we're still positioning the assets in 3D space and it will make our lives a lot easier if we start from this zero position. Now that we're happy with the position of the tile map within the level, we can change the viewport to an orthographic perspective. Select the right view from the viewport toolbar. By default, this view uses the brush wireframe render view. While this is often useful in a traditional 3D game, it's not very useful for us. Change the view mode to unlit so that we can see what we're doing. And then we can reposition the view until we can see the entire map clearly. A quick way to build out tile level is to create new instances of our tile map. Let's do that now by selecting the tile map from the viewport, holding down the alt key on the keyboard and left click tracking with the mouse. When we release the button, we'll have a new instance of our tile map. Now let's zoom in a little and try to line up these two tile maps. It's at this point you may encounter an issue. For most grid snap sizes, we can't seem to snap the tiles together correctly. This is because the default settings for the generated grid will not put subdivisions on the grid in the right place to match up with our tile map grid. We could use a snap size of 1, which we've used in the past. However, this becomes awkward and clumsy when we're putting together lots of tile maps. Luckily, there's an easy fix of this for all grid sizes. Select edit, editor preferences in the editor. In the search dialog, search for power of 2. Check the checkbox for use power of 2 snap size. Once that's done, we can close the editor preferences window. Now, when we try to reposition the tile maps, we'll note that they'll neatly snap together. And, the FB open the snap sizes dialog, we can see that they're all piles of 2. Go ahead and place a few more instances in the level if you'd like, making sure that they are all snapped together perfectly. Our level file is now looking pretty functional, but we're missing one vital piece. Something within the level to instruct Unreal Engine to create our player character when the game is run. To address this, we'll add a player start actor. From the modes panel on the left-hand side, locate the player start in the basic category and drag it into the viewport. The player character will be created at the same location as this player start object, so we need to make sure that it places the character at the correct depth in our scene, in front of our background, but behind our foreground layer in the tile map. As we place our tile map at Y0, we can consider the foreground layer to also be at Y0. And so in order to place our character behind that, we should position our player at Y-4. Use the location section of the transform to adjust this value for the player start. With that completed, save the level. Now, start a play in edit session by pressing the play button in the toolbar or pressing Alt P on the keyboard. Note that for now, we're still using the start-to-content player character, but we can at least see that they're being spawned correctly and are interacting with the collision of our new tile map. All that we need to do now is finish building our own character so that we can instruct the player start actor to use that instead. In this video, we created a brand new level file and populated it with two important assets, the tile map asset and the player start actor. We added our previously created tile map into the level file and adjusted its position to be consistent with any future additions to the level. We created several instances from the single tile map to expand the size to the level. We also adjusted our snap grid size to allow us to perfectly align multiple instances together within the level file. Finally, we added a player start actor and positioned it so that any player character created by it would land neatly onto our tile map's collision.