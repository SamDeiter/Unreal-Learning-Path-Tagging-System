Now that we have the basics down, we're ready to take a look at some documentation. We'll start with Unreal.EditorAssetLibrary, a class that exposes functionality associated with the content browser. Using this utility class, we'll get a list of all the file paths for each asset in the project. Let's take a look at Google. I've entered Unreal Python API, and what you can see here is 427 is what is popped up. We haven't quite got to the release of 5.0, but the docs are out there. It's important at this point to note that when you're looking for documentation on the Python API for Unreal, you're going to want to make sure that you're paying attention to the version. The versions don't change that much from one to the next, but there are occasionally differences. It's a good idea to just try to stay as current as possible. We'll take a look now at the Unreal Python 5 docs here. It's not all classes. There are some other object types here as well, but there are many, many things in here. I think it's approximately 5,500 different objects. There's a lot to sort through, if you're looking for something specific, but we'll talk about strategy for that here in a moment. Before we get to that, let's take a look at this Unreal Python API introduction link. It's going to be a short little blur, but there's a very useful link here, scripting the editor using Python. This has got to cover some of the stuff that we've already talked about in terms of which plugins need to be enabled and how to run the Python scripts. This is a great page to read through as you are beginning your journey with Python and Unreal. What we're looking for here is how to access functionality associated with content browser. If I come over to the quick search feature and type in content browser, what you'll see is quite a bit of data here. This isn't really going to be an effective strategy for tracking down something that you might be looking for. Fortunately, Google has already poured through all of these documents and is very, very good at giving us the data that we're looking for. All we need to do, really, typically, is start with Unreal Python. Then whatever you're trying to do, you just go ahead and type that in. In this case, we're looking for the content browser. Here we have Unreal.EditorAssetLibrary, 4.427, which at this point is going to be fine for what we're looking for. Right at the top here, we can see there's going to be a description of the class. This is going to give us some common functionality associated with content browser. If you have source control in your project, you can access that source control, check things out. You can save things. You can delete things. You can find out if things exist. You can duplicate things and so on and so forth. There are a lot of methods here, but the one that we're going to be paying attention to in this module is list assets. So what we're going to have initially is what the arguments for the method are. In this case, there's going to be three, one of which is required and two are optional. The parameters for the arguments are going to be here. We have directory path. That's going to be a stream, which would be just a path to the root folder essentially that we want to start looking in. Recursive is a bullion and include folders also a bullion. You can see what those default values are here. It's going to return an array of strings and each string will be a path to every asset that finds underneath your directory path. So let's go ahead and write a little code here to see how we can access list assets. The first thing I like to do. Especially with some of these longer class names. Just go ahead and copy it directly from the docs. I'm going to create a little function here called list asset paths. And we'll create an instance of the editor asset library. Now I'm going to create a list to hold the return values. Set this list equal to. The list assets method. So the only thing that we need to add here at this point is the root that we want to start looking in. Let's take a look at Unreal. Try to figure out what that might be. So if I click on the very top folder here, I'm going to get forward slash all. The next one down. It's going to be forward slash all for slash game. It's called content, but as far as the directory. I'm really like to call it forward slash game. So let's take a look at what happens if we try to use forward slash all. And just to confirm that everything is working, I'm going to go ahead and print the asset paths. This is Python 3. So we need to make sure that we're putting our print statements in parentheses. Go and save this. Or reload our tutorial script. And now we can call the list asset paths function. And what we get is an error. It can't convert the path forward slash all because it does not map to a root. So what we're looking for is a project root here. So that's going to be content for in this case, the network project. And the path for that is going to be forward slash all for slash game. But just to save us a little bit of time, what it's really looking for here is forward slash game. It's always going to be forward slash game. If you want to find the root folder of your project. This is the address, even though it's called content, the content browser. That's what we're looking for. So I'm going to go ahead and save this. We can reload our tutorial script. And we can call the list assets function again. And here you can see we get the path for every asset in the entire project. So this is going to form the foundation for a lot of what is coming. Because we'll be doing a lot of sorting through the every asset in the project looking for all of the static meshes or other kinds of assets that are available and unreal before we move on. I have one more thing I'd like to point out. There's a consistent capitalization scheme across all of unreal documentation. That is each class is going to be prefaced with the lowercase and real. And then the first letter of each word will be capitalized. For each method, it's going to be lowercase with an underscore. So this applies to properties as well. We'll take a look at that when we get to a class with properties. And as far as my code goes, I like camel case. So if you see a function that is using camel case or variables that are using camel case, that's something that I have written. So if you're you're looking through the code, you can easily identify the stuff that is something that I've written for the course, something that comes from unreal and as a class. Or something that is a method of property from an unreal class. So another comment task is to get the user selection from either the content browser or the world outliner. So we'll take a look at that in the next module.