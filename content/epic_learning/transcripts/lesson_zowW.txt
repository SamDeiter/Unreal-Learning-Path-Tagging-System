One of the most useful components in the camera system, besides the camera, of course, is going to be the spring-up. This is one of those optional components in the pipeline, which the unrelangeneers have realized that would often be manually created or implemented directly into a camera setup, and they've therefore implemented one flexible component for developers to simply drag and drop into their setup. If you're following along with the example project, I've purposely set up the character class here with just the camera attached just above its head, floating kind of awkwardly around, and it really doesn't feel great to control. It feels very snappy when we move around. As we can see here, it just generally isn't going to be an easy thing to improve or build upon in its current state. It's somewhat an over exaggeration, but it really does go to show once we've made some small, simple changes with the implementation of a spring-up component, just how much better it can make the control of our character fill. Inside of our player or character class, we can see to the left-hand side, I've already implemented the camera component in C++, as I knew that we'd need this a little bit later, and I wanted something to demonstrate us looking through. When it comes to implementing this on the blueprint side, this is very, very simple, but just to make sure that everything's covered in both options, as I've mentioned, we will go through the process of implementing a camera and a spring arm in blueprint or inside of the editor. In this case, as we won't need any actual blueprint code. To create these components, of course, as simple as going over to the component section, we'll click on add. The first thing that we'd normally do is implement the spring arm. We'll look for the spring arm component. We'll add this here. The general hierarchy that we're aiming for is we would want to add the spring arm component to either the root, or in this case, the capsule component we have here. Then we'd nest the camera onto the spring arm. What we really want to do is try to keep the camera at some default values, the default rotation, location, which is already something we've broken a rule of here. I've added a location offset and a rotation offset to get this hovering somewhat above the character's head. We'll return to this one a bit later to fix this one up. What we can see now is that we have the spring arm component on the root component. This gives us this red line. If you haven't seen this before, this is basically the distance away from the character that the camera will be setting. The next step would be to place the camera onto the spring arm. Of course, because this was defined and implemented in C++, we won't get the option to do this. Just for demonstration purposes, we'll add another component and we'll find the camera. We'll add a camera component to the spring arm component. You can see, as I've mentioned, that the camera is now sat that far away from the character based on the spring arm length. We'll be looking more at the features of the spring arm in the next topic. This is really just the implementation and how to get them to exist in the class. This is the default setup of a spring arm and camera component if you haven't worked these before. What we can do from here, if we wanted, that would be to start adding some rotation and values to set the exact position and where we wanted the spring arm. To look at the character, the final implementation will be in C++. What we can do for now is we're just going to get rid of these and jump over to the code file. I've already started. This has taken me exactly to where I want to be, which is in the character header file. If you're not already in the header, then jump on over there. We can take a quick look at what I already have implemented here. This is no different from implementing any other component in C++, really, when you're wanting to nest it in the hierarchy in the way that we'll be doing here. All we have is a U-camera component, which I've named camera, with a U-property of edit anywhere, a blueprint read right, and a category of camera. Of course, I've also included the pull declaration of the U-camera component, which allows us to use that in this class. In a very similar way, we'll want to pull, declare the U-spring arm component, which is the next thing that will be implementing. So, I'll just drop this down a line and provide the U-spring arm component here. But I'll just drop a couple of lines down here. Again, we're just going to implement the U-spring arm component, I'll name this one spring arm, and provide this the same U-property. So, it will be again, edit anywhere, blueprint read right, and in a category of camera, this just means that we can have for lack tests to updating and editing this in both the editor. And we can also change the properties and details of it in blueprints if we wanted to kind of do some quick dynamic tests, which will be especially useful for a more dynamic camera system. So, moving on over to the codefile now, as again, with any standard approach when implementing components, this is just the finding that we want a spring arm component in the class. What we then need to do is jump on over to the codefile. So, the accompanying character codefile, and in a very similar way, we can kind of take reference from what is already implemented from the camera component. So, just to cover this, again, we're saying that the camera component, which has been defined in the header, is now going to be created. We'll do that using the create default sub-object cool. We'll pass in the type of U-camera component, and we're going to name this one camera. And what I've done here, and we will need to update this in just a moment, but I'm setting the camera to be attached to the root component. Now, ideally, as I've mentioned, this will be attached to the spring arm, but of course, at this stage, we haven't had the spring arm kind of existing at this point. So, just above the camera, again, I'm just going to now create the spring arm. So, again, we'll be using the create default sub-object, and we'll be passing in the U-spring arm component as the type. I'll name this one spring arm. And again, we want to make sure that we set up the root attachment so that there's no ambiguity or uncertainty when it comes to what this component will be attached to. So, the root component in the character class, if you're not familiar, is going to be the capsule collider, which is exactly where I wanted this to be attached. And then we can make that other change that I mentioned, which is basically going to be copying the spring arm, and we'll be making the camera attach to the spring arm component, like. Now, we've got some red squigglys. This is probably just because we need the other include. So, just to mention that I've already included for us the camera forward slash camera component.h. If you're doing this in your own project, you'll need to include this as well. And of course, for the spring arm, we need to include the game framework forward slash spring arm component.h. With that included, we can see that the errors have gone away. And that has the relevance information about the components that we'll be using. And that's really all we need to do is the bare minimum to get the spring arm component and the camera implemented in C++ and attach them to each other. Before jumping back over to Unreal Compiling, I also wanted to mention that again, as with any component inside of Unreal, if you have the options inside of the editor in the details panel to change things and update things like the target properties that we have there, provided two examples here, which would be things like the target arm length, which would be the property controlling that red line that we've already seen. And we've also got the options to do collision tests, which you can see I've now set to false and the arm length to a thousand. We do have full access to, of course, update and provide default values for those in C++. This is completely optional. Some people prefer to do this. Some don't. What I'm going to do is actually remove these just because I find that when creating components like this in C++, I quite like to keep the lines of code to a minimum where possible. The main reason here is purely because I know that things like the target arm length and even the collision tests might be something that I change either on the fly or that I'll be testing quite a lot inside of the editor, because there will be some cases where you don't want these to be set, as these are values which can be a big part of the kind of trial and error process of setting up a character and getting it to fill exactly as you want. And therefore providing default values in code like this is just kind of an extra step that really isn't needed as they already have values pre-filled. So I'm just going to remove these, but I just wanted to mention that you can start looking through what's available and setting your own default properties if you wanted. So the final step is I'm just going to jump back in to our unrelated editor and we'll just do a hot reload time here. Of course, if this is the first time that you're running the project, it could be one of the first times that you're compiling the code as well, so this may take a little while. So what we'll do is when this is come back and compiled successfully, we'll pick back up and confirm the last few things inside of the editor. Okay, so we have a successful compilation there. So I'm just going to jump into the plan to see if that's worked as expected. There's a good chance this hasn't updated in a hot reload because we're adding completely new components. So we may just need to restart the editor. What I'm going to do in fact is just close this and I'll load this directly through Visual Studio now. So I'll get this built again. This should be all that we need to do is just close the project, open it back up and that new spring arm component should not be available for us in the project. So with those changes made and updated, we can see we have exactly what I wanted to aim for here. So we have the spring arm attached to the root component or the capsule collider and the camera attached to the spring arm. If we have a look at this, we'll need to make some changes as we go through. One of the important ones to make actually is, like I've mentioned, the idea here is that we're making the the offsets and the position changes now through the spring arm. So if you have the same setup here, we're going to grab the camera and we'll just reset the location and rotation to zero on each of the axes. What you should end up with is something like this where the camera is nested right at the end of the spring arm and that will be looking somewhat at the players waste. This isn't again quite what we're looking for. This isn't really ideal, but we can update this very, very easily in the next topic. And that will be the topic where we're looking at the different features provided by the spring arm, which is really going to give us that flexibility on taking control of how the camera is kind of processed, moved around and things like that. With our spring arm component created and implemented, it's worth mentioning at this stage that following along with the C++ approaches will be useful for this course, just because the things that we'll be doing in the future will require us to have access to references of both the spring arm and the camera component in some of the future code. But of course, you now know how to do this in a blueprint and C++ in your own projects. Next, we can look at the customization options available through our newly added spring arm component.