In this video we're going to script the spawning of our collectible items. The system is going to spawn an orb from a spawner blueprint. We're going to script material swaps as a visual key for when a spawn pad has an orb and use a timeline to make the spawn more visually appealing. To begin with we're going to create a new active blueprint. So I'm going to right-click in our Blueprints folder, select Blueprint class and click actor. I'm going to call this new blueprint BP underscore orb spawner. I'm going to open it up and click open full blueprint editor. I'm going to swap over to the viewport and then in the components list I'm going to add a static mesh component. I'm going to call the static mesh component pad mesh. Over in the static mesh setting I'm going to choose a static mesh. This one is called SM underscore spawn pad base. Now the spawn pad base is quite big so we're going to change the scale to 0.35 so it's around about the same size as our orb. I'm going to compile and save and then go back to our content browser. I'm going to create a child blueprint of the existing orb from the stackable project. So in Blueprints and game elements I'm going to select BP energy orb. I'm going to right-click and create a child blueprint class. I'm going to call it BP underscore energy orb underscore collectible. As with the character blueprint in the previous video I'm going to drag this into our Blueprints folder and choose move here. I'm going to open up the BP underscore energy orb collectible. So I'm going to go to the event graph and delete the parent actor begin overlap node. This will prevent any of the existing logic from the parent class from happening when we overlap this version. So I'm going to compile and save this and go back to the BP underscore orb spawner. In the event graph I'm going to delete the begin overlap and event tick nodes and drag out from begin play. I'm going to type spawn actor and select spawn actor from class. From the class dropdown I'm going to select our orb pick up unit type BP underscore energy orb and choose collectible. I'm going to expand the node out and under owner I'm going to drag out and type self and choose get a reference to self. This is something we're going to use later to be able to communicate back from the orb that spawned to the spawner itself. To set the spawn transform we're going to right click and split the struct pin. We're only worried about setting the transform location at this point. And then I'm going to right click over here until I get actor location. I'm going to drag out and I'm going to choose an ad node. Move things around a bit to make it be easier to read and then plug in the output from the ad node into the spawn transform location. And then going to add 60 to the Z variable so that our orb will spawn 60 units up from the location of our spawn pad. And then going to take the pad mesh. I'm going to drag out of this pin. I'm going to type set material. I'm going to plug this in. I'm going to change the element index to one so that it changes the center of our pad mesh and change the material to M.I. underscore glow. And then you're going to compile and save. So when you go back to the level I'm going to drag in a BP orb spawner. Then I'm just going to press play. You can see now that we have our spawner and the orb that is spawned in place. The center of the spawner pad is now lit up which shows that it has an orb above it. Next we're going to create the animation for our spawning. So inside the energy orb collectible. We're going to add to the begin play logic. For now I'm just going to move these nodes out of the way and then drag out of the parent begin play and build our new logic on the end of here. Firstly I'm going to create a new variable. This variable is going to be called pick up location. And I'm going to change the variable type to effector. I'm going to drag this in and choose a set node. I'm going to plug this in to begin play chain. I'm going to drag out and type get actor location and choose to get actor location option. This is going to store where our orb first spawns. From here I'm going to add a new timeline. So I'm going to type timeline and select add timeline. I'm going to call this timeline TL underscore spawn. Then going to double click and open up the timeline. I'm going to set the length to be one second and make sure that I press tab or enter to confirm that setting. I'm now going to add two float curves by clicking add track and selecting add float track. The first one I'm going to call height alpha. And the second one I'm going to call scale. On the height alpha curve I'm going to add two keys. The first key I'm going to set to time zero value zero. And the second key I'm going to set to time one value one. And then going to uncheck synchronize view and just click the zoom to fit vertical. Just so I can see the full curve. You can select both points right click and choose auto. This will give an appealing curve to our height position. In the scale curve I'm going to add three points. So first off I'm going to right click and add my three keys. The first key I'm going to set to time zero value zero. The second key I'm going to add to time 0.8 and value 1.2. For the third key I'm going to set time and value both to one. And once again I'm going to scale it to to fit the view. With each of them selected again I'm going to press one on the keyboard to set the auto tangents. If you want to you can click the top point and flatten out its tangents. This will just give a more flat top to our curve. So now I'm going to go back out to the event graph and set up our update logic. We're going to drag out and type set actor location. We want to choose set at location and not set actor location and rotation. I only leave plenty of space because we're going to need to do some calculations in between these two nodes. Now I'm going to drag out to set at the location and add a set actor scale 3d node. This is going to be used to set the scale of our actor. So to set our location on the timeline update we're going to start from pick up location. And then we're going to add an offset value. The offset value we're going to add is 80 units. We're then going to use a Lurk node. So I'm going to drag out of pick up location and type Lurk and choose Lurk vector. You move things around a little bit just to tidy up. Now the Lurk vector node will move from our A point which is our pick up location to our B point which is going to be our pick up location plus our offset. Now choose what the value is based on the alpha pin. So I'm going to drag out of alpha and connect this to height alpha and plug our Lurk node into new location. To make the blue print more readable I'm going to add two reroute nodes by double clicking on the connection and dragging these out to make it a lot tidier. To set the scale I'm simply going to drag from new scale 3d to the scale float pin. This create a float to vector node which will do the conversion for us and set all three values of the vector to whatever the scale value is. I've added a reroute node just to tidy things up. From finished we're going to drag out and set our pick up location once more. So set pick up location and again we're going to set this based on the actor's current location. So get actor location. This value is going to be used later on when the player picks the pickup up. So I'm going to compile and save and then swap back to the BPObsporn. I'm now going to remove the offset that we put in here because the offset is now inside our animation. I'm going to go back to the level and press play and we should know see that the orb spawns, moves upwards, grows and shrinks back down to its full size. Now we have the orb spawning and we've added an extra little bit of visual appeal. We can move on to setting up the collection of the orb which we'll do in the next video.