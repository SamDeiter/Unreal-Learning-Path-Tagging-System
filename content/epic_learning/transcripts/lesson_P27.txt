When working with outside of your network connections, you might have to use the Stun or Turn servers. And we previously saw using a public Stun server in order to facilitate a connection between a mobile device and a remote server over the internet. But that was public and there wasn't very much security. So what can we do for more security and for private servers? We'll provide with the engine and the folder that you can see below are compiled Stun and Turn servers for windows that we can go ahead and use. So looking inside this folder, we can find we have a Stun server and a Turn server executable and then a few bat files to work with them. Now these are going to be very familiar to the previous video. If we start the Stun server, it will simply start a Stun server on the Stun server port, listening on the local machine for connections. If we do the same thing for the Turn server, it's going to start up again on the Turn server port, listening internally. Now in terms of customization options, if we edit the Turn server, PowerShell file, you will actually find a few settings in here. You'll notice we've commented out the local IP information, getting it from the AWS server since we're not deployed to an AWS server and replaced it with our local IP address, which is our internet IP address for this machine. But in addition to that, we have some things set up to allow us to add authentication. You'll notice it has an authentication file, a realm, the file information, and then a user name and a password. Now these are some default user names and passwords for our example, but in order to actually use these, we need to tell our server, our pixel streaming server, to use authentication. Now you can configure these to match your settings. And then what you want to do is when you actually start the server itself, we'll go back into our server folder, go into where we start up our stun or turn server and edit, just like we did before, you're going to make that public IP, your public IP for where this stun server or turn server will be located. And inside of the configuration options is where we'll set up the authentication. So there's a config.json file in the signally web server folder. And if we edit that, you can find a few options. One of them is use authentication false and it defaults to false. So if you want, you can enable us to true. And if you were to run a turn server, so for example, if I go into my power show for my turn server here, you'll find it's set to connect using the authentication name and credentials that we've set up in our local turn server. Now one thing you may have noticed is I haven't talked much about a actual turn server itself. For the most part, the stun server should hopefully be all that you need. The turn server is kind of like a board advanced stun server, except it actually relays the media stream between the application and the browser. So it kind of skips the signally web server part for transmitting the web RTC data. A lot of mobile devices and mobile services don't allow the web RTC protocol to connect directly. So you may have to actually run the stun server and the turn server if you want to support mobile devices. The nice thing is it falls back and forth as needed, so it will use whatever is appropriate.