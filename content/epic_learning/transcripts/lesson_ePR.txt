We are going to be migrating across the refresh visibility function of the quest marker. To our second example, it's going to be bringing together knowledge of migrating stuff into C++ that we've learnt in previous modules. So the idea is that we have this quest marker. We've seen them in the world. There are these floaty things with particle effects that move across in the last module and the particle effects enable and disable depending on your progress through a quest. Refresh visibility is just the process of going and querying the quest, seeing whether it's active, seeing whether we're at the progress at which this quest marker should be shown and setting the visibility as such. Now to move this across, we need to move across some of the dependencies into C++ as well or at least make them blueprint implementable events, such as the quest manage getting hold of the quest manager, getting the quest, seeing whether the quest is active and so on and so forth. All of that is needing to come across into C++. The same goes for these variables that we depend on, namely the quest name and show at progress. Now the other two should be reasonably straightforward. The other functions that we rely upon are relatively straightforward, but these variables already exist and are overridden in our main map. So if we search for quest marker, you're going to see that we've got a bunch of them already in the scene and they have the variables already overridden. quest name and show at progress. So we're going to need to manually migrate that across as possible to do an automatic process for this, but we aren't going to go into that in this module. So the way I'm going to do this is I'm going to rename the original blueprint variables so that we keep them around for copying purposes and over to BP underscore. Then I'm going to go over and create the U properties over in our C++ base class. So we're going to go to the quest marker.h and down to the protected section, I'm going to create a new U property visible. In fact, edit anywhere and we can make it blueprints read only because we don't need the ability to change these over in blueprint. And then we can have it. First one is an F name called quest name. Then the second one is show at progress and it's an int 32. So we're going to use the same U property header and it's going to be int 32 show at progress. And we could set some default values here just to be explicit. So we're going to have zero here and this is going to be F name none just by default anyway. So I'm not going to bother setting that one. So we'll go ahead and build and see what happens over in the editor. Let's dive over into the main map and have a look at markers. I'm going to select first the top most marker that was showing up. And in the details pane, I'm going to search for quest space name and we see that we get the BP quest name and the quest name from C++. So just going to copy that across and I'm going to fast forward through to copy across the rest. Then we're going to repeat the process for show at progress. Once that's all done, go ahead and save your map. It now has the new variables in it and we need to start working on these functions. We're going to be using them as blueprint implementable events. So let's start by implementing these quest manager. So the quest manager is on the quest marker that function get quest manager. So we're going to create a U function that is going to be a blueprint pure remembering that that also means it's blueprint callable and we're going to make a blueprint implementable event. And then that is going to be called get quest manager and what's the type it's going to have to return. Well, it is going to need to include the quest manager header. So hash include quest manager dot h means we can use the type a quest manager star and then it doesn't need a type and we can make this const because it's also pure. So I can go ahead and build that and we can try and migrate that across. And then because actually this get quest manager is part of a BP quest library, what we're going to have to do is go ahead and call it from within an override. So we go to override and we want to override get quest manager here and what we're going to be returning is our get quest manager from the BP quest library like so and that is enough for us right now. The next step is going to be to implement get quest. Now currently we have get quest and we have get quest itself is calling to get quest index, both of which are currently just implemented in blueprints. Now the issue here is that if we try to migrate them across, they is currently return the deprecated quests and we want to be returning the C++1. So we are going to rename these so that they are the deprecated things so that our C++ can return the non deprecated stuff. So we've got first of all get quest index. This one's already in C++ so we don't have to worry. Then we've got get quest which isn't in C++ and returns from the deprecated array. So we're going to prefix it with deprecated underscore like so. And then over in the C++ we can implement if we go and open up a quest manager .h. We can go ahead and implement a public function forget quest u function which is going to be a blueprint pure and it's not going to be a blueprint native event because we're going to implement it here in C++. It's going to return the f quest info and again it's going to be called getquest and it takes an f name which is the name of the quest and I can make this a const function while I'm at it. I'm going to go ahead and copy this signature and create the function over in the C++. So we've got get quest like so and it is going to go ahead and return the quest from array. So we're going to need to get the quest list but we also need to get the quest index. So get quest index which is going to take the name that we have passed in here and that's what we're going to go ahead and return out. Now one thing I've forgotten to do is add the prefix a quest manager colon colon in front of my function which is why it's giving me that funny look and it needs to be const as well. Now because that's const that means that getquest index also needs to be const which I think will be fine over in the blueprint. So let's give that a go and compile and see if it works. Go over to the BP quest manager compile over here. See what is going on. It says get quests can modify state and cannot be called on self because it is a read only target in this context. So that just means that we need to make sure that get quests is also const so go and mark it as such and we can try and compile and save and everything is working at Hankee Dory. So we go back to quest manager. What is left? We have got the getquest manager. We have got deprecated get quest and we now need is active quest on the quest manager which again is just going to be implemented via blueprint because it is a rabbit nest of potential things that could need to be implemented and brought out to C++. So we don't want to bring them all over in one go. So we're going to do a blueprint implementable event. So it's going to be public. You function blueprint pure and we're going to be returning a Boolean. It is called is active quest and we're going to need to give it an argument of the quest ID. So f name and it's called in this case quest ID so I'm trying to match that as much as possible quest ID and remember this is returning active so we'd like to as much as possible try and match things up. So we're going to change that to return value like so and without the space I think is what we need to do just camel case it and then we're going to make this const and we're going to make sure that it is a blueprint implementable event build it and try and migrate over to it. So as we can see it's giving us the error we would expect let's go and cut the body out and delete active quests and go and re override it. Now selecting is active quest let's paste in our body again and try and connect up the nodes that we need quest ID like so. Now if I go ahead and compile this should be fairly all right we just need to make sure is active index is also const so let's go ahead and compile that save that change that's had the ripple effect of us making is quest complete also const so let's just go and make that change to compile and save and hopefully everything over here is working hunky dory and in fact it's even just reconnected is quest up correctly with that renamed return value. So all that remains to be done is to move over refresh visibility now which is a public function it's not pure or anything like that so let's go over to quest marker and add a new public function which is going to be blueprints callable and implemented over here and C++. So the return value is going to be none there is no return value there's also no input so it's going to be void refresh visibility and it's taking no arguments and it's non-conced and we're going to take that signature and create it over in the quest marker and then we're going to implement the body try and migrate across as much as possible this spirit of this function. So the first thing is we need to get hold of the quest it's f quest info the quest that we're currently dealing with which we're going to do by doing get manager arrow get quest for this we are going to use the quest name variable that we've migrated across then we're going to try and calculate the visibility so we can use an intermediate variable here to make things more readable we're going to do a again a get quest manager I've misspelled that on the line above so get quest manager arrow is active quest and then we want to use again this quest name and crucially we want the quest.progress to be equal to our show at progress variable and finally because we have our components set up here we can access our particle system components we can do an arrow to that and we can call as set visibility so we've got set visibility and you might guess there is a set visibility function in c++ as well there sure is and it can take a single boolean which is going to be our visibility so that should do the triggers go ahead and build and we can see we've got the error over here in refresh visibility that's because it's no longer here in blueprint we can go ahead and delete the blueprint version and compile and save go over to our main window and the map and let's try playing with new changes so you notice that there are some blueprint compilation errors that's where we were referencing to refresh so let's go ahead and open those erroring loop prints if we can and go through and see what the compilation errors are if you go through and compile most of these will actually just completely clear themselves up they just needed a recompile and if we go back to the main map hit play we should hopefully see that we can refresh the visibility of these markers by walking through them walk through the marker has updated disappeared and the next one has enabled so we've successfully moved across the functionality of refresh visibility from blueprint into c++ and in the process we have learned how to migrate you properties that were already existent in the main map one final clear up step to do is go to the bp quest marker and delete these two bp quest name and bp show progress because they are no longer in use by any function in here and we've already migrated across the data in the main map