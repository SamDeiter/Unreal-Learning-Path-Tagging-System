Welcome. In this chapter of the material essential concepts course, we're going to look at textures. What we would like to do is import a number of textures from a door into the Unreal Engine. By doing so, we're going to have to take a look at texture sizes and requirements that come with it. Common formats used, the actual process of importing textures and a brief word about compression. Now, as part of the content that came with this chapter, you will have these four textures over here. If we open them up in Photoshop or any other package of choice, we will get this. This is the diffuse texture. You can see underscore D from the fuse, underscore N from normal map, underscore M from metallic and underscore R from roughness. Now, exactly what the metallic and roughness map do, by the way, we'll cover that in a later chapter. Right now, the most important thing for us is how do we get these images into the Unreal Engine. To do so, we need to make sure they have the right size. In image image size, you can see it's set to 2048. It has to be exactly this number or a similar one to it. These textures are already the right size, but if you look back at my slides, it has to be one of these values for it to be correct. One, two, four, eight, sixteen, thirty, two, and all the way up to eight thousand one and ninety two, power of two essentially, those are acceptable values. Combinations are fine too, so an texture could be sixteen in width, but four thousand ninety six pixels in height. That's fine too, but it has to be one of these numbers. If you would import something that's not one of these numbers, it's a different resolution. It will still import and it will still seemingly look okay, but those textures will not have Mip maps. Now, exactly what Mip maps are is kind of a deeper discussion and is a little bit too much for an introductory course, but essentially a Mip map is a smaller version of the same texture, so the engine will generate different resolution versions of each texture it imports, so that in the distance, rather than displaying the full resolution image, it can display the lower resolution texture or image. And by doing so, it will save memory, it will make it easier to stream in textures as well, and it will visually smoothen out the image a little bit. So, long reason, long discussion, what Mip maps are, exactly how they work and exactly when you need them, so essentially it goes like this. If you're uncertain whether or not you need Mip maps, you probably need them. 95% of the content needs Mip maps, therefore, if uncertain, always stick to one of these numbers and you'll be fine. You can't go wrong then.