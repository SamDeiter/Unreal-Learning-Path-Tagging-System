We migrated our assets from a project that was built for use on a desktop with much more power to support the assets than we have on our HoloLens 2. In this module, we're going to optimize the materials, both to get rid of features we aren't using, as well as cleaning them up to be much more performant. We're also going to see how we can quickly optimize our use of the textures within our materials and remove the ones we aren't using. Unreal Engine provides some great tools and view modes to assist us with this task. As we make these changes, we will use the preview rendering level to visualize how the scene will look when rendered on the HoloLens. Using the E-Shader complexity view mode, we will see just how much the various materials in our scene cost. I've taken the architecture from the spatial mapping map and pulled it into main. This architecture will be the only meshes that are in our scene by default and will continue to have the same purpose they have had which is to line up in front of the bookcase in my space. I'll keep them in their current state for now and we'll discuss optimizing them in the next module. Let's begin by setting up the editor to give us a preview of what our materials will look like after being rendered by a mobile device or our HoloLens 2. To see our current shader complexity relative to being rendered on our desktop, open the view mode options and under optimization view mode, set it to shader complexity. Rotate the camera to have the crosshairs over the mesh we want to measure. Other settings change the preview rendering level to Android ES 3.1. This will change the preview platform and add a button to the main toolbar that we can use to toggle mobile preview. Because this change will take some time, I'll skip ahead. The great thing about the assets that were imported is that they are all inheriting from parent or master materials. This allows us to make the changes to the one material and all that inherit from it will be affected. Before we change the master material, let's create a simple mobile friendly material to give us an understanding of the changes we will make to the master material. Inside of our materials folder, create a new material. I'll name mine M underscore mobile material. Looking at the details panel, we'll be keeping the blend mode at opaque. Select and translucent are slow and should only be used if your project strictly requires it. Scroll down to mobile and turn off use lightmap directionality. This will flatten the lightmap created for the shader which will make it a much cheaper material. When setting our project settings, we made sure to use forward rendering with forward shading. The forward shading options here are all meant to improve the visual fidelity of this material, but for max performance, we should keep these features off. Our world builder project is intended to run on a HoloLens 2 or AR capable device to place holograms in our world. The lighting of the objects is much less important for the project than the visibility and performance of it. Before we move to the master material, let's go over a few best practices when building the material. If your project contains materials with tile UVs in its textures with this kind of setup, rather than having each node calculate the tile, use the customize UV options to do this work. You can add additional inputs as you need them, but do try to keep this as minimal as possible. So instead of using that setup, remove the UV calculations and plug it into the customize UV zero slot. If you had multiple slots added, change the value of const coordinate on the node you want to alter. Here is a basic material that we could use as a master material for your project. It allows us to set a color or a texture for the base color using a static switch. The use of static parameters is also an important element for creating optimized instance materials. We can create the material instance and the material can remove an entire branch of unused information with no runtime cost. This is the alternative to using dynamic material instances inside of blueprints as we did in the previous course to retrieve the feed from the PV camera. This is because a dynamic material instance will load all the nodes from all of the branches. Alright, let's move on to the master material that all of our assets use. It is located in world builder assets shared materials. Open material master. As expected, this material has a lot more going on. So let's change its preview mode settings to ES3.1. Instantly, we can see many lines go dark. This is telling us that these nodes are not supported by this renderer and can be removed, or better optimized by using a feature level switch. For simplicity, I'll avoid using the switch, but it is a powerful node that allows us to create one material that will adjust based on the requested feature levels or platforms being used to run the app. So I'll remove the metal, the roughness, and the ambient occlusion nodes, and then click clean up. I'll also go through and remove nodes that add features that we don't need because of their cost. Things like adding animated noise for the emissive material, or desaturating the base color. If you noticed, there were two lights in the main map that were brought over from the UI problem map. These lights will be removed because I want the holograms to have that hologram feel. Having shadows on the holograms removes from that, and if the renderer sees a fully dark pixel, it will treat it as a masked pixel and appear like a hole through the mesh. With that, I'm going to set the shading model from default lit to unlit. And because we are using unlit, we cannot take advantage of the normal maps, so I will remove those two. Before we move on, we'll need to clean up this graph. I've gone through and removed nodes that add features we don't need because of their cost. Things like adding animated noise for the emissive material, or desaturating the base color. We are now left with a flexible yet simplified material that will give us all the options we need at a much cheaper cost. Press apply, and when it finishes compiling the shaders, return to the map and see the shader complexity. Much better. Let's take a quick look at the textures we are using inside of these materials. Navigate to the world builder folder. Add a new filter and under materials and textures, select texture. Here are all the texture files we have in our project. Select all of them and right-click. Select size map. Right now it is showing their disk size. See that the heaviest assets here aren't even in the scene. Change the size to display to memory size. Now we are looking at how much memory space is needed to load this asset. While individually it doesn't look like much, added up is resulting in more than 250 megabytes of memory being taken up by textures alone. We can quickly add a limit to how large the texture is allowed to be. If I'm all selected, right-click on them and under asset actions, open bulk edit via property matrix. On the right side we can make edits to the entire selection. You need to be careful here because any edit you make will of course be made on everything. Under compression we can set the maximum texture size to a power of 2 value. If we really want to optimize it, we can pull it way down to 256 or 512. I'll set it to 1024. We can immediately see the results by looking at our meshes in the map. I think it still looks good and the cost of it now is much lower. To see an updated memory size chart, right-click on the assets and again open the size map. Now the combined cost for all of these textures is at a much more comfortable size. Adding onto this effort, we can now remove all the textures that aren't used anymore. All the textures ending with underscore ARM, which means ambient occlusion, roughness and metallic, or underscore N for normal or underscore D for displacement, in their names, are for features that we are not using anymore and can safely be removed. In this module we created a basic mobile friendly material and used what we learned to alter the heavily used master material. We also changed the view mode to allow us to see the complexity of the materials used by our imported assets, and lastly we used the size viewer and matrix editor to bulk edit the max size of some previously heavy textures and removed the ones no longer being used. In the next module, we will learn some additional tips and tricks we can use to further optimize the project.