We're continuing on geometry rendering. We're going to wrap it up with talking about the verdict shaders and how that impacts things. It's a fairly brief amount of information that should this will go relatively quickly. Okay, so verdict shaders. We actually have multiple different types of shaders. There are many different types of shaders, verdict shaders, pixel shaders, whole shaders, domain shaders, and so on. A shader is, if you want to put it this way, a simple little program that performs calculations on its input variables and outputs different variables. A number comes in, it calculates it, it pulls out. That's it. Shaders are, however, very, very efficient. So the reason why we have to perform these calculations using a shader system is because we don't have enough hardware power to run this on other parts of the hardware. For example, we come up with a general GPU, architecture, CPU, and so on. So we've designed something called a shader system which is meant to process a huge number of often simple calculations and often in parallel. We're going to get back to this further in when we talk about pixel shaders. So let's keep this simple right now and focus only on the verdict shaders and the basics of what a shader is. The verdict shader is the first one we go through. It does three things. It converts the local verdicts position to world handles shading and can apply offsets. Let's take a look at those in detail. So first of all, it handles, it converts local positions to world. If you have a box, as you can see here, that box is saved in local position because if that box has the pivot point in the middle and let's say that the box is 50 units from, you know, in away from the pivot point, that means there's a hundred units per site. Then this point here, the verdicts over here would be Z50 and the one here would be Z-50. And it's saved like that. It's saved as position Z50, which in fact, X50 probably Y-50, Z50. Those are the numbers it's saving in us. That's what a number, the verdicts, as remember, when it's placed in the world, and in this case it's located here, some random location, this is no longer supposed to be 50 on Z. It's supposed to be minus 92 plus 50, so you have the conversion from local to world space. And that is where the verdict should be rendered. So verdict shaders convert that. It converts the local information of where the vertices are to where they actually should be in the world based on the position the object has in the world. There's nothing you can do about that. You can't optimize it. It just happens. Likewise with this, it handles the smoothing. It handles heart-edges, soft-edges, verdicts, colors, smoothing back in the days when, you know, before direct text 11 and even 9. We're talking direct text 7, for example. It handles more of the lighting, the shading, those kind of things. Again, it's not much you can do about it. It just happens. But a third one is interesting because a third one does really impact you. The third one is an additional offset. Look, if you go back to slides, this is basically offsetting the model. That's what I'm doing. You know where the model is and it's offsetting the model to match the world location. Since you're offsetting it anyway, why not let the user add a second offset on top of the first offset? That is what world position offset does in Unreal. As you use the verdict shader to offset and move measures. These three cylinders again, the middle one has a material applied that offset set. So it goes up in the air. It's being moved. And there is in Unreal as well. Let's just take a look in Unreal here. In any material we have, I take the statue here, the material here. In any material we have, we have this input called world position offset. That will literally offset it. If I input a number here, that number is not a color. It's going to be read as a position in the world. So if I say z, which red, green, blue, we become x, y, z, 500, the model will be 500 units higher. If I now approve this, apply it. That statue will not be here. It will be somewhere up in the air. Once the shader is compiled, you can see the statue is now there. You literally offset it. The interesting thing though is that CPU is not aware that this is happening. We'll get back to in a second. Now, why do we do this? We do this because we do this for cloth. So cloth is verdict shader driven. Water displacement. So waves in the water, verdict shader driven. And most commonly, foliage is verdict shader driven. The animation of wind on foliage is done on the verdict shader. This movement is the same thing. We're simply offsetting the vertices, but we're only offsetting the top vertices. The bottom ones, they don't move. In this case, we probably use verdict scholars or some kind of, you know, some kind of map that identifies which vertices should be allowed to move and which one should not. The ones with verdicts color black should not move. The ones with verdicts color white should move. And that's it. And the reason we're doing that in turn is because we would never be able to animate it. I mean, imagine animating the position of grass using bones as skeleton or something, which means the CPU would end up calculating the positions. Imagine you then have a forest, with say a million vertices, you count your animated numbers of a million vertices. You can't calculate position of a million vertices per frame, but the verdict shaders are so optimized that they can. That's why we're doing this using the verdict shaders. That's what it just says, so that's why we animate things. And also they're super optimized, right? This is why they're central to the rendering pipeline start to end. So the verdict shaders are the first one, the pixel shaders are even more important and they cover almost everything we do is pixel shader driven. They are super optimized. Interesting is they don't actually move the model. It's just a visual effect. It's only when it gets to the renderer. The renderer simply displays the model elsewhere. The model has not moved, we're just rendering it elsewhere. So the CPU does not know that the model has moved. So things like physics or collisions will not take anything into account. If you go back to Unreal right now and statue we just moved, if you would run this experience or play this game and you would try to jump on top of this platform and walk through, you would get stuck because the collision of the statue is still here. It's still there. In fact, you can see it. It's going to be very visible on the video, but the frame right there is the collision of the statue that did not move because the collision is handled by the CPU. The CPU does not know that this is moved. It can't even know even if the GPU would tell back to the CPU that it has moved the object using the verdict shader. It doesn't help because the CPU is ahead in time. This is happening in the past as far as the CPU is concerned. So keep that in mind. You can only use this for visual effects. You cannot use this for things that have to be accurate or they have to trigger things or where the physics are important, the collision is important or anything like that, just visual effects. Blacks for example, you know, those things would work very well with verdict shaders. Most of this straightforward, the more complex animation is obviously the slower, the more vertices it has obviously the slower, but therefore it means that high poly objects are better of having simpler verdict shaders or rather a simpler setup connected to world position offset. So if you have a very high polygon tree, it might be better to give that high poly tree a simpler wind animation than lower polyvegetation nearby. And in fact, one thing that almost no one does is to disable world position offset in a distance. Like this tree is not animated right now, but imagine the this tree is animated in the wind and it's moving in the wind. If you're at this distance away, you can still see the tree, but you probably can't see the tree moving anymore. So what you want to do is you want to disable the wind animation on a tree, you want to disable the world position offset at a certain distance, at a distance at which you can't see it moving more. You're still rendering it, but at least you're not moving all those vertices. Because if this tree would have one million vertices, it doesn't, but let's just say it has, and you would be here, which is far away, you're still calculating those one million vertices moving in the world. So optimize things, try to fade out these animation, these effects, these offsets, based on distance where possible. It will save some performance. And that brings us to the end of this one. We'll continue by taking a look at the Restorization and Gebuffer.