Sometimes when creating landscapes for real time projects, we'll be fortunate enough to have been supplied with a Heightmap, we'll create the exact shape and elevation of terrain needed, something that we can then augment and change as we see fit. For a Heightmap to be usable in Unreal, it first of all needs to be supplied as a grayscale image, and also come in at a bit depth of 16 bits per pixel, the formats that use or support these requirements, being the .png and .rar formats. The question that we will want to tackle first of all is how we go about importing and then using such a map. Let's first of all come to the landscape tab of the mode section, and under the new landscape heading, click the import from file radial button. Here we can click the browse button for the Heightmap file. Now we want to navigate to the downloaded exercise files folder, and then just click to select the Height underscore map.png file. Now we will discuss the following settings in more detail, but for now, because we have our Heightmap file loaded, let's quickly make the following adjustments to get our terrain into place. We're going to set the Z location value to around about 20,000, the section size to 31 by 31 quads, the number of components, we can set to 12 by 12, and then press the import button. Using the WSA and D keys now, we can move our camera around the 3D view, and take a look at what the terrain actually looks like. It being good to keep in mind that from this point forward, the landscape can now be edited and modified by means of the tools that we will look at throughout this course, should we want our need to. Before moving on here, we will want to come to the place tab, select the landscape and Gizmo actor entries in the world outliner, and simply delete them, as we will be creating and working on our terrain from directly inside Unreal, knowing how to use imported Heightmaps via the import options though, means that we can either handcraft a Heightmap from inside a painting application, if we like, or we can download a file depicting a real-world location via a number of sites that provide such for free. Now this gives us the ability to create a terrain that closely matches the real-world location of our visualization, if that is what our project needs.