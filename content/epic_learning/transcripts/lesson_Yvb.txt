Time to put our collision detection system to good use then by next creating a loop that once it finds a point of which no mesh intersection will take place can interrupt itself and spawn one of the user defined mesh types. We'll also want the loop if it doesn't detect any empty space to continue until it does detect a space, with this needing to repeat over and over again of course. We can set this up then by first of all dragging from the output of our sphere traced by channel node and then creating a branch node. The return value on the sphere traced node we will pipe into the condition input of the branch node. This means when the branch determines that the sphere trace has a collision it will move onto the next set of code in our blueprints. To do this we will drag from the true output of our branch node. We can create a do n node by searching for the phrase flow control and then selecting it from the filtered list. Meaning we can then pass its exit output back into the set spawn pipe variable. Again to explain this will stop the code from simply continuing on and spawning an object but will instead just repeat the loop. We can now drag from the false output of the branch node and then search for and add a sequence node. We can pipe it then zero output into the reset input on our do n loop so that the process of checking collision can begin again once a clear space is being found. Obviously though we not only want to reset the loop but we also do eventually want to spawn a mesh and so we will be using the then one output for this once we have built the spawning system of course. Although completed in just a few simple steps it is this part of the code that will determine whether or not a mesh actor is spawned and instruct the loop to continue looking for a suitable place if it hasn't been able to do so so far.