In this module, we'll build our first specialized block, which is the foundation for the level. That'll be the floor. The floor will be a child class of the BP-based block and use the materials from the BP-based block. We'll also be unbreakable. Our objectives for this module are to write a sequence as part of a construction script. We'll create an instant static mesh with similar properties as a static mesh. We'll utilize a nested for loop or a loop inside of a loop. We'll use instance editable variables to control the size of the floor actor. To get started, find the BP-based block blueprint in the Blueprints folder. Right-click on it and select Create Child Blueprint class. Name the new blueprint BP underscore, lore. Open the new blueprint for editing. By creating a child class, our new class will inherit all of the attributes and data of its parent class, as well as any functions, like the Get Attach location. By using the object-oriented mechanism of inheritance, we can make use of the components of functionality of the parent class and specialize the child class in a way that makes sense for it. Let's make sure that the floor is not breakable. So we'll need to see our parent class as variables. In the My Blueprint panel, click the drop down and select Show Inherited Variables. You can then open up the Variables panel, select the Is Breakable Value, and then uncheck the Is Breakable Value so it's false and click Compile. Now we'll add the components that this class needs. Select the default scene root, look on the Add Component button, and search for Instanced Static Mesh. Name this Instanced Block Mesh. We'll use this block to dynamically create instances of the block used for the floor. This is much less expensive in terms of memory and rendering than if we use several instances of the same Static Mesh. We need to add two variables to control the sides of the floor. Take the Add Variable button and name the first variable, floor, size, x. In the Details panel, change the variable type to Integer. Click Compile and set the default value to 20. The floor size y will be the same as the floor size x in its default value and it will also be an integer. Because of that, I'm just going to select the floor size x and click Ctrl W and change the name of the variable to floor size y. Click the Compile button and you'll see that the default value is also 20. Another change we'll make to the floor size x and the floor size y is we'll make them Instanced Editable. So in the Details panel, click on Instanced Editable for floor size x, select floor size y, and make that Instanced Editable as well. And click Compile. That will expose the variables in the Editor so that we can change the size of the floor in the Editor rather than come back and modify the Blueprint. And when we make multiple instances of the floor, we can make them with different sizes as well. We're now going to edit the Construction script. Click on the Construction Script tab to open it. In the Construction Scripts graph, if you don't see a connection from the Construction Scripts entry node to the Parents Construction script, right-click on the Construction Script node and select Add Call to Parent Function. Then link those two nodes up. This will call the Parents Construction Script. This is useful in the event the Parent Construction Script has code that should be run before the child class is created. If you've used other object oriented programming languages such as Java, this is analogous to calling your super classes constructor explicitly. Now in this particular example, the Parents Construction Script doesn't do anything, but it might in the future, and you probably want to call it from all of the classes that inherit from it. The first bit of code will add will be to snap the Actor location to a unit of 100. This will ensure a Math works for the Get Attached Location function in the Base Block. Type-click and type Get Actor Location and select the Get Actor location node. This returns the location of the Actor and World Space. Drag off of the Return Value and type in Snap and select Vector Snap to Grid. Set the Grid Size to 100. Drag off of the Return Value of the Vector Snap to Grid and type in Set Actor Location. Hook that up to the Parent Construction Script. For this to work correctly, we need to make sure the Construction Script doesn't run when the Actor is dragged in the level. Go to the Class Settings and under the Blueprint Options uncheck Run Construction Script on Drag. Use a Sequence node with three nodes to help organize the code. Press S on the keyboard and left-click to bring up a Sequence node. We'll use three execution pins for our Sequence node, so click the Add Pin button to add the third one. Hook up the Sequence node to the Set Actor location. On Sequence Wire Zero, set the Static Mashed to the Instant Static Mashed component to the same Static Mashed as the Super Class. To do this, drag out the Block Mashed, drag out the Instant Static Mashed and drag off of the Instant Static Mashed and type in Set Static Mashed. To get the Static Mashed from the Block Mashed, drag off of the Block Mashed and type in Static Mashed and select Get Static Mashed. And hook up the Return Value of that to the New Mesh. Also make sure you hook up the Sequence for zero to the Set Static Mashed Pin. We can now destroy the Block Mashed component because we won't be needing it anymore with this class. So drag out another Block Mesh, drag off of it and type in Destroy Component and hook that execution pin to the Set Static Mashed Pin. On Sequence Wire One, we'll write the code to build the floor. We're going to use a nested for loop, which is a for loop inside of a for loop. Drag out of Sequence One and type in For Loop. The first index should be zero. The last index should be the Floor Size X minus one. I'll explain in a minute. Drag out the Floor Size X and select Get Floor Size X. Drag off of the Floor Size X, type in a minus symbol and select Minus Ant and choose Use the Default Value of One and hook that up to the last index. The reason we're subtracting one from the Floor X size is that the Floor X size has a value of 20 by default and the first index is zero. That's 21 numbers, zero through 20. We want 20 for the entire number of blocks. Zero through 19 would be 20 floor blocks. We have to subtract one from the Floor X size. To the person creating the level, it wouldn't make sense for them to specify 19 and get 20 floor blocks. We do that math for them here in the Construction Script. We're going to use the index of the loop body to help us figure out where to put the block in the X-axis. So right click on the index and select Promote to Local Variables. Local variables appear in a different location than Blueprint variables and they're local to the script that they're in. Name the new local variable X. Hook up the execution wire to the loop body. From that node, drag off the exact pin and type in, where loop again. And for this four loops last index, we use the Floor Size Y. As a shortcut to getting a variable value, hold down the Control key and drag the variable out. Once again, reduce the value by one. So type in a minus sign, in minus in. Use one for the value and hook that up to the last index. We'll promote the inner four loops index to a variable as well. Right click and select Promote to Local Variable and use the name Y. Hook up that execution pin. All that's left to do is to add the instance of the Instance Static Mesh and calculate where it should go. Drag out the Instance Block Mesh. Drag off of the Instance Block Mesh and type in Add Instance. Hook that up to the Set Y Execution Pin. To create an instance, we need to have a Transform, a location, a rotation and a scale. We're only going to furnish the location. Right click on the Instance Transform and select Split Struct Pin. We're going to calculate the X, Y and Z, so we'll be setting them independently. Right click on the Instance Transform location and select Split Struct Pin as well. This will give us access to all three of those values. The Z value will always be 50, as we're offsetting all of the blocks by 50 units to make up for the fact that the point of origin of the blocks is in the center of the block. Get the X from the local variables and the Y as well. These will hold the value 0 through whatever the last index is. We'll multiply that value by 100 because each block is 100 units cubed. So drag off of the X, type in a multiplication and we're going to use int times float because the Instance Transform location is a float. This will convert the integer X to a float when we multiply it by a float, which is going to be 100. Duplicate this node, select it and press Control W and hook up the Y value. We have to offset these by 50 again to account for the point of origin difference. So drag off of the product, type in plus and select float plus float and use 50 for the other value. Duplicate this node, Control W and hook in the product of the Y times 100. You can now directly connect the X value and the Y value. To check if this is working, click Compile, go into the Viewport and have a look at your 20x20 floor. Now that the floor is created, we have to adjust the box collision so it will cover the entire floor. In the construction script, drag out the box collision component. Drag off of the box collision and find the Set Box Extent node. Hook that up to Sequence Wire 2. I'm going to reposition the nodes a little bit to make it easier to do that. To have the value of the box extent, right click on it and select Split Struck Pin. This allows us to adjust the X, Y and Z separately. We all know that the Z is going to be 50. Recall that the box extent is a radius extending out from the center of the box. To calculate the X and the Y, we'll use the floor X size and the floor Y. So drag those out and get them. Again, holding down the Control key to do a get. And we'll multiply those times float by 50. I'm going to duplicate that node, Control W, hook up the floor size Y and hook those up to the box extent. And that will cover the size of the box extent. Now observe that it is not in the correct location yet. You have to move it to the center. To do that, drag out another copy of the box collision component and type in Set World Location. To calculate the world location, we'll make a vector and we'll add the current actor location to that. To right-click and type in Make Vector, we'll use the calculated center point of the box collision. So the X goes into the X, the Y calculation goes into the Y and Z is going to be 50. So this tells us the location in local space. And click get actor location and drag off of the make vector plus vector plus vector and add the offset in local space to the current actor location and connect that to the new location. Make sure your code is compiled and saved and then go back to the block game map. You can now drag an instance of the BP floor out and change its size using the default variable values. So for floor size X, we have 20. We can change that by clicking and typing a new value or dragging slowly. Don't go too crazy with the floor X size and the Y size yet. And you can also reposition and observe that when you reposition the floor, it is snapping to the grade units of 100 by 100. I'm going to lower that down in so it just rests right on top of this island. And it looks like I made the floor size Y just a little bit too wide. There we go. 23 looks pretty good and 27 in the X. In this module, we wrote a construction script for the floor using a sequence in the nested for loop. We created an instant static mesh with similar properties as a static mesh. We applied instance editable variables to allow us to modify the floor in the editor.