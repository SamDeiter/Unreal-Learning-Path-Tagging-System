In the previous video, we finished up making a basic 2D side scroller prototype. But what if we wanted to make a 2D game that operated from a different perspective? In this video, we'll create the foundation of a top down 2D game. Making changes to our input, character blueprint and level file to accommodate this change in perspective. Before we do anything else, we'll need to adjust our inputs. We won't be needing a jump input for our top down game, but we will need the ability to move up and down the screen. And the logical key to do this on the keyboard would be W. To clear the way for our new access mapping, let's unbind the W key from the jump input. From the main editor window, go to Edit, Project Settings and locate the input section on the left hand side. In the Action Mapping's rollout, locate the Jump Mapping. Once you've found that, press the X next to the W binding to remove it. With that cleared up, now we need to add the new access to instruct the character to move up and down the screen. Navigate to the Access Mapping section and click the plus icon to add a new mapping. Select this new access name and call it Move Up. Now, let's map some values to this access. Click the plus icon next to the Move Up access until we have a total of five inputs. Click the keyboard icon on the next entry and press S. Change the scale to minus one. Now, let's configure the GamePad. Select the next drop down, expand the GamePad rollout and select GamePad Left Thumbstick Y-axis. Leave the scale set to one. Finally, let's finish up with two more common keyboard controls for this movement. Press the keyboard icon to the left of the axis and press the Up arrow on your keyboard. Once again, leave the scale set to one. Finally, click the keyboard icon on the next axis and press the down arrow on your keyboard. Set this scale to minus one. You may have already noticed that the scale values and mappings are very similar to the move right mappings above. That's because we're doing the exactly the same thing, just in another axis. That takes care of all the input changes we need to make. We can now close the Engine Input window and return to the main editor window. We can now direct our focus towards the player character. For this top down perspective, we're going to have to make some fundamental changes to the blueprint in order for it to work as we expect. So let's make a duplicate of the existing player character. Navigate to Content, My Content, Blueprints and right-click on My 2D Player character and select Duplicate. Let's add a suffix to the existing name of underscore top down. Now we have a duplicate of our character called My 2D Player Character, underscore top down. Let's double-click the new blueprint to open the Blueprint Editor. Let's begin by selecting the Viewport tab as we want to start by adjusting the position and rotation values of some of the existing components. Doing so with the Viewport tab open will allow us to visualize these changes. First, select the sprite component. In the Transform rollout, click the arrow next to rotation and select World. We need to do this as we will be later using the Event Graph to independently rotate components of the character in World Space. With that change made, now change the Absolute rotation to minus 90 in X. To make it a little easier to see what we're doing here, let's change our perspective in the Viewport. Select the top view from the Viewport toolbar and then select the Unlit view mode. That looks a little better and if we zoom in, we can see our first problem. The capsule is now the complete wrong size, so let's adjust that now. Select the capsule component from the components panel and adjust the capsule half-hike to 60 and the capsule radius to 40. That looks a little closer. Let's return the Viewport camera to the perspective mode so that we can get a better look at our camera and boom arm. Begin by selecting the Spring Arm component from the components panel. Expand the rotation entry in the Transform rollout in the Details panel and select World to set the Transform value to an Absolute rotation. In the Camera Settings rollout, disable Use Pawn Control rotation as we can no longer rely on this to give us the correct rotation. While we're here, also check the Inherit Your option. Now, let's adjust the rotation of the boom arm to something more suitable for a top-down game. Change the rotation values to X0, Y, minus 60, Z, minus 90. If we reposition our Viewport, we can see that the camera is now looking down onto the sprite, which is much more fitting for a top-down perspective game. Let's just make a few more tweaks to the Spring Arm. Change the target arm length to 500 and the socket offset to X0, Y0, Z75. We can instantly see the changes that has made in the Viewport. They're just small tweaks, really, but it'll make it feel better when we get to the end. Finally, let's add a new component, another arrow component. We'll use this to help visualize the facing direction of the character has been moved around, as it will not always match the other components in the character. Ensuring that the character itself is selected at the top of the component panel, click the Add Component button and search for arrow and then add it to the character. Name this new arrow component, Forward Vector. With the Forward Vector arrow selected in the Details panel, uncheck the Hidden In Game checkbox in the Rendering rollout. Don't worry too much about this arrow for now, as it will become easier to see its purpose later on in the Module. At this stage, we can compile and save the Blueprint as we're done adding and configuring the components. With our components configured, it's time to move onto the Event Graph. Select the Event Graph tab in the Blueprint Editor. Let's begin by adding the Up and Down movement. Right-click in the Event Graph and search for Move Up to find the Input Access Move Up event and add it to the Graph. Drag from the Access Value Output Pin of the event and search for Add Movement Input. Note that adding it in this way will automatically connect the execution and input pin to the note. Now we just need to define what this scale value will be applied to. Set the world direction to x0, y-1, z0. In terms of getting our character to functionally move in these directions, that's all we need to do. However, now we need to move on to tackle the slightly more complex animation and facing direction issues. Let's start with the visual aspect of the character's rotation. Previously, it was only possible for our character to be facing in one of two directions, and so we could literally flip the sprite to match this direction. However, things are a little bit more complicated now, and we need to deal with the relative rotation of the sprite, as we will be rotating the sprite independently from the rest of the character controller. To do this, we will need to capture the relative rotation of the sprite in the Construction script. So let's head over to the Construction Script tab and do that now. Let's start by dragging a reference to the sprite component into the graph. Drag from this sprite reference and type relative rotation until we can see the Get Relative Rotation node. Drag from the relative rotation and select Promote to Variable to create a new Rotator variable. Name this new variable sprite relative offset. Connect the Execution Pin of the Set node that's been created to the Execution Pin of the Construction Script. Now, if we ever change the relative rotation of the sprite within the Editor, it will automatically be updated and stored in our sprite relative offset variable. Let's return to the main event graph and continue working on the logic to handle this sprite rotation. Currently, our logic for rotating the sprite is at the end of our tick event. We check to see if the Player Character has a velocity in X and the status of the Moving Right ball. We then rotate the entire controller using the Set Control Rotation function. With the Rotator, we created on the fly to rotate around the Z axis. It's here that we need to make a crucial change. Rather than rotating the entire controller and relying on the sprite to match this rotation, we need the sprite to rotate independently from the controller so that we can navigate naturally from a top-down perspective without getting confused. Let's start by breaking this logic away into a separate event so that it's easier to manage. Hold down Alt and left-click on the Execution Pin of the Update Walk Speed node in the graph to break this connection. Use the mouse to make a marquee selection around the nodes following this and drag them into some clear space. You can hold down Shift to select any other nodes you couldn't get in the initial marquee selection. Now let's create a new custom event. Right-click on the graph and begin typing Custom Event into the Search dialog until we can see the Custom Event node. Name this new custom event Update Facing Direction. Connect the Execution Pin from this new custom event into the Compare Float macro. Before we forget, let's make sure that this new event is called Every Tick, as it was previously when connected to the Tick event directly. Locate the Tick event and right-click nearby and begin typing Update Facing Direction to get a reference to a new custom event. Place the node and connect the Execution Pin from the previous Update Walk Speed node into it. With that taken care of, let's return to our Update Facing Direction event. Let's focus on the true path of the branch here. In the Side Scroller version, this meant that the character sprite should be facing towards the right. As this was the default orientation of the character, we applied a new rotation of 0 in x, y and z to reset it. Our top-down equivalent to this will be to reset the relative offset of the sprite to the default orientation. Using the variable we just stored in the construction script. Let's begin by deleting the old logic, the Set Control Rotation function, and the two functions we used to provide the target a new rotation. Drag from the previous Execution Pin and type Set World Rotation, and select the Set World Rotation sprite function. Note that this will automatically bring an a reference to our sprite component and attach it to the target input of the function. Now all we need to do is provide a rotator for the new rotation input of this function. Hold down Control and left-click drag the sprite relative offset variable into the graph to get the reference to it, and then connect this directly to the new rotation input of the Set World Rotation node. Now let's do something similar for the false branch of this logic when we want the sprite to be rotated in the opposite direction. Once again, delete the existing nodes, but leave the Make Rotator node as we'll need that in a minute. Save a few seconds here, we can also copy paste the previous Set World Rotation nodes and variables. When nearly there, the only change we need to make is to invert the rotation of our relative rotation and combine that with a 180-degree rotation in the Z or your axis. Essentially, this will boil down to being the opposite of the above logic we just implemented. Let's start with the inversion. Disconnect the sprite relative offset from the Set World Rotation node and drag from the variable and type invert rotator. Next, drag from the return value of invert rotator and type combine rotators. Next, connect the return value from the Make Rotator node we left behind into the B input of the Combine Rotators function. Finally, take the return value of the Combine Rotators function and connect it to the new rotation input of the Set World Rotation function. And with that, we can take a deep breath. We've implemented the biggest change between this and our side-scrawler character. Now, what we've done so far should already give you a pretty good starting point, but let's build the best foundation we can towards a functional top-down perspective character. One of the first things you might want to add is the ability to fire bullets or attack in a certain direction. Typically, we'd want to do this from what we often refer to as the forward vector of the character. This was much easier with our side-scrawler as we kept the controller and sprite orientations matching. However, in this style of game, we aren't going to rotate the character sprite to match the controller angle, and so we'll need to smoothly generate the perceived forward vector for the character as we go. Let's get started on that process by creating a new custom event and calling it UpdateControllerDirection. We will once again be using the character's velocity to help us decide how to make these changes, so let's add a getVol. Drag from the return value of the node and type Vector is 0. To make our upcoming branch easy to understand, drag from the return value of Vector is 0 and type not. Now drag from this not node and create a branch. Connect the execution from the custom event into the branch. Doing things this way around makes it a bit more human readable in my opinion. This branch will execute the true pathway if the vector is not 0. That is to say, the character currently has a velocity. Let's start by right-clicking on the graph and typing getController to get our current controller. Drag from the return value of our getController function and type setControlRotation. Connect this new function to the true execution path of the branch. Next, we need to supply this new rotation value. We'll do this by finding what's known as the look at rotation between a 0-doubt vector and a normalized vector generated from a character's current velocity. Don't worry if this seems a bit confusing. I promise you'll be able to appreciate exactly what's happening visually in the game with the forward-effect arrow we created. Once the game's up and running, we'll be to see this forward-effect arrow move around and you'll understand what's happening. For now, let's just focus on the implementation. Drag from the new rotation value on the setControlRotation node and type find look at rotation. Now, drag from the start value of this new node and type makeVector. Leave the x, y and z values all at 0. This is our 0-doubt vector. Now, drag from the target input node of the find look at location function. Begin typing normalize until you can see the normalize option here. Make sure to select this one not the normalize 2d. Now, from the normalize function, drag out once more from the a input and type get velocity. With that last node connected, we've made all the changes we need to the character blueprint. So now, let's go ahead and compile, save and close the blueprint. While our top-down 2d character is ready to go, our current tile set and tile maps are really suitable for this perspective. So we're going to have to improvise with basic primitives included in Unreal Engine, combined with a few of the starter assets. Let's start by creating a new level to place the items in. Using the content browser, navigate to content, my content levels. Right-click within the content browser and select level from the create basic asset section. Name the new level, my top-down level. Double-click the newly created level file to open it. If the dialog box pops up to save content, just hit save selected to be safe. Now, we're looking at a completely blank level file. If we look in the world outline into the top right, there's nothing in here at all. Let's start by creating what will be the floor for the character to walk on. In the mode panel, in the basic section, drag a cube from the panel into the main viewport. In the transform rollout of the details panel, set the scale to x16y16z0.25 to give us some space to move around in. While we're here, let's also change our viewport from the right view to the perspective view. This will highlight enough issue. Currently, we'll be in the lit render view mode, and there are no lights in this scene. That didn't matter for the size crawler game, but it does here. Let's just quickly add a directional light so that our cube won't be completely black when we're in the game. Locate the directional light asset from the mode panel by typing directional until it appears in the search dialog box. Drag the directional light from the panel into the viewport. Note that a placement of the actor in the level doesn't influence the light settings. We can happily leave the defaults for this example. In the viewport, we can see that Unreal Engine wants us to build the lighting, so let's do that now. Click the drop down arrow next to the build icon in the toolbar and select build lighting only. Now, if we reposition our camera in the viewport, we can see a visible, if not very exciting, solid white floor. Now that we have a basic floor and some lighting, let's add a few sprites with collision as detail. Navigate to content 2D side-scrawler sprites and locate the ledge asset. Drag the asset from the content browser into the viewport. With the asset selected, press E on the keyboard to activate the rotation gizmo. Rotate the ledge 90 degrees so that it is looking up from the floor in the same direction as the player character. Alternatively, you can set this directly in the rotation section of the transform rollout in the details panel. We can see that the X rotation value has been set to minus 90. With the main viewport selected, press W on the keyboard to select the movement gizmo. And then hold down ALT and left click drag to duplicate the ledge. Be sure to position them wide enough apart so that they will be room for a character to move between them. We can confirm that these ledges actually have collision by pressing ALT C on the keyboard or by selecting collision from the show menu of the viewport toolbar. With the movement gizmo still selected, raise the ledges up slightly if you need to, but ensure that there are no gaps in the collision between the floor and the base of the ledge. Now all we need to do is add a player start from the modes panel by searching for player in the search box and dragging it into the viewport. Make sure to position this player start comfortably above the floor so there's no chance of the character falling through it. Now let's change our game mode to spawn the top down character by default. Let's change our game mode to spawn the top down character by locating the 2D size crawler game mode in content, 2D size crawler BP, blueprints. Double-click to open the game mode and then change the default pawn class to my 2D player character underscore top down. Once that's done, we can compile and save the game mode. We can then close the window and return to the main editor window and start a play in editor session. And that's it, it works. Now we can move our character up and down the screen as well as from side to side. The character also collides with the collision volume with the ledges as we'd expect. If we move around in a circle, we can now also see the results of our independent rotation of the controller versus the sprite component. As you move around, the sprite only moves between left and right facing, but the forward vector arrow shows us the facing of the character controller. Doing things this way gives you a good start point for adding directional attacks or bullets. Moving directly up and down exposes the biggest weakness of our current setup. We have no animation to play here that makes sense, and it looks odd for the character to continue walking sideways when moving like this. We also ignore movement in this direction when switching between idle and run walk animations. However, we could easily adjust our blueprint logic to include a new animation of this type once it existed. And with that, we've laid the foundations towards a paper 2D game that can be played from a top down perspective. While there's still plenty of work to do here, this gives you a great head start versus the starter project. Now that we have everything in this event hooked up, the last thing to do is make sure the event is called as part of our tick event. Begin by locating the tick event in the event graph. Drag from the execution pin of the previous update facing direction node and add in a new event. Once our new update controller direction event has been added to the event tick, we are done with this blueprint. We can compile and save and move on to making a level. In this module, we created a new character and control system for a 2D game played from a top down perspective rather than the side scroller perspective used for the rest of the course. To do this, we adjusted the control inputs to add the ability to move up and down, and added blueprint logic to the player character to translate these inputs into actions from the character movement component. When it came to making the visuals look correct, we adjusted the character rotation and matched the camera to this new rotation. We also took this opportunity to define these as absolute rotations so that we could more easily manage the sprite and character components having different rotation values. Finally, we modified our controller and sprite rotation blueprint logic to handle these extra axes of movement and created a new level that functions from a top down perspective. We finished up by testing the level and considering what next steps would be required to create a fully functioning top down character using paper 2D.