Almost all classes available in the Unrope Python API in Harry functionality from parent classes. Occasionally, you might want to examine all the methods in a given class, including the inherited ones. Using the built-in function, DUR, we can easily print and filter these methods. Let's take a look at a quick example here. If I select a static mesh actor, and if, by the way, you don't see the label static mesh actor here, you can just right click up here and toggle on type. With the static mesh actor selected, in the details panel, I can easily modify the material assignments associated with the static mesh. This functionality is located here, generally, on the static mesh component. But if I do a search here for material, as we sort through them, what you may notice is that there isn't an obvious method associated with getting materials or setting materials. So that's a bit of functionality that you're likely going to need if you're doing this kind of work. But it doesn't live here at least some of the documentation, of course, static mesh component. So I'm going to go ahead and copy the name of this class, because we're going to need it here in a second. And what I want to do is I'm going to use DUR to print out all the methods available on the Unreal static mesh component class. We'll go ahead and copy this. And I'll paste it up here so you can see it. So for item in DUR, and then in the parentheses, we feed in the class that we want to learn more about, goal in print item. And once I've got that, I can simply hit enter and we will see. There's lots and lots of things in here. A lot of this stuff is going to be included in the documentation, but there's a lot of stuff in here that isn't. So rather than looking through it all, I'm just going to go ahead and type in material. And immediately we can see there's a lot of stuff in here that's a lot more useful. So we can use get material. We can do a get material index, get material slot names. We get all the materials. We can get the number of materials. We can set the material. So on so forth. So there is a lot of really, really useful things that you can even set primers on materials. So all that stuff exists on the static mesh component, but it is inherited from parent classes. So if we click here and take a look at the stuff on mesh component, we can just do a search for materials here. And we'll just kind of scroll through. And I mean, it doesn't really matter where it gets inherited from as long as we can access it. And that's all coming from unreal dot mesh component. But we don't really ever need to worry about unreal dot mesh component. All we really need to be messing with typically is the static mesh component, which again is what we'd be looking for for a lot of the functionality here. So just because the method doesn't exist on the documentation, doesn't mean that the class doesn't actually have access to that functionality. Okay, let's write a little bit of code here to explore the relationship between static mesh components and the inherited methods. I'm going to use a little bit of code from our previous function because really I just want to drill down to the static mesh components. So the first thing I'll do is grab the level actors. And then I'll iterate over all of the level actors to find the static mesh actors. And then I will use the static mesh component property to return the static mesh component associated with each static mesh actor. So hopefully that's all clear there. So now that I've got the second mesh component, I want to get the number of materials for it. So we might remember we hop back over to the editor, just explore that list of functionality. We have sitting on our static mesh component here and we can look at materials. So one of static mesh components parent classes has a method on it called get materials. We could go up a hyperlinks looking for this documentation, but an easier way to do this is just to print it here by using help. So I'm going to go ahead and clear the log, clear my search filter there. And what we can see is get materials doesn't take any arguments, returns an array of material interface objects. And that's all we need. So to use it, it's as simple as adding dot get underscore materials and then open those prints. The first thing that I want to do just to kind of keep this clear though, as I'm going to print the name of the object that we're working on. So we're going to go ahead and print the name of the static mesh actor. And then we're getting the list of the materials here in this variable. And then we're iterating over that list and printing it all the individual elements in it, which are going to be the materials associated with each static mesh actor. Go ahead and grab my function name. And as expected, we have the static mesh actor name. And then the material class information associated with that. So as you may guess if we just want to clear that up a little bit, we just add a get name. And try that again. So you can see this is the static mesh name. And then this is the material instance associated with that asset material instances have a lot of good data on them. You can get scalar parameters. You can get texture parameter values and you can get vector parameter values. So I'll just go ahead and we'll grab the texture parameter values. And I'll show you what that looks like. You would want to confirm it. It's got textures and then it's a material instance and some other things. And I'm just going to go ahead and do it. I'm going to try except so that we can speed this up a bit. So we're going to go ahead and print the material name. And then we're going to print the texture parameter values on that material. And then I'll just print a little space here. Let's do a reload. And some of these things are not going to have any texture parameters. But for the ones that do what we can see is it's going to tell you what the name of the texture slot is. And then what the path of the texture is. And once you've got the path, you can find out all kinds of other data like what the compression values are and what the texture resolution is and what the texture group is and all kinds of important performance related data. So being able to get this is really useful. And you don't necessarily want to go through every texture in the game. You kind of just want to grab all the textures related to all the static mesh actors in a problem area. This is an easy way to go about doing that. The next thing I want to show you is how to use the set material method. So let's just take a quick look at what those options are. To filter for material here. So we're looking for set material. So we'll just do a quick help on that. And what you can see is it wants the element index and it wants the material. So we're going to create a material here. I'm just going to put it in my Python folder. So we'll make a material. Let's call it M underscore test. Right click and create a constant three. Plug that into base color. Just to make it nice and easy to see. We'll go ahead and call it red. Save it. I'm going to create a material instance. Call it MI test and save it. So once again, we're going to need. I'm not sure where it went, but let me grab that help again. So we're going to need the element index. And to get that, we're just going to use getting the materials. So we can take a look at that. And that's just going to return an int. So that'll work out well. We are going to go up to our static mesh component. So this will give us our range, the number of materials. So we'll use I as the index, the element index. And then for the material, we actually have to give it the object itself. So we're going to track that down using the path. Go ahead and use the editor asset library class from way back when. We're looking for the find asset data method. And this will be looking for the path. And you can hopefully see it says, for slash game, for slash Python. And once we get the asset data, we can go ahead and just use the get asset method. And that'll return the object itself. Go ahead and save this. Reload. And there you go. You can very, very easily set the material on every single element of every single static mesh actor in the level. So as beautiful as this is, it is unlikely that you want to do this specific thing, but it is completely routine to need to find all of the static mesh actors that are using a particular material or a material instance with a parent that you need to modify and then running through procedurally and updating those is obviously a lot easier than doing it manually. Bigger picture, I find most of the functionality or modifying attributes on assets and the editor were expected to be or where Google suggests I look, but occasionally you will need to take a harder look at the available methods on a class using dur and help is a great way to narrow the search and find useful information about inherited methods.