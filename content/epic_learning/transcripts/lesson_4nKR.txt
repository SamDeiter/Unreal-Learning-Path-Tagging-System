Now we can pick up Orbs, we need the spawner to respawn new Orbs. In this video we're going to set up a few variations to the spawning to give designers more control over the gameplay experience. The spawning system is going to be set up to have a delayed spawn option, so when we collect the orb a new one doesn't spawn straight away. To make the system more versatile we'll set things up so the spawn will have the option of a delayed first spawn or an immediate spawn at the start of the game. For communicating with the spawner we're going to implement an interface. Interfaces allow us to communicate between Blueprints without needing to reference an actor by its specific class, otherwise known as casting. Interfaces help us to keep the file and memory size of Blueprints lower by not having to contain all of the information about other actors which occurs when using casts. We're going to get started by creating our Blueprint interface, so you're going to right-click in the Contemporosa and go to Blueprints and select Blueprint Interface. You'll call it BPI for Blueprint Interface, underscore, pick up. So I'm going to open the interface and inside I'm going to create two functions. The first one is going to be called spawn and the second is going to be called delayed spawn. I'm also going to go into the class settings and set the Blueprint display name to pick up Interface. I'm going to compile and save that and close it down. I'm now going to open up the spawner, go to class settings and under Implemented Interfaces I'm going to add a pick up interface, type BPI underscore and select BPI underscore pick up. Should now see pick up interface appear here. Your interfaces need to be on both actors which are going to use the interface and communicate with each other. So as well as our BP orb spawner, we also need to go to our Energy orb collectible, go into its class settings and add the BPI underscore pick up here as well. I'm going to compile and save both of those and then move over to the spawner and replace the Begin Player event with our new function. I'm going to right-click and type spawn event. Now have two events we can use which have come from the interface. One is event spawn and the other is event delayed spawn. I'm going to select event spawn. This now gives us an interface based event. We can tell that because it says from pick up interface and we also have this interface icon in the top corner. I'm going to plug this into spawn actor. Next we're going to create our event begin play logic. So firstly we're going to create a new variable. The variable is going to be called delay spawn question mark. I'm going to drag that in and choose get delay spawn. In a drag out and create a branch by typing if. I'm going to plug that into event play. If delayed spawn is true, we're going to call the event delayed spawn. So if the pick up interface we're going to select delayed spawn. If it's false, we're going to select spawn. Now you'll need to scroll around for this one to find the right event. There's so many other nodes that are called spawn. I'm going to move this up a little bit at the way because next we're going to set up our delayed spawn event. So I'm going to right-click and look for delayed spawn and select delayed spawn event. We're going to use the delayed spawn event both for delaying the spawn from the beginning but also respawning a new orb after a set amount of time. We don't want the orb to instantly spawn every time we pick one up as that won't make for a very good gameplay. So for delayed spawn, the first thing we're going to do is set our material back to be metal instead of the glowing light at the center. So I'm going to grab the pad mesh and drag out and type set material again. Now plug this in and the material element index that we need is element index 1. And we're going to set this to MI underscore metal. From here, we then going to set a timer by event. So I'm going to drag out and type set timer by event. We're going to connect the event pin to the event spawn here. And I'm going to promote the time variable and call this spawn delay. Just tidy this up a little bit. So when we call delayed spawn, we're going to set the center of the pad to be metal. And then wait, however long the spawn delay is, to then run event spawn. From begin play, if delay spawn is true, we're going to run through that. If delayed spawn is false, we're just going to go straight to spawn to spawn our orb and turn the light on. We need to make sure we set a time for our spawn delay. So compile and then change the spawn delay to three seconds. I'm also going to make this variable instance editable. So each spawn pad can have its own delay time when we place them in the world. We're also going to set delay spawn to be instance editable. This means that each pad can choose whether it's going to have a delayed spawn or an immediate spawn. So we're going to go back to the energy orb collectible. And before the timeline, we're going to tell the spawn pad to play the delayed spawn event. To do this, we're going to use a get owner note. From get owner, we're going to drag out and we're going to type delayed spawn. The delayed spawn message node will attempt to call delayed spawn on whatever the owner is. If the owner doesn't have the interface, this won't work. But as our owner does, it will. If you remember when we first set up the spawn pad, we set the owner as part of the spawn. So setting the owner to self gives us spawned orb and object reference of the spawn pad. So it can communicate back to it with the interface. Which means when we use the get owner here, it will call delayed spawn on the spawn pad that created it. I'm going to compile and save and jump over to the map. With each of the spawners that I've currently got in the map, I'm going to set them up slightly differently. This is the first one I'm going to leave as it is. It's going to have a spawn delay of three, but I'm not going to have a delayed spawn. This should mean that the orb is there from the beginning of the game. For each of the others, I'm going to turn on delayed spawn and set different delay times. Now when we play the level, our orb spawn at different times. If we run over and pick them up, they will respawn at different times as well, as each of the delayed spawn times is slightly different. Now we have the collection and respawn working, we can move on to delivery.