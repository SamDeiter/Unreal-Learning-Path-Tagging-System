It would be nice to have a visual indication of what the current building block is. The method we'll use here will allow the player to appear as if they have a block in their hand. Another nice feature would be to switch between third-person view and first-person view. And the ability to zoom the camera in and out. Our objectives for this module are to implement a visual indication of what the current block is. We'll also support switching between third-person and first-person view and implement a way to zoom the camera in and out. Start off by opening the BP block player. Go to the Viewport tab and then select the mesh from the list of components. Click the Add Component button and search for Child Actor. Name the Child Actor Preview Block. The preview block will be attached to a socket that was created and added to the skeletal mesh. In the Details panel, find the Parent Socket. Click the Search icon next to it and select Block Socket. I'm going to give you a specific set of values for the Transform for the Location Rotation and Scale. You can modify these later. I suggest that you start with these and see how it looks first. The Location XYZ will use negative 7, 6 and 0 for the rotation for the X 110 and for the Y negative 80 and for the Z negative 150. Select the Loc icon in the Scale setting and then in the X edit box type in 0.4 by center. Notice what the Loc icon selected, all three of the axes are locked and they'll all scale to 0.4. Set the Child Actor class by going to the Child Actor class setting in the Details, click the drop down and searching for BP underscore Base Block. Click Compile and Save and observe that you now have a Base Block attached to the player location where the socket is which is right on the player's hand. Go to the Event Graph. We're now going to add a function to set the current Bill Block and change the preview block. To do that, click the Add Function button and name the function Set Preview Block. Set Preview Block will use the components and variables that are already set up so we won't need any parameters. Drag out the preview block and drag out the current block class and get it. To the preview block, drag off of that and type in Set Child Actor class. Find the Set Actor Child class node, click that up. Use the current block as the class that we're setting that to. Drag out the preview block again and from that, drag off the Child Actor. Get Child Actor. Press that to a BP Base Block and hook that up. We need to take the collision from the BP Base Block and set it to no collision. Otherwise, the preview block will interfere with the player. The preview block really shouldn't have a collision because it doesn't respond to anything. It's just being attached visually so that we can attach to the player so that we can visually see what the block is. So drag off of the as BP Base Block and type in Box Collision to get the Box Collision. From there, drag off and type Set Collision Enabled. We want to use the setting No Collision. Go back to the Event Graph and find the spot where we set the current block. That is right here. I think it could be a good idea to put a comment around that while we're here. I'll add all of that to App C Set Current Block. The spot after where the current block is set, drag out a Set Preview Block function call and hook that up. We also want to put the Set Preview Block function call in the Begin Play event as well. I'll drag one out here and after we set up the All Blocks array and get the last index, I'll just hook that up. The reason we're doing that is so that that collision gets taken off the preview block. We'll go back to the map and check it out. Now we have a block attached to us. When we move the mouse wheel to change the block, we see the current block is changing the preview block. It is in fact the block that we are using when we place a block down. Another nice feature to have would be the ability to switch back and forth between the third person view and the first person view. To add that, we'll need to add an Action Mapping. We'll open up the Project Settings. Click on the Edit Menu and Project Settings and then go to the Input Settings and add a new Action Mapping. Open the new Action Mapping toggle FPV underscore TPV for first person view, third person view, and I'll use the Eboard F key for that and close out of the Project Settings. Find an open area to write some code and then right click and search for that new Action Mapping that we just added. Select the first person view and then click on the F key for the first person view. Since this is a toggle, we'll make use of the Flip Flop node. Drag off the press and type in Flip Flop. For the A-wire, we'll toggle from third person, which is the default view, to the first person view. We're going to need the Mesh and what we'll do is hide the Mesh. Go to the Mesh when it's time to go back to third person. There's other settings. We'll have to change, but that'll be the first step. Drag off of the Mesh and type in Set, Inning Game, and hook that up to the A-pin. We want the Mesh to be hidden. I'm going to organize this specific manner here because these nodes are going to be the same for the Beanie option. We're just going to have the settings be a little bit different. After we hide the Mesh, we have to adjust where the crosshair is. I'll get the crosshair and drag off of that and type in Crosshair Y Location, Set, Crosshair Y Location. In third person view, it's a quarter of the way down the screen for the first person view. It'd be good if it were directly in the center of the screen. We'll set the Y location to 0.5. We'll adjust the length of the spring arm. Drag off the spring arm and type in Target Arm Length. We want to set the target arm length. We'll set that to zero for first person view. I'm going to keep moving this over because there's quite a bit of code that we have to add here actually. Get yourself a nice, wide open space to do this. The next thing we need to do is tell it to use the Controller Rotation Yaw. Set Use Controller Rotation Yaw. We want that to be true. That first person view, we just use the Rotation Yaw and directly turn. We're not turning it character. These next bits of code are a little bit strange. What they have to do with is the way the animations work when the skeletal mesh is hidden. When we hide the skeletal mesh, the animations might play for a few frames. It will leave our preview block in a strange location until we start playing the animation. This also affects the animation montage when we place a block. What we're going to do is we're going to detach the preview block from the mesh's socket when we're in first person view and we'll reattach it when we go back to third person view. There's another way to do this, which is, I'll show you this, just so you can try it if you like. You won't have to write the pieces of code that follow. When the mesh component, you can go down to the optimization section and where it says visibility-based tick option, always tick-pose. This will work only when rendered, but it will work partially when you hide the mesh and then it will stop working. You could change that to always tick-pose and refresh-pones and then it will always do the animation even when the mesh is hidden. The problem is, in first person view, that the preview mesh is going to be flying around all over the place because it's attached to the hand and it's going to be waving around in front of you. You can try it if you like. I found that I didn't like it and I went with this code that we're about to write here. Let's continue with that. Let's drag out the preview block and we'll also need the mesh. The first thing I want to do is I want to get the relative transform of the preview block so that I could set it back to that relative transform when we show the mesh again. I'm going to drag off of that and type get relative transform and I'm going to save that to a variable so that we can use it when we toggle back. I'm going to right-click and promote to variable and I'll name that the previous preview block transform and let's hook that up. We have the previous preview block transform. We can attach a component to a component which is really just going to detach from the socket. You'll see that in a second. Drag off of the preview block, attach component to component is what we're looking for and again we're going to have to this code. There we go. That should be the last time for that, hopefully. Hook that up here and the preview block is going to be attached to the mesh which it already is but we're clearing out the socket name this time. We want to keep the location rule of the preview block to the world location. Keep world, keep world and scale, keep world as well. This will now work as far as toggling to first-person view. Let's test it out. Let's compile, save, go back to the map, click play and you can see I could tap the F key and switch to first-person view. Still have the preview block attached. It still changes but it doesn't animate when we move it around and it doesn't do the animation montage because it's not attached to the socket anymore and we can tap the F key all we want. It won't toggle back because we haven't written the code for the B side of this flip flop. For the B side of the flip flop, we're going to duplicate the set hidden in game. Select it, press control W. Make sure you hook up the mesh as the target and hook up the B pin to that and for the new hidden value we want it to be false. We don't want it hidden in game anymore. We'll duplicate the set while location for the crosshair. Select it, control W and hook that up. Make sure you hook up the crosshair target and set the location back to 0.25. The spring arm select the set target arm length to duplicate that. Hook up the target, hook up the execution pin and change that to 300 which was the original length of the target spring arm. For use, control the rotation yaw. We want to set that to false. Now we just need to reattach the preview block to the mesh. So I'm going to select the attached component to component to duplicate it with control W and hook up the execution pin. We want to attach the preview block to the mesh. The socket name is called block socket spelled as you see here. And for the location it doesn't really matter what you put here because we're going to reset that in just a second. So just leave that for to keep world and what we'll do is we'll set the relative transfer of the preview block. So it'll be snapped back to where we originally had it. So I'm going to drag off of the preview block and that over here set relative transform, hook that up and then get the previous preview block relative transform, drag that out and like so. Let's compile, save and go back and test this. Play press F to change the first person view. Let's F again to toggle back to third person and confirm that the preview block is in fact attached in the right location. Let's go back to the BP block player and comment this big chunk of code that we just wrote. So I'll select all of it, press C for the comment toggle F, PV and third person view. So the next feature I think would be really useful is the ability to zoom in and out and we'll use a couple of inputs for that as well. So click on edit and go to project settings and input again and we'll use some action mappings for this as well. So plus on the action mappings and we're going to need two of them. So just add that second one. So the personal add is zoom in and we'll set that to the mouse wheel being rolled up. So find mouse and mouse wheel up. Now we've already got a mouse wheel up and down to select the next block. So let's add a control key modifier for that. So it'll be control mouse wheel up to zoom in and we'll add the control key modifier for the zoom out as well. So that'll be control key mouse wheel down to zoom out. Let's add that right now. Zoom out and mouse wheel down. So we have a mouse wheel down. There we go and close out of the project settings. Now we're going to write a support function for the zooming in and out. So go to find the add function button, select it and name this get min spring arm length. And the reason we're writing this is we want the spring arm to only get so short when we're in third person view. If we let it get down to zero, it'll be squashed up in the player and it won't look good. So we'll just get the to figure out if we're in first person or third person view. We'll just check if the mesh is visible or not. Drag the mesh out and drag off that type in is visible. And we'll need a branch hold on be key. And that will be the condition. So if the mesh is visible, we're in third person view. We'll make two return notes. So select the get min spring arm length and add an output. And that is going to be the min white and that will be a float. So if the mesh is visible, we'll return 100 and just duplicate that node. Oh, it didn't take value there. 100 enter. And if it's not visible, or in first person mode, we'll let the spring arm get all the way down to zero. Now, one other change we're going to make here is we won't make it so that you actually have to call the function. You will call the function, but you won't have to execute the function. We'll make the function pure so we can just retrieve the value from it. And you'll see the difference in just a second. And this is pretty handy if you're just retrieving a value. So go back to the event graph and let's start writing the code to zoom in and zoom out. So we're going to need our two input actions. We'll get the zoom in. Since these are closely related, we'll get the zoom out. Put that right below it. In fact, we're going to kind of write both of these at the same time. So we'll get the spring arm. And from that, we'll get the target arm length. And when the player zooms in, we'll reduce the length by 20. So drag off of that minus float minus float and reduce it by 20. And when we zoom out, we'll increase the length by 20. So again, off of the target arm length, type in plus float plus float and set that to 20. Now we'll use the get min spring arm length function that we just wrote. That right in the middle of them. And notice again, now this is a pure function so that we don't have to actually run the execution wire through it. It'll just run whenever we need the value of it. And you can see the difference here. I'll toggle the pure function. That's what it looks like if it's not pure. So since it's not modifying anything and it's just getting a value, it's handy to be able to do that. Then what we'll do is we'll clamp this float value. So we'll drag off of that search for clamp and clamp float. And the value is going to be between the minimum spring arm length and 1500. That'll be the maximum value. That will allow the zoom out to go. You can set that to more if you like, but I think when you try it, you'll find that 1500's a good value. Let's duplicate this. And we'll hook up the minimum again. And we'll hook up the plus 20 to that. Again, we'll clamp that at 1500. So the minimum values, whatever that returns and 1500. And finally, we'll get the spring arm again. And we will set the target arm length. The target length set the target arm length to the new value returned from that clamp node. And we'll duplicate that. Make sure you hook up the target. The value from the clamp and when we zoom out, we'll hook that up. Kneading that up a little bit. And put a comment around it. Zoom. And raw in and out. Let's test it out. I declared that it worked when I put the comment around it, but I haven't really test it. Let's try it out. So we'll come back in the editor press play. And I'll hold on the control key and roll the mouse wheel back towards me. And I'm zooming out. And you can see you can get fairly close. That's 100 units away. That's probably close enough. You can edit it if you like or change it. And you'll see that the adding block functionality still works. And when we talk all first-person view, we can again zoom out and zoom in as well. So that works. All right, so we successfully added the zoom camera in and out. In this module, we implemented the preview block as a visual indicator of what the current bill block is. We added the ability to toggle between third-person view and first-person view and also added the ability to zoom the camera in and out.