Hello, this is a quick overview of the landscape design tool that we built for Project Titan. The goal with this was to build something that could work at 8km in size, similarly to how the landmass tools work, but also support a really large number of stamps in the world and a large number of modifications to it. For Titan we ended up using about 600-700 stamps and it ran pretty quickly. We've basically sectioned out the map into an 8x8 grid and then we used that to basically only update certain areas of it. It's very, very hacky, very quick and dirty for a lot of this stuff, but I'll just give you a quick run through of how it works. There's a few different ways of controlling the mass in the world. There's stamps, which is the primary way of doing it, and these work by basically taking in a height map. I've generated a load of height maps ahead of time and imported them in. Then I've also got an erosion map that I've baked as well that has a bunch of different points of data in. The idea is that you can use those to generate extra information for texture data. There's the stamps. They have a number of different ways of placing them. You can choose a different blend mode, min, max, fairly self-explanatory. Overlay will basically add the content on top and then lerp will blend between the content. It uses the height as the blend point, so you can control that blend. You can draw a debug box, which will tell you how much space that particular thing's taking in, and you can also scale that in if you want to. Then there's also a cutoff value that lets you set a minimum starting point for the asset. You can ignore height, weight, the weight map information, and then there's some debug information in there as well. All of that needs to be stripped out because it's no longer used. Originally, we were going to do it, so basically every single stamp held weight map information on it, but that just was not practical in the end, so we ended up moving to a generic system for that. There is a generic material stamp as well, so you can basically set in a scalar values, a blend value, and then a noise texture that you can plug into there, and then you can use that to scale this stuff up and down. There's a blend control for that, so you can soften it or make it sharper. The one that I've provided has a bunch of different blend options inside the R, G, and V channels, and that's pretty much it on those. What's nice about that is that you can go in very easily, make your own materials, and set those to override, and then you can blend on top of what you've currently got. And then finally, there is a spline tool for modifying your height information. This is probably the jankiest part of it, but again, I needed something that was really, really quick. The landmass tool that's currently in the editor uses some really quite complicated and expensive methods for calculating this kind of stuff, so I ended up doing a really cheap and dirty approximation of the spline and position, and then output that to a shared texture. This basically allows you to do height conforming, so it can do add and subtract. Again, there's loads of options inside this for blending between different modes so that you can do just the min, just the max, or you can do, again, like alert to control all of that stuff. And then finally, for all of those, basically we needed to figure out a good way of doing order of operation, because again, if we've got 600 stamps, the way that this works is that it's just going to run through all of the ones that it needs to run through in order to regenerate that particular section of the landscape. So if there's like 30 stamps in this particular corner, it's going to run through and it's just going to reprocess them, and it's just going to gather them all arbitrarily. So in order to have a sort operation, so for stuff like this where we've got some addition to the landscape and then we have a subtraction on top of that, there is a tag which basically says this is basically setting the order that it will do it in. So it will go through and find all of the tags for zero, and then it will fire all the ones for one and two and so on and so on until it's done all of the stamps. And that way you can basically increase or decrease the sort order so you can kind of prioritize ones to fire later on in the operation. And that way you can have the stamps firing later. So we can do the subtraction after the addition has been made. That's really important, especially when you're trying to bend between lots of different ones. Depending on the order that you render that sequence, it's going to change how that works. And then finally, we have the height information, the weight information. So this is done through, again, another custom LSD weight that I've added in. There's a bunch of different ways of doing this, but basically you have, I've created kind of two base layers. So there's an angle layer and a height layer that you can add in. You say which weight map target you want it to be. If you click on the angle layer or height layer, it will automatically populate the material and some of the base information in there as well. And then it will do the same for the angle layer as well. So it will kind of populate all of that information in for you. Basically, all that's doing is it kind of runs through the height data and it uses that to calculate it. So all of the erosion information is already pre-processed in these textures. So whenever you set one of these stamps up, it will map that height information to whichever mask you've got in there. And those things are linked. So basically whichever one you pick, it will pick up the correct layer. And that way you get the kind of nice erosion layering and kind of base that you can then start using to blend between different states. So that's kind of how we ended up building it. And that way you get multiple layers so you can go in and add all of your height information. There is a manager, which basically is responsible for kind of doing the accumulation and the generation. So you can see this is my RG packed, which gives me the 16 bit height data for the actual height map. But then inside that, I've also got a blue channel, which is generating all of my erosion layer as well. So I can then use that as a single channel because I have a channel free. So I ended up packing it in. And all of that's kind of in the process of trying to reduce the number of render targets that we're creating. So we end up creating just one for the height map. We create a shape mask, which is what the splines all use to generate. So they all use a single render target for that. And then we have the weight map visualizer that runs from that. So for the duration of while you're editing, the weight map will basically only ever give you a visualization. So with this one, we have grass, sand, and we have rock. And you can add new ones in. So you can say that this one's going to be like snow. And then you can set up your visualization color for that. These are all completely arbitrary. And then basically, you can also just randomly generate colors as well for it. And then if you've tied this to an actual landscape, so you can have a landscape in here, which is how we did it for Titan, there's a landmass interface that will basically go through and you can kind of write it out. So it will generate all of the textures for you. So for this one, we have all of the updates. So we can basically force the whole render target to update. If you've got a landscape, you can tell it to update the landscape as well. So you basically always work in this visualization mode. So this is just a series of planes that are using world position offset to visualize the actual landscape. And then it doesn't actually change the landscape until you tell it to. So you press this button and it will use the landmass interface that I've built in as part of that to basically transfer all of this data over to the landscape. And then the weight data uses the save weight preview function. Because again, we're trying to reduce the number of AK textures that we're generating. It basically renders each of these one at a time and saves them out. So it will go through, it will regenerate the entire weight map, but just for grass and then save that out. And then same for sand, same for rock and same for snow. And that way you basically get the unique weight map data for each one. And that just gets saved out to an arbitrary location. So it's all 100% blueprint. So it's inside a plugin called landscape designer. All of the blueprints are here so you can go in and pick them apart. They're all, again, they're not doing anything particularly crazy. And there's definitely a lot more that you could do to improve them. But basically, it's firing from the construction script. All it does is it, so before it tries to make a render target, it checks to see if there's a landmass already present. And if so, we just use those textures again to try and avoid creating duplicate, you know, kind of render targets that are really, really big. If not, then it will make one for itself. And then we create an invalidation texture, which is an 8 by 8 pixel texture. So again, really small. We use that to basically mask out the areas that we're currently working on so that we only update those particular cells. And that means that even if we've got like 500 stamps, it's still only updating a small section of that unless all of those stamps are really, really big. Then we have our weight map brush, which again we use to do just our visualization. And then we have our shape mask. So basically, we're only creating three render targets to generate all the data. That was kind of the main goal with that. And then we create our dynamic material instance so that we can preview all of it. We create our instance static mesh component, which again we apply the material to and add some instances. This basically sets our mesh that we use so you can kind of see the instancing working there. This is set to a really high resolution, so it does give you one pixel per meter, same as the default landscape. And then it kind of strips that back as much as possible, just to try and keep things reasonably optimized. And then that's basically all the construction script does. So the splat manager will set it up. And then when update render target is called, which all of these assets will call, it will basically run through and render out the target. It gets all of the particular assets that it needs, sorts them by tag, so that basically does the prioritization. And then each one will run through. So we do our weight map accumulation. And then we do our render height target on top of that as well. And that's pretty much it. You can dive in and pick apart the assets as much as you like. If you've got any questions, let me know. I'll be happy to jump on a call and help out any way I can.