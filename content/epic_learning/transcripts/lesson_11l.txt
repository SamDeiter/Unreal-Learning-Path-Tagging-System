In this video, we'll improve on our usage of the Magic Leap Controller by utilizing its full six degrees of freedom by attaching the site model to the controller and moving it throughout space. We'll also toggle this attachment on and off using the trigger of the controller. In Unreal Engine, let's start by saving our level as a new map. We'll call it 0-2-Underscore using ML Motion Controller Movement. Once that's saved, let's go to our Blueprints folder and duplicate our XRPON Touchpad Blueprint. We'll call this one XR-UnderscorePon-UnderscoreMotionController. What we want to do now is replace our current pawn with a new one we're going to use. A simple way to do that is select it, right-click in the viewport, and go to replace selected actor. We know it's changed because if we pull this out, it now says Edit XRPONMotionController, so the name there will stay the same. Because we'll be doing this a few times throughout the course, why don't we rename XRPON to just say XRPON? Open up the XRPONMotionController Blueprint. You can leave or delete your Touchpad functions, because these won't interfere with what we're about to do, I'll leave them. And up here under Add Component, let's add the MotionController component. You'll notice that this is not specifically a Magic Leap component, as this MotionController component gets used across many different devices, such as the HTC Vive and the Oculus Rift and PlayStation VR. It simply denotes that there is a controller that will be tracked in space. Enabling the MotionController to work is straightforward enough. We'll simply click on the MotionController, scroll down its Details panel, and where it says MotionSource, the default will likely be left. We just want to change that to any hand, which just means it'll work with the left or the right hand, depending on what the player has set up inside their Magic Leap device. And from here, just to see it in action, let's add a simple cube as an attachment to that MotionController and scale it down to .1. Again, if the path lock is on, all three of those will change at the same time. Press VR Preview, and very briefly, just take your controller, make sure it's turned on, move it up, and you should see that cube rotate and move with the controller. And if you left your touchpad gestures on, of course, those will still work too. Ideally, there'd be a one-to-one relationship between your controller in the real world and your controller in the virtual world. So let's delete the cube, and let's import a new mesh. We don't have a mesh's folder yet, so let's go to New Folder and type Meshes. Inside the mesh's folder, we're going to import an object that comes with the Magic Leap SDK. So if you navigate back to that folder, you're going to go to Virtual Device, Data, Gizmo Models, and then you'll see one called Controller.fbx. Let's drag that into our new Meshes folder. We will be using this model to trigger events, so auto-generate collision is perfect, hit the dropdown below it, and let's make sure that all the different meshes in this will be combined together. And then lastly, for import rotation, it's 90 degrees off, so let's fix that now. Go ahead and choose Import, and that'll come in along with its materials. If we'd like, we could create a separate materials folder, and bring that back up to our parent directory, and move all of these materials into it. Now be sure to select your controller mesh, and now back in your XR-PON, let's go add component and type Static, and you'll see that we're given the option to bring in the controller as we have it highlighted. We will want this to be attached to the motion controller, so let's call it Controller Model, and then drag it on top of the motion controller to attach it. Let's compile, and see what that looks like, and that looks pretty good to me. Has the correct scale, moves with my real controller, and next we'll be exploring the trigger here on the back. Now that we see our Virtual Controller is aligned with our real world controller, it's time to set up the trigger to attach and detach the site model. To do that, we'll start by going up to Edit, Project Settings, and then Input. We're going to create an Action Mapping that we'll call Toggle Site Model Attachment. And the reason we're doing this here instead of inside the Blueprint is the Trigger is a valuable button, and we may want to change it a later time to be used for other purposes. Additionally, if you type in Trigger, and find the Motion Controller Right Trigger, we can also make it so that the left trigger will work as well. Similar to the any hand setup we already did with the Motion Controller, and if you plan on working in the Editor at all, you could also use a keyboard mapping such as the Spacebar. These could be changed at any time, and now that that's set up, we can go back and door XR-PON, right-click, and if we type in Toggle Site Model Attachment, that comes up as an Action Event now, which will execute whenever any of the buttons we've chosen under Input are pressed. And essentially what we'll be doing is making it so when you press the trigger once, the Site Model will attach to the controller, and when you press it a second time, it will detach. This is a perfect use case for a FlipClop, which is basically a simple Boolean operation that turns something on and off over and over again. The first click goes A, the second click goes B, third click goes A, fourth click goes B, etc, etc. And the first thing we'll be doing is attaching to a component. The target will be the Site Model, and the parent will be the Motion Controller. With a location rotation in scale, all we care about is location and rotation, but we need to make sure that it keeps the world that way when you detach it, it won't take a local value, such as 001, and then send it far away from where you are. So that'll happen on your first click. Next, for the B part of the FlipClop, we'll be detaching from that component. We don't actually want any of these, so pick any, delete it, and the target will actually be the Site Model, which will be converted to its root component. Once again, make sure for location and rotation, we have KeepWorld, just in case someday you decide to change the scale, we can also set those to KeepWorld. And this should work, though, I really do like to have some kind of debug information. So let's start with what we did for the Touchpad, with a PrintString. This one can say Attach, this one can say Detach. Though I'd also very much like to take advantage of the haptic feedback that the Magic Leap Controller provides, so let's add some of that in. For the next node, we'll say GetMotionSourceForHand, which defaults to Left again. Unfortunately, any hand will not work inside this Blueprint, so we will need to choose either the Left or the Right hand, depending on what the Magic Leap User Setup is. In this case, for me, it's Right Hand, and then Next, we'll say PlayHapticPattern, which again falls under Magic Leap. The motion source will be our motion source for our hand, and the haptic pattern and the intensity can be anything we want. When we grab it, I'm going to make it buzz, and when we un-grab it or detach it, I'll do something a little softer, I'll do a click. And this should work. Lastly, let's give this a little bit more space, and put a comment box around it. In the comment box, we'll write MoveModel as Attachment to Motion Controller. With that, I'll set up Hit Compile, or if you like, hit the drop down, and there's an option for Save on Compile whenever there's success. That will save everything. Let's go into our VR Preview mode and see if it works. So I'm going to click. I felt a vibration. I can move the model wherever I want. And when I click again, say I found the table, I can let go, and it's now detached. Click to Attach. Click to Detach. And if we go to our Output Log, we'll see that those attaches and detaches registered. In this video, we located the model using the full six degrees of freedom with our controller, and even began to utilize button input.