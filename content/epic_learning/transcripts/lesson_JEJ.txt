Welcome back again. The next step is rendering of the geometry. So now that CPU has figured out everything, you know, about the positions of objects, and we know what we actually have to render, we start the actual rendering itself. We start off by rendering the geometry. Now this is a very large and very extensive part of the rendering. This is where a lot of the work is done. This also has major implications on performance and workflow. So because it's such a large part, we're going to divide this up in a couple of different sections. So let's start off with the basics and section one. Before we can actually start rendering the geometry, we have another problem that comes up. And that problem is this one. We have to do a prepass or an early Z pass because we know where everything is. We know what we have to render, but we don't really know the order that we are rendering things in. We don't really know what renders in front of what and that gives us other problems because for example, we have a case like this. Let's say you have a very large box. The box is so big, it covers the entire frame. Right. So this background color you see here, that's being just looking at a box. In front of the box, there is a teapot. So there's a teapot rendered in front. Now the rendering happens model per model as well. We're not rendering pixel per pixel, we're not rendering little squares, not rendering lines. We're rendering object per object. So it would literally be rendered background object, next object, next object, next object object, object project. I'll get back to that in a bit. But first this problem, if we've rendered it like this where the background gets rendered first and then the object gets rendered on top because it's in front of it, then the pixels that make up the teapots are calculated here and they're calculated here again. And we don't want to do that because that's very slow. Imagine having a complicated environment where you have hundreds of objects or thousands of objects and they're all in front of each other. If you would render them one by one by one and then render each of them on top of the previous result, then you're going to be recalculating the same pixels over and over and over and over again. You're going to end up calculating millions of pixels that no one will ever see because there is something else that is eventually rendered on top. Okay, that's the problem. And now you might say, okay, fine, why don't you just render the teapot first? Because if you render the teapot first, then you know where you don't render the background. But that doesn't work because you have a situation like this where the blue model is both a foreground and the background at the same time. You get way too many exceptions, so that doesn't work. So what we do instead is we do a z pass, we do a depth pass, we render it in depth, and we basically already pre-rendered the environment. We are pre-rendering the geometry, but with less information than the actual geometry rendering will eventually do. We render it in an extremely simple basic way. But by having done that, at least we know where everything is and we can mask things out, so it will end up being rendered like this. When the geometry renders itself, it had already done a pre-pass and pre-passed already figured out that eventually these pixels will be occupied by a different model. Therefore, when it renders the background, it doesn't even render the pixels where later on something else will be rendered on top. It only renders what it knows will be actually visible in the final frame that it outputs. That's the pre-pass. Now there's not much you can do about this. There are a couple of settings in Unreal. If you are in Unreal, there are in the project settings in rendering. You do see things here related to this, but if you're not entirely familiar with it, I would not touch it. You can see your early z pass, how it's determined, move balls in early z pass and so on. That is related to it, but again, other than that, you don't have much control over it. So I would just move on, but for the sake of reference, this is actually rendering happening first. Good. Now we can start rendering the actual geometry. Now that rendering is done per object, or rather more specifically, it's done draw call per draw call. A draw call is what we call a single thing that is being processed and rendered. Now this usually means a group of polygons sharing the same properties. That in turn usually means an object. So here we have the same scene. Twice, but here we have a, you know, this cylinder is broken up. So here we have five objects. The sky is one model. The floor is a model. And then we have three cylinders. This is five draw calls. This will be rendered in five steps. Sky, floor, cylinder, cylinder, cylinder. This one is six draw calls. Because while this is one cylinder here, the same cylinder we have there, it has two different materials. So every material is also a draw call. If you have a model that has ten different materials applied to it, that is ten draw calls. Okay. So every group of polygons sharing the same properties, these two are not sharing the same properties. There are different material. Um, and this tends to add up. We'll get back to it in a bit. This is why the draw calls are one of the heaviest expenses you have. You have to get this right if you want to have good performance. This is actually rendered like so. This is the actual rendering from Unreal Captures. Without textures, just the normals, but you see the Z-pass in action as well. It first figures out it wants to render the floor. It renders the floor, but without any of the pixels where it knows it will be covered by something else later. Then it renders the bottom half of the cylinder on the right, middle, left cylinder. Then eventually it completes the cylinder with a top. You can clearly see this cylinder has been done in two steps. And then the sky is eventually ahead behind it. So it renders object per object is being outputted to the G-buffer and written to the frame. The render order, let's actually go back to the slide for a render order that you see here, depends on a number of things. None of them are too important for you, you know, when working on these things. It just happens all in the background. The reason why it took the floor first and then the cylinder there is because it tends to sort the scene by material type. Because that's fast. Otherwise it has to do a render stage change in the hardware. And it doesn't really matter exactly in which order it does it in as long as it groups them together. And this is why it did the bottom of the cylinder first. Then the next one, because this has the same material as that one, then this one, because now all three of them have the same material on it and eventually completes it. That's why it didn't do the bottom, then the top and then this one, because that would be a switch in material as it switches to that one and then switches back to the middle one. Other than that, don't worry too much about the render order. It's not much you can do about it and it doesn't really impact you. Now you can measure this with start urhi among other commands. Let's go to the editor. Let's show you this in here. So that means that every object here is a draw call. This thing, that thing, that thing, that thing, that thing, all of them. If you have the command start rhi from rendering head interface, you can then see the number of triangles drawn and the number of primitive calls, the draw primitive calls, that is the draw calls. We have 1091 different objects that are being rendered here, different draw calls. That results in 620,000 about triangles. Now understand that lighting does things to this as well. So what we're going to see here probably is that in my chains when we turn on lighting, you can see when we turn on lighting, mean how if A's in the thousand triangles and we have 1130 draw calls. So there is a difference there. Also be aware that unreal and most other real time renderers, they're so complicated nowadays. There is a lot more happening beneath a scene and beneath a surface than just rendering the object. So if you would make a completely empty level or you just import a model like a simple box, the numbers you see here are not going to be what you expect. There is a certain base cost. You'll have for example 100 draw calls even if you only have a single object in front of you and you might have around 100,000 or so triangles even for a black empty scene because of some hidden back end things being done. So I'm just pointing out this is not exactly going to much of what you see in a 3D program. In any case, start rhi shows you the numbers. 2 to 3,000 is reasonable. That's kind of standard for most games nowadays. It's standard for what the hardware is built for. You could do 5,000, probably still runs more than 5,000 is getting high. More than 10,000 is probably a problem. Probably is the key word here. It's very difficult to say when something will be a problem. It depends on the hardware, it depends on what you're doing with it, it depends on the rest of the rendering. For example, you could have 20,000 draw calls but use almost no other features. And in that case, 20,000 is going to be fine. You could have 5,000 draw calls and get really slow performance because you've done something else that's really heavy and that takes up the performance. It depends on the whole picture. But usually around 10,000 is probably not the best idea. If you're doing a mobile application or game or you're doing a VR application or game, the number is far lower. Usually a few hundred for mobile VR that say a thousand ish. Again, it depends on what exactly you're doing. The draw calls have a huge impact on performance, as it says here. Each time the render is done, it needs to receive commands from the render threat which adds overhead. The draw calls have a much bigger impact in polycount in many ways, scenarios. You know, people used to be very fixated on triangle count polycounts. How many triangles is this model? How many triangles is that model? But the truth is that nowadays it doesn't really matter that much anymore. It used to matter in the 90s when the renderer was so simple and had so few features that one of the only things it did have was the polycon count. So yeah, in that case, the polycon counts an important thing to keep in mind. But in this case with under-langen 4 and most real-time renderers out there, in the hardware we have nowadays, it's actually the draw calls plus many, many other factors which have a much bigger impact than the polycon count. Look, the problem with the draw calls is this. Imagine copying a single one-gear byte file on your computer. How long would it take? I don't know, a minute to two, three minutes, you know, something like that. And compare that to copying one million, one kilobyte files, we get a still one gigabyte. So the amount of data is equal, right? If you want to put it in model terms, the amount of triangles is equal. But the copying one million tiny little files is probably going to take a day or two days or half a week or whatever it would take. It would take a long, long time for the computer to complete it. Well, it's the exact same amount of data. So it's not necessarily about the size. It's about how you handle that size, about what you do with it. And the reason the computer would be slow with those one million files is because it would start, transfer file, stop, then call home and say, I'm done transferring the file. Can I please have the next file? Then the next file has to be searched somewhere and pulled off from the hard drive and transferred to the new location. And again, and again, the same goes for draw calls. If you want to put as an extremely simple terms and kind of stupid terms, we honest, then it would be that every time it's done rendering something, it has to call home, phone home and say, I am done. Could you please give me the next piece of information? Then it needs to wait because it takes a bit of time before the next piece of information has been found and has been transferred. And then when it gets to the next piece of information, it's can't start rendering that again. But we have to delay. We had a delay where renders a little bit stop renders a little bit stop renders a little bit stop. That's stop in between them. That's what's getting what's getting to you. So here's a very nice example. Here we have a number of planes. I'll show you the planes in the editor first before we talk about this. Just to give you a better understanding. Here is the player looking at the pilot shaders one second. So it's just an orange reddish plane. That's it. It has a single material. It is fairly high poly. It's probably going to be hard to see in the video. But it's 32,768 triangles. So it's high poly, but it's just a plane. In the next slide, by the way, we want to show you this one, which is the exact same plane, except it's only 32 triangles. And it has a lot of materials. There's two opposite, two extremes. One very low poly model, one very high poly model, one pot model with only one material on it and one model with a lot of materials on it. In this scenario, here we have a couple of hundred of them in front of us. And this is rendering 86 million triangles. So this is 86 million triangles. And it results in 3000 draw calls. 3000 draw calls, 86 million triangles. And it runs 33 frames per second. Here is exact same environment, same scene, but all the planes were replaced by the other one. This is only 100,000 triangles. So it's nothing. 86 million compared to 100,000. And it's 44,000 draw calls. This runs four frames per second. The 86 million 30 frames per second, 100,000 triangles, four frames per second. The difference is the draw calls 44,000 compared to 3000. In fact, here's even more extreme. Here is just two orange planes. This results in 157,000 and 186 draw calls. And this is 236 frames per second. This is 50% more than the triangle count of this. So the triangle count is really not that important, at least not for this part of the rendering. There are other parts with a triangle count, also an impact, but not for this part of the rendering. The draw calls are much, much more expensive because what we have here is the absolute worst-case scenario we could throw at the engine and the renderer because every single triangle is actually a different material. That means that every single triangle is a draw call. That means that every time it renders a single triangle it has a stop, wait, render, stop, wait, render. So that's why it's slow. We'll continue in the next video with the next part on the geometry rendering.