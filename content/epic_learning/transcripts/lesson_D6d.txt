We are now going to replace the default motion controller meshes with the animated 3D hands from the unreal engine VR template and Develop logic to display the correct hands for each controller. Let's start in a new project so that we can retrieve the skeletal mesh of the VR hands that are Provided in the VR template. I'm going to create just a blank project, but you can also start in the VR template. I will keep this project and our studio showcase project separate so that we can take only what we need. If you started with the blank project as I did, you can click the add new button and select add feature or content pack. This will provide you with all the assets of the different templates. Select virtual reality and add to project. We need to search for the mannequin hand right skeletal mesh. It is in the content virtual reality mannequin character mesh folder. Right click on the skill to mesh and select asset actions and migrate. Here's a list of all the items that we're going to need to get started with the hands except the single animation asset which will grab it a bit. Migrate them into your studio showcases content folder. We will also need the mannequin hand right open animation in the animations folder. Migrate that as well. Let's return to our studio showcase project and open BP VR hand select the motion controller component and turn off display device model. Now we'll add a scale to mesh and set the mesh to our new mannequin hand right. If you zoom into it, you will see that it is of course the right hand. Let's set up some logic to change the scaling based on which hand it is for. In the construction script after we set the motion source in our motion controller component, add a set relative scale 3D node to the scale to mesh. Drag from the new scale 3D input and add a select vector node. We will select which option to pick based on if emotion controller hand is equal to left in a meaning it is the left hand. Make the scale one minus one one. Adding that minus one to the y coordinate will flip it to match the left hand in B make it 111 all positive so it is how we see it here. Test this in the viewport by changing the value of emotion controller hand. And then let's test this in VR. Cool. There we go. Now that we have the skeletal mesh of our hand in our motion controller blueprint again feel more like we are interacting with the 3D world. Next we give the hands life by creating an animation blueprint to control the hand poses.