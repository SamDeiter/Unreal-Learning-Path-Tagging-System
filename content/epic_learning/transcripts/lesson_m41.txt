The Magic Leap 1 can detect 8 different hand gestures on both the left and the right hand. Once you've located your site model appropriately, perhaps you'd like to show that model to someone without having to explain the functions of a controller. In this case, gesture detection may be an ideal way to allow them to interact with the model using only their hands. In this module, we'll return to the two design options set up earlier in the course and use a hand gesture to toggle which one is off and which one is on. To start, let's save a new map. We'll call it 06, underscore, using ml, underscore, gesture toggle. Once that's saved, let's go into our Blueprints folder, find our most recent pawn, and duplicate it, and add gesture detection to it. This pawn, once again, will replace the current one in our map, so we'll click XR pawn, which is currently the motion controller one, click in the scene, and say replace selected asset with gesture detection. We can now see that that's correct. Let's open up the pawn and add gesture detection. The first thing we need to do is tell and relunge into look out for certain gestures inside the Magic Leap. We do this over in event begin play because we'll need to have this search happening right from when the experience starts. So, let's drag out from the event begin play pin, and we're going to set the configuration under Magic Leap, and now we will have an array, which shows which gestures we want to be looking for, so we can drag out from static gestures to activate, and choose make array. Here, we have all the eight gestures magically pin recognize, in addition to no pose and no hand. Open hand back is a very easy gesture to recognize, but I also find it can be triggered accidentally. Similarly, with thumb gesture, if someone is using the controller, this can be triggered accidentally as well. A good discrete gesture to recognize that is not likely to be done accidentally would be the OK symbol. Let's choose that one, and if we wanted to add any more gestures to be recognized, we would do that with add pin, but for the purposes of what we're doing right now, one is enough. Moving forward, the next thing we need to do is set the gesture confidence threshold. This simply designs how confident Unreal Engine has to be that a certain gesture is being seen before it activates that action. Let's change that to the OK symbol, and let's ask for a confidence of 90 percent done as point nine as a float value. That's all we need in begin play. We can do a quick comment box, and say set up gesture recognition. We don't need these, so we can actually just delete them to clear out our space a little bit. Now let's scroll down, and what we're going to do is make it so that the left hand OK symbol will trigger design option one, while the right hand OK symbol will trigger design option two. If you'd like, you're welcome to try this as a single flip flop, where a single gesture from either hand will toggle between the two design options, but I find that sometimes that can happen accidentally, so it's better to have a discrete option on a discrete hand. So, let's look for the left hand OK symbol, so if we type ML, left, OK, it will come up as a gamepad event. Similarly, let's type ML right, OK, and that also comes up as a gamepad event. Let's give that some space. And before we start building out this blueprint, let's think about what we'll need. We'll be toggling the design options, which are actors, just like our site model. So why don't we go ahead and duplicate our site model, and we'll type option one there, and it's already set up as instance editable. So let's make sure we set that variable when we exit out of our blueprint. And then we can duplicate here for option two over under ML left, OK, we're going to want to set the visibility of none of these, so you can pick any of them and delete the option that comes up. Set visibility is done with scene component. We are using actors, and typically if you'd want to show or hide an actor, you would do it with set hidden in game. But this does not allow for propagated children, and because our actor has many children that we want to turn on and off, we do want to use set visibility. So let's delete that, and drag site model option one into the target, which will convert it to its root component. Now remember that for the left, OK option, we want to turn site model option one on as well as all of its children. Additionally, we're going to want to turn site model two off and all of its children. So we can copy this over and paste it over, give it plenty of space. Except instead of site model option one, we'll want site model option two. For site model option one, we want everything to turn on. For site model option two, we want everything to turn off. And that's it. Though of course I'd also love to have a string that confirms what we're doing. So our output log will read, activate, design, option one. Now of course I'd love to also add some haptic feedback here, but we won't be holding a controller so that won't do very much, besides confuse whoever is standing close to the controller. Instead let's add a sound. You're welcome to use a new sound similar to how we've done in previous modules, but because the user will likely be done with image tracking by the time they're toggling design options, I'll use it. We use those sounds again. For design option one, I'll use the image tracking found queue, and for design option two, I'll use the image track lost queue. And now for the right hand, which we'll toggle design option two, we can just copy all of this over and make a few little changes. For site model option one, we'll be turning that off. For site model option two, we'll be turning that on. Over in the string, we'll want to say that we're activating design option two, and as I just said, we're going to be using the image track lost queue. Let's put a comment box around all this. We'll say toggle design options with gestures. Let's compile that, close out. Don't forget to set up your options, including the site model, which does disappear when you swap out the pawn. There's our site model. Here's option one, and there's option two. And now with your magic leap set up in zero iteration, let's go ahead and play. And we can see of course, design option one, and design option two. Very easy to compare and contrast. Note that if you're using a newer version of magic leap's SDK, 0.22.0 or later, you'll also be able to activate hand-meishing, where your hands will include digital geometry, similarly to the static objects in your environment. Now as I mentioned, there's no haptic feedback because we're not using the controller, but having an appropriately selected sound can do a lot to add to presence. For example, here, the sound being queued by the left and right hands feel like they're being generated by my fingers touching when I'm forming the OK gesture. Congratulations! You've taken your first steps towards using your hands.