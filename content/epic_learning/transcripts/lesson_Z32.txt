The next stage for allowing the viewer to click on the shader ball at runtime and randomly change the color is to add the blueprint code necessary for that to work inside of the level blueprints. So to get to that we'll go to Blueprints at the top and open level blueprint. From here all we really need is event begin play again if you have anything else you can feel free to delete that. The first thing we need to do is allow the viewer to see the mouse and be able to click with it. For that we're going to need a reference to the player controller so we'll right click and search for get player controller. And this essentially represents the viewer. From here we're going to pull off of the return value and we're going to search for show mouse cursor. And we want to use set show mouse cursor and here we want to make sure this show mouse cursor has a check box next to it to enable it. Now we'll pull off of the event begin play output and pipe that into the input of our set show mouse cursor. From here we'll pull off of the get player controller again and this label search for enable click events again we want to use set enable click events and we'll pull off of the output of our show mouse cursor and pipe that into the enable click events again make sure that enable click events is checked. Now we'll be able to see our mouse cursor and click with it. So from here we need to test where the viewer is clicking and if he or she is clicking on the appropriate object then we will fire off the custom event using an event dispatcher that changes the color of the object in this case the shader ball. To do that we need to get the result under the click so we're going to pull off of get player controller again and we're going to search for get hit result under. And in this case we want cursor for objects. Before we can proceed we need to specify what object types we want the viewer to be able to click on. So we'll pull off of the object types input here and when we do so this gives us access to make array. So we'll go ahead and click make array and right now we're allowing the viewer to click on static objects in the world if we wanted to we could click this add pin here to add another element to our array and we could specify also allowing the viewer to click on dynamic objects. In this case we only really need static as the shader ball is not a movable object. Next up we need to get the hit result so we'll pull off of the hit result and do a search for break hit result and this will give us access to all the information about the object that we've clicked on. To go any further we need to test for the left mouse button click so we'll right click and search for left mouse button. If it's clicked we want to test whether or not we clicked on one of the object types here. So from pressed we'll do a branch and then for the condition we want to bring the return value of our get hit result under cursor and pipe that into the condition. From here we need a reference to the actor we clicked on so we're going to pull off of the hit actor output here and then we want to do a get class because we need to know which actor we clicked on we need to know what type of object it is because if it's not the blue print shader ball we don't want to continue. So now that we have the class of the object we're going to find out what class it is. To do that we'll pull off of the return value and search for class is child of and for the parent class we want to put in here our BP underscore shader ball so we'll search for BP underscore shader ball. If the object we clicked on is in fact of BP shader ball then it will return a value of true which will allow us to proceed with changing the color of the object. For that we're going to pull off of the true value here on the left mouse button branch and we'll add another branch so we'll search for if and then we need the condition of our object whether or not it's the right class we'll pipe that into the condition. Now from here we need to get an actual reference to the actor so we're going to pull off of hit actor on our break hit result and we're going to search for cast 2 BP underscore shader ball. Using this reference we can now get access to the custom event that will fire off our color change. We do that by pulling off of the as BP shader ball output here and do a search for color change event and this is the event that we specified using the event dispatcher. Now we need to change the color value using a random notes we're going to right click here and search for random vector and we want random unit vector and then we can pipe that return value into our color value here. Last of all we need to take this branch and pipe the true output into the cast 2 BP shader ball. So now we can see that when the object is clicked on it will test whether or not the object is of the correct class that is the BP shader ball and if it is it will use the cast 2 BP shader ball node here to get access to the color change event and change the color randomly. So now we're all set let's go ahead and save this. We'll play the scene and we can see that right away upon creation it changes the texture to the rock texture and it also changes the color to green. Now if we click on any of the other shader balls nothing happens but once we click on the BP shader ball we can see it's beginning to change the color at random. Every time we click we get a different color. Excellent. Dynamic material instances give us a lot of flexibility and are quite simple to set up. Being able to alter materials at runtime will allow for all sorts of interactions for our viewers such as being able to customize the environment. Next we'll look at how to alter multiple copies of the same parameter across multiple material instances using parameter collections.