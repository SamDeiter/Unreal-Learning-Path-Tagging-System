We're now ready to export our assets from SketchUpPro into Unreal Engine. The first step is to make sure that the Datasmith plugin is installed. Now we can find this using a browser. All we have to do is go to the Unreal Engine website and search for Datasmith plugins. You should be able to find a page that looks something like this. Now this is basically just a download page. It contains all the exporters for all the packages that Datasmith supports. In here you should find a SketchUpPro exporter and it will tell you what versions of SketchUp that it supports. In this case it's 2019 through 2022 and I'm using SketchUp 2022. This should be the one that works. You want to make sure that you get the one for the operating system you're using SketchUp with. Once you have that all you have to do is download that exporter and that will bring up a file that you can then run to install. Now I've already installed it so I'll let you go ahead and run this and make sure that SketchUp is closed when this program is run so that way the install works properly. Once you have it installed you should be able to go into the extensions window in SketchUpPro and you should see this in the extension manager. So if you open up extension manager you should see Unreal Datasmith. Make sure that that's enabled and you should be ready to go. Now you can find this in a couple of places here. We can go into extensions. We have Datasmith where it has all of the options for the plugin or if you right-click above your toolbar you should be able to turn on Datasmith and that will give you a floating window that you can drag up to the toolbar to dock it. Now once you have this installed you should be ready to export your SketchUp assets into Unreal Engine.