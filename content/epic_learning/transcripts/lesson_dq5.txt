Alright, so back here we have a character setup and we should be done for now. What we can do is maybe select all the nodes, press C on the keyboard and that creates a command. That's easier to organize and make sure you know which part is what. Compile, save and close. You don't know we don't really need anything else in the player. Now the widget has button but nothing is really happening to it right now. Where we do that is on the graph side. So if you click on the graph here on the top right corner, you can see that there is some nodes already set up. We don't really need them for now so let's select them all and delete. This is basically the blueprint for the widgets. Compile. You can see that the variable, the button are already there. The go there automatically and we'll set it up for you. So you can see that if I click on the button, I do have events. Event basically will fire every single time the button is on click, on press, on release, on over or on over. What interests us right now is on click. So let's select for example button wall color A and click the little plus. Any unreal will create event for you. Now we have on click button A. This will fire every single time we press on the button A. So we know that we need to change our material collection. Unreal can help us find the nodes and we know that we have to do material collection and we need to change a vector parameter. So drag from there and start writing. Vector and you can see it already showing off. Material. Get vector parameter value or set vector parameter value. We want to set it. Now within the collection you can click the little plus and select my color or select it in the content browser again and click the little arrow. Now the parameter's name we need to decide what it is. In this case is the wall. Click wall. And if you try to compile it will have an arrow because the parameter value has no nothing attached to it. So it doesn't know what to do with it. It knows you want to change that one but it doesn't know with what. And that's a linear color structure. So if you drag it and say make linear color structure that's a bit complicated. So delete that and instead right click on the pin and say promo to variable. And that will be a wall color a. Now you can see that the default value it asks to compile the blueprints. Let's compile and now we can choose a color. So we could choose a color but for the color a will want the first original one. So again go back to your material collection. Right click copy. Right click paste. Now color a is the default color. So let's do the same for color b but before that let's set up of all color b. Click on the variable color a and do control w on your keyboard. It will duplicate it. We name it and that will be color b. Again compile to make sure you can change it and change the color. Let's take an orange yellowish. Come sing it. Click okay. Compile. Now we want to assign that to the bottom B. Select the bottom B and like with it before on click create the new event. Select that node. Put your mouse in the correct place and control w. Duplicate it where your mouse is. Connect them and color b value. You can do with control. You can drag it and select get. Or you can directly drag it to the pin. The tree way works. Save. Now let's try that to see if it works. If we press the button a, it would be the default color. We press the button b. It will be the color b we set up here. Let's play. Right click to move the mouse. World color a, nothing happened. Color b, color changed. Now the lights are storing the light map. So that's perfect. The color will change without affecting anything. Reflection itself would be affected. Color a, default color, color b, new color. That works. So now let's set up the second one. The curtain. For to do that, let's create two more variables for the color. Duplicate this. Control w on your keyboard. It's called it curtain. Color a. Duplicate. Curtain. Color b. Now for the color a, we go back here. Copy. Compile to make sure we can affect it. Paste. And the second one here, we make something like a blue. Save. Select these two nodes. Control w to have a copy of it. And then on the bottom for the curtain, a, on click. Plus b, on click. Plus. We have a two new event. Connect. Connect. Now let's select our color for the curtain. Color a. Color b. Now let's not forget to change the parameter name. Because right now it will modify the world color. We want it to modify the curtain color. So for this two, compile and save. Let's try it out. Press play. Move around. The wall default. Get all curtain default. Blue. You can see the reflection changing as well. All right. All of that is working. Have a different color interior. Just with some button. You can mix and match. Now for the next step, we'll want maybe that the UI is not there all the time. And only when we press spacebar, it shows up. And at the same time, if the color a is already set up, we cannot click on the button. And if you click on color b, then this one gets gray out and this one is active. So let's set that up now.