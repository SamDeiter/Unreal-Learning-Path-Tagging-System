The next one is the pixel shaders and HLSL. First thing we're going to take a look at is how they tie into the material editor. How they're part of the wider architecture and how it's working behind the scenes. You understand the bigger picture and shader permutations because that's something you're going to notice. I want to talk a little bit about what's happening there and why it's happening. So let's start off with the architecture. The materials render using pixel shaders and vertex shaders. Now if you're not familiar with what pixel shaders and vertex shaders are, you should refer to the rendering class that we've done because we explained it in that plot they are there. So we're going to go over this little bit faster here. I'm still going to go through the basics. These shaders, the pixel shaders and vertex shaders are responsible for the calculations on vertices and pixels that contribute to the final image. Essentially there's small little programs that run series of calculations on every single vertex or every single pixel. They're extremely central to the rendering pipeline of any modern real-time renderer. Now these pixel shaders, they differ per platform. They usually have written in HLSL, high level shadow language. And that's what is used on DirectX, so computers, PC, Windows, etc. And then in Unreal it gets cross-compiled to the target platform. So every platform, for example, an Android phone and Android device, is not going to use HLSL. It's going to use its own format, though we cross-compile into those other formats. We actually write it and author it in HLSL, which is a standard format for DirectX. At the very bottom here you can see a small piece of sample code. And we're going to get back to that several times later on through this class. Now usually it works this way. This is how most real-time renderers handle it. You would write a shader in HLSL with certain key variables undefined, for example what texture it uses. So what it does is define the functionality it's defined, but to precise numbers how reflective is it, how bright is it, what color is it, what kind of texture is it, those things are undefined. Within the editor, you would then define those things. You would then say, okay, please use this shader with this HLSL code. But these are the textures and for example the texture tiling and how metallic it is, etc. These are the values you're supposed to use and then eventually it gets rendered because it takes the shader plus the values you input it. And that's what you see. A real-go is a little bit further than that in order to empower the user. So in Unreal, we do the same thing. We actually write the shaders in HLSL, which I'll show you in a second. And then the material editor exists. The material editor then uses the code that's here and murders its own work with that and then generates another shader in itself. So this is the actual HLSL shader that's going to be used, but that's a combination of essentially a template shader mixed with the work you've done in the material editor and that generates a new shader. That also is still not always, but usually has certain values undefined, which the material instance system can then tap into and define those values and then eventually that's what you see. So we have two different tools that are embedded in here in order to empower you. Now to show you how this action works, you can see here in In-Engine slash shaders, you can see these files. Let me go to that folder here. You can see this is where I haven't real installed. In Engine, in shaders and then in this case, private, and you can see these are the files that are being used. I open one of them up, you can just open them up in any text editor. This is for example, a volumetric fork, but it's just a random one I picked. And you can see this is the actual HLSL code that is resulting in the look and feel and the functionality of in this case volumetric fork. Now it's also important to understand that it within the material editor, it's actually made up of two different parts. And we're going to talk about that at length later on when we really dive into the material editor, but you've got the expression network, which is what we have over here. And then we got that big block at the end. In fact, let me switch to Unreal, show you directly in there. Take any any one of these materials. So you got this part and you got that block. You can see there's certain inputs undefined here. They're actually grayed out and they're disabled. The number of inputs that are enabled depends on the settings here. And again, we'll cover all of this in that later, but the point is, what you pick here will result in different shader templates being used from over here. And those shader templates in turn will enable or disable certain inputs here. So this thing is controlled mostly by what happens here in combination with the settings you have on the bottom left here, whereas all of this work is up to you, the user, in the sense of how you combine and how you build these big graphs and networks and so on. So it's two distinct parts is the combination of that work with that work that makes material system work. Now, because every usage situation demands something specific of the rendering, the generated shaders must support a large set of permutation. Right? So if you go back here for a second, we generate a shader, but this shader is going to experience a widely different set of usage cases. So in order to work for that, we're going to have to make sure that we have a lot of permutations and all the different versions are going to have to be generated. We wouldn't be able to support too many features within the same shader, because that would be sub-efficient. So therefore, we automatically behind the scenes without that you notice it, we create many different versions of every single material that you make. So here's the problem, right? You have a material that compiles HLSL and in fact, I haven't shown that. Let's do that first. Just to show you this, you can actually see the HLSL code in window, HLSL code. This is what this material plus the underlying existing HLSL code generates. Now, this is too complicated to really dive into. You can't change it either. This is more just to give you an idea of what happens behind the scene. But either way, that shader gets generated. Right? And then let's say that that shader on that material in essence is going to be used on this box, but this box is statically lit and therefore light mapped. So that it has a certain piece of functionality that's unique to this box. It has a light map, because not everything has a light map. So therefore that shader will not have the required code to blend in the light map with the rest of the functionality. So it's going to append a certain amount of code and say, okay, so this is all the functionality we had, but now we're going to add this to it in order to resolve this particular usage scenario. So it has now made a new version of it. And for every usage case, it encounters is going to do the same thing again. So this is the new piece, the shader, and in the new version is going to change a certain part of the code. This results in what you sometimes see in Unreal, where it says, I'm going to compile shaders for you, 32,634 or whatever other number you've got. Usually you don't get huge numbers. Usually you get a number like say it's going to compile 200 shaders or 400 or 800 and you're going to wonder, but I don't have 800 materials. I mean, I for sure is certainly don't have to it 2000 different materials. So what is it doing? Well, it's building these permutations and the permutations are partially down to the usage box within the material. Now a few people ever pay attention to this, which is not the best thing, because this has the potential to sometimes break a project in certain cases. Show you this in the editor as well. Within the material, if you're in the main properties here, towards the bottom of this section, you got a section called usage. This is what is used with. For example, that light map scenario on that box we just had, that's this used with static lighting. This essentially tells the shader, please make a version that has been prepared to blend in a light map and some other properties. So if you do that, then we'll build a second shader permutation for this usage scenario and the same goes for all the other ones. Now these get set automatically in editor. This is why most people don't think about this because the editor is automatically figuring out, okay, you just apply this to a box with static lighting and it automatically sets that true. This is also why you sometimes open up a project in Unreal. You don't really do anything. You just open something up and then you close a project and yet the editor is asking you to save a number of the assets. It says some of those assets were modified and you're going to wonder why are they modified. I didn't do anything. I just opened the project. Well, it's modified for one of the reasons it gets modified is because it automatically sets this sometimes. If you fail to set this correctly, then everything will work just fine in the editor and even when you run the game or project. But when you eventually package the project, when you eventually would do this, package the project and wait for a while right, have it packaged and then eventually you get your distributed. The executable you can send to other people. When you run that executable, it would fail and it would fail to show the material and you would have a default material displayed on that particular model. That's because of these flags. But that's not the only reason because that doesn't explain why we get tens and tens of thousands of shares. There's a lot of other things happening in the background. Some of it is not exposed to the editor. So the things we see here are only the things that are exposed to the editor. But again, there's more things happening than just this. But that explains the very large numbers. If you use a quality switch, then it will make a different shader version for each and every one of these versions for one of the inputs. Same for a switch. It will make a version where this is true and make a version where this is false. So if you got 10 switches lined up, if you got a large master material within a must material instance setup, something we'll discuss later on. And let's say it has 10 different switches in there. You're going to have a lot of permutations because every combination of true and false results in a version of that shader. Same for the feature level switch. Same for, for example, you can disable direct extents shaders as a shader model for that has an impact there as well. There's a couple of other months you can enable mostly for mobile or open jail there. There is a flag within the project settings that says allow static lighting. If you turn it off, you actually reduce the number of shaders generated automatically as well. There is some boxes where actually it's literally called shader permutation reduction. And again, there too, you can disable these things because the strip certain functionality out of the shaders, therefore resulting in fewer perutations. Normally, you don't care about that. You just leave it be regularly usage the default settings will do fine. We'll only touch these things if you really actively trying to reduce the number of shaders in your project. And again, for most project, there is no need to do that. In this section of the class, we've gone over pixel shaders and HLSL. We looked into how it ties into material editor and we took a look into why shader permutations are created and why it creates so many of them and how we can possibly reduce the amount a little bit. I hope you have a better understanding of the general architecture now and how it's tied together behind the scenes and we'll move on to the next section.