So we're going to talk about programming in this class. And well, I'm Mario. We will do a first intro and then a schematic overview of the content. For the intro, basically, the philosophy behind the programming pipeline and workflow that we have is to provide all the tools a programmer will need to create a project. So not only provide the way of programming the code, but also to managing that code in a nice way, and also providing tools to do that the most streamlined way possible. The language C++ was choose because of the performance and because we can create a layer over C++ that provides higher level capabilities to the language. But for fast duration cycles, we have blueprints that is our script in language. And so we use C++ for the core features that we have to have in our projects. We have already built in gameplay framework and in the engine that provides a very flexible way of approaching and creating your project. And also within the in the programming framework, we have networking capabilities already created. So just from the beginning, a real engine used C++ and for a long time, it also used a real script as script in language. Very similar to Java object oriented. It was compiled, so it was in part of the executable. And for example, it had some code features like state machines embedded in the language. It was meant to provide a prepared framework, but the core features were implemented in C++. C++ is what we will be covering in this section. So to have a very high level view of the content that we will be addressing, we have the source code more or less in the center. Then to organize that source code, we have the real build tool that will generate all the files and structures we need, depending on our requirements. And then in the source code, we will have the base data that is actors that are from objects, base data types, other kinds of data assets. We will have the framework I mentioned. As you can see here, you can play framework. We will have the memory management, replication, ties to the network framework. And then once we have worked in the source code, the pipeline will be to compile, use a real automation tool to build, cook, pack and have a ship break. And in any of those stages, we will be debugging and profiling whatever we need to to nail the ship in break.