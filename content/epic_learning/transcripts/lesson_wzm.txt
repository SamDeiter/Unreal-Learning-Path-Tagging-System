We're going to overview the different life cycle methods that have automatically been added to our C++. So the first thing we want to do is open up the C++. If you haven't got it open already, now as a couple of ways of getting to the Visual Studio project, one way is to go to explorer and open up the solution file there. The other way would be to go to your content browser and then if you open up the sidebar here using this little icon, you can go to your C++ classes and the Blueprint to C++, which is this project, and you can choose to open the C++ files this way directly. So then once that has been loaded up, what we can do is have a look at what we've got in here. There's a slight difference between what's in the quest manager and what's in the grab. Let's have a look at the header file first of all. So what we've got is a constructor. That should be fairly familiar. We'll talk a little bit about when the constructor is called, however. We've got BeginPlay, which from Blueprints may be familiar and tick Component, which again may be familiar from Blueprint but we're going to talk about when it is called. Now if we go over to the quest manager.h, pretty much the similar thing. We've got a constructor, a BeginPlay, which looks exactly the same. However, tick is different. It's tick instead of tick Component, and that's just the difference between Actors and Component. So let's have a look at when these are called by going into the C++ file and putting some log statements in. Logging is a very valuable skill in C++ and in any coding. The way we do it here is to use the Huey underscore log macro. Then we have to give a category. Now if you don't have a category set up or just want to be debugging at this moment, then we can just do log temp, which is a preset up category for temporary logs. Then we can choose the verbosity. We are going to go for warning because this makes it just slightly more visible in the log. There's a long list of potential other levels of verbosity. You can find the full list over in logverbosity.h. There's no log, fatal error, warning display, log verbose, very verbose, and so on. Now we're just going to stick with warning for our debug logs to make them stick out off the log. Next thing you have to use the text macro, which just makes sure that we're using whatever character type our platform supports. So on Windows, for example, it'll be a W char and it just avoids any unnecessary conversions back and forth. Then we can put a log in here. We're going to write grabber constructor. As it happens with ulog, you actually don't have to put a semicolon on the end of the line, but I tend to do it anyway just for good habit. I'm also going to remove this comment line just above. That's going to tell us when the constructor is being constructed. I'm going to put similarly one in begin play and call it the grabber begin play. And I'm going to put one in tick components as well and call it grabber tick component. Now I've gone ahead and done the exact same thing over in questmg.cpp. I've put one in the constructor, begin play so that we can have an example of all of these things happening and it should be fairly obvious what the result is going to be. So now I have gone ahead and compiled. We can go over into Unreal and there's no need to reload the editor. We should just be able to go ahead and hit play and see what's going on in the log. But where is the log? So first we'll go to make sure we've got that. I'm going to go to window and then we're going to go to the developer tools and we are then going to go and find the output log and then the output log. I'm just having docked next to the content browser. And what we'd expect to see is the warning messages coming up in yellow. So they're nice and obvious. And first of all, we can see right off the bat that our constructors for the grabber and quest manager have already been called and we haven't even hit play yet. So we can infer from that the constructors are actually called at edit time as well as play time. So if we go ahead and hit play and then we quickly want to stop because ticking happens every single second. You can see that the ticking and the tick component are being called not every second but every frame and they're alternating which would make sense. The first thing that is called though in fact is the constructor that's called again when you play a separate instance of the game. And then after the constructor we are getting begin play which is happening before the first tick. So that's what those methods are useful for. They allow us to insert something that happens after we're already constructed. Everything is already in the world. But before ticking begins and then when ticking happens which is every single frame. The constructor on the other hand has to be called in the editor because if we want to bring up an instance of this thing to be able to add default values and so on that's all happening over in the constructor which leads me to the question of this line here line 10 the primary actor tick can ever tick equals true and there's an analogous line over in the grab a CPP where we've got the primary component tick can ever tick equals true. Now this is a efficiency measure. If we set these things to false then this object is not even going to be considered for ticking and allows us to save some CPU cycles. However this will be completely overridden if you do use a tick in your blueprint so just bear that in mind. Just to summarize what we have seen is how we can use constructors begin play and tick component both in actors and in components to allow us to hook into different stages of the lifecycle of an object.