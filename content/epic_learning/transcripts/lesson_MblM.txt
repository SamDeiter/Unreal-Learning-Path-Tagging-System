In this video, we'll take our first steps towards creating a dynamically lit paper2d based game. We'll create a new material, apply it to existing assets, and make tweaks to our project for best results. Let's begin by finding and duplicating the existing material that we're using for our paper2d project. We'll make a copy of this existing material just in case we break it when we make our alterations. To make this copy, we'll need access to the existing paper2d material. By default, this is hidden from viewing the content browser, so let's go ahead and make that visible. Select View Options in the bottom right hand corner of the content browser, and then check Show Engine Content and Show Plugin Content. We can now see there's many more folders available on the left hand side. From these new folders, locate paper2d content, and then default sprites material. With this material selected, make a copy of it, and name the new one default sprite lit material. Let's go ahead and open that new material in the material editor by double clicking on it in the content browser. In the Details panel on the left hand side of the material roll out, change the shading model from Unlit to Default Lit. In the Node Graph, break the link from the top multiply node that is connected to the emissive color by Alt-left clicking on the node. Connect this output to the base color input of the default sprite lit material node. Next, add a constant into the graph by right clicking in an empty area and typing Constant. Leave the value set to zero. Connect this constant to the specular input of the material. This makes sure to remove any possibility of specular highlights on anything using this material. We're done for now, so head up to the top of the toolbar and select Apply. Now we're done here, so hit Save, and then close the material editor window. While we're here, we should make a few more changes to our project. By default, this project is using an orthographic camera, and that's going to cost some headaches for our new lighting setup. So let's go ahead and make a few changes to the project to avoid that. Open the project settings window via Edit Project Settings. In the search dialog box at the top of the project settings window, search for Forward Shading. Check the forward shading check box to enable the forward shading rendering model. After we check that box, we'll be prompted by our real engine to restart the editor, which we should do by clicking the Restart Now button. When the editor restarted, we returned to the project settings window. This gives us an opportunity to head off another issue before it becomes a problem. As we start to build larger levels, we'll soon find that our light sources gradually stop working as they are placed further and further from the origin of the level. This is a limitation of the specific way that the orthographic camera is implemented within our real engine, and the details are a little beyond the scope of this course. However, luckily there's a quick and easy way to work around it and keep us on target. Return to the project settings window via Edit Project Settings. In the search dialog box at the top of the project settings window, search for Min Screen Radius for lights. When using the orthographic camera, this radius is positioned at the origin of the level, which isn't very helpful. For that reason, we should change this value to 0, which will disable any culling of light sources regardless of distance. With that change made, we're done here, so we can close the project settings window. With all those issues taken care of, it's now time to change our existing assets over to use the new material. Ensure that tile map level is open in the main viewport. From this viewport, select Unreal Paper 2D tile set, underscore master, underscore tile map from the scene. In the details panel, on the right hand side, locate the materials roll out. Use the drop down from element 0 to select unnually created default sprite lit material. Don't worry if everything momentarily goes grey, we'll just compile the shaders, and it should look pretty much the same. With the new material applied to our tile map, now let's apply it to our character. Using the content browser, navigate to content, 2D size growl of BP, Blueprints, and double click on the My 2D player character to open the character in the Blueprint Editor. Select the sprite component from the Components panel on the left hand side. Then, moving over to the details panel on the right hand side, locate the materials roll out, and use the drop down next to element 0 to select the same default lit sprite material. With that selected, compile and save the Blueprint, and then close the Blueprint Editor window. Now, it's time to see what difference those material changes have actually made. From the main viewport, select the lit view mode from the view mode dropdown. Now, of course, you're going to notice, everything's going to go black. Well, that's because we have no light sources. We'll tweak these sources later, but for now, hold L and left click to place a basic light source within the scene. Position the light in front of the tile map and see how the tiles light up. Next, let's start a plane-edith session to look at the effect. We can see that the light sources have an effect on both the character and the environment. At this stage, feel free to tune your light source and move it around and extra points, whatever you feel like. Once you're happy, save the project, and we're ready to move on. In this video, we created a brand new material that reacts to light sources within the level. We began by locating the default paper 2D material within the content browser and changing the shading model to default lit. We then applied this new material to both our character and our tile map. We then made several changes to our project to better support a lit 2D environment with an orthographic camera. We changed the forward renderer and disabled the culling of distant light sources that would cause issues as we continue to build our level using an orthographic camera. We finished up by adding a light source to our level and reviewing the results in a plane-edith session.