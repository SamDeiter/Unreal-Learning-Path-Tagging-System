In this lesson, we are going over collisions. We'll be going over these topics. What is a collision buyer? How should a collision buyer look? Creating a collision buyer in Maya and exporting it out. Difference is between complex collision and simple collision, using Unreal's engine's tools to create a generated collision, and checking collisions in Unreal Engine using the view modes. Collision is a mesh that is often invisible to the player that blocks or reacts to the action of two objects or assets touching each other. For instance, a wall might have player collision so the player cannot walk through the wall. There are multiple uses for collision meshes, but the most common in relation to environment art would just be a simple blocker, such as the wall we just mentioned. The block can be used to stop weapon fire, character traversal, and other interactions with gameplay and design in mind. Typically, it will be the job of the environment artist to create the collision or an asset, but not to determine its interaction with design and gameplay. Here we can see a mesh in what the corresponding collision mesh on a might look like. Collision meshes are meant to be very simple and cheap to render, so it's best to keep the amount of meshes you use to create a collision on an asset as low as possible. Also note your collision can be made of multiple simple objects and doesn't need to be made of one custom made shape. Typically, it's best to go whatever route has the lowest polys and vertical at the end. Also, it's good to know design rules on the project and how the asset will be used. Most times, the collision boxes are just simply added to prevent the player or character from walking through the assets, so the collision doesn't need to be refined for every piece of the asset's shape. The next part of collision meshes to consider is how to properly make them. Collision meshes have to be convex and shaped to be properly used in engine. This means there cannot be any inner extrusion portions of the collision mesh or in real-worldly way not be unable to use the collision as properly. We can see here the shape on the left is considered convex as it does not have any inner extrusions. This shape would work for a collision mesh. The shape on the right, on the other hand, would not work as it has inner extrusions and would not be usable. There are a few ways we can make collisions for our meshes. First, let's start with a method of using an external software modeling package such as Maya to create our collision. Here in Maya, we have a chair that we want to add collision to. There are a few meshes that we have at our disposal when making collisions in an external package that Unreal will recognize. UBX, UCX, and USX. Each type is referring to how you name the collision mesh when you create it and what type of shape the collision mesh is. UBX is a simple box collision. UCX is a custom collision shape and USX is a sphere collision shape. You have to determine as you make each collision shape which one you'll be using. For this example, we'll keep it simple to get us started by using just box shapes to make collisions. You can see here I've made two boxes that encompassed the shape or silhouette of the mesh to be used as collision. I now need to ensure my names for the meshes are appropriately set. Let's first name our asset that is our base mesh appropriately by naming it SM-UnderScore-Chair. Then, for each collision mesh, I need to use the exact same name but add UBX in the underscore in front of each asset's name and add a number to the end to represent how many collisions are in total. For this scenario, I only need two, so I'll name it UBX-UnderScore and then at the end, I'll add underscore 0-1. And for the other, I'll add underscore 0-2. Then, with everything named appropriately, I want to middle mouse click and drag the collision assets on top of the main mesh to make the collision meshes children of the main parent mesh. This will notify to Unreal when I import it in that it should be treated as collision mesh and not as a primary mesh. We'll select everything including the collision meshes and the main mesh and select export selection to save our mesh FBX file. Now, back in Unreal, we can import the asset in but let's make sure that we untick this box at the top called auto-chenerate collision. This will ensure it brings in with our own custom collision instead of making its own newly generated collision. Now, let's check to make sure that all our collisions made it in successfully. We can open the mesh here and enable the collision viewer at the top. Each mesh generally has two types of collisions used for multiple different gameplay and design purposes. Simple collision, the collision that we're making, is mostly for blocking the player from walking through something in general overall level collision. Complex collision is per polyphase collision, meaning it's used when each face of the model needs to be accounted for when certain types of interaction dealing with collision is needed via gameplay or hit detection. For now, let's turn on simple collision to reveal our import collision. We can see that everything came in correctly and even in Unreal, if we select the collision boxes, we have the controls to tweak or adjust the placement of the scale and engine after leaving our modeling software package behind. While we are here, let's also take a quick glance at the complex collision we were just talking about. You can see here every polyphase is highlighted, showing that every face would be used for this type of collision. Another way we can check level collision and collision on assets is to use the collision view mode in the level. We can test this by dropping our mesh into our level and turning on that view mode here in view modes, player collision. Now let's say we didn't want to use an external software to make our collision. There are many tools that can be used directly in engine to create our collision. First, let's remove our existing collision here by selecting remove collision from the collision menu. In this menu, you can also see that all the collision options are available for us to making collisions in engine. Each has a different approach as to how it makes that collision. Spear, capsule, and box are exactly as they sound. They make collision encompassing the object based on those shapes. Let's make a simple box collision and test with that first. We can see that it makes a box that surrounds the mesh, but it's not quite accurate enough on its own. So we would need to make some tweaking and potentially another box generated to make the collision similar to the collisions we made externally. Other options are also using the DOP collision options, which generate collision based on figuring out the shape of the object in the X, Y, and Z, and making a collision shape more accurately as it febbles and adds more points to the collision box based on the number provided in the DOP description. Now let's use the AutoConvex collision. This tool is the most accurate, but also the most expensive collision to use, as it takes the whole shape into account to try to automatically generate collision based on an approximation of the major forms of the mesh. When we select this option, it opens the convex decomposition panel here on the bottom right. This controls how accurate the shapes that it creates for collisions are, as well as how many collisions shapes it will make. Whole counts increase the amount of shapes used to create the full collision. Max Hall of Verts is how manyverts are being allotted per whole or collision piece that is being generated, and whole precision is how refined and specific the whole shapes would be relative to the assets shape. It's definitely suggested you just start with less holes inverts and go up and refine as needed to get the desired results. Keeping in mind the higher the holes,verts, and accuracy for the auto-generated collision, the more expensive it will be when used in engine. One last trick when working with collisions is if you need an object to really have every single face be reactive to collision, you can navigate to the options to enable use complex collision as simple. This essentially marks the complex collision we talked about previously, and lets the engine know to use that collision for both simple and complex interactions. This is the most expensive type of collision to use, but some meshes may call for this, pending the scenario.