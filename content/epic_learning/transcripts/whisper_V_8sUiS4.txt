At this point of the course, you know how to create basic transformations. Wouldn't it be great to reuse the same logic, but with different values? Fortunately, Unreal provides a few mechanisms to make it possible. We can add parameter nodes to the material graph. Then we can store presets of the parameter's values in an asset type called a material instance. So let me show you from scratch how to add parameters to the node graph and then how to create instances. On the bottom left, I click on Content Drawer, select any folder, right click and create a material. Let's call it M, UV, Move, Aram Let Rice. Now I double click it to open and as shown in the previous sections, I click on empty, material domain in the details panel change to user interface. I right click and get user interface UV. Now to visualize the UVs, I make an append node, append vector and append a constant of 0 as the blue value and to final color, apply and save. Now of course, to assign it, I click a widget in the brush and image section, move parameterized, legislated, compile and save. So here we'll have the preview of what we do and we'll work in the material window. So constants are what we have used so far and as you remember from the previous sections, to move UVs, we have to add UVs. Now I'll add 0 and to display UVs that are always between 0 and 1, so repeated when they go beyond 1 or lower than 0, I use a node called frag. So frag leaves just the fractional part of the number, deleting the integer. So it will be more understandable when I add something. So normalized UVs, I append to create a vector of two components and what you would typically do is to right click and create a constant. So in a second, we'll upgrade these two parameters. For now, I want to move the UVs by 0.3 in the X axis and this is what happens with frag and this will be without frag. So the bigger the value, this color will increase. As you can see, the green is no longer visible because the value of red is so big that it dominates. So when I add frag, everything starts to repeat itself in the 0 to 1 range. And this is what happens typically with the image, which is very useful as a visualization because textures get repeated too. So to understand this concept, refer to the previous sections and here we have two values that we can add to the UVs to move them. So let's make a parameter. It's as simple as right clicking on a constant and clicking Convert to Parameter. That's one way to do it. You have to name it because this name will be visible in the material instance. So I would say Shift U. You can use space bars, you can use underscores. I typically go without them. And in the details panel you can see the parameter name is Shift U. Default value is 0 but you can change it. And then you can also specify for the user of the material instance the desired minimum and maximum. For example, if you want the user to keep the slider between 0 and 1, you can do that. It's non-obligatory, they can still override the value, but it's a clear suggestion in the UI. You also have a group so you can group parameters and you have sort priority to decide their order. For now let's not focus on that, but just know that you can right click a constant and Convert to Parameter. I will show a second way to do it in a moment, but for now let's see how it will look like in the material instance. So how do we create a material instance? You go to the Content Drawer, find your material and now you have two ways. Either you right click on Empty, you go to... sorry again... right click on Empty, go to Material and create a material instance. It goes out of the window. So... material instance and it creates an asset. Or you can right click the material, the main material and do Create Material Instance. So both ways lead to the same result and we call them MI by convention. Material Instance to distinguish from Materials. So what's the difference? Well, material is the graph, is the logic. While material instance is just a configuration of this. So here it is, I double click it. As you can see there is no graph. There is just a slot where you can choose the parent and here you choose the parent material. And its parameters are visible on the top. Let's go back to our Move Parameterize and there is only one parameter, it's called Shift-U. By default is what we set in the graph, so 0.3, but it's disabled. So when it's disabled it will always take the value from the parent. So if I go back to the graph and do 0.4, save and apply and go back to my instance. It's unchecked and it's inherited 0.4. So I can override the parameters in instances per single parameter, which is very useful. So I click on the left on the checkbox, left to the name and this Shift-U is now overridesable. So I would like to say 0.75 and save it. Now if I go back to my parent and change the default, nothing changes here. Because this material parameter is overridden. I can of course unclick it, it will go back to the same value as the parent, but when clicked it overrides. So these are the basics. Material Instance is just an asset that you can choose the parent, you can even change some modes like Blend Mode, Shading Model and so on. For example you can switch between translucent and opaque. And you can also create material instances out of material instances. So you can right click on this and create material instance. It should inherit all the parameter tweaks as well, but you can still override the chosen ones. So it's useful sometimes. And typically you would like to just have one instance out of the material and duplicate them. So for example this material instance would be Move Parameterized 0.75. So I would name it Move Parameterized 0.75 for example. And I now can right click and duplicate that and name it Move Parameterized 0.2 and set it to 0.2. For example. So now I have two different instances with just one parent that still has the same logic. So thanks to the fact that I didn't duplicate the material, I can for example change the blue value for now just as a test. It changes the appearance. And both these instances inherit the same logic. So that's the preferred way to make different variants of the same material. Keep the logic common, which will make it so much faster for you to work with when you want to improve something. And also it will help in optimization, because there is less shaders to compile. There is less logic to be let's say pre-computed in form of different shader variants. So I know it's a technical explanation, but in general the less materials you have and the more you rely on material instances, the better for performance. Let's go back here. I will duplicate the widget. And now in the DDS panel, in the Appearance Brush Image, I can choose instances. So MI, UV, and find the ones I made. Where are they? Oh, here. So that's one. And that's the second one. And now I can tweak them by double clicking on the image slot, changing the parameter override. And as you can see, it reacts, keeping the other widget intact. If you want to reset a property to the default, you have a button on the right. It's a left-facing arrow. It says, reset this property to its default value. When you click it, it inherits the basics from the material, which you can still disable or enable, but in general it's reverted to whatever the parent has. So by now you know how to create a parameter by right-clicking a constant and convert to parameter. There is also a second method. Instead of converting, I will delete the V param. It says error because nothing is attached. And I right-click on empty and search for parameter. And there's a bunch of parameters. You can have a texture sample parameter. You can have a scalar parameter. And scalar parameter means a single value. This is exactly what we want. So let's select this and call it Shift P. So as you can see, the result is exactly the same. I can either right-click a constant and convert to parameter or right-click and search for parameter and choose the appropriate type. There's also a vector parameter, which allows me to set four values at once. RGBA. So I can use it here as well. Just I will need to append, sorry, append vector, the red and green. For example. But this is a waste because then I have two unused numbers. So I prefer two scalars in this example. And if I save and increase Shift V, as you can see, everything works as intended. And now in the material instance, I have two parameters to set. Shift U and Shift V. Now there is one more concept to explain. It's how to set a parameter in the UMG itself, itself in the logic. So here we have these materials. But let me make a slider. Not an editor utility, but just common slider. Analog slider. Let's put it here. And I would like to bind the slider's value to the Shift U value. So let's call it Analog Slider Shift U. Is variable. Yes. And now its value. I go to Graph. I want to get the analog slider. And onValue. Assign onValueChanged. And I want to do it in pre-construct. And now if value of this analog slider has changed, I would like to take the material and update it. So here it is. This border name I would rename it to, let's say, DynamicUVShiftBorder. Is variable. Yes. I go back to Graph by clicking on the right upper corner. Graph button. I take my DynamicUVShift from the left, from variables, drag and drop it into the graph. I want to get the material. Get DynamicMaterial. And in this event, when the value of slider has changed, I get this material and do setScalerParameterValue. It asks me about two things. It wants me to provide the value, so I want to take the value from the slider. By dragging from the onValueChanged event. And parameter name has to be exactly what we had in the node. So it was Shift U. It's exactly the parameter name. So getMaterial, setValue. And compile. Now I want to run this widget. Oh, it's beyond the screen. Let's go back to Designer. Move everything away. Sorry. Everything. Okay. I will take these. Put them on the top. And now, save, compile, run widget. Here they are. So by moving the slider, I'm updating the value in the parameter. In the DynamicMaterial instance. So we have dealt with constant material instances, which is what we create in the Content Browser. And UMG also creates a DynamicMaterial instance that can be updated in real time for every widget. So to show you again, let's go back to Graph. Right upper corner. So I took the analog slider, put it onto the graph. And first I search for onValueChanged. Assign onValueChanged, which created this binding and a custom event. And in this custom event, I get the DynamicMaterial from the widget that I want to control, this border. I get the DynamicMaterial and set Scalar parameter. You can also find that vector parameter value. Let me clean up this logic by removing those nodes, because I haven't used them. And this is what you are looking for. So for example, in the pre-construct, you bind. And in the custom event, you do stuff. You perform the logic. We have shown how to expose parameters in the graph, and it made it possible to store material presets, as well as control them from a widget.