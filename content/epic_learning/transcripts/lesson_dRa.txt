Although we want our user to have plenty of control over the scatter tool when working inside Unreal, we will also want to put in some safeguards so as to ensure the tool remains stable. And so, in this video we are going to clamp the number of meshes that can be spanned per area and also create some loops in the blueprint that will allow us to use our array variables to the full. To control the number of meshes that need to be spanned, let's drag out the number of meshes into your variable that we created earlier and choose the get option when prompted. We can then drag from its output in order to search for and add a clamp node, on which we want the min value to be set to 1 and the max value to 10,000. Now we have done this because of the potentially dangerous nature of the tool, i.e. its ability to quickly use up all available resources. So we are building a failsafe into it so that the user can't accidentally add so many objects to the system that they bring the entire engine to a halt. Now obviously feel free to adjust these two numbers according to the projects that you will be working on as the value i have set might still be too high for your computer resources or maybe too low and so can be increased. From its return value them, we want to drag out, search for and then add another far loop node. Holding down the control key we can click and drag on the first index input and switch it to the last index input instead just as before. Though we will in this instance set the first index to a value of 1 so that we don't get a duplicate mesh. We can then pipe the completed output from our original far loop node into its execution input. Now just to explain we have added these steps because once the loop has run through the entire number of selected meshes to spawn we want it to move on to the next part of the flow and look at where the blueprint can and cannot spawn meshes. When creating tools it's usually a good idea to preempt potential problems. Now this will allow us to build safeguards into the blueprint flow early on. Again this can stop our tool from potentially being frustrating to use.