Welcome to World Aligned Textures in Normals. I'm Paul Neal for Epic Games. We'll have a look at how to create world space projected textures that use the World Aligned Texture node to remove the need for traditional UVs for texturing, and makes it possible to blend mesh assets together as World Aligned Textures will always blend from one object to the next. We'll also have a look at adding detail maps that can be blended into the base textures. To get started in the Materials folder, we'll right click and choose Material, and we'll call this M underscore World Aligned example, and then double click on it to open it. We'll undo it and add in the textures that we'll be using. From the Textures folder, we can find the Stone tile and the Stone tile normal, and then redact our material back to the top so that we can begin building it. The texture sample nodes that we're using sample the UV coordinates of the asset that the material has been applied to. What we need to do is project our textures in World Space in X, Y, and Z so that even if objects are of different sizes, they'll receive the same size texture. To do this, we'll right click and type in World Aligned and find the World Aligned Texture node, and we'll take the output of our texture and input it into the texture object. However, you can see that this isn't allowed. This is because a texture object is a direct sampling of the texture that has been stored within the project. We can convert our texture sample to a texture object by right clicking on it and choosing Convert to Texture Object. We can now take the output and place it into the input of the texture object in World Aligned Texture node. There are three outputs, the X, Y, texture, the Z texture, and the XYZ that we'll be using. We can take the output of that into the base color. You can see that the texture on the texture sphere looks very convincing that it's been wrapped around the object without any UV seams whatsoever. If you look closely, however, you can see there's a slight blurring. For more, the texture goes from the top of the sphere down into the side of the object. This is because it's blending them together based on the normals of the surface. We'll need a way to control the texture size. We can see there's an input in the World Aligned Texture. So we'll create a constant to put into that. I'll hold down one on the keyboard and left click. We'll take the output of that into the texture size. The default texture size is a value of 100. We'll also right click on our constant and choose Convert to Parameter. We'll type in a name that we can use later to understand what this value may control. In this case, we're going to call this texture size. For the normal map, we can't use the World Aligned Texture. We're going to need to use the World Aligned Normal. The World Aligned Normal differs from the World Aligned Texture. And that it takes into account that normal maps return a vector that need to be blended correctly for them to work. Once again, we'll take the texture sample for the normal map, right click on it, choose Convert to Texture Object, and then take the output into the Texture Object. We can use the same texture size value and input that into the Texture Size, and use the same XYZ Texture output and place that into the normal input of the material. We'll see that we now have a nice bumpy surface beginning to look a lot like stone. Our base texture is a gray scale texture, and we may want to introduce a bit of color to this so that we can either cool it down or warm it up for our needs. The way we'll do this is by blending in a color value. We'll need a constant vector 3 by holding down 3 on the keyboard and left-clicking, and then we'll use a blend screen node by right-clicking and typing in Blend, and you should find the Blend Screen node. Then connect the output of our constant vector 3 into the Blend, and the output of the world-aligned texture into the base, then take the output of the Blend Screen into the base color. A Blend Screen lightens the base color by the Blend Color. It does this by doing a 1-minus on both colors, multiplying them together, and taking the 1-minus of the result as the output. Adjusting our constant vector 3 to have a touch of blue in it, you can see that we can cool down the results of the stone, or we could push it into the reds and it'd be able to warm up those colors. We'll be able to adjust this in our material instance once we're complete, so we'll convert our constant vector 3 to a parameter by right-clicking on it, and then giving it a name. In this case, we'll just call it Color. We'll leave the default color as black, so that there's no change to the base texture. For the base color, it'd also be great to have some way of being able to adjust the values of the end result, this way we can darken it or lighten if we like, as well to be able to get just the right feel to the stone. Once again, we're going to introduce some new nodes. We're going to use a Multiply node, we're going to hold down M and left-click, and we'll also use an Add node, which is A on the keyboard and left-click, and we'll take the output of the multiply into the A channel of the add, we'll take the output of the blend screen into the A channel of the multiply, and then the output of the add into the base color. This way we'll be able to multiply the end result, as well as add to the end result. We'll need two constants that we'll be able to adjust in our material instance, hold down 1 and left-click, and we'll take the output of the first one into the multiply, we'll set the default value for the multiply to be a value of 1, and then we'll take the output of the second constant into the B channel of the add, we'll leave that as 0 so that there's no change. Once again, we'll convert both of these to a parameter by right-click and choosing Convert to Parameter, and we'll call the first one Multiply color, and we'll call the second one Offset Color. We'll then create a comment for these nodes so that we know what this set of nodes does within our material. I'll select all of them and press C on the keyboard to create a comment, and we'll just simply call this color so that if we ever come back to this material, we have an understanding of what these are for. We should also group together the parameters so that we can find them easily in the material instance, we'll select all of them, and other than material expression, we can find a group parameter that we can type a name into, in this case, we'll simply call this color. This will allow all of these parameters to show up under a single heading and make them easy to locate. You can also control the output of our normal maps so that we can increase or decrease the amount of overall bump that there is. To do this, we're going to use a node called Flatten Normal, all right-click, and type in Flatten, and the Flatten Normal node is designed to remove the effect of a normal map. I take the output of the XYZ texture into the normal, and the result from the Flatten Normal into the normal. You can see that we have the ability to add a flatness, and that will be a constant vector 1, so I'll hold down 1 and left-click, and we can take the output of it into the flatness. With the value of 0, we'll have the full normal amount, we increase it to a value of 1, this will flatten the normals and we no longer see any effect of them. This may be a little counterintuitive to an artist, so we're going to reverse this value so that 0 outputs a no normal, and 1 outputs the whole amount. We'll create a node called 1-, right-click, and type in 1-, take the output of our constant into 1-, and the output of 1-minus into flatness. Then we'll right-click on our constant, converted to a parameter, we'll call this Normal Amount. We'll leave the default value as 1, so that we have the full normal effect. We should select all of the nodes now, press C to create a comment, and we'll call this Normal's. And again, we'll group the parameter input, so that if we have multiple eventually, they'll all show up in one place, we'll create a group name called Normal's. We'd also like the ability to add in a detailed texture, in this case it'll be in the form of cracks. I'll just undock the material, and we'll drag in the crack tile and the crack tile normal. We'll place the normal texture down below the existing normals, and the crack base map up at the top. Once again, both of these textures are going to be world-aligned textures, so I'll make a copy of the world-aligned texture node that we have by selecting it, we need to convert the texture sample into a texture object again, and take the output into the texture input. We're going to start with, we'll use the existing texture size, and then we'll do the same for the normal map down below, so we'll make a copy of the world-aligned normal with Ctrl-W, convert it to a texture object, and take the output into the texture input. input and once again we'll also control the size with the existing texture size parameter that we have. We'll now blend these two together. We have the crack showing up on top of the base stone texture that we have and we'll do this simply with a multiply node. Give myself a bit more room to work and I'll create a multiply node by holding down M and left clicking and taking the output of the XYZ texture of the cracks into the H channel and the XYZ texture of the base stone texture and the output of the multiply back into the base blend screen that we have and you can see now that we have dark lines on our stone. This is going to start to form cracks in a rock. We'll also need to blend together the normals. Normals are a little different to blend together because they represent vectors or changes in vectors from the existing normals on the asset. We'll need a blend angle corrected normals node to blend the two normal textures together. So we'll take the output of the original normal and then our new cracks into the additional normal slot and then connect the output of the blend angle corrected normals the input of the flattened normal. We'd also like to control the amount of cracks that we have in the end material. To do that we'll need a constant factor one which we'll right click on and say convert to parameter we'll call this detail amount and we'll add this value to the existing texture map so we'll press A and create an add node and take the output of the rolled align texture into the H channel and the output of the detail amount into the B channel and then we'll take the output of the add into the H channel of the multiply. So with a value of zero you can see that we don't have any cracks at all. I take the value of our constant up to one you can see that the texture becomes very light and this is because we've now pushed not only the whites but also the blacks up in the cracks so that we're multiplying in a much higher value than one. We need to clamp off the result of our add node so that it never returns a value higher than one. To this we're going to use a saturate node. Saturate node is free on modern GPUs and we'll clamp the value between zero and one for us. Take the output of the saturate into the H channel of the multiply and we'll take the detail amount back down to zero and you can see the detail amount at zero returns the cracks and at one it doesn't return any cracks. Once again this is a little backwards for artists we'll use a one-minus node to invert the input value so that zero doesn't return any cracks and one returns the full amount. We can't control the normal blending the same way for the cracks. In this case we'll have to use a flattened normal node again to be able to blend in our crack normal. Right click and type in Latin normal and we'll take the output of our world-aligned normal for the cracks into the normal channel of the flat normal output of the flattened normal into the additional normal of the blend angle corrected normals and then we'll use the output of our one-minus for the detail amount and we'll take that into the flatness. We should do a little more organizing now so that we know where to find things in our material when we come back to it. We'll select all the nodes that are controlling the base color and we'll press C and we'll call this base textures and we'll select all the nodes controlling the normal textures and again C call this normal textures. It would be nice to have a way to control the size of the cracks differently than the size of the base texture for the stone. We'll create a new input for this as a constant vector 1 so one on the keyboard and left click and we'll right click on it and convert to parameter and call this detail size and we'll multiply the base texture size by our detail size so that the texture size changes it will also change the size of the cracks but we can individually control the cracks themselves. This we're going to need to multiply we'll hold down M and left click take the output of the texture size into A and the detail size into B and then we'll control the cracks for both the base texture and the normal texture with this new output. We'll select all of these nodes here and we'll put them into a comment again and we'll call this parameters. We'll also select the parameter nodes and we'll put that into a group called parameters as well. Lastly we should have a way to control the specular input of our material and for this we're just going to simply use the output of our base texture maps and we'll use a multiply in an add node. Let's make a little room so that we can build this and I'll hold down M and left click for a multiply an A and left click for the add and take the multiply into the H and all the add. We'll take the output of the textures that we have that are going to control the base color and we'll place that into the H channel the multiply and then the output of the add into the specular. Now we'll need again two inputs here to be able to control the multiply value and the add value so we'll create constant vector ones with one on the keyboard and left clicking and we'll take the multiply and we'll set that to a value one and take the output of it into the multiply node and the constant that's going to control the add will leave it zero place that into the add and we'll select both right click and convert to parameter again and we'll call this spec multiplier and we'll call the next one spec offset. Once again I'll select all the nodes and create a comment call the specular and we'll also select the two constant vector ones that we've created parameters from and we'll put those into a group as well called specular. We've exposed parameters to the material instance we can also expose the textures themselves so they can be switched to new ones if we like. To do this we can right click on them and choose converted parameter and then we'll give it a name. We'll have to replace the texture in it with the one that we were using previously and we can do that for all four of them and then place them in a groups that we can find them easily. With our material built all that's left to do is to save it and then create a material instance so that we can work with it. We'll find the M world align example right click on it and say create material instance out of the menu. We'll rename this by replacing the underscore instance that has been added and change the prefix to mi and we'll drag and drop our new material instance onto one of our rocks and then press F to zoom in on it and we'll double click on the material instance and then we can go about changing the parameters that we've created. You can see that they've been grouped together and it makes it very easy to find what we're looking for. We'll add a little bit of blue by turning on the color and adding a touch of blue into the overall look and feel of it. Turn the multiply down to darken it. Increase the size of the detail. Maybe increase the size of the overall textures. Add a little bit of specular and if we like we can even change the base textures that we're using for this material. This allows us to create a material that has multiple purposes to it. We can create any kind of stone that we'd like in our scene. You can see with the material using rolled-aligned textures that were able to select it and move it and you can see that the stone passes through the texture. This has the advantage that if we scale it you can see the texture remains the same size. This means that the object doesn't look at so it's out of place in the scene with textures that are being stretched. Also if we make a copy of it and place them side by side you can see that the texture spans the distance across both objects making it look like they're one. This can be a great way of being able to place many objects together to be able to build up larger ones from them. We've covered some of the basics of creating UV-list tileable textures using the rolled-aligned texture and normal nodes as well as how to create adjustments in your materials so that it can be customized for your specific needs with the ability to be able to add detail textures to enhance the look in the feel of the materials.