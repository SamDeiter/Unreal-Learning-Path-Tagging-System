Welcome to the fifth coming to the rescue challenge, a module where we will build upon what we learned in this course combined with previous courses to solve a problem. The previous modules have prepared us to solve the problem and use what we've learned to extend a user interface that enables us to create persistent spatial anchors in our scene with intentional names that we can use to attach various actors to. This coming to the rescue challenge is a bit different than our previous ones as the person who needs assistance is our end users. We will carry on with our large building wayfinding example and make sure our app shows them where to find things like the light switch and the door out by pinning tool tips to various pins around our space. On the issue of the light, throughout these courses we have seen a cylindrical light that is placed in my office. We have been unable to have it lit and we need a way to tell the user where the switch is located so that they can turn it on. In this module, we will be expanding on our toolbox to allow for intentional naming of anchors so that our blueprints can identify and work with them. To save time, we have already created all the new assets that will be used in this module. They are all located in the project files that come with this course. To help our end users identify the location of the switch we need to find, we will use a new blueprint that searches for and attaches to specific anchors. Once the anchor is found, it will call our BP-PON to pin it to the anchor and bring along any actor that is attached to it. We will use this system to attach a tool tip component provided with MRTK to inform us of where the light switch is located so our users can turn it on. Let's get started in our spatial mapping problem map. Inside of our blueprint folder, drag two BP anchor locator blueprints into the scene. Also drag in one BP-FocusCube. BP-FocusCube is a child of BP-Ancher locator and the only addition is that it has a static mesh that holds a material that will react when our focus is on it. Open the parent BP-Ancher locator. At begin play, if the variable anchor defined is not set to none, a new timer will start that searches the cached loaded AR pins inside of BP-PON. If loaded AR pins contains one with the same name as the value of anchor defined, call attach anchor to pin and then pause the timer. See that anchor defined is instance editable and exposed on spawn. We can use this variable to search for specific anchors that are in our scene. In our case, we want to find the light switch, the light source and the exit to the next room. Let's add those anchor names in our enumeration variable. It is located inside of world builder blueprints enums and open e-intentional anchors. It currently has only one value, none. After reviewing this system, we know that a value of none given to an anchor will result in an auto-generated name, but no anchor will be named none. So we need to be sure that this value gets changed. The reason we are using an enumeration asset for this purpose is so we can programmatically expand our system to accompany any new amount of needed named anchors. It will all be based on how many enumerators we add here. Let's add the three new values for use. I'll add light dash source, light dash switch and two next room. Now we have four enumerators. Open our BP toolbox located in world builder UI blueprints. Let's set this up together. To allow our users to set the name of the anchor, we'll use the slider component. With the options seen component selected, press add component and search for child actor. Rename it to child actor underscore name slider. Set its class to uxt pinch slider actor. There are a few things we need to change on this component. Open the viewport. I'm going to rotate the slider so it is vertical and the ticks are on the left side and I'll move it up just a bit. In the details panel, open the child actor template options. Open slider components and then title text and set the horizontal alignment to left. Do the same for value text right here. Here the pinch slider options set the track length to 20 so it is roughly the same size as our toolbox. Under slider text, I'll change the title to set anchor name. Okay, let's set this up in the event graph. Create a new event named prepare slider. Call it after we call cycle anchors when preparing the anchor tab. And from prepare slider, drag in a reference to our child actor name slider. Get the child actor and cast it to uxt pinch slider actor. What we want to do is set the amount of tick marks to the amount of intentional names we have. Um, as uxt pinch slider actor set num tick marks. Right click in the graph and search for get number of entries in e intentional anchors and plug the return value into num tick marks. Again, drag from as uxt pinch slider actor and get pinch slider. You can now bind an event to on update value. From event drag backwards and create a new custom event. Rename it to slider value change. Now when the user changes the value of the slider, this event will be called. Duplicate the event and rename it to set anchor name. Call set anchor name after slider value change and pass on the inputs. Also call it after we bind on update value and plug the pinch slider into the slider input and keep the value at zero. Calling it when the anchor tab is prepared will make sure the text is set at the start. From slider create a set value node. We'll use this to provide a snapping mechanic so the slider will always be at an int value matching the ticks. From value on the event create a map range clamped node. The slider will provide a zero to one value so in range a can stay at 0.0 and set in range be to 1.0. We want to map this range from 0.0 to 1 minus the number of entries in e intentional anchors instead. Now this map range will change the return value all based on how many enumerators there are. We'll then take the return value and round it to the nearest int value. From the round node convert the return value to a float and from the conversion create a division node. Plug the difference of the number of entries in e intentional anchors minus one node into the divisor. This will auto convert the int into a float and lastly the quotient into value. Now as we move the slider up and down it will snap to the nearest tick. Okay moving on after we set the value drag from the return value on the round node and convert to a byte. From that result convert again to e intentional anchor and lastly convert that result to a string. Help the strings return value to a variable named anchor name and set it immediately. We want to make sure that the name on this slider is this anchor name. So again get a reference to child actor name slider. Get the child actor and cast to uxt pinch slider actor. Get the value text and set the text to our anchor name. Also, pan up to our add new anchor event. We can now plug this into the anchor name input on the add local anchor event. Open the set tab function. When we first created BP toolbox we created a macro that helped us show or hide the buttons depending on if they were active. We have now added a child actor that is not a pressable button actor to the components here. Open the toggle UI from button macro. See that we are using a pure cast. This can no longer be assumed and if we tried to run the project as is we would receive an error because uxt pinch slider actor is not nor inherits from uxt pressable button actor. Right click on the pure cast and at the bottom convert it to an impure cast. Then plug it in. Now when it is called with pinch slider actor the cast will fail. However, we need to also make sure that the UI visibility function is also called. Otherwise the slider can still be altered when hidden. So from cast failed cast from the child actor to uxt pinch slider actor. Get the pinch slider then call set UI visibility. Plug in the new visibility enum into the new visibility input. Alright, we can now create intentionally named local spatial anchors programmatically. Let's finish adding the tool tips to the map. Open the place actors tab and search for uxt tool tip actor and drag in three of them. One for each of the anchor locator or focus cubes. Attach one to each of them and reset their transforms. Okay, with this first one selected set anchor to find to light source and select the tool tip actor and set the widget class to w light source tool tip. Using the eyedropper select its parent and change the component name to default scene root. Also scale it down to 0.1.1.1. We can move the tool tip around and the spline mesh will remain fixed on the root of BP anchor locator. For BP focus cube set the anchor to find light switch and the tool tips widget class to w light switch tool tip. Repeat the same component name change as before as well as scaling it down to 0.1. And lastly set the final anchor locator to 2 next room and the widget class to w 2 next room tool tip. Also make sure to change the component name and scale it down. For this one I'll set the spline mesh component to not visible. Okay, I've modified the spline mesh in each one to make it a little more pretty. Let's try this on the HoloLens. I'm not going to simulate through Unreal Engine but run it directly from the HoloLens 2 as a packaged version to show you how that looks. We will learn how to package this and upload it in a later course but we have some optimizing to do before we get there. I want to show you what the map manager looks like as well as how these persist over sessions. World Builder. Okay, in the anchors tab we can see that the slider now has 4 ticks for us to use. I'll first place the 2 next room anchor and put it right here on the door. And our anchor locator has found it and brought the tool tip with it. World Builder. Okay, now I can place the light source on the source that we want to turn on. And put it right there. Okay, cool. World Builder. And lastly, I'll place light switch over here. And now we won't lose it again and our end users will have no problem finding it. Okay, I'm going to open the HoloLens menu and go home. Clicking home only does not close the app. I need to find the starting screen and close it. There we go. And now I will relaunch. And your apps will build there. Okay. World Builder. Now I can press load all with display loaded set to off. And here are anchors and the tool tips. And best of all, now I know exactly where the switch is to this light. That's great. The last thing I wanted to show was what these anchors looked like inside of the map manager. Inside of the Windows device portal under system is the map manager. Here we can see that there are three system persisted anchors that have been made. We can see that the anchors we have made are here and will persist across sessions making them very useful. In this course, we modified our AR session to scan the world around us, making it interactable and tested it with physics objects. We expanded on our toolbox UI to visualize the scanned mesh and again to create persistent local spatial anchors in our scene. We created a system to either generate readable names for the anchors or use intentional names for further use. We used our system to solve a new problem to assist our end users by adding wayfinding to our space and instructions for toggling on a light. This was all accomplished by us creating a programmatically updating system that changes based on input given by the user. The initial setup of a system like this is a little heavy at the start, but it is designed to grow with the needs of the project. As more anchors are needed, we only need to add new enumerators and our system will expand. We still have lots to learn. In the next course, we will access our PV camera data and apply it to meshes in our scene.