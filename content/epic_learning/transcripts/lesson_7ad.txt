What we have now is the opportunity to add a more organic feature to our scatter tool as we look to create the scale file of control that will be a very important part of the tools final effect. Let's put our spawn point to good use then by having it control not only positional information but also the scale and rotation of spawn of meshes too. Indeed, we can start setting up our scale control by dragging out instances of the radius and spawn point variables and then using the get option when prompted. Now the reason we're doing this is because we need access to both the radius and spawn point info so that we can let the blueprint know how far away from the centre of the spawn area the mesh is going to be placed. We will also need to do some maths here in order to create the far left and so let's first of all drag from the radius option and select the divide node in order to increase the size of the user defined spawned area. This will just ensure that objects are staying at full scale far a little while before they start to far left in size. We will use a value of 0.7 to do this. Next, we want to drag out from the spawn point and search far and add a vector length node as this will give us the distance of our object from the centre of the spawn zone. We can then drag from the divide node, search for a minus node and hook it up to the vector length, piping it into the second value. If we now divide this using another divide node by the original radius value, what we get is a usable small scale value that will get even smaller the further away the mesh is from the centre of the spawn zone. As we don't want either big or super small scales, let's also add a clamp node. And then set the min and max values to 0.6 and one respectively are indeed any scale values that work for you. Meaning we now have the values that we need far the scale far left option to work. With the nodes that do all of the needed math calculations in place then we have a firm foundation from which we can now start to affect our mesh objects scale.