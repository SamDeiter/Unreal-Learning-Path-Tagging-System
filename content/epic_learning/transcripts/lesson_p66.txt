All right, we are done with the material. Now we can work on our UI. The UI in Unreal I call widget and works the same as a blueprint. So in our tutorial folder, let's create a new folder here. Let's call it UI. And then right click in here. And in the last one, called user interface, you have widget blueprint. Click on that one and name it. I would call it widget, my controls. Double click to open and you open the widget editor. So the widget editor has two panel. One is the designer is where you're going to design the way everything looks on any button, what color, this kind of thing, even animation. The graph is kind of a blueprint and take care of everything that's functionality for your widget. So let's first draw something. There is a lot of documentation on the Unreal Engine website about each of these nodes. So I will keep it simple for this. We are more interested in the functionality. So let's go first to border canvas. And now the little flower here is basically what's called an anchor. The anchor will decide that if the resolution change, which side should the widget be attached to. So in our case, we'll probably want the middle button. Move that here. Make it a bit bigger. And for this demo, we're going to have, for example, a small text on the top that tells us what this is, as well as then, all color, curtain color, and each of them two possibilities. So let's do something, for example, 500 long, and then 150. And then we can align it minus 250. And then let's say minus 160 to have a bit of space, maybe 170. Let's give it a color. You'll want to change the color in appearance. Color in constant color and opacity will affect all its children. We are only interested in this one. Let's give it a dark gray. Now to be able to get the next thing, we'll want a few objects here that are in a vertical manner. And to do that, we just use something called vertical bars. Drag it on the border. Perfect. It's there. You don't see the difference yet, but this will allow us to put different nodes on top of each other while keeping them in a vertical manner. So what we need next is text. Drag it on the vertical box. You can see it here. And let's name that one controls. And let's put it in the center. Perfect. Now we'll want the similar way we use a vertical box we'd need a horizontal box to have the title, wall color, as well as the two button. But let's take a horizontal box and drag it on the vertical box. So it goes under the control. It's there. From there we can put another text under horizontal box this time. Let's make it a bit smaller. So if you go to fonts and size, on the horizontal box, one button and two button. Now we'd want them to probably take most of the space that is left. So you can do that by selecting both of them and clicking on the fill. Fill will basically stretch them to make sure that they take all the space equal. Perfect. Let's name this one. Walls. Perfect. Now what we can do is just right click on the horizontal box. Copy. Go back on the vertical box and paste. Now we have twice that. Let's go to a text here again. Let's call it curtains. Now is a bit push. So let's add some space here to realign them. Perfect. And let's start naming our button. So you can see button zero. Press F2 to rename. Let's call it button wall color A. Button wall color B. Do the same for the curtain. Button. Curtains color A. Button. Curtains color B. Now for each of these buttons, we're going to need a little bit of text on top of it. So let's do that. Let's select the text and then drag the text on top of the button itself. You can see that the little orange outline change. Make sure you're on top of the button. Wow, that's a big text. So let's make it smaller. Perfect. Maybe 12. Let's call it color A. And again, let's copy that one or control C. Go to the color B, control V, and do the same for this two. Control V, control V. Now let's rename the two B button to color B. And there's a bit tight to each other. So let's put a bit of space. You can do that by selecting any of the UI elements and go to padding and add a bit of padding in there. That will push to give a bit of space. So let's say 5 here or 10 and then select the horizontal box and do the same here. 10. So we have a bit of space. Compile and save. Now we get our widget. So the next step, we're going to have to put functionality. But before that, if you were to play the game, you would see that the UI is not there. So we're going to have to add this UI. And that's what we're going to do next.