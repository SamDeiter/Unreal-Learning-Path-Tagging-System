In this video, we're going to look at giving a user the ability to change the size of the Spawn area as and when they need to. To do this, we will be putting a number of our already created framework variables to use, which means we will finally get to see how they start to fit into the workings of our tool. In the Advanced scatter blueprint then, let's just make sure that we are inside the Construction script area. Now just to elaborate on why we're doing this, we're going to be adding all of our blueprint code to the Construction script area because this is a tool used inside the editor. And we will want the code to run and continue to run whenever the scatter tool is placed inside a level. Building the blueprint in this fashion means we can give access to the expose variables to the user and as Construction scripts are ran continuously, they will be able to see it update live as they make changes. Now, the first thing that we will want to do then is left click and drag our radius variable from the variables tab and then drop it onto the graph view, choosing the get option. We will also want to drag the collision trace range variable onto the graph and again choose the get option when prompted. Now, we've added these so that they can control the overall size of the Spawn area. So the radius variable will set the x and y dimensions whilst the trace range will set the z or z depth. Now, clicking and dragging on the radius variable output them, we can release the mouse button and then search for and add the make vector node. We also want to make sure that the radius is pived into the x and y inputs. The radius option will obviously control the overall coverage of our collision box and we could split these up if we wanted so that we have individual controls for each axis. This of course giving us greater control over the scattering area. In this case though, we will keep things simple and set the Spawn area to a square by using the same value in both fields. Once done, we can pipe the collision trace range output into the z input of the make vector node. Now to explain why we've done that, this essentially means that our Spawn area will now show inside the level roughly how far out the blueprint is looking in order to find collisions. Indeed, we will be able to test these controls out and see if they are actually working as soon as we have set up the collision logic. Whilst the user's ability to set the overall size of the Spawn area in a level has been completed though, the viewport representation of the tool still needs to be able to make use of the size and depth information that we have set up, which is exactly what we will go on to doing next.