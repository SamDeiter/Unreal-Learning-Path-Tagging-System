At this point, we've seen pretty much every different way that we can bring different data assets and files into our project. We can then use those to make different types of data assets and work with the information within those data files. To wrap things up, we're going to look at the other side of things, and this will be taking the information that we've been working with and exporting this back out from the engine. But we've already seen that there are different ways that we can do that as well by exporting things like a data table we can turn that into a CSV. But there will be times when you might want things to be a little bit more dynamic, and potentially taking the updates or the changes that you've made in code and exporting that as well. This isn't something I've seen covered very much, but there is definitely a way that we can do that. So if we start off inside of our code file, we want to be inside of the data library class that we created at the beginning of the course. So first of all, starting off in our header, we're just going to want to create a new function. This will just be a function that will be exposing it to Blueprint. It's going to be a very simple implementation, but hopefully once you've seen this kind of basic implementation, you can build upon that and you can take in your custom information from data file, add a few rows or something through the code, and then export that back out if needed. So it's growing down to the bottom of the header. So we're just going to create our new function underneath the generated body. So this function will be making use of some built-in functionality. This is the content I mentioned, which has been a little bit less than others in the engine and tutorials and things. We'll be making use of this built-in functionality so that you can begin to see how you could work with these. That reason I know that the outcome is going to be a function which is going to be calling a static Boolean or returning a static Boolean. So for that reason, we'll create or declare a new function as a static Boo named Save Table. This function will need a few different parameters, so we're going to have an F-string named Save Directory, second F-string named FileName and then a UDataTablePointer named DataTable and finally a Boolean named AllowOverwriting. These properties will just allow some flexibility as well as ensuring that we always provide the required information such as the directory in which we want to save our file to and a file name so that we can have unique file names. Things like the AllowOverwriting are pretty much optional, but again, this is going to just improve the reusability of this so that we can have some control over where we create loads of new files or if we just always default to overwriting the previous one regardless. Then we'll define a U function. I'm going to make this BlueprintColable so that we can work with this over in our Data Blueprint classes, but this in a category named Save and I'm also going to provide this one a meta as a specifier for the keywords and again I'm going to set this to be Save. Just so this is going to be very easy for us to find and work with. As we're putting this in a Blueprint function library, we'll be able to use this pretty much anywhere in the project. The final step is going to need to define this function now, so in RIDER I'm just going to use the shortcut Alt and Enter and then generate our implementation. Again, you should be quite familiar with C++, so hopefully creating a simple definition should be quite so easy if you're outside of RIDER. The main thing is that we're going to have our Boolean types, the name of the function, and our OpenClose curly brackets. Now the first thing we're just going to do the tidying of our Save Directory, and all this really looks like is we're going to get the Save Directory and we're going to add the take one of that is been set to. We're going to use a function inside of our Blueprint to return the current project directory just for simplicity. You could hard code this if you wanted, but I'm going to take that directory that's returned and just apply those two forward slashes to that. Then I'm also going to take the Save Directory and do another plus equal of the file names, we're going to get that location and the name that we want to give the file. Then we're going to do our security checks here, so the first thing again, as it in the past, if we don't have that data table being passed in, we don't have anything to work with, so we'll just return, of course, being a typed function, we need to make sure that we return something in this case or just say false. We're just going to do a quick safety check as well. If we haven't allowed the file to be overwritten, we're just going to want to do a quick check from our platform file manager and check if a file exists. What this will look like, we'll say from the platform file manager, the F platform file manager, we're going to get the platform file and check the file exists function passing in our save directory. So this has already been formatted, this is going to be the exact directory that we'll be working from, including the file name. If that file does already exist, then we're just going to want to return false again. You can add some extra layers here, you could check the file name, and if so, you could add something like a random date to it, so you could still create multiple files. I'm ensuring that the name is just slightly different, but in this case, we're just going to go for the simplified version. Again, if we already have a file and we don't want to replace that, then we'll just return it out of the function altogether. So if we've got past both of these different scenarios, then we know that we have our file data ready to work with. We have no conflicting files, or we're not worried about overwriting. If we do have a conflicting file in the folder, what we're going to do is return the use of that built-in function that I mentioned, which is our static pool function, and this is going to be the F file helper, and then using the save string to file function. And we can use another built-in function from here, which is a function housed within the U data table itself. So we're going to use our data table that we have available. And we're going to call one of these functions here. So we're going to say data table, and then get table as, and we've got the option to save this as a CSV, yes, but of course, we've been working mostly with CSV, so I'm going to stick to this. And the second thing that this function wants, we can see that it wants the string to save. So this yet table as CSV is returning a string, and we can just finish this with the save directory. So we're going to pass in the string that we want to save, and the directory we want it to be located. And that that is everything we need to do. We've made a very simple custom function here, taking in the use of some existing functionality. We work with the files that we have, and use the built-in file manager to help us place and take to where we want those files to be stored. Once we've saved and compiled this, we're going to head back on over to our editor, and we can see how we can get this working from one of our blueprints, as we've made this blueprint callable. Back inside the editor, the final few steps are going to be very simple. We're just going to navigate to the data items folder, and open the BP underscore data item table. Inside of this, it doesn't really matter where we place this logic. I'm just going to put it on be gameplay, as this will just make it very simple. We can press play. We have three different data items in the world. We know that this is going to work, and then we can check our file being saved. So what we want to do is simply find that save table function that we've just created. You can see here, it comes under the save category, and it would also come up just with the word save because of that meta information we've provided. So we'll find the save table. Plug this in, and then we can take note of the different aspects that we need to fill. So we've exposed all of this in a fairly usable way. We want to first of all plug in our data table. So we've obviously throughout the entirety of this project, we've been tracking this. So we just find our item data table, the one that we've been using to populate the information in our classes. We want a file name, so we can make this pretty much anything. You could expose this to a variable if you wanted. I'm just going to call this one test file. It's very important that we add the dot CSV to the end of this because we haven't otherwise called this to provide a specific file type. And of course, if you wanted to work with JSON, then you just change this to test file.json. And finally, we want that saved directory. So remember, I said we're going to use another built-in functionality. We'll pull from here. And we can use any of these project directories. I think in fact, I'm going to put it in the content directory, or we could even work with the saved directory. In this case, I know it's going to be easier just to navigate to the content directory. So I'm going to choose this one. Ideally, we'd probably want to work with the saved directory, though. But again, just for testing purposes, this should work very well. So we'll compile and save this. And back in impress play. Now, the first thing we want to do, in fact, is I'm going to go to the content folder. So this is why I said it will be easier to navigate to. We can right-click here. Go to the show and explore. And I'll just drag this over. This will take us straight to the content folder, which is why we knew we could get straight to that one, whereas we cannot get straight to the saved folder. And if we just keep this in mind, so we have that ready to go, we can see that we don't have any save files in this folder. If we hit the play button, we can stop the play immediately. Back into our content folder, and we can see we now have our test files.csv. That definitely worked. We'll just open this and check that the formatting has worked. We can see now we have a perfect CSV file exported. Now, one thing to note is to this point, I haven't been able to find a way. I don't think there exists even in C++, an efficient or correct way to add any information like an extra row or anything at runtime to a data table. They seem to be completely redone only. But this does kind of lower the effectiveness of this. But if you want to tell something like a save file, so that if you were changing things in the editor, every time you want to check for those changes and save this back out to have some form of backup. Because of course, you could be adding huge amounts of information through the editor. As we can always come into our data information we have here. And of course, you can add your new rows and information inside of this file. So if you wanted to kind of automate the process, this is where they functionality may come in handy. As I mentioned at the moment, I haven't found any way that we can add a row through Blueprints or C++. And then save like that extra information. I hopefully knowing that we can save those files and we can make use of some of those built-in functionalities. This is still going to prove useful to know how to use. Well, that was it. As I said, just to kind of add bonus, it's not going to be something that a lot of projects will use. It's going to be good as a kind of like I mentioned, a backup if you wanted to automate the process of making sure that recent updates you've made are stored or having a kind of collection of different iterations as you go through and add and work with your data table inside of Unreal. And you can very easily and automatically now get that back out of the engine, export that back out of the engine and store your backup CSV or JSON files on your system somewhere.