In this video we're going to configure our UMG assets for the game loop. For our game loop we're going to create two screens, one for the start and one for the end. The start screen will prompt the player to press any key to begin the game. The end screen will change depending on if the player wins or loses. It will also prompt them to press enter to retry or escape to quit. Whilst this is a primitive set of screens and won't form a proper menu, they'll provide the core game loop and elements to get to started. We're also going to add a timer to the HUD. To start with we're going to create two new UMG assets. So I'm going to right click user interface widget blueprint, let's let user widget and call this first one UI underscore start screen. They're going to duplicate it and call this one UI underscore end screen. Firstly I'm going to concentrate on the start screen. So I'm going to start by adding a canvas panel and then a background blur which can be found under special effects. You take the background blur and make sure the anchors fill the screen. So I'm going to click anchors and select fill. And then you're going to set the offset right and offset bottom both to zero. You should then see the background blur object fill the canvas panel. I'm going to set the blur strength to 100. I'm now going to add a text block to the canvas panel. You're going to select it and set the anchor to the center of the screen. Now to send to the text blocks to the center of the screen, I'm going to change position X and position Y back to zero and then set the alignment to 0.5 0.5. This will place that text block in the middle of the screen. You're going to turn size to content on and then set the text to say press any button to start. And that's our start screen done. So I'm going to compile it and save it and open up the end screen. To begin with, I'm going to do the same things. I'm going to add a canvas panel and a background blur. Set the background blur to fill the screen and set all of the offsets to zero. Once again, I'm going to set the blur strength to 100. And now I'm going to add a text. So I'm going to use a vertical box. So I'm going to add the vertical box to the canvas panel and once again, anchor this to the center. I'm going to set the position X and Y to zero and the alignment to 0.5. I'm going to turn on size to content to make sure its size is depending on the text boxes we're going to put in it. And now I'm going to put in three text boxes. The first one I'm going to set to say end screen text all in capitals. The reason why I'm setting it to this is that I don't ever want to see that text. We're going to set that dynamically when the player wins or loses. And then you're going to add two more text boxes. The top text box is going to say press enter to retry. Make sure it's aligned to the center. And with the bottom text box, I'm going to set this to say press escape to quit. And once again, I'm going to align that horizontally to the center. I'm then going to center to align the end screen text and set the size to 34. I'm going to take our end screen text, turn it into a variable and then call this win loss text. I'm going to go over to my graph, delete the existing functions and create a new custom event. This custom event, I'm going to call set end screen text. From here, I'm going to take the win loss text, drag out and use set text text. And unlike last time, I'm not going to convert this into an integer. Instead, I want to use a select node. So in the select node, I'm going to type two options. You lose. And you win. I put an exclamation point after reach. I'm going to change the index to be a Boolean. So now, we can see that when the Boolean is false, you lose. And when it's true, you win. Then we're going to drag this index into our event and rename it from index to be win. Question mark. And we're going to compile and save. We're unable to test these screens just yet as we need to set up our game mode first. We're going to start doing that in the next video.