In this video, we'll build upon our AI character work from the previous module and get our AI moving around as well as avoiding platform edges. Let's get started by setting up the AI controller and its association with the AI character. We're doing this because we want to access a reference of the enemy character that can be manipulated via the AI controller. In the content browser, navigate to content 2D sidescroller BP, Blueprints, enemy AI, and double click to open in the Blueprint Editor. Ensure that the event graph is selected and marquee select the existing two nodes and delete them. That way, we can start with a blank sheet before getting into it. The first thing we want to do is create an event on possess in the event graph, so right-click on the graph and locate that event. Drag from the possessed porn output of the event and create a cast to enemy character node. Note that doing it this way, automatically connects the possessed porn output of the event and the execution pin to the node, which saves a little time. We want to have continuous access to this new enemy character reference, so let's promote it to a variable. Do this by dragging from the as enemy character output and locating promote to variable. And this new variable enemy character. For the very basics, we don't need to do too much more and to start with, let's just get the AI moving and deal with each problem as it erases. Let's create a new on tick event in the graph, right-click in the graph and locate on tick. Control left-click drag the enemy character variable into the event graph. Drag from this newly placed node and locate the add movement input node. Control left-click drag another reference to the enemy character and then drag from this output and locate get actor forward vector. Connect a return value from this function to the world direction of the add movement input function. Ensured that the scale value is set to 1. What we've done here is found the facing direction of our character and had a scale value of 1 and that will mean they will move in the direction they are currently facing. Don't forget to connect the execution pin from the event tick into the add movement input function. Now let's give it a go. Compile, save and close the blue printer. Let's make sure that the player and the enemy are placed in such a way that they won't interact with each other right now and the enemy has some space to move. Begin a plane out of the session and observe the results. You can see that our AI walks along normally but unfortunately they fall straight off the platform so we should probably fix that. Let's discuss how we're going to go about fixing this problem. What do we want the enemy AI to do in this situation? We'd like them to detect the ledge, stop moving and reverse direction. Let's start by detecting the edge. In the content browser locate the enemy character and double click to open the blue printer move over to the viewport tab. We'll do that so we can visualize the changes we're making as we make them. In order to locate an edge we're going to use a shape to check for collision and overlaps in front of the enemy character. We want to pair this to the capsule component so make sure that that is selected in the component hierarchy in the components panel on the left hand side. Click on the add component button and select box collision. Name this new box collision component ledge check volume. In the transform rollout of the details panel on the right hand side set the location to 30 0 minus 20 and the scale to 0.25 11. With that done we should see the block placed in front of the character and a little way below their feet. Let's just confirm that by changing the viewport to the right perspective and selecting the unlit view mode. Zooming in we can see the new box collision shape is in just the right place. With that done we can save and compile the blueprint. With our new volume created now we'll create a function to check for overlaps between this volume and the tile map. Begin by moving over to the event graph tab and create a new function called check ledge. Locate the ledge check volume we just created in the components panel and control left click it into the event graph. Drag from the output of this variable and locate get overlapping actors. This function does exactly what it says on the tin. It will get every actor that overlaps with our newly created volume. We can see that the output of this function is an array of actor objects so let's see how many objects are in this array. Drag from the overlapping actors array output and locate length. Next drag from the output of the length function and put in the greater than symbol. This node will now return true if the ledge check volume is currently overlapping with any other actor. But this would include the player and anything else in the world but we only want to consider tiles from the tile map. Luckily there's a built-in way to detect the current flaw that the character is moving along so we'll use that. From the components panel control left click drag from the character movement component. Drag out from the character movement variable and locate get current flaw. This will return a structure which we need to break open so let's do that by dragging from the structure and locating break find flaw result. Now we're getting somewhere. We have a boolean that will indicate if the current flaw is the walkable flaw that is to say what the character is currently moving on. Drag from this walkable flaw boolean and find the AND boolean operator. Select the output of our previous logic into the AND operator. Reposition the existing nodes to make sure everything lines up neatly and doesn't overlap. With those hooked up the output of this boolean will now return true if the box collider overlaps with any other object and the other object is a current walkable flaw. This will indicate that there's more walkable platform in the facing direction of the enemy character contained within the ledge check volume. If this returns false it means that the volume did not overlap with anything including a walkable flaw. We want to execute different logic depending on the outcome of this test so let's connect a branch node that the output of the AND node. When we add the branch that's another good opportunity to tidy things up so that we can line up the execution pin from the check ledge to the branch. Once everything looks a little bit neater go ahead and connect the check ledge to the branch with the execution wire. Okay so this check ledge function is looking good let's return to the main event graph and make sure this function is actually called. Move back to the main event graph and locate event tick by double clicking it in the roll out in the my blueprint tab. Drag from the execution pin of the event tick without disconnecting the existing connection. Locate on newly created check ledge function. Notice that if we add it in this way we don't have to reconnect the execution pin to the handle animation function. And with that hooked up we're now checking during every tick event to see if we've reached the end of a ledge. Great there's just one problem we're not actually doing anything with that information yet so we should probably go ahead and do that. This detection of ledges is a great example of how we can use event dispatches to neatly manage this process between multiple actors in a clean and flexible way. The ideal way to handle this is that the AI controller responds to an event that originated from within the enemy character. First we need to create an event the AI controller can receive. Let's start by making the event itself using an event dispatcher. In the my blueprint panel click the plus next to event dispatches to create one. Name this new event dispatcher on detected ledge. In the my blueprint panel double click on the check ledge function to return to editing. Locate the branch node that we have here that isn't doing anything at the moment. Drag from the false condition of the branch in the check ledge function. This will be called when nothing is overlapping with the box volume. Thus there's no ledge in front of the enemy character. Locate call on detected ledge. Now whenever this node is executed the message will be sent. However at the moment nothing is listening for this message so nothing will happen. In our real engine we call the process of listening for these messages binding. Now that we have our events set up correctly let's move to the AI controller to bind to this specific event. Return to the content browser and double click on the enemy AI blueprint to open it. In some clear space control left click and drag the enemy character variable into the graph. Drag from this reference and locate bind event to on detected ledge. Marquee select these two nodes and position them at the end of the event on possess execution chain. Connect the execution pin from the previous enemy character set variable into this new bind event. We're almost there. We just need to create the event that will be fired when this bind event is triggered. Drag from the event on a bind event node and locate add custom event. Name this event path blocked as well. We're using the same event to cover multiple reasons why the enemy character should turn around as we progress. Congratulations we've just bound our first event with the custom event set up. Let's return to the enemy character events graph. What we want to do now is create a new custom event and main this custom event rotate. Drag from the execution pin and locate add actor world rotation. Change the delta rotation to 0 0 180. Thus rotating the actor around the Z axis. Compile the blueprint and return to the enemy AI controller. Create a new reference to the enemy character variable and drag it into the event graph. Drag from this variable and locate the new rotate custom event that we just created. Now connect the execution of this rotate function to the path blocked custom event that we created. Take this opportunity to reposition any existing nodes to keep things neat and separate. Okay perfect that's everything set up. Now whenever this event occurs the rotate event is occurred on the correct enemy character. Let's return to the main viewport and have a look at this. From the main toolbar from the play dropdown select simulate. Selecting simulate from the viewport menu like this removes the player character and we can focus on the AI alone. With the game running in simulate mode we can see that it works. The AI now walks backwards and forwards without falling to their death. So that handles ledges but what about bumping into walls? So let's handle the other common instance directly bumping into a wall or other solid obstacle. To test this properly we'll need to make some adjustments to our tile maps. So let's modify the existing scene to include a wall on one side and a ledge on the other. Select tile map in the viewport and press control E or press edit map in the details panel on the right hand side. This will open the tile map editor. Ensure that the middle layer is selected from the layer list in the details panel on the right hand side. I also recommend turning on the grid from the top option by selecting tile grid in the toolbar. We just need something functional for now so let's copy paste some existing tiles. Let's shift left click to select multiple tiles at the current platform and place them up a path like so. We can do the same thing to select tiles on the other side quickly. For example that's really all we need to do so let's save the tile map and return to the editor. Now it would also be a good time to add another light source. Select the existing light source, hold down alt and drag to drag a second light source into the scene. Ensuring that we're still in simulate mode let's press play and see what happens. Well that's not ideal. While they avoid the ledge when they come to the wall they don't seem to move anymore. While the animation may make it look like they've actually decided to stop they're actually continuously trying to move which isn't really good or desirable. What we need to do here is get them to use the same reverse functionality just as they do with the ledge. Stop the simulation session and return to the enemy character event graph. Now just like before we need to create an event dispatcher to pass this message to the enemy AI controller. Create a new event dispatcher and name it on hit obstacle. Now we just need to add something to call the new on hit obstacle event dispatcher. Luckily we have something built in that we can use. Right click in an empty space on the graph and locate event hit. Now we need to do is drag from the execution pin of event hit and locate call on hit obstacle. Now whenever the enemy character hits any sort of blocking object the event will be called. This is also very simplified and we could use other details provided by the hit event to make this more granular but it works for now. Let's finish the job by returning to the enemy AI controller. Create a new reference to the enemy character variable and create a bind event to on hit obstacle node similar to what we did before. Marquis select these nodes and position them near to our previous bind event. Don't forget to hook up the execution wire so that both of these are fired. Connect the event from this new bind event to the previous path blocked custom event. We do this because we want exactly the same functionality for both events. Now's a good time to double click on this execution wire to add a re-root node. We can now reposition the re-root node to prevent these events lines from overlapping. With everything hooked up here let's go and compile and save the enemy character and compile and save the enemy AI. And we're done with the blueprints and we can return to the main viewport. Once again let's run a simulation session and see what happens. Great there we go the AI will now avoid ledges and they will turn away when they face a wall. Perfect. Let's stop the session and quickly review what we've done. Returning to the enemy character event graph we can see the changes that we've made. But let's put these together and organize them into a comment. Locate the rotate custom event and place it near to our event hit. Marquis select these nodes and press C to add a comment. Let's write navigation. Let's compile, save and close that blueprint. Let's return to the enemy AI controller to do a similar thing. Marquis select everything connected to the event on possess and press C to add a comment. For this comment to remind us what it does let's add cast to enemy character and bind events. We can compile, save and close this blueprint and that finishes this section. In this video we made significant progress with our AI character. We began by using the AI controller in conjunction with the enemy character to get them moving. Before them going on to deal with common issues such as falling off platforms and bumping into walls. We added a collision volume to the character to detect the absence of a platform ahead of them and used event dispatches to manage communication between the AI controller and the enemy character actor ensuring that the logic was handled in the right place at the right time. We finished up by using the on hit event to detect when the character was going to collide with the wall and use the same event dispatcher method to handle it. Finally we finished up by using the simulate mode of the editor to test AI functionality without being distracted by the player character.