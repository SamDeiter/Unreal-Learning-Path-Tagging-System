Hi everyone, I'm Dugu Rui and I'll be guiding you through this gameplay ability system tutorial. This is the fifth lecture in our guest series. In this session, we'll implement a core character that helps and learn how to link it to a UI widget. Let's go over today's learning objectives and learn how to define a custom C++ attribute set class. Learn how to override C++ functions to clamp attribute values within a valid range. Learn how to bind attribute changes to a UI widget. Before we begin, let me show you a video of what we'll build in this session. In this lecture, we'll implement a health attribute and connect it to UI widget for both the player and NPC characters. Here are the core components we'll focus on today. In this lecture, we'll be covering the attribute set located on the right side of the gas flow diagram. As first, talk about gameplay attribute data. A core data structure you need to understand when working with the attribute sets. Gameplay attribute data is a structure used to manage the value of a single character stat. Typically, you might use a flow variable to represent a stat, but instead of just using float, it is strongly recommended to use this structure. That's because gameplay attribute data contains two flow values, base value and current value. This allows for more systematic control of various stat changes during gameplay. Base value represents the permanent base value over stat. Think of it as the actual stat data. However, temporary effects such as shoot buffs may apply, and these shoot temporarily alter the stat value. These are an opponent and typically expire after some time. That's where current value comes in. Having both values allows you to flexibly apply game rules. If you need the current stat during gameplay, use current value. If you want to permanently change the stat, for example, after leveling up, modify base value. And since these two values are always linked, it's good practice to initialize them to the same value when setting up the attribute. It's a class that groups together multiple gameplay attribute data variables. So to define a stat, simply declare it as a gameplay attribute data variable. For each stat, it's highly recommended to also define a corresponding max stat. For example, if you have a health, you should also declare max health. This ensures system stability by keeping stat values within safe bounds. To make this work reliably, override the pre-attribute change function in your attribute stat class. This function is called whenever an attribute is about to change. By adding logic here, you can clamp values and enforce the limits safely. Let's consider how we might manage the health stat specifically. First, we should ensure that the health value never exceeds max health. And since max health is often used for UI division, for example progress bars, it should never be zero. It's better to keep it above one. A post-attribute change function is also available, which is triggered after stat changes. This is useful for notifying other components like UI widgets to update their displays. Note that attribute stat must be written in C++. You can't create them in Blueprint. So how do we initialize an attribute stat and link it to a UI? Here's the flow. First, ASC calls initialized component and looks for attribute stat object owned by the actor. It registers them for management. Right after this, the actor's posted initialized component function is called. This is a great point to link the UI. You can bind a custom event in your UI to delegate, declared in your attribute stat. When attribute value changes via gameplay abilities, the UI updates automatically. Now let's move on to a hand-drawn example. We'll implement an attribute stat and connect it to the UI so that attribute changes are reflected in real time. For this session, we prepared the example for start-to-keep file in advance. When extracting the file, we'll rename the folder to example 4. Now let's go through the basic set of for the exercise. Right-click the project file, while holding SHIFT, and select Generate Visual Studio files. This will generate the solution file. Double-click it to open the solution. Make sure the start-to-project is set to Arena Beta Gas. Then proceed to build a project. Once the build is complete, launch the editor. For this session, we will duplicate the level from the previous lecture and create a new one named step 4, which has been set as the default level. Now let's officially begin the hand-drawn session. To create an attribute to set, we first needed to generate a C++ class. From the tools menu, select new C++ class, set the parent class to attribute to set, and name it AB Character Attribute to Set. Proceed to create and compile the class. This attribute to set will be used to define the Character Space Set. Once it's complete, go to Visual Studio. Select the re-road all button. Navigate to the header file of the newly added AB Character Attribute to Set class. First, declare a constructor. Then let's define the character's most basic set, Health. To manage the health value, we declare a variable using the app gameplay attribute data struct and name it Health. Then add the U property macro. So it's visible in Blueprint. Set it as Blueprint with only. Since the value should only be changed by rules, gas defined. This is because state changes will be controlled through specific logic. Set the category to attribute. Typically, we manage stats like Health using a float. But let's look at how app gameplay attribute data manages it instead. If we jump to the declaration of app gameplay attribute data, you see it consists of two slots, base value and current value. Base value is used for permanent values like character-based stats. Current value is full-temperly effect like drops or shows. If we initialize with the value, both fields are set to that value by default. Let's return to our file. Now that we've defined the health stat, it's good practice to also define max health to help keep health values safe. This ensures all step values stay within valid expected ranges. So let's declare max health right above health. Next, we declare get and set functions for each stat. Each stat has base and current values. If we manually created functions for each, we need a total just for health. To simplify, Unreal provides macros to generate essential functions automatically. First, include the ability system component that H header. Then, use the attribute to access the basic macro. After calling the macro, pass in the class and the attribute name. We'll start with max health. If you go to the macro definition, you'll see it generates full functions for attribute. Want to identify the attribute, want to get a value, want to set it, and want to initialize it. To get it, use current value and set it, use space value. The initializer sets both values to the same number. Only four are declared instead of eight, because these are the ones primarily used. Let's go back. Copy and paste the macro code for health as well. Now, let's implement the constructor as a definition. The max health, an initial value of 100, then call init health using get max health, if the macro was set up collectively. Open the parent attribute to that class declaration. Go to the parent class declaration. It provides a virtual function to help with this. The implementation is empty, so we'll copy and override the pre-attributed change function. This function is called whenever an attribute is about to change. The new value is passed by reprons, which allows us to modify it before it finalized. Let's add the definition. Since the base class has no logic, we don't need to call super. First, let's constrain max health. Check if the incoming attribute is max health. So the new max health value to be at least 1.0. This is because max health is often used as a divider in the HP bar UI. If it was zero or less than one, it could cause issues. Now, let's constrain health. This should not be less than zero or more than max health. Use f-mass clamp to enforce this. That completes our base configuration for health. Let's apply it to our character. Go to abbasiccharacter.h. And forward the declaration to declare a uab character attribute to set pointer. Set it as a read only u-property with a category attribute. In the definition file. Include the ab character attribute to set that h-hether. In the constructor, create the object using create the parts sub-object. Then manually register the attribute to set with the a-s-c. The four gameplay begins. It's good to initialize the character stats. For that, we override the actor's post-initialized components function. So declare the virtual function override in the h-hether. Then implement it. Code parent function using super. Set our custom initialization logic here. Ideally, this would be in blueprints. But there's no blueprint equivalent event for post-initialized components. So we declare a new blueprint implementable event. Back in the h-hether. Declare a function named abpost-initialized component. Use u-function with a blueprint implementable event. Specifier. Then code this function in the post-initialized component implementation. This will allow blueprint to implement this event. Let's use live coding to compile. Once compiled successfully, let's go back to the editor. Replay to check if the initial attribute values are collected is set to 100. Now we create the h-bar widget. Go to bpabc. Add the abpost-initialized components event at the bottom. Use a sequence node to handle multiple tasks. First, create the h-bar. Use a create widget node to create the widget and assign wbphbbar which I prepared in advance. Store the widget in a variable. Then assign it to the widget property of the character's h-bbar component. Click play to confirm that the h-bbar appears on the character. The value shown as 100 is just the default from the widget, not the actual attribute. We now need to link the widget to the attribute to set. In the next step, we retrieve the attribute to set from asc. Then connect the widget to the attribute to set so it updates automatically when values change. Let's go back visual studio. This time, we'll use the poster attribute to change function from the attribute to set. This function is called after an attribute value has changed, we can use it to notify the UI to refresh. First, differentiate between health and max health. We'll use the delegate to link this with the UI. This delegate will be designed to pass two parameters, the previous value and the current value. Select the declare dynamic multi-cast delegate to programs macro. Set the delegate type to receive subscriptions as app attribute data changes. Then enter float, order value, and float new value in order to complete the delegate type declaration. Next, declare the on max health changed and on health changed the delegate using this tie. And add the blueprint of sign-up specifier in new property so that blueprint events can subscribe to them. Add a mutable keyword in front to allow binding even in a constant context. There was a typo here, so let's fix it. Now, in the definition file, code of broadcast function when an attribute changes so that any subscript blueprint event will be executed. That concludes the task needed in the attribute to set. Compile the code using live coding. Now create an event in UI blueprint to subscribe to this delegate. Open the WBPHP bar widget in the UI folder and go to the graph. In here, if I let it prepare the UI logic, that displays the HP bar based on the front health and quantum max health values set in the widget. So whenever the attribute changes, we just need to update the UI variables and code the repression HP bar function in an empty space above create a custom event and name it bind the character attributes. Here we make the event that subscribes to the delegate we said earlier. Set it up to take the character's attribute to set as an input parameter. From the incoming attribute to set, get the delegate and connect it to the blueprint event function. Search for bind and get the max health change the delegate. Drag from the red pin to create a custom event connected to it, name this event, set max health change the BP. In this event, update the quantum max health value, then call repression HP bar. Next, get the own health change the delegate. Again, drag from the red pin. Name this one, set health change the BP. In this event, update the quantum health value and call repression HP bar just like before. Click compile and save. Now add the logic to call this new event in the UI. Go to BPADPC. Pass the attribute to set, obtained from the ASC into the bind character attribute function of the UI visit. The player's HP bar visit is now fully linked to the character's attribute set. With the connection complete, the UI will only update if the values actually change. To test this, we'll change the two values directly in code. In the definition of base character, go right after the initialization function call. Change the attribute to values at the point. Use the set numeric attribute based function from the ASC. Call get max health attribute from the character's attribute set to set max health. For testing, enter 0.1 as is the initial value. Copy the code and this time set health instead of max health with a value of 200.0. With this setup, based on our attribute set logic, max health will be clamped to one and health will also become one. Compile again using live coding. Press play to check the result. If it works collectively, change the maximum value from 0.1 to 200. Perform live compile again. Press play to confirm the value as returned to 200, completing the basic C++ implementation. Now, apply the same functionality to the NPC. NPC work will proceed the same as for the PC. Copy the HP bar visit variable from BPABPC. Paste it into the variables of BPAB NPC. Copy the entire AB post initialize component block and paste it to implement the same logic. Press play and you'll see the NPC now also has the visit attached. Press play and confirm the updated values. Lastly, let's link the HOT HP bar with the player's attributes. We'll implement the same binding logic in the HOT as in the HP bar visit. So go to BPABPC, cut the widget creation logic from the possessive event. Instead, we'll execute it earlier from AB post initialize component. Open the HP bar visit blueprint. Copy the same logic from HP bar visit and paste it into HOT visit. Since the variable and function names match, it should work right away. Go to BPABPC, then call the bind character attribute chip function and connect the attribute to set pin. Compile and press play to confirm it works. If everything is working collectively, we'll rep up here. Exit the unreal editor. Wait a moment, then return to be your studio to create the final build. That's all for today's exercise. Let's review what we covered in this session. Attribute to set can currently only be created using C++. Each attribute holds two values, base and current. It is recommended to define a maximum attribute value separately for each set. Use pre-attribute to change the validated incoming values before they are applied. Use post attribute to change to notify related systems or components of the change. Impose the initialize component. Find the UI widget to the corresponding attribute to set for automatic updates. Try using the provided step 4 level to practice implementing different attribute to set and UIs. That wraps up this lecture. I hope you are looking forward to the next one. I'm Dugu Rhee. Thanks for watching. Bye bye.