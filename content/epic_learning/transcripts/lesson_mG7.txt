Now that we understand the value of behavior trees, it's time to create our very own behavior tree within Unreal Engine. We will begin by creating the necessary assets, and then adding functionality to our AI controller to run the behavior tree automatically. We will finish up by creating our first behavior tree branch that will replace the random one-ter logic we created earlier with something much more flexible. Let's begin by creating the assets we'll need. First, create a new folder in Content, third-person BP, Blueprints. Name it BT underscore assets. Open the folder, and then right-click in the Content Browser, select Artificial Intelligence and then Behavior Tree. Name this asset BT underscore enemy AI. The BT briefs to note this asset as a behavior tree. Once again, right-click in the Content Browser, select Artificial Intelligence and then Blackboard. Name this asset VV underscore enemy AI. As you may have realised, the BT prefix to note this asset as a Blackboard. The next step is to ensure that our AI actually uses this behavior tree. To do this, we need to open the AI controller. Within the Content Browser, navigate to the Blueprints folder and open AIC underscore third-person character. Right-click in the Event Graph and begin typing event on possess. Drag out from the execution narrow of this event and begin typing run behavior tree. Select our newly created behavior tree asset from the BT asset dropdown inside this function. Compile by hitting F7 or the compile button or toolbar and save this blueprint before closing the Blueprint Eraser. Next, we need to ensure that our Blackboard we created is associated with our behavior tree. Return to the BT underscore assets folder within the Content Browser and open BT underscore enemy AI. Moving over to the Details panel in the AI rollout expand the behavior tree rollout and ensure that our Blackboard asset BB underscore enemy AI is selected. Next, we need to create somewhere within the Blackboard to store a target location which we'll later ask the AI to move towards as part of our new random wonder functionality. We'll do this by creating a new Blackboard key. With the behavior tree still open, select the Blackboard tab from the top right hand side of the window. Now, move over to the Blackboard tab on the left hand side and press the new key button. Select the Blackboard key type of vector. Name this vector target location. Return to the behavior tree tab on the top right hand side and though in the Blackboard tab on the bottom right hand side, we can now see our new vector. The next step is to create a new task that encapsulates all the logic of our previous random wonder function. Like the old version, this task will find a new valid location that can be reached from the AI's current location. From the behavior tree window, select new task. And then select BT task underscore blueprint base. This will create the new task and open the event graph. However, before we do this, let's quickly organize our folder structure. Minimize the event graph to return to the main editor window and the content browser. In the content browser, locate the newly created task and rename it to BT T underscore find navigable location. As you may have guessed, the BT T prefix indicates that this is a behavior tree task. As it is a task, we should probably create a folder to keep it within. So let's do that now. Right click in the content browser and select new folder. Name this new folder tasks. Drag our newly created task inside and double click to open the tasks folder and then double click again on the task to return to the event graph. We will start by adding a new event. This event is called to receive execute AI. And this is the event that will be triggered by the behavior tree when it attempts to run the task. Add one now by right clicking on the event graph and starting to type event receive execute AI. Within this task, we'll make use of the very same get random reachable point in radius function. The only difference will be what will execute this function and what we will do with the result. Begin by adding the function in the same way as before by right clicking on the event graph and typing get random reachable point in radius. The first piece of information we need is the origin and luckily the event receive execute AI gives us a really easy way to do this. One of the pieces of information it provides is the pawn that is currently controlling. And we can use this to derive the actor location. Drag out from the controlled pawn pin and begin typing get actor location. Connect the return value of the get actor location function to the origin of the get random reachable point in radius. Set the radius value to 1000. Now we need to do something with the random location that is returned from this function. Unlike our previous setup, once this task is completed all information contained within it is destroyed. What we need to do then is transfer this random location from the task to the behavior tree and to the blackboard. We can do this by using a blackboard key. Drag out from the random location and begin typing set blackboard value as vector. Note the second piece of information that this function requires is the value, the vector value. By dragging it out in this way we've automatically connected the random location to the value. However, we still need a blackboard key. Drag out from the key value of the function and select promote to variable. Name this new variable target location key. In the my blueprint panel, in the variables roll out ensure that this new variable is set to public as we will need to manipulate it from the behavior tree itself. Press F7 to compile this task. Before we forget, connect the execution pin from the event receive execute AI to the set blackboard value as vector function. Now we're nearly finished with this task. The only thing left to do is to tell the behavior tree itself that this task is complete. It's a very common mistake to forget to add this. And if we don't, the behavior tree will never consider this task as finished and will get stuck on this task forever. Right click in the event graph and begin typing finish execute. Connect the execution pin between the set blackboard value as vector function and the finish execute function. Note that we have a ball that indicates success or failure. The cool thing we can do here is we can directly connect the return value of the get random reach point in radius to this success variable. This means that as long as the function is able to find a random location, then the task itself can be marked as the success. All other outcomes will be considered a failure. Note that it's a big improvement on our first implementation as we can actually tell if it's succeeded or failed rather than just having to assume that it's succeeded. With that, the task is complete. We can press F7 or hit the compile button to compile the blueprint, hit control S to save the blueprint and close the window. And with that, our random one to sequence is complete. Even with a tree this simple, we can begin to see the power of behavior trees. Just by ordering our task in a simple sequence, we've implied some useful logic. Remember that a sequence abort when any task fails. So the first task cannot find a suitable location. We don't even try to move towards it as the sequences are boughted. Each one of these tasks has clearly defined states, running, stopped and completed, including whether they will complete due successfully or not. This is completely in contrast to our simplified version, which was essentially flying blind, and had no idea about the status of anything it'd been asked to do previously. In this video, we put our behavior tree theory into practice. We began by creating the behavior tree and blackboard assets for our project. Next, we saw how the behavior tree asset is where we will place all of our logic while the blackboard acts as the memory for the AI. We associated the behavior tree with our AI character by adding blueprint script to the AI controller, instructing it to run the behavior tree when we start playing the game. Finally, we recreated our earlier random wonder logic as a behavior tree branch, and saw how we could replicate this behavior in a much more flexible, easier to read manner using behavior trees.