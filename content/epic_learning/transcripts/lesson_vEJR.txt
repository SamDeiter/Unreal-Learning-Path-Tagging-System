We will now look at setting up a custom HUD or heads up display to translate the position of our objective markers that we will place in the level into icons that are drawn on top of all the level elements on screen. We're going to create our own custom HUD class that finds the location of all the objective markers that we placed in the world and draws them as on screen data as another layer on the screen. So by default in the world settings here under game mode, the third person game mode uses this C++ HUD class. This doesn't have any functions for us to use that are useful for us to show our objective markers. So we're going to create our own custom one that is a child of this class and then we're going to reassign our custom HUD class here in the world settings. We'll begin creating the custom HUD class. We're going to open up the content drawer and navigate to the third person Blueprints folder where we already have our player UI widget and I'm going to create a new class and we're going to search here for HUD as our parent class and select that and I'll name this HUD player UI. At the moment we're just going to use this for objective markers but further on maybe we could add more to it so we'll just name it HUD player UI. I'll save this and open it up so that we can do some actual scripting inside the HUD class. Looks like we need to open the full Blueprint Editor to have access to the event graph. So here we are. We don't need all these events. We're just going to use event begin play at this point. I'm going to create a new function to initialize our UI refs. In this case our UI refs are only the objective markers that exist in the world. So we're calling this the beginning of play so it's not too expensive and it doesn't cause too many problems. So right here I'm saving a reference to all the objective markers and I'll call this initialize function at the start. Next I'm going to add an event for when the HUD is being drawn. This is roughly every tick or thereabouts. I'm going to save the outputs of the screen size X and Y come from this. Using this in some of our calculations of how we draw things. So I'm going to connect those and make a new function which is our draw objective markers function. This is where we finally connect back to the objective markers that we placed in the world. So right here I'm going to loop through all the objective markers in the world. I'm going to look for to make sure they're valid. I'm going to do a new valid check. If it's valid I'm going to get a reference to its enabled marker. Only enabled ones should be drawn in the HUD so we want to see we're going to do the drawing based off the true of this branch check. Now we're going to do a draw texture on the right hand side of the event graph. This is what is actually going to draw it on the screen. So the texture we want to draw I'm going to make this a variable at this point. We're called that waypoint texture. This is going to change based on if it's off screen depending on the player's pitching on and stuff like that. So we're going to connect that there. I'm going to make the scale its own variable. We call that objective marker scale and the rotation. This will change based on the players where the players looking in relation to the objective marker as well. Also making the rotation a variable as well. Okay. So I'm going to grab a reference to the objective marker component. That's the billboard we have placed inside that actor that actually determines the world location. So I'm going to get the world location here and I'm going to feed this location into a project world to screen function. Get the world position from that and get a reference to the player controller at index zero, which is again our sort of default single player character. The only character we have in our game design at this point. So we're going to take the screen position and break this up into the x and y that the length and width more or less of the plane of the screen. And I'm going to say these out as variable screen position x and screen position y. Just connect these to the true output of the enabled of the marker itself. And I'll just sort of move these around to give us a little more room. And I'm going to set up some some screen locations here for where we draw the icon on the screen. Basically what I'm doing here on screen x and screen y is I'm indenting the marker so that it's drawn in a little bit from the very edge of the screen. These are just some minor aesthetic concerns. It's not really the focus what we're doing or the focus of the course, but these are just some values that I'm inputting here. To limit how far on the edges of the screen that these things can be drawn, but it's sort of aesthetic and we won't really get it into too much detail on the specifics of this. But I'm using screen space x. I'm going to left and right and screen size y, top and bottom and just bringing in how far to the edge it will be drawn. So multiplying this by 0.95 to bring it in there on that edge. And then on the y, I'm going to multiply this by 0.9. And that will just bring it in a little bit from the edge there and I'll set a minimum clamp value of 5. Next I'm going to go over to the draw texture function and I'm just setting some values here that I thought worked okay for a proto-UI for what we're doing in the course. It's not really the focus of the course. We're just giving it something to work with as level designers. So next we're going to need a function to draw and change the appearance of the marker image. So we're going to call this function set marker image. We're going to call it right before the draw texture. This is going to change a few things before draw texture function is called. Inside this function we're going to do a branch check. We're going to do some other determinations of where the objective marker is or the icon is on the screen depending on which way the player is looking. Right here if it's true, we'll do the math on this branch check in a moment. But if it's true we're going to draw the objective arrow. If it's an arrow it means the player is not looking at the screen space of the icon in the world. And if it's the waypoint texture down below that means they are looking at it. So we're going to do a pure function here called is off screen horizontal. Here we'll just do it. A parameter is off screen horizontal. Actually we want to set this to the output here. So it's a it's add that parameter to the output is off screen horizontal. And then we're going to do some checks here. We're going to add the on screen sorry the screen position X. And if it's greater than screen size Y. And there'll be some boundaries here as well. It's greater than multiplying screen size X times 0.95 that brings things in a little bit. And we'll do an or for another condition. If screen position X is less than five. And we'll connect this to is off screen horizontal. And we're going to make sure this is a pure function. So let's see a drag and drop it's not yet we can see that's not what a pure function looks like. I'll click pure for the function and now it can just be run whenever that branch check is done. And then if it's false and if it's true there's two different things that happen let's do another branch check here. Screen position X is less than five. And we're going to set this objective marker rotation to 0 but to 180 so it gets flipped. The other direction meaning the players looking like 180 degrees 180 degrees the wrong direction the arrow points them back to look at where it should be in the world. And if it's false the objective marker rotation 0 and it looks in the other direction. So we're going to add another pure function here to is off screen vertical. And we'll pass the parameter in the output. Result of this rather. So we'll do screen position Y is greater than screen size Y. And we're going to do a multiply here as well. Screen size Y times 0.9 so again indent thing a little bit on the edge of the screen. You were at will at an orbillion. Similar to our last is a screen horizontal. Here function and we'll do screen position Y is less than five. And we'll connect this to is off screen vertical. Compile and save this. And make sure it's a pure function as well so we can connect it to our branch. And run it on every branch check. So we'll connect it there. And again we want to change the waypoint texture depending if the player is looking out the icon location in the screen space or not. And again if it's true we'll be drawing the objective arrow. If it's false we'll be drawing the objective waypoint the diamond shape. And then if the arrow we want to rotate the arrow again based on which way the player is looking off screen. So we'll take screen position Y. It's less than five. Set the objective marker rotation minus 90 again to turn the arrow inwards toward the center of the screen from one side of the screen or the other side of the screen. And if the objective the waypoint texture is just the diamond we don't want any rotation just we're going to leave it at zero. So another way of our set marker function all made we're going to add it to the event received draw HUD. So it draws this every tick. Now let's do a quick test here. Okay one last thing we have to do is in the world settings we're going to change our HUD class to our new HUD player UI. And we're going to give a little test here and see how this is working. So if I hit all P to play an editor we see okay we see our icons in the world the diamond icon. However it's very big. Means we missed a scale setting somewhere. Now we missed a scale setting somewhere in one of the functions in the HUD. Let's go back to that let's have a look here. So we can draw objective markers and yeah it looks like okay our objective marker default value scale is one which is really big. So we can change that easily we can just shrink this way down. Let's do 0.125. We don't want this to be an overwhelming distracting element for the player during their experience. So let's see how that looks. Okay that's a good size but it's it's pinned to the top of the screen which means I missed a value somewhere in the HUD. It's trying that in the wrong place and again these values are largely cosmetic but some of them are necessary to make it drawn screen. Oh it looks like okay down here on screens on yeah on the Y it looks like I should have added 0.9 I added 0 which means it was never going to draw on screen. So I added 0.9 to that and it should be working now and there we go it looks like it's actually drawing where we place the objective marker in the world. Right so when we turn our camera away from it we see that it's showing us the location of the marker off screen so we can pivot our camera towards it. So that's good enough for what we need for proto UI. And now that we have completed our basic gameplay UI functionality we can begin adding all of the objective markers required to inform the player of necessary destinations during gameplay progression.