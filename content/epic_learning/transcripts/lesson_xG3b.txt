Another way to move data between SketchUp Pro and Unreal Engine is to use DirectLink. Now this sets up a live connection between SketchUp and Unreal Engine, and when you enact changes in SketchUp, those are reflected in Unreal Engine. So all we have to do is go over to our extensions, DataSmith, and instead of Export, we're just going to turn on toggle auto sync. Now in the toolbar, it's this button here, so when that's blue, you should have auto sync turned on. Now once that is switched on, you can move over into Unreal Engine and accept that connection and create the link. So all we have to do is hop over to Unreal Engine, and then we're going to add in DataSmith, but instead of a file import, we're going to do a DirectLink import. And then that just gives me the name of the file, and I'm just going to go ahead and hit select. Now when I do, again, it asks me where do we want to put it, as well as what do we want to import. And once we do, it's exactly the same as importing the file. Now what this does is creates a subfolder for that file that contains all the geometry, materials, and textures. And again, it's just like an import. You can manipulate your data exactly the same way. But the one difference is that this data is connected. So let's go back over to SketchUp and select an object and change it. So I'm going to select this planter here, and let's just go ahead and move it a little bit along the red axis to the left, and then hit the synchronized button here. And when I do, it should synchronize in Unreal Engine. And there we go. So this particular object has now changed. Now if I wanted to, I could select that object in Unreal Engine and move it somewhere. So let's go ahead and just move it forward a little bit. And as you see, when I move this forward, its location is changing from a value of zero. So if I zero this out, it's exactly where it is in the SketchUp file. But what we're doing is we're creating an offset here. So as I move it forward, you can see it's 800 units towards me. Now if I go back over to SketchUp, and I select this, and I move it back, and then synchronize, you'll see that in Unreal Engine, this has moved back, but it also has that offset. So if I type in zero here in the Y direction, you'll see it will go back. Now this brings up some issues that may pop up when you are working with DirectLink. And that is any changes in Unreal Engine will basically be relative to the original SketchUp file. So if you go to SketchUp file, it changes, your changes in Unreal Engine will also reflect that. So that can get kind of complicated, particularly with materials. So if you add new materials in Unreal Engine and you synchronize, they will go back to the original SketchUp materials. So you've got to be careful with that. And because of that, sometimes a DirectFile import might be a little bit more efficient. And as you can see, this does have a lot of power, and it's a great way to work with other people who are using SketchUp.