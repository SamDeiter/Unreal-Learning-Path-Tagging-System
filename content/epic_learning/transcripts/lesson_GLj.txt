What is the feature pack and why we want to use it? So feature pack is used for adding content to an existing project. It can be used to add in C++ code as well as Blueprint code. It also adds the ability to add an input settings to an existing project, which is in addition to our content packs. Now it does have a few cons. It has to be installed into your engine directory. It is version specific and is not created automatically. It does require some knowledge of the command line. Now you may not have used feature packs before, but they're pretty easy to use. If we go to our Add New button at the top left, add feature or content pack, you'll find our Blueprint and C++ feature packs. And you can easily select one of these, add to project, and it's going to add all the appropriate information. We're going to go ahead and take a look at how we can create our custom feature pack and add it to an existing project.