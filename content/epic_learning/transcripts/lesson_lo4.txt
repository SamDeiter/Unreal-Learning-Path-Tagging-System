Now we have our basic assets sorted. We're going to set up a control system to allow the player to control the character. We're going to make use of Unreal Engine 5's new enhanced input system. Unreal Engine 5's enhanced input system uses input action assets and an input mapping context to configure our controls. The actions once created are set up inside the mapping context with addition of input modifiers to handle inversion of inputs when using WA S&D, while as dead zones for analog thumb sticks on gamepad controllers. The mapping context is then added as part of the character blueprint to enable the use of the enhanced input action events. To start with, create a new folder called input. Inside, right-click and go to input, input action. I'm going to name this IA underscore move forward CC. The IA stands for input action move forward is the action that we're going to do and CC for character course just to keep these input actions separate from the input actions already in the stack of our project. Go to open up the input action and change the value type from digital ball to access one defloat. This will change the input's behavior from being an on-off button to being a minus one to positive one float variable. I'm going to save and close that down. I'm now going to duplicate the input action twice, pressing control and D. The first one will be IA underscore move right CC. And the second one will be IA underscore turn CC. These impractions will be used for moving left and right and turning. The next thing we need to create is an input mapping context. Right-click and go to input and select input mapping context. I'm going to call this I am for input mapping underscore third person CC. And save it. Now we need to add the input actions to the input mapping context. We need to add the input actions to the mapping list to do this, click the plus next to mappings and from the drop down select the input actions we just created. Putting CC in the search box should filter the list to show the input actions we just created. I'm going to start with IA underscore move forward CC. Now we need to add the buttons that we'd like to use for this input action. By clicking the icon, we can then press the button on the keyboard and it will automatically select the chosen control. We'll W added for forwards and now we're going to add S for backwards. Because S is the negative of forwards, we need to go to the modifiers and add a modifier. From the drop down select negate and this will change the input to minus one instead of positive one. The last control I'm going to add is the gamepad left thumbstick waxes. So I'm going to add a control binding and from the drop down select gamepad and then I want left thumbstick Y axis. For any thumbstick we need to add a dead zone. So I'm going to add a modifier from the drop down select dead zone. We can expand this out and change the type from radial to axial. This is because the thumbstick is only working on one axis. So we only need the dead zone for a single axis. The input actions and control bindings are shown here. Feel free to pause this video whilst you set those up. When you're ready, continue watching and we'll create the blueprint logic for each of these input events. With the control binding setup, now we can go over to the character blueprint and setup our logic. In the character blueprint, we can navigate to event graph and below the existing notes. We're going to right click and type event possess. From this we're going to drag out of new controller and cast to player controller. From here we're going to search for enhanced and choose get enhanced input local player subsystem. Then going to drag out here and search for add mapping context. This node allows us to add our mapping context to the player controller. From the mapping context drop down we're going to select I am on score third person Ceasey. Next we're going to create the logic for our input actions for movement. We're going to right click and look for move forward Ceasey. The input action event for move forward is going to call an add movement input node. The add movement input node requires a world direction and a scale value. For the scale value we're going to use our control value. For the world direction we're going to make use of the control rotation of the character. So I'm going to right click and type get control rotation. From this we're going to break the rotate. And this is because we only want to use the your parameter. I'm going to drag out of your and type make rotate it. Now this will connect the your to the role so we're going to disconnect that way in holding alt and clicking and then reconnect the your input. From here we're going to drag out and we want to get forward vector. Our forward vector is then going to be connected to the world direction of our add movement input node. And then create the move right Ceasey event node. And duplicate the add movement input with control D and connect it to the trigger pin. Then going to drag out of our make rotate and get right vector. This is going to be the world direction for our move right and we're going to connect the scale value to our control action value. The last event we need to create is for our turning so I'm going to right click and type turn Ceasey and select IA on the score turn Ceasey. And from here I'm going to drag out and type add controller your input. And connect the file to the action value. And now we should be done so I can pile and save and go back to am I press play and now when I press any of the inputs the character will move around. Moving the mouse will spin the character and we can do these all at the same time. With the control setup we can now move our character around at this point it's purely functional but we will be adding the visual aspects such as animations a little later on in the course. Next up we're going to return to our camera and look at some options for alternative third person cameras and how to implement them.