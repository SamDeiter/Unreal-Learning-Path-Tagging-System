In this module, we'll take a look at two more classes, Editor Utility Library, and Editor Actor Subsystem. Editor Utility Library gives us access to functionality associated with the content browser, and Editor Actor Subsystem provides functionality associated with the Outliner. Let's start by taking a look at the documentation for Editor Utility Library. The method that we'll be looking for here is get selected assets. So we can begin writing a little bit of code. I create a new function. We'll call this one get selection content browser. I create an instance of my Editor Utility Library. Let's go ahead and grab the name. I'm going to need to create a list to hold my selection. Let's get the method name get selected assets. Then we can print out each selected asset. We'll go ahead and save. I'm going to copy my function name. We can reload. We'll call this function and run it. We can see as nothing is printed. The reason for that is we don't have anything selected. I've navigated into my meshes folder. It'll be northwood meshes, structures. We'll just grab a few of these. We'll head back over. Run that script again. We'll see how we get a little bit of print out about each object that we have selected. We'll look at ways to get more information out of this soon. But for now, let's look at Editor Actor Subsystem. There are a lot of Actor-oriented methods in here. You can duplicate Actors, destroy Actors. You can clear any selected Actors. The one that we're going to be looking at initially is going to be get all level Actors. We'll have a bit of a peculiarity though about this class, the Editor Actor Subsystem. We'll talk about that here in a moment. First, I'm going to go ahead and create a little function. We'll copy the name of this class. We'll create a list to hold our selection. We'll create a list to hold our selection. We'll use get all level Actors. We'll take in the previous function. Save it. Copy the function name. We'll reload. When I run this, I'm going to get an error. The error is get all level Actors, which is our method name. This object needs an argument. If you look at the documentation, it clearly does not need an argument. There are two ways of going about solving this. The easiest one is you treat this Editor-Dactor Subsystem like a function itself. We'll go ahead and save it. Clear the log. You can reload. Call that function again. You can see here are all of the Actors. It's just a little bit of an unusual extra thing that's required. For a lot of the other classes, it's not necessary to do this. If you do call a method on a class, and you get an error that says that your method needs an argument, even though the documentation indicates that no argument is necessary, just go ahead and call the Editor-Dactor Subsystem as you would a function. The other way to access the functionality here inside Editor-Dactor Subsystem and I suspect other Subsystem type classes is to use get Editor-Subsystem. This is just another Unreal class. I'm going to go ahead and copy the name. If we feed Unreal Editor-Actor Subsystem, without the parentheses, into get Editor-Subsystem, then it will also work as we expect. We can reload. Call the function again. Here is our printout. Again, these two are functionally identical, and you can use either one of them, whichever one you prefer. I'll stick with this one because it's just a little bit shorter. The next thing I want to look at here in Editor-Dactor Subsystem is get selected level Actors. So create a new function. And reuse this line here. Just make sure we've got the spelling on this correct. Then we'll print each selected actor. Go ahead and save it. Copy the function name. Clear the output. We'll reload. And I'll just select a few Actors randomly here. And you can see we are getting data on the current selection of Actors in the world. So now that we've talked about how to access selections in the content browser and the Outliner, I want to bring something to your attention that could be useful going forward. What I've done here is a simple Google search for Unreal Python Outliner. And the first class that pops up here is the Editor-level library. If I open this in a new tab, what you can see is it's going to do a lot of the common functionalities associated with the world editor, which is the old name for the Outliner. It's been renamed in 5.0. And if you look down, you will see a lot of the same methods. In fact, they may all be the same methods that we saw in editor to actor subsystem. In particular, here is get all level Actors and get selected level Actors will be down a little bit further. So this is the documentation for 4.26, which is happen to be the one that popped up. If I set this to 5.0, we scroll down a little bit further. What we're going to see is the get all level Actors in the documentation. It has been deprecated and get selected level Actors has been deprecated as well. In fact, almost everything in here has been deprecated. So what does that mean? That means it still works, but they're phasing it out. So you may see a warning if you're dealing with a class here that says that so and so method has been deprecated. So what you might want to do in that case is go ahead and highlight the name of the method that you're interested in. And then if you just go to the regular Python API documentation, make sure you're looking at 5.0 and then just type the method name into your quick search. And it will show you what classes have methods that have similar names. So you can see here editor actor subsystem is the top list and then editor level library is the is the second one down. So this does still work, but again, to future proof of code, you're going to want to make sure that you're using the latest version of the class. So that is editor, subtract just a thing to bring to your attention as you move forward with this. It's a good idea to make sure that you're dealing with the most recent documentation Google not always select for for that. So just keep your eye out. And if you see any warnings about a method being deprecated, this is the process to go through to find a resolution for that. Getting user selections is a routine part of using the unrope Python API, especially with tools work in this module we covered two classes editor utility library and editor actor subsystem, which gives us the ability to gather user selections from the content browser and outline irrespectively. And the next module will look at how to organize assets by class. You