In this video we'll add some depth to our gameplay by giving our player character a health value that can be modified by the existing damage system. Let's start by setting up the player character. Mavigate to content 2D size crawler BP, Blueprints and open my 2D player character. Locate the event any damage in the event graph. Note that the moment if the player character takes any damage we just instantly respond them using the simple respawn function. Because we've already configured our projectile to use the damage system it's pretty easy to make the damage received adjust to health value and then decide if the player character needs to be respawned or not. To do this we'll need a few variables to store and manipulate the characters health. So let's create those now. Add 2 new float variables, one called current HP and one called max HP. Place both of them in a damage category by either selecting damage from the category drop down in the details panel or left click dragging them into the damage section within the mypleaprint tab. Next, compile the blueprint so that we can set default values for these variables. We can leave current HP set to zero but we'll need to set max HP to 100. Let's start by initialising the player character with full health when we start the game. Create a new function called initialise HP. Control left click drag a reference to max HP into the function event graph. Next alt left click drag current HP into the graph to create a set note. Select the execution pin from the initialise HP function into the set note. Now connect the output from the max HP node into the current HP input of the set note. Now when this function is called the player character will be set to the maximum health value. We set this up now so we can expand it in the future easily. Let's return to the main graph and call this function. Locate event begin play within the graph. Follow the existing execution path until the end and find bind event to on finished playing. Drag from the execution pin following this and locate the initialise HP function we just created. With that we'll make sure the player character always starts with full health. Now that we have set up the initial pool of health it's time to start taking away from that value as the player character receives damage. Locate event any damage within the graph. Select and delete the connected respawn node as we'll be handling things a little differently from now on. Let's begin by filtering out any trigger events with a damage value of 0. We might want to do something with those values but we certainly don't want to process any of our additional health and damage related logic. Select a branch by pressing B and clicking nearby on the graph. Connect the execution pin from any damage event and connect it to the branch we just created. Now we'll check if the damage value is greater than 0 and use that as the condition for our branch. Drag from the damage output of the event and locate the float greater than float node. By default the bottom value is set to 0 so we can move on and connect the return value from this node into the branch we just created. Now if we receive any damage with a damage value of 0 this will go to the false branch and we'll leave this blank for now meaning that no further processing will take place. For now let's focus on the true execution path. That's when the damage value received is more than 0. To calculate our new health value we can just subtract the damage value from the current HP value. Left click drag current HP into the graph. Drag from the output of this variable and locate float minus float. For the lower value of this node we want to use the damage output of the event again. To try and keep things tidy I suggest adding a re-root node as part of the damage output then you can drag from this re-root node into our function. It's worth taking the time to reorder the nodes to keep everything readable. Now alt left click drag another reference to current HP in order to get a set node and connect the execution wire to the true path of the branch. Take the output of our float minus float node to the current HP input of the set node. With that done we need to check that the player character still has some health left after taking the damage. Drag from the output of the current HP set node and locate the float greater than float node. We can leave the lower value of this as 0 as we want to make sure that we have at least some health left. Create a new branch and connect it to the output of the float greater than float node. Make sure to connect the execution from the set node into the branch. Let's handle the most straightforward case first. The player character currently has zero health. That will be the false path of this branch. Drag from the execution pin of the false output of the branch and locate respawn so that the player will be reset just as they were previously when they received any damage. With that we are done working on the blueprint so we can compile, save and return to the main editor window. Let's quickly start a plain editor session and observe the results. And there we go. If we stand in front the enemy for a while they hit us enough times and then we reset as before. However, I think we can agree this has room for improvement. There's absolutely no feedback that we've received any damage so we should work on that next. But for now let's finish the player editor session. Let's reopen a My2D player character and return to the any damage event. To let the player know that they've received some damage we'll knock them back a little bit to show that they've been hit. We'll do this with a new function. Create a new function called process knockback. In order to make sure we apply the knockback in the correct direction we'll need a reference to whichever actor cause the damage. Add an input to the function and call it damageer. Make this input of the actor type. When we're working with actors it's a good idea to make sure we have a valid actor before doing any other work. If we don't have a valid actor we shouldn't attempt to process the knockback. So let's start right away with a validity check. Drag from the damageer input and locate the is valid function with the question mark as we want to use this to drive execution. Connect the execution pin from the function into the is valid node. If this actor reference isn't valid we don't want to do anything. But if it is valid we want to launch the player character in a specific direction. Click from the is valid execution pin and locate launch character. The bulk of this function will be spent calculating this launch velocity. But before we move on make sure to check xy override and z override. If we hover over these inputs we can see what they actually do. Checking the box means that our calculated values will replace the velocity values of the target character rather than be added to what's already there. We don't want the knockback velocity to vary depending upon what the player is currently doing. That would feel really strange. With that taken care of let's move on to calculating the launch velocity. Move the launch character node over to the right hand side to give ourselves some space to work. Calculating this velocity uses some similar vector mass to what we've used before to determine facing directions. We'll start by getting the location of the damageer. Drag from the damage input of the function and locate get actor location. Drag from the return value of this node and locate vector minus vector. As you've probably guessed we'll also need the location of the player character for this lower value. Click on an empty space on the graph and locate get actor location. Note that in this case the target is set to self. Connect the return value from this node to the lower input of the vector minus vector node. We only need to know the x value of this vector to decide on the knockback direction. So drag from the output of the vector minus vector node and locate break vector. Drag from the x output of the break vector node and locate sine float. Now we're almost ready to start applying our knockback. Create a new vector variable called knockback direction and place it in the damage category. Compile the blueprint so that we can set the default value. Set the default value to minus 0.50 and 0.6 for x, y and z respectively. Drag a reference to this vector into the graph somewhere near where we place the sine float node. We'll work with the x axis first so drag from the knockback direction reference and locate break vector. Connect the x value of the break vector and locate float multiplied by float. Connect the output of the sine float to the lower value and with that the vector will now be applied in the correct direction but we still need to apply the other dimensions. Let's introduce the z axis. Drag from the output of the float multiplied by float node and locate make vector. We can leave the y as 0 as it's not applicable for our game but we still need to supply the z. Copy and paste the knockback direction and break vector nodes and connect the z axis output to the z axis input of the make vector. And with that the vector created by this new node has the correct direction relative to the damaging actor. Now all we need to do is add the appropriate amount of force. Drag from the return value of the make vector node and locate the vector multiplied by float node. The amount of force applied is something we're likely to tweak as we go along so let's make it into a variable. Drag from the float input of the node and select promote to variable. In this variable, damage knockback force and place it into the damage category. Compile the blueprint so that we can set a default value. Set the default value to be 700. Finally, we're almost there. The return vector from this node is now our launch velocity. So locate our launch character node, position it nearby and connect the result of this node into the launch velocity input. And with that, this function is finished so we can return to the main event graph. Locate the branch we were working on previously and drag from the true execution pin and locate the process knockback function we've just been working on. Don't forget that we need to supply a reference to the damageer which fortunately the any damage event provides. Locate the damage cause output on the event and connect it to the damageer input on the process knockback function. It's worth adding some re-root nodes at this stage to attempt to minimize the wire crossover. And with that, we're done with my 2D player character. We can compile, save and return to the main editor window. Let's start a plain editor session and observe the results. There we go. It's pretty clear now when the enemy is hitting us. We have a good idea of when we're taking damage and we can see that the knockback moves us in the opposite direction to where the projectile came from. Perfect. In this video, we added a basic health system to our player character. We began by setting up the required variables to manage the player character's current and maximum health value and added a function to initialize the health value as part of the begin play event. We then moved on to modify the current health value whenever the player character receives damage from any damage event, replacing our existing logic with something more flexible. We made sure that the player character will die and be reset when they reach zero health. Finally, we added some visual feedback that the player character is receiving damage by adding a knockback effect. We used the damage source and the player character's location to calculate the direction the player character should be knocked back using a new function. This function is called any time the player character receives damage with a damage value above zero. Finally, we observed the results in a play in editor session.