Next up, let's do the grass. I'm going to create a closer TV material. I'm still open. Let's create a new material for grass. M underscore grass. Same thing there. Let's open it up. Drag it into the view and let's find a texture for it. In the same place, material slash content slash textures. It's called T underscore field grass underscore zero one underscore D. Drag it into the material here. And we're going to be using opacity mask. Opacity mask will become available if we set not shading model, but this in this case blend mode from opaque to most. See now we have opacity mask. Everything else remains the same. We have opacity mask and we connect it to the alpha of the texture. There's the alpha output. And I'll put it to a box here. It's easy to see, but you can see it now. See the outlines of grass, but the grass is still black. I'm also going to connect RGB to base color. We have grass as no longer black, but it's too reflective. You can see there's a reflection of light on it. So I'm going to solder very simple by just setting specular to zero with a constant. Now we have this. Now one more problem is you can see the grass is invisible from one side of the polygon. There's actually a property called two sided. And if you enable that, the material is visible from both ends. This is great for anything to do with like glass, for example, vegetation, certain magical effects or other special effects. All of those should probably be set to two sided. Okay, and then we have our basic gross material. Must is used for anything that is either entirely visible or entirely not visible. It's essentially a simplified form of translucency. Except this one doesn't support fading. It doesn't fade from one value to the next. It's either 100% there or not. That makes it ideal for, for example, chain fence. It makes it ideal for vegetation, grasses, leaves, anything like that. Rendering something in must is much more optimized than rendering it in translucent, which is the next thing we're going to do in the next section. So must is going to render way faster. Therefore if you can use must, please use must and you will have a much better performance and fuel rendering artifacts. This is the safer part to take. Let's apply this to the model as well.