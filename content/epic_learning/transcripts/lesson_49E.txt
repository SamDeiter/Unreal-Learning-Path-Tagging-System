In this module, we will create a new actor. This actor will serve as a pressure plate. And on this actor, we can assign a door in our level to open when something stands on top of it. Why don't we go ahead and make a little puzzle? We'll create a pressure plate that, and when something stands on top of that pressure plate, a door will open. So instead of making a child blueprint of the B.P. door parent, we'll create a new blueprint actor. And we'll call this B.P. pressure plate. Let's open it up. Same as before, we'll add a static mesh components. And we'll call this pressure plate. Let's set the mesh to a cube. And let's scale it down. Make it 0.1. Compile. Let's add another box collision. We don't need to rename that. It's fine. And drag that up. Make it a bit bigger. Alright, that's fine. So when we scroll down, this time, we'll allow overlap all dynamic, which generate overlap events enabled. This way, anything dynamic that we throw on top of it that has generate overlap events enabled will generate an overlap event. Compile and save. Next up, we'll create a peek in overlap and select the box again. And we'll have an end overlap just as before. Compile. When we overlap, we want to open the door. But if you try to do that, open door is not a references understand. One door is not an event in the parent class, which is the actor. So we need a reference to a door. We can do that by making a new variable. We'll call this door reference. Then in the details panel, change the variable type to a pp parent or pp door parent. And set it as an object reference. This will allow us to select a level instance of a door and set that as a reference. It needs to be editable because we want to select a door in our world. Compile and save. Now drag this out, hold control so you get that reference. And now we can call open door. So whenever someone enters, open door will play. But we can't just call close door when the component and overlap stops. Let's say we push a box onto the pressure plate and we are self-stand on the pressure plate. If we walk away, the door would still close, even though the box is there, if it doesn't call the begin overlap. So we want to make sure that we do a little check. What we can do is we can grab the box and we can do get overlapping actors. We need to do a class filter, which we'll just set to actor because pretty much any actor is good enough for us. This will return an array. An array is basically just a list of all the actors that are currently overlapping. Now we don't really care about this list. We just want to know if the first result is a valid result. As if the first result is not valid, then we know that we can close the door. So we can get this way. We get the first one in the list. And now we can say, is valid. We want the second one with the question mark. Connect these two together. And if it's not valid, we will close the door. So when is not valid, we connect and we will close the door. Let's try that. Let's drag one in. Compile and save. Let's place a BPDore parent right here. But this has no way to open. Let's put a pressure plate behind these cubes so that we can shoot the cubes and they can fall onto the pressure plate. And now you can see we need to set a door reference. And we can see both the BPC overlap and the BPDore parent. If you set the reference variable to a parent, then you also get access to all the children. But we want the parent, of course. So we can do it this way. You can also select this, which allows us to pick and actor directly in the scene. One last thing we need to check on these cubes, if they have the right collision settings. So let's scroll down. They are physics actors, so that should be fine. But they don't generate overlap effects. So maybe we need to enable that if we want to shoot the blocks that fall on the pressure plate. Let's see if it works. Let's try it ourselves first. When we stand on it, it opens. And when we leave, it does not close. What would this mean? Let's exit out of there. Let's return to the pressure plate. If you want to debug something like this, you could do a print string like we did before. But you can also do right mouse button and then watch this value. So now we'll keep an eye on this. So when we press play, we'll move to the pressure plate, stand on top of it and leave. Shift F1 allows you to get your mouse and then we can go back to here. Here at the top we can select an object to debug. We need to select our pressure plate. And now we can actually see what is overlapping. So it says watching actor, BP pressure plate. So it's overlapping himself. There's no way in the get overlapping actors to exclude yourself. So the first result in this list will always be himself. That's unfortunate, but not something that's really annoying. We can just set this to get one. This way we ignore the zero result, which is always himself. And we try to check the next actor. Compile and save. We can stop watching. You can stop watching by just clicking this little loop icon. Let's try it again. We stand on top of it. The door opens. I want to get to it, but I'm too late. But... If we shoot one of these guys, the door now opens and we can walk through it. We created a new blueprint actor that went to the pond, will open a door that we can assign in our level instance of the pressure plate. In the next module, we will enable input on a child of the door and set up logic for the input expressed.