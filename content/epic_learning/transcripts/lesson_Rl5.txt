Before we can go any further building our character, we'll need an environment for them to move around in. In this video, we'll take our first step towards that goal by creating some assets to help us build the levels themselves. Let's get started by creating and configuring a tile set actor. Using the Content Browser, navigate to Content, My Content and create a new folder called Tile Sets. Okay, Unreal, Paper 2D tile set underscore master, which we placed in Content, My Content Textures. Right-click on the asset and select Sprite Actions Create tile set. This will create the new tile set asset and append tile set to the existing name. Using the sources panel, move the new asset into the tile sets folder. We've over to the tile sets folder and double-click the tile set asset to open it in the tile set editor. In the tile set editor, we have two main windows. A tile set view on the left and a single tile editor view on the right. We can select the tile in the tile set view by left clicking it and the selected tile will automatically be shown in the single tile editor view. However, when we try and move through the tiles now, we can see that the selections don't match up to the expected tiles. This is because the default settings for the tile set do not match the authored texture, so let's go through and adjust them now. The first option we'll see directly beneath the single tile editor in the tile set rollout is tile size. By default, this has been set to 32 in both X and Y, which happens to be the correct size for our tiles. So far so good, but let's look at the other options and try and figure out what the problem might be. Further down in the tile set rollout, we can see a border margin rollout. Expand this rollout by clicking on the arrow before the title. Let's bring the margin in on all sides by setting a value of 4 for the left, top, right and bottom. We can see this change represented in the tile set view. Once we change the border margin, we can see a grey box around the outside of the tiles move inwards. Now when we select the top left tile, it looks a little bit closer, but all of the subsequent tiles are still way off. This is because we haven't told Unreal Engine about the spaces between each of our tiles. Let's define that now by using the per tile spacing option. Set both the X and Y values to 8. Now we can select any of the tiles, and our selection will be perfectly in the centre of each one. If we zoom in on one of the tiles, you may have noticed that each selection actually has a small amount of padding around the outside. This padding is an intentional feature of the original texture, and the technical reasons behind including it are beyond the scope of this course. I just want you to be aware that this padding is intentional, correct, and necessary for our game to look its best. Now that all the tiles look correct visually, we still need to add collision for them to work functionally. If we tried to use them as they are right now, the player character would fall right through them. Luckily for us, the tileset editor will let us define collision on a per tile basis. From the toolbar, click the colliding tiles button. This will highlight any tiles in the tileset view that have had custom collision created for them. At the moment, we haven't created any custom collision, so none of them are highlighted. Let's change that, starting with the top left tile. Select the top left tile in the tileset view and click the Add Box button from the toolbar. When we click that button, two things will happen. A collision box is created and displayed in the single tile editor, and the tile is now highlighted by the colliding tiles option in the tileset view. By default, the collision box will cover the entire tile. However, if we look closer at the selected tile, we can see that's not actually what we want. First, we need to select which points or edges to adjust. We can click individual edges and points to move them, or left click and drag a selection around the points we want to move. We can zoom in and out with the mouse wheel to make sure our selection is perfect. If we can move around by holding the right mouse button if we need to do so. We can also adjust our grid size using the viewport controls, and I would suggest setting this to a value of 1 to make it pixel perfect. Select the lower edge of the box using your preferred method, and move it up to match the visual of the tile. Note that the highlighting in the tile set view on the left does not represent the shape of the collision. It only indicates that the tile has collision. This can often be confusing if you start by making simple boxes that will all be the same size. Now, we can use this simple method to deal with all the box shape tiles, but what about some of the more complicated shapes? Not all tiles are simple squares, and for those tiles we will want to use a polygonal shape. Select one of the angled tiles from the set using the tile set view. Next, select add polygon from the toolbar. Now, when we move the cursor over the single tile editor window, we can see a small plus icon, indicating that a left click will add points to the polygon. Go ahead and place a few points in a clockwise direction to define the outline of this shape. We don't need to worry about getting it perfect at this stage as we can adjust it later, and add or remove points as necessary. When we reach the last point, double click to finish the polygon. Also take note that in the top left of this viewport, we get text reminders of the actions we can perform. Now, let's refine the polygon we've just created. We can add more points by holding down shift and left clicking along an edge. We can also remove points by selecting them directly and pressing the delete key. When it comes to adjusting the points we've created, we may end up in a situation where two points just refuse to line up properly. This is a result of drawing out the polygon without snapping to the grid. If this happens to you, simply hit the snap to pixel grid option and I'll fix it for you. With our polygon configured, we're done with this tile. All that's left to do is repeat the process for each and every tile that needs collision, using either the add box or add polygon option. Note that some of these tiles, such as the railings here, do not need any collision as they are intended to be visual decoration only. Once all tiles are complete, we can save the tile set by pressing the save button in the toolbar and closing the tile set editor. In this video, we took our first step towards creating a tile-based 2D environment. We created a tile set from our previously imported assets using the paper 2D tools. We then finished up by defining the collision volumes for each tile so that the player character will be able to stand on the tiles in upcoming modules.