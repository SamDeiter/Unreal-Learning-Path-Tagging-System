In this module, we'll think about the best occasion to track score and we'll communicate the score between our different actors. Let's start by making the reload bar a little bit bigger because I find it hard to see when I'm playing. Track it down a little bit, compile and save. That should be fine. So now we need to think about our full accuracy multiplier. What do I mean by this? I mean, as long as we keep hitting targets, we should get more and more points. To see I hit one target, I get two points for the next target and I hit, if I hit the second target as well, I get three points for the next target and I hit. But as soon as we miss a shot, that multiplier will be reset to zero. When I think about this, the best way, the best place to track this would be in our weapon in our soul drive or shotgun. So we should probably communicate the points through our damage interface. Leave damage, input points, integer. So now we're sending points to the target. But the target as soon as it gets hit will be destroyed. So where do we store these points? For me, the logical next location would be the target spawner. Of course, we could create a new actor or do something like this in the game mode, but I also think it's okay for us to store it in the target spawner since that has a reference to the targets anyway and we can set up all the logic localized in here, which is something that will keep us much logic focused in one location. If you can, we could use an event dispatcher that we could bind to the target that once is destroyed, we check it score. But we can also do it a little bit different. You can add a custom event and we'll call it Update Match Score. And we'll give it input integer points. We will add it to another integer, which is the match score. We will set that after we edit them together. Now we need to send that message from a target before it is destroyed to the target spawner. Create a new variable and we'll call it spawner reference. And we will set that reference as the target spawner object reference because it's an object in our world instance editable so we can also enable expose on spawn. So the moment it spawned, we can set this variable. Set that reference and then update match score. How many points? Well we communicated that through our damage interface. Go back to our target spawner, spawn actor and here you can see spawner reference. If you do not see spawner reference, then click right mouse button on here and press refresh notes. It will refresh it and then it will show up for sure. This is a reference to self. Now let's have a look. We'll add a print string here. Just so we know what our score is. I'm about to save. Let's go to our weapons. In the assault rifle, we can have a look. Points. How many points should it add? How that depends. You can promote this to a variable. Say points. And we want to give one more point than what's given before. So we'll do plus and a set. Reconnect this instead of this. So now every time we hit the target that implements the PPI damage interface, we will increase the points by one and we will then send those points. If we don't hit them, we can set the points to zero. We don't hit an actor that implements the interface. We can reset our points to zero and then the next target that we hit will be set to one by default. For a shotgun, this becomes a little bit more complicated. Open up the shotgun and here we go. We fire eight different traces. So what do we need to do now? Well, we just need to check if one of those hits a target. So we need to promote this to a variable as well called points. Yes. And we need to create a new variable. And we'll call this it relevant actor. If we send damage, then we'll set this to true. And before our for loop, we will set it to false. So by default, it's false. So say all of these bullets fly somewhere and none of them hit anything. And at the end, after completed, that value will be false. If it's true, we will increase score with one. So factor plus factor. You can also use increment integer. This will add one number to the points integer. It's the same as how we did it here. It's just a little bit less code, a little bit cleaner. And another thing to keep in mind here is when we don't hit one, we should set it to one and not zero because we add points after we are done shooting instead of before like we do on the assault rifle. But did like this, you could get pretty crazy scores if you never miss. So you could opt in to add a clamp. We can do that here in the BP target. So we clamp the value. It's either one and a maximum of five. If you have more than five, then it's just CNES 5. The maximum value you could possibly get is five. Let's go ahead and test press play. We have a weapon interact. Two, four, seven. My aim is gone eight, ten, thirteen. I missed. So 14 and I'm going to add now 16, that's plus two, 19, that's plus three. In the next module, we'll place this information on the screen using UMG and we'll have a look at the game instance.