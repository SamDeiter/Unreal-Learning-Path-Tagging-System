With our hands now in the Motion Controller Blueprint, let's create a simple animation blueprint to control their pose based on input from the motion controllers. We'll also modify and create animation assets, then create a BlendSpace 1D to connect it altogether. Open the animation, Manic and Hand, right open. I renamed it to VR Hand, underscore Open, and made a duplicate of it named VR Hand Trigger. Let's revise the VR Hand Trigger. In the Scales and Tree, select the individual bone you want to rotate. I wanted to appear as if we were pointing to the map when we click, so I rotated the fingers until I was happy with the point. If your animation is playing, be sure to pause it from the bottom of the toolbar and slide it to frame zero. Select any bone and add a key, then press Apply to save. We now have our second animation. Create a new BlendSpace 1D based on Manic and Hand, right skeleton. Name it to VR Hand, underscore BS. We'll be working with a BlendSpace 1D and not a BlendSpace asset because there's only one dimension influencing the blend and that would be our Trigger Access event. Open the BlendSpace and let's set it up. In the Horizontal Access Settings, set name to Trigger. Add our VR Hand Open to the very left above zero in the BlendSpace and VR Hand Trigger to the very right. This says that if the value of Trigger is zero, then play the animation VR Hand Open. If it is one, play VR Hand Trigger. You can hold down Shift and move around the BlendSpace with your mouse to see the Blend in action. Create a new animation blueprint based on Manic and Hand, right skeleton. Name it VR Hand, and add a key. Drag VR Hand BS into the Anum Graph. You can see that it has one input, Trigger, and one output of an animation. Plug the output into the result input of the final animation pose function. Create a new float variable named TriggerPressed and plug that into the Trigger input and compile. You can see that as we adjust the value from zero to one, our hand is again blending between our two animations. Open BP VR Hand and create a new custom event called Update Anum BP with a float input named Access Input. Drag in a reference to the Skeletal Mesh and get the Anum instance. From Anum Instance, cast to our new VR Hand Anum BP. And as VR Hand Anum BP, set the new TriggerPressed variable from Access Input. Be sure to set VR Hand Anum BP as the animation blueprint in the Skeletal Mesh. Inside our BPC VR PAN, create a new function named Send Access Input to VR hands with a float input named Access Input and a booleanput named Right. Drag in both the left hand and right hand variables, and from them, call our new Update Anum BP event. Connect the two Access Inputs and drag from Target and select the target from our right boolean. Find those Access-based events we made in a previous video. Remember that we have that check built in so that if the value is less than a certain amount, no calls will be made. But if it is above or equal to that amount, you will call our function Send Access Input to VR Hand and send over the Access Input. Make right false for the left hand and true for the right hand. Alright, let's test. You can see as I press the trigger, they are blending between our two animations. So we can spawn the map, interact with it, teleport to your locations, and preview new areas now with hands. You learned how to modify and create new animation assets for use in a Blend Space and control the 3D hands using an animation blueprint. In these last couple of modules, we went that extra mile to really show off the showcase. We have completed our navigation project and the last step is to package as a runnable.exe file. Although this is not a packaging course, there are a few tips I want to give you about packaging a VR project.