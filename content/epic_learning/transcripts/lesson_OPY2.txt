Before we bring in any character in Unreal to create a custom control rig, we need to make sure that the character's skeletal mesh is compatible with the engine. First thing to look for is the file format. To import a skeletal mesh character into Unreal Engine, the file format type should be an FBX. While importing your character from a preferred DCC application, make sure to export it in an FBX format just like I did here. After for one is to one scaling, try to use centimeters as world units in your DCC application, as Unreal's default measurement unit is in centimeters. Next thing is to check if your character joint hierarchy setup is compatible with Unreal Engine. For this, you need to make sure that all the joints in your character have a single parent joint, usually named as root. Under the root joint, you can create a preferred structure based on your requirements. Just make sure to use leaf bones while setting up the twist joints for your limbs, where they are not parented to the next branch in the skeleton hierarchy. Instead of a joint, root can also be an empty group, but that's not a best practice. Now you'll find this kind of joint setup across all kinds of characters that you see in Unreal. Beat creatures are a generic humanoid biped character like the mannequin. Finally, make sure to avoid using any special nodes or functions that are specific to the DCC application you are using. These might not translate into Unreal as intended, and you might face some issues while importing a character in Unreal. Now that we know what are the basic requirements while exporting a character's character mesh that is compatible with Unreal, let's move on to the next module and import the FBX character.