We have built several systems for tracking and determining intent with our head, our eyes, our blinks, and our hands. In this module, we're going to prepare our pawn to receive direct contextual input. HoloLens 2, while tracking our hands, can also identify gestures and tell Unreal Engine when the user has performed them so specific functions can be called. Let's get started in our BP pawn. Create a custom event named Configure gestures. Call this event at the end of Prepare4AR. Now add the appropriately named node Configure gesture. This node will modify our input config file to listen for these new inputs. Tag backwards from gesture config and select makexr gesture config. This structure is asking us which gestures to listen for and communicate to the engine. Tap is the familiar method we've been using to interact with our actors and hold is simply holding the tap for a bit of time. These two inputs tap and hold are action inputs. They will act like a keyboard press in that it is either pressed or not pressed, giving one call when that value changes. We're just going to need to configure the hold gesture. Access gestures are, as they are named, axis inputs. They too will only provide on or off as in 0.0 or 1.0, but they will constantly provide this value. We can use this constant stream for many purposes. For example, if you wanted to have an actor perform an action only while we are gesturing to it, such as moving or rotating or animating, etc. we can use the axis value and axis event as a call providing that float as a return. We won't use the axis gestures now, so we can keep it set to none. Open the project settings and scroll down to input. Add a new action mapping and name it interact with long. Add two new inputs to the interact with long. First, Windows spatial input left hold gesture and Windows spatial input right hold gesture. The reason we are doing both instead of the basic hold gesture that alone uses both hands is because we will need to find out which hand did the gesture. Return to BP-PON and add this input event to our graph, interact with long. What we want to accomplish with interact with long is to communicate with blueprints that are using our blueprint interface. A new function will be called to tell this blueprint to do something. Let's add that function to our interface. It is located in the core folder within the blueprint folder. Open BPI HoloLens. Create a new function named interact with object and give it one input named right hand and set it to a boole. We are using our hands to interact with the world using components from the MRTK. These components were built from source C++ files and don't have all the pieces revealed to us in blueprints. So we need to collect the information we need ourselves. Pan over to where we spawned the UXT hand interaction actors. The component we want from it is the FAR cursor which is a little white disk at the end of the beam coming from our hands. If I drag from return value and search for UXT FAR cursor, I received no results. But having used the UXT hand interaction in our scene, we know that it contains one. Because we have access to the module class, UXT FAR cursor component, we can search for this actor and if it does contain it, we can cache a reference to it. So we'll need to do this with both of the hands. And this is another benefit of having the MRTK being modulately designed. From the left return value, create a Git component by class node and set the class to UXT FAR cursor component. From the return value, create an Is valid node. Then promote to a variable named left hand cursor. Alright now repeat the process for the right hand and name that variable as right hand cursor. Now we have direct access to the cursors that we have been using to interact with our scene. Let's finish up this input. Create a new sphere collision component and name it interaction overlap. Set its radius to 1. The reason we will set the radius so low is so we can interact with a single actor. If it was a large radius, it would interact with everything that's being overlapped. Create a new custom event named check overlaps from hand. Give it a vector input named hit location and another boolean put named right hand. From our interact with long event, when pressed is called call our new check overlap from hand event. We need to provide it with the hit location and the right hand inputs. Drag in a reference of both left and right hand cursor. From one of them, get the world location and plug it into hit location. Detach the reference on get world location and instead create a select node. Plug left hand cursor into option 0 and right hand cursor into option 1. By default, these are integer options but when a boolean connects to the wild card, they will change to false and true. Now, to determine which hand called the event, we can check if the key converted to a name and then again to a string contains the word right. We can then use this return value and plug it into the select node and the check overlaps from hand event. Let's finish that event. Drag in a reference of interaction overlap and set the world location to the hit location. From interact with overlap, create a get overlapping actors node and set the class filter to actor. This is going to give us an array of all the overlapping actors which could be anything in the scene. If you want to put a more specific class such as our BP puzzle piece, the sphere would only search for classes that adherits from BP puzzle piece. But we wanted to look for anything at overlaps and we'll add further constraints in a different way. Create a 4-Each loop from the overlapping actors and from array element, check if the actor does implement interface. Set the interface to BPI HoloLens and if true, call interact with object. Connect the right hand input to the check overlap from hand event. We need to do one more thing so we can test. Open BP puzzle piece. Set our BPI HoloLens interface and compile. Add event interact with object and from the puzzle piece component, create a set simulate physics. From right hand, create a knot node and plug that into simulate. Lastly, the UXT generic manipulator component works by modifying the root component of an actor. Because the puzzle piece is not the root, we need to specify that the puzzle piece is what we want to manipulate. Select UXT generic manipulator and open the advanced options in the details panel. Type puzzle piece into component name. Alright, let's try this. If we pick up one of these spheres with our right hand, we will communicate with it and be able to move it around as always, but when I let go, it'll just stay floating in mid-air. I'm going to just put it over here above the floor. If I use my left hand now, I know that physics was turned on and when I let go, gravity will take over and the puzzle piece will fall. I can use my right hand to toggle it off if I wanted. Because we specified that we want to manipulate the puzzle piece, we can still grab it with our left hand, but to turn off physics, we must use our right. In this module, we prepared our pawn to configure gesture inputs. We modified the project settings and created a new input that will be called by either hand. We accessed the hands far cursor by searching inside of a source file. Then we created new custom events that called a function within our blueprint interface to interact with another blueprint. We also prepared our UXT generic manipulator to work on a specific component. In the next module, we will begin preparing for our hand problem map by expanding the scene and using more of the components from MRTK.