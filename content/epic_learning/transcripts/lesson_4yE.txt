you properties away of exposing C++ variables to be accessed and set in blueprint and in your default blueprint values. So let's have a look at the use case that we're going to be exploring. We want to migrate across some variables that are currently in blueprint only and we want to make them accessible to C++. So C++ needs to know about these particularly I'm looking at the blueprint grabber. So if we open up our blueprint grabber BP grabber then what we will see is that there are some variables over in the my blueprint tab one of which is the max grab distance and as an experiment we're going to be migrating this across into C++ but still having all the functionality that hangs off it working in blueprint which you can do because blueprint is able to access C++ but not really vice versa not so easily anyway. And we want to figure out how we can set its default value in blueprints and how we can access it because if I right click on this and find references you'll see that there are references using this in blueprint we want to make sure that we can carry on using it in blueprint. So how do we do this? Let's go over to C++ base class for the grabber.h. At the bottom of this file we're going to add a new protected section to the C++ header and in this protected section I'm just going to be writing out the variables so I'm having it as a separate section I like to have it below my methods that's just an organizational thing you can put it wherever you like in your C++ class it's not actually necessary that it's there and so we want to put a variable in here we know what the name is going to be it's going to be max grab distance but what's the type going to be if we don't have a look in blueprint you can see that the type if I'm just hovering over it is float but also here in the details pane where it is variable type it's float there as well so we can use the float variable that corresponds in C++ to give it a type and we can also give it a default value the default value as it currently stands in blueprint is 100 so without any override from blueprint that is what it's going to be but this alone is not enough because the blueprint system and the introspection system does not know about this variable to do that we need to use a pre-processor directive called you property and you property we can use to annotate any variable in C++ so that the header tool can has it and understand that there's variable has some properties about it it can use it to do serialization it can use it to do garbage collection and most importantly it can expose it to blueprint that's why we're using it today but to expose it to blueprint it's not enough just to put you property we also have to put some access modifiers on it and there are a bunch of different access modifiers depending on what you want to give access to this is the definitive list of them for you it depends on whether we want read only access or read right access first and foremost and then also whether we want to have this access in the defaults the instance or the blueprint out let me just dive into that and unpack it a little bit for you when we say in blueprint we mean in the blueprint graph so in order to access and read max grab distance here in this blueprint function in this graph we need to have read access at least to blueprint so we're at least talking about having blueprint read only as one of our access modifiers however we also probably want to give the designers the ability to override the default max grab distance in the class defaults the class defaults you can see a list of all the class defaults by going to the toolbar class defaults then in the details pane you will see the list of variables and their defaults and these can be overrides from what the defaults were in C++ so for example the designer could set the max grab distance here to 200 and that would override what was in C++ so that's what we mean by defaults in this table and finally instance is when you actually put this blueprint in the world anything in the world outliner is an instance so if I select one of these actors you can see in their details pane again they have this list of properties and their values and if you override anything here this is the instance so you can choose that you want to make it editable in default only or in the instance only or visible in the instance only or visible in default only or you can have it visible anywhere or edit anywhere so in our case we do want to allow the designer to edit it both in the instance and in the defaults and that's quite a common thing to want to do so we can have it as edit anywhere if on the other hand we wanted this to be an entirely read only property then we would probably use visible anywhere but that is quite common and then we want to be able to read this from blueprint because at the moment we won't be able to access this from a blueprint graph so for it to be readable from blueprint we want blueprint read only because I don't actually want to give access at runtime to setting this variable only in the defaults and in the instances in a particular map now with that written we can go back to Unreal and hit compile to compile as C++ make sure it's within the main map view and not within the blueprints because this will only compile blueprints and when the compilation is complete if we go back to our BP grabber we will see that we have a compilation warning but if we click compile that goes away what's actually happened here is because we use the exact same name and C++ the blueprint variable has been renamed to max grab distance underscore zero just so there isn't a conflict between it and its base class C++ variable and if we go to class defaults you can see that we also have the grab distance zero set up but further down where we have a grabber section there's another max grab distance and this is the one from C++ what we're actually going to do is try and remove any usage of this blueprint variable as it is confusing things now so if we right click to find references again and you can see it's only referenced in this one location and the default hasn't been overridden in the class defaults or in any of the maps if it had been overridden in the maps it might be a little bit more difficult to go through and update all of the default values in the maps over we can quite easily just remove this node and replace it with the max grab distance from C++ the one that doesn't have a zero on the end and that should replace any usages of it so we no longer have any usages which means that we can go ahead and delete the blueprint variable so if we compile and save that we should be able to go and play I'm just going to go and hop into the village to make sure that the grabber is still working right click somewhere in the village and play from here and then I'm going to go up to a pumpkin and try and grab it so my grabber is still working hopefully meaning that the whole movement of that variable over has indeed worked so we've learnt about the U property pre-process directive and the different access modifiers that we can put in there in order to expose our C++ variables to blueprint and have them accessed there as well as overridden