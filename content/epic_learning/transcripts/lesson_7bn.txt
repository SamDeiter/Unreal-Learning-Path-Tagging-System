When the targets are part of the textures, the render target is a texture that's essentially generated by the engine or the editor. So when I look at generating a texture within the editor in runtime, when to use them, to use for cameras, to use for baking complex materials to render targets or textures, and they're used for other types of advanced effects. It's also possible to create textures within the editor or within the engine. These are often called render targets. So because most data blade materials are texture based, it is possible to actually generate some of those textures within the editor directly. There is a possibility to do some procedural texture generation or rather procedural materials. This is something shown briefly earlier when we talked about performance. We talked about the noise we had generated there. The noise was not based on the texture. It was procedurally generated in the editor. Now we said it was really expensive. That's why we often don't use much procedurally generated material content. Right, because it leads to a higher shader cost, you do reduce memory consumption. But because we usually have a lot of memory available, whereas processing power tends to be more premium, we tend to be more texture based. But again, because it's so texture based, it is possible to generate these textures in the editor. So baking a material or baking a camera view or otherwise generating a texture is called render targets. And you can use them for all kinds of different things. Let's start off with a simple one which is a picture you saw here of simply the camera itself. So over here we have a cube which has a material applied and the material uses a very simple material. It uses a texture and this is the texture it uses. It has alpha, it's going to hide the alpha. And as you can see, this is simply what the camera can see. We'll move it to a cypher second and here we have a camera. This is the camera that is capturing to this texture. If you move the camera, it also shows up there. So this is ideal to create, for example, a security camera. You could create a mirror like this as well. If you want to have a very sharp and precise mirror and the resolution of these things is set within the texture itself. Here it says size x and y. So if you set this to 2048 you obviously capture it at a higher resolution. Now it's black until I move it and updates and there we are. Now we have a sharper. This is the 2D version. You can see it also on the camera. It's capture scene capture 2D. In fact you can look for it in the left list over here. If you type here scene capture you see a 2D one and a cube one. The cube one is the same but it captures to a cube map rather than a 2D texture. But the idea is the same. These can be very perform and heavy, especially if you have them update in real time. If you have them continuously captured to this texture it's going to get very heavy. So be careful with that. Because you're essentially simply re-rendering the entire view again. So that's the simplest use and the first use. And another thing you can do with render targets is as a way of simplifying some kind of procedurally generated pattern or such. So for example here we have this noise example again. It's a different version of it. But we have noise and it generates this kind of pattern we see over here. But again it's fairly expensive. In fact if you look at the bottom you can see that this simple little setup here is already 526 instructions which is a bit much for what we get back for it. So it would be better to have this as a texture than to use noise in material. And we can use a render target for that too. In fact we have an example big render target here. And you can just make these render targets by the way with the right click menu, materials and textures. And here you see cube render target, render target. And I believe canvas render targets are not a version of it. But you can use those to bake either the camera to it or to bake the material to it. So we've made one of them. That's this one. It's black right now. And we're going to write the material to the render target. In fact we made a tiny little blueprint here. That allows us to do exactly that. So there's nothing going on here almost. It's literally just this. What it does is that when this functional this event runs and it's going to draw the material to the render target and it's going to draw this material to this render target. And this thing represents the material. We can, sorry, the texture. You can see it here. It has the texture here. That's the property defining the texture. And this is defining the material. This has the property that holds the material. That's all it does. And then when we have that and we simply double click that we get this in interface view. So we don't see the blueprint behind it. We simply see a little menu here. This is the function we had the event we had. So it's now a button and these are the two variables we had. So we can say, okay, you know what? This is the render target. We filled it in there and this is the material. And I'm going to say bake material to render target. And what it does is it updates this with the view that this one had. So now we have it as a texture. You see this is that thing as a texture and we can then right click it and we can do create static texture which creates a regular texture from it because this is the render target texture. It's not a normal texture. You can see the color here is different than a regular texture. This is a regular texture. And this is the baked result of create static texture. We'll have given you this. Again, that's alpha. And then we have it in texture format. And in fact, then we could go back to the material. We get the difference set of having that. I might be slightly different here because it might change the tidings slightly, but still it's 526 instructions. And as I do this, it's now 31 instructions. So the huge difference. You can see a slightly different look. I changed the effects since I made this texture. Now, once you have this kind of system built in, you can even go another step further because if you could generate textures in the editor or in game or in during runtime, then you could be writing all kinds of things to it. If you switch to the content examples project that you can download from the Unreal Engine Launcher, there's one particular level called Blueprint Render to target. Blueprint Render to target is a couple of examples of the same system where you can create textures using render targets. And here's an applied form of that, water, for example. So I'm going to play the game here for a second. And then we're going to go to water. And if I click and drag, it's going to create a wave right click. So this entire effect where you can just basically create ripples in the water is done using render targets as well. It's the same idea because we have a texture that we've write to. We basically paint on a texture in real time and then we use that texture in a material setup that causes displacement of the water surface and rippling in surface. Look at how this was done. And it's fairly complicated, so this is definitely an advanced applied form of it. I'm trying to select the water surface. The water surface comes from a Blueprint. The Blueprint simply holds the surface and that's it. And it has a whole number of functionality in here. But essentially this functionality checks where is the crosshair, where is where are you clicking from and convert that position to generating a ring with a couple of additional parameters and then rendering that ring. That's what we do over here. To render target. And then that render target is the one that eventually holds the information. You can see this is what it would make. In real time and then that in turn would be used by the material, which is fairly simple material, really. But it uses these two special textures. Both of these are generated, two different versions of the same thing. They're generated in real time. Another practical use of this would, for example, be snow. So anytime you see a game or real time application where you have snow and you see a character walking through the snow and it's as if they leave behind the trail in the snow, where the snow got compressed and pushed aside or pushed down or so, that's the render target as well. So any of those kind of usage cases are rendered targets. So I just thought about what render targets are and the fact that they are generating a texture within the editor runtime as a render target. It is used for cameras, it's used for banking complex materials and it's used for advanced effects such as the rippling of water in the set of you have seen.