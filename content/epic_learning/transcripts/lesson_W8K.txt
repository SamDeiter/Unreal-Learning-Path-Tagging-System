In this video we'll add yet another common player action, jumping. We'll begin by adding a new input for jumping, before using the movement components to implement the mechanical functionality of the jump itself. We'll finish up by laying the groundwork for a more granular control of flip the animations to allow us to implement the multiple flip-butt jump and fall animation cycle as intended. With running taking care of, the next thing we want to do is add the ability for the player to jump. Luckily, this input has already been configured, and jumping is something already supported by the character movement component. Let's finish, I'll play in editor session, and return to the Blueprint Editor window for our character. Let's find some empty space in the event graph and begin configuring our jump. The first step is to add the jump input action into our graph. Drag-click within an empty space in the graph and type input jump and select the event. Drag from the pressed execution pin and type jump to select the jump function. Drag from the released execution pin and type stop jumping. And that's it from the mechanical side, that's all we need to do. We can confirm this by saving and compiling the Blueprint and starting a play in editor session. With the play in editor session running, we can use whichever key we have bound, in my case I'm using the default, so W or space on the keyboard. As we can see, the character jumps and land is expected, but we aren't using any of the jump animations. In fact, it's a little bit worse than that. We're playing the wrong animation of the character walking, which looks very odd. So let's move on to the more difficult part, and getting ready to handle all the different animations for the jump. Let's finish the play in editor session and return to the Blueprint editor. Adding our range of new jump animations brings up a slight problem we have. So far, our run and walk animations have been very easy to work with. They just loop forever until we change to another animation, which also loops. This means it's been perfectly fine for us just to use the set flipbook function as part of our handle animation event. However, our jump animations don't work like that. The jump animation consists of several discrete animations that do not loop and should only occur in a particular order. We have a jump animation that should play once the character leaps into the air, a transition animation that should be played as they reach the apex of the jump, and finally a fall animation as they come back down. Let's tackle these issues step by step. The first step is having more control over how and when our flipbook animations play. We'll do this by creating a function that will compare the flipbook that is currently playing to another one that has been requested to play elsewhere in our Blueprint. Let's start by creating the function itself. Click the plus icon in the function section of the my Blueprint panel. This will create and open our new function. With everything else in the project, we should aim to make the names as descriptive as possible, so let's call this update flipbook animation. The first thing we want to do is compare the flipbook that we're attempting to update with whichever one is currently playing. To do this, we will need to add an input to our function. In the Details panel, hit the plus icon next to the Inputs rollout. Using this new input, new animation, to remind us that this will contain the new flipbook animation we're trying to play. Use the drop down to set this input to the paper flipbook object reference type. Now we can see that we have access to this input when the function is called. Drag from the new animation pin we created and type exclamation mark equals to find the not equal math node. This node will now compare the flipbook pass to it from our function, with another flipbook and return true or false, depending on if the flipbooks are the same. Control left click drag the sprite component from the components panel. This gives us a reference to the sprite on the character, which we can now use to get access to the current flipbook. Drag from the sprite output pin and type get flipbook to find the get flipbook function. Now we can use the return value from this function to connect to our math node. Drag from the ball return of the function and create a branch by typing branch into the search window and selecting it. Before we forget, hook the execution pin from the function into the branch. Now let's think about what we'll use this branch for. If the two flipbook references are not equal, then it means the requested new animation does not match the one that is currently playing. This means that we need to set the new flipbook as we were doing before, but also ensure that it plays from the start. Select our sprite reference node and copy and paste it to save time. Move the node into some clear space and drag from the output and search for set flipbook. Select the true execution path of the branch to this new set flipbook node. We can also use a re-root node to stop the wires from crossing over. Now we just need to tell this set flipbook function which new flipbook we want to use, so get another reference to the new animation variable by right-clicking in the graph and typing new animation until it appears. Now we can connect this to the new flipbook input of the set flipbook function. And with that, we're almost there. We just need to make sure that the flipbook plays from a very first animation frame, and luckily for us, there's a built-in function from paper2d to do just that. Let's copy and paste another reference to the sprite component, drag from the pin and type play from start. Then this new node neatly and then connect the execution pin between set flipbook and play from start. And that's everything we need to do for this function, so before we compile and save, let's look at what it does. When we call this function, we also supply the new animation we want to play. This is a flipbook reference. We compare this new requested animation with the currently playing animation on the character. If they do not match, then we need to set a new flipbook. We set the new flipbook and we ensure that it plays from the start. Crucially, if we compare them and they are not different, we don't need to do anything. That's why the false execution pin of the branch has nothing connected to it. With that done, let's compile and save before returning to the main eventcraft tab. Now let's actually call and use this function as part of the eventcraft. Locate our handle animation event in the graph and locate the set flipbook function. This node needs to be replaced with our new function, so we don't need it anymore. Delete the function as well as its associated sprite reference. Drag from the return value of the selector node and begin typing update flipbook animation until our function appears. Select the function and add it to the graph. Because we dragged in our function from this flipbook reference, the new animation pin will automatically be connected. All we need to do now is connect the execution pin from the handle animation event to the update flipbook animation function. Before we go any further, now's the good time to check we haven't broken any existing functionality. Let's compile and save the blueprint and return to the main editor and start a play in editor session. As we can see, everything's still the same and sometimes that's good. This is a really good start towards adding more complex control of our flipbook animations, but obviously, they still work to do. We need to make further changes so that flipbook updates we request every tick do not interfere with later changes we request for your events that fire far less frequently. The obvious examples being falling and jumping. Let's end our play in editor session and return to the blueprint editor. To get started on these changes, let's disconnect the execution pin from the handle animation event. As we're going to be making substantial changes. Scroll down alt and left click on the execution pin. The first step will be to branch execution dependent upon if the character is currently walking. Troll left click drag reference to the character movement component into the graph. Drag from this reference and locate is walking. For now, let's give ourselves a little bit more room to work in. If you select the nodes we're working on, move them into an empty space in the event graph. Drag from the return value of the is walking function and create a branch. As we are about to include a mix of looping and non-looping animations, we need to explicitly state that our walking animations do loop. First, let's get a reference to the sprite component into the graph by control left click dragging the sprite component from the components pedal into the effect graph. Now drag from that component reference and find set looping and place the node. In the set looping function, let's check the box for the new looping ball to say that we do want looping to be true. Let's reposition these nodes to keep everything neat and start hooking things up. Select the execution pin from the handle animation custom event into the branch. Connect the execution pin from the true output of the branch into the set looping function. At this point, we can now reconnect to our existing handle animation blueprint logic. Let's take a few moments to reorganize things to give ourselves space to fit it in. We can also resize our comments box to keep all the nodes within it and then move the comment box to move all of the nodes to line them up neatly. While we're here, we can make a little refinement to our selector process. Instead of checking for velocity in any direction, let's instead focus on the x-axis. Remove the vector length math function and instead use a break vector function. To reposition the nodes to give ourselves a bit more space, drag from the x-output of the break vector function and create a not equal function. To default value of 0 on the bottom for comparison is just what we need. We can delete our previous math function and connect the output of this math function into the index of the selector. Now our walking animation will only be considered when we have a velocity in the x-axis. Now let's just finish up this section by handling the false path from the first branch. This branch will be executed when we are not walking. The falling animation we're using does not loop, so we want to make sure that if we're falling, we disable looping. First, let's confirm that we are actually in the falling state. Let's get another reference to the character movement component from the components panel and drag from the output to locate the is-falling function. Let's create a new branch and connect it to the false execution path of the previous branch. Connect the output from the is-falling math function to the condition input of the second branch. Finally, to save time, mark you select the sprite and set looping function above and copy-paste them. On the copied version, make sure that new looping is set to false by unchecking the box in the function. Connect this new set looping function to the true output of the previous branch. Let's just take a moment to reorganize the nodes so it's easy to read what's going on. And with that little bit of tidy up complete, let's compile and save the blue print. So we've made some substantial changes here. Let's review the changes we have made and why we've made them. The first thing we do as part of this event is use the character movement component to check if we're currently walking. If we are walking, we want to set the flipbook animation to loop, as our walking animations are designed to loop. We then use some selectors to check if the character is running, walking or stationary, and select the correct flipbook. According to the first branch, if we are not walking, we then check if we are falling. If we are falling, we set the flipbook animation looping to false, as our non-walking animations do not loop, and we will be handling the transitions ourselves soon. Note that this falling branch does not directly update any flipbooks. This path exists to prevent the update animation event from being called and overriding the event base flipbook changes we are about to implement. Remember that our handle animation event is called every frame by the tick event. If we didn't implement these branches, our event based logic would be overridden in the next frame. In this video, we added yet another new input to our game, jump. We use this new input to activate the built-in jump functionality of the character movement component. Adding this new functionality exposed to weakness in our current implementation of flipbook logic. We are unable to handle multiple non-looping flipbooks that must be played in a specific order. We finished up by beginning to address these weaknesses. We added more branches to our flipbook selection logic to check for different movement states, such as jumping and falling, and then use this information to set the looping state of various flipbooks within our character.