Unreal provides a high fidelity, physically based material and rendering system. Physically based materials provide an artist friendly real-time algorithm based upon three core material inputs. Metallic, specular, and roughness. With these three inputs, we can create virtually any material we'd ever need for creating architectural surfaces that are photorealistic. Besides metallic, specular, and roughness, though, we do have other inputs here, such as emissive, normal, world displacement, as well as world position offset for controlling vertices on an object based on the material. Then we also have ambient occlusion and pixel depth offset. For most architectural visualizations, we only have to worry about a few of these, primarily metallic, specular, and roughness. This is the main material node for our PBR-based material. If we select it and then look over in the details panel, you'll get some other options here that you can set. We're using what kind of material we're using. In this case, surface material. This is what you'll be using 99% of the time. Under Blend Mode, we're currently set to opaque, but we can also choose Masked, Translucent, Additive, Modulate, or Alpha Composite. The ones you'll be using most of the time when building architectural scenes will be opaque, Masked, or Translucent. Masked is great for things like tree leaves, and then Translucent is great for anything transparent like windows. You also have the option to make your material too sighted. Metallic's specular and roughness are the primary inputs we'll be using, so let's discuss them for a moment. Metallic means either an object is metallic or isn't metallic. You generally do not want to use a value other than 0 or 1 here, 0 meaning not metallic or 1 meaning metallic. Specular of course is the control of the specular highlights on the surface. As you can see here, it defaults at a value of 0.5. Roughness controls how rough a surface is. To rougher the surface, the more diffused the light is when it bounces off of the surface. This is great for creating glossy reflections. A value of 0 means the surface is completely smooth, as a mirror would be, for a completely matte or non-reflective surface you would set your roughness value to 1. Most of the material inputs can be driven by either constants or textures. For instance, we'll go ahead and drive our base color by a texture. The easiest way to do that is to hold down the T key on the keyboard and left click. This creates a texture sample. We'll then use the RGB alpha output at the very top of the texture sample and drive the base color input. Of course, we'll need to apply a texture to our texture sample. In the details panel, we can use the drop down to pick the texture we want. Let's set our preview output here to a cube. Let's take note of the inputs and outputs here on our texture sample. First of all, we have UV inputs and text inputs. Most of the time, you won't need to worry about these inputs. However, if you do want to use a different UV coordinate for the texture sample, we can drive it into the UV input here. To do that, simply hold down U and left click to create a texture coordinate. This will give you the access to the various UV coordinate systems of your mesh. If we go to the details panel and look here at the top, we have coordinate index currently set to 0. That is the default value. We can specify any other UV coordinate index here if we wish. We can also drive tiling here. We've got our U tiling and our V tiling as well as being able to mirror our UV coordinates here. Simply drive the output of this UV texture coordinate into the UV input of the texture sample. The very top is our red, green, blue, alpha output. And below this, we have each channel separately. So we have red, green, blue, and alpha. Let's go ahead and drive our metallic value with the same texture. So we can do that by simply pulling off of the output for our RGB alpha and then driving that into the metallic input. However, nothing has changed yet. That's because we also need to specify a roughness. For this, I'm going to use a constant. I'll pull off of the roughness input and do a search for constant and we're going to leave the value at 0. Now we can see in the preview window here that we have a nice reflective metallic cube with the unreal logo on it. Let's set the roughness value to 0.2. This should give us a much blurrier reflection. Our normal input allows us to input an RGB normal map, which works a lot like a bump map. Let's go ahead and bring a normal map texture into our editor here. We'll hit T and then left click. From the drop down, we'll browse for our normal map and then we'll pipe the RGB output into the normal input of our material. Here we can see the effect of the normal map. It does a great job of adding details to our mesh without actually increasing the geometry size. It works a lot like a bump map. However, if you have dynamic lights or the object moves through the environment, the normal map shading will change based on the location of the lighting. First of all, let's take a look at a translucent material. We have window glass here. If we select the node and look under our details panel, you can see the blend mode is set to translucent. This will make both the opacity and refraction inputs available for use. Following the refraction, wiring down to the material expressions we can see what we have is a franel node attached to a linear interpolation node. Linear interpolation simply blends between two values A and B using a third value C. You can use a texture map to drive the blending. In this case, we're using the franel expression. The values instead of being wired in are simply specified under the details panel here. So we can see we're blending between a value of 1 and about 1.24. The franel node allows us to access the exponent value and the base reflect fraction. But as these values were brought in from the V-Ray material, I'm going to leave them as they are so that we maintain the exact same appearance as we had in 3ds max. Unreal Studios Material System gives you a lot of power at your fingertips. There are a lot of material expressions that you can make use of, but chances are in most of your design visualization projects, you're going to be sticking to the basics, texture samples, multiplying nodes, constants, and so forth. And the physically based rendering system in Unreal is going to give you the power to create virtually any material with ease. And remember, when using Datasmith and exporting V-Ray materials, those materials will automatically be rebuilt for you in Unreal Studio. Likewise, if you're exporting with standard materials and using FBX, Unreal Studio will do the same for those. Building materials in Unreal can become much more in-depth. So I advise you if you'd like to learn more about using the Material Editor and all of the various material expressions, as well as learning about things like material functions, which allow you to essentially create your own expressions. Check out some of the great learning materials available from Epic. Up next we'll discuss the gray ball and chrome sphere setup, which is a technique used in films, as well as games, that will allow you to ensure your materials and lighting look great.