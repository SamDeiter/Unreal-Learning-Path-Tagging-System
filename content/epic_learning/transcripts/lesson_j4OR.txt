We have analyzed our project and we've decided our textures are too high of a resolution for our target platform. We'll now need to go in and adjust the maximum size. We could adjust them outside of the editor and re-import them from the original change source, or we could change the texture inside of the editor. The problem with this is we'll have to now open all of our textures one at a time, change them individually, and then save them. And depend on our project size, this could take quite a while. Thankfully, we have the property matrix, which can help change all of them at once. What we might have to do by hand is open up each of our textures, go to our compression settings, and under advanced, we have a maximum texture size. And I can force it to a different size other than the default size. However, having to do this for each and every texture might become a chore. We have two here, not a problem. What about the case of our building where we have many more? We can choose any amount of assets we have inside of our content browser. We can right-click, go to asset actions, and then bulk edit via property matrix. After a little bit of time, because it is going to have to load these items into memory in order to get the properties, we're going to get our property matrix on the screen. We can see here I have the property matrix, and it gives us a nice grid view of all of our items. We can see all the names, any properties that it's already set up like our tiling methods. We also have access to things such as the different display types on the right, so we can see texture properties, import settings, etc. If we want to remove something, like let's say I don't need the LOD bias, I can right-click and remove it. And if we want to add something, let's say I want to see the number of cinematic map levels, we can pin it, and I'll add it to our property matrix. The nice thing is, any of these properties we can change all at once for all of our items. If we were to select all of our items, we can use the Shift key to select them all, or you can control to select individual ones. We can see all of the things that are shared between them. And remember, we want to change the maximum texture size. So here, compression, maximum texture size, I can change this to save 512. I can hit Enter, it's now adjusted it, and then I can go ahead and save all of them. It's going to go through and make that change to all of my textures. Now if we go back to our map and we look at our textures, we can see right here, maxing gain 512 by 512, and it took significantly less time. And this is useful for any of the properties we might find inside of here. We also have the ability to look at our property editor, which gives us a nice detailed like view for all of our properties, so we can adjust it all at once. You note that just because I show this on textures doesn't mean it's limited to just textures. Here we can see I have a bunch of my animation sequences, and I can bulk edit them all at once. One final tool that can help us when working with assets in the Unreal Editor is our size map. It lets us view the size of assets and the assets contained within them, both in memory and on disk.