One of the easiest ways and my preference to get external data assets prepared is actually to create them inside of Unreal and then export them for use later. We already have the first step of this process complete which is to create that struct that we've defined to be used as a template of the data element and this will be used inside of our external data file or data assets. To begin we'll create a new folder in the Blueprints folder named Data. Instead of the new folder right-click in here, scroll down to MISC and select the Create a new data table. As I mentioned previously, if you don't see the correct item information table option appearing that we've created the structure that we've created in our library a little bit earlier, if that isn't showing in the list for you here like you can see it wasn't for me, then as I mentioned previously you just need to potentially recompile things in the editor using the hot reload compiler just here. And most of the time that will help everything sync up without needing to restart the editor anything you can see that I can recreate a new data table and the option now appears as it should. From this dropdown select the item information table to be used as the template of information for the table to hold. Notice here that you won't see the generic structure that we've made so the generic item info structure that we've also created and this is what I meant in the previous video when I said that if we didn't include that extra bit of information about the F table row type, then we wouldn't see it as an option when we came to creating our data table class. And finally just name this one item info table. So if we go ahead and open our new data table, you'll notice that at the top here all of the column names reflect the elements that we've just declared in our item information struct. If we click the add button in the tools panel will be given a new row of data to fill. This isn't a required step but this will work as a nice example to see all of this represented when we export our file in a moment. We can fill the information here with pretty much anything right now as it's just a quick example but I'll use the drop down for the item type and I'll set this one to be the radio and then simply type test into all of the other text boxes for now. With that done if we hit save and return to the data folder we can right click on our data table. You'll see here that we have two options at the top of the pop up window which are going to be to export CSV and export JSON. What we want to do is export to one of each and name them something like item info CSV and item info JSON respectively. Then navigate to the location that you've just exported your files and we can begin by opening your CSV file. As mentioned previously you can open this in a number of ways including a plain text editor if needed. I'll be using Excel as this makes this type of file much easier to work with. The option of course would be Google Sheets if you don't have access to the Microsoft Suite. What you should see here is all of the information from the struct that we've just created as the column names and then our first row that we've implemented inside of our data table. The reason I like to do this inside of Unreal first and then export it and take that approach is that it avoids the chance of typos placing elements in the wrong order or other structural or syntax issues that can be made if you try to create this in a fresh Excel sheet and then import it into the engine. By adding this first row as well if you're working with someone else they kind of have a template of how the information should be filled out and again it gives a better idea of what other people need to do when you're working with others in a team who may not be familiar with the process. So as a quick test then let's add another row of information. I'll make this one for the trophy and I'll fill all of the information with something specific to that item. I'll also update the rocket information that we now have to be something a little bit more relevant and remember when filling out the details in the item type column these do need to match exactly with the names of the enums that we've created in the data library class. So for me this was the none radio trophy or spaceship options for my project. And on that note I'll just add a final row to show what happens if we get this wrong by purposely placing an invalid name inside of this cell. This won't cause any crashes or anything so you can test this site as well if you wanted to follow along. But again we'll be removing this or changing it a little bit later. So with all of our changes made be sure to save and close your editor to avoid issues when we're importing. And then we can take a quick look at the alternative approach of working with the JSON files. So head back to where you've exported your files select and open the JSON version. And for this I'll be using sublime text over notepad as this just has some default syntax specific coloring which is quite useful when working with JSON it just makes it a little bit more readable. So inside of the file we can see that this is very similar to the CSV. I had only exported the first example row and we can see all of the information about the item separated out below again matching the information inside of the custom struct that we've made earlier. The easiest way to begin adding new data to our JSON file is going to be a little bit different to CSV. What we want to do here is we're going to add a comma after our first curly bracket here selecting all of the information between and including the curly brackets, copying this and then pasting this just below the comma. We can then go through and start filling out some information about the new item that we've just created. This again won't be too important as it will only be used as a kind of example so we can add any details here that we like. The only thing that would be important if we want to work with this when importing it back to the project will be ensuring that the item type exists in the enum. Of those different item types that we've created in our code again if not then we'll just see what happens when we provide an invalid input just here. That's really everything for now though when it comes to exporting files from Unreal to work with externally. There was a little setup kind of required with the struct but we'd need to create that ready for use when we import data files if we were to do it the other way around for a project anyway. So hopefully you can see the benefits of starting in Engine and exporting things out as we then have this nice guide and a kind of predefined structure for us to follow. So a lot easier to make mistakes and again when working with other people it would just be that little bit easier for them to see the ideal way to structure their own inputs as they're going through and filling out the information.