I've created a project for this course using free assets from the Epic Marketplace. The environment is from the Soul Cave project. The boy and his animation have been imported from a boy and his kite. The second character is the Unreal Manicain, whose skeletal mesh and animation loops have been imported from the MCO Mocap Basics package. The project is available for download from the Marketplace using a redemption code. If you've already downloaded the project, open it and the Soul Cave environment will load by default. The boy and the mannequin are already in the scene. Load the sequence called Scene 1 underscore master located under the Cinematics button to follow along. If you've not downloaded the project and you would like to follow along an Unreal Engine, pause this video and get the project from the Marketplace now. By default, Unreal Engine uses a PBR or Physically-Based Rendering Pipeline. This pipeline combines physically-based materials with physically-based lights to give the most realistic render possible as a default. Let's examine some of the materials in our scene to determine how they work within the PBR pipeline. So in order to better visualize how these materials are working in the PBR pipeline, let's go to this lit button, left click, and browse down to buffer visualization. And at the very top, we see we have an overview option, and let's select that. And now we can see all of our PBR inputs laid out across the screen. These are all of our G-buffers of our PBR material, base color, specular, subsurface, world normal, opacity, etc. So let's select the boy and examine his skin material. Over in the Details panel, if I browse down to the Materials tab, I see Element 3 is a skin shader underscore inst. If I left click on the magnifying glass, I can browse to where the material exists in the content browser. So this material that's applied to the boy is an instance of this material, skin underscore shader, which is the parent. So let's open up the parent by double-left clicking. And we can see we have a root material node that has all of the inputs of our PBR material that correspond with the G-buffers laid out on the screen. The base color input is going to be the result of all this shader code, which is a combination of what are known as material expressions that are being plugged together to equal what we see in this G-buffer. The same thing is true for what's plugged into the specular input. We can see that displayed here. Roughness, again, if we minimize this, we can see is down here. And we have a subsurface, amount, and a normal map. So now let's take a look at the mannequin. Once again, if we go over to the materials tab and we select the magnifying glass and browse to where the M-UE4 man underscore body is located, double-left click, we see we have a different looking material layout. Now these are what are known as material functions, each one of these nodes here, and these differ from material expressions in that if we double-click on one, the ML plastic shiny beige, it'll open up, and we can see we actually have a few material expressions that are defining a part of what the material should look like. So material functions are a collection of material expressions to organize into one node to perform a function on a material. So this material function is defining the color of the mannequin, which is set by this constant three vector, which gives us a red, green, and blue parameter to define the overall color of, which is kind of gray. And we have a roughness amount, which is a float value that's near zero. The result of which, as we can see in this sphere, in the material editor, is a very reflective material. If we look at the mannequin's chest, for example, we can see a highlight that indicates he has a higher reflectivity. So having this low value plugged into the roughness, if we minimize our material, is going to give us a black value that's going to render very nearly perfect reflections that aren't blurry. The boy, however, has very blurred reflections indicated by the lighter values on the skin, shown in the roughness, G-buffer. So now let's take a look at one of the materials in the environment. Let's select the statue in the center of the Skizibo. And once again, over in our details panel, let's go to the Materials tab, and let's select this M-Soul statue chrome at the top of the material element zero. Let's hit the magnifying glass, open it up, and once again, we see something that looks a bit more like the boy's material set up with a base material that has all of the inputs for our PBR material. We have a texture map that's defining our base color. We have a one vector being plugged into the metallic input and the specular input, which is going to make this a fully metallic object with full specularity. We can see that here in the specular G-buffer, the full white value of the chrome. Furthermore, if we minimize this in the metallic G-buffer, we see that the statue is fully white, and the environment, which is non-metallic, is black, which is indicated by a value of zero. We also have a normal map, and a few custom texture nodes, which we don't need to worry about. As we see with these three materials, there are a number of ways to build shaders within a PBR workflow. One noteworthy thing to take away is that the mannequin's flat base color will more readily lend itself to a graphic style once we start stylizing our renders. By default, Unreal Engine uses a deferred renderer, which writes out each material's G-buffer, i.e. the base color, the metallic, the roughness, etc. What is known as a render target? The compositor uses the buffer data, along with the mathematical parameters of the lights to calculate the lighting info. Each shadow casting light will also render a depth buffer to use to generate the shadow. There are two main categories of lights in Unreal Engine, static and dynamic. At a high level, static lights do not affect moving actors and dynamic lights do. So if we go back to where buffer visualization is, and we left click and we select our lit view, and then we hit G to come out of game mode, we can see our icons for our cameras, our reflections spheres, lights, etc. So let's select this light back here by the right of the boy, and over in the details panel under Transform, I see I have three options under the Mobility section, static, stationary, and movable. So static lights at a high level are fully static. Stationary lights are partly static and partly dynamic, and movable lights are 100% dynamic. So the stylizations will be applying to our renders will all be rendered in real time, accordingly dynamic lights are what we need to use when lighting our stylized renders. We now have an introduction to the default deferred renderer and PBR pipeline within Unreal Engine. We've examined how the materials G buffers are stored in render targets and then combined with the lighting. Once the materials and lights are rendered to a final combined image buffer, we can affect how they appear with post process volumes. And the next module will discuss best practices for preparing your models and textures to achieve a specific look.