we are going to be looking at how we can expose functions that are written in C++ to being called from Blueprint using the U function pre-process directive. So let's dive in and see how it's done. The first thing we're going to do is have a look at our scenario. So here we've got our BP grab, we're still working inside of that. And I want to give you a better idea of how this grabber actually works if you haven't already poked around inside it for yourself. Now the idea of the grabber is that it's a component, you put it on an actor and you point it in the direction that you want it to grab at things. It has a scene component, so it has a location in space. You point it in the right direction, you put it in the right place and then you hit the grab button or basically call the grab function. So let's have a look at what this grab function does, shall we? The first thing it does is it does a ray trace for any physics bodies in the scene and returns conditionally whether there are some actors or not or a component that has been hit. Then what we go on to do is we set it to simulate physics if it wasn't doing so already so that it can wave around in the air, this is particularly useful with the X which actually starts off hit and stuck inside the wooden block. But that's a detail, don't have to worry about it too much. And then it grabs the component using a physics handle component and it centers that on the center of mass of the component that we hit and then finally it notifies any quest actors that we've grabbed something such as when we grab the X. Now let's have a look at the trace for physics bodies. That is not a built-in function that is defined here in the BP grab, but it's one of the private functions that don't get called from outside of this class. Now what's going on here is we're doing a sphere trace for objects. We're starting at the location of this particular scene component. Then we're going to this get max grab location which is calculated using our max grab distance which we've moved over into C++ and the reason we moved it over in C++ is so that we can move this function over into C++. We can go in and have a look at the function, it's a fairly straightforward math function. This is the kind of thing that would be condensed down into C++ very easily. It'll be very legible. In fact, it will probably be a one-liner and use far less space than this. Let's head over to the C++ where we can create a function for this. I'm going to create my functions above the variables and I'm going to add it to the protected section we've already got up here where we've got a big in-play and we're going to have a U function because we want it to be callable from Blueprint. Again, we've got some access specifiers in here that I'm going to talk about in just a second. Then we want to write the signature of the function. What is the signature of the function? Let's have a look. We click on the node of the function either here or in the my Blueprint. Then what we can see is that it has no inputs and it is checked off as a Blueprint pure, meaning that where it is called, if we go back to the call site of trace of physics, you can see that it has no execution pin. That's because it is a pure function. It just simplifies things in the Blueprint, especially when it doesn't matter what order it gets executed in because it's essentially a C++ const function. If we go back to our getmax grab location, you can see it does have an execution pin in here, but that's really not very necessary because everything inside it is also a pure function or a pure node. What we're seeing is that actually the return value is a vector. The question is what is a vector in terms of C++ types? Let's answer the question of what the common types map over to in C++. Let's start with the fundamental types. The fundamental types are things like floats, integers and so on. Now float, as we've already seen, maps over to a C++ float. That's pretty straightforward. What about a double? I hear you ask. Well, actually that cannot be represented in Blueprint. You have to use a float if you want it to be accessible from Blueprint. You can use doubles elsewhere if it's just going to stay within the C++ domains, but this is an area where Blueprint makes a simplifying assumption assumes you're not going to use doubles. Similar simplifying assumptions are that you aren't going to use unsigned integers in Blueprint. You only used signed integers. In C++, we have both int 32 and u int 32, as well as int 64 and u int 64, but in Blueprint, we've only got the integer and the integer 64, and ball is a pretty straightforward mapping. Now, what about the utility types? These are things like string, name, vector, rotator, transform, all the kinds of things that you see in the drop down on any Blueprint type. You can see you've got all our basic types like Boolean, bytes, integers, floats. There we've got names and strings and text and vector and rotator and so on. These in C++ tend to have the prefix F. In fact, most struct types seem to have the prefix of F. We have F string, F name, F vector, F rotator, and F transform. All things provided for us. That kind of answers our question, but I'm going to go on just a little bit longer and show you about object types. Things like if you had a u object reference or an actor reference or an actor component reference, scene component reference, so on. In C++, these would be pointers. You'd have a u object pointer, an a actor pointer, a u actor component pointer, and you can see the prefix is coming in. Most things derived from u object will have the u prefix and unless they are an actor in which case they have the a prefix. That's just a straightforward thing that you will see all around the place. That hopefully answers the question. We want our return type to be an F vector. It's not going to be a pointer because it's just a struct and it's going to be F vector. We're going to call it the get maxrab location. Well, because there's a C++, we know that we can make this const because actually we don't want it to be able to modify the contents of this class. Then we want to put some keywords inside a u function so that we can make sure this function is callable from blueprint. The first one is blueprint, callable and no extra credit for figuring out what that one does. It makes it callable from a blueprint graph. If you want to check off that little check mark of pure, then we can use also blueprint, pure, and that just does exactly that thing of making sure there's no execution pin when we call this in blueprint. Finally, we have to go ahead and actually implement something here. I'm going to create a copy of the signature as the clipboard and go over into the C++ file by the way I used control K control O to do that in visual studio if you don't know that already. I'm going to go ahead and stick this below the tick component and at the moment it's just creating a default F vector to return that suits me for now and we're going to fill out the function in just a bit. Once you've compiled and you go over into blueprint, you'll notice that we now have a blueprint compile error and that is saying that the function name get max grab location is already used and is not compatible with the parent function because we haven't said that the parent functions virtual. We haven't said anything about being able to override it. So it's not happy blueprint saying we can't have two functions of the same name and it's darn well right. So if we right click on that get max grab location and find references, we want to go and replace those references with the new get max grab location. Now it may have actually already done that. So I'm going to go ahead and delete the get max grab location and you can see that the node calling to get max grab location seems to be in place. Let's go ahead and compile and it seems to be working. We can go ahead and test the grabber by going into the village right clicking and playing from here and if we go and try and grab one of the pumpkins is not going to work. Obviously because I haven't filled out to grab location so how do we want this to actually work? Maybe I prematurely deleted that function. So I'm just going to undo so that we get our function back and we can just have a look at it. So what it's doing is it's getting the world location and summing that with the get world rotation the forward vector of the world rotation of this scene component multiplied by the grab distance adding it to its current world location. So that's basically projecting forward our forward direction by the grab distance. Now we want to go over and do this in C++ and for the time being I'm just going to tell you what we need to include and what functions we're going to use. We're going to be looking at how you configure that out for yourself. So the first thing is getting the world location. Well this is a scene component. We should be able to call a function on ourselves to do this and it's called get component location what get world location. So that's like the confusing but there you go. And what we want to do is we want to sum that with the result of this multiplication. So we want to get the forward vector of the get world rotation which again is going to be a get component rotation which returns if we hover over it is returning an F rotator as we would expect because the return value here in blueprint is a rotator. And we are going to use a library called the kismet math library. So that's going to be hash kismet forward slash kismet math library dot h. So that should give us access to a static object called the u kismet math library. And using colon colon we can access one of the static functions in here called get forward vector and get forward vector takes in an F rotator and returns it's forward vector. So that's exactly what we want. Let's just rearrange our formula here a little bit. So we've got a forward vector that we're adding to a component location all that remains to be done is for us to multiply that by a max grab distance which is already in C++ so we easily have access to that. And finally we just want to be able to return that result. So just as I said this big function over in blueprint has condensed down to a one liner in C++. Let's save and compile that and also while we're at it go and delete and get max grab location again. Now once the compilation is complete, let's compile and save our blueprint check there are no errors go back to our main map and play from our village walk up to a pumpkin and see if we're able to grab it. Yes we are. So we have successfully been able to migrate a function from blueprint 2c++ while retaining all the calls to it that already existed in blueprint. We've seen how the UF function macro has enabled us to add some of the metadata that blueprint needs and we've seen how we can map blueprint types over in to C++.