Before we can get to work on our own 2D project, we first need to create a new project to store all of our assets. In this video, we will see how to create a new project using the Epic Launcher and how to correctly configure the options to include the starter content. Let's start by opening the Epic Launcher. With the Launcher open, ensure the Unreal Engine is selected on the left-hand side. Now, moving over to the right-hand side, press the Launch button in the top right-hand corner. This will open the Unreal Project Browser. From the browser, in the new project category, select Games and click the next button at the bottom of the window. Next, we want to select the 2D side scroller template and click the next button to continue to the projects settings page. Now, let's quickly run through the project settings. We will be focusing on Blueprint for this course, so select Blueprint Project. We will be developing this project for PC, as well as using that as our target platform, so we can select Desktop slash Console as the target hardware. We can also leave the Performance settings set to Maximum Quality. While ray tracing is lovely, we won't be using it for this project, so we can leave that option disabled in the project settings. And finally, while we will be creating our own assets, it's nice to compare to the existing starter content, so we will want to include that. The last thing left to do here is choose a name and location to store the project. I suggest naming the project MyP2D project and hit the Create Project button. After creating the new project, it will automatically be opened by the editor, ready for us to use. Let's take a moment to review the newly created project. By default, this project will open the 2D side scroller example map. We can confirm this by taking a look in the World Outliner tab on the right-hand side. Next, let's start playing editor session to see what the starter content looks like in action. We can do this by pressing Alt-P on the keyboard, or by left-clicking the Play button in the toolbar above the main viewport. With the game now running, move the cursor over the main viewport and left-click to select it. We are now controlling the game character. We can see that the character has an idle animation to play when we don't give any input. Let's see what happens when we supply some input with the keyboard. Pressing A or D will move the character left or right. We can also jump by pressing W on the keyboard. Note that we can also use the left stick and bottom face button of the gamepad if we have one connected. Now that we've seen the game actually works, let's stop the play and enter session and take a closer look at the assets we're using. Press Escape on the keyboard to end the play and editor session. Let's finish up this quick tour of the starter project by looking at some key project settings and assets. We'll start by seeing whether Paper 2D plugin itself is enabled. Open the Edit menu in the editor and select Plugins from the configuration section. We can search for Paper 2D by typing it into the already selected search box. Once we find the plugin, we can confirm that it is currently enabled. Now that we have confirmed that the plugin is enabled, we can close the plugins window. Now, let's take a look at some working examples of the asset types included in the plugin. Let's quickly navigate around the map. Use the viewport camera controls to move around. We can see now that the character, platforms and background are all 2D assets but are arranged in 3D space as different layers. Now, let's focus in and take a closer look at the player character. Select the character by left clicking them in the viewport or if you prefer, you can search for 2D side-scroller character in the world outline. With the 2D side-scroller character selected, select Edit Blueprint in the details panel on the right-hand side. Take a look at the character in the Blueprint Editor. For now, let's just focus on the viewport tab. Select the viewport tab by left clicking it. With the viewport tab selected, let's focus our attention on the components panel on the left-hand side. hover the mouse over 2D side-scroller character, self, and note that the parent class is Paper Character. This is a new class added by the Paper 2D plugin. If we move down to select the camera side view camera component, we can see that the projection mode is set to orthographic in the camera setting section of the details panel. This means that the game camera is using an orthographic projection. We can see what that looks like by changing the viewport to an orthographic view. Select the left view for the viewport controls, and then select the Unlit view mode, as wireframe makes it a little hard to tell what we're looking at. We'll take a much closer look at the character later, but for now, we're done, and we can close this window to return to the main edit the window. Let's return our attention to the central viewport and select one of the ledges in the level. Once again, move over to the details panel and looking in the components section, hover over the parent object, and note that it is a Paper Sprite Actor. Yet another actor type added by the plugin. And with that, we've seen the main features of the starter project. Now it's time to get to work in the following modules, creating our very own assets. We have now created and configured a brand new project from the Epic Launcher, ready to begin building our own 2D game. We confirmed that the Paper 2D plugin was enabled, allowing us to use all the tools it provides to import our source assets. We took a quick look at the starter content, giving us some idea of the assets that we are going to be creating from scratch during the remainder of the course.