With Unreal Engine Open, let's open the plugins menu. To do this, let's go to Edit and select Plugins. Inside the Plugins window, let's locate the Compositing section and we're going to want to enable two different plugins. The first plugin we're going to want to enable is the Composure plugin. The second plugin we want to enable is the Lens Distortion plugin and this is required for Composure to work correctly. Before our plugin changes can take effect, we're going to want to restart our Unreal Editor. So let's select Restart now. Let's close our plugins window and if we go to the Window menu, we should see a few new options available after enabling our plugins. We should see Composure Compositing and Layers. Before getting started with Composure and Compositing in real time, it's while that certain steps are taken to not only achieve the best results, but to ensure everything works as it should. One of the first things to keep in mind when approaching real time compositing is that is real time compositing, meaning equipment and hardware are going to greatly impact the results you will get. When working in real time, you will need to keep in mind that everything has a performance cost. To make sure that the visual results you are seeing are being displayed at a smooth frame rate, you will want to use the Stat FPS command in the console. To do this, let's select Summer in our Viewport and let's press the Tilday key and we should notice that we get the console command prompt at the bottom of our screen and we're going to type in Stat, Space, FPS and press Enter. We should now notice a new FPS counter at the top right of our Viewport by default. The more layers you add, the more complex the scene and thus the more performance impact you will notice to your frame rate. Aside from layers, actors, lighting and raw footage will be large contributors to your frame rate loss. So please keep that in mind when approaching real time compositing. You also want to use the GPU profiler tool to see where the direct performance cost is coming from for a single frame. You can access this console command by typing Profile GPU. So once again, let's press the Tilday key to open up the console command and we're going to type in Profile GPU and press Enter. You will notice that you get access to each pass and you can open up each layer that further see its impact on performance. For example, if we were to open Scene, we can now see everything that is contained within the scene. We can use the visual bar graph above or we can open the labeled Text tabs at the bottom. Details of the duration and milliseconds of each pass are located at the bottom of the window. Here's a snapshot from a Composure-Based Scene for reference. For a better understanding of how to handle real-time performance, be sure to check out Jords real-time rendering fundamentals course. It is also important to note that there is currently no way to access or export each pass individually. It is either occurring as the final pass or no pass at all. In our next module, we will discuss how Composure Facilitates Real-Time Compositing.