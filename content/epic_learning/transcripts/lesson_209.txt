In the previous video, we began making changes to our Flipbook Handling code to account for looping and non-looping animations. We isolated states where we do not want animations to loop and set ourselves up to implement custom logic for handling jump, fall and landing animations. In this video, we'll make use of events to handle these newly identified states, as well as ensuring that our jump animations always play in the intended order. Now that we've refined our existing Flipbook animation control, let's finish up by creating our event-based jump animation. Our starting point will be when the player character begins the jump. Luckily, we are already provided with an event for this. Right-click on the graph and type on-jump to locate the event. As the name of this suggests and the tooltip confirms, this event is fired when the character has just started jumping. The first thing we'll want to do when jumping is play the jump animation. We'll call the jump animation here using our update Flipbook animation function. Drag from the on-jump to event and locate the function in the search box. In the dropdown below the new animation input pin, select the jump animation, so that's Flipbook underscore UE4 robot underscore jump. Now, when we press the jump input, the character movement component will handle the movement of the jump and as we start to jump up, the first part of the animation will play. The next event we need to capture is when the player character reaches the apex of their jump. Once again, Unreal Engine helps us out here and the character movement component already has an event for this exact moment. When the player character reaches the apex of their jump, an event is broadcast called on-reached jump apex. If we bind to this event, we can fire an event in our graph at the exact moment the player character reaches this apex. Right-click in the event graph and type on-reached jump apex and find the bind event to on-reached jump apex node. Now we need to tell Unreal Engine which event we want to use when this happens. Drag from the event on this node and type create event. In the select function drop down and select create a matching event. This will automatically create a new custom event and select it in the drop down. Name this new event on jump apex reached. Before we work on the event itself, we need to make sure that this event binding takes place before we could possibly have jumps with the character. Right-click and type begin play into the search bar until it appears. Using this node to the same location as our bind event and connect the execution pin. Now our binding will be set up correctly every time the game is started. Now we just need to tell our new event to play the desired animation. Locate the on-jump apex reached event we just created. Place a new update flip book animation node and select the air trans animation in the drop down. So that's flip book underscore UE4 robot underscore air trans. Now the character will start playing this flip book at just the right time. And with that, we're nearly there. We just need to wrap up a few loose ends. By default, the method we're using to be notified about this apex will disable itself after it has fired once. This means with our current setup it would only work the very first time we jumped, which obviously isn't what we want. Luckily for us, we can resolve this very easily with our simple character. Locate our on-jumped event and add a reference to the character movement component. Drag from the reference and search for set notify apex. Set the value of the notify apex ball to true and connect the execution pin from the update flip book animation into this node. Now every time we begin a jump, we will also reset the apex notification, allowing our animations to be called and played correctly. And with that, that takes care of the jump and apex stages of our animation. Now we just need to address falling. The falling animation should only start playing after the air trans animation has finished playing. So we'll need to check two things. Firstly, that the air trans animation is currently new and secondly that it has finished playing. The first step is to bind a new event for when the flip book finishes playing. Let's locate the event begin play. After giving ourselves a bit more room to work, let's drag another reference to our sprite component. From the output of the sprite component reference, drag out from the reference to the sprite component and search for bind on finish playing. Before we forget, connect the execution pin to the previous binding. Just like we did before, drag from the event of this bind node and search for create event. From the dropdown in this new create event node, select create matching event. Name this new custom event on flip book playback complete. It's important to note that this event will now fire for every flip book that finishes playing. So we also need to check to see if it's the animation we're looking for. That's the air trans animation. Drag another reference to the sprite component and from that reference, search for get flip book. Drag from the return value of this function and search for equal. Select the equal object function. From the dropdown of the lower pin, locate the air trans flip book. So that's flip book underscore UE4 robot underscore air trans. Now drag from the output pin of this node and create a branch. And before we forget, connect the execution pin from the on flip book playback complete custom event to the new branch. According to our branch, we only want to change to the falling animation if the current flip book is the air trans. So in order to do that, let's connect an update flip book animation function to the true path of the branch. Now to set the new animation variable, use the dropdown to select the falling animation. So that's flip book underscore UE4 robot underscore four. And with that, we're ready to test out our new logic compile and save the blueprint and return to the main editor window. Let's start a new plane editor session and it works. We can move from side to side, jump and see the full range animations. There is one minor thing we can change. Our current setup assumes that we will only want to enter the falling animation after the jump and air trans animation. But there's one specific case where that's not true. And that is walking directly off a ledge. Let's end our plane editor session and return to the blueprint editor and address this. In an empty section of the graph, right click and search for event walking off ledge. I'm pretty sure you can guess what this is for. This event has a lot of useful output pins, but right now we don't actually need them. All we need is the execution pin. Click from the execution pin and add another update flip book animation function. Once again, select the falling animation in the drop down of the new animation input. So that's flip book underscore UE4 robot underscore four. Once again, let's compile and save our character blueprint and start a new plane editor session. Now when we walk off the edge of a platform, we can see how we go right into the falling animation just as we'd expect. And with that, we've done it. The only thing that's left to do is spend a few moments and just straighten up our event graph. Neat in up all the sections and use comment boxes to organise it in a way that makes sense to you. In this video, we finished implementing our new logic path to handle the more complex jump and fall animations. We made use of events and notifiers to ensure that our flip book animations were updated at exactly the right time. This allowed the character to transition from jumping to falling to landing back on the ground perfectly. Once these changes were complete, we finished the video by reviewing our work in a play in editor session.