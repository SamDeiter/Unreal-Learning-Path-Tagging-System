While we've now created our character and we can place him in the level, they aren't very interactive at the moment as the player can't actually control them. In this video, we'll give the player character the ability to move left and right by defining action mappings for buttons and adding corresponding blueprint logic to the character to react to those mappings. We'll then move on to ensure that the character's sprite faces the direction of movement automatically before finishing up by implementing a basic run and walk movement speed. Let's start by adding the left and right movement. Moving left and right will naturally start with player input, so let's take a look at the input bindings that have already been set up in the project. Go to Edit, Project Settings and in the left hand menu, Locate Input. With the Input section open, expand the Axis mappings rollout. Select that we have a move right axis here which we can also expand. With this rollout expanded, we can see how keyboard keys and the gamepad left thumbstick will impact the axis. When we want to move to the right, the scale will be 1, and when we want to move to the left, the scale will be minus 1. We can see that A and D and the left and right arrows on the keyboard are bound, and that a left thumbstick on the default gamepad has also been configured. You could change these if you'd like, but I'm happy with the defaults. With the current bindings confirmed, we can close the Project Settings window. Now that we know which binding we want to use, let's head over to our player character and implement movement dependent upon that input. Use the Content Browser to navigate to Content, My Content, Blueprints, and double-click the My 2D Player Character asset to open it in the Blueprint Editor. Make sure we have the Event Graph tab open. Let's start by removing the default nodes in the Event Graph so that we can build everything in the graph step by step ourselves so we understand how it all fits together. Mark you select the existing nodes and their comments and press the Delete key on the keyboard. Now let's add our first event. Click in the Event Graph and begin typing Move Right and select the event called Move Right to place it into the Event Graph. If we hover over this event, the tool tip will tell us exactly what the event does and when it is fired. This event gets the value of the Move Right axis once per frame. We can also hover over the output pin of the event node to see that the axis value is delivered as a float. This float value will contain the 1 or minus 1 value we saw in the Engine Input section of the Project Settings window. Let's quickly test the axis value is giving us the output we expect by directly connecting it to the Character Movement. We will do this by using a function called Add Movement Input. Let's add the function to the graph by right-clicking and typing Add Movement until we see the function. Let the axis value output pin from the Move Right event into the Scale Value Input pin of the Add Movement Input function. As we expect the PlayerCactor to move along the X axis, set the World Direction Vector to X1 Y0 Z0. These two changes will apply our Scale Value of minus 1 to 1 in the X axis, resulting in movement in either a left or right direction. The last thing we need to do is connect the execution pin from the Input Axis Move Right to the Add Movement Input. We can also reposition the Add Movement Input node just to make things look a little bit neater. At this stage, we can compile and save our blueprint. The next thing we want to do is quickly test this works in game. However, the PlayerStart that we placed in our level earlier will currently spawn the 2D Character from the Start to Content. And we need to change this to spawn on new character. Minimize the Blueprint Editor window and then using the Content Browser, navigate to 2D Side Scroller BP, Blueprints and double-click the 2D Side Scroller Game Mode to open it. Within the Classes rollout, locate the default porn class and from the drop-down select My 2D Player Character that we created earlier. That's the only change we need to make to the Game Mode so we can compile and save the Game Mode. We can now minimize this window and return to the main viewport. If it's not already open, open the My tile map level in the Content Browser. Once the level is open, start a play and edit a session. And we can see that it works. We can use our left and right inputs to move the character, left and right. When I say it works, it kind of works. We can see two main issues right away. The Player Character doesn't change their facing direction and they don't play a walking animation. Let's tackle the facing direction first as that's a little bit more complicated. Let's stop our play and edit a session for now and return to the Player Character Blueprint. What we need to do now is find a way to tell which direction the Player Character is currently moving and then use that information to update the character's facing direction to match. We'll do this by adding some more logic directly after the Move Right event. So for now, let's delete the Add Movement Input node. Let's start by using the Axis value from the Move Right event to see if the Player is attempting to move to the right. Drag from the output pin of the Axis value and enter the greater than symbol on your keyboard to locate the float greater than float function. In this instance, we do not need to attach another input pin and we can leave the default value of 0 for the comparison float. If the value coming into this function is greater than 0, then the return value will be true. And as we saw earlier, a positive float value on this Axis indicates that the Player Input will move them to the right. We need to apply some more logics to determine the final outcome though, so let's create a branch that will utilise the ball output pin from this function. Drag from the ball output pin of the function, a begin typing branch until the branch node appears. Now, we can do something similar to check if the Player is attempting to move to the left. Rather than create a lot of messy connections to our initial Axis value, let's use the Get Move Right function to get access to the value of the Move Right Axis without having to directly connect it. Click in the graph and begin typing Move Right until it is selected. Drag from the return value of the Move Right function and enter the less than symbol on your keyboard to select the float less than float function. Once again, we don't need to connect another comparison value, the default of 0 is what we actually want to use to compare. Let's connect the output of this function to another branch by repeating the process of dragging from the output pin and selecting the branch node by typing branch into the search dialog box. Now, let's finish up this little bit of logic by connecting all of the execution pins. Connect the execution pin from the Move Right event into our first branch. Connect the false execution pin of the first branch node into our second branch node. This now captures the three possible states we can determine from the Axis value. If the first branch returns true, the Axis value is positive and the player is attempting to move to the right. If the branch returns false, however, the following branch checks if the value is negative. If the value is negative, then this branch returns true, indicating movement to the left. Otherwise, by the process of elimination, value must be exactly 0 and the branch returns false, indicating that there is no movement either left or right. Well, that's useful logic, but we're not actually doing anything with the fine execution pin of these branches just yet. What we'll do is we'll create somewhere to store the information within the blueprint to allow us to use it elsewhere in the event craft. We'll do this by creating a new BULL variable. Click the plus icon on the right hand side of the variables roll out in the MyBluePrint panel. Name this new variable is moving right and double check it is a Boolean variable. Now we will set the value of this variable using the execution pins of the two branches we created. Hold down Alt and drag the is moving right variable into the event craft. Select the set node to the true execution pin of the first branch. This path through the branch indicates that we are moving right, so check the box within the set node to set the is moving right variable to true. Select the set node we have created and use the copy paste functionality to quickly create another version of the node. Connect this new copy to the true execution pin of the second branch. This path through our logic indicates that we are not moving to the right as we're moving to the left. So go ahead and uncheck the box to set is moving right to false. And with that change, we're nearly done with this section. All that's left to do is recreate our add movement input function and connect it back to the graph. Right click in an empty space at the event graph and begin typing add movement input and select it. Now whatever the outcome is from any of these branches, we will always want to process the add movement input. So we need to connect the execution pins from each path to this function. You can use whichever method you feel is best here, but my suggestion is to add re-root nodes by clicking on the execution wire and connecting the branch up in a way that doesn't leave all the wires overlapping. With all of our logic assembled, we just need to feed the same values back into the add movement function. Let's start by setting the world direction to x1 y0 z0. Finally, let's copy paste our get move right function to gain access to the axis value and connect it to the scale input value. Now we can compile and save our blueprint and check that it still works the way we expected with a play in editor session. Now we have a ball value that can be referenced quickly to check the facing direction of the player, which we can actually use to alter the rotation of the character. Rather than just rotate the sprite, we will alter the rotation of the entire controller, which will in turn rotate the sprite and it's being controlled that way. The player character can change direction and velocity very quickly, so we want to keep an eye on this and adjust the character accordingly. To do this, we will make use of the tick event. Note that this event is activated every in-game tick and has the potential to be abused, so we should always consider if it's the best option. In this case, it is. Let's create the tick event node in the graph and give it a bit of space for any other nodes to give us some room to work. So let's get to work and figure out which direction the character should be facing. In order to work that out, we need to know the character's velocity. We can get this information easily from the character movement component. Drag the character movement component from the components panel into the event graph to get a reference to it. Next, drag from the output pin of the character movement component and begin typing velocity into the search box. Roll down to the bottom and locate get velocity in the velocity roll out. For now, we're only interested in the velocity in the x dimension. But the velocity variable we've just gained access to is a vector, which contains values for velocity in x, y and z. Luckily, Unreal Engine has a handy break vector function that will allow us to access just the dimension we need. Drag from the velocity output pin and type break into the search box until you can see the break vector function and then select it. With that all set up, now we just need to process the logic for the three possible states we can infer from the x velocity. These states are moving left, moving right and not moving at all. We'll use a single compare float macro to capture and process these states. Drag from the x axis output of the break vector node and begin typing compare float until it appears. Once again, for our uses, the default compare value will be zero, which is what we want. We can see that this macro gives us the exact execution pins we want, greater than, equal to, and less than. All that's left to do is to use these execution paths to correctly rotate the controller. Let's start with the greater than execution path. This means that the current velocity is greater than zero in x, and this means that the player character is moving to the right and we'll want to set the controller to match this. To get a reference to the controller, right click in the event graph and type get controller into the search box until it appears. Now drag from the return value of the get controller function and begin typing set control rotation until the function appears. If we look at the set control rotation node, we can see that we need to provide a new rotation. We'll do this now by creating a make rotator node. Right click on the event graph and begin typing make rotator to locate the function. This handy function lets us create a new rotator on the fly and connect it to the set control rotation function. Let's move over to the viewport tab for a moment and look at our character's default facing direction. We can see that by default the sprite is facing towards the right. And if we select the sprite from the components panel and look in the rotation value in the transform roll of the details panel, we can see that all the values are set to zero. This means we want to set these same zero values when the character is moving to the right. If we head back to the event graph, we can see that the default values of zero are exactly what we want in its instance. So we can go ahead and connect the return value of the make rotator function to the new rotation pin of the set control rotation function. Finally, let's hook the greater than execution pin from the compare float macro to the set control rotation function. Once that's connected, use the marquee select and move the nodes we've been working on slightly higher up. And I also suggest using a re-root node to prevent the execution wire from crossing over. This keeps everything in our event graph easy to read. The work we've done so far will make it much easier to handle the opposite direction, that is when the player should be facing to the left. marquee select the three nodes we just created, so that's the set control rotation, get control that and make rotator, and use copy and paste to duplicate them. Reposition the notes a little bit lower down and connect them to the less than execution pin from the compare float macro. Now all we need to do is change the Z or your value in the make rotator function to 180. We can confirm this is the correct rotation by heading back to the viewport tab, selecting the sprite and adjusting the Z rotation to 180 in the transform roller of the details panel. Just make sure to put it back to zero before continuing. Just return to the event graph and work on the final section of the compare float macro, the equal two section. Making use of the positive and negative velocity made it pretty easy for us to tell which direction the player character should be facing. But what if they aren't moving at all? Well, that's where the ball we created earlier comes in. By definition, this ball will be set the last time a player input moves the character, and we can use that to determine the facing direction. Create a branch near the is equal to execution pin of the compare float macro by holding B on the keyboard and clicking on the graph. Connect the execution pin from the compare float macro to the branch, and reposition our other nodes using the marquee select if we need a bit more space. The condition that we want to use this branch for is the status of the is moving right ball, so hold down control and drag the is moving right variable from the variables roll out of the my blueprint panel into the graph. Connect the output from the is moving right node into the condition input of the branch. If this branch returns true, then the player character should face to the right with a controller rotation of 0 in x, y and z. This is exactly the same thing that we did with the greater than path from the compare float macro, so let's reuse the very same logic. Create another re-root node along the execution wire of the greater than path, and then reposition the nodes ahead of the branch to make everything up meter. Now we can connect the true input of our newly created branch to this re-root node, and we can reposition this node to make sure there's no crossover of the wires. Now we've ensured that the exact same logic will be applied in both instances without having to duplicate any blueprint script. We can do a similar thing for facing left. Repeat this process for the false execution path and the less path of the compare float macro. It's worth taking some extra time here to make sure everything is laid out neatly. Organize the execution flow so that it's clear to you exactly how this logic works. Finally, there's just one more very important thing we need to do. Connect the execution pin from the tick event into our compare float macro. This way it will actually be called each tick. And with that, we're all done with a blueprint code that will set the player character facing direction every tick. We've got just one more thing left to do to complete this module, but before we do that, let's take a quick look at our progress so far by returning to the main viewport and starting a play in editor session. We can see that as the character moves left and right, they face the correct direction during movement. We can also see that when we stop giving input, they will continue to face in the last direction of travel. It might not feel that much, but we've come a long way since we first started, and we're well on our way to making a fully functional 2D character. However, the last remaining issue is pretty obvious. While the character is physically moving, they are still playing the same idle flipbook, which looks pretty weird. What we want is for the idle flipbook animation to play when there is no movement, and the walking flipbook animation to play as the character moves. To decide which flipbook to play, we will once again be using the velocity of the player character. So let's finish our playing editor session and return to the blueprint editor window. As we will be making a lot more flipbook based animation decisions later, it's a good idea to be organized from the start and create a custom event to handle the animation changes. Right-click in the event graph and begin typing custom event into the search bar until add custom event is highlighted, then select it to add it to the graph. Let's name this event handle animation. The first thing we need is a reference to the current velocity of the player, so add a get velocity function by right-clicking on the graph and typing it into the search box. As you would expect from our previous use case, this function returns a vector, but in this instance, we don't actually need to bother splitting it. For the time being, all we care about is if the value is anything other than 0, which indicates the character is moving. Instead, we can make use of the vector length function that provides a float as a return value. Drag the vector from the get velocity function and begin typing vector length until it appears and select it. Now, we just need to check if the value of this is greater than 0, so drag from the float return value and enter the greater than sign on the keyboard in the search box to find our faithful greater than function. We'll be coming back to this and expanding the functionality, for now let's just worry about two states and two flip-button animations we want to use, idle and walking. Because we only have these two simple states, we can quickly use a simple select node to pick between the two flip-books. Drag from the ball return value of our greater than function and type select. Scroll down to the bottom and find the select struct. This will create the new select function, automatically connecting the ball from the greater than function into this node as the index. We can see two possible results from the select with diamonds next to them. True and false. The grey colour of the diamond indicates that they are wild cards, meaning that at the moment that looks at any type of variable. However, we know that we want this to be a flip book, so right click on the node and select change pin type and then select paper flip book object reference. Now diamonds turn blue and if we hover over them we can see that both the true and false and return values are object references. We can now use the drop down menus within the select node to pick the correct assets, so let's do that now. If the player isn't moving, the length of the velocity vector will be zero and the greater than function will return false. Therefore, we will set the false selector to our idle animation by using the drop down and selecting flip book underscore UE4 robot idle stand. If however the character is moving, the length of the velocity vector will be greater than zero and the greater than function will return true. Therefore, we will set the true selector to our walk animation, flip book underscore UE4 robot underscore walk. Now that we've selected the correct flip book, we need to actually apply this to our character. Select from the return value of the select node, begin typing set flip book into the search bar and selecting it once it appears. By dragging this function directly from the return value, the new function is automatically associated with the sprite component of our character. Now all we need to do is connect the execution pin from our handle animation custom event to the set flip book node and our custom function is ready to be called as part of our tick event. Right click on a blank spot on the event graph and begin typing handle animation until our custom event is visible in the search results. Now connect this node to the event tick, placing it before the compare float macro. Now we'll update the animation as often as we update the facing direction to keep everything in sync. Let's finish up by doing a quick tidy round before closing the blueprint. Marquee select our input code and press C to create a comment box around it. Let's add a comment that says handle input. Next, let's make a Marquee selection around the nodes from the tick event and add another comment box saying handle animation and facing direction. Finally, let's do the same for our handle animation event. Now we can see at a glance what each section of our event graph is responsible for. And with that, we finish this section. We can hit compile and save in the toolbar and return to the main editor window. Let's start another plain editor session and try moving left and right as well as letting go of the controls. Let's see how our facing direction and animation work as we move around. However, there is one other thing that's been bugging me and here's the perfect opportunity to fix it. While the animations look great, the movement speed just doesn't feel right to me. Let's return to our character in the blueprint editor and fix this up. Select the character movement component in the components tab. While the default values in here are pretty good for a standard 3D character, they really aren't right for a tiny pixel 2D character. To make things feel a little bit nicer in my opinion, I suggest changing the following options. For each one, we can search for them in the details panel. The first is the gravity scale. I suggest setting this to 4. The next is max acceleration. I suggest setting this to 1024. I suggest setting the max step height for walking to 10. The walkable floor angle to 46. The ground friction to 3. The max walks speed to 78. And the braking acceleration walking value to 32. Finally, let's quickly set up something to help us in the next module and adjust the jump said velocity. Let's change that to 1024. Finally, let's disable in part base velocity y. And with that, we completed all the changes. We can compile and save and rerun a play in editor session. And that feels much better to me. It's important to note that all of these values are what I'd consider to be magic numbers. I've just tuned them until they feel good to me. That means don't worry if you don't like these numbers, change them to whatever you like. While we're here and making small tweaks and fixes to improve the feel of our game, you may have noticed that our character is actually floating a little bit above the tile surface. While we made our best guess when we created the character, sometimes we need to make adjustments when we actually see the game running. Luckily for us, we can easily fix this by adjusting the Sprout position on our player character to compensate for the issue. Let's return once more to our character in the blue print editor. Move over to the Viewport tab so that we can see the impact of our change. With the Viewport tab selected, select the Sprite component from the Components panel. In the Transform rollout, change the Location Z value from –2 to –4. If we reposition our camera view in the Viewport, we can see how this places the feet of the character just below the collision capsule. If we compile and save our character and start another plane editor session, we can see that this looks much better visually. And with that minor tweak taken care of, we're done with this section of the course. In this video, we added basic movement to a player-controlled character. We use Player Input in the Character Event Graph to manipulate settings within the character movement component. We then match up the visuals by using Blue Print Logic to ensure the correct flip-butt plays in the correct orientation as the player turns left and right. We use the Tick event to constantly check the velocity of the player and adjust the rotation of the player controller. We added a simple selector to change between idle and walking animations depending on the velocity vector length of the player character. Finally, we tweak some values in the character movement component to something more suitable for a small scale 2D pixel art game.