In this module, we'll take a look at how we can set up a batch process for our project. To begin, let's take a look at the faceware knowledge base. In the left section, we'll see the retargeted menu. If we open this up, we'll notice a section over here called batch functionality. We have melcommands for Maya, batch commands in MaxScript for 3D Studio Max, and Python commands for MotionBuilder. Since we're working in Maya, let's take a look at the melcommands. On this page, we'll find multiple melcommands that will allow us to execute several of facial retargeters functions. Underneath them, we'll have the arguments and parameters that will allow us to customize how these commands are called. For the purpose of our batch process, we'll be working with these commands as strings and passing them through mel. If you'd like to set up your own batch process, you can use these lines of code as reference or print screen and type them at your own pace. Let's take a look at the script that we wrote for our batch retargeter. We'll do a quick breakdown of all the different functions that we have in here, as well as the prerequisites. The first couple of lines here are library imports that will be necessary for Maya. In this section, these are the paths to all the files and folders that will need in order to run our batch retargeter. The names of the variables are not specific. You can choose to name them whichever way you want. In this case, the project root is the folder that contains all the subfolders for all of our particular retargeter jobs. Inside of these subfolders, we would expect to find an FWR file, which was exported during the parameterization process of Analyzer. In this line, we'll have the Maya file from which all of our batch retargeting jobs will start. This scene should contain our facial rig with no keys, everything reset to neutral, as well as a camera and lighting setup by default. This will be especially useful for reviewing our work and potentially playblasting the results. In this line, we have the Character Setup. The Character Setup XML file contains all of our controllers as well as their default values. In this line, we have the shared poses. This is the collection of all the poses that we created during our ROM process, as well as any custom poses that we felt could be beneficial for the batch process. In this section, we'll add any custom settings that we want to apply to our giving character. This could be something that's more global for all characters, but we may want to create custom values for each character. For example, we have the position, rotation, and scale for the image plane that we place next to our character. This will likely change depending on the character heights and possibly the size of our HMC footage. Over here, we have the face for values. At the moment, we only have one set of values for all three face groups, so that would include the smoothing, the prune spacing, and the pruning. For now, I've kept it at a very low value of 10, 0, and 10, but we may want to split this into three sets of values for the brows, eyes, and mouth. In this section, we have a couple of global variables that we'll be using across multiple functions. Over here, we have the Maya Eval function. This will allow us to run specific commands that we provide as strings, and we'll run them through Maya. We have a try-except function in order to catch any potential errors and flag them through this print line. Over here, we have our run retargeter function. This is where the bulk of our batch processing will take place. To begin, we have to adjust the paths in order to use a forward slash instead of a backslash for Maya. To load the FWR file, we use the FTI Open Performance function. We'll provide the FWR path that we've fixed over here, as well as the character setup file in order to detect the controllers that we'll be animating in our scene. If we get an error loading this, then of course we shouldn't move forward and we'll return a false. If we were successful in opening our FWR file, we want to load our shared pose database. This will allow us to use our shared poses without having to create any custom ones to generate our animation for an entire timeline. The next section here will allow us to reposition our image plane next to our character. In this section, we'll be cycling between our three face groups. Using the IAM edit retargeting range function, we'll be able to determine the frame range in which we're retargeting, and we'll set the values for smoothing, prune spacing, and pruning that we've determined at the top of the script. After we've edited the range for this particular face group, it's time to retarget that face group. Important arguments here are AS for autosolve. We'll be setting this to 0 to disable it, and we'll be using sp for shared poses, we'll set it to 1 in order to use the shared pose database. And then we'll repeat this for each face group. After we've generated animation on our timeline, it's time to save our myophile. The quickest way to find the file path for our myophile would simply be to switch the extension that we had for our FWR file and switch it to an MA extension. And then we can rename our current open scene and save the file. Lastly, we want to export the FBX file in order to bring our animation into Unreal. To do that, we can search for the facial controller's selection set in order to select all of our animated controllers. Once we've found the facial control's selection set, we determine the path for our exported FBX file. Using this command, we can select all of our facial controllers and simply use this function here to export the selected animation. If the file already exists, we'll be overriding it. We'll also use a try accept as well as a couple of print lines here to detect whether we had issues along the way. Finally, in this bottom section, we have the lines that we'll call in order to execute this script. To begin, I'd like to use something called a kill switch, which is simply just a text file that we place in a location of our choice that we can delete at any moment if we want to interrupt the process without having to shut Maya down completely. In these lines, we generate that text file and we just put in a default line here, which is delete this file to interrupt the batch process. In this section, we collect all of the subfolders under our project route. We'll also be iterating to be able to keep track of how many jobs we've generated so far. Then for every job that we have in this folder, we'll be determining the job directory and making sure that we have an FWR file within that folder. The first thing we do is to check that the kill switch still exists. If it does not, then we interrupt the loop. If the FWR exists, we will open our faceware base file and begin retargeting it. If it does not exist, we simply print an error message and then resume and continue on to the next job. That should complete this brief tour of how we can write a very simple batch retargeter. After we finish writing the script for our batch retargeter, we want to execute that code within Maya. To do that, we need to open the script editor. There are two places we can find the script editor. One is at the top toolbar over here under Windows, General Editors, Script Editor. Another place we can find it is down here in the bottom right corner by clicking on this shortcut here. In the upper portion, you might see some lines of code. That's the history of your previous operations. In the bottom over here, we have tabs for all of our open scripts. By default, you might see a mail tab over here. Since we're working with Python, we've opened a new Python tab. To open new tabs, you can click the plus sign here and select Python. In here, we have the lines that will allow us to execute our Python script. You can assign the path of our Python file to any variable of your choice. We'll use a try-except function here to catch any potential errors and we'll simply use the exec file command to run our script. To execute the script, we can click the execute button up here or we can select our lines and hit the enter key on our numpad. Let me move this out of the way down here and hit the execute button to start. The base here in the water of the lock and press all, including the French queen, before she heard that symphony again, just as a young author wanted. In this example, I loaded a job that came from our batch retargetar script. I noticed that there was a bit of jitters in the middle when it comes to the mouth and the eyes and therefore I loaded the FWR file back in retargetar and simply changed the pruning and smoothing values so that we can see a before and after a comparison. Setting the pruning and smoothing values to 10 would mean that we're very confident in our tracking model and that we know that it'll be able to cover a very wide range of expressions with very minimal jitters. However, in this case, since we're using a tracking model that's composed of different recording sessions, I would recommend to keep a value closer to 20 or 30%. The base here in the water of the lock and press all, including the French queen, before she heard that symphony again, just as a young author wanted. As we saw in this example, when we played before and after, we get a much smoother result with these current values. And then, if we wanted to do a technical pass, we can create new custom poses specific to this given performance and start with maybe a handful and then work along our way to fill in our timeline. For example, here the blink, we noticed that the eyes aren't quite close. So you might think that it's related to the poses in our shared post library. When in fact, we see here that the tracking could be slightly better to contact the upper and lower eyelid. The mouth on the other hand is tracking fairly well, which is why the pose here is pretty accurate. Of course, we could pull the lower lip a little higher here and mix some small adjustments like so. Here's an example of a ch-sound that the tracking seems to be working relatively well, but we're noticing that we're not getting those teeth contacts as we would probably be hoping. So this would be a frame where we'd want to store in new custom pose. And if we felt that it was a pose that we're likely to see again, we could incorporate that into the shared poses for future use. Being explored, the melcomens required for setting up a returgeter batch, we can now combine the strengths of a robust tracking model and an extensive shared pose library to generate new animations with little to no manual input.