So, in this module, without going too deep down the rabbit hole of color science, we're going to get into Unreal's color output options, specifically the Unreal tone curve, linear sRGB and ocio options. So open colorio is a color management solution geared towards motion picture production, with an emphasis on visual effects and animation. Though the view working in bigger productions may have a color space pipeline you need to conform to, and that is where ocio comes in. With the ocio integration into Unreal Engine, you can choose to output your renders to whatever color space you want as well as preview it in the viewport. Whether that's Alexa Log C, Rec 709, or the industry standard, aces. Again, this is a very advanced topic that quickly goes beyond the scope of this course, and its usage varies on a per-production basis. But together, we'll learn about how to set it up. This is likely something that most users will not need to use, but as always, knowledge is power. So first off, you're going to need to download an ocio config, so we can find that on ocio's official website, the link of which will be in the video description, and then going on the download tab up top. You'll find the downloads section up here, and you can download sample ocio configurations. Now the one you download here is going to be entirely dependent on your production's needs, so bear in mind this may be different depending on what you're working on. But for the sake of this course, sample ocio configurations will be just fine. You download the zip file, and you'll be ready to go. Now going back into Unreal, we're going to go make sure to have the ocio plugin enabled, so by going to settings, plugins, and we can search for ocio. And in theory, you should have this enabled by default, but again, we're just covering our bases here, making sure that it's enabled so that you don't run into any issues. So there's one thing we need to do here in the content browser, and that's create an ocio config file. So this is going to be an asset that will be found in your project. We absolutely need to create this. So we're going to right click on the content browser, go to mislaneous, and create open color IO configuration. People create a new asset here, and by double clicking on it, we can open it. And you'll get a new window right here. What you need to do now is find a configuration file in the zip file that you just downloaded from the ocio website. So we're going to click on the three dots here, and in the folder that we downloaded from the ocio website, you'll find a series of other folders, depending on your production you might want to use Nuke default. Or in my case, I'm going to be using aces 1.0.3. In either of these folders, you will find a config dot ocio file, and that is what we need to load into Unreal. It's uncorrectly. You should now have the config file path found right here. And now we need to designate which color spaces we want to use in our project. So in the desired color spaces here, I'm going to click on the little plus. You add another one, and another one. So the first one I'm going to choose is Utility, Linear, SRGB. Because Unreal, when the tone curve is disabled, will output your renders in Linear, SRGB. That is the input. Next, a common one that filmmakers like to use is Ari Alexa Log C, and the last one will be aces CG, which is commonly used in the VFX industry. But again, you can add any color space you want. This is 100% going to depend on your production. There is no right or wrong answer to use here. In general, the only one that is pretty much always necessary is Utility, Linear, SRGB. The remaining others depend on your needs. So once we've set up our color spaces, we're going to hit the Save button and close this window. And now we're ready to display our color space changes in our viewport. So we're going to do that by clicking on the lit button up top here. Go down to OCEIO display, Enable Display, Select an OCEIO asset that's the configuration file that we just created. And now we need to set our input and output. The input is going to be Utility Linear SRGB. Because like I said, when the tone curve is disabled and unreal, Unreal will output a linear SRGB image. And the second one will be your output. And in this case, I'm going to set it to REV3 Log C. And you'll see right away that our image got much flatter, much less contrasty. And that is entirely normal. A log color space is a flat profile. And this is a very desirable color profile because it makes it so easy to color grade in post production. So you can also change your output to ACES. And it is completely normal for it to look like this because ACES is a linear profile. In my case, I personally love to work in log C. But because like I said, that flat profile makes it much easier to color grade in post production. So this is just a viewport transform. So if you want to render your frames in it with the movie render queue with this color space make-d in, we can do that. So we're going to our sequencer, making sure we have our sequence loaded. Again, if you don't, go to Cinematics, load your sequence, or create a new level sequence. We're going to open up the movie render queue settings. We're going to open our movie render queue settings right here. And on the big green settings button up top here, we're going to click on it and add color output. In the color output tab here, you'll see we need to do exactly what we just did in the viewport. So where it says OCIO configuration is enabled, click on yes, unfold this tab here. And where it says configuration source, we're going to designate the configuration file that we created. Our source color space will be linear SRGB and a destination color space will be REV3 log C or whatever color space you want to render in. When rendering with OCIO configuration on, it will automatically disable the tone curves. So you don't need to set this. In fact, you can't set this. Once you've done this, hit the accept button and you can hit render local. And you'll see right here in the render preview, we have our log color space applied to our render correctly. If on the other hand, you don't want to export with a color space baked in, you don't need to. You can just uncheck this and disable the tone curve right here become available with OCIO disabled. And the tone curve disabled Unreal will give you a linear SRGB render. This makes it very easy for colorless to use a color transform node to convert that to whatever color space they want in post productions. So again, this is a pretty advanced topic and will change depending on your production. So there's no real right or wrong way to do this. It all depends on your needs. So here we have our render. As we can see, the Alexa V3 log C profile has been baked into it and has given us a much softer, flatter profile. This makes it so easy to grade. This is extremely desirable for filmmakers and colorists alike. So we've only barely scratched the surface of how deep and complicated color science can be. But fortunately, the core principles of how to use OCIO in the Unreal Engine, along with the linear workflow, are pretty simple and easy to understand.