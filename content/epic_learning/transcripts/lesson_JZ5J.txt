Hi everyone, I'm Deguri and I will be guiding you through this gameplay ability system course. This is the 6th lecture in the series where we'll implement a feature that you just gameplay effects to modify the attributes of a tech target. Here are today's learning objectives. Understand the interaction between gameplay effects and attributes by using modifiers. Learn how to configure an instant gameplay effect to immediately reduce a target health attribute. Learn how to handle character deaths when character health reaches zero. Before we begin, let me show you a preview of today's example. In this lecture, we will reduce an NPC's health as a result of a tech and implement a death mechanic when health reaches zero. These are the core components we'll be covering today. We'll focus on the instant type of gameplay effect in this lecture. The gameplay effect we'll explore today plays a key role in the feedback loop created by the gas framework. This is the part where gameplay abilities and attributes become intellectually connected. A gameplay effect is a data asset that defines the effect. A gameplay effect is a data asset that defines the effect triggered by a gameplay ability on each target. This asset contains a variety of features that affect the target attributes. On the right side, you'll notice there are a lot of settings. Gameplay effects are designed to fully control within the blueprint editor. This editor should only be used to define the default values not logic. Technically, you could create the effect with C++, but the process is overly complex and inefficient. So it's best to understand that gameplay effects should be handled in blueprint only. Gameplay effects are categorized as either instant, which apply effects immediately, or for duration, which lasts over time. Since there are so many settings involved, we'll focus solely on the instant type in this lecture. Here's how a gameplay effect is triggered. Each effect operates independently within the gas system. That's why all relevant information is passed in advance, so the effect can process itself once triggered. This package data is called a gameplay effect spec. A spec includes the effect class, contextual data through gameplay effect context, and additional values like level and stack count. Based on this spec, you can apply effects to yourself or to target defined in target data structure. A good example of a self-applied effect will be increasing the player's stat on level up. A target applied effect will be reducing on enemies' health by landing on attack. The result of gameplay effect directly influences the attribute to set. Attribute to set can interpret the context of incoming effect and implement the custom logic accordingly. To support this, they provide two virtual functions. The first is pre-gameplay effect execute. This function is called before, and attribute is modified. And it decides whether the changes should proceed. Returning force can set the attribute modification caused by effect. The second is post-gameplay effect execute. This one runs after the attribute has already been modified, allowing you to react to the final value or set additional logic. Since this happens, after the value is finalized, it's the best place to implement custom response logic. Note, both functions are only triggered when a base value is modified. Now let's work through a hand-john example. We'll create a gameplay effect and apply it to reduce the NPC's health. For this session, I'd prepare the exemplify start-dick file in advance, when on zipping, rename the folder to example 5. Let's go ahead and set up the project for this session. Right-click the project file with the shift and select Generator Visual Studio files. This will generate the solution file. Double-click to open the solution. Make sure the start-up project is set to Arena Battle Gats. Then build a solution. Once the build is complete, run the editor. As in the previous lecture, I've duplicated the previous lever and named it step 5 and set it as the default level. In this session, we'll implement a feature where an NPC's health decreases when hit by a player using a gameplay effect. First, create a folder named GE, short for gameplay effect. Inside it, just like in GA, create a Editor folder. Create a new blueprint and set its parent class to gameplay effect. Name the blueprint BPGE attack hit. Save the asset. Then open the gameplay effect blueprint. One important thing to remember here is that you should never use blueprint logic in a gameplay effect. Only default values should be set. When you first open it, the default view is too small. So close the window without changing anything. And then we open it. Now it should switch to a view that only shows default values. Let's proceed with the basic configuration here. Depending on the player's skill, there are various ways to modify attributes. Gameplay effects support all of these through extensive options. But in this lecture, we'll only use the most basic settings. First, make sure the gameplay effect type is set to instant. This setting applies the effect immediately when triggered. If you click it, you'll see two other options, Infinite and Hatch duration, which work differently from instant. We'll go over those later. For now, we'll stick with the instant type. Instant gameplay effect, they likely modify the base value of attributes. So let's implement a simple case where AT house is deducted when a character is hit. To configure attribute value changes, use the modifier section in the center. Add a modifier and specify the target attribute. In this case, it will be house. Set the operator since we want to subtract from the current value, select add. There is no subtract option, so think of it as adding a negative number. There are many ways to deduct house, such as factoring in attack power in defense for final damage. Now we'll skip that for now and simply subtract a fixed number and minus 80 into scalar float. This means that when the gameplay effect is triggered, the target health will be reduced by 80. Now that everything's set, compile and save to finish. Next, we need to trigger this gameplay effect when the player hits the target that will happen inside the GA hit tag. Let's go to GA hit tag and trigger the gameplay effect for the NPC. Delete the print string node and instead we'll add the logic to apply the gameplay effect. In Blueprint, this is fairly easy using the apply gameplay effect target node. The target pin refers to the color, so we'll leave it as is. And for target data, we pass in the target actors we detected. Then set the effect class and while there are additional inputs like level and stack count, we'll stick with the default for now. Compile and save then press play to test. You'll see the target health decrease as it's packed it. We didn't actually too much, just one node and some Blueprint settings and it works. This is one of the biggest strengths of the gas framework. Everything we built so far has been leading to this moment. Now when health hits zero, the target should die. Let's implement that. We will trick death as one of the abilities and create a gameplay ability for it. Go to the GA, character folder and name it PPGA death. Since this needs to perform a death animation, we will use the play montage and wait ability task that we used before. And the death task is the name for the task instance. For the montage to play, we'll use the AM death animation montage that I have prepared in advance. Let's take a moment to look at this montage. In the animation folder, I have lately created the montage and set it to trigger an effect when the character dies. Now, let's move on to configuring the basic settings to activate this gameplay ability. First, define an asset tag to trigger the ability and a state tag to represent the character's state. Create a gameplay tag, character that ability, that death and set it in asset tags. Then create character that's dead that's dead and assign it to activation on the tags. Since the dead character shouldn't be able to do anything, open PPGA or tag and PPGA or tag hit and add character that's dead to their activation blocked tags. This will prevent those abilities from being triggered while dead. Also, make sure a dead character can't die again by adding the same tag to his own activation blocked tags, since death should happen only once per actor. Set the instant scene policy to instant per actor. Now, let's move to visual studio and handle the actual triggering. We need to detect when health drops to zero or below in the character's attribute set. We could use posted attribute value change as shown in the previous lecture, but that only works for individual attribute changes. It's not ideal for complex changes caused by gameplay effects. Instead, we'll use the virtual function, post the gameplay effect execute. Declarate this function and add a definition. This function is called whenever a gameplay effect modifies a base value. Inside, check if health is less than or equal to zero and then trigger a death event. We'll declare a delegate and pass in the attacker as a parameter. Use the clear dynamic multicast delegate one parameter. The delegate type will be f out of health and the parameter will be a actor pointer in instigator. Declare a variable of this delegate type named own out of health. Add the appropriate u-property annotation. In the function definition, broadcast the event. Here, you can get the instigator, the one who applied the effect from the effect context. This data comes from the data parameter. To use it, include the gameplay effect extension that H header. Use effect spec to get effect context. And from that, extract the instigator to pass into the broadcast. Now go to AB base character and create a function to receive this event. Name it on out of health cpp and match the parameter to the delegate. Add u-function above it to bind it dynamically. Return type is void. In the definition, there's not much to do. Just hide the HP bar when the character dies. Now link this function to the delegate from the attribute to Z. Use ASC cassette to access the attribute to Z. And use the dynamic to bind on out of health cpp. Now, when health drops to zero, the delegate will fire and hide the HP bar. Let's test this with live coding. Let's now configure the BPGA desk ability to handle what happens when the character dies. The first thing to do is disable collisions. Use Z actor enable collision with force to prevent interaction. If the actor is a character, set movement mode to non to stop any movement. Then, after the animation ends, wait a few seconds and end the ability. That's the full set up. Let's now trigger the desk ability at the right time. We'll focus on NPCs for now. In the NPCs initial abilities, assign the same three abilities as the player. But now a problem appears. Because we've made some major changes, live coding caused a mismatch. Let's close the editor safely, rebuild from visual studio and we open the editor. Now, the NPCs should be fine. After that, find the attribute set delegate directly to a new custom event. Create a custom event named on out of health BP. Here, let's first display the instigator's name on screen. Use a print string node. Connect it to an append node with a string. Queue by. Then use ASC to activate the ability using gameplay tags. Use make gameplay tag container and add the gameplay tag, character that ability that desk. Set a lifespan of 5 seconds so that NPC disappears. Hit play to test. If you reduce the NPCs health to zero, it collapses as expected. After dying, it no longer collides with the player and disappears after a while, so that wraps up the basic gameplay effect feature and the desk event flow. That's it for this hand-john session. Let's summarize what we've learned in this session. The gameplay effect should operate based on default values set in blueprint. The instant type immediately modifies the target's attribute. The gameplay effect can be applied to oneself or to a target stored in target data. The post gameplay effect executed function in the attribute set can be used to implement final logic before the effect is applied. Try using the provided step 5 level to practice implementing different gameplay effects. That wraps up today's lecture. I hope you are looking forward to the next one as well. This has been Duku Rii. Thanks for watching. Bye bye.