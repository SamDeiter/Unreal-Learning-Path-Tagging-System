Time to set up our static mesh actors inside the blueprint, and then get that to add instance versions of them so that we can start to populate the spawn area in our scene. Dragging out from the far loops loop body output, then we want to search far and add an add instance static mesh component node, and quite a mouthful to say for being honest. Now we've added this node so that the system will create an instance of a mesh that will then be filled with one of the mesh actors that the user has selected in the project. Now we will want to use this in order to set up a new static mesh, and so let's drag from its output, and then search far and add the set static mesh option that will select our spawnable mesh far as. Let's then make sure that the return value from our add instance node is piped into the target input of the static mesh node. As we will also want a new mesh that will then be passed onto any other nodes that we create, let's pipe the output of the get node that we created into the new mesh input of the set static mesh node. To tell the newly created blank exactly what mesh we want it to be, let's drag the instance to meshes array onto the graph, and choose the get option. We can then drag from its output in order to search far and add to array node. We can then pipe the output of the set static mesh node into it. Now this is going to add static meshes into the array so that it will know exactly what it is supposed to be spawning. Finally, let's pipe the return value of our add instance mesh node into the add nodes new item input. Now just to explain, this is to set up the loopable system that populates our arrays with new mesh instances to the amount that the user picks, though we are not done with this system just yet. These additions to our blueprint, although seemingly complex, essentially select and store each of the objects we want to spawn, which is a very integral part of our system.