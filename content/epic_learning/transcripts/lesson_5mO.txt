By default, the edges of the field of view inside the Magic Leap One have a hard clip. This can be jarring when digital content suddenly enters or leaves your view. In this module, we'll work through how to set up a vignette that allows digital content to more softly fade in and out of your view, thereby heightening its presence in your environment. What we'll be doing is setting up a frame vignette material using a simple texture, then adding it to our post-process materials. Note that if you happen to be going through this course and using a newer version of Magic Leap SSTK, starting with 0.21.0, there's actually a checkbox for an automated vignette. With that, let's start, by of course, saving a new map. We'll call this one, 0.5, underscore, using ML, underscore, frame vignette. First, we'll need a frame vignette texture. Feel free to use anything you'd like, though one is provided. The white of the texture will be what you see, while the fade out into black will be the opacity going down to 0. Bring this vignette or any other that you'd like into your texture's folder. Go back up and go into our materials folder, where we're now going to create a new material. Let's call it vignette. For the material domain, we're going to be making this a post-process material, which only includes an emissive color. The preview will now render as pure black. Dragging out from a emissive color will add a scene texture node and change its scene texture ID to post-process input 0. That will help our error go away. Now let's add in our texture sample, which of course will be our vignette. And all we're going to be doing is multiplying the RGBA value by the scene texture, which will give us a nice fading black vignette. To implement this, we can close out of our vignette, save it, and then find our post-process volume. Under post-process materials, we'll add a value to the array and say that we'll use an asset reference. We can type in vignette, and there it is. And you can already see it active inside the editor. Let's test it out using zero iteration. Note while this does smooth the transition of digital content into your field of view, it also cuts down on the total field of view, use wisely.