Now that we're familiar with the concepts, we are begin by creating an actor blueprint for our ScalitorMesh. To do this, we're going to right-click on the ScalitorMesh in the Content browser, navigate to Asset Actions, and click on Create Blueprint using this. We're going to navigate to the same folder where we have our ScalitorMesh, and name our Blueprint BP on the ScalitorMesh, and click OK. Now that we have our Blueprint open, we're going to reference the color values of our character and customize him through the blueprint. To do this, we're going to go to the Construction Script tab, right-click in the empty space, and type in Set Vector Parameter. We're going to click on the Set Vector Parameter Value or Materials node, and what you'll see is we have now created the Set Vector Parameter Value or Materials node, which already has our ScalitorMesh referenced into it as the target. What this node does is it takes all the materials of our ScalitorMesh, searches for a specific parameter name, and changes it to a set value. To know the name of the parameter we want to change, let's open up the material instance of our character by navigating to the materials folder, and opening up the material instance. Now what we see here is that the two color values are represented as Color1 and Color2. So now we'll go back to our Blueprint and change the parameter name to Color1. We're going to connect this node to the Construction Script and Compile. Now in the viewport what you'll see is that your character turned black. This is because we don't have any value yet assigned to the parameter value. So now we go to the Variables tab, click on Create Variable, and name our variable Color1. Now we go to the Variable type and change it from Boolean to linear color. We will now drag and drop our variable into the Construction Script, click on Get Color1, and plug it into the parameter value. Now what you'll see is an extra node has been created. What's happening here is that we have created a Color parameter, which is being converted into a three vector, which is being fed into the parameter value on our node. Click on Compile. So now if we return to the viewport, the character is still black, but if we click on the Blueprint root, you'll see that we have Color1 exposed. So if you click on that and set a specific color, in this case let's go with the red, you can see that it appears on the character. So now we have the first color set up as a customizable parameter. Let's do the same for the second one. We're going to right click again, set vector parameter, you can set vector parameter value on materials, change the parameter name to Color2 as we have seen it in the material instance, create a variable, name it Color2, drag and drop it in, get Color2, plug it into the parameter value, and connect it to our previous node. Click Compile. So now if we go back to our viewport and click on our Blueprint root again, we will see that Color2 appears as a parameter as well. So now if we click on it and change it to, let's say, blue, you will see that the secondary color shows up as it should. Okay, let's click on Compile. Let's save our Blueprint. And now if we drag our Blueprint into our level, what you see is that we don't have access to the same variables as we did when editing the Blueprint. This is because the variables have not been exposed yet. To use them in the editor, you need to expose them. So let's open up our Blueprint. Let's click on the Color1 parameter and click on Instance Editor and expose on spawn. Now let's do the same for Color2 and I'm going to correct the type of in the name. Click on Instance Editor and expose on spawn. Now click on Compile and save the Blueprint. What you can see now is that the two color values have appeared in the editor and we can change them when working with our character in the level. So if I were to duplicate the character by copying and pasting him and changing the color values, I instantly have two customized characters in my scene. Let's make the green a bit darker and there we go. Now we have two color customized characters in the scene. Another thing you can do in a Blueprint like this is attach an object to your skeleton mesh. To do this, we're going to open up the Blueprint, go to the Add Component tab and search for Static Mesh. Click on the Static Mesh option. Now what you'll see here is we have an empty static mesh added into our Blueprint. In this case, I want to use the basic engine shapes that Unreal offers, so what I'm going to do now is go to the Content Browser, click on View Options, click on Show Engine Content, navigate to the Engine Content folder, open up the Basic shapes folder and drag in the sphere. Now you'll see that the static machine in the Blueprint has become the sphere. What I also want to do is attach the Static Mesh to the right hand of my character. Also under the Socket tab, on the Parent's Socket, I will search for the right hand of my character. There we go. If I click on it, the static mesh will instantly snap to the right hand. So now I'll position this sphere so it's in the character's hand. I will scale it down and position it so it looks like it's in the form of his hand. If I compile the Blueprint now and save it, what you'll see is both of the characters have the sphere in their hands. However, one thing you'll notice is that the static mesh isn't exposed as a parameter, so to change it, I have to go into the Blueprint in the Additza, click on the static mesh component and drag a different machine there. Now sometimes when working with characters like this, you have a set number of meshes that you want to use with the character and you don't want to go in and drag and drop every single time you want to change something. So going off of this premise, what we'll do is set up a slider in the Blueprint so we can switch between a set number of static meshes. So let's navigate back to our Blueprint, open it up and we're going to create a new variable and we're going to name it right hand props. Now we're going to change the variable type from linear color to static mesh and we're going to change it to a static mesh object reference. We're also going to change it from a singular object reference to an array. I'm going to click on compile. What you'll see now under the default value tab is we have the right hand props, but so far with zero array elements. So we're going to click on the plus icon and add three array elements. We're going to navigate back to our engine content, back to our basic shapes folder and for the first one we're going to drag in the sphere, for the second one the cylinder and for the third one the cube. We're going to click on compile, go back to our construction script, drag and drop our static mesh in and from the pin we're going to drag out into empty space and type in set, static and we're going to choose the components set static mesh option. Now we're dragging in our right hand props variable, clicking got get right hand props and now we're creating another variable naming it to prop slider and changing the variable type from a static mesh to an integer and changing it back to a single variable. We're going to click on compile and we're going to set the slider range from zero to two and we're going to set the value range from zero to two as well. Now we're dragging in our prop slider variable into the construction script, selecting get props slider and from the right hand props variable we're going to drag out into empty space and we're going to search for a get, click on the get in parentheses a copy note. We're going to link up our prop slider into the other input of the get node and then plug that into the set static mesh node. Now what we have done here is we have created the array which contains our static meshes. We have created a slider which will use to select between these static meshes and both of these were plugged into a get node which takes the array and matches it to the integer that we set in the prop slider value. So now we're going to connect this to the last node we just placed. Click on the prop slider variable and select instant editable and expose on spawn. Now we're going to compile the blueprint, save the blueprint and now if I select my actor in the level what you'll see is we have three values, color one, color two and prop slider and now if I drag the slider I can change between the set static meshes that I've included in my array. One thing to keep in mind when using this method is that array started zero and go up by one for each element. So make sure that the value range of your slider represents the same values that you've set in your array. So with all this setup I can duplicate my character once more, set a different color. This time I'm going to change it to a very dark orange making it brown, change the secondary color to green and change the slider to use the cylinder. And I can go back to my green character and use the slider to change it to something else. This is how you create customizable characters using blueprints.