This module is all about getting C++ and a blueprint to collaborate. And the main way we're going to be doing this is through C++ based classes. We're also going to overview the different ways that we can communicate and can't communicate between C++ and Blueprint. So first let's take a look at how we can inherit from Blueprint to C++. We are going to be working with the grabber and the quest manager. So we've got Blueprint versions of both of these. But we want to move the functionality over into C++. Now realistically if you've got a large class here, you're not going to move across all the functionality in one go. So you want C++ and Blueprint to be able to collaborate and work together at least for a while. The best way to do this is to have your Blueprint inherit from a C++ class. Now you'll notice this class has got a U prefix in front of it. That just means it's a component that's more about the prefixes in a link against this module. Now you might be saying, but hang on, the Blueprint's already there. I've got to reparent. That doesn't seem to make sense. Surely I could just have my new class, for example, this a quest manager inherit from the Blueprint. Well, no, we can't. And the reason for that is that at compile time the C++ has no idea about Blueprint. There's no real understanding of what is in the Blueprint world. So what we end up with is that we have to inherit from C++ always. And what we've got is these two layers, the C++ layer and the Blueprint layer. So the Blueprint layer can interact with C++. It can interact with other Blueprint classes, but the C++ layer can only interact with other C++ classes. And that might sound very restrictive, and it would be where it not for one caveat, which is that runtime calls are exempt from this. So, for example, the grabber in C++ could make a call to a Blueprint function or Blueprint implementation of complete quest. But the important thing to note here is that this is a runtime call. It still has to be backed up by interfaces that are in C++. So what we'd end up with is grabber is really calling to a function that it knows about the interface from C++, for example, the a quest manager. And the BP quest manager is simply implementing that interface. So the runtime call can cross the barrier. But the compile time stuff all still has to be pointing in these directions. So the C++ to C++ and Blueprint can go into the C++ layer as we're seeing here. So no contravention of this layering. So let's have a go at reparenting two of our Blueprints. The first one I want to do is the grabber, as we've already seen. So we're going to go to Content, Blueprints, and open up the grabber Blueprint here. Now just for consistency, this one doesn't have the BP prefix. So I'm going to go ahead and rename it with an F2 to BP underscore grabber. And I'm going to do the same thing with the Cuspol3. Now the reason for this is just to make it really easy for us to distinguish in any drop down, whether we're talking about the Blueprint version or the C++ version of a class. So next up, we want to go ahead and create a C++ class. To do that, we go to the Content browser, go to Add New and New C++ class. The first thing it presented with is a choice of the parent class. Now what should we choose as the parent class? Well, ideally, because we're replacing the parent that already exists of the BP grabber, we want to choose the same parent so that we just slot in between. So I'm just going to cancel this wizard and go to the BP grabber. And I would like to open up the class settings and go to the Details pane where you can see that the parent class is currently the scene component. You can also see this at the top right hand of this Blueprint. Now, so that means we want to go and add New Create C++ class and we want to choose the scene component. Now just a quick reminder, the scene component is the one that has a scene transform as opposed to an actor component that does not have any transform. Now, it's hit Next and we want to give this a name. Now, we just call it a grabber without any sort of prefix because the prefix will automatically be added by this wizard and go ahead and create class. Now, what this is going to do is it's going to generate the code. It's going to generate a Visual Studio project file and it's going to compile all of our C++ code and load it into the engine. So once this has done compiling, it's going to load up Visual Studio with the two files it's just created a header file and a C++ file. And the header file and C++ file have some stuff in them already prefilled out. I'm not going to worry too much about the stuff that's already in there, but we are going to just have a little bit of a look at some of the stuff that might be unfamiliar to you as a C++ programmer. And that is these kind of pre-processed directives. They look like macros and actually some of them are macros and some of them are being used by the Unreal header tool to remove boilerplate and allow us to do cool things that we can't usually do in C++. Now, you'll notice that this means that we've got generated bodies usually in C++ classes that we have this blueprint to C++ API that goes in front of our, or just after our class and in front of our class name. And that will be different depending on your project. And then we have this U class directive right at the top which gives us metadata about the class. Now, I don't expect you to understand what's within the brackets at the moment, but these are class specifiers. The final thing that you'll notice is a slightly odd about this class is that we've got this include for grabber.generated.h. That's a file that's generated by the Unreal header tool automatically for us with a bunch of boilerplate in it. This always has to be the last include in a header file. So now we would expect that given that we've got this class, we've compiled it, we should be able to go and repair it to it in the BP grabber. The way we do this is we go to the class settings again, go to the parent class and just change it. We'd look for the grabber. But you'll find that it is not there. Now, the reason that it is not there, even if we restarted the editor, is because this C++ class is not set to Blueprintable. That means that it is not derivable in Blueprint. And that is one of the class specifiers that we can actually set. So it's a comma-separated list, so we can add a new item to this list and just add Blueprintable with this capitalization. Now, I'm not going to give you a full list of class specifiers here. You can find the full list linked against this module. However, it is important to note that the Blueprintable specifier is inheritable. So if you seen component had been Blueprintable, then the UGrabber wouldn't have needed me to explicitly mark it as Blueprintable. We'll see that later on when we are deriving from actor. So now, let's go over back into Unreal and try and repair it again. So we're going to go to the drop-down and we're going to look for the grabber. Now, this time it's not showing up, and this is because I need to reboot the engine. And in some engine versions, that is the case, and some it is not. So I'm just going to go ahead and save what it's asking me to save and restart the engine. So now that we've made that change, it's important to make sure we build the project. I'm going to do this using Control Shift B, or you can do it in any of the number ways, including in the Editor using the Compile button. And once it's successfully compiled, let's go back into the Editor. Now, if I go to Class Settings, Parent Class drop-down, and search for Grabber, you can see that we've now got a C++ base class there, and we can repair it to it. So now we have got that parent-child relationship, and we'll be exploiting this relationship for them to be able to communicate later on. However, now let's embed the habit and try and repair the other class here, which was the Quest Manager. So we'll go back to our Content Browser. We're going to search for where the Quest Manager is. It's under Quest BPQuest Manager. Let's open that up, and let's go and have a look at our class settings and see that the parent class here is an actor. So they'll go back to the Content Browser, add new C++ class, and choose Actor as our base class. Then we can go ahead and create the name for the actor, which is going to be just Quest Manager. And I can even choose a folder to put this in. I'm going to put it in the Quests folder, just to mimic the structure that we've got for Blueprint. And I'm going to go ahead and create class. And again, it's going to generate the C++ code and compile that code. So again, it's launched up some very similar looking code that has been pre-generated for us. Again, we've got the generated.h. We've got an empty U class directive, and we've got a generated body. So looking very similar, the difference being that we're inheriting from actor rather than U scene component. Now, if we go in to Unreal and to our BPQuest Manager, and we can actually reparent straight away without adding Blueprint. So let's just go and have a look for Quest Manager, and you'll see it's right there. Now, the reason for that is if we go and have a look at the definition of a actor, I'm doing that using F12 in Visual Studio to jump to the definition, then you'll see that it's U class directive already includes Blueprintable, which is to say that that Blueprintable metadata has been inherited in a Quest Manager automatically. We have created base classes both for the Grabber and the Quest Manager, and we have reparented to them in preparation for the work to communicate between Blueprint and C++.