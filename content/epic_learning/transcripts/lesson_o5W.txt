In this video, besides putting our newly created scale file off to good use, we are also going to give the option for it to make no change to mesh scale should the relevant checkbox be left empty by the user, which we can make a start at, but first of all dragging out the cluster scale file a variable, then choosing the get option when prompted, we can then drag from its output, and add a select float node as well. The reason for this is that it will now allow us to choose between using the scale variation that we worked out in the last video, or, if the Boolean is set to false, selecting the b value that we will set to 1, meaning no change in scale, and we will pipe the clamp nodes return value into the input. As we also want to make sure that we get variation in our scale, we can drag out our scale min and max variables, and again choose the get option when prompted. We will also want a random seed node, now we could use the original instance we dragged out here in the float, but for this course, and so that new users have some clarity, I will just add a new instance of the variable here. Dragging from the scale min port, we can search for and add another random float in range from stream node. We can then make the max variable into the max input, and then the random seed variable into the stream input. We next want to combine the cluster scale logic and the min and max scale logic together by dragging from the select float node, and then add a multiply node that we can then hook up to our two flows. Finally to put all of this to good use, let's drag off the multiply node, and search for and add a make vector node, and then pipe the result into all three inputs. This is now creating our object scale. Now we'll be able to see the far left in action in just a little while, but for now, we can be happy knowing that we've been able to tell the system what to do whether this Boolean option is set to true, so in other words we have scale far left, are a set to false, and have just random scale variation as normal.