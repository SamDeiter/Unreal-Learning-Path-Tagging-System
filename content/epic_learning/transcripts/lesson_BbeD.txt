Picking up from where we left off with Gaze and Dwell, we're going to expand on what we've built with a new mechanic to communicate with the gate using Gaze and Commit. Previously, we spoke about Gaze and Dwell. This is a method of interaction that relies on a time to determine intent, sort of like leaving the mouse hovering over a button until it does something. Now, regardless of how long we look at the button, nothing will happen until we actively commit to pressing it. Let's jump right in. Open our BP pawn. After the IGaze tracking event, see that we have a GitGaze data node followed by a branch. The return value from GitGaze data will be false if the HoloLens2 cannot detect our eyes, thus not being able to provide any data. Because we are calling this event every frame, most of the time it should be true. So, we're going to use these brief false returns to detect when a user blinks. Create a new event named DetectBlink and add a booleanput named GazeTracking. Call it right after the GitGaze data node and plug the return value into GazeTracking. Alright, as I mentioned, most of the time the return will be true, but when it's not, we need a way to keep track of it. Create a new boole variable named EyesClosed. Create a branch from GazeTracking and from false, create a new branch with the condition of EyesClosed. From false, set EyesClosed to True and create a new delay node. Because we are capable of blinking very fast, let's only allow about 0.15 seconds for a blink. Then check the value of EyesClosed with a new branch. Now is where we will count the number of blinks by the user, so we can create a sort of single, double, or triple-click logic. Create a new int variable named BlinkCount. If EyesClosed is false, increment BlinkCount by 1. What we are saying here is that the eyes are only allowed to be closed for 0.15 seconds, otherwise the logic will not count as a blink. We only want a maximum of 3 blinks, so create another branch and check if BlinkCount is greater than 3, and if so, set it to 0. At this point, we have an int variable cached with the number of blinks from our user. Now we need to decide what to do with this info. Create a new event named userBlinkT and call it from false. This combination of gazeTracking and up to 3 blinks will allow for a lot of possibilities in our projects. Return to the beginning of our detectBlinksEvent, and from true, create another branch from EyesClosed, and again from true, set it to false. As we've done previously, this will prevent unnecessary memory usage as this will be called every frame. Next, create a retriggerable delay of half a second. Each time the user closes their eyes and reopens them again, this timer will restart allowing them multiple blinks. When the delay ends, set BlinkCount to 0. Alright, now onto the userBlinkDevents. Get a reference to BlinkCount and create a switch on int node. Add three new pins and set the start index to 1. Now we have three routes our logic can take based on how many times the user has blinked. From here, we will call the toggle gate event we created in the previous module. After the eye gazeTracking event, find the line trace. From outHit, promote to a variable named eyeTrackingHit, then disconnect the branch. Back to our switch node. From three, get a reference to the eyeTrackingHit variable and split the struct pin. From eyeTrackingHitHitActor, cast to BPIslandGaze, then call toggleGate and plug eyeTrackingHitComponent into the component input. Let's quickly review. What we have created is a way to detect if the user blinks. If so, count the number of times blinked and if the number is 3 and they are looking at the button, open the gate. Before we try it, open BPIslandGaze. We need to remove the time restraints we put in for handling gaze and dwell. Simply change the execution line from the first branch and plug it directly into set visibility. One more thing. Open the viewport and select SMButtonA push. And pull it out just a little bit to help ensure that the line trace can hit it. Let's try it. I'm going to look at the gate but not the button and blink. Okay, nothing happened. What about double blink or triple blink? Again, nothing. I'll look directly at the button and blink only once. And nothing happened. How about a triple blink? And there it goes. Our friend can get their treasure. Now it does not matter how long we stare at the button. We must commit to the action for something to happen. In this module, we modified our tracking logic to accommodate a gaze and commit system. We also added the ability to count the amount of blinks from a user giving us many possibilities for many types of contextual interactions. Next, we will develop a dynamic solution to create a system of gaze and dwell in some situations and gaze and commit in others.