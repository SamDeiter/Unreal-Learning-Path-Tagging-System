The process of bringing intractable 2D widgets created in UMG into our 3D scene is as easy as adding a couple of components. Bringing them into 3D will allow us to communicate directly with the widget by communicating instead to the blueprints that contain them. In this module, we will create a new blueprint to render our 2D widget in 3D space. We will be adding a widget component to a new blueprint. We'll discuss the various ways it can be rendered, and lastly we will discuss how the editor handles the size of widgets. Inside of the UI folder, create a new folder named Blueprints. Inside, create a new actor blueprint and name it BPWidget Blueprint. Open it up and add a widget component. This component will allow us to render our 2D widget in 3D. In the Details panel, set the Widget class to our W Modify Lights. Remember that we set the scale of it inside of UMG to 1280x720. Here it is being set to 500x500. To set it to render at the size we designed it in, set the draw size to 1280x720. Unreal Engine is going to translate the pixels used to the units Unreal Engine is using in 3D, which is centimeters. Return to the map and drag a BP widget blueprint into the scene and see how large it is. Let's rotate it so it faces us. We're definitely going to need to scale this down, but let's scale the widget and not the blueprint so we can reuse or spawn it without worrying about the scale. Before we do that, select the island in the middle here and copy its location, paste it into the location on the widget blueprint. So we can see that we need to both scale it and move it up a bit. Return to BPWidget Blueprint. Change to the front view in wireframe and we can see the bound of the widget at this size. Pull it down to the same scale as the rest of the assets we've worked with so far. 0.05 and place it right above the pivot. To get rid of the scene root that is preventing us from moving the widget around, open class defaults and set editor billboard scale to zero. There we go. Now we can move it to where we want it. Compile and return to the map. Here is a much better scale of our UI. I think though that it would be very beneficial to have a background so we can't see directly through it. There are a couple of ways we can do this. Open back up our BPWidget Blueprint. Select the widget and in the details panel, scroll down to the rendering section. Here is an option to change the background color. Set it to black, but see that nothing is changing. The blend mode is currently set to masked, which will read the graphic data from the widget and render each pixel as either on or off. So we would be unable to have a transparent pixel. We can set it to a pick and that will give us the background, but we'll still be unable to have any transparent pixels if we needed them. If your design does have pixels in it that must be transparent, you'll need to set the blend mode here to transparent. There is a better way to add a background that is within the widget and would keep all the widget-related mechanics together, which is a much smoother communication workflow. Set the background color back to default and the blend mode to opaque. Return to the UMG Editor and right-click on Vertical Box. Select Wrap with Overlay. The Vertical Box has a new slot overlay options in the Details panel and it has defaulted to the top left. Set it to span both horizontally and vertically. From the palette, we can now add an image to this overlay. Span this the same way and change its color to a dark blue. Keeping the hierarchy in mind, the lower a widget is in this stack, the higher up to the surface it is. So we need to place this behind our Vertical Box. Compile the widget. Now return to the map. We can see our light editor widget in 3D space. Let's press Preview and VR to see it in the HoloLens 2. Despite its minimalism, I think it looks great. Keep in mind that this is now a blueprint that we can interact with. See how our handbeams collide with it. Let's quickly add a Bounce Control to the blueprint. After adding the component, set the config to Bounce Control Slate 2D. Open this config. It currently allows for non-uniform scaling, but we want this to be uniform. So set this to True. The last step will be to change the Collision Profile of it, so it does not prevent us from interacting with the buttons and slider. Open the Advanced Options in UXT Bounce Control and set the Collision Profile to None. Save and let's see it again. Okay, much better. We can now also rotate it around its Z-axis and scale it from afar. In this module, we created a new blueprint that allowed us to render our 2D widget in 3D and place it in our world. We learned that Unrelangent translates pixels to centimeters and scaled our widget to make sense for our situation. We discussed widget rendering and created a background inside of UMG. Lastly, we added Bounce Control to the widget, allowing us to uniformly scale and rotate the widget in the map with our hands near and from afar. Next, we will expand on our widget and our blueprint so that we can interact with them to change the colors and intensity of the lights.