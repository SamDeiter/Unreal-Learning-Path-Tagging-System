Welcome to two-sided Materials with Trim Sheets. I'm Paul Neal for Epic Games. We'll explore how to create a configurable sign material using a Trim Sheet with 16 different sign options and a separate back material. This sign will be using a simple, one-sided plane and set up a material to show both the front and back faces. We'll also be able to control the damage mount for the shape of the plane using noise-based displacements. Before we get started, let's have a look at the texture that we'll be working with. From the textures folder, double-click on Sign Trim. Our sign will be a simple square plane that we'll be working with. And our texture will have 16 different options for the sign that we'd like to be able to display. Since not all the signs are square, we also have an alpha channel stored. And this will allow us to cut out the sign so that it's transparent where we need it to be. Next, we'll need to create our material, so in the Materials folder, we'll right-click, choose Material, and we'll name this M underscore from Material, Sign Example. Can then double-click on it to open? We'll dock it to the top. First, we'll need to expose the opacity mass parameter so that we can use the alpha channel in our texture. From the Details panel, under Blend Mode, you can see it's set to opaque. We'll open this up, we'll set it to Masked. Mask will allow us to have that access to the opacity mass channel and utilize our alpha. We'll also want to check the two-sided option, since by default, a single-sided plane can't be seen on the back side of it. This will allow us to apply a material to both sides of the asset. We'll need to add four textures that we're going to be working with. To do this, I'll hold down T on the keyboard and left-click to create a Texture Sample node. From the Details panel, we'll click on the None and we'll search for Sign. We can find the Sign Trim and we'll need a second one for the Sign Trim Normal. Then we'll also need two more. The first will be called Paint. I'm using a basic paint-stree-he texture to be able to create the metal look on the back. This paint texture can be used multiple different ways. We'll also need the Normal map for that as well. We have the four textures we'll be using for the entire material. We can now organize these. We'll put the Sign textures on top of the metal texture that we're using. We want to mix these together so that they're mixed 50-50 right now with a Lurp node, which stands for Linear Interpolate. Hold down L and left-click and I'll take the RGB output of both textures into the AMB channel of the Lurp. We'll do the same for the Normal map as well. Again, L on the keyboard and left-click and then the output from the RGB channels again. We can directly take the output of the Lurp now into the Base Color and into the Normal map channel so that we have those already connected and we'll be able to see what the result is. We're mixing them together. You can see that the result is this mixed mess of the two maps, 50-50, mixed between them, so we're really not seeing what we want yet. For the Alpha of the Lurps, we'll use a two-sided Sign node. A two-sided Sign node returns the forward-facing normals, as well as inverted normals for the back faces. We'll want to clamp this value off between 0 and 1 and to do that we'll drag out, we'll type in Saturate. Saturate node will clamp that value between 0 and 1. And we use this because it's free on modern GPUs to calculate. We'll also need to invert the output so that the Sign texture shows up in the front and the Metal texture shows up in the back. And to do this, we'll use the 1-minus and we'll take the output of the 1-minus into the Alpha of both of the Lurp nodes. And with a showing on a plane for our preview, you can see now we have one texture on the front and the other on the back. We may want to change the textures in our final signs with the material instances. And to be able to do that, we want to turn these into parameters. So I'll right-click and say Convert to Parameter. And then we can type in names for these. And this will be Sign Texture. And for the normal wall, we'll call this Sign Normal. And then for the back material, we'll do the same thing. We'll call this Back Texture and Back Normal. We can then select them all, press C to create a comment, and we can group these together in a sense visually so that we can easily find them and know what they're doing within our material when we come back to it. We'll call this one Base Color and we'll call the Normals. Normals. And then we'll select the ones that are handling the two-sided filtering and call it two-sided. With the textures, we want these to show up in order in our material instance. And to do that, we can set a priority for them. Our Sign Texture should be on top, and the sort priority for all by default is zero. I can then choose the Sign Normal and set this to a priority one. And then the Back Textures as two and three. We'll also group the textures so that when we look for them in the material instance, they'll show up altogether. In the Group Dropdown menu, we'll type the name Textures and Press Enter. At this point, we'll add control for our metallic specular and roughness. The specular needs to remain at zero, so we'll create Constant Vector 1 by holding down one in the keyboard and left-clicking and take that into the specular. And then for the Metallic and Roughness, we'll create two more Constant Vector 1s with one on the keyboard and left-clicking. And we'll take those into the corresponding Metallic and Roughness connections. Select them both. We'll call this Metallic and Roughness. With them both selected, we can group them together so they're easily found in the material instance. We'll call this Surface. And now we're going to set up the Controls so that we can choose which Sign Trim element is showing up on the plane in any given time. To do this, we're going to use a node called Flipbook. Right-click and type in Flipbook. Now the Flipbook node is usually used for animating particle systems with something like Fire, for instance, where there will be many pictures of Fire at different states, and it would loop through them during playback. In this case, we're going to use it to control which one we'd like to see by putting in an index value. We'll take the output of the UVs into the UVs for both the Sign texture and the Sign Normals. And you can see in the display now, our texture is automatically looping through four elements at a time within our texture. We can control this by using inputs for the animation, the rows, and the columns. We'll start with the rows and columns with a constant vector 1, hold down one on the keyboard, and left-click, and we'll select both and right-click and convert to parameter. We'll call the top one rows and the bottom one columns. And those will be used to input into our rows and columns input in the Flipbook. The defaults for both of these will be a value of four since we have four rows and four columns. You can see that Flipbook is now showing us each one individually as opposed to four to time. We then need to control the animation phase input with another constant vector 1, so one on the keyboard, and left-click, and take the output into the animation input. You can see now we're just displaying the stop sign. With our new constant vector 1, we'll right-click and convert it to a parameter, and we'll name this index, the index, of the trim item that we'd like to use within the texture. The animation phase of Flipbook starts from zero and goes to a value of one to go through all the 16 different textures within our texture map. This would make it very difficult for an artist to figure out what value they should type in to display any given element. It would be much better if it was simply an index as in 0, 1, 2, 3, 4, and so on, up to a value of 15 to be able to show all 16 elements. To do this, we're going to need a multiply node. That's M on the keyboard and left-click, as well as a divide, which is D and left-click, and we'll take the number of rows into Channel A of the multiply, number of columns, into the B channel of the multiply, and then divide the index by the result of the multiplication, and then take the output of the divide into the animation input of Flipbook. The index now will return the correct values. Zero will return the stop sign, and one will return the yield. And 15 will return the no-you-turn sign. We'll default this to a value of zero, so it shows the stop sign. Once again, we'll set the priority order, so index will come first, and then the rows, and then the columns of the value of two, select them all, and group them under inputs. We'll then select them all and add a comment pressing C, and we'll call this UVs. As you can see, the output of the sign is showing correctly on the front and back, however, the alpha is not cutting out the shape of the stop sign or any of the others. Our sign texture has an alpha channel built in, and we expose the opacity mask in our material, so they'll take the output of the alpha into the opacity mask. This will filter out the shape of the stop sign for not only the front, but also the back. Now we need to control the damage. The damage is going to use the world position offset input of the material. World position offset controls the position of the vertices within the model. For this, we're going to start with a noise. So our right-click and type in noise will generate a random noise pattern. To control the size and position of the noise, we'll need to get the local position of the asset that the material is on. We'll right-click and type in local position, then we'll also need an input value to control the scale of the noise. So we'll use a constant vector 1, so 1 on the keyboard and left-click. We'll right-click on the constant vector 1, and we'll rename this to damage scale. Then we'll multiply the local position and the damage scale together. So, aim on the keyboard and left-click, take the output of our two nodes into the multiply, and the output of the multiply into the position of the noise. We'll need a way to control the amount of damage that we'd like. So once again, we'll need a constant vector 1, one on the keyboard and left-click, and right-click, convert it to a parameter, and we'll call this damage amount. We'll multiply the output of the noise with the damage amount, so aim on the keyboard and left-click, and then take the output of our nodes into the multiply. The final step is that we have to ensure that we're pushing the vertices of our asset along their vertex normal when we're displacing them. So we'll need a vertex normal WS node. The WS stands for world space, and this returns the direction that the normals are pointing in. Once again, we'll use a multiply, and we'll multiply the output of our noise, and the vertex normal WS together, so that we know they're moving in the correct direction. And we'll take the output of the final multiply into the world position offset. As default, we'll set the damage scale to quite a small value, 0.02, and we'll leave the damage at a value of 0. Once again, I'll select all the nodes, we'll create a comment, and we'll call this damage, and we'll select the damage scale and damage amount. Again, we'll group these into a group called damage. With our material complete, we can create a material instance. I'll find the M sign example we're working on in our materials folder, right-click, and choose create material instance. We'll remove the underscore instance that was added to the name, and we will change the M underscore to M underscore from material instance. And then drag and drop that into our sign, which is just a simple plane in the scene, and double-click on the instance to open it, and we can adjust the parameters for it. Under inputs, we have the index, and you can see now we can dial in the index of the item we'd like to be able to see. We can also change the textures because we've exposed those as well to the material instance. And in this case, we wanted to change our sign texture to another one that only had one column of information. We can easily change the columns as well to a value of one, and you can see that the index still functions correctly now, and steps through each row. This allows the material to be very flexible, and allows to use it over and over again in different projects. You can also dial up the metallic parameter, give it a little bit of roughness, as well as dial in an amount of damage into the sign as well, so that it deforms the sign, and looks like it's been beaten up a bit. We can also change the scale of the overall noise as well to determine how we'd like it to look. Thanks for following along with two side of material with the Dream Sheets. We had a look at a simple way to control customizable signs using a simple plane, 4x4 tile texture, and using the flipbook node to control which index the texture is being used. Creating flexible materials can speed up production, and make it easier to develop complex levels.