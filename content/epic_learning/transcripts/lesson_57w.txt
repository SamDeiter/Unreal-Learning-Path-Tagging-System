If you're going to be following along, we are using a blank project with the starter content added. We're simply going to use this as a generic project so we can create our editor utility widgets. The first thing we're going to want to do is, well, create an editor utility widget so we can look at it and understand it. I'm going to go ahead and create a new folder in my content called widgets so that way I can store them in their own location. Now if we right click in our content browser, we can go to editor utilities and we can click on editor utility widget. I'm going to name this one E UW for editor utility widget file rename because that's going to be what I'm going to be doing. And we can go ahead and open it up. So if we double click, it's going to open up the editor. Now if you worked with UMG, this should look familiar. It's the same editor. It just has enhanced abilities for editor scripting because at the top right we can see the parent class is editor utility widget. You have all the same UMG tools. We can use layouts. We can use anchoring. But for our example, since we know where we're going to put it and we just want to make this a little bit quick and dirty, we're just going to drop a few buttons in and go from there. So in the top left corner, I'm going to go ahead and drag a button in and drop it and then I'm going to drag some text onto the button itself. I'm going to click on the text in my hierarchy and rename this to well rename. Click back on my button itself, click on size to content and then we're done. It doesn't need to look fancy. This is just an editor utility. No one else is going to see it except people working on it. We just need something we click on. If we go to our graph, this is where all of our key features are going to be. I'm going to delete everything that comes with it by default. We will click on our button in our variables list and we will choose on clicked to add a clicked event for a button. Now it's a little bit ugly. It's called button 171. Let's go and rename that. We'll go back to our designer. We'll click on our button in the hierarchy. We're going to rename this to button underscore rename. We know we're going to have another button later for our example. So calling a button rename allows us to easily identify it. We'll go back into our graph and then we'll start working on our code. Now there's something important here. This is just the editor. How do we actually run the code when we actually want to use it? If we save and compile and we go back into our main level and we look at our content browser, we have our widget. But if we double click on the widget, again, we go back into the editor. So how do we actually run it? Going back to our content browser, if we right click on the widget at the top, we have actions run editor utility widget. When we do that, it opens a brand new window tab that we can put wherever we want that has our button. In this case, I'm going to dock it and the top left next to my modes so I can easily access it. Now if we click on it, nothing should happen because we have no code hooked up. One nice thing is after you've ran a widget, if you ever want to run it again in that project, you go up to window, you'll have a new tab under general called editor utility widgets, and any of your widgets will show up in that list. If I was to go ahead and start programming and setting up my editor utility, I'm going to run into an issue and let me show you that. If I right click and I start typing in get selected, we're going to get a few options in here. However, we're not going to get the exact ones we want because I forgot to do something. What I forget to do, well, what I mentioned in the previous video, if you don't enable the plugin for editor scriptings, you're not going to have access to all of the options that we needed want. So if I go back into my project, I go to edit, plugins, we type in scripting, you'll find my editor scripting utility's plugin is not enabled. Now, I'm going to quickly enable this. We'll be right back. And after a quick restart, we now have our editor scripting utility plugin enabled. If we go back into our utility widget, if you had to close and restart, you'll have to redo this. If not, good for you for paying attention and turn it on when you start a new project. I'll open up our widget itself that we want to work on by editing it. We'll dock it to make it easier and go back into our graph view. Now if we were to right click and type in get selected, you'll notice we actually have a new note here called get selected level actors. That's from the level utility editor scripting. So that's important to note, without that plugin enabled, you are going to have this options, you're going to have access to less of your asset information and you may run into nodes that simply don't exist. Now that we have that out of the way, let's get started for real this time. So what are we going to do? Our goal is going to be, if we go back in and look through our starter content and we go back to our shapes, our goal is going to be grab any of the selected content in our asset browser. So if you're out of if we want to rename it, so is it something we want to rename, how do we want to rename it and then rename it? First thing first is we need to get the selected assets inside of our content browser. So let's do that. We'll go back in, we'll right click and we'll type in get selected and one of the nodes is get selected assets, which grabs all of our selected assets. We'll go ahead and hope that up and now we have all the selected assets easily usable. We want to loop through these selected assets and see if they match what we want. So we'll go and drag off of the return value, we'll do a four each loop and now we can connect this up with the execute and now we have access to every individual item we have selected. Okay. Now, how do we know if it's the right item we want to select? Well, we're going to make a variable map that's going to map a class to a string. Let's do that. If we go to variables, click on new variable and we'll call this one the file, rename map. And this is what we're going to use for renaming our files. We want to be of type class for objects and we want to be of type string for what we're going to rename it to. In order to do that, let's go ahead and go to our variable type on our list here. We'll click on it. We're going to type in object and we're going to find the actual object object. So it's this one right here where it says the base class for objects and we're going to want the class reference. We don't want the object reference. Object references are things that actually exist. Classes are what we use to make objects that exist. So we'll choose the class. It's the one in purple. Make sure that's selected and now we have the ability to select the class. But now we want to say, hey, if you're this class, we want to rename it a certain way. If we click on the icon to the right of the variable type, it gives us a drop down to change the type. The bottom one will be map or dictionary. Now if you don't have this option, make sure you have object class selection. Not all variable types can be set up as a map. So we want to make sure it's object class first so we can turn this into a map. And then for the second variable type, we want to make this a string. Go ahead and compile and save so we have access to our variable. And now we're ready to set it up. We're going to add a default value. So we'll click the plus next. We'll go ahead and go to where it says none. We're going to type in static mesh. And we're going to find the static mesh object type right here. Once we click on that, how do we want it to be renamed? So hey, if you're a stack mesh, what's your name supposed to be? Well, we want it to be SM underscore and then the name of the file itself that we're renaming. How do we indicate that though? Well, we're going to make a parameter itself. We're going to use some braces. We're going to put something inside the braces. And then when it's searching, it's going to say, hey, if you find this inside the braces, replace it with this. So we're going to replace one thing with another. I'm going to use the brace, FN brace to close out. Now, I'm going to go ahead and open up note pad here and show you that this is what I'm looking for. And you can see this is what we want. So static mesh underscore brace, file name brace, and what that's going to end up doing when we replace is do stack mesh underscore and then file name. Simple as that, simple replace. Now that we have this saved out, we're ready to go. And the nice thing about this is the way we're doing this is we can easily add in multiple items to our map. We just simply look through it and then we can rename it based on our string that we've set up in the map. We need to figure out if this item here, so the individual item we've selected out of all of our items we have chosen is of the right class, which class will this one here? And the easy thing is we can just drag off type in class, we can see get class. So we're going to get the class of this item, and we're going to compare it to the class that's inside of our file rename map. Drag out of the variable, get it, and here is our find file rename map. Drag off of that type in find, click on find, and now we're going to look through this map for a certain class. And that's easy enough because we can hook up these two nodes. And now we can get class, look through here in the file rename map, and we're going to see if it exists. So if we've selected something that's not inside of our file rename map, it's not that class, say a audio file or blueprint, well, nothing's going to happen. We can do that by using an if, so drag off of the find, doing if, hook this up from our loop body. And now we have a simple loop that's going to say, hey, does it exist inside of our map? If so, do something, if not, do nothing. Now the do nothing means we're going to ignore it. So we're not really worried about that. But the do something is something we're going to want to do. And this should be easy enough, we need to get the found name, and we're going to replace it. So if we typed replace, we have a bunch of different options. At the bottom under string, we actually have replace, replaces all occurrence of a substring. So part of a string with something else. We'll click on that and now we have our replace node. We need the source string that we're going to look at. What we're going to replace and what we're going to replace it with. Now the nice thing is, source string is really simple. Our map, if it matches, gives us back that source string. We're going to add our map again. Remember we have a class and then we have a string that we want to use to replace. We'll drag off of the find and pull that into our source string. Okay, what do we want to replace from our source string and what do we want to replace it with? Well, inside of our source string, again, remember we have brace, FN brace. So we'll put that into here. Brace, FN brace. I'll go ahead and zoom in a little bit more so we can see that easy enough. We're going to take our source string. Anytime brace, FN brace shows up, we're going to replace it with something else. What do we want to replace it with? Well, the file name of the object that we selected. If we go back to our objects, our static mesh, you can look, we have a path. We have a path to where that object's at and then at the top, we have the file name. In this case, shape underscore cylinder. And the nice thing is there is some functions built in to grab that information and give it to us. If we drag off of the object, we can go ahead and we can get the path name. And this will give us the path name of the object we have selected. Inside of here, it actually gives us a bunch of stuff. So we get the full path name, including the file, but we only want the file. So how do we get just the file? Well, again, we have another nice function off of here called split. And this is going to go ahead and split out the information. Now note there's two of them, string split. That splits a string and path split splits out the path. We're getting a path. So make sure you use the correct path one. This gives us the path, the file name and the extension. Of course, we want the file name so we can hook that up to two. And now this is going to go ahead and do all of our actual work for us and give us back a string with the new name. Okay, now that we have the string with the new name, what do we do with it? Right now if we were to run this, nothing's going to happen because we're not actually committing our changes to disk. And that one's easy enough. We're trying to rename the file. So what if we type in rename? And our first one is going to give an editor one called rename asset. Now, we actually have two of them here again. One's rename asset cannot move folder. The other rename asset basically equivalent to a move. In this case, we want the first one for the development editor because this will take in the actual object, which we have right here as we loop through it and take in the new name and rename it. We'll hook this up to true. We're going to go and plug in the replace. So the new name is here and then we're going to drag from our array element on our loop to our rename asset. This is the basics of the code and with this you are done. We'll go and compile and save and we'll test this out. So we'll go into our content browser. If you don't have the widget open, we can go back into our widget folder and we can right click and run it. It's going to load it up. One thing to note about editor utility widgets is if you change the widget itself, the looks of it, not the code, but the looks. You have to close the tab and then reopen it so it refreshes the display. In this case, we haven't added a button, so it's not an issue. Let's go ahead and grab from our shapes folder, one of these meshes, shape underscore cone and click rename. After a short while, we've seen it's not renamed to SM underscore shape underscore cone. And of course, since we're using the node get all selected assets, we can select multiple items. Here's four of them in this case and click rename and it'll go through and it's going to rename all of them. We now have five of them renamed properly. Now one thing that's nice about this, like I mentioned before, it's super easy to expand. So we'll go ahead and go back to our code, go back to our file, rename map, it will add a new extension, we'll add a new mapping. We're going to change this one to blueprint. So we'll look up blueprint. In this case, make sure we grab our blueprint for our class type and this will move on BP underscore brace FN brace. So our boot prints are going to rename to BP underscore file name, just like our static meshes are renamed to SM underscore file name, compile and save out our code. Now let's go back in and look for blueprint to change out. So why don't you use a filter to find our blueprints really easily and we'll look and we'll see we have a few things in here. We actually have one named properly, but the rest are not, which is slightly annoying. Let's grab this effect steam blueprint, click rename and you notice it, rename it. Now to show you that it's working in terms of only finding things in our list, we can start searching for multiple things. So we'll go have our blueprint class filter, we're going to grab textures and we'll go ahead and grab static meshes. So now we have a bunch of things selected. If I wanted to, I could scroll down. Here's a few meshes that are not named properly and a few textures that actually are named properly. I have textures and meshes selected, rename. You notice it did not rename the textures, but it successfully renamed our stack meshes.