Welcome, in this chapter on realtime rendering fundamentals, we are going to take a look at performance. Target frame rate NMS, the frame time, GPU and CPU, profiling basics in the four most common performance issues, which means a draw call, pixel and verdict shaders, translucency and shadows. Let's start off with the target frame rate NMS. If you're new to realtime rendering, the idea of having a target frame rate might be something that you're unfamiliar with. So identify a frame rate that you want your content to run at and then make sure that all the work that you do and all the features that you enable or disable or the render quality you're going for everything you do contributes to achieving and maintaining that frame rate. Typical frame rates we aim for are 30 frames per second or 60 frames per second. But there's also MS. In fact, MS stands for milliseconds and it's the unit that we rather use in frames per second. So rather than measuring performance and how the environment and rendering is running, in frames per second we rather do this in MS milliseconds. That means there is 1000 milliseconds per second. So essentially if you have 30 frames per second, it comes down to one second divided by 30 or 1000 in this case divided by 30 and that is 33.3 milliseconds per frame. That is a cost for rendering a single image. The higher that cost, the lower your frame rate. For example at 60 FPS, 60 frames per second, the MS cost would be 16 milliseconds per frame. So the lower the MS number, the better. This is a better way of measuring performance and this is what we tend to use through the engine and when we are in production. It's the MS values we're paying attention to and they're much more precise in the frame rate. Now that aside, you should also be able to identify how you can measure this in the editor. I'm going to go to the editor here and I'm in the level performance underscore example level. This is in the folder lighting and score rendering, rendering fundamentals, finished assets. In here, in the console or the output log, you can see I have the output log here, the output log might be easiest to use the console as a specific button on the keyboard. It doesn't work unless you have a standard English keyboard, but the output log, which you can get with window developer tools, output log if you don't here to have it open. And in there at the bottom you can type commands. If you are the type start FPS, you would see the frame rate. You can by the way also achieve this by doing a start, a show of PS there or one of these things. That's the same thing as typing the command, but I find it typing the command easier. Just repeat this. Type start FPS and it'll give you the frame rate. And you can see the frame rate has the FPS frames per second, but also MS, the mini seconds cost. And again, that one is more interesting usually than the FPS. While we are preparing the environment here, you should also enter this T.max FPS 600 or some other large number. This removes the cap on the frame rate, because you can see I have 60 FPS right now. If I enter Tmax FPS 600, it'll be uncapped. I now have around 200 frames per second. If you want to assess performance, it's usually better to uncap the frame rate, because otherwise the cap might hide the effect of what you're doing. So have the cap removed so we can properly identify how much the frame rate is changing, depending on what we're doing. Now, start FPS aside, there is also another one called start unit. So enter start unit in the output lock. I entered into console here, but same thing. And you will get more numbers underneath. In fact, these will say frame, game, draw and GPU. So you get a whole bunch of numbers. They're all in MS as well. Frame is the entire thing. So frame is essentially the same value as that, more or less, because these two are the same. But the game and GPU are the most important ones. Draw is a little bit more special, so we're going to primarily focus on game and GPU. What happens is that part of the rendering is down on the CPU, that is the game. So game stands for the CPU, and part of the rendering is down on the GPU. There are essentially different threats and often even different processors. And the rendering work is the virus. The CPU tends to handle everything that has to do with the position, rotation, the transforms essentially of something in the environment and in the application. That means it's going to calculate the animations. It's going to calculate the physics, it's going to calculate the collision, it's going to calculate artificial intelligence. It's going to calculate when something is being spawned or destroyed at runtime and so on. Everything that results in an object changing position, rotation, scale and so on. That is calculated by the CPU. The GPU essentially handles all the actual rendering. So lighting the rendering of the models themselves, reflections, shaders and so on and so on. Both of these happen at the same time. And it's very crucial if you're going to understand real time rendering performance. How these two work together. If the wood, for example, if your frame rate would be low and you're looking for more performance, you first have to identify if the bottleneck is the CPU or the GPU because it would be pointless to optimize content on the GPU if it's a CPU holding it back. Both of these have to be in sync. So the slowest of the two, that is going to be determining your performance. And that is really crucial to understand. So identify where it's slow because otherwise you will not be able to change your workflow or your assets or start optimizing. Right. Then we should also take a look at profiling basics. So profiling basis, this is already kind of part of it. You bring up start FPS or start unit so you can identify the frame rate and then there are many, many more commands. And this is way too much to dive into for an introductory course. But you have a lot of these start commands, start space and then you have, for example, our H.I. And this isn't even suggested and it might not, it's probably not suggested in the output of the log either. If you type start urh, I don't type this here for you again so you can see it more clearly. Then you get a list of, you get some additional information and values. In fact, another one you can type is start scene rendering, for example. If you want to disable one of the start by the way, you can type it again. So you can see if I enter start urh, I again, it removes that panel. And if I then enter start scene rendering, I get this over here. This might need a bit more screen real estate. And here you can again see the MS cost. So it shows you how much each step takes. For example, lighting drawing takes 0.21 milliseconds. So now I know that the cost of rendering lighting is this many milliseconds per frame. And you have many more stats. It's also accessible here. Top left of the viewport, start. And then essentially all of these are the same things. He can open them up and they will show you specific stats on what's happening. I'll GPU for example, let me hide start scene rendering. Because it is the GPU one, etc, etc. So that is a basic profile. And there's all the ways of doing this too. There is a GPU profiler, hence one, hence one. Bring the numbers back up. And we're back to where we were. Now the four most common performance issues that you will encounter. But because all of these four are really heavy, so you're likely to encounter them, heavy for performance, or they're also easy to do wrong. So either because of the work you do or because the engine in itself, you're likely to encounter these four. In this level that we were working in, we have these four little pots here, which are essentially examples of what we're going to talk about. And this is not any order that are in the slides. It doesn't really matter where we start. But let's start with translucency. Translucency is very difficult to run and very expensive. Here we have a particle that renders different colors, transparent planes. That's all it does. But this is really expensive right now. In fact, the frame rate, and this is why you need to turn off the cap on the frame rate, it's 200 frames per second. You can see if I go in here, it's still 200 frames per second. The issue is with building small example cases. It's kind of difficult to really see the impact of things, but you are having an impact because computers are so powerful nowadays. So what I did here is I made sure that all of these examples are configurable. If you click on the particle in the middle and then look in the properties on the right, you should see a section called instance parameters. Expand that and expand the first entry zero in there. And then here it says, scalar. Scalar is a value. It might not be eight for you, but it's a certain value. Try increasing that because that's the number of particles it's buying. Try setting that to 50 or 150 or some huge numbers you're getting on a lot of planes. And then try to place the camera in the middle. You can see I'm now at 45 frames per second. If I put this to say 20, I'm now at 150 frames per second. So my frame rate went from 150 frames per second to 35 frames per second. And it's only because of this. In fact, it's a more interesting part of it. If I move away, I'm back at 160 frames per second, even though it's still rendering 250 translucent planes. So it's also the distance that impacts it. This is due to pixel shader operations and translucencies for you have the on-that. If you would go to the viewport and you would go to lit or whatever it's up to the third button here, it should be subtly to properly assess these things. Then you go to optimization view modes and shader complexity. You can see it's not even red anymore. It's now solid white. You can see on the bottom here it says good, bad, extremely bad. So it's extremely bad. The every translucent plane gets rendered adds a layer on top of the screen and those pixels have to be recalculated because those pixels have to blend in the translucent layer on top. And for every layer that gets added, there's more and more and more pixels have to be recalculated again and again and again for each layer. And this very quickly makes the pixel shader expense huge. So you use shader complexity and be aware of what translucency is heavy. This is also why in the distance it gets cheaper because there are simply fewer pixels that have to be rendering those translucent planes. So there's fewer pixels to run those calculations on than there are now. And that is the difference in expense. This is pixel shader based. The same thing is true for materials. Again, this is pixel shader based. This statue, we can open up the material here for a second, has an extremely complex material. In fact, it doesn't look complex, but noise with quality 10 added several times on top of each other gives an extremely expensive material. We can see it is expensive because the stats panel window starts. One is this is compiled because it's still compiling and takes a long time because it's expensive. It's going to show us the cost and it's going to be around 2000 I believe in instruction count. That is the expense of materials. And you can see here once it compiled, 20091 instruction, that is the expense. You can see here too. This is 2000. So halfway into this scale is about 1000 instructions. And as you get closer and closer, you can see I'm at 160 frames per second. Switch back to lit mode. 160 frames per second. I start going close to the surface. Performance is dropping. I'm now 90 frames per second and we're around 80ish 90 frames per second now. So again, the more pixels on the screen use a complex expensive material, the bigger the impact of that complex material will be on performance. Again, pixel shader related. Then you've got draw calls, which is a very interesting one. This one, if you click on the little statues here, it can configure those statues. In fact, you can see there's a static mesh assigned to it here on the right. If you click the magnify glass, it will select that one and count on. So some doing that is currently using this. We actually, we have three different statue versions. When the environment is rendered, it will render it not pixel by pixel. It will not render it blocked by block or region by region. It will not render it in lines or anything like that. It's going to render it object or object. If you switch to render doc here for a second, I was used earlier in these courses on rendering. And we can analyze what is being actually rendered on the GPU. This is standalone program. It's not related to Unreal. It's about real time rendering in general, but it can capture data from the graphical card so you can analyze it. I'm just going to switch to the unlit view here. And I'm going to show you how it actually builds on the scene. This is the rendering of the environment happening. And you can very clearly see what it does. It builds it up, mesh per mesh. That's how it renders. So it slowly builds up the image. Model by model. Each of those models is a draw call. So that's what a draw call is. Draw call can be a little bit out of a few other things too, but let's give the symbol and say every model is a draw call. And therefore, the expenses, not in the polygon count of the environment, but in how many draw calls you have. How many separate models are forming the environment? In this case, we have quite a lot of small statues. And you can see what would happen. If you would set the number of statues here to say 750, the frame rate went to 14 because we have many more draw calls. You can figure out what a draw call count is if you type stud or h i. And in stud or h i, you can see triangles drawn. So that's a polygon count and draw primitive calls. Those are the draw calls. It's 8400 draw calls. And you can experiment a little. So the statue with all the colors is low on polygon count, but has many different draw calls. Open it up. You can see every one of these materials is a draw call as well. And every statue will be a draw call. If you assign this one instead, we can see we now have 14 million triangles, but we have 26,000 30 times actually it's more still going up. 40,000 draw calls. We have 10 frames per second. If I switch the exact same number of statues over to this other statue, which only has one material, but is a lot higher polygon count is 96,000 triangles. We go from 10 frames per second to 14, 15 frames per second. And we now have 380 million triangles in view, but only 8,000 draw calls. So there's a lot more to say about the subject, but be aware that it's rendering draw call a draw call and experiment a little by using these three different statues and try changing the number of models rendered. And you'll see what kind of impact it has on the performance. And then lastly, we have dynamic shadows. Dynamic shadows are really heavy. In fact, it's not a dynamic light that's heavy. It's the dynamic shadows that are heavy. It's the same kind of setup here. We have a number of statues. You can again exchange them for different types of statue. I have currently SM statue itself selected here. I have 50 statues placed. That's good enough. Let's just give it a 50, but we can also click the lights now. So click on the lights and all the lights were placed with scripts. So the script randomly places lights in the environment and you can use that script to now say how many lights you want to have and whether or not the lights should have shadows. Because if you turn the shadows off, I go from 70 frames per second to 100 frames per second. The shadows that are making the difference, not the lights themselves. In fact, if you were to delete the lights in general, if you turn on the shadows, then delete the lights on 110 frames per second. The lital lights are at 140 frames per second. So it makes a difference, but not as big as the shadows. The best way you can test this is by assigning a very high model to the meshes on the ground. If I take the statue called rendering performance too, which has a triangle count of 96,000 triangles per mesh, and I assign that one. Let me go back to the lights. Have you turned on shadow casting? You can see what happens to performance. I'm 10 frames per second. So now suddenly the shadows have a huge impact on performance. In fact, shadows, dynamic shadows, get heavier, the higher the polygon count of the environment is. So if you intend to use a lot of dynamic shadows, you need to be more careful with the polygon count on the environment. Whereas if you're going to use only static lighting or no dynamic shadows at all, you can have a much higher polygon count. And it's clearly visible here too. If you change the statue to the lower polyversion, the same statue, then even though the shadows are enabled, we still have very good performance. We save it around again, and it's really slow. So we've gone over target frame rate, frame time, and milliseconds frame time, and GPU CPU, profiling basics, and then the four most common performance issues, which is the impact of draw calls, pixel and vertex shader impact, translucency is heavy, also because of pixel shaders, and dynamic shadows are really slow in particular if you use high polyvincance on your models.