We've now seen the process of exporting our data files from Unreal. So we'll take a look at how we can get these back into the system to see how the engine will process that information and again how we can work with it internally. First, let's take a closer look at the structure of our data files and how we might create them externally if we wanted to take that approach rather than starting inside of Unreal, exporting things, editing them and then bringing them back in. Instead of the item info CSV, we're only really interested in row one just here when looking at the minimum requirements to kind of create what is essentially a CSV data file. So we use this as a template. We can see first of all one important thing here is in column A. We can see that the first row in column A has the three kind of dashes here and these were automatically assigned when we exported this from the engine. The other option that you'll see people take here is to set this to the word name, but name it does make sense as this is essentially what all of this column is needed for to allow us to set a custom name identifier for each of our sets of data. So I'll update this before we import this back into the engine and we'll see what this changes when we do import this. So that's the first important thing that we always want to keep in mind is that we reserve the first column for your naming identifiers and we set this to be either name or those three dashes or left empty. After this column, you're free to name each of the other columns in row one to pretty much whatever you want them to be based on the data that you'll be representing. So if this was to represent a data set of people, this could be things like their name, age, location, job role and so on. And inside of Unreal, what you'd need to do is create a structure or a struct to match this. So it likely be something again like the string representations for all of those to name an integer for the age and then some more strings for the location, job role and so on. And that would pretty much bring us to where we are now. So at that point, you'd have a CSV created externally and struct waiting in your project ready to kind of connect that information together. And the final step is going to be to saving our file ready for the import to Unreal. In this case, it's important that we use the option to save this as a dot CSV file. And remember, when you've saved it, be sure to close Excel if you're using that as your editor. Otherwise, there can be some issues when importing this back into the engine. Next we'll take a quick look at creating a simple JSON file as well. So again, we'll use our existing one as a template just so we have an idea of what we're looking at. This one can be a little bit more difficult to get started. And again, there are plenty of websites that will allow you to provide your data set and then convert that into a JSON layout or format for you. And then just simply copy that into your text file. If we were to do this by hand though, we can break this down into a small number of required steps. The first thing is going to be encapsulating everything inside of some square brackets, as seen here. This indicates that we're essentially using or creating an array of different sets of information. For the array of data, each element containing a group of data will be inside a pair of open and closed curly brackets. We then have some familiar data here to demonstrate how we format the information. To begin, similar to our CSV, we kind of need the default line, which is automatically going to be set to name when we export this from Unreal. And this is essentially column A from our CSV that was set by default in the CSV export to three dashes. Then note that each category of SVS is housed inside of a pair of double quotation marks. These are often called the property name. This is then closed off with a colon and then the custom information that we want to assign to this is placed in another set of double quotation marks. These pieces of custom information are often referred to as property information. When the pair is complete, this line would be referred to as a property. Each different pair of property name and property information are then closed with a comma to denote that the next line will be a new category or a new property. We then essentially go through a similar process, line by line, creating our different properties, made of a property name and the accompanying property information followed by a comma. So it's also important to note that the final property is not followed by a comma. Most passes will expect this to be left empty to signify the end of your current object. The final step again, as with any file is that we're going to ensure that we save this as a JSON file. And if the option isn't available in the editor that you're using, then you can just save this as an unspecified file or no providing no file type and just make sure that you save this with your file name and then dot J S O N. And that's really all there is to creating a CSV and JSON file. Similarly, with the formatting is the main thing. And you can of course add additional complexity and nested array information inside of your JSON. We're required. Returning to the Unreal Editor, we want to navigate to our data folder to get these into the project will right click and select input. As you would for pretty much any other external file that you'd usually bring in, such as audio or meshes. Then navigate to the location that you've saved your data files and first choose to import the item info CSV. This will provide a few drop down options we want to keep it imported as a data table. And then for the data table row, we want to set this to be our item information table from the drop line. As this is the structure that we've created to match our column information. So another very important thing there. If we were to choose one of the other options that don't match our data, you'll get an import error. If you haven't seen that before, you might want to make the wrong choice quickly just to see what the result is. But I'll stick with the item information table to get this work in correctly. We can leave the other fields as their default and then click apply. This is made an entirely new data table named item info CSV. I think this naming should be fine as it makes it clear which file this originated from, whilst we're testing our different options of bringing different files into the project. If we go ahead and open this, we can check some of the changes compared to our original data table. The first thing is that the cell we renamed from the three dashes to name is nice that to a row name, making it much clearer what this is representing. And we can then see that we have the new information about the trophy and the updated information about the rocket radio. We also have the third row named invalid test. And the important thing here is that there were no errors or issues from setting the item type to something that didn't exist. Instead, it's defaulted to be the first element in our enum, which is why I've included the none option. So this is going to make it very easy to detect where a kind of human error might have occurred. If we set a rule that we never want any item to be set to none, then we can know that we can easily come back through and update anything here which might have been mistyped or just forgotten. So I'll update this now as we'll be making this my kind of main data table going forward as it has the most complete information. There are two ways that we can access and change the data. Unfortunately, we can't change anything in the data table panel at the top here, but we can select any row and then fill the information below in the row editor. Alternatively, we can use this drop down which allows us again to set the row that we want to edit. And again, we can fill that information down inside of the row editor. So I'll set the row name to spaceship. Note that this is the one thing that we can and also have to change in the data tables section at the top and not the row editor. This can be a little bit confusing, but you can get used to it. Then we'll also change the item type to spaceship from the drop down and fill some of the fillatex for the item name, item cost, item info and rotation duration. This can be pretty much anything, just something so that we can confirm it's different from the other items when we load this to our widget a little bit later. When you have that complete, just for kind of testing purposes really, let's repeat our import steps back in the data folder. This time we're going to select our JSON file as the template. And again, setting the drop down options to data table and the row type to the item information table and then click apply. With this, we now have the item info json. So open the new data table and we can see the results. Now at this stage, there's not a whole lot for us to see here, as it's pretty much exactly the same as our CSV results, besides the row information being less complete. The first column is again set to row name rather than being at left blank, just because the JSON file already had that set to name, but other than that, that is pretty much the main difference of the process of getting CSV and JSON into the engine. You can see there's really nothing between them. So if that done to tidy things up, I'm just going to go ahead and delete the item info table and the item info json. I then rename the item info CSV to item info table, as this now has more of the information filled out and I kind of liked the original naming convention. Then just before wrapping up inside of the item info table, I'm just going to go ahead and fill out all of the other asset information that we have available up to this point, but that we've been leaving blank up tonight. This is going to be quite simple to fill. Each item has a mesh named specifically for that item. So trophy, radio and spaceship, and then likewise they also have an eye contacture. This one, there are a lot of different textures you may see here. The ones we're looking for are T underscore and then the name of the item they're representing. So be sure also to provide each of them a rotation duration. We're not going to use this right now, but that will be used a little bit later. This is going to be used to set how long it takes for a complete rotation when we have the items in the level and when we've gone ahead and created our curve float class, which is one of the later topics, but we can come back and fill that curve float slot a little bit later. And then with that complete, you've now seen how we can create a data table purely from a struct inside of Unreal and then fill the information internally or how we can set up a file externally and then import it into the engine just as long as we have that valid struct to find in the project waiting to be matched against our data.