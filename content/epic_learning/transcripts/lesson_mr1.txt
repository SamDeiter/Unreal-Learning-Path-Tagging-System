Composure Facilitates Real-Time Compositing by layering objects in the Composure Plugin Framework. Let's start layering Composure by layering how to work with its layer system. Layers are an in-editor concept used to assign CG Actors to. To quickly showcase the layer system, let's drop in the default queue and the default directional light. Let's select the queue, and we're going to drag and drop this into our viewport. And then we're going to go to our Light section, and we're going to drag and drop in the directional light. Now we're going to want to open the Layers tab. To do this, let's go to the Windows menu, and select Layers. We'll now notice that our Layers tab has been created at the top right next to our World Outliner. Let's create a new layer. So we're going to select our default queue, and then in our Layers tab, we're going to right-click, and we're going to choose the option Add Selected Actors to New Layer. We're going to call this layer Cubes and Press Enter. So what we have done is we have assigned a CG Actor to a specific layer. Layers are going to be used with layer elements in our Compositing Framework in Composure. We can toggle the visibility of layers by clicking the eyeball icon. We will also notice that when we select our layer, we also get the information of how many stack mesh actors are assigned to that specific layer. And we can also use it to select multiple actors within that layer if we're working with more detailed scenes. To demonstrate this, let's drag and drop in another default queue. So let's go back to Basic and drag and drop in another queue. And we're going to assign this to our Cubes layer. So with this new Cubes selected, we're going to select Cubes and right-click, and choose Add Selected Actors to Selected Layers. And if we select it, we'll select all Actors on that layer. We will be assigning these layers in each of our Composure Material layer elements. So what are layer elements? Let's talk about the different elements that make up Composure. You have three different types of layer elements, not to be confused with layers. Media Plate, which is used to drive a Media File Source, CG Layer, which refers to any CG actors that you've assigned to a particular layer in the Layers tab, and CG Matte, which can be used for creating mattes from your CG Actors. Inside Layers, you can choose to include or exclude specific CG Actors. It's important to note that the more compositing element layers that you have, the more costly the scene will become, and you will notice that your frame rate will drop. Remember to use the GPU Profiler tool, as it's essential to making sure that you are aware of your frame rate throughout the compositing process. Now let's move on to setting up our CG scenes.