In this video, we'll take the first steps towards having a fully functional AI character that moves around in our game. We'll set up our assets and folder structure before moving on to the creation of the enemy blueprint and AI controller. Let's begin by creating our folder structure. Navigate to content 2D sidescroller BP sprites. Create a new folder called enemies. Open this new folder and make a new folder inside called red. Now open the red folder and create separate folders for each of the animations we're going to be adding. Create folders called air trans, attack, fall, idle stand, jump, run and walk. Now using the menu on the left hand side of the content browser, navigate to content 2D sidescroller BP flipbooks. We're going to do a similar thing here, but for the flipbooks, begin by creating a new folder called enemies. Open this folder and then create another new folder called red. Just as before, we'll create new folders for each flipbook. Air trans, attack, fall, idle stand, jump, run and walk. Next, navigate to content 2D sidescroller BP textures and import all of the TGA files that have been included as source assets. Simply drag them for a Windows Explorer into the content browser. With all the assets still selected, right click and select sprite actions apply paper 2D texture settings. Next, we'll go through each one and extract the individual frames of the sprite. For each one, we'll right click on the asset select sprite actions and then extract sprite. This will bring up the extract sprites window and we just need to make a couple of changes to extract the sprites correctly. When the settings roll out, locate sprite extract mode and change it to grid. Next, in the grid roll out, ensure that the width and height is 64. Now looking over to the left hand side, we can see that we have the correct layout for our different sprites. With that all set up, hit the extract button in the bottom right hand corner. Hopefully, Unreal Engine will keep these new extracted sprites selected, so now we can drag them into the correct folder. This is why our folder structure is useful because the names of these will match up perfectly with the names of the extracted sprites. Now we need to do is repeat the process for every imported texture and put them into the correct folder. For example here, I've got the extracted air trans sprites so I'll drag them into the air trans folder. Once I've dragged them over to the folder, I'll select move here to move the extracted sprites into the correct folder. Now I'm just going to go through all the other sprites and repeat the same process. And with that done, we've extracted all of the sprites from the new texture assets and placed them in the correct folders. The next step will be to take these extracted sprites and turn them into an animated flip book. Let's start again with the air trans animation. Use the content browser to navigate to the air trans folder where we'll see our three extracted sprites. Left click in the main window and press control A to select all of the sprites. Looking over on the left hand side of the content browser, ensure that we can now see the flip book folder structure. With all the sprites in the folder selected, right click and select create flip book from the sprite actions menu. This will automatically create a new flip book with a correct name so we can just hit enter. If we hover over this new asset, we can see it is now a flip book animation. Drag this new flip book animation into the correct folder in the content browser. Now we just need to go through all the folders and repeat the same process until we have all of the flip book animations in the correct flip book folders. Now, let's quickly check our work. In the content browser, if we navigate to content 2D size scroller BP, flip books, enemies, red, we can see our different animations. In the content browser, if we navigate through the different folders, we can see all the different flip book animations playing correctly. If we navigate to content 2D size scroller BP sprites enemies red and scroll through the folders, we can see all of the correctly extracted sprites that made up the flip book animation. And with that, we've added all the animations we need and now it's time to start building our enemy character. Let's begin the process of creating a new enemy character by creating a new folder to store them in. Navigate to content 2D size scroller BP, blueprints and create a folder called enemies. We'll form the basis of our AR character using the player character. So let's start by duplicating the player character and renaming it enemy character. Right click on my 2D player character and select duplicate. From a prompted for a name, name this enemy character. Drag this new enemy character blueprint into the newly created enemies folder. Open the folder and double click enemy character to open the blueprint editor. Let's begin by updating some of the source assets used for this character. From the components panel on the left hand side, select the sprite component. In a details panel on the right hand side, within the sprite rollout locate source flip book. From the drop down, search and select the new enemy red variant. While this won't change the animations in game, it'll make the enemy character easy to spot within the level editor. Now let's start removing things that we don't need for the enemy character. For a start, the enemy character doesn't need a camera system, so let's begin by removing that. Moving back over to the components panel on the left hand side, hold control and select both the spring arm and the camera from the panel and press delete key to remove both of them. Now within the event graph, we can remove all the nodes that are related to controller input. We can begin by deleting everything within that handle input comment section. We can draw marquee box around everything and hit the delete key. To help us find the other bits to remove, use the My Blueprint tab and expand the event graph. Locate input action run and double click to show it in the graph. Once again, use the marquee select within the event graph and hit the delete key. We can use the same method to get rid of input action jump. Move around the graph to locate our camera control section and marquee select all of it and delete it as we won't need any camera controls for the AI. Finally, use the My Blueprint panel to locate the event tick and double click. Currently we have the update camera function which we won't need for the AI, so let's remove it. With the update camera function selected, press shift and delete to remove the node without breaking the link between event tick and handle animation. Finally, we need to remove a function that we're no longer using. Return to the My Blueprint tab and in a function section, locate update camera. Left click to select the update camera function and press the delete key to remove it. With that last function removed, we've removed everything we need to from the enemy character and we can go on to start adding new features. For now though, compile and save the blueprint. Our next thing to take care of is to hook up all of our new animations. Before we make any changes, let's review how it works currently. Using the My Blueprint tab, locate the handle animation custom event and double click it. This will take us to the custom event and we can look at the blueprint. You'll notice as we scroll through, we have a number of selectors with hard-coded values. When it was just a player character, we worked under the assumption that we'd only ever need one set of animations. This is essentially hard-coded to use that one set of animations. What we should do now as we add enemies is code something that's a bit more flexible. We need to be able to handle multiple enemy types and colors. We'll do this by creating variables for each animation we might want to use and plug them in at the correct location. Let's begin by creating all of these new variables. Create a paper flip book variable called Fall animation. Create a new animations category to hold all these variables in. Now let's go ahead and create the other required animation references. So that will be Air Trans Animation, Run Animation, War Animation, Idle Stand Animation, Attack Animation and Jump Animation. Now we want to add default values to all of these variables and to do that we need to compile the blueprint first. Once that's done, go through all the new animation variables and assign the associated animation. All the names should match up perfectly with the existing assets. And with that we've got all of our new animation variables set up, so let's compile and save the blueprint before moving on. Now we have all of our animation variables set up, it's time to correctly assign them within the event graph. Let's start with the Handle Animation Comment block. Moving over to the right hand side of this block, find the two selectors. Locate the first selector that uses the Is Running Ball to switch between Running and Walking animations. If we hover over the false, we can see that it uses the Walk animation and if we hover over the true, we can see that it uses the Run animation. We can drag these new animation variables into the event graph by control left clicking and dragging them into the center. Drag our new Walk and Run variables into the graph this way. Connect them both up to the appropriate connection on the selector, so the Run animation to the true input and the Walk animation to the false input. We need to do something similar for the Net selector. Also using the same process, bring the idle stand animation in and connect it to the false input of the selector. It's worth taking a few moments to use the re-root nodes and reposition everything so things are neat and tidy and lines don't overlap. That takes care of this block, now we need to move on to a few special cases. Using the MyBluePrint tab, locate the on-jump apex-reached event. Here, looking at the update flipbook animation function, we can see the AirTrans animation is called. So once again, we can plug in our AirTrans animation variable at this point. Confidently, the next one we need to look at is just below, the on-the-flipbook playback complete custom event. If we look at the is equals to node here, we can see that the AirTrans animation is used, so we can plug in our AirTrans animation variable. And if we move to the end of this little block, we can see that the full animation is called, so we'll plug that in as well. Confidently, just below this is the event on walking off-legs. In this instance, once again, we can see the full animation is called, so we can plug in our full animation variable. That should take care of all the animations, so we can compile and save. Our next step is going to be to add an AI controller, and to do that, we need to go back to the main window and the content browser, so we can close the Blueprint Editor window. In the content browser, navigate to Content, 2D size scroller BP, Blueprints, Enemies. Select click in an empty space in the browser and select Blueprint class from the Create Basic Assets section. In the All Classes rollout, in the search, type AI controller, and select AI controller once it's visible. In the bottom right-hand corner, hit the green select button. We'll be prompted to name this new AI controller. Let's name it Enemy AI. While we're here and while we have the tile map level open, let's drag the enemy character from the content browser into the level. With the enemy character still selected in the viewport, set the location Y to minus 4 in the Transform rollout. This ensures that they'll be placed behind the foreground layer of our tile map and sit nicely on the other tiles. Now with them placed in the scene and in the Great Location, we can set up the AI controller. Locate AI controller class in the Details panel on the right-hand side in the porn rollout. From the drop-down, select our newly created Enemy AI controller. And with that, we can start a play in editor session. And there we go. We have an enemy character stood within the scene next to our player character. But they're not doing very much. So in the next section, we'll use the AI controller to get something done. In this video, we laid the foundation for a fully functional AI character within our game. We began by setting up a neat and tidy file structure before importing all of our source assets and creating the required sprites and subsequent flipbooks for the character's animations. Using a copy of our player character Blueprint as a starting point, we removed any components and Blueprint logic that we didn't require for our AI character. We then went on to modify our basic animation system to handle the new enemy character animation assets. We finished up by adding an AI controller and associating it with the enemy Blueprint for placing them in the level and testing it with a play in editor session.