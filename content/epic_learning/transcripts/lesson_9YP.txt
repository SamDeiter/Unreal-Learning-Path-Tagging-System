Before our behaviour tree becomes more complex, it is important that we know the fastest and most effective ways to follow the flow of execution through the tree. That way we can ensure everything is working the way we intended. For that reason, we will return to the gameplay debugger and take a close look at the behaviour tree category. We will also take a look at how we can use breakpoints within the tree itself to step through execution, making it easier to understand what is happening. Let's start by taking a look at the behaviour tree window during gameplay. Before pressing play, let's reposition our player in the scene so they won't instantly be spotted by the AI. With the player repositioned, we can now start gameplay by pressing play on the toolbar or hitting alt-p on the keyboard. Now open up the behaviour tree from the content browser. Move to the toolbar and the drop-down that currently says no debug object selected. Click this and select AIC underscore third person character. Note that we can now see our behaviour tree executing in real time. In the bottom right hand corner in the blackboard panel, we can also see our various blackboard keys being updated. Specifically, we can see the target location being updated each time our fine suitable location task is run. Note that sometimes these blackboard values can change quite fast, so it can be useful to take things step by step. Select the weight node in our random wonder sequence and press F9 on the keyboard to set a breakpoint. Note that next time this node is executed, the behaviour tree pauses and we can see this by the pause text in the top right hand corner of the window. Now we can look in the blackboard panel in the bottom right hand corner and see the target location without it updating. We can also use the back into button on the toolbar to move backwards through the executed nodes of the behaviour tree. If we take a few steps back, we can see when the selector was evaluated. Note that the decorator we created on the chase player sequence returned a failed value. This means that our random wonder sequence was selected and the first node of this sequence was executed. We can see this execution flow by the animated lines drawn between the various nodes. Note that if we continue to step backwards through execution, we'll see the target location change each time that that node had been executed. As you can see, this is an incredibly powerful feature. It would allow us to work out how an AI ended up in a certain node within a behaviour tree very easily. As our behaviour tree becomes more and more complex, we'll rely on this feature more and more to debug any issues we encounter. Some of the functionality we've just looked at overlaps neatly with the gameplay debugger. While we are in the editor, the method we have just used is often the most straightforward. However, if you're running a compiled build outside of the editor, or you don't have much screen real estate, then a gameplay debugger is very useful. Run the game as before by hitting play in the toolbar or pressing Alt-P on the keyboard. Activate the gameplay debugger by pressing the key that you bound to it earlier as before. So that we can stay focused, press 1 on the numpad to disable the AI category, leaving only 2 the behaviour tree category enabled. So that all the values remain static, press pause on the keyboard to pause running of the game. Looking within the behaviour tree category, we can see the nodes that we used to reach the current running node in the behaviour tree. We can see that we started at a selector, and then we began the chase player sequence, and we're currently within the weight node of that sequence. On the right hand side of this category, we can also see the blackboard information. We can see that has line of sight is currently set to true. The self-actor is the AI itself, the target actor is our player character, and the target location will be the location of the third person character. Just as we saw within the behaviour tree window, all of this information would be invaluable for debugging an AI that doesn't appear to be doing what we'd like it to do. We'll continue to use this behaviour tree category within the gameplay debugger, going forward through the rest of the course, to help us verify that all of our changes are working as expected. In this video, we learned how the gameplay debugger can be used to monitor the state of our behaviour tree during runtime. We also saw how we could set breakpoints within the editor to monitor execution flow through the tree step by step, knowing how to use these tools will be invaluable as the complexity of our tree increases throughout the duration of the course.