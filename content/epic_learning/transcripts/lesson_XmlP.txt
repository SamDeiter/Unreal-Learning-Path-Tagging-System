Once you have the Datasmith plugin installed, we can export our SketchUp project into Unreal Engine. Now we can do this in one of two places. We can go into extensions, Datasmith, and you'll see an option for export. We also have it in the toolbar, which will be this button. So all you have to do is press the button and export it. Now it's going to default to your file name, in this case, we're just going to call it Modern Cabin. Go ahead and hit Save. And that exports the file. Now if you look at the folder, you'll see that we actually have two things in that folder. We have a U Datasmith file, which is the Datasmith file. But we also have a folder called Modern Cabin underscore Assets. So Modern Cabin Datasmith and Modern Cabin Assets. And then if I go into that Assets folder, you'll see it's all of the JPEG files, plus all the geometry that we use. So this is really everything that will get imported into Unreal Engine. Now once you have this, all you have to do is import it into Unreal Engine. So let's go over to Unreal Engine. And I have a blank architectural project setup. So I've got my main, which is basically just a blank project. But by using the ARC Viz default project, I get that content plus some starter content that I can use. So all we have to do is go ahead and hit Add, Datasmith file import. And then find that U Datasmith file. And once we do that, it's going to ask us where we want to put this. Now I'm going to select the Content folder because this will actually create a subfolder containing all of our assets. Now typically, you'll want to create a subfolder for your content, but this should work. So let's go ahead and hit OK for this. And we want to make sure that we import geometry, as well as materials and textures. Now SketchUp doesn't have lights, but if you do have multiple scenes in your SketchUp file, you can import those as cameras. So let's go ahead and just select Import. And there we go. So as you can see, it creates this subfolder called Modern Cabin. I go in there and that's where all my assets live. So now I have this particular asset. And you can see it over here in the Outliner. So if I select that, I can say move it up above the floor level. If I want to, I could hide the floor so I don't see it. And then I've got my assets. So you can see it came across pretty well. And if I look at this Modern Cabin, you'll see that it basically has all of the groups that I created. And if we go into Materials here in the Modern Cabin subfolder, you'll see that we have all the materials in the scene as well. Now once you have this imported, you can then move on to Materials and Lights to further add to your scene.