Being able to access the feed directly from the PV camera on our visor is the first step to sharing what you see with other people or systems. In this module, we will prepare our BP toolbox to toggle the camera and our BP pawn to handle this mechanic. Let's jump right into our BP pawn. We'll again maintain the data we retrieve from the camera here for easy access from other blueprints. Create a new custom event named Prepare for Camera Capture. Call it at the end of Prepare for AR. We're going to keep track of whether the AR capture is running or not, so create a new bool variable named AR Capture on. What do we call Prepare for Camera Capture? Add a Create Dynamic Material Instance node. Set the parent to our Mcam Capture material that we made in the previous module and promote the return to a variable named M underscore Cam Capture. Creating a dynamic material instance will allow us to alter the parameters within the parent material during runtime. We'll access the Cam Capture parameter from this cached variable. Create a new custom event named toggle AR Capture. Give it one bool input named on off. Plug on off into a branch and from true, search for toggle AR Capture. Set on off to true and set the capture type to camera. This node will request access to the camera data and if successful, set our bool variable AR Capture on to true and connect it to a branch. This will prevent the attempt to use the data if something is wrong. From false, we can create a print string to inform the user that there is an issue. From true, search for set timer by event. We'll create this final event in a moment. Pull backwards from time and create a division node. We want this event to run 60 times per second so we can set it to 1 over 60. Set looping to true and promote the return value to a variable named Cam Texture underscore timer. From false at the beginning of this event, set AR Capture on to false and create a toggle AR Capture node. Keep on off false and then pause our Cam Texture timer. Let's create the event that we want to run at 60 frames per second. Name it set cam texture. Back at the timer, drag backwards from event and search for create event. In the drop down, set it to set cam texture. From set cam texture, create a git AR texture node. Set the texture type to camera image and check if the return value is valid. If valid, from our dynamic material instance variable, Mcam Capture, create a set texture parameter value node. Set the name to cam capture and plug the return value from git AR texture into value. We now have a system that is prepared to render our feed to a material at the request of the user. Let's get the UI updated to set this all in motion. Open our BP toolbox located in world builder, UI blueprints. Under tab spatial anchor, duplicate one of the child actors as we will also use a switch actor to turn the camera on and off. I'll bring it down to the bottom row at minus 6.4 to give it a little separation. We need to change the name of the switch and have it start off. Open the child actor template and under button set the label to toggle PV cam. Then under toggle button set is initially checked to false. As with the rest of the buttons, we need to bind an event to them so we are updated when they are pressed. Create a custom event and name it prepare toggle camera. Let's call it at the end of our prepare events after begin play. Drag in a reference to our new child actor toggle camera component and get the child actor broadcast to UXT pressable switch button actor. From as UXT pressable switch button actor get the toggle state component and bind an event to on toggle. And backwards for the event and create a new event named toggle camera. Toggle camera will now be called each time the switch button is pressed and we can retrieve the new value. Now we can call our toggle AR capture inside of BP PON. To do this, get the player PON and cast to BP PON. This BP PON called toggle AR capture and from toggle state get is checked and plug it into on off. Before we move on, we need to make sure to turn this button on and off when the tabs change. Open the set tab function. Let's simply move over this anchor logic and just like with display mesh and spatial mesh drag in a reference to toggle cam. Duplicate the toggle UI from button macro and plug toggle cam into self. We'll use the same select node to set the new visibility. The last thing we need to do is open the new asset in our scene BP screen. What we want to do is wait for BP PON to set its cam capture material to the proper dynamic material and then use that material for our screen. To give BP PON enough time to get it all set up, from begin play, first add a delay node. I'll set it to 0.5 seconds. That's plenty of time. Alright, right click and get our player PON and cast to BP PON. As BP PON, get the variable Mcam capture. Bring in a reference to screen and create a set material node. Plug in Mcam capture into material. Alright, we're ready to see this. As mentioned in the previous module, if we try to simulate this from within Unreal Engine, we will only see the default texture. We need to run this on a packaged version directly from the HoloLens too. We will go over packaging our project in a couple courses, but we have some cleaning up to do before that. I'll package it on my end so we can see what this looks like. World Builder. And I'll change to the spatial mapping tab. We can see that the screen is still the default texture that is inside of that material. If we turn on the PV cam, the system starts up and there is the feed to our camera. In this course, we prepared our project to retrieve the data collected by our PV camera and used it by applying it to a texture that can be accessed by any actor in our scene. For more advanced use cases, this feed can be fed into services that go beyond this course and learning path. We have come a long way in this project. Starting out, we created a system to track our head and eye gaze. We rendered and created interactions with our hands. Using UI, we were able to alter the scene. After moving on to spatial, locatable audio sources, we mapped our voices to control the content in our project, scanned the environment around us to manage and reliably load local anchors, and now we can see a live feed from our camera. And to think there is still more to go. In the next course, we will use tools and principles to optimize our project to find a good balance of features and performance.