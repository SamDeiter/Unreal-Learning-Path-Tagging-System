We're moving on now by taking a look at shaders and materials, therefore, as well as things like pixel shaders and the impact they have on performance, the impact they have on workflow limitations, and so on. Right, so a pixel shader is similar to a vertex shader that we've discussed in the geometry rendering videos. It's a small program, if you want to call like that, that is meant to run a lot of calculation of some simple calculations, but a lot of them at the same time. So it's a set of calculations run by the GPU. It's done directly on the graphical card and it's going to modify the colors of pixels. So it's a little program that runs on a pixel. Every single pixel will be, or not every single one, but most pixels will be covered by these calculations. This is extremely central to the rendering pipeline. It's used for everything and every part of the step, every step of the rendering from the moment we finished the geometry rendering and even there already was involved all the way to the end. So we drive the entire material system with it, but we also drive lighting with it. We drive far with it. We drive reflection, post processing, color correction, and so forth. Basically anything that comes along that we haven't discussed yet is pixel shader driven. Here an extremely basic example. If you have an image and you zoom in, you'll obviously get the pixels. Now for each and every single pixel, it would take inputs. It takes the current values or some other input variables. Then run a calculation on them and then output something. In this case, very basic example, take a current pixel color at 50%. This now becomes a new color. So the pixel is now a bit red. Then take the next pixel, do the same thing, write that pixel. It just iterates through all the pixels, running the calculation. That's the pixel shader. You can selectively apply pixel shaders, pixel per pixels. You don't have to do it across the entire image, across the entire frame. You can have certain rules. You can, for example, say any pixel that adheres to this rule that is, for example, red, please make that pixel now green. You can have rules. This is why we have mask images. Now pixels, pixel shaders are written in shader language. They differ per platform. So every platform has its own shader language code. On direct X, we are using HLSL, high level shader language. It's a very small amount of sample code. It doesn't really mean anything. It's just a randomly copy-pasted piece of code. But you can see the format. That's the most important thing we want to highlight here. This is how it's written. Now, usually in game engines and real-time renderers, this is the normal setup. You will have a shader that is written and has a certain number of key variables of the find. For example, what textures to use? It's going to define how the lights should interact with the textures and how reflective it should be, and so on. But a number of variables are undefined. Those variables then get passed in. For example, we take the shader as written, then insert the textures into it and other data, for example, we're going to say it should be this metallic, the texture should tile this many times, and so on. And then eventually, we can render that. We can output it and render it on the model. And we have these variables and this functionality. That's the normal way to go. Now in real, does this more complicated by taking a larger setup meant to empower you further? So here it's a multi-stage process. You have a couple of steps in between here. We also start with a shader, something that has been written, but it's more of a template. That template is then used to power the material editor where additional things are made. The notes, the expressions you made in a material editor, once compiled, they are merged together with a template and generate a new shader. That shader then still has a number of parameters and variables undefined. They get defined in material instance and then eventually it is outputted to the object. So we have this thing here. Also you could argue that it's so much similar to that step, but most importantly, we have the material editor. And that's what makes the Unreal Engine for material system extremely powerful. I'm going to take a look at the editor itself. Right. So here in the editor, we can take open a couple of materials, something random here is a pillow material and we see this setup. This can be when it's applied or saved, it will actually be compiled to HLSL code. That is why it says here, HLSL code. You get this. It's very large because it blends in a lot of functionality from the template, but this is what it outputs. This is the actual shader. That shader was based on a template. A template comes from a folder in the installation directory of the engine. So engine slash shaders, private, and it's these files. If you open these files up in a text editor, here are the basic notepads. Display over random one of these files. I opened up a surfal tree. You see the original HLSL code. Now since 417, on the engine 4.17, you can also create these shaders and blend them and merge them into plugins or your own project. So before you had to modify this directory and place them somewhere in here, which obviously post the problem because then it was in the engine itself. But nowadays you can also write these directly into your own project or plugins. Again, this is then used as template. This is why it explains why if you go back to the material editor, you have certain inputs and some of these inputs are grayed out. In fact, if I now make this not blend mode opaque but translucent, suddenly these are grayed out and these are ungrateful, these are became active. It now switched templates and your template simply has other inputs. It requires an output. So you get different functionality. You could therefore create your own templates to extend this list, these lists here with something else on top of it. In fact, that would probably be more performance than trying to build everything in here. If you need absolute maximum power and maximum performance, you would create your own shading model or similar, like we've done with, for example, clear code, which is used for card paints or surfaces like that or hair or cloth or eye rendering because then it's written directly in HLSL code and then over here you can still build networks of functionality that will be merged with that template. Another thing to note is that every time it generates these shaders, it actually generates more than one shader. This is why in the bottom of Unreal you sometimes see this box that says compiling 450 shaders or some random rather large number. That number is quite large because it doesn't just generate one shader per material, it has to generate one shader per use of that material. And use means where is it actually used for? So it is a property here in the bottom of group of properties called usage. And here it says use to aesthetic lighting, use with mesh particles and so on. So each one of these results in a shader. And we have to do it like that because if we would have to create one shader that has to cover all of these situations, then we would get a single shader only that is really, really complex and therefore would take more performance. So it's better to then create say 15 specialized shaders that only have that part of functionality that you actually need than to create giant complicated shaders. So that is why that number is so big because it has to create a lot of permutations or slightly different versions. And also take a look at that in render dog which will show us the same thing. Render dog launching here, let's open them to frame we has. And you can see here in pipeline state, this is where you can see information from the geometry. This is the verdict shader modifying the position of the vertices and so on. These were not really using all shaders used for displacement mapping. Eventually gets restrized as explained in earlier parts of this course. And then we have the pixel shader. So we restrize the image which is technically done with pixel shaders as well. And then we modify it again with the pixel shader. You can even see even view this pixel shader over here, which won't help us because this is the compiled version of it. This is the compiled version of that. And it's not very readable, but you can see it running here. You can see that it takes a certain input textures and it outputs certain textures as well. And eventually we have the output merge over everything comes together. Now a big portion of the pipeline is PBR base. PBR stands for physical base rendering. They are using specular metallic and roughness as inputs. And that's what we're using to calculate almost all shading that we have in the environment. I'm not going to go too deep into what PBR is. We have a lot of other resources on that. But we're going to highlight why we have PBR. Now PBR is unified shading. We call it unified shading because almost the entire image and all the models in it and all the materials in it are build up of that same shader in the background, the same PBR system. We're doing that in order to get maximum efficiency. Look, it's the same thing that we've talked about all the way in the intro. If we know what we can expect, then we can design the system. We can design the performance to match that one thing that we know we're going to get. If you know that we only have a single real shader, a single shading model that's used for almost everything, we can optimize everything we do for that one and single only shading model we have that will give us a better performance. It also improves the art pipeline by being more predictable. If again, we're only using single shading model, then we know that everything will use that and if everything uses that, then everything will be more consistent and it's easier to predict how something will look. You're not going to end up with situations where shading model A doesn't tend to work well with shading model B or it doesn't work well if you use indoors but outdoors it works well. We have a single model and it works everywhere and for everything. And also we simply face inherent restrictions because of the use of the G-buffer and our compositing base workflow. Remember we basically create a lot of different images and then we composite them together? That means that anything we do at this point we're doing with materials has to operate on the G-buffer. Whatever information it needs, it needs to pull that information from the G-buffer. And the G-buffer has a color, it has lighting in there, it has the depth in there, those kind of things but it doesn't have all information. So we face a restriction where we have to find a shading model that looks good, performs well and can be done using the information available within the G-buffer and compositing base workflow. There's a couple of basic examples of different roughness and metallic values. Basically those two numbers drive almost everything. Specular is not used much. The specular value we're basically only using metallic and roughness which requires a number between 0 and 1. So those two numbers are supposed to give you almost any result. Now as part of this there is also a shading model and a shading model as in a texture generated by the G-buffer. It's a number of mask images that identify which pixels use other shading models in a VR, because we do have all the shading models. It's just they're not common and they're kind of a specialized uses but we do have them. That means we have to be able to identify where to apply those different shading models. So these pixels will then take an alternative rendering path forward. We can basically say and we could have done the same, let's switch back to render monkey here for a second. If you have the G-buffer, let's open it up, take the base pass again and let's look at this. If you would have this image right here which is basically roughness, this is metallic, this is specular and this is roughness. You could then say every pixel that is white, please follow this set of calculations, every pixel that is black, please follow this set of calculations. You can run different calculations based on what color the pixel has right now in a specific part of the buffer, specific image. So with the shading model, we do the same thing. We generate this. Everything that's green is simply PBR but we can now identify things which are different. So these spheres on the left again as mentioned earlier, these are using cloth rendering, hair rendering, eye rendering, you know, different shading models, different surface types. And they are identified here and then we can say everything that's red, please use this system, please use this shader language and shader in the background, everything that's for example, you know, purple here, please use that as well, that's how we differentiate it. Now, we technically have most of what we need to render the materials but I can't show you the materials yet because we don't have the reflections which is an integral part of rendering materials. So before we can move on or before I can show you the result, we actually have to move on first and immediately take a look at the reflections. Here's the things you have to take into account and remember, a material or a shader has a maximum number of texture samplers it can look up. Usually this is 16 of which usually only 13 can be used. You can use shared samples however and that will allow you to use up to 128 different textures but only on DirectX X11 or 12 so not on your platforms. Another size issue mostly causes lag and freezes, not frame rate loss. And for that matter, sometimes you can see that when you start the application, you start the game, there's some blurry textures, right? Not everything has a resolution that you expect. When that happens, that is happening because it doesn't have the bandwidth or the memory to quickly enough transport the full resolution versions of those textures. So instead it falls back on a low midmap level on a blurry one, so at least can display something while it's still trying to bring over the higher resolution version. When that happens, you either have too much going on, there's too much content there, or you should look into optimizing the texture pool size. Let me show you that directly in real here. This might be hard to read. There is a texture pool, I'm not sure it can still set this in, for example, streaming pool size. You set the pool size and the pool size, the amount of memory that has been allocated for the textures. So it reserves this space in memory. If you say that the texture pool is one gigabyte, then it will set one gigabyte aside in the memory right away. It just captures that space and that will then be filled up by the texture of the world. But if the textures of the world exceed that amount, you're going to start seeing blurry, some textures are going to go blurry in the view in what you're looking at. That's because the pool size has been exceeded, and then you should raise the pool or optimize your content. And also, actually, if you take a look at one one in the editor, so the maximum number of textures, if you open up a material again, this is why it says at the bottom, texture is samplers 7 out of 16. We're using 7 textures here out of the 16. Now, we can go count them and we probably don't get to 7, which is because we're not using a couple of them outside of this view. For example, light maps from the lighting, that's the texture too. There might be a shadow map, there might be some additional textures, the reflections could be counted as one and so on. So it's not just what you're using here, there's a couple of overhead textures as well. You can see if you click on one of these textures, you can set sampler source to a shared sampler source, and that will allow you to use more textures up to 128. This is, for example, how you can create a landscape with many more different textures than the default 12, 13, 14 roughly. Right, now pixel shaders have a very big impact on performance. If you talk about what makes rendering real-time rendering slow, it's usually the geometry rendering in a draw course, therefore, in itself. It's the pixel shaders, it's translucency, and it's dynamic shadow rendering. It's those four that cause almost all issues with performance. The pixel shaders are slow, but not because they're inherently slow, they're simply slow because we do a huge amount of work with them. We do almost everything with them, as mentioned, even the lighting, we do the fork with them, reflections, everything is pixel shader driven, so yes, obviously it's going to have a big impact. The higher the resolution, the bigger the impact of more complex materials. And pixel shaders in general, the more pixels there are on the screen, the more times you have to run complicated calculation again. To the more complicated, to more pixels and the more complex your calculations, the slower it's going to get the bigger impact of pixel shaders. So a complex material is there for more expensive, the more pixels it covers on screen, and it can show this in the viewport. And this also goes for everything that we do when it comes to pixel shader performance things. The more pixels it covers, the slower it is. That's an interesting one, because over here, for example, we have the statue. And the statue has a noise material on it. It's just a random material. In fact, all we've done is this, I took the noise, generated some noise, 32 levels, and I'm just adding them on top of each other for the sake of making this complicated. You can see it is complicated because at the bottom, it says base-pass shaders, 7098 instructions, 798 calculations. It's pretty heavy. Normally, you would be around 100 to 200. That's acceptable. You're not going to get it lower than 60 or so usually. So about 100 is fine. This is 800. And then 800 results in, if we would use a few modes, shader complexity, this being red. So if you fly around here, you can see everything is green. This is what you would expect, different shades of green. You can see there's a cross here in the middle, by the way. So you can aim in at something, and then you can see here, VS and PS, pixel shader, verdict shader. You can see the expense of the two. As you look around, but if you look at the statue, it's obviously quite expensive. Could be worse, but it's already here almost in the middle of the bar. Now the thing is, this gets heavier, the more pixels it covers. That means that if I'm here, this is taking up a lot more performance, this material, than if I would look at statue from here, because now it only has to render x number of pixels. So the expense of anything pixel shader related comes down to how many pixels does it actually cover on the screen. It is therefore more reasonable to have a very complex material on an object in the distance, than it is to have a very complex material on an object right in front of you. That wraps up the materials and we'll move on by taking a look at the reflections, so we can show you how those blend in together with the materials.