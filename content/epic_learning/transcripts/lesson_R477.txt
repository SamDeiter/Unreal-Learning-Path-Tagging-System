Unreal Motion Graphics or UMG is Unreal's tool for designing and developing the code that powers any type of user interface inside of your project. Let's power the editor to see if these work. Now to create a UMG widget, we need to first right-click instead of our content browser, go down to user interface and then select widget blueprint. When we do this, we are going to get a pick parent class for our new widget blueprint. Most commonly you're going to select user widget, give the widget a name, and then we can open it up. The UMG editor is split into two different categories. There is the designer where we add things like borders or check mark boxes or images or progress bars or text box or sliders or any other thing that we need in order to have an interactive UI. The graph is where we make that interactive. Again, the designer is where we make things pretty and the graph is where we make things functional. The graph, as you notice, does look a lot like a blueprint graph and that's because it does function in the similar way. Inside the designer, if we have a button, let me just drag a button here. If I come over here on the details panel, see this on-click. If I press that, what's going to happen is I now get this on-click, which I can then left-click and drag and look for things like a print string. But when I activate this UI, when I press this button, it will say hello. UMGs are an extremely efficient way to make UI without having to use Unreal's C++ implementation of UI, which would be slate. I highly recommend any work that you do inside of Unreal for UI's be done inside of Unreal Motion Graphics, as it's a very, very easy and lightweight tool to use.