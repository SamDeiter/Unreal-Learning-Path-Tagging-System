To modify any pre-authored animation and underl Engine, we don't necessarily need to create a custom control rig, especially when it comes to quick changes that do not require such high level rig functionality like IK and stuff. So we can use an Inbuilt option of FK control rig to create such quick modifications. To showcase this in action, I'll open a basic level sequence asset with just the skeletal mesh track in it. I'll add an animation and adjust the playback range. And now there are two ways you can use to quickly make changes to the existing animation. First, click on the skeletal mesh track under the control rig and select FK control rig. This adds a new FK control rig track for the character. But you'll notice that the animation doesn't play anymore, that's because FK control rig track is now overriding the animation track. So right click on the FK control rig and select additive. Now you can select any bone from the anim outliner here, manipulate it and add a keyframe. So that's how you can manipulate animations using additive FK control rig. This is very similar to how you would do it in the animation editor. Next method of using FK control rig gives you slightly more control over the editing process as compared to this one. So for that you can right click on the character track and we have an option called edit with FK control rig. You get an option box for baking keys with different parameters that you want to bake, we'll leave it to default for now. Then there is an option to reduce keys. I don't want to reduce keys so I'll leave it as it is and hit create. This makes the animation data on every frame. If you expand this FK control rig we can see the keys big for every individual bone track. We can modify individual keys here but that can be a bit tricky as we have keys on each and every frame. So let's head to the curve editor where you can easily manipulate the graphs to change a pose. Let's say I don't want my character to lean forward to such an extent while running. I want to keep the spine a little more up front. For that I'll select the spine and go to rotation. Here we can select the range of keys or the whole curve and move it accordingly using the transform tool. I could simply rotate it from the viewport as that sounds more intuitive and easy but if I rotate it from here you'll notice that this particular value applies only for a single frame. I'll any of which ways need to match the spine for the whole timeline. So instead I picked the whole curve and moved it accordingly. Now using FK control rig is good enough to make small tweaks or modifications but it'll be very tedious to make a full fledged modification or clean up a mocap data using that. So let's move on to the next module where we'll use backwards solve to create an intuitive workflow and overcome the limitation of the FK control rig with some custom logic and IK setup.