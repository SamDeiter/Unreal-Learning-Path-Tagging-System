In this module, you'll be learning all about the ad node, specifically how it can be used to brighten our values in the material graph. So if you want to create a new one, you can just right click type add, or you can hold A and click. And a new ad node will get created. So with this node, you'll notice it has two input pins, which are used to add two different values together. So those values could be constants, they could be constant three vectors. You can add colors to the single constants. You could do a bunch of different stuff with this. And at this example, let's just change these values to 0.5 really quick. And I'm just going to plug that directly into the base color. As soon as I do that, my material becomes white. So as I told you earlier, black is zero and white is one. As we add values together, they typically get brighter. So just as an example, I'm going to delete. One of these numbers, or I'm just going to delete both of these values. And I'm going to create a constant three vector by holding three and left click. So that's our color node. And I'm just going to plug that into the first input pin on my ad. And as soon as I do that, I can change this to a red value. And now if I add another constant, I can just plug it into the second input pin of the ad. And let's say I just want to show you what happens when you add colors to values. So if I change this to 0.25, you can see it's getting brighter and brighter, but change it to one. Now it's basically just white. So as we add numbers, it just keeps getting brighter and brighter. And it's just saturating our values because you can't have super red and super bright at the same time. So if I click this color node, just to kind of show you what's happening as I add more value to it with my constant here. So this number is basically just getting closer and closer and closer to one. So now that you guys are aware how the ad node functions and how you can basically take two different values and pair them up with it. Let's take it a step further and let's see if we can brighten that texture with it. So it's going to delete everything and kind of start from scratch just to quickly show you guys. So let's minimize this material editor. And I've kind of just navigated to this Nordic moss material folder right here. And I'm just going to drag my albedo texture in from it. And I'm going to create an ad node by holding a and left clicking connect that directly to the input pin. And connect that directly to base color. And as soon as I do that, you'll see that our material is very white. So the reason for this is that our ad node basically if I just disconnect this pin right here, our ad node is comprised by default when nothing is connected to it of two different values zero and one. So it's always taking a and adding it to be, but a is zero and b is one. So if I input my green moss texture into a it's adding by default with nothing connected to it, a value of one to it. So it's making it basically just white. So that's not good. We don't want that. It's way too bright for a moss texture and it's no good for anybody. So we're going to actually just select that ad node and you know, instead of creating a new constant value and inputting that. That's kind of redundant. We don't really need to unless you know we're creating customizable parameters or inputting texture masks. You can just delete that and we can just leave that ad node by itself and manually input a value in the details panel here. So let's set that back to zero. So when I'm adding black to this texture, it's not brightening it at all because black has no brightness in it. So it's basically the same math as taking one plus zero equals one. Just keep that in mind. Adding black to things adds nothing but adding white to things adds brightness. So just to really hammer that home, let's take two textures and add them together just so you guys can see what the final result will be and how the ad node affects them. So if I minimize my material, go back into my content browser. I'm just going to go back into my texture filter here. And in my project. The texture I'm going to choose is this noise underscore mask, but it could basically just be any black and white texture with a lot of contrast in it. This one will really show you the result. Right. So if I drag that directly into my material editor, maximize the window. And I'm just going to drag that output pin of RGB and connect that there. So now you can see that if I open up this texture really quick. So you can just really see those values. So like you have lots of blacks, it's pretty cloudy. So on here, the black areas are invisible in my final output of my material. So the ad node is great for mixing two different textures together because all of the white areas in your textures will get brighter and brighter. And all of the black areas won't be affected at all. So this is super useful for masking specific parts of your material out or you know, only having parts of your material get brighter.