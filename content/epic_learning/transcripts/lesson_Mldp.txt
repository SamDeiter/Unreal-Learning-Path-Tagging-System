In this module, we will have our very first look at blackboards and behavior tree. In the behavior tree, we can create a flow of logic that the AI should follow when making decisions. Inside this behavior tree, we can add custom logic in the form of tasks. And tasks are basically just blueprints. So once we get our heads around the flow of a behavior tree, we can use our blueprint knowledge to add functionality to the behavior tree. And blackboards are where we can store variables that can be referenced and used in the behavior tree. Let's get started. Now it's time to add some behavior to our AI. Let's get started with behavior trees and blackboards. So I'm going to go to artificial intelligence and first we'll create a blackboard. And a blackboard, which I'll call BB on that, is basically where we store variables that we want access to in the behavior tree. You can see in the top here, we should be able to switch between behavior tree and blackboard, but since we don't have a behavior tree yet, let us add some variables to this. So first we have our enemy. In this case, the player character and the key type is object and the base class will be character. The next one we have is the start location, which is a vector. Then we'll call this start location. We also had a roaming range, which was a float. So I'll go roaming range. And we have the behavior, which is an enumerator. We can select enum and say default behavior. Now for this one, we need to go to key type as well and make sure that we set that to the AI behavior. And I think that's all we need to do in here for now. Let's actually create our behavior tree. So artificial intelligence behavior tree and call this pt or behavior tree on that. Now we can assign a blackboard to it, bb on that. I think it associated the blackboard automatically because it's the only blackboard that we have. So we already see all of the variables in here. And this is the behavior tree. So now we have a behavior tree, but before jumping in, we want to get started with making sure it's all set up. So we're going to our undead controller, open it up on begin play. We want to say run behavior tree. What is the behavior tree? Well, our only one pt on that. So now when we begin the behavior tree is being started. And when our AI dies, we already stop it. Next, we're going back to our BPAI on that. And we'll want to set some variables here. We can actually communicate with our behavior tree by doing get AI controller. So now we get a reference to the AI controller. The control actor is ourselves. From the AI controller, we can do get blackboard. And now we can set a value as a float, set a value as a bull or an enum. So we can set any value on the blackboard as long as the names match up. Now what I like to do is make this into a pure function. So I'm going to say make literal name. And I'm going to grab all these items, write most button and say collapse to function. And then call this and call this BP data, like board data, make this a pure one. And then inside of here, make sure that the literal float is connected. And this is called key name. So now what we can do for the float is connect this up. We have the wandering in the AI settings, max wander distance connected. And what did we call that in the blackboard? We call that roaming range. You can copy paste it or you can just type it out. So now it's going to set a key with this name called roaming range with this value. So the key in the blackboard will have the same value as in our player character. We're going to do the same for the start location and the default behavior set value as enum. Enumerator being our default behavior and the name being default behavior. That being the key name. This one is the start location. So set value as vector connect this as well. And get our start location vector. Make sure it's named the same. It was just start location together. Now we shouldn't forget about the last one, which is over here. Get Pb data. And that is the enemy pawn. So set object value as object. Here we go. The name and our enemy character and the name is simply enemy. So what we're doing here is setting the variables in the blackboard so that we have access to them in our behavior tree, which is updated all of these. So we have access to them in here. We have now created a behavior tree with associated blackboard. And we've made sure to start that behavior tree. When our AI spawns and when our AI dies, the behavior tree is stopped and the logic will stop executing. In the next module, we'll dive deeper into the behavior tree.