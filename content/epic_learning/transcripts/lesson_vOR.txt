Welcome to the fourth coming to the Rescue Challenge, a module where we will build upon what we learned in this course combined with previous courses to solve a problem. The previous modules have prepared us to solve the problem and use what we've learned to fix our broken cube. We will be using our voice to interact with our scene by identifying where we should be focusing our attention. This course has enabled the use of speech mapping that we have set up inside of our pond and the previous courses have taught us how to spatialize audio in the scene and use traditional stereo audio in our UI. Before that, we learned how to create and interact with UI elements and track the gaze of our eyes and head. We have spent a lot of time preparing the mechanics we will use to solve this problem. Our intention with this solution is to identify what the cube needs in order to be fixed, spawn a menu to then spawn a tool that will need either by pressing the button with our hands or using voice commands and use our hands to move it to the location it needs to be. When the cube is fixed, our job is done and we can safely close the cube. Let's get started by building the menu we'll use. Create a new blueprint in our UI Blueprints folder. Set the class to actor. Name it BPToolBox and open it. We're going to make this menu using a few components provided in MRTK. It will be similar but not as complex as our previous object spawner blueprint. Add a static mesh component and set the mesh to SM Backplate Rounded Thick 4. Set the location to minus 0.01, 0, minus 1.5 and scale it to 1616. Next add three child actor components. For the top two, set the child actor class to UXT Pressible Switch button actor. Set the third ones class to UXT Pressible button actor. We'll use the top two to choose which tool to spawn and the third as the spawn button. Arrange them vertically placing the top one at 0, 0, 3.2. Leave the second switch at 0, 0, 0 and place the third button at 0, 0, minus 6.4. Let's work on the top one first. Rename it to child actor cube. In the Details panel, open UXT Pressible button. Set the label to cube. One audio component and under Sound, set the sound to cube selected. We won't be adding an attinuation to these sounds as they will work much better in traditional stereo. Next rename the second child actor to child actor sphere. Again in the Details panel, set the label to sphere and the sound to sphere selected. Rename the last child actor to child actor spawn. Set the label to spawn and under icon brush, choose an icon that works well for this button. I'll select these primitive shapes here. EC96. To make sure that only one switch is on at any given time, add a new component. Search for UXT toggle group. In the Details panel, under toggle references, set the first one to the display name of the top button, so child actor underscore cube. Set the second one to child actor underscore sphere. And finally, the selected index to zero so that cube begins switched on. UXT toggle group will make sure that one or the other is switched on, but not both. Lastly, add a UXT follow component. Set orientation type to Face camera. With our menu built, let's set up the communication between it and our pawn. On the Event graph, create a new function and name it Voice, underscore Set switch. Give it one input named selected index and set it as an integer. Open the function, drag in a reference to UXT toggle group and create a set selected index node. Selected to the selected index input. And now drag in a reference to both child actor cube and child actor sphere. From one of them, get the child actor and detach it. Pull backwards from target and create a select node. Plug cube into zero and sphere into one. And connect the index to the selected index input. From child actor cast to UXT pressable switch button actor. And as UXT pressable switch button actor, get the audio component. Call play. Return to the event graph. From begin play, pull in a reference to child actor spawn. Again, get the child actor and cast to UXT pressable button actor. As UXT pressable button actor, get the button component and create an assign event to on button pressed. Rename the generated event to spawn button pressed. Because we want to communicate back to our BP upon what we want to spawn, create a new event dispatcher named user pressed spawn. Provide one input of type actor class. Be sure that it is the purple class input. Change its name to to spawn and call it after spawn button pressed. Save the call a little over and drag backwards from to spawn and promote it to a variable. Name it the same. Let's create a new custom event to set to spawn. Set the name of this new event to set to spawn. Get a set to spawn node and drag backwards and create a select node. Begin a reference to our toggle group and plug the selected index into index. Set option 0 to BP puzzle piece cube and option 1 to BP puzzle piece. Be sure to call set to spawn after spawn button pressed so that our spawn actor node knows what to spawn exactly. What we have created here is a way for the user to interact with our toolbox either with hand interaction or voice, either of which will return with the same outcome. Return to BP upon. From our HL World Builder event, add a spawn actor from class node. We need to move it over a little. Set the class to our new BP toolbox. We can split the struct and keep the values at 0 because we have added the UXT follow to the blueprint so it will always be where we need it. Promote the return value to a variable and name it BP toolbox. Then from BP toolbox, assign an event to user pressed spawn. I'll quickly delete the generated text on the event. From two spawn, create a spawn actor from class node. We'll take care of the transform shortly. After we spawn it, destroy BP toolbox. Run over to before we spawn BP toolbox and check if BP toolbox is valid. If so, destroy it. If not, connect to the spawn actor node. This way we can toggle it there when we need it and if we decide that we don't need it, we can destroy it without spawning anything. Duplicate the variable and is valid check to all three speech mappings. From UICube, drag from BP toolbox and call voice set switch. Remember that the cube is the zero index. Repeat the call after UI sphere and set the selected index to one. From UI spawn, drag from BP toolbox and call spawn button pressed. We can leave these two inputs empty. We can now use our hands or our voice to spawn the tool we need. Let's finish this by telling it where to spawn the actor. Pan up to our head orientation tracking event. From the line traced by channel, promote the out hit to a variable and name it head tracking hit. Back to our user pressed spawn event. Split the spawn transform struct. Drag in a reference to head tracking hit and split the struct. From trace start, create a look at rotation node. Plug target into trace end. From the return value, get the forward vector and multiply it by a float. Let's place the actor one meter away so set the float to 100. Again from trace start, create an add vector node and plug the product into the add node. Plug the sum into spawn transform location. The final thing we need to do is make sure BP fix me only accepts the proper tool. Open BP fix me. Turn down to on component begin overlap. From other actor cast to BP puzzle piece cube. Remember that BP puzzle piece cube inherits from BP puzzle piece. So if we started with BP puzzle piece, both the cube and the sphere would be accepted. So from cast failed cast now to BP puzzle piece. If the user tried to use the cube, it would succeed on the first cast and then do nothing. But if they used the sphere, it would fail the first cast and succeed with the second. Alright, compile and save. Let's solve this problem. First we need to identify the status of our cubes. This check. Attention. All of them beeped except one. We know it's the one on the top, but let's make sure that's the only one. We can look directly at one of them and again status check. Okay, that one is good. How about this one? Status check. Also good. Status check. Attention. Alright, this one needs attention. Let's find out what's wrong. Visual interact. Okay, with it opened up, we can see that an important part is missing and because it's indicated by an orange representation of it, I know what tool to use. World builder. I don't need the cube, so choose sphere. Sphere selected. Spawn. Here is my spawned sphere. I'll quickly replace it and it accepted it. It's charging up and when it glows, I know it's working as expected. Very good. Awesome. Okay, single interact and now it's closing. We have accomplished a lot in this course. We prepared our project and our devices to listen for our voice and respond to custom speech mappings that allowed us to interact with the virtual world. We solved a previous coming to the rescue problem by altering the blueprints to work with both hand interaction and voice interaction. We created keywords to work on an individual actor or be broadcasted out to any actor that was listening. After creating a toolbox, we built it to be ready for hand or voice interactions and set it up to play a sound in traditional stereo audio to indicate the change. Using spatial sound, we were able to identify which of the actors in our scene needed our attention and after voicing a keyword, spawned a toolkit into the scene that tracked our location and allowed us to spawn the missing piece. In the next course, we are going to map our real world and further immerse our holograms into our reality.