With the tools and resources ready, in this module, we will begin using Azure Spatial Anchors as world anchors. Our system, as it currently stands, has the ability for the user to create, store, and retrieve local persistent anchors. Now we will add in the option to create anchors that are stored in the cloud. We're going to be building onto our current system inside of BP-PON to create local anchors and, if the connection is successful, we'll instead create anchors stored in the cloud. Let's jump right in and get started. Open BP-PON. As a quick review of the current system, when a user wanted to create a local persistent anchor, they brought up the toolbox UI and spawned a mesh representing the location of an anchor, either by manually moving the mesh or by using the tap to place mechanic to place on a spatial mesh. Once the user moved the mesh, the anchor was created. When they are in the map and they wanted to delete an anchor, they could do so on the toolbox UI or on the anchor meshes themselves. We'll use the same system for our Azure Spatial Anchors. We'll focus on saving them and deleting them right now. The mesh representing the anchor, when moved, will call the Save Local Anchor Event. Let's begin there. First, let's create a new macro. We'll use a lot here. Name it Use Azure Anchors. Give it one exact input and name it then. So the name hides. Give it two exact outputs. The first, I'll call Cloud Anchor and the second, Local Anchor. This will be a simple macro that only contains our Azure Session Running Bull variable connected to a branch. After everything is connected, return to the Event Graph. After the pin component node, detach and add our new macro. Reattach the local anchor line to how it was. We'll be creating and calling new events specifically for Azure Spatial Anchors. Create the first one and name it Azure underscore Construct Cloud Anchor. Give it one input of type AR pin and name it the same. From our Macros Cloud Anchor output, call Azure Construct Cloud Anchor and plug in the AR pin to the return on the pin component node. From AR pin on our new event, call Construct Cloud Anchor. Now the Cloud Anchor is created, but it is not yet saved to the cloud. We'll add that in a bit. We can provide an expiration to the pin itself and when it expires, it will be removed from our Azure account. First, let's make sure the anchor has been successfully constructed. From out result, check if it is equal to success. Plug the equals node into a branch and from out Azure Cloud Spatial Anchor, create a set expiration node. This is requesting a second's lifetime for the anchor. I'll provide a short five hours, so 18,000. We have our spatial anchor constructed and now we need to save it. Create another new event named Azure underscore prepare to save Azure spatial anchor. Give it one input of type Azure Cloud Spatial Anchor and name it the same. Call this event after we set the expiration and plug the constructed cloud anchor into the input. To keep track of these new anchors to be saved, create a new variable named Azure Cloud Spatial Anchor underscore to save and set the type to Azure Cloud Spatial Anchor. Turn the variable into an array. From the prepare to save Azure spatial anchor event, add the Azure Cloud Spatial Anchor to the array. One more custom event for the saving of the anchor. Name it Azure underscore save Azure spatial anchor. We will separate these events like this in case there is a buffer of several new anchors that need to be saved. Call this event after we added to our array. We will only use this array for the creation and saving of our anchors and the older loaded AR pins variable for use in our anchor management system. If you would like more information about that system and how it works, we build it in a course named mapping the real world. From Azure save Azure spatial anchor, get a reference to our Azure Cloud Spatial Anchor underscore to save array variable. We will go through each anchor and attempt to save it to the cloud. Some anchors require more spatial data and if the save is unsuccessful, we will want the event to try again until it succeeds. From Azure Cloud Spatial Anchor to save, get the length and check if it is greater than zero. If true, create a git node from the array and promote the return to a variable. Name it temp underscore saving anchor. Create a save cloud anchor node. See that there is a little clock on the top right of the node. This indicates that it is an async function that needs time to complete. So check if out result is successful and if so, we want to add it to our loaded AR pins variable for integration into our current system. Drag in a reference to loaded AR pins and create an ad node. From temp saving anchor, get the AR pin component within. Also get the Azure Cloud identifier and plug that into the name input. The system we are integrating to displays the name above the anchor. We need to create an event in BP anchor to get that working as we will have to rename it. Open BP anchor. Create a new event named rename anchor. Drag in a set reference to anchor name and drag backwards to create an input on the event. From the UXT text render component, set the text by plugging the anchor name into value. Compile and return to BP pun. Create a new variable named anchor's pending names. Set it as an array and set it to type BP underscore anchors. We'll need to do this as we are spawning the anchor long before they have officially been saved. Pan up to the spawning of BP anchor. After we call add interaction component, add our macro. From cloud anchor, add this new anchor to the array. Back to our saving event. After the add node, drag in a reference to our anchor's pending names array and get the zero index. From the output, call rename anchor and plug the cloud identifier into anchor name. Next from anchor's pending names, remove the zero index. Let's also remove the temp saving anchor from our azure cloud spatial anchors to save array because we know that's been successful. To update our toolbox, call the cycle current anchor in toolbox event. Finally, call azure save azure spatial anchor to go through this process again in case there are more to save. Call the same function back at the branch after attempting the save in case it returns false. Before we test all of this, let's also add the delete mechanic. Find the delete local anchor event and add a use azure anchor's macro right after the event and connect local anchor to remove arp from local store. Create a new event named azure underscore prepare to delete azure spatial anchor. Give it one input of type name and call it anchor name. Call it from the cloud anchor output from our macro and then reconnect to the is valid node. Plug the anchor name from the delete local anchor event into our new event. From our new event, drag in a reference to our loaded arpins and get its keys and its values. From keys, create a find node and plug the item to find into the anchor name. From value, get the index from the find node. From the return, call get cloud anchor. Now we have the anchor we want to delete. Duplicate the azure cloud spatial anchor is underscore to save variable and rename it to end with to delete. Drag in a reference to our new azure cloud spatial anchor to delete variable and add the return to it. Okay, we'll do much the same thing that we did with the save event. Create another custom event named azure underscore delete azure spatial anchor. Drag in a reference of our new to delete array and check if its length is greater than zero. If so, promote the zero index to a new variable named temp underscore deleting anchor. From our new variable, search for delete cloud anchor. Remember that the little clock at the top right signifies that this is an async function. So let's keep track if the deletion was successful and if not, try again. If true, get a reference of our temp variable and get its ar pin. Get the debug name and remove it from our loaded ar pins map variable. Then remove the item from the azure cloud spatial anchor to delete array. Call cycle current anchor in toolbox to update it and then call azure delete azure spatial anchor to remove any other anchors that are waiting. Also, make sure to call azure delete azure spatial anchor at the end of the azure prepare to delete azure spatial anchor event. The last thing we'll do before we try this is in BP toolbox. Find the add new anchor event. From anchor name, create a select string node. We'll set the default name on whether the azure session running pool inside of BP pawn is true. If it is running, set a name that indicates that it's waiting to be changed to the cloud identifier string. I'll name it a waiting identifier and then plug in anchor name into B. All right, let's try this out. Of course, you need to make sure that you package this to the hollow lens as it will not work with ran from the editor. Okay, I'll walk around the space for a bit so there is a good map to reference. Under the anchors tab in the toolbox, I'll press add new. As soon as it's moved, the anchor is going to attempt to save. And there is the identifier. All right, let's create one more. And while we wait for that to save, I'll delete this first one. In this module, we added on to our local persistent anchors by adding support for azure spatial anchors that are saved in the cloud. We added events for constructing, saving and deleting an anchor from the cloud and integrated this with our current system. In the next and final module, we will prepare to share these new anchors across sessions and to other devices.