If we needed to run the game every time we wanted to test a new EQS query, it would take a long time to iterate upon their design. Luckily, Unreal Engine includes an EQS testing porn to help the situation. The EQS testing porn allows us to quickly iterate and test our queries from within the editor. We'll create an EQS testing porn and use it to view the results of the EQS queries we have made in the previous videos. We'll start by creating the EQS testing porn and then adding it to the level. Navigate to the EQS folder in the Content Browser. Right-click in the Content Browser and then select Blueprint class. Expand the all classes roll out and then begin typing EQS testing porn. Press select in the window to add one to the current folder in the Content Browser. Name this new Blueprint EQS underscore my testing porn. Left-click and drag the porn from the Content Browser into the level. Using the Move tool, raise it above the ground before pressing the Enkey on the keyboard to drop it onto the surface. With the porn selected, move to the Details panel and locate the EQS rollout. Let's begin by taking a look at our Find Closes Food Source EQS query. Select it from the query template dropdown. Reposition the camera in the viewport to get a better look at the area surrounding the porn. Note that now we can see some numbers on top of our food source actors. These numbers represent the score given to them by the EQS query. Note that the closest food source here has a value of 1, which is the highest score. And the food source that is furthest away from the actor has a value of 0. Note that we can use the Move tool to move the porn around and these scores will be updated each time we release it. If I move it closer to this food source in the top right-hand corner, it now has the value of 1. Now let's take a look at the more complicated Hide Near Player EQS query. Return to the EQS rollout in the Details panel and change the query template to Hide Near Player. Now we can see a visual representation of the grid we defined as part of the EQS query. Each point in this grid has a score value. However, what we can see at the moment doesn't look quite right, there's far too many valid points. This is because we are running this query from within the editor and not running the game itself. This means the context we created for the Player Character doesn't work. What we will need to do is modify this context to instead use the testing porn rather than the Player Character. Open the context folder and then open our EQS C underscore Player context. While hovering over the return value of the get player character, press Alt and left click. This will disconnect the node without removing it. We're doing this because after we complete the demonstration, we will want to return this context to representing the Player Character in-game. For now, what we want to do is change this context to represent the EQS testing porn. As the testing porn will be the querying actor in this case, we can use the reference provided by the Provide Single Actor function. Drag out from this function and find the cast to EQS My Testing porn. In this instance, it's safe to convert this to a pure cast. We can do this by right clicking on the node and then selecting Convert to Pure Cast. We can now connect this My Testing porn reference to the resulting actor pin of the return node. We're now finished modifying this context so we can compile and save the context before closing the window. Now we can return to the main editor viewport and test that functionality correctly. Select the testing porn and notice that the results have now changed. Some of the points have now turned blue, indicating that they have been filtered out. The text written on them that says Trace lets us know that the Trace Test is responsible for filtering them. The remaining green points are all places the AI could stand that will be hidden from the current location of the EQS testing porn. But which of these will actually be selected? In the EQS rollout, change the querying mode to Single Best Item. Now we can see the winning result. The winner is shown in light green and says Winner, which is hopefully quite self explanatory. The other valid but unselected points are shown in dark green. While the game running, the winning result here is the one that would be selected. While this is very useful, what if we wanted even more detail? Or to check through each of our tests individually? We can do that too. Locate the step-to-debug draw in the EQS rollout of the details panel. The step number correlates to the order of execution of the tests within the generator. Return to the EQS folder and open EQS Hide Near Player so we can compare each test and the results. The first test was our visibility test. If you recall, this would filter out any positions where the Trace would hear, indicating there would be visible if they stood here. If we return to the main viewport and change the step-to-debug draw to one, we can see the result of just this test. All the red points are where the Trace failed, indicating they would be hidden from the player. Where all the blue values indicate where the Trace was successful. Returning once more to the EQS query, we can check the second test, which was our distance test. We inverted the scoring factor so that we would prefer shorter distances, and then the test purpose was set to score the results only. If we return to the viewport and change the step-to-debug draw to two, we can see this happening. The nodes that passed through the first section of the test, the Trace, into the second part, are shown in different colours. The lowest scoring value, which are the ones that are the greatest distance from the query, are shown in red. Those points with the shortest distance being closest to the query are shown in the colour towards green. Finally, returning to a query or one more time, we can see that we checked there was a valid path between the query and the target position. If we return to the viewport for the last time, and change step-to-debug draw to three, we now return to the view we first saw when we opened the EQS testing tab. This view shows the culmination of all three tests and shows us our winner. Showing this final step shows the culmination of all the tests and also, due to the querying mode, shows the winning point. As you can see, this is extremely useful for debugging any issues you might have with the complex EQS query. The only thing left to do now is to reset the context that we changed because going forward, we'll want to use that in-game. The last thing we need to do is to reset the context that we altered. We'll do this so that we can continue normal testing within the game. Reopen the context folder from the content browser, and then open EQS-C underscore player. Left-click on the cast to EQS underscore my testing porn node and press the delete key on the keyboard to delete it. Reposition the getPlayerC character function and reconnect the return value to the resulting actor of the return node. Compile and then save the context before closing the window. In this video, we added an EQS testing porn to our project. We then placed a porn into the level and configured it to display the results of the EQS queries that we created in the earlier videos. We altered an existing context asset to specify the testing porn itself, so the results were lying upon the player character, which does not exist in the editor, gave representative results. We then took a look at all the information the porn can display, including positions that were scored and positions that were filtered. We also took a step-by-step look at the hide near player query that we created. We finished by taking a look at examples of both of our created EQS queries running on the EQS testing porn.