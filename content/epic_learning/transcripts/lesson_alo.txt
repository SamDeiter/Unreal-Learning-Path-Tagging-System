Now that we have our first complete data table ready to work with, we'll see how data can be pulled from this using Blueprints. Let's first start by going to the Data Items folder and opening the BP underscore Data Item table. Remember that both of the classes here are pretty much a duplicate of each other. They just have different parent classes so that we can easily test the C++ functionality later for either data assets or data tables. You can see in the top right-hand side of the class is a child of the Data Act table. And the other one, of course, is going to be a child of Data Actor asset. To get started with our code, I'm going to do this in the construction script so that we can see this updating live in the editor based on what we set the item type to be. You can begin by calling the utility function get data table row. Inside of this node, we're going to want to set the data table or the type of data table that we're going to be looking through to be the one that we've just created, the item info table. And we can manually change the row name here using the drop down, but again, ideally, we're going to take this from an enum, which will move over to our parent C++ class and declare now. So go ahead if you don't already have that open and get the Data Actor base class open. Doing it this way will allow us to very easily update this in the editor. As I mentioned in an earlier video, this removes the likelihood of typos or choosing an item that doesn't exist. So when your ID is fully loaded and the solution is ready inside of the Data Actor base header, we can begin our updates. I've just noticed as well that the two components are ready provided. I've accidentally named the category category. So I suppose this is a live example of why copy and pasting even repetitive simple code can be sometimes risky. I know that I actually wanted the category to be named components, so I'll just update this now whilst we're here. Then below both of these components, still inside of the protected section as nothing outside of our child data actor classes will need to access this, but it will need to be available in the child classes. So I'll put that in the protected section. This is going to be a new item type and I'll name this one item type and I'll initialize this to be the item type none just so that we have a default set. For this, we'll also need to go ahead and include the data library.h in our include section and the directory path will be the project name forward slash data forward slash data library.h. For the new property, I'm going to make this edit anywhere, blueprint read right and I'll place this in a category named item info. And with that small change made, hit compile and check that that's been successful and then return to the editor. Back inside of our blueprint where we left off, we can now search for our new item type. We're going to get that and we'll plug the return pin into the row name for an auto conversion from an enum to a name. Now this will actually cause a little bit of an issue when trying to use or use this to look through our data table. But I want to include the error as a kind of highlight so that we know what to look out for in case you forget this next step in your own projects and you'll kind of recognize hopefully the results of not taking the step will go through in a moment. So if we throw in first of all print string before the get data table row and plug in the result, hit compile and quickly return to the map or see that just over here to the left, we're having it print e item type colon colon none is returned. Now the problem here is that the item type part of the text being included. This would guarantee that we never find a valid result in our table because we only have names which have been cleaned such as none radio trophy and spaceship. So if the thing we're searching for doesn't match one of those exactly then we're not going to get anything at all. Now an easy fix for this inside of blueprint is first of all we're going to convert our enum to a string and then plugging that string return pin into our name for a string to name conversion. If we then plug this into the print string whilst we have it just that we can check what this new result is we'll hit compile back in the map. We can see that this is now printing none without any other text. So this is what we want to check against in our data row when processing our queries in the moment. But in short we can see that was just simply cleaning the text for us. We're going to want to remove the print string and hook the execution pins back up to the get data table row. The next thing that we want to do is get all of the information if a row has been found matching the row name that we've passed in. We can do that by pulling from the out to row and choosing to break the item information table. This returns all of the information about our found row. Ideally we're going to want to store this information in case that we want to use this in multiple places which in our example we definitely will. So just a quick cut way to another project I have a quick example of what I've seen other tutorials or examples of other people storing information from a data table how that can sometimes look and it can get a little bit messy especially inside of blueprints. So what you often see is each variable being stored individually or created individually and then stored based on the return of a table of some sort like we have here using a long line of kind of satin notes. So I just say that this is something to be aware of. It might be best to avoid this as as you can see here is already taking up a fair amount of space just for a few pieces of data. So we're not going to be doing this here instead we're going to head back into our data asset base class the C++ version and declare another new variable. Now this is going to be an F generic item info struct. I'm going to name this one generic item info. This is to use the generic struct that we've created that I mentioned a little bit earlier in the course. And I said that this is going to be useful to improve our code even though it provided some initial duplicate code. This is going to be very useful so that we can see how we can set this in our C++ version and our blueprints to improve some of our processes as we go through. So I'll provide this with the same you property as the item type. I'll then compile and again return back to our blueprint when that's finished. So again in the construction script we're going to search for the set generic item info right click on the blue input pin select the split structure pin and then select the small error icon to drop down all of our structure information with that's available. We can I simply hook up each respective pin from the break node to the make node. This provides us with a struct that we can now access and pick and choose specific details from as we need. Remember that we needed the two different structs because we needed specifically one defined to be a data table row and the other structures that just kind of hold and work with this information in more generic cases outside of data tables just like this one that we'll be saying now. And then the final step this is really just a visual thing so that we can see these changes in real time through the editor as we change things. We want to set the static mesh information here. So let's control drag the mesh component into our construction script drag from the return pin and search for the set static mesh. And you can probably see what's happening here for the new mesh we're going to want to get our newly created generic item info variable break the struct and plug in the mesh value into the new mesh. You could also just right click the pin and choose to split the structure pin as a lot of people are ready familiar with to get all of the variables that we need. But there's one interesting thing that we can do with a broken struct compared to a split struct. So if we now select the break node we can click this hide unconnected pins button to the right this will collapse the information so that only the elements and use are displayed. And I think that makes for quite a tidy blueprint for what we're doing. So that's the first step we now have the data being accessed with and storing that somewhere and we're applying some of this information to our mesh component. So let's go back into the editor drag a few of our BP underscore data item table actors into the world and test our results. If we scroll down to the item info category and set these to a valid item type we should now see that the object reflects this information we can also drop down the generic item info variable and edit things in here. And just to highlight that this isn't an ideal solution in a production ready project. You'd probably make most of these variables visible anywhere or even completely hidden when they're not required. But so that we can test our steps and see that things are being updated as we go through the course will keep this exposed for now. The important thing here is that we can see that each item is pulling the correct name, cost, item info and so on from the data table. This is also what I described a little bit earlier that we don't need to create a BP item spaceship and then a BP item radio and so on. We just have our one item class and everything is then populated based on the item type that we define. So our final step just for some kind of visual finish and to allow us to get familiar with using our obtained information we're going to pass this to a widget. So inside of our event graph you can see that I've already provided some logic to check if the player overlaps the collider then create and display a widget if that's true, if that is the player. Up to this point we haven't had all of the required information to make use of this. So let's complete the widget class first of all. Inside of the graph section of the widget select the set item details function and add a new input variable. In this one item data and set this to be the generic item info type. If we pull from this new pin and break the information we can plug the name into a pen V on the name node, cost into a pen to be on the cost node info into the append be on the one which says info and then finally image into the brush settings on the final node we have here. You can also go ahead and add some rerit nodes as required. We won't be seeing this function again this is really all we need to do in the widget everything else is set up just to be displayed. So I think this will probably do for kind of blueprint maintenance here and then also be sure to remember to hook the function execution pin to the first set text execution pin. So if we compile and save the changes here close the widget and then go back into our the blueprint. We can see that the set item details has the new input pin that we've just created. So if we fill that with the generic item info that we've stored in this class and this is why storing it earlier was going to be useful so we can bypass this around and reuse the information as required. So to test our final changes this is now everything ready to kind of be pulled and displayed. We can go into our map we'll press play and if we fly around and touch any of the colliders on our items we're greeted with our lovely piece of UI providing some information about the item that we're overlapping. So this was just another example of the information being confirmed against the data table. This is the idea like come compared to having the item infern a struct fully exposed on the object but it's a much easier way to test the details and correct things in the editor having that exposed for the purpose as a set of this course. But just keep in mind as I mentioned earlier that you'd probably want to make the information either hidden or at most visible rather than editable but of course having it editable. If we needed to test changes or something for the course you just wanted to play around with things it's useful for our purposes at the moment. But that's really it for our blueprint example for now. We have a data table being read filling that information on an object based on the data and then also being passed and displayed through a widget.