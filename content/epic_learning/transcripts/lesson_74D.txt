Now that we've compared the different shading models, we're ready to begin building our ClearCode shader. To get started, we'll go into our working directory, and we'll create a new material. Let's call it M underscore clearCode. The first step in our material creation is to go to the details panel and change our shading model to clearCode. This of course exposes the clearCode input and a clearCode roughness input. This will add our base color. Rather than use a texture in this case, we're going to use a color. I'm going to set all the values to 0.123. And that should give us a nice gray. From metallic, we'll use a simple scalar parameter, and we'll set this value to 0.5. Now for roughness, we're going to use a texture. So we'll hold down T, create a texture sample, and we're going to search for carbon, and then we want to use T carbon fiber R. Before we put this into the roughness input, we're going to do a linear interpolate. We'll need two scalars for our linear interpolate. We're going to set the first one to a value of 0.1. The second one will set to 3. Let's go ahead and hold down the L key now and create our Lurp, and we'll put the 0.1 value into A, and the 3 value into B. Now we can take the red channel of our roughness texture, and plug that into the alpha. From here, we're going to take our Lurp and put it into roughness. We're going to create a scalar, set it to a value of 1, and then plug that into the clear code input. As for roughness, we're also going to use a simple scalar value. We're going to set that value to 0.1, 2, 2, 5, and then plug that into the clear code roughness input. Now we need to control the normal mapping of R2 layers. We're going to have a normal map that controls the paint layer, that is the clear translucent layer, as well as a normal map for the bottom surface underneath that paint layer. Let's start with the paint layer. We're going to hold down T, create a texture sample, and the details panel we're going to search for car paint. There we go. We're going to use T, car paint, normal. Now we're going to put this through a flattened normal node so that we can control the intensity of the normal map. We'll put a simple scalar value here, put that into the flatness input, and we'll set this to value 0.98. Then we can plug this into the normal input. Now that we have our normal map for our clear code layer, we need the normal map for the surface underneath. The next node that we need will not be available to you in the material editor until you've made a change in your project settings. So let's go there now. Go to settings, and then project settings. From here, at the very top, search for clear, and then you'll find clear code enable second normal. Make sure this is checked. Let's jump back over to our material. Now that we have access to the clear code node, we can right click and search for clear code. You'll find it here clear code normal custom output. You'll notice that it doesn't have an output. This means we don't have to plug it into the result node over here. But we do of course need to plug a normal map into its input. So we'll create another texture sample, and it will search for the carbon fiber texture. Here we are carbon fiber normal map. Again, we need to pipe this into a flattened normal. We'll put that into our clear code bottom normal. And then we need a parameter to control this. So we'll hold down the one key, create our scalar, and plug that into flatness. I'm going to set the default value to 0.5. So let's do some tweaking here. First of all, we do need to apply some UV map tiling here to our different textures. So we're going to start out with a multiply node. And then we'll plug that into the UV inputs of all of our different texture samples here. So we've got our roughness texture and our two normal maps. We'll need a texture coordinate. So hold down you to create that. Plug that into the A input of our multiply. And then we'll create a scalar value to plug into the B input. And we're going to set this to a default value of 30. Let's go ahead and convert some of these into parameters now. We'll right click here. Convert this to the parameter UV tiling. And then we'll set up the flattened normal parameter here. We'll just call it bottom normal intensity. Let's also set up our clear coat roughness parameter. We'll call it exactly that clear coat roughness. And we'll set up our clear coat effect parameter. So we'll call it clear coat. As well as our top normal, we'll convert that to a parameter as well. We call it clear coat normal intensity. Let's go ahead and save that. And we'll create a material instance. We'll call it MI clear coat. Let's go ahead and apply this now to element zero of our shader ball. All right, so our shader is applied. Let's go ahead and tweak the material instance parameters. So we'll need to enable everything. So we've got our bottom normal intensity, our clear coat effect, our clear coat normal intensity, our clear coat roughness and our UV tiling. Let's see if we can match it up with the shader ball next to it. First of all, we're going to tweak the UV tiling. I'm going to set it to a value of five. That should match up with the shader ball next to it a little better. If we tweak the clear coat setting as I bring this down, you can see the surface becomes less reflective. And if I play with the bottom normal intensity, if I set this to one, we lose our normal map, you'll notice that the color is gray and we don't have a texture defining the shape of our carbon fiber. But instead, it's being defined by that lower normal map. Likewise, we can play with the clear coat normal map. Let's bring this down to 0.1. And you can see now it has more of a stippled look to it. And as I play with this value, we can see the stippling effect becomes more or less diffuse. Let's go ahead and set it back to a value of 0.98 and we get a nice smooth surface. And again, we have our clear coat roughness. Playing with this value makes the clear coat layer more or less glossy. Let's look at one more thing we can do with our clear coat effect. And that is to change the color. So let's go ahead and convert our color now, our base color to a parameter. And then we'll tweak it in the material instance. Inside the material instance, we'll enable the color picker here and select that. And now as we play with the color, you can see that we're changing obviously the base color of our shader ball here while still maintaining the normal map. So it appears as if the carbon fiber itself is having its color changed. Your coat is a great shading model that works well for any surface that has a translucent layer of paint or clear film over it. This is a common paint style found on cars, so clear coat works really well there. However, it also works great for surfaces such as glazed pottery and dishware or even furniture and architectural features with wax coverings or high polished translucent paint over bumpy surfaces. Continuing with our discussion of shading models up next, we're going to look at the two sided foliage shading model and discuss when and why you'd use it.