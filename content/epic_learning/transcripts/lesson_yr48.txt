We have now learned both what Gaze and Dwell and Gaze and Commit are, as well as how to set them up using Blueprints. In this module, we're going to again expand on the logic to create a dynamic solution that will allow us to use both for different purposes. We're going to use Gaze and Commit to select an object to attach to our headgaze, as well as detach it, and Gaze and Dwell to inform our friend that we have solved the problem and they can get their treasure. To accomplish this, we'll need to create a few new assets. Let's get started in the Gaze problem map. What we have here are two islands, our character alone on an island and the treasure on another. It appears that someone forgot to build the bridge between the two islands. We will need to create a solution to get that bridge set up using these extra floor pieces that are floating around. Navigate to the Blueprint folder inside of World Builder. Create a new folder named Enums. Inside of it, create a new enumeration asset by right-clicking and under the Blueprints category, select Enumeration. Name it E-Gaze. Of course, E-for-enumeration, and that'll make it much easier to find when we create new variables. Open it and add two new enumerators. The first one, named Gaze and Dwell, and the second named Gaze and Commit. And save it and close. Inside of the core folder, create a new Blueprint interface named BPI underscore hollow lens. We're going to set up reusable functions here that we can use now and in later courses. Open up BPI hollow lens. Rename the new function to interact with user. Give it one input of type E-Gaze and name it Gaze method. For now, we'll add one more function named Celebrate. Compile and close this. Let's first add this to the characters Blueprint. Open AI Friend. It is a simple character Blueprint that, after begin play, it simply waits a bit and begins to search for whatever value of move to target is set to. Delete Event Begin Play's logic. We'll call it instead when the bridge is ready. For a Blueprint to use the Blueprint interface, we need to implement it. Open the class settings at the top and on the right under Interfaces, add our BPI hollow lens and compile. Now that it is implementing the interface, it has access to all the functions within and can be called by other Blueprints. Right-click on the editor and search for Event, Interact with User. Now any other Blueprint can call this event and this event will run. But we only want it to perform a further action if Gaze method is set to Gaze and Dwell. From Gaze method, promote to a new variable with the same name. Delete this set as we want to compare the two instead. Control Drag a reference of Gaze method into the editor and compare the two. If true, call the Search function. So instead of starting on their own, they will wait until we say go. Next, right-click and add Event Celebrate. After they retrieve their treasure, we'll call this event and they will play a new animation. There's a couple of steps to get this working. Alright, get a reference to the Skeletal Mesh component, named Mesh. From Mesh, create a GitAnim instance node so we can access the Animation Blueprint. Drag from the Return value and cast to Epic Character NMP. Because we know that the character's animation Blueprint will never change, we can right-click on the Cast node and convert it to a pure cast. And from As Epic Character NMP set is celebrating to true, then add a 0.2 second delay and set it back to false. Return to the Content Browser and navigate to Master Pickup BP and open it. Find the Begin Overlapped Event. Towards the end of it, move Destroy Actor over a bit. Okay, now from Other Actor in the Overlapped Event, call Celebrate and call it right before we call Destroy Actor. If the Actor that overlapped with this box is implementing the interface and using the function Celebrate, the function will be called. Go ahead and compile and close this. Now, let's move on to the floor pieces. Open BP Nav Bridge. All we'll do here is implement the interface for passive communication and add the E-Gaze variable. Again, class settings at the top and implement BPI HoloLens. Lastly, add a new variable of type E-Gaze and name it Gaze method and set to Gaze and Commit. Compile and close. Inside of BP pawn, let's clean up a little from the previous module. At the end of the eye tracking event, delete the Cast to BP Island Gaze and the following event call as we don't want to directly try to communicate with it. For one, that blueprint isn't even in this map. Plus, we're now going to be taking advantage of using blueprint interfaces for communication, which are a much smarter way to design our blueprints. We have now prepared the mechanics we have learned in multiple blueprints that in the next module, we will use to build a path so our friend can get their treasure. Get ready for our first coming to the rescue module.