In this video we will look at creating an all-important collision detection option in order to stop our scatter system from spawning mesh actors inside ones that have already been placed in the scene. Our first port of Car then is to drag the spawn point variable onto the graph view and this time choose the set option. We need to make sure that each position our loop selects, so where each mesh is, gets passed into this vector so that we can then use it for multiple aspects our parts of our tool. Dragging from the output of the make vector node that we have just created, let's pipe that into the vector input on our spawn point variable and then pipe the loop body output from our last far loop node into its execution input. Now if you're wondering just what the loop body node does, well we will come back to that a little later on. For now though, let's start on the collision detection itself by dragging out the collision trace range variable and then choosing the get option. As collisions are going to be tested far by looking straight down from each mesh's current position using a capsule like trace, we want to search far and then add another make vector node here and then we want to make sure that the collision trace range variable is piped into its Z or Z input. To explain the collision trace range is the measurement that will determine how far the blueprint looks in order to test far a possible collision. And so we will need to combine this with the information regarding where the current mesh is going to be spawned so that we can see if any other mesh is in the way, which of course means more maths notes. Dragging from the output of our variable again, we can add a divide node with a value of five in its second float input. Now this will be the radius or search area for our trace, we are dividing by five here as this will mean that no matter what length we set our trace range to, the radius for the collision detection won't be too large as we are surrounding the object in a capsule like trace. Let's drag from our divide node now creates a second one and then set its second input to a value of one, which we will now use. In order to calculate our endpoint dragging from its output let's create a new make vector node and then ensure that it is fed into the Z axis only let's then create to add vector to vector nodes far the outputs with one being our start and the other being our end point. Although we will need to add a position offset here in order to compensate for the spawn areas actual position in the world which we will do later. With the groundwork for our detection system in place we are now in a position to test whether or not there are any objects in the way, although we do still have more to add to complete the system and so let's move on to our next exercise.