Now that we are familiar with control rig editor and what are the different rig elements that we can create, it's time to create controls or visual shapes that we use in sequencer to animate the character. For the control rig hierarchy, I'll start with the root control or the global control. So I can right click and create a new control and I'll rename this as root control. Under the details panel, I'll stick with the Euler transform as control type as I want the root control to have location, rotation and scale. Then let's keep the animatable property to true as we want to use this control for animation in sequencer. Under the transform category, we can define the placement of root control using offset transform. Well, keep the location rotation to be zero as the root control is going to be on the origin. And let's change the shape so that it's not a default sphere anymore. Finally, with the root control selected, I'll switch to the shape manipulation mode by hitting control plus period on the keyboard and scale the control so it's big enough to encapsulate the mesh. I'll press control plus period again to toggle out of this mode. Next I'll create another control for our pelvis utilizing a slightly different method. This time I'll look for the target bone which is the pelvis in this case. Select the bone and press control plus N. This will create a new control snap to the pelvis bone. In this case, the control is created under the bone hierarchy with the selected joint as the parent. Hence, you'll see the offset transform to be zero. I'll go ahead and rename this to body control. Let's change the shape and scale of the control so it has better visibility than just being a small sphere hidden inside the mesh. I'll give it a different color than the root control and hit compile. You'll notice that the same color is reflected in the rig hierarchy as well. Finally, in the rig hierarchy tab, we'll drag a body control under the root control as we want all the controls to follow the root. Lastly, I'll create another control using yet another method to set the control transform. This time we'll create a pull vector control for hand as it'll be required with the IK hands setup to adjust our elbow orientation. Let's create one for the left hand. Now we'll look for the elbow joint which is lower arm L. Right click on it and this time under new we'll select add controls for selected. This creates another control outside the bone hierarchy with its offset transform set in world space. I'll clear the search and let's rename this. Let's change the shape to pyramid solid. Now the offset transform of this control is snap to the elbow joint but we want the control placed outside of the mesh where it is easy to visualize where the elbow is pointing to. Also, it makes it easy enough to select the controller while animating. So you can do this by switching to setup event but I'll show you a different technique. So I'll move this control in the viewport and you'll see your current transform being updated in the details panel. But we want to keep our current transform to zero for animation and update our offset transform to match the current transform of the control. To do that, just right click on the control and you'll see different options here under the transform section. Click the option that says set offset transform from current. This updates our offset transform with the desired values while setting the current transform to zero. With that, I've shown you different ways that you can use to create controls for the character. So I'll skip to a complete setup with all the controls created using similar methods. Now I've created all these controls and arrange them in a hierarchy. Before we end this module, let me show you how to use the multi editing feature in control. So I'll select all the spine controls here and now I can change all their colors at once. Not just colors, I can also change all the shape properties for multiple controls at once. Now that we know how to create and setup controls for different joints, so let's move on to the next module and define the functionality for the controls in the rig graph.