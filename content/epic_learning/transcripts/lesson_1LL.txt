Some surfaces have traditionally been difficult to recreate in 3D, such as cloth. There are a number of ways to simulate cloth surfaces in Unreal Engine, including using subsurface materials and Fresnel effects. But Unreal provides a cloth shading model, which works great for simulating the fuzzy aspect of cloth when seen at different angles. Let's look at how we can make use of this shading model to create a highly customizable cloth surface. Before we begin, make sure you have the exercises map loaded found under content maps. From here, go up into content and then into the working folder, and we're going to create a new material. We'll call this material M underscore cloth master. While we're at it, let's go ahead and create a material instance, and we'll call this M i underscore stool cloth. Before we continue, I'm going to be making use of a substance material to get a cloth texture. If you have your own cloth texture laying around, feel free to use that. But you'll find this substance under content, substances. The substance I'm using is called synthetic for-way diamond, and we'll be using the base color texture. Let's go back to our material and open it up. The first step we need to take in order to make use of the cloth shader is of course to go to the details panel, and where it says shading model, we'll set the drop down to cloth. The blend mode should be left at opaque. If you wanted to use this cloth on a two-sided object such as a curtain, be sure to check to side it. In this case, we won't be doing that. Now with cloth shading model selected, of course we have the basic PBR inputs here, but we also have access to fuzz color and cloth. The cloth input will allow us to specify whether or not a specific area of a mesh is in fact cloth, so we could use a gray scale texture map here. In this case, we're going to use a simple scalar value of 1. Before we get into fuzz color, let's go ahead and set up the base color, the roughness, and the normal map. We're going to set up this particular material so that it makes use of a base cloth texture and a pattern texture such as stripes. We're going to make it highly customizable so that you can change the color of the pattern texture as well as whether or not the normals between the two textures should be blended or whether or not we want to have a pattern at all and a few other options. Let's start out by adding the textures that we're going to need. We'll create two texture samples. The first texture sample will be used for our cloth texture. In a details panel, we'll use the drop down and search for diamond. This gives us our substance texture list here. The one we want is the one that has base color as the suffix. Now for our pattern, we're going to use a texture called stripes, so we'll search for that, and we have it here, T underscore stripes. We're going to need UV mapping coordinates for both of these so that we can control the tiling. We're going to use our custom material function, search for tiling, and then we'll copy and paste that to get a second copy. The first tiling UV tiling output will go into the UV input of our first texture sample and then the second one into the UV inputs of our second texture sample. They will need a couple of vector twos for each of these tiling functions. The first one here will go into panor speed and then the second one into UV tiling. And it will repeat the process for the second tiling function. Let's go ahead and convert this to a parameter and call it cloth tiling. And we'll convert the second one down here also to a parameter and call it pattern tiling. Let's set these to default values of one. From here we can continue building our base color graph. Now what we need to do is blend these two textures together for our base color. We're also going to set up so we can control the colors of our pattern here for that we're going to need a three vector and we'll convert this to a parameter and call it cloth pattern color. From here we're going to need a multiply node and we're going to multiply our cloth pattern color by the alpha output of our pattern texture sample. And let's go ahead and set a default color value for our cloth pattern color. Let's go with something like blue. If we open up our multiply node here we can now see we have blue stripes. Alright, so next up we're going to multiply the texture sample for the cloth by the inverted alpha channel. For that we'll create a one minus node holding down the O key and then take the alpha channel of our texture pattern and plug that in there and then we'll need another multiply node and we'll multiply the inverted alpha channel by the texture samples RGB output. And if we open up this multiply node now we can see we have the cloth texture in a stripe configuration as well. But in this case the stripes are inverted from the stripes above. From here we'll set up a multiply that will allow us to control the intensity of the color of our cloth texture here. So for that we'll need another multiply node and then a scalar. We'll take this multiply and then plug that into the A input of our multiply here and then our scalar into the B input. We'll convert the scalar into a parameter and we'll call it cloth texture intensity. From here we simply need to add these two together. So we'll create our add node, take the multiply output here, put that into the A input of our add and then our second multiply will plug that into the B input of our add and then we can see the final result of the two being added together. Now right now this looks the same as this. That's because our color intensity for the cloth is set to zero. Let's go ahead and give it a default value of one and now we can see the two added together. To allow for further customization we're going to add a static switch parameter that will allow us to enable or disable the stripes. So we'll right click here and search for switch parameter and what we want here is static switch parameter. We're going to name it use pattern color and then for true we want to go ahead and bring the add output into the true input here. For false however we simply want to take the RGB output of the original texture sample for the cloth and we'll plug that into false. Now we can take this and bring it right into our base color and we're all finished with our base color graph. We can move this out of the way now and we'll set up our roughness graph. Our roughness graph will be set up so that we can control the roughness of both the underlying cloth texture and our pattern. For this we're going to need a couple of linear interpolate nodes so we'll hold down the L key to create those. We're going to plug the first linear interpolate into the A input of the second linear interpolate and we'll need three scalar values. We're going to set the first scalar here to a value of 0.8 and then plug that into the A input of our LERP and then the second one we're going to convert that to a parameter. We're going to call this one cloth roughness. This will control the roughness of the underlying cloth texture and we'll plug that into the B input. Now we simply need to plug the cloth texture sample into the alpha channel here. We're going to use the alpha channel of the cloth texture sample. But before we do, let's go ahead and add a clamp node here. This will allow us to clamp the gray scale alpha between the values of 0 and 1 and then we can plug that into the alpha input there. This is set up to be future proof as currently the alpha channel on this texture sample is simply white. Next our third scalar here is going to go into the B input of our second LERP and we'll convert this one to a parameter as well and call it cloth pattern roughness. We're going to need another clamp node here and we'll plug that into the alpha channel of our second LERP and this time we're going to use the pattern texture sample. We're going to use another static switch parameter here and we'll call this one use pattern roughness. Now the true input needs to have our LERP plugged into it here and then if false if we don't want to use the pattern roughness, we'll want the second LERP to plug into that and now we can plug this into our roughness input. So now that we've got the roughness and base color input it into our material, we'll pick up in the next chapter with the normal maps and look at how to blend the two different normal maps together as well as looking at adding the fuzz color.