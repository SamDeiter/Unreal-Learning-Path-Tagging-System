We're going to be looking at how you can find the C++ equivalent functions to nodes you might be using in Blueprint. In the last lecture, I magically went and did that. I went and found that Get Component Location was the replacement for Get World Location. And also, I used this function, this mysterious function called Get Forward Vector. Now, how did I know that? How did I know that I needed to include the UKismat Math Library at the top as well? How did I know all of this? Well, actually, the process can be fairly straightforward to map the two and the trick is in hovering over the nodes in your Blueprint Graph. So, if you've got, for example, Get World Location, as I've got here, I'm in the BPGrabber and I am in the private function called Get Hold Location, which is what we're going to be migrating across in this module. And if I hover over Get World Location, you can see that the target is Scene Component, which makes sense. In fact, it is a U Scene Component in C++. And if we hover over the Get Component location, we should be able to see that, indeed, that is on the U Scene Component, and it is on the U Scene Component because we are applying it to this grabber. If you go and have a look at the node, you can see that the target is actually self, this current Blueprint. So, that's kind of how this is starting to map across. But then you see that the name is different. And you think, well, how did I find out that the name was, what it was? Well, there's two ways of figuring this out. Really, well, the best way to figure this out is to actually go and have a look for where that name is coming from in the Source Code. So, first things first, we need to go to the file that declares the U Scene Component, the U Scene Component Head of File. Two ways of doing that, you could select any of the symbols to the U Scene Component and hit F12. Or you could do a Control-Comer to bring up Visual Studios Go to All, and you could just type in Scene Component, or one word without any prefixes. And you can see that there is a lot of stuff comes up here. If I add .H, then we can go straight to the Head of File. Let me just close down this view, and we can see the tab that has opened up is the scenecomponent.H. Now, in this file, if I looked for Get World Location, you can see that it is actually some of the metadata applied to a U function that is obviously callable from Air Blueprints. And you can see that the actual name in C++ is K2 underscore get component location. That doesn't seem very nice. That doesn't seem like what we should be calling. So if you hit F12 to go and have a look at this definition, you can see that all this is doing is calling to another function called Get Component Location. And that's what we ended up using. So oftentimes, there are wrappers for the Blueprint around C++ functionality. And in C++, we can just use the C++ functionality directly. And we'll see the same sort of wrapper stuff going on when we talked about that forward vector. So we've got, again, a node which is Get Forward Vector. How did I know that I need to use the Kismet Math Library? Because if you hover over the node, it says that the target of this is the Kismet Math Library. Now, it doesn't have a self because it's actually a static function. So if I again do my control comma trick and I go to Kismet Math Library dot H, and we go and open that up, you could go and search for what was the function Get Forward Vector. And there's basically here, there's Get Forward Vector, it's a static function. But if I F12 into it, you can actually see that I could simplify the code that I've got in my grabber.cpp. If we go over to the grabber.cpp, you see I'm calling this big static function to get the forward vector. But what this is telling me in the Kismet Math Library is that the implementation is just calling dot vector on our rotator. So that's what I could replace it with. Instead of getting the forward vector, I can just do my Get Component Rotation dot vector. And that gets me the forward vector of a rotator. Now, you could repeat this approach with all sorts of nodes. For example, in our function in the grabber bp, if we look forward trace for physics bodies, you can see there is a sphere trace for objects. If I hover over that, then it is going to tell me where this came from. And indeed in this case, it's saying it's coming from the Kismet System Library. So again, you could go and have a look at that. You could see that there is a bunch of cool stuff that the Kismet System Library is doing. So you probably would want to use that. But you could also use some of the underlying stuff that the Kismet System Library is using. And by the way, when we see the word Kismet, this is often code for Blueprints because Kismet is the system that predated Blueprint and came before it and Blueprint is its successor. But throughout C++, we often have reference to Kismet or K2. And that is basically what it's saying that this stuff is libraries specifically intended for use with Blueprint. So let's have a look at some of the most common includes you are going to encounter. The first one is you world. You world is the class that represents a map or a level. And that's a bit confusing because in Blueprint terminology that world is very little used, but you do see this terminology crop up in the world outliner. So that's how you might be able to remember what it means. And it's include is engine forward slash world.h. Then the actor. So a actor is under Game Framework forward slash actor. The U component in in fact the scene component is located under the components folder. The U gameplay statics which we haven't encountered yet, but does crop up quite a lot, especially for things like getting a player controller. So if we had the get player controller here, then if you hover over that, you can see that that goes to the gameplay statics. And gameplay statics is often just a wrapper around some other stuff like Uworld, but it's often a very helpful wrapper just like the Kismet system library which is on the next line. And then whenever we see things like the Kismet Math library, we can usually just imagine that that's a wrapper around something in F-Math or something on the particular classes. So if it's a F vector, then you might have functions on the F vector or the F rotator. And if it's utilities like clamping and lurping and sine waves, often they have counterparts in F-Math. So I recommend using the counterparts from F-Math. You can use the ones from the Kismet Math library if you want to, but F-Math is essentially the underlying thing in C++ anyway. So let's put some of this into practice by bringing it across some new functions. The two functions that I want to bring across in this module is the get hold location and get physics component, bring them over into C++. In order to do that, get hold location does reference a variable that is blueprint only so we need to bring that variable across two, which is the whole distance, but we know how to do that already. In fact, if you're feeling very confident and want to give this a try, you could pause the video and have a go then come back and see me do the result. So first off, we'll go into the grab a dot h and close down at any extraneous tabs that we don't actually need. And we're going to create a new U property and it's not going to be the max grab distance, but it is going to be the hold distance. So let's call it that. And I'm just going to fix up the indentation, which often gets messed up by these macros unfortunately in visual studio. Now what do we want that whole distance to be? Well, 100 seems to be the default there in the blueprint anyway. So that's what I'm going to stick with. And then hopefully we should be able to go ahead and build the result. And as a result, we see that our whole distance there has this underscore zero. And I'm not going to bother replacing it just yet because we're just trying to get this get hold location function over in to C++ anyway. So let's do that first. Let's copy or let's just type it out for simplest to say we're going to do an F function. We want it also to be blueprint callable. And we do want it to be blueprint pure as well. And it's going to return what? Again, it's going to return an F vector. It's going to be called get hold location. And it doesn't need to take any arguments. It is also const. Now I am going to go ahead and copy the signature to the clipboard. And we can have a go at the implementation. Now the implementation should look very similar to our get max grab location because pretty much we're doing the same sort of thing. We're just adding the whole distance instead. So let's go ahead and actually copy most of this and just multiply by the hold distance. Now if you were doing this a lot and you had a lot of repetition like that, then I would suggest trying to create some sort of function. But at this stage, I think it makes sense just to have a bit of duplication. So after we have compiled, we should go in and see that our hold location is now giving us an error. And we can delete the whole location, delete the whole distance. Now hopefully in the event graph where get hold location is being used, it has been updated now to the new version from C++. So what also remains for us to take across is this get physics component. Let's see where get physics component is used apart from here in the event graph. Find references used in a couple of places. It's used in grab. It's used in release and in the event graph itself, of course, and in the creation of the function. So we want to do this equivalent over in C++. So that requires us to know what to do with converting these to nodes. So the first one is get owner. And you can see the target is the act component, but actually the target is ourselves because act component is a super class of the scene component. So we should be able to see quite easily how to get hold of that. Then we have get component by class, which the target is actor. And I'm going to show you how we can find out what this implementation is in C++, but also how you can use a slightly easier thing in C++ to get hold of the physics handle. But first we have to do the signature. So let's go over to the grab.h and we're looking for get physics component. That's what we're going to create. And it's going to be pure as well and blueprint, callable. So exactly the same as before. So I'm going to copy the u function and our return value here is going to be of type physics handle component object reference. Okay. So let's have a look. We know that the type is a physics handle component. What is that type? If you want to try and find it, we could do a control comma again and do a physics handle component. And you can see we've got physics handle component, dot h. And if we open that up and hover over the tab, you can see that the path is quite long. I just want to show you how you can cut down a path that is this long down to one that makes more sense. Usually you start after something like classes or public, you don't have to include the whole thing. And usually there's only one folder. So it is physics engine forward slash physics handle component. I think that we want to include. So let's go over to a header file. And just above again, remember the last include in this file has to be the generator.h. So just above that one, we're going to do a hash include physics engine forward slash physics handle component like so. And that if we open it up, where is the actual class here? It's called u physics hand component, which we should have kind of anticipated because we know that u is the prefix for a lot of things. And because we're dealing with a reference, this needs to be a pointer to a u physics handle component that we are returning here from a blueprint callable. And then we're going to call it get physics component. It doesn't take any arguments. And it should also be cons because we're not actually modifying anything in this getter. So let's copy the signature, go over to our c++ and try and create an implementation. So get owner is fairly straightforward. You can just call get owner on the grabber. So get owner will return us an actor. And then we need to dereference that actor in order to call something on it as the target, which is going to be a get component by class get component. Let's see what's available there. Nothing's showing up. The reason this isn't showing up is because we haven't included that actor header. So there is a fairly straightforward two fairly straightforward ways to do this. The first one is we could try and click on the actor symbol somewhere. So we could jump to the get owner definition. And then we could jump to the return type with f12. And that would take us to the actor file one way. The other way is to use the control comma again to do the same thing. And as we said, it's game framework forward slash actor. We said that in the slides already. So this one I can just include in the c++ file. It's going to be hash include game framework forward slash actor. And that means that now we can dereference get owner IntelliSense should work. So get component by class. You can see that that is an option indeed. But I'm saying that there's a better option. If we f12 to the definition of that, I'm actually going to go to the header file by control k0 and try and look for the same get component by class. And what we will see here is that there are a lot of functions with very similar names. Find component by class get component by class get component by tab. And further down here, just a little bit, there's a templated version. Find component by class. And you can see the template automatically does some casting for us is just a little bit nicer to deal with because we know that we're going to get the right type back. So let's do that one instead. Find component by class. And we know that the type we want is the u physics handle component. And that's all we have to do. We don't have to pass anything into this function. The templating does everything for us and c++. And that's what we can go ahead and return from our function. And because the function types match up, you see the return type there is a pointer to a u physics handle component. So once we've gone ahead and built that and go over into our blueprint, you can see we've got error. But we can now delete get physics handle component compile and all the references to get physics component have been updated and are now calling to our c++ code. So if we go ahead and test it out, everything should be as before. Hopefully you now understand what some of the common includes you will see in c++ are and do also how you can find these includes from existing blueprint nodes or use blueprint to discover functionality. And hopefully feel more confident into diving into the c++ in order to find the underlying functions that are being wrapped and the correct include locations.