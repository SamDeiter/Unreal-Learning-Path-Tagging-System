In this final exercise for the course, we will get our blueprint to use all of the options and parameters that we've created in order to fill the spawn area with our static mesh actors. Come into the graph view then we can drag out from the get node and create an add instance node. Coming to the sequence node that we said we would be using a couple of videos back, this being located near the end of the collision detection flow. Let's take it's then one output and pipe it into the execution input of our newly created add instance node. Now the reason we've done this is because it means that when our collision detection finds a space to spawn an object in, it will fire this add instance node and add a mesh to the scene based on the current transform options and selected meshes from our stream. Finally, we want to drag from our make transform nodes output and place it into the instance transform input, after which we can compile and save. Let's then click on the spawn area that is placed in the scene and in the details tab come to the meshes to spawn a ray option that we exposed and click on the plus button four times in the newly created array inputs we will search for the word grass and then add the grass meshes that come with the sample file. So 0 1 to 0 5. Then if we move our spawn area to the tree in the center of the level, we can increase the spawn area radius to around about a value of 307. The number of meshes to spawn can be set to 3000. And then we can move the spawn area around to see if our collision detection is actually working by stopping the flowers from spawning into the rocks. Finally, let's turn the cluster and scale file options both on and off to see if they are also working. And with that, we have created a very versatile and usable scattering tool for our unreal projects. To test our seed control, we can come back to the blueprint, click the make public button next to our variable and then we can compile, save and test it out in the level.