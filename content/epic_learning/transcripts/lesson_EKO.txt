We have built a custom widget with the intention of communicating with lights around the scene. After rendering this widget in our 3D scene, we need to prepare it to communicate with other blueprints. In this module, we will set up the buttons and slider on our widget to alter the lighting in our scene. While we can communicate with the lights in the scene without turning them into their own blueprints, doing so will make reusing and managing them simpler. Select one of the lights and click on Blueprint slash Add Script. We'll name it BP basic light. Select Harvest Components and set the parent class to Actor. We have chosen Actor as the parent so that we can create child instances of it with different types of lights if we needed. You may have noticed that the lights along the main wall had a stronger intensity than the ones down here. We need to be able to change these in the map as well as account for those differences within our blueprint. Let's set up those events now. So we don't need to do this multiple times. Let's again use our BPI HoloLens to define the function. Open it and we'll need two functions. Name the first one Update Light Color. Add one input named New Color and set its type to Linear Color. The next function name it Update Light Intensity. Give it one float input named Alpha. And Compile. Return to our BP basic light and implement the interface and compile. Add both of our functions here, Event Update Light Color and Update Light Intensity. From Update Light Color, drag in a reference to our light component and create a set light color node. We will plug it directly into New Color so that the widget will decide on the actual color to translate the string on our buttons to. When we press on Blue, what does that mean? Which blue should the light change to? We'll let the widget be in charge of that. Create a new variable named Starting Intensity. Set it to a float. Set the default value to 40.0 and turn on Instance Editable. Open the Construction script so changes to this variable take immediate effect. Drag in a reference to the light and create a Set Intensity node. Plug Starting Intensity into New Intensity. Return to the Event Graph. From Alpha, create a Map Range Clamped node. Keep InRange A and OutRange A at 0.0. Set InRange B to 1.0 and plug Starting Intensity into OutRange B. Drag in another reference to the light and create a Set Intensity node. Plug Return Value into New Intensity. The Map Range Clamped node will convert the values 0 to 1 to 0 to Starting Intensity. Okay, before we move on, pulling these lights into a Blueprint has set the mobility to Moveable. We need to switch it back to Stationary. Compile and return to the Map. Set the bottom two lights starting Intensities to 5. So when our slider is at 0, all the lights will be off. When it is at 1, the top lights will be 40 and the bottom lights will be 5. Very nifty. Alright, our lights are set up. Open BP Widget Blueprint. MRTK has provided us an easy to set up component for this exact purpose. To interact with our widget with the HoloLens 2, add a UXT widget to the Blueprint. That's it. We can interact with the widget. Open our W Modify Lights widget. Before we set up any events, take some time to organize the hierarchy little so the widgets are easier to find in the graph. Name the buttons, button underscore and the color they represent, and name the slider, slider underscore intensity. Select the image and set is variable to false as we won't change any parameters on it. Open the graph. In the variable window, we can see all the interactable items we can add to the graph. Select button blue. We can add an event for on-click, on-pressed, and on-released if we need. But because we only want the change of a color and nothing to happen when we let go of the button, we will just use the on-click event. Create an on-click for all three of our buttons. Now create a new custom event named update color. Give it one input named new color and set the type to linear color. Instead of searching the map every time we want to communicate with our lights, let's cache that info into a variable and set it at the widget's version of begin play. Create a new variable named lights in map. Set it to type BP basic light and change it to an array. From event construct at the top, create a get all actors with interface. Set the interface to BPI hollow lens and add a four-inch loop. To identify our blueprint, we'll cast from the array element to BP basic light. Drag in a reference to our lights in map array and add the actor to the array. Now we can use this array to only communicate with our lights, which will be much more performant. Return to the new update color event and from lights in map, create a four-inch loop. From array element, call update light color and plug new color into new color. Now on all the on-click events, we can call update color and provide each a unique color. Select slider intensity and add an event on value changed. Duplicate the lights in map and four-inch loop, but instead call update light intensity. Okay, let's test this in the hollow lens too. These light colors are clearly much brighter than the blue that was set by default. Luckily, we can adjust this intensity. In this module, we added interactivity to our 3D widget. We used buttons to change the color of a new light blueprint and a slider to adjust the intensity of the lights. In the next module, we will prepare a spawnable widget alongside an MRTK button that will follow the position of our hands.