With our first tile set now created, it is finally time to use that to create a tile map we can use in our level. We will begin by creating this new tile map and configuring it to use the tile set. We will then use the tile map tools to paint out a layout suitable for our player character to navigate in an upcoming module. Let's start by creating a new folder to store our tile maps. Using the Content Browser navigate to Content My Content and create a new folder called tile maps. Next, open the tile sets folder, so that's Content My Content tile sets. And right click on the Unreal Paper 2D tile set, underscore master, underscore tile set we created previously. On the tile set action section at the top of the menu select create tile map. Once we press enter to confirm this name, use the sources view of the Content Browser to move this asset into the tile map folder. Navigate to the tile map folder and double click the new asset to open the tile map editor. On the left, we have a viewport displaying the active tile set. And if we hover over the active tile set option, we can see that it is already set to our master tile set. The largest viewport in the middle shows us the tile map itself, which is currently empty. Moving over to the details panel on the right hand side, we can see that the tile width and tile height have automatically been adjusted to match our tile set. This has happened because we generated the tile map directly from the tile set. Just above these options we have map width and map height. These numbers set the number of tiles used within the map. Let's go ahead and make our map a little bit bigger and set both of these values to 24. And now let's return to the central viewport and zoom out so we can get a bit of view of the entire map at once. We can also use the tile grid option in the toolbar to help us visualize how this larger map is broken down into those 24 by 24 tiles. And with that, we're almost ready to start populating our tile map. We just need to set up a few layers first. At the very top of the details panel, we have the tile map rollout. This rollout contains the tile layer list and we use this now to define the layers that we need. By default, new tile maps have just a single layer called layer1. We're going to need three layers, one functional layer with a collision that the player character will stand upon and two decorative layers without collision that will be placed in front and behind of this functional layer. To help us remember the purpose of each layer, we'll name them appropriately to indicate the theoretical position relative to the camera. That will be foreground, middle and background. The player will be positioned in the middle layer with tiles we want to show in front of them placed on the foreground layer. Finally, anything we want to place behind the player character will be in the background layer. Let's start by left clicking the layer1 text in a layer list and renaming this layer to background. For this background layer, ensure that the layer collides checkbox is not checked. We do not want the player to collide with anything on this layer. It only exists for visual detail. Now let's create a new layer by clicking the new layer button to insert a new layer above the background. Let's use the same process to rename this layer, middle. For this layer, make sure that the layer collides checkbox is checked in the selected layer rollout below. This means this tile layer will use the collision data we created for the tile set. Finally, let's repeat the process one more time to add a new layer and name this layer foreground. Ensure that layer collides is not checked for this foreground layer. Note that because of the order we added the new layers, they are already in the correct order in the layer list, with a foreground at the top and the background at the bottom. If at any point we need to change this order, we can do that by using the bring forward and send back with buttons directly below the list. There's just one more important thing we should take note of when it comes to the layers. In the setup rollout of the details panel, there is a variable called separation per layer, which defines the relative depth of each of the layers we just created. This is important because when we come to place our player character, we need to make sure that they're placed at the correct depth, being in front of the background, but behind the foreground. We'll start with the most important layer first, but one the player character will be directly interacting with, the middle layer. Start by ensuring that the middle layer is selected from the tile map rollout in the details panel. With that confirmed, let's start by selecting a single tile from our tile set and placing it into that middle layer of the tile map. Select one of the platform top pieces from the tile set by left clicking it in the active tile set. With the tile selected, move the mouse over to the central viewport and we'll see a preview of the tile placement. We can also see some text to indicate which tile is currently active and which mode we are in. We are currently in the paint mode and this means left clicking will paint a tile directly onto the tile map of the selected layer. We can also click and drag with a single tile to repeatedly stamp down that same tile. If we make a mistake, we can press Ctrl Z to undo the last tile placement. Or, if we prefer, we can select the eraser and left click to erase tiles. Just make sure to return to the paint mode by selecting it from the same toolbar after you're done. We can also hold down shift and left click drag within the main window to get a selection of tiles within the tile map. If we hold down shift and left click and reposition the mouse, we can see we have the entire selection ready to stamp down. This makes it quick and easy to replicate common platforms and shapes that we already have within the tile map. We can also select multiple tiles from the active tile set. And let's do that now to bring in this unreal logo that consists of four separate tiles. If click within the active tile set and select all four tiles and then release the mouse. Now, we can position this set of tiles in the central viewport just like before. Now, let's use some more of the tiles and the tools to create the edges of this platform. Begin by selecting one of the corner tiles and place it at the end of the platform. With the same corner piece still selected, click the flip horizontal button in the toolbar above the tile set. Now we can place this tile down at the opposite end of the platform. Now we'll use the flip vertical button to place the tiles underneath. And we can once again use the flip horizontal tool to get the opposite corner. Note that these flip tools work with any current selection, not just a selection from the active tile set. We can demonstrate that now by selecting a portion of the upper platform and flipping it to form the lower side of the platform. Hold down shift and left click to select a section of the upper platform. Use the flip vertical option and place the new sections. Now we just need our two sides. Select the correct tile from the active tile set and use the same method to place the left and right sides. Note that in some instances you may find it easier to use the rotate tools here. With the edges of this platform complete, we just left with a few empty tiles on the inside of the platform. Let's deal with those quickly by using the fill tool. Select the fill tool from the tile set toolbar and then select the solid green tile from the active tile set below. With the tile selected, click in the gaps in the platforms to fill them in. Now let's use the fill tool again to quickly paint in our background. Select the background layer in the tile map rollout in the details panel. Ensure that the fill tool is still selected and then select the plain tan tile from the tile set. Click anywhere within the main viewport to fill the entire background layer. Note that we can toggle the visibility of the middle layer in the tile map rollout by clicking the eye next to the layer name. We can do this to confirm that the entire background layer is filled. Let's finish up this first platform with some foreground detail. Select the foreground layer from the tile layer list. Using the same techniques as we did to create the platform in the middle layer, let's create a section of railing. After selecting the foreground layer, make sure that you have the paint tool selected. With the paint tool selected, find the left hand side of the railing in the active tile set. We can then also add the lower piece of the railing by selecting it in the active tile set and placing it below this section. Now let's add the middle section of the railing by selecting the correct tile and left click dragging across the top of the platform. Finally, let's create the other end of the railing by holding down shift in the middle window and drag selecting the left hand side pieces. Once we have the selection, let's flip the brush horizontally and paint in the other end. And there we have it, our first platform making use of all three tile map players. At this point, we have all we need so we can save and close the tile map. With that example platform completed, we can spend some time building out each layer until we've made something that we're happy with. Feel free to experiment with this provided tile set and spend as long as you like working on your own tile map. If you want some inspiration, check out the included example map, called example underscore tile map, included with the completed project. This should give you a good idea of how the set can be used to create all kinds of variation and detail for all the layers of your tile map. In this video, we built our very first 2D tile map. We created a new tile map asset and used our previously created tile set to populate the map. We created three layers of tiles within the map, a middle layer that included collision for the player character to stand on, and a foreground and background layer to add visual detail to the map. We used the tile map editor tools to copy, paste and flip tiles to quickly and easily build a visually interesting tile map ready to become part of a level file.