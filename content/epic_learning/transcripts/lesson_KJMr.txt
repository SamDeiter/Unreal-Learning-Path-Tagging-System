There are lots of different types of objects in a typical and real project. These can include static meshes, materials, textures, sculptal meshes, lights, various volumes, and a lot more. Each type of asset is an instance of a class. These classes contain a variety of valuable information and functionality. We can put to good use, assuming we can access it. In this module, we'll take a look at how to organize assets by class. And we're going to begin by revisiting the documentation here for the Editor-Esset Library class. This is the class that we used to gather all of the paths to each asset in the project. And we're going to scroll down a little bit until we find a method called Find-AssetData. So Find-AssetData takes as its only argument an asset path and returns to us an asset data object. Well, what's that? Conveniently, we can just right-click on it and open this in a new tab, which I've already done up here. So the asset data is going to give us some high level information about the asset itself without having to load the asset into memory. So we can certainly get the asset itself if we want, we want to grab the object. We get the class, we can get the full name, I don't know if it's loaded and so on and so forth. So, and there's a couple of properties here, which will be a little bit new. Properties function very similar to methods except they don't take any arguments, even though these methods also don't take any arguments. You may notice some apparent duplication. We've got a property called asset class and we have a method called get class. There are some important differences. Asset class is going to return the name of the class and get class is going to return the object of the class. So if all we want is the class name itself, i.e. Texture 2D or Setic Mesh, then this is the best way to go back in that. It's also some editor properties here. We will take a look at getting and setting editor properties here shortly. So let's go and take a look at code. I've created a new function here called get asset class. And the first thing that we need to kick this off is going to be the asset path. So if we're to got that up here, I could set this function to return asset paths, but it's kind of a short function and I'll probably just get rid of it. I'd rather try to keep things as consolidated as possible. So I'm just going to copy this functionality down. We've got the instance of an editor asset library and then we're going to use list assets, which is being fed the root directory for the project. So we've got all the paths in the project here stored in this asset paths list. And now we need to go through and get the asset data associated with each asset path. We hop over to the documentation to make sure I'm getting this right. And then just to make sure that I have everything set up. Quickly, I'm going to go in print asset data. So we will reload. Go and grab. Our function name. And what we get is a whole bunch of asset data objects. And you can see with the quick glance, a lot of it is human readable. And we can see the asset name and asset class. And there's going to be some paths, the various components. But particularly all we're interested in is just to confirm that it's looking correct. So I think we're good on that front. So we have the asset data. I don't really need to print that anymore. Let's get the asset class now. And we'll return to the documentation. Again, one is the name. So we'll use the asset class property. And we will add a print statement. Once again, to confirm everything is working as it should. Go and save that. We will reload. Run our function. And we can see that we are getting. The class of every single object here in the content browser. This is not the class of every object in the world. This is just the stuff that's available here at that root for slash gain folder. So now that that's working. Actually, I'm going to run that one more time. Because I'm going to be looking for a specific class here. So I'd like to set up a little bit of a modification here. So that I can return all assets of a particular class. So what I'm going to do is. Just add an argument to our get asset class, all this class type. So we're getting our asset class down here. And I'm going to be collecting these to be returned. So. Create an empty assets list here. And I'm just going to add some logic. So currently what I'm doing is I'm appending the path, right? But I might actually just want to grab the object itself rather than the path to it. So to get that, I'm just going to go ahead and. We'll use the asset class here instead. Sorry, asset data. And if we revisit the docs on asset data. Get asset. And if we revisit the docs on asset data, get asset. So what this is going to do is if I say, I'm looking for all the static meshes, this will append all of the things whose class aesthetic mesh into this assets list. Check and then return. And once again, just to confirm everything is working before we hit our return statement. We'll just throw a print in there. Save this and back over to Unreal. We'll do a reload. But now with my get asset class, I'm going to need to supply an argument here. So when I had my print out earlier, I took a glance at what the capitalization was for static mesh, hopefully I got it right. So here are all the static meshes in the project. Let's go ahead and see if we can update that with texture 2Ds. And here we have all the texture 2Ds in the entire project. So this is just a couple of examples to show that we now have a simple process for gathering all instances of a specified class in the project. The next step is to start asking questions about all these objects. And the next module will do a deeper dive on static meshes.