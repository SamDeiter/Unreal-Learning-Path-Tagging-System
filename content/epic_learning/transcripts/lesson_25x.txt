One of the most powerful additions to our scatter Blueprints functionality is going to be a raise. A raise will allow us to give the user control over storing multiple actors, but also allow us to store lots of vital bits of information pertaining to the static mesh actors as well. And so, we will once again want to come to the My Blueprint section inside our Advanced scatter Blueprint. And then click the Add New Variable button. We can call it Mesh is to spawn. After which we can change its variable type by typing static mesh into the search field. And then after expanding the static mesh rollout, click the Reference option. Now, sitting next to the Variable Type option, we have a button that if we click, well let us change the container or container's type as well. And so, let's select the array option. So, the one that has a grid of nine squares far at side-con. Now to give a little bit of explanation here, the array is needed because we want the user to be able to decide which static mesh actors are going to be distributed inside the designated spawn area. And because an array can store multiple sets of data, it sees perfect for storing multiple static mesh actors. Combine this with our ability to make these arrays public, which we can go ahead and do, and we are onto a winner. Now, we will then want to create another array by clicking the New Variable button. And then in the Variable Type drop-down, search for the words Instanced static mesh. From the filtered list that we get, we can expand the Instance static mesh component, and then select the Object Reference option. After which, we can change its container type to an array and then give it the name InstancedMeshers. To give a bit of context then, our first array will store the mesh actors that are to be selectable by the user. And then this new array will store all of the instances of those meshes, with the user being able to determine the number of stored meshes via the Intage Variable. Now, although we won't be putting these arrays to use until later on in the course, it is still good to get all of the framework in place so that everything is set up and ready to go as soon as we need it. And of course, if we still aren't seeing how we might use these arrays, or even understand how they fit into our blueprint, don't worry, as when we see them in action, they will make a lot more sense to us.