Now that we have our data Smith file imported into Unreal Engine, we can make some modifications to quickly optimize the import workflow for our scene. To do this, we can utilize some of the free sample Python scripts provided by Epic Games. In this module, we will swap some Revit materials with our own higher quality materials. Revit assigns metadata to different families in a Revit project. Metadata is most often used to store real-world information about the architectural element that the asset represents, such as cost, material, or information regarding physical properties like weight or insulation efficiency. Fortunately for us, we can actually utilize this metadata by using the Datasmith file format. To access this data, select an object in your project, scroll down to asset user data in the details panel at the bottom, expand the drop down, and under Datasmith user data, we can see all of the metadata that has been assigned to this object. In fact, every object that we have imported using the Datasmith importer also has metadata assigned to it, and you can readily access all of that data here in the details panel. The architect for this project already assigned materials in Revit, and we can see those whenever we select an object. Take a look here in the details panel, we already have a concrete material assigned to this floor. Let's replace this concrete on the ground with a much higher quality material. In Unreal, we have access to a pack of starter content assets that includes several high quality materials that I think could be great for this project. Let's add the starter content to our project now by going to add new in the content browser and choosing add, feature, or content pack at the top. Under Content Packs, let's choose the starter content and add to project. In our content browser, we can now see a starter content folder containing a ton of great free assets. Expand starter content in our content browser and take a look at some of the really nice materials we now have in our project. This poor concrete material looks really nice, so let's use that for our exterior. We could easily drag and drop this material onto the actors that already have the Revit concrete, but what if you had hundreds or thousands of objects that you want to use this material instead? Let's undo both of those actions by going up to edit, undo. Epic Games has provided several free, helpful Python scripts that we can use to make our data management process much easier, including a script designed to replace materials in our scene. To access this feature, we first need to enable the Python script plugin in our project. Let's go up to edit, choose plugins, select built-in, and search for Python. Enable this plugin, choose yes, and restart now. We'll place a link to this box folder in the provided assets for this course. In this shared folder with our Python files, go to Unreal Studio Examples, and download replaced materials. These other scripts could be helpful for optimizing your Unreal workflow, but we'll only be using this one script for now to swap out our materials. Locate the downloaded file and unzip it. In that folder, we can see these UASET file types and a Python file type. Let's open the script to see what we have to work with. I'm using Sublime Text, a free text editor designed for writing Python. By default, this script is set up to find all static meshes that are using this red material, and instead use this white material. All we have to do is simply replace the references for both materials. Let's navigate to these in our content browser to get the file path for our material. Select the ground object, then select the magnifying glass next to the material in the details panel to find where it is in your content browser. This material is located in DS, Epic House underscore DS, materials. If we go back to our Python script, we just need to input that path here for the red material. So starting with engine type game slash DS slash Epic House underscore DS slash materials slash concrete underscore hyphen underscore dark underscore underscore patio underscore. Then let's find the file path for our port concrete as well. Go to starter content materials and here's our port concrete in Python. Select the white material file path and type game slash starter content slash materials slash M underscore concrete underscore poured be very careful when you type these because the script won't do anything if you have a typo in here. Save this file and let's go back to unreal. Next, we just need to go to file execute Python script and navigate to the script that we just saved. Immediately, we see that all of the objects that had that red at concrete material now have the starter content concrete. If we want to do, we could modify this Python script and use it to replace our glass, wood, and carpet materials to do so. Just swap out the references that we typed into our Python script, resave the file and execute the Python script again. We could also use some of the other provided scripts to replace 3d objects with higher quality meshes like light fixtures or furniture. We can also code the same process in our data Smith file blueprint as well in our content browser. Navigate to the folder that we imported from our data Smith import and double click this data Smith scene file to open it. If you already have experience with blueprints in Unreal, this window will look very familiar. In this section in the middle, we have a data prep graph that shows how our code will execute when we trigger an event. This event trigger pipeline traversal node has an execution pin with a line to a sequence node. So when we fire this event by re-importing our data Smith file, this node graph will fire this traversal node from the sequence node than any nodes following that that are connected to our graph. For more information about blueprints and event graphs, you may want to consider checking out the Unreal Engine documentation. On the left, we have a library of operations that we can use for our data Smith file. We could drag and drop these commands into the data prep graph, or you can right click in the graph and search for blueprint nodes like you would in a material editor or blueprint graph. Along the top, we have scene preview and asset preview. This will list all of the assets that we have in our data Smith file when we load them into memory to make our changes. Let's create a simple blueprint script to replace materials now. First, let's drag a substitute material node into our graph. This node has an execute pin, an array of object references input, a string where we can input the name of the material, an option for how we want to match that text, and a drop-down where we can choose what material to use as a substitute. Click and drag on the then-zero pin of the sequence node and connect this to the substitute material. Next, let's drag a get-actors node into our graph. This node returns all actors in the data Smith import as an array. Drag the return value of this node into the selected objects of our substitute material, then in material search, we need to provide the exact name of the material that we want to replace. Remember, be mindful of typos here. I'll drag this top bar on my screen so I can see more of what's happening in the viewport. If I wanted to replace the aluminum material on these mullions, I could type the name of the material into the material search, choose exact match, then input the material I would like to use instead. Let's find a material to use in our content browser. In our starter content folder, under materials, we have this nice chrome material we can use. In our event graph, select the drop-down in substitute material and type chrome. Left-click that option. I don't need to see my viewport anymore, so I'll maximize this screen. If you want to do something else here, such as filter geometry by metadata tags, you could easily do that as well. If you drag a filter by metadata node into your graph, you can see options for key and value inputs. Take another look at our editor. In the details panel, go down to the bottom to the asset user data on a static mesh. Here's all the metadata for this mullion that we have selected. This first column on the left holds the key text and the second column holds the value text. So if we wanted to find all objects that have type material set to aluminum, we could type underscore material in the key box and aluminum in the value box. Alt-left-click on the return value pin on our git actors to break the connection, then slide that node over to the left. Let's plug it into the input on filter by metadata and plug the return value of our filter by metadata node into our substitute material node. Now instead of getting all actors and substituting the aluminum material, we are only performing the operation on actors that match this metadata input. Using this filter by metadata node, you could also set mobility settings, collision information, or compute preferred lightmap UV density for only specific actors. For this course, I would actually like to replace the aluminum on all of our imported actors that have the data Smith aluminum material. So I'll select the filter node, hit delete on the keyboard to remove it, and plug git actors back into substitute material. At the top of the window, choose compile and save before we continue. This green check mark indicates that there are no warnings or errors in our event graph. If you see a yellow exclamation point or a red X, verify that the logic in your event graph is laid out correctly. If we choose load scene here on our toolbar along the top, we will load all of our data Smith file information. When I'm using a relatively small environment, this only takes a few seconds. Our scene preview window shows all of the actors that are contained in our import. In the asset preview window, we can see a list of the geometries, textures, and materials that will be created when we choose import on our toolbar. When we do import, Unreal will fire this event trigger pipeline traversal node and re-import our file using any modifications that we have designated in our event graph. Go ahead and click import. And this is complete. Close this window and head back to the viewport. We can now see that all of the mullions have that chrome material. If at any point you would like to re-import your data Smith file, you can do so by choosing the root data Smith scene actor file and your word outliner at the top of the list and select update actors from scene. You can also respawn actors that you may have deleted by checking the respawn deleted actors checkbox here. In this module, we reviewed the definition of metadata. We discussed how to find metadata in Unreal Engine. We replaced materials using a Python script and we used the data Smith event graph to modify our import. We now have all of the data that we need from Revit in our Unreal project. Up next, we'll learn about master materials, material instances, and how to make our own in our project.