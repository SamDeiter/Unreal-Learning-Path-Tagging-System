So we have our line tray set up in order to see which light switch in the level we are activating. We have our light actors in place that can now turn themselves off and unwentalled, but what we don't have at this moment in time are switches that know what lights they should be instructing. And so, let's fix that by inside the content browser, making sure that we have located the light switch blueprint and have double clicked on it so as to gain access to its event graph. Now as with the previous blueprint, we need to put each part of the overall puzzle in place first, so that we can then get them all to work together. Before we can do that, of course, we need to create some simple logic to have the light switches communicating with the light. Let's right-click then, search for the add custom event node, and then add one to the graph, calling this one light switch. And then pressing enter our return. Just as with the lights themselves, here we are telling the switch what to do via a custom event that can then be triggered via our line trace hitting the switch. And in fact, we are essentially aiming to create a cascade of events, so we click the mouse, which fires the trace, which hits the switch, which runs an event that triggers the light event as well, that eventually will turn the light off our on. And to do all of that properly, of course, we will need the switch to know which light it is supposed to be communicating with. Inside the variables tab then, let's click to create a new variable. Call it lights, and then set the variable type by searching for actor in the list. And from under the object type list, expanding the actor flyout, let's choose the object reference entry. We are using the object reference variable type because it gives us the same functionality as already seen in action with the Blueprint's components, only with the added flexibility that it doesn't need the actor to be part of this specific Blueprint. We do need to make some changes to this reference, however, meaning we want to click the icon to the right of the variable type, so that we can make it an array container. We need to make this change because we would otherwise only be able to have this switch affect a single Blueprint in the level. As most visualization projects have multiple lights in them, we want to let the user decide if it affects just one light in the scene or several. And for this, we need an array. A variable type that can be thought of as, let's say, an expandable bus with multiple rows and our columns that can contain multiple seats. Instead of chairs in our case, though, we are storing object references. Still, we are not done with it quite yet. And so coming back to the variables tab, we want to make this new array public by clicking the icon. We can then drag a reference of it into our graph under the Custom Event, after which we can compile the Blueprint via the compile button. We change the array to public here as this gives the level designer the ability to place the switch in the level as they like, and then, from the Details tab, FartherStaticMesh, pick the light or light in the level that they want it to affect, making this very easy to use indeed. Now that we have a place to store our lights then, we also need to tell the switch what to do with them. After dragging out from the Custom Event then, and searching Far and adding a FarEachLoopNode from the array section, we can pipe our array variable into its array input. We are using the FarEachLoopNode here because we want the Blueprint to perform the following actions on every single light in the array. Without the FarEachLoopNode, this simply would not work. What we need to do next is tell the system what Blueprint code it are to execute. After dragging from the LoopBody output, we can search for Cast2BP, underscore while underscore light, and then, add the node of the same name, making sure that we don't add the class variant of this node. After which, we can pipe the array element from our FarEachLoopNode into the ObjectInput of the Cast2Node. This casting process, being how we are going to get the light switch, to communicate with the lights themselves. Now that we have cast to each light in the array, we can perform the next step now, which is to left-click and drag on the Cast2Node's execution output. And then, after releasing the mouse, search for and add a switched custom event to the graph, piping the BPWARL light output into the target slot of our new node, and then, compiling. We are essentially looking here to see the lights that sit in our array have the appropriate properties, in our case, the custom event called switched, and then, if they do, execute it. And as easy as that, we have learnt how to not only get two blueprints to communicate with each other successfully, but also how to run a custom event.