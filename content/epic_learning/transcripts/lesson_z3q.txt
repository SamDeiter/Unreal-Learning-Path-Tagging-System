Sound can be used to drive behaviours and actions within a game. In this video we'll look at an example of how we might use the envelope of a sound that is its volume over time to affect the appearance of an actor. In cave 1 that's bookmark 6 of the demonstration level, there's a fairy who is strayed down into the caves and is now hopelessly lost. To bring a little life to what is essentially just a glowing sphere, we're going to make the scale of the sphere, material and particle systems, respond to the fairy's speech. Since the fairy's dialogue is not going to be processed or changed in real time in the game, we can analyse the sound files offline to create what's called cooked envelope data. And then we're going to use that data to change the graphics while the sound is playing. This is a lot more efficient than trying to analyse the sound in real time. If we open up that blueprint here, so we're editing cave fairy, we can see that it's got an audio component and we'll open up that sound queue and then we'll navigate to these sound files in the content browser. I've got quite a few, so we're going to edit them all at once by right clicking and choosing asset actions, bulk edit via property matrix. The area we're looking for in the menu here is the analysis section, envelope. I'm going to enable amplitude envelope analysis. Then I'm going to save those files. Once this envelope analysis is enabled, we can get a reference to that audio component, drag out and find get cooked envelope data. This node interpolates and averages the envelope data across all playing sounds. In this case we're only playing one sound at a time, but it could also be used with sound cues where there are multiple simultaneous sounds. To drive this function here, we're going to get events from on audio playback per cent. We'll select our audio component here and then down here we can see that we've got events on audio playback per cent here and we'll move that to the right position and connect that up. So let's just add a print string and see what kind of values we're getting out of that. Now, the constant stream of values from that print string is quite hard to follow. What we're interested in finding is the maximum value that occurs so we can scale the range of numbers appropriately. So we'll start by deleting that print string and instead we'll assign these values to a float variable that we've created called n value. So here we will set that variable. So every frame of the game, this audio playback per cent is creating an event that gets the cooked envelope data from whatever sounds are playing on this audio component and it's setting that to this float variable called n value. Now we want to use these values to scale the graphics of the fairy so we need to see what range we're dealing with. So we've set up a bit of a debug system over here. So if true, if debug is on, then we take that envelope value and we add it to an array. So essentially what we do is we build up an array full of all the envelope values for that sound. Now at the end of the sound, in other words, is the audio playback per cent greater than 0.99? What we'll do then is we'll read through all the values we've got in that array and we'll find the maximum value. That is the peak amplitude of each phrase and then after a brief pause, we'll clear that array ready for the next sound. So let's enable debug and see what we get. So the first phrase, that gives us a value of 0.07, 0.04, 0.04, 0.08 and having looked at all the sound waves that play back on that sound cue, I know that we get a maximum of 0.1. So we've used that 0.1 as our maximum range of in values that we might get and then we're mapping them to appropriate ranges to scale the particle system in the static mesh and to make some adjustments to this dynamic material here. So we'll link that up. It's just going to operate whether the debug is on or off. So it comes off of the true and also off of the false. So the particle system and static mesh you can see here in the viewport particle system static mesh. The dynamic material is affecting a parameter name called scaling. This is actually set up in the construction script. So we're creating a dynamic material instance and if we find this in the content browser, we can edit that and see that it relates to this parameter here called scaling. So let's now see the envelope of the sound affecting those graphics of the cave fairy. I seem to be lost down here. I think I'm going mad down here. We can turn that debug off now. Now that we've established this relationship between the audio envelope and the appearance of the cave fairy, let's go back and examine the settings within the envelope analysis in a little more detail. The attack and release time is set in milliseconds and these control how quickly the envelope analyzer responds to changes in amplitude. If you're using the envelope to affect the graphics within the game, then these will be determined to a great extent by the kind of aesthetic response you want. So at the moment, we've got an envelope attack time of 50, which produced the effect we just saw. If we change that to 10 milliseconds, then the graphics would be responding more quickly to the attack portions of our envelope. Now if we were to track the envelope of a sound with perfect accuracy, then we'd be tracking it at the sound sample rate, for example 48 kilohertz, which is 48,000 times a second. This would obviously generate a lot of data. The envelope follow a frame size allows us to group together a given number of samples and get the average value across that group. So we have less data to store. The smaller the frame size, the higher the accuracy, but the larger the memory use. The larger frame size is used less data, but obviously less accurate. Having said this, remember that these values are only being checked via the on-audio playback percent event here, which is linked to the game frame rate. So unless you can guarantee you'll be running at a very high frame rate, then audio frame sizes of less than 102 for won't make much difference anyway. As with all these things, it's a balance between achieving the aesthetic effect you're after and the memory or processing impact that the systems can have. If we change that, for example, to 4096 and had an attack time of 200, we get a slightly less responsive K-fairy. So let's compare that to our initial settings. So you can see there, although I'm having to generate and store more data for each sound, it gives me the responsiveness that creates the aesthetic effect I'm after. In this video, we've looked at how you can track the envelope data of a playing sound, and then use this data to drive other objects or events in the game. We've looked at a simple example where the audio is driving the scale of an actor, but this could be much more sophisticated. For example, you could use the breathing audio of a character to drive their upper body movement. Remember that this technique just uses the cooked envelope data of the original sounds. If you're doing any modulations on that sound, which could be as simple as changing a volume to applying effects, then you might want to look at real-time analysis, which will be covering in a later video. However, if this is not necessary, then it's a good idea to stick to this computationally cheap method.