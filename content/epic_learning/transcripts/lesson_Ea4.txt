With our devices and voice inputs tested and working, we're ready to create a solution for a much bigger problem. In this module, we will expand on the mechanics we have built so far to repair a broken object. We're going to begin by exploring the map and the problem that we need to solve. We'll need to create several new speech mappings to use them to communicate with blueprints in the scene and spawn the asset that we will use to fix one of them. Using a combination of spatial and traditional stereo audio will help guide us through this map. Let's begin inside of the voice map located in World Builder Maps by course Voice. Looking around, everything seems similar to the other maps, but of course there are these four cubes here. We have been tasked with finding out which one of them is broken and fixing it with the right asset. Now because we are in the editor, it's of course going to be simple to determine which of them is quote unquote broken. But think of this as an experience you are building for a client. They want to demonstrate how to fix this cube and their learners will only have the packaged file. So they will need to go through the process of identifying where the problem is and solving it. Since we are in the editor, we can see that this BP fix me has its instance editable bool variable fixed set to false. So this one needs fixing. To get a better look at what needs fixing, set show exploded to true. We can see that after the walls have been separated, the center sphere is missing and now represented by a glowing sphere. The whole show exploded on another BP fix me. This one here is fixed and the center sphere is glowing white within a blue volume. We need to create a way to allow the end user to fix this on their own. Open BP fix me. This is a fairly straightforward blueprint with much of it already set up. These yellow arrows all contain child components and we can see which child components belong to which arrow by setting show exploded to true. Let's quickly look in the event graph to see what's happening. At begin play, if show exploded was true, it will reset it to false and pull all the cube balls back to their default locations, which is stored in the dictionary variable named starting location. This variable holds two values per array index. The reference to an arrow component and its world location. This data is retrieved inside of the get starting data function and used in the explode function to interpolate the location from the starting point to 25 centimeters in the forward direction of the arrow. Arrow 25 is a bit special in that it will also rotate. You will not be able to see this in action in the viewport, but the function will rotate it away at the same time its location is changing, resulting in a free view of the problem area. To understand what I mean, call open cube after false at begin play and simulate the scene. We can now see the area we need to focus on. What we need to do is create a way to identify which box is broken, open it up, spawn in the replacement part, move this part to the proper spot and close the cube. Let's get started by creating the speech mappings we will use for this. Open project settings and scroll down to input. We can remove the toggle gate action as it is no longer needed. Add a new element and set the action name to status check and the speech keyword to status check. Again, remember the spaces here. Add another and set the action name to HL underscore world builder and the speech keyword to world builder. The world builder command is going to be used as a primary voice command. We must first initiate world builder and can then proceed with spawning the needed tool. For the UI commands, we'll need three more speech mappings. Add one element and set the action name to UI underscore cube and the speech keyword to choose cube. Add another element and set the action name to UI underscore sphere and the speech keyword to choose sphere. And for the last element, set the action name to UI underscore spawn and the speech keyword to spawn. Let's move on to getting these cubes to open and close using our voice. Open BP on. Pan down and we're going to add our new events here. Add event status check. HL underscore world builder. UI cube, UI sphere and UI spawn. Reviewing what we did in the previous module, pan to the interact with single event. We have split the eye tracking hit struct and are calling interact with object and have set the right hand input to true. We need to remember that if we use this speech mapping while looking at an actor that is implementing our BPI HoloLens and using the event interact with object, the return value of right hand will always be true. Recall that we also used this function in our gesture input. Pan over to interact with long. And look at the checked overlap from hand event. At the end, we call interact with object and set right hand based on which hand is gesturing. So we should prepare our assets to work regardless if they are interacted with from our hand gestures or from our focused speech commands. Open BP fix me. We are going to implement the interface here and set up the needed functions. Open class settings and implement our BPI HoloLens interface and compile. Add event interact with object. For BP fix me, we are going to treat this event call as a toggle instead of directly telling it to open or close. Create a new bull variable named is exploded. We want to prevent the ability to call these functions if the timeline in the blueprint is currently playing. So in the My Blueprint panel, search for Timeline and drag in a git reference. From timeline open close, search for is playing. Get the knot of the return value and connect to a branch. From true as in knot playing, connect is exploded to a new branch. If true, call close cube and if false, call open cube. Turn down to the timeline. Drag in a set is exploded and connect it to finished. From direction create an equals node and if the direction returns forward, plug the return value into is exploded. When we call close cube, the timeline will play in reverse and return backward when it finishes. You will now be able to focus our gaze onto one of the cubes and open or close it when needed. Let's move on to the status check mapping. We need to add a new function to our blueprint interface. Open BPI HoloLens and add a new function. Name it give status update. Drag in BPPON after status check is called, get another reference to eye tracking hit and split the struct. From eye tracking hit actor create a does implement interface node. Set the interface as our BPI HoloLens. If true, call give status update from the hit actor. If false, create a get all actors with interface node and set the interface to BPI HoloLens. Create a for each loop and from the array element, call give status update. This will allow us to either check the status of an individual actor or from all of them using the same voice command. Return to BPFIXME. Select the audio component that is under power and then sphere. Set the attenuation to the attenuation asset we created in the previous course, 5 meter ATT. Because it now uses spatialized audio, we will be able to identify which of the cubes needs attention. I click in the graph and search for event give status update. Drag in a get reference to our fixed variable as well as our audio component. From audio, create a set sound node. Drag backwards from new sound and create a select node and plug fixed into index. Set true to beep beep beep and set false to attention. Next call the play function from our audio component. Okay, we can now open and close the cubes and request their status as to whether or not they are fixed, all using voice commands. As they are using spatialized sound, we will easily be able to identify which of them needs attention. Let's try it. We have our four cubes here and it appears that everything is fine. I'll just want to be sure of that and while focusing on this one on the top right, I'll say status check and it played the beeping sound. So all is good here. I'll look away from them as if concentrating on something else and again say status check. Attention. One of them needs attention and I think it was this one on the top left. I can focus on it and again say status check. Attention. It alone played the attention sound. Okay, let's open it up. Single interact and now that it's open, I can see that there is a piece missing that needs to be fixed. Just to be certain, I'll open a different cube to check what it should look like. Single interact. All right, so the sphere in the middle is missing. Now that I know what to replace, I can close the fixed cube. Single interact. In this module, we expanded on our means of interaction by implementing our voice into a useful mechanic. We created new speech mappings that can be used dynamically with individual actors or broadcast to any actor that's listening. Using new functions, we are able to spatially identify a playing audio source in the world by vocally requesting a status update. In the next and final module of this course, we will finish the solution to this problem by enabling us to spawn the replacement asset that the cube requires to be fixed.