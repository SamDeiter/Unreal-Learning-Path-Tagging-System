In this module, you will learn how to do transformations like translation and scale in practice, using nodes. In the previous lesson, we had texture coordinates being multiplied by a number to achieve a zoom behavior. I didn't explain why, and now is the right moment to do it. So transformations that are mostly performed on UVs are scale, so resizing, flip, so mirror, and translation, which is moving. All of them can be achieved by basic math operations. Let me create a new material. So on the bottom left, I click Content Drawer. I go to any folder like Materials, wherever I want to have my temporary one. I click on the empty space in the Content Drawer, right click, and select Material. And let's name it mBasicTransform. Now I double click it, and in the previous section I showed that in the Details panel, you go to Material, the Material Domain, and change it to User Interface. It changes our output, in the sense it now has final color and screen position. Now to get the UVs, I right click on empty and select Get User Interface UV, and Normalize UVs are what we typically use. So again, to recap the previous section, I take it to final color, click Apply in the toolbar to see the changes, and now I can create a border widget by dragging and dropping it from the palette. And in the Details panel, Appearance, Brush, Image, I choose my basic transform. And these are the UVs displayed as color. So this is what we already knew. Now to transform these UVs, let me visualize them better, similar to what's above, by showing a texture. So I want to right click, Texture Sample, choosing a texture called Oxo, O-X-O, which is the CAD. And now Normalized UVs go to the UVs input in the Texture Sample, and the RGB output goes to the final color. I click Apply, and this is what we get. Now the last missing thing is Opacity, so again I click on empty. I go to the Details panel, Material, Blend Mode, and change Opaque to Translucent, and the alpha output from the Texture Sample goes to Opacity input. And that's what we get. Now to scale the CAD, I need to add a basic math operation. So in between the UVs output and the UVs input in Texture Sample, I can perform any math I want, and I can even combine the operations. So scaling is multiplying, because the number by default goes from 0 to 1 in the other corner. So to get a half portion of the texture, I need to multiply them by half, or divide them by 2. So I right click, search for Multiply, my input will be the UV vector. Fortunately, vectors can be multiplied by either another vector of the same number of components or by a scalar, so single number. So here, to multiply both axes by the same value, I input a scalar of half, and click Apply. And as you can see, now I can see only the half of the CAD. Now if I wanted to move it, before multiplying, I can add another node, so right click and Add. So while multiplying is scaling, addition is moving. So normalized UVs go to A, the output of the addition replaces A in Multiply, so I basically chain the operations. And now I can add a value, but I want to add it only in the U axis. I want to move it a little to the right, but not in the vertical axis. So it means I want to add something to the U, but nothing to the V. Therefore, I have to create a vector of two components. I can do it either by right clicking and searching for Constant Tool Vector, which gives me X and Y, which are the same as U and V. And now I can put it into B, or I can make an Append Vector node, which allows me to append vectors of any sizes to create bigger vectors. For example, a constant scalar, another constant scalar, when I put them into A and B, they also create a vector of two components. So this, a single constant two vector and the result of the append are the same thing. Let me show you by first doing X 0.3 in the vector 2 and apply. As you can see, the cat shifted a bit. Let's return to 0 and 0.3 again. So adding numbers to the UVs moves things while multiplying it scales them. Now I multiply by 0.8. Of course, multiplying by 1 or adding 0 is a neutral operation, right? So multiply by 1, now I'm only moving 0, does precisely nothing. 0.3 shifts it a bit to the left. Now one interesting behavior is that here we are exceeding the 0 to 1 range. So what was 1.0, now is 1.3. So what happens then? By default, textures are being wrapped around. So if you have values bigger than 1, the image starts to repeat itself. So if I add half, still I have the next half of the cat. If I add an and a half, it will look exactly the same, because it wrapped around 10 times and then a half. So let's say UVs are infinite in a sense, it also depends on the settings of the image. So if I don't want to have it wrapped, let's return to 0.3, I have to go to the texture, by double clicking it. And now in the settings in the details panel, there is X-axis tiling method and Y-axis tiling method. So this is how it behaves when it's set to wrap. But let me change it to clamp on the X-axis and you can see the cat disappears. So wrap means repeat itself infinitely, while clamp means stop at 0 to 1 UV range and then just repeat the last pixel infinitely, which here is transparent. So that's the difference and it depends on the settings in the texture. Wrap is the default mode, so the image will start repeating. Let's keep that in mind. So to recap again, I added 0.3 in the X-axis, in the U-axis, to move it. If I use instead the append node to append constant of 0.3 scalar to 0 in the V, it will be exactly the same. So it's up to you which one to use. You may wonder when the append comes in handy. Well, let me show you one thing that I will explain in a much later section, but just to tease you. Let's assume I don't have a constant in U or X and let's say I have time. I put this time into A and 0 into V and as you can see the cat got the zoomies. It started moving. So that's why append comes in handy if you want to append logic, not just the constants, to create a vector. And as I said, the operations can be combined. So now multiply to scale can be added as well. Let's say multiply by 1.5 to get some tiling or by 0.5 to get half of the cat. So this was just a teaser because I will show the time component animation much later. For now just remember that you can do things like that. So again, let's try the movement. This is what happens, right? And this is the visualization of the UVs. The checker, I made it visible only the portion that spans 0 to 1. So here you can see we exceeded 1 and went beyond this, but the texture gets repeated. So for now you have learned that adding is moving and multiplying is scaling. There is one more thing to explain. Math happens per component in shader languages and in the Material Editor in Unreal. So here I have such an example that the addition can have two components, but multiplication can have them as well. So I can return to 0 here for now. And in the B, I can again create right click and constant to vector and multiply in one axis by half, but in another by two. For example, I take the white output, which is the combined vector, and put it into B and apply. And now you can see that the axes are treated separately. I can change the scale per axis. And of course here we exceed 1 because now it's 0 to 1, 1 to 2, 2 to 3, because I multiplied the original UVs by 3, therefore the image gets repeated. Now if I want to flip the image, I would use a negative number. So either a negative minus 1 or negative minus 3. And in terms of movement, if I want to move to the other direction, I have two ways. I can either use the add minus 1 or minus half, for example, or instead of adding a negative number, I can just subtract, which is another node. So I can subtract half and half. And as you can see, the result is exactly the same than adding a negative number. So to sum up this section, we learned how to move, how to scale, and then in the next section we will learn how to rotate.