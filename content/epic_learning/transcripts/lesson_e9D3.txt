While the editor does include many assets to get you up and running with your project quickly, you'll get to a point where you want to use your custom content. So we'll start by looking at how to add that content into the editor. There are different ways to import or add content into the editor. One way is to go into your content browser, so I can see my content browser on the bottom here. We can right-click and choose import. Once we choose import, it'll ask us what file we want to import from a selected type. In this case, I'm going to choose a FBX file or a static mesh. And once it comes in, it's going to give me an import options dialog box. There are different dialog boxes for different import types. If I was to go ahead and try to import again and choose a wave file, it's going to bring it in without the import box because there are no options for it. If I was to bring in a normal map texture, so let's go with this one, for example, it's going to bring it in and it's also going to detect that it's a normal map and convert it over from a normal color texture into a normal map type. You can choose multiple files at once. When you open, it'll go ahead and attempt to import as many as it can. In this case, we already have one. It's going to ask us to we want to override it or skip it. Something that has an import box will show up. Anything that does not will not show up. You can also access that same import menu from the add button. We also have drag and drop. Here I have my folder and I can grab one or multiple files, drag them and drop them into my content browser or the specific folder I want to. And it will do the same normal importing process. An additional way is handling an entire scene or a grouping of objects into the world as one piece rather than individual assets. We can go up to the top and choose file, import into level. I would then choose the item I want to import. In this case, I have something pre-built out of multiple meshes. And once I bring it in, it's going to ask us where. We'll go and put it in my folder. And we have a special scene import options dialog box. There are many options in here, but some of the basic ones you may want to be aware of are which items do you want to actually bring in. We can see the hierarchy and I can select individual items. You can also see by default, it may choose to create level actors, one actor with components or one blueprint asset. And this option is useful because you could bring everything in as individual items in the same positions they were but not grouped together. Or you could create a blueprint asset which automatically groups them together into a blueprint. You also have additional options for how the stack meshes will be imported, such as maybe giving the collision if it's missing. If you're dealing with skeletal meshes, you have additional skeletal meshes. And the materials, it'll tell you which materials is going to go and create based on the items you're importing. Once we're done with that, we can hit import after we've imported. We can now see we have our entire grouping of items brought in. Even though these are individual meshes, they're brought in into the correct spots. And materials have been created that we can then set up for textures. By importing assets into our project, we are taking the original type, a texture, a sound file, a static mesh, and converting it to an internal format that Unreal uses in the U-asset format. At that point, we've imported it into our project and the original files are no longer needed. We won't need them unless we want to re-import and make changes to the original items. Unless thing is using Quixel to import content, we can go up to the add menu and choose Quixel Bridge, or right-click in your content browser and choose add Quixel Content. Quixel Bridge will then load up. You have a large amount of items in this library that you can then add and import into your project without leaving the editor. Now that we've brought a few different assets into our project by importing them and letting the editor prepare them for use, let's start looking at working with them inside of the editor.