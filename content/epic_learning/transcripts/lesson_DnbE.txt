With our basic lighting and camera now configured, let's move on to adding our first new gameplay feature, player control over the camera position. By the end of this video, we'll be able to use player input to move the camera independently of the player character's position. Let's start by adding some new access mappings for up, down and left, right. Open project settings and locate the input section on the left hand side within the engine section. To help us focus on what we need to change, collapse the existing move right and move up access mappings. To add a new mapping, hit the plus sign next to access mappings. Select the text with the new mapping and name it look up. Hit the plus icon again to create another axis. Name this axis look right. Select these are conceptually similar to our movement axis, which are move right and move up. The next step is to set a control input to these new mappings. Press the plus next to the look up axis and select gamepad right thumbstick y axis. Set the scale for this new mapping to minus one. Let's do the same thing again to select a keyboard key. Press the plus icon next to the look up axis and select a keyboard key. I suggest using the eye key for looking up. We can leave this keyboard key with the scale of one. Now we just need to add a keyboard key for looking down. Repeat the process except this time, select the keyboard key K. Set the scale for this key to minus one. You can now collapse this axis to focus on the new mapping of look right. Hit the plus icon next to the look right axis and add a gamepad right thumbstick x axis with the scale of one. Now we just need to add our two keyboard keys for the same function. Add a keyboard value of j with the scale of minus one. And then a keyboard value of l with the scale of one. Don't worry if it's not completely clear how these are going to work just yet. We'll shortly see these mappings in context as we create the blueprint logic. With that, we're done with the project settings window so we can close it and return to the main editor window. Now let's begin adding the blueprint logic to our player character to control the camera. In the content browser navigate to content 2D sidescrawler BP, blueprints and double-click the my2d player character to open a blueprint in the editor. Make sure that the event graph tab is selected. We should be able to see the existing logic that handles player movement controls and animation. Let's begin by creating some variables we'll need to manage our new movable camera. In the my blueprint panel on the left-hand side, hit the plus icon in the variable section and create a new variable called Player Camera Offset. This variable will store the position where the camera should be placed relative to the player character and it should be a vector type. Let's move over to the Detail to Panel on the right-hand side to set the correct variable type. While we're over here in the Details panel, let's keep things organized by creating a new category to store these variables. Set the category dropdown and type Camera. Using the same process, let's create two new float variables called Player Camera Max Offset and Player Camera Lurps Speed, place them in the same camera category. These two float variables will allow us to set the furthest the camera can move from the player character as well as how quickly they are allowed to reach that distance. We want to set some default values for these variables but we can only do that after we compiled a Blueprint, so let's do that now. Once we've done that, set the Player Camera Max Offset Default Value to 100 in the Details Panel on the right-hand side. Now let's select Player Camera Lurps Speed and set the default value to 5. And with those default values set up, we can move on to setting up the control logic in the Event Graph. Now it's time to make use of those access mappings alongside our new variables to control camera position. Let's start by using our LookUpAxis to define the Player Camera Offset variable. Right-click in the Event Graph and search for InputAxis LookUp. Our ultimate goal is to set the Player Camera Offset Vector, so drag from the execution pin of the axis and search for Player Camera Offset. Select the Set Player Camera Offset node. Drag this new set node far off to the right-hand side as we've got quite a bit of logic to create first. The first step will be to create a target offset based upon the value of the axis. We can do this by multiplying the current axis value with our Player Camera Max Offset variable. Start by getting a reference to our Player Camera Max Offset variable by holding down Control and left-click tracking the variable from the My Blueprint panel into the Event Graph. Now drag from the axis value output pin of the InputAxis LookUp event and locate the float multiplied by float node. Next, drag from the output of the Player Camera Max Offset node into the second input of the Multiply node. The output of this Multiply node now gives us the Offset value, but we still need a way to smoothly move the camera from where it is now to this new location. We'll achieve this by using a Lurp node to control the rate of change in the Z axis of our vector. We need to supply our Lurp with two values representing where the camera is now and where we'd like it to move to. Let's start by finding out where the camera is now. By controlling left-click tracking a reference to the Player Camera Offset variable in the Event Graph. Remember that this axis mapping is only for looking up and down, so we only care about the Z axis of the vector. Drag from the Player Camera Offset node we placed and locate Break Vector. Drag from the Z output of the node and locate the Lurp node. Note that this Z output is automatically linked to the A input of the Lurp node, representing our Start point. We've already calculated our endpoint by multiplying the Max Offset by the current axis value. So let's connect the output pin of the multiplication node to the B input of the Lurp. Now we just need to take the return value from the Lurp and make it into an appropriate vector for the new value of Player Camera Offset. Right-click in the Event Graph and locate Make Vector. Drag from the return value output of the Lurp node into the Z input of the Make Vector node. Connect the return value of the Make Vector node to the input of the Z node for the Player Camera Offset. And that will take care of the Z axis of the vector. However we don't want to lose whatever the current values for X and Y are. We can make sure that the previous values are maintained by connecting the X and Y outputs of the Break Vector to the X and Y inputs of our Make Vector. At this point, while it's a little messy, we're almost there. We just need something to drive the alpha value of the Lurp. This alpha value is the float that drives the linear interpolation between the two values we've plugged in. This is where our Player Camera Lurp Speed Variable comes in. Control left-click drag the Player Camera Lurp Speed Variable into the Event Graph. Drag from the output of this new variable and locate float multiplied by float. Once again, right-click in the Event Graph and locate the Get World Delta Seconds node. Connect the return value of this node to the second input of the Multiply node. Multiplying the Lurp Speed Variable in this way gives us a smooth change over time, independent of frame rate. There's only one more thing we need to do before we hook this up to our alpha of the Lurp. Looking at the tooltip for the Lurp function, we can see that the alpha value needs to be between 0 and 1. For that reason, we used a clamp function to make sure that the output of this multiplication is between 0 and 1. Drag from the output of the Multiply node and locate the Clamp Flute function. As this is a common use of the node, you can see that default min and max are already set to what we need, 0 and 1. We can now connect the return value of this clamp into the alpha value of the Lurp. That's everything we need for the function, but it could do with a little tidy up. It's worth taking the time to reposition the nodes to make sure everything is nice and readable and lines crossing over is minimized. Feel free to stretch your things the same as me or take your own path, but it's always worth taking the time to tidy up as you go. Now that we've created our offset, we actually need to move the camera to that position at some point. What we'll want to do is update the camera position at the same time that we handle all of our character animation. This is taking place currently in the Tick event. To stretch this properly, we'll create another function. In the My Blueprint panel, click the plus next to the Functions rollout to create a new function. Call this new function update camera. Note that we're automatically taken inside this function in the graph. Our goal for this function is to move the spring arm using the offset value we calculated previously. This will in turn move the camera as it is attached to the spring arm. We can see this reflected in the hierarchy in the Components panel. Drag from the Update Camera Execution pin within the function and locate Set World Location. Spring Arm. Now we just need to set the new Location vector for this function. We can do this by adding together the current location of the character and the camera offset we generated using our previous logic. Right-click on the graph and locate Get Actor Location. Drag from the return value of this new function and select Vector Plus Vector. Control left-click are existing player camera offset variable into the Event Graph and connect it to the Vector Plus Vector node. Connect the output of this node to the new location input. Connect the output of this addition to the new location input of the Set World Location function. Take this opportunity to make sure your graph looks as neat as possible. Something like I've got here is nice and easy to read from left to right. With that our function is complete and now we just need to make sure that it's cooled during every tick. Return to the main event graph and in the My Blueprint panel expand the Event Graph roll out. Double-click on Event Tick to instantly be taken to it in the graph. Select the Event Tick node and move it to the left to give ourselves a little bit of space. Now without breaking the existing connection, drag from the output of the event and select the Update Camera function. This adds our new function in between the Event Tick and the existing Handle Animation function. This means that our new Update Camera function is called every tick alongside the other events. Now it's worth taking the time to rearrange the nodes and the comment box to keep everything neat. With that done, compile, save and close the Blueprint Editor window. Now start a new Play and Editor session and try using either the GamePad or your keyboard to look up and down. While this works, moving our camera around exposes another issue we need to fix. You'll find that moving the camera down turns the display black. This is because the Spring Arm is colliding with your Metry in the world. Luckily, this is very easy to fix. Stop the Play and Editor session and open my 2D Player character in the Blueprint Editor again. Select the Spring Arm component from the Components panel. Moving up to the Details panel in the Camera Collision rollout, uncheck Do Collision Test. Compile, save and close the Blueprint Editor window. And now let's run another Play and Editor session and observe how the camera now correctly behaves. Screen never goes black, looking up or down. So there we have it, looking up and down. Now we just need to do something similar for left and right. Luckily for us, adding the left and right access isn't going to be that much more difficult. Begin by reopening my 2D Player character in the Blueprint Editor. It's a good idea to keep similar functionality close together in the Event Graph, so begin by locating the Input Access lookup event using the My Blueprint panel as before. Right-click in the Event Graph and locate the Input Access look right. Because the function R to here is very similar to what we've already made, marquee select the existing Input Access lookup code and copy it. Next paste it somewhere near to the new Input Access look right. So we don't forget to do it later, begin by connecting the execution pin from the Input Access look right to the set node over on the right hand side. Zooming in little, we can see that we need to connect the access value from the Input Access look right to the multiplication node below. Now we just need to make one more alteration and we're done. The only difference between this and the last example is that we want to feed the X value into the LURP rather than the Z. This connects the X connection by holding ALT and left-clicking on the X output of the break vector. Move the Z output of the break vector to the X output by holding CTRL and left-click dragging from Z to X on the break vector node. Alter the Inputs and Outputs from the break vector and Make Vector so that Y and Z are passed through, the X enters and exits the LURP. With that done, let's confirm what we have changed. Y and Z are passed through unaltered and X is altered via the LURP. With that done, compile, save and close the Blueprint Editor. Start another play and edit the session and confirm that everything is working. That's great, we're done. Before we move on, let's place a comment box around these new nodes to keep it neat and tidy. Return to the Blueprint Editor for the player character and marquee select around both events. With everything selected, press C for comment. I suggest naming this Camera Controls. And with that, we're done. We can compile, save and close the Blueprint Editor. In this video, we added new functionality to our game, independent Camera Controls. We began by adding two new access mappings for up, down and left, right. We then added Blueprint Logic to our player character to calculate an offset position for the camera based upon the current position of this axis and some tunable speed and maximum offset variables. We then used a LURP node to smoothly blend between the camera's current position and the new target position. We finished up by testing this functionality in a play and editor session.