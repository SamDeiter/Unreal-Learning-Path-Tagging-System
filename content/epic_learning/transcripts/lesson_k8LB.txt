Now that we have the character already, it's time to head to the sequencer, where we can start animating our character using forward solve. To use a sequence editor, you first need to create a level sequence asset. So I can either create it via this cinematic dropdown here, and select add level sequence, or I can simply drag the control rig asset onto the viewport. This automatically creates a level sequence for us, with a skeletal mesh added here in the tracks window along with the control rig track. Also note that if you create a level sequence via this method, then it automatically creates this filter, which isolates the control rig track. So I'll go ahead and clear that, and that displays all the tracks present here. Now before we start animating the character using this control rig, let's first make sure that we've enabled Auto key, and we have the desired frame rate specified. It's 30 by default, but you can change it from here as per your requirements. Apart from that, I would like to highlight a few features specific to control rig workflow. Like there is a dedicated animation tab on the left of the viewport, with some useful tools that I'll cover later in this course. For now, I'll toggle this select option on, which will let us select only the controls. This prevents us from accidentally selecting the environment or anything else in the scene while animating. On the right side here of the viewport, there is an anim outliner, which displays all the controls that we've created for the selected control rig asset as per the defined hierarchy, unlike the sequencer, which displays all the controls parallel to one another in a random order. Now you can quickly find any control using the search bar up here. Finally, I'll turn off these snapping options in the viewport. Now that we are all set for animation, let's start creating the first pose. One thing to keep in mind while animating with Auto key on is that it only works when you have an existing key in the timeline, so you manually have to key everything you move on the first frame. Also while animating, if you feel that the pose doesn't look good and you want to reset back to the base pose, then you can use the shortcut key Ctrl plus G on the keyboard to reset the selected control. Or if you want to reset all the controls at once, you can use Ctrl plus Shift plus G. For the first pose, I'll make Mani stand straight with his legs wide apart. And then I'll place both his hands on the waist to give him a strong stance. Making an animation from scratch is time-taking process, so for the purpose of this course, I won't be making a detailed animation, so you can ignore these penetrations here. The whole idea here is to make you familiar with creating animation from scratch using Ctrl plus G. Once we are done with the first pose, you can select the Mani can control rig in the sequence editor and add a key to all the controllers. This will move a few frames ahead in the timeline and create a second pose. For this pose, I'll go with the casual wave. Now moving just the wrist looks very robotic here, so we'll animate these other controls like the shoulder and the spine as well. Now we have a decent looking wave here. But when I scrub to the timeline, you'll notice that the wrist is acting weird in between our two keyframes. This is because of the default interpolation between the two keys that Unreal is calculating right now. I'll add a few more in between poses here, which will define a clear path for the wrist to follow. Now let me scrub the timeline once to check if we are on the right path so far. Everything looks good except for the fact that Mani is still not waving at us. So I'll go ahead and add a few more poses here to make him wave at the camera. Now that we are done adding all the poses, let's hit play and see what we have here. Yeah, this is the worst wave in the history that you might have seen, plus it's super slow. We can bring the keys closer to speed this up a little, but at this point, we already have more than just two or three keys in the timeline. And it will be quite tedious to manually retime everything. So how do we do it efficiently? This is where a curve editor will be super helpful. If you're an animator, you know the importance of curve editors or graph editor as we call it in some DCC applications. If you're not familiar, then let me give you a quick overview of this. Inside the curve editor, we have all our control names on the left, just like sequence editor. And on the right here, we have our animation represented with these curves. On the top, we have multiple tangent interpolation method to define the transition between any two keyframes. These are great out for now as no curve is selected in the curve editor. So I'll go ahead and select a curve here. And now I can see the interpolation method by default is set to auto tangent. Auto tangent adds an ease in at the start and an ease out at the end. You can switch between these interpolation types by clicking on them or using the number keys from one to five map with these. So if you press two, you can flat out the tangent like this. You can press three to break these tangents to control them individually on either side. Then you can press four for a linear tangent with a straight line curve. And finally press five to switch to constant interpolation, which is also known as stepped as the value stays constant till the next keyframe in the timeline. I'll use these to modify the curves for my wave animation. Now to speed up our animation, I'll use the retime tool here. I can adjust the whole length of my animation by dragging these two anchor points on the first and the last frame. So I'll drag this closer to speed up the animation. You can even delete these anchor points via this cross and create new ones by double-clicking on any frame. This is pretty much the same as retime tool in Maya. Once we are done with the modifications, let's hit play and see. This looks decent enough given the time restriction that we have. Now that I'm done creating my wave animation, I can also use it as an individual animation asset. If you right click on the skeletal mesh track, Unreal provides us with a few options to convert this animation into different animation assets. So we can either bake our current pose and the timeline to oppose asset or bake it into an animation asset. To do that, select the bake animation sequence and it will ask for a path. I'll select the animation folder, rename my animation and click OK. This will show another pop-up with some export options where you can define what properties you want to bake into the animation sequence. By default, the necessary options are tagged like transforms, moves, curves, etc. There is also an option to toggle the space. So you can take this option if you want to bake the root motion data in world space. Otherwise, it will bake the animation at the origin. I'll leave it untied and click export to animation sequence. This bakes an individual animation asset for me. Now this won't affect your control-req sequence. You can still make changes or add more stuff in here, but you'll need to create a different animation sequence every time you want to bake the animation. To overcome this, there is another option called create-linked animation sequence. So if I select this option, you'll see a similar process for it as the big animation sequence. But this creates a direct link between the control-req sequence and this animation sequence. Now every time I change the animation in the control-req sequence and click save, it'll reflect those changes in the link animation sequence. To showcase this, I'll go ahead and move the right hand away from the hip, add a few keys to it, and once I'm done, I'll hit save. Now right-click on the skeletal mesh track and select open-linked animation sequence, and there it is with all the updated changes. So that's a basic example of how you can animate a character using control-req with forward solve and convert it into a separate animation asset. With that, let's move on to the next module where we'll see how to modify existing MoCAP animations using FK control-req.