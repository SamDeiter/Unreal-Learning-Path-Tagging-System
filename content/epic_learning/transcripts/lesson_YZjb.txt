With our ViewTarget class setup and tested in Blueprint, we can jump on over to Visual Studio or your ID of choice and get this working in C++. Back inside of our code file, then the first thing we want to do is setup the overlap bindings. So the first thing we'll do, now that we've created our overlap box, we know that we have access to this. We'll grab this a few times. For the first time, we'll access the function set, generate overlap events to true, just to ensure that we are getting the overlap events working. This is set to true by default, but again, we'll just set this to be quite explicit, because this is the main and only thing that this component is really intended to do. Then next, we want to access the on component to begin overlap function. So this is the default implementation within the box component, and we want to add a dynamic binding. The context actor in which we'll be binding this will be in this, so inside of our ViewTarget and the function will be binding to will be the on overlap begin. And of course, this will be through our ACF underscore ViewTarget actor. Then we want to do exactly the same thing, but this time for the end overlap, so we'll create a final ad dynamic binding. Again, the context actor will be this, this time we'll be binding it to the on overlap end that we've created inside of this class. So this is why it was very important to get the correct function parameter calls when we were creating these functions, because they wanted to match exactly to the functions that we're binding them to. If we remember that we also needed access to our player controller to call the relevant functions, which is why we've stored that variable over in our header file a little bit earlier. So to get this, we're quite simply use the default functionality to find the current player controller. So we'll say the PC is equal to the UGamePlayStatics function return of the get player controller. Again, the world context is this and we want player controller 0. So it's nice and simple. We now know that we have the player controller stored in ready to use. And to allow us to access the player controller, when we just jump back up to the top of the file, we'll add another include for the kismet forward slash gameplaystatics.h. We can jump down to the on overlap begin, very similar setup to the blueprints. So we'll start with this one. We want to do the same kind of validity check. So we'll say if we have a view target, so if view target to check for a valid and usable actor to view through. And if we do have that available through our player controller, we'll call the set view target with blend. And again, we're just passing in the view target, which is the thing we want to look through, which is the view target we're setting in our editor. And we can also pass in the blend time, which again, we're setting up through our editor for quick updates and iterations. In this case, we'll also be taking the code a little bit further in the C++ side of things. So we'll also be tracking the B is overlapped. And we're going to set this to be true. We'll use this a little bit later when we start doing some update checks in the tick function. I've also commented out here. Just wanted to kind of show you how simple it was to use the other option. We can also use the set view target function, which is just taking the view target this time. And of course, because there's no blend, this is essentially without blend, we don't need a blend time to be set. The main difference here is we're just going to get a very kind of quick snap over to the other view target, rather than this kind of more comfortable interblating blend animation. And finally, in this case, where we're storing the player controller on begin play, I will just update the safety check here. I'll also say we'll look for the view target and the PC. So we'll only enter into this if we have a valid view target and a valid player controller to call the function through. It's unlikely something's gone seriously wrong, of course, if we don't have a player controller, but we can add the safety checks in just in case. Moving down to the on overlap end function, we'll do something very similar again. So we're just going to do our if check to make sure that we do have a valid player controller completely up to you, whether you wanted to skip that check. And this is going to be very, very simple. What we do here, we're just going to reset the view target with blend calling the set view target with blend through the player controller. This time we're going to set the view target to be the get porn return from the player controller. And again, we'll just pass in the blend time to be the stored blend time variable. And then the final thing we want to do is just make sure that we're resetting the B is overlapped to be false because of course, we're about to implement some functionality, which will make the camera constantly look at the character if it's a dynamic camera. And we don't really want that to be happening when the player isn't looking through that camera any longer. It won't exactly crash the game or cause any problems, but we can save some performance without rotating a camera around that we really want to see the functionality being played act. Then down inside of the tick function, we can now finally look at that very simplified version of what is a kind of dynamic camera. The first thing is we can just return straight from this function if two of these checks return false. So if we don't have a view target, so we don't have a valid view target, or if the camera isn't dynamic anyway, then we can just return from the tick function. If you want it to be very particular and you weren't going to update this dynamically, then you could also just disable the tick function here if we know that this is never going to be relevant. But in this case, again, this is going to be fine. It's not going to destroy our project. Carrying on from this, if we're able to get past the first check, then we can double check here that if the B is overlap, it's been set to true. So we know that the player has walked into our radius. Then we're just going to use some very very simple math calculations here to set an F-rotator, which we'll call rot. This will be equal to the U-Kismet Math Library. We'll use the find look at rotation function here. This once the starting points are where to look from, which will be the view target and then the function get act location. And the final location where we want to look at, which will be the player controller, the get, pawn function, and again the get act location. So we'll be rotating or looking from the camera's position, the view target towards the player character. Then quite simply, we'll set the view target rotation, so the camera component that we're looking through. And we use the set act of rotation to be our F-rotator that we've just created rot. And as we're using the U-Kismet Library, let's not forget to go back up to the top of the file and we'll also include the kismet forward slash kismetmathlibrary.h. With that done, we'll do the usual. We'll jump back on over to the editor. That's the code side of things that kind of wrapped up and ready to go. We'll hit the hot recompile and double check that that's all gone through successfully. In this case, it was very quick that that's all gone through and worked as intended. And we can start playing around some things here. So we can see that we're getting exactly the same behaviors before. So when we walk into the box area here, we are lurping into looking through our new view target. We can come back out and because we've exposed the blend time variable, we can see what a longer or shorter blend time will look like. Generally, one feels pretty comfortable. Lower than that can start getting quite snappy, longer can take a little bit too long to move to what we're hoping to look through. And we're also just going to play around with the area that we can run around in, making it a little bit bigger when this is dynamic is going to make it a little bit easier to test and easier to see what's going on. So feel free again to pause the video, change the box collider size and maybe move some obstacles into the box if you wanted. After a small bit of time playing around, this is what I have. So no huge changes. And then of course, the final thing we want to test is if we set the boolean to be dynamic, we can now see that the camera is trying to rotate and follow where the player is going. So it's kind of constantly looking at us. It's not completely static. As I mentioned, it's not a super complex dynamic camera, but that gives you an idea of the difference between a static camera, which does nothing apart from look at where it was ready pointing and a dynamic camera, which is trying to keep the player in view in this case. A more dynamic example might be trying to pair around or avoid things like the cube that I've placed in front of the character when it runs the complexity will be down to the type of project you're looking to implement these cameras into. Another thing I just wanted to show very quickly as well, as I've mentioned, there are a load of benefits of setting the view target to be another camera because we can pass things along like the post processing, the field of view and things like that. But if we wanted, remember, it does take in an actor class, so it could be absolutely anything that's in the world. In this case, we can set it to be the white cube that I have stood up and we can now start viewing through the white cube as we can see here. So it's a little bit awkwardly placed and I've got a warning here that I've set this, I'm trying to rotate it, but I've completely forgotten to make the cube moveable. So if we quickly go back out, I'll make this moveable and we can now test it. So we get exactly the same functionality. It's still trying to look through this and follow the character around as it moves, but we're just doing this now through a cube. So that's really the implementation of all of this and now that we've got all of the terms and everything's hopefully syncing up with the different ways that you can use it, the different options you have available to and how all of this comes together and works through the camera framework. What I want to do is just jump back into the code files very quickly and provide some insights as to what's been happening under the hood, so as to speak, as we've been using all of this. So hopefully if all of this is proven to be interesting, you can dig a little bit deeper and start looking through what functions are being called exactly what's expected, what they're doing and things like that. So to begin with inside of the ViewTigate actor, this will be a perfect place to start. We can look at the set ViewTigate with Blend function from our PlayerController. We can right-click on the context actions and we can navigate to the definition. From here we can see what we've got some useful comments above of exactly what's kind of expected, what the different components are and the parameters that are needed. This is of course blocked us into the header file, so I'm just going to jump over to the code file and I'll find the set ViewTigate with Blend function to see exactly what's going on. So quite simply, all of those parameters were updating what the BlendTime is, what Blend function to use, all of the things that you'd expect based on the parameters being used. The important thing here is that we then have another function being called which is the SetViewTigate. So if we navigate a little bit further and see what the SetViewTigate is doing, and this is what I really wanted to dive in because again, that class, that thing that a lot of people don't necessarily know about is once again being used and is an integral component or class in the structure of what's happening here. So when we call the SetViewTigate function, we have the parameters that we've just set up, all of those will be getting passed in. We're now going to access the PlayerCamerManager again and we're going to use the SetViewTigate function through the PlayerCamerManager this time. Now just use the context quick actions here and we now want to jump into the PlayerCamerManager class to have a little look at what's going on under the hood here. And this is where we can see it may have looked as though the PlayerController was very kind of simple implementation relative to what we kind of see going on in the run time. It's the PlayerCamerManager which is doing a lot of things in the background. Doing all of the validity checks, making sure that we have and are keeping track of different ViewTigates, the current ViewTigate, the one that we're trying to look at, tracking whether already partway through a blend when something is happening. So all of that is set up here. And as I've mentioned at the beginning of the course, it's not very often that you'll want to change very much inside of the PlayerCamerManager. But if there is some very unique kind of ViewTigate swapping that you want to do, this may be where it's getting overridden and you may want to add your own implementation on top of this. So again, this is why I just wanted to mention it's very worth knowing what it's kind of going on in some of these lower level classes or the very least that they exist and what they're doing. So in short, this we can kind of scroll through here now. I won't go too in depth into what's happening, but it's checking if we have valid classes to swap between. It will also be doing things like if you go into other parts of this class functionality, it will be checking if we're going to an actor which already has a camera component. And again, this is why we'll be checking if it has a camera component, whether it can take on the things like the new field of view from one camera to another, whereas of course if it's just going to an actor, like I've shown you with the cube, then of course there's no field of view or post-processing values to take from that. So it'll just look directly through that actor. It's usually quite an interesting read going through some of the engine code in seeing what's happening, but in essence, that was really all it takes to call those functions. So switching between different cameras, adding various smoothness and blend transitions and blend speeds, as you could see is very, very simple, whether in Blueprint or C++.