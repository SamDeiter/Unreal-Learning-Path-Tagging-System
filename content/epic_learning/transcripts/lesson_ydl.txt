In this module, we'll get a better sense of how our PC hardware is rendering the scene and what is keeping us from achieving our target frame rate of 60 frames per second. Before we modify any settings, let's set a bookmark so we can always jump back to the same camera view in our viewport. That way, we're always getting an accurate update for our optimization when we're making changes. Move your view in the viewport and hit Control-1 to set a hotkey. Now, if I accidentally move the camera, I can just hit one again to jump back to the viewpoint. Let's enter a new console command. Type Tilda-Stat-Unit. The game and draw threads are both rendered on the CPU. Fortunately, those are running great in our scene and they are not the bottleneck. GPU, on the other hand, is slowing us down. If our project was utilizing dynamic resolution to adjust the screen resolution based on GPU workload, we would also see a metric here for primary and secondary screen percentages. Let's open the GPU visualizer using the hotkey control shift comma. This tool takes a snapshot of the scene and provides a concise breakdown of what the GPU is doing to render the viewport. Basically, our task will be to get some control over the biggest defenders here in this window. When we highlight over different areas, we can see that post-processing and shadow depths are taking the most time to render. Click on post-processing. It looks like the main culprit here is FFT Bloom, which is an image-based convolution bloom effect for realistic lighting. This is most visible in the highlights of the image, here on the front plate of the bike, and on some of these reflective parts in the middle of the body. Let's look at shadow depths, too. It looks like there's a few point lights in our scene that are taking a really long time to render. In the world outliner, select the post-process volume. Go to the Bloom effect in the lens settings and set the method to standard. Next, select these four dynamic point lights that have been placed in the scene, and let's just delete them. Open the GPU visualizer again with control shift comma, and we can see that post-processing and shadow depths are taking much less time. Furthermore, we can see that we're now sitting at a really smooth 60 frames per second. Since we deleted the accent lights around the motorcycle and removed the cinematic bloom, we could modify our scene with some less expensive features. A few nicely positioned static lights or more interesting HDRI skylight could really help to make the lighting more colorful, and we could adjust the settings for the standard bloom to be more interesting as well. If your CPU is the bottleneck, there are a few tools you can use to identify what exactly is the issue. Enter the console command stat scene rendering. And first see if you have a high amount of mesh draw calls. Put simply, a draw call is an instruction sent by the engine code to draw a group of polygons. The more groups of polygons you have in your scene with various complex materials, the more draw calls you will have. You can see that our scene here has very few draw calls because we have a very small amount of geometry with not a lot of materials applied. But if you're working in a huge architecture file, you could easily have thousands of draw calls. You could reduce the number of draw calls by removing or merging objects, removing extraneous material channels, reducing the amount of shadows being cast by dynamic lights, and reducing the amount of objects that are visible in the viewport from any angle that your user will be able to view. Type stat scene rendering again to make this go away. If your game thread is the bottleneck, you can type stat game to get a better look at the issue. This will show you how many tick operations are occurring on each frame in the viewport. If you have complex blueprints that are using events on tick, that may be evident in this profiler. For now though, same as before, this isn't an issue, so I don't need to make any changes. In this module, we learned about how to identify GPU and CPU bottlenecks in an unrelentient project. We also learned some easy ways to reduce draw calls to help make a project run at a higher frame rate. Next, we'll look at some additional commands and functions we can use to help improve performance even further.