Now that we've looked at the FBX export option, let's take a look at data smith. I think you're going to find that data smith is a lot faster, a lot more intuitive and requires less work on your part. With data smith, you can export single assets at a time just as with FBX export. And that may come in handy later on in the pipeline when you're making changes and you want to bring an asset in by itself. But in this case, we're going to export the entire scene. And all we have to do for this is go to file export and in the export dialog, we'll go to our save as type and choose unreal data smith or you data smith here. And we'll save this as arc exterior. And you can see here that the data smith export options window is quite a bit simpler than the FBX options. All we really need here is what we want to include visible objects or selection. We'll leave this at visible objects. It should be selected this way by default when you choose to export and then hit OK. And we'll go ahead and let this run and we'll come back to you once it's finished. Once data smith finishes exporting, it's going to open the output warnings window here, which will give you any kind of indications for issues that still need to be corrected. For instance, we have invalid objects here and it lists out a bunch of rectangles and other objects here that are not supported in the export process. They might be broken, corrupted in some way or the geometry might be way too small. So you'll want to obviously go in and check these items and then verify whatever it is that may be wrong with and fix it. And I've left a bunch of items in the scene here so that on our first export process, you can see some of these errors. We also have the partially supported section here. As we spoke about in the materials chapter, we have some materials here that aren't supported. First of all, we have the V-Ray two-sided material. So it's telling us here that it's going to fall back to the first child of that material. So we can leave that. It should be fine, actually, or we can go in and get rid of this V-Ray two-sided material. Then we also have the noise map, which again, we'll have to either replace with a texture or use the noise node in the material editor for Unreal Studio. Below this, it's just letting us know kind of a warning that all bump maps are going to be converted to normal maps. And then we have the invalid transform section. And this is going to list out any meshes in the scene that need to have their transforms reset to match, as we spoke about earlier, we want to have that Z up, X forward, and so forth. So this is going to be a good list here in indicator of whether or not you need to go in still and fix transforms on any of your mesh objects. And we have quite a few to deal with still. Now we can import the data Smith file in Unreal using the top menu here, import U Data Smith file. If we click the little drop down next to it, we can see we can also import CAD data. And there's all sorts of CAD data formats that we can bring in here. But we're going to just go ahead and import the data Smith file format. Here's our arch exterior U Data Smith file here. Now we just need to specify a location for it to import into. I'm going to choose this Data Smith import folder I've created. You can see here that the Data Smith import options window is quite a bit simpler. Again, then the FBX import options window, the only real options we have here are what we want to import. So we have geometry, materials and textures checked, as well as lights and cameras. We can check all of that as well. Then we have our show advanced drop down and this allows us to specify the minimum and maximum light map resolutions. Here I like to specify the same size for both. And what this means is the size or resolution of our light maps will be 512 by 512. We could increase this to a higher resolution, but I like to start out at 512. Then the minimum resolution is basically going to tell Unreal to use as much of that light map as possible when generating light map UVs. So there'll be just enough padding to keep the light mapping from bleeding over into other light map UV shells. But it should use as much of the actual light map texture as possible when generating those shells. And it would just need to hit the import button. And that is all there is to it. We spent some time waiting for the import process to finish. Here is our entire scene brought in using Data Smith. All of the materials and a V-Ray materials in this case are correctly applied to the scene. If we look under our world outliner panel here, we can see all of the different assets and we've got our groups still being respected here. So we have these panel groups. Now if we go into the Data Smith import folder we created, you'll find a subfolder with the name of the Data Smith file, so Arch-Exterior. And then in that folder, you'll find three subfolders, geometries, materials, and textures. Self-explanatory what's in here, but let's go ahead and go into the geometries folder. You can see all of the assets from the scene are in here. Now we haven't spent enough time putting some of these assets together. They're still being imported as singular objects. And that is part of the reason why this is running a little bit slow here. We've got a lot of singular objects. There's very little instant scene being used here and some materials that need to be worked on and so forth. But it's just that easy using Data Smith to bring in the entire scene with very little work required. And it just comes in with everything the way it looked inside of 3D Studio Max. So for instance, also if we go into the materials folder, we can see all of our V-Ray materials here nicely set up ready to go. And we can of course start to edit them here inside of Unreal Studio. If you made some changes to an asset in your 3D tool and wanted to re-import it into Unreal Studio, simply go into that asset folder under geometries, then pick the asset you've made changes to, right click it, and choose re-import. So this covers most of your export and import options here using your 3D tool with EPX and Data Smith. If you want any more details on this, be sure to check out my Data Preparation course, where I go into a little more detail on each export option. Next we'll look at some ways that we can optimize this content to get the best performance out of it at runtime.