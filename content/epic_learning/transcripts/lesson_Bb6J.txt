Let's start building the RIG logic for a Backwardsolve event chain in our Control Rig asset. This will allow us to use some high level functions in our Rig setup like IK while modifying an existing animation data. Now to start with Backwardsolve, right click on the Rig graph and look for Backwards. Under the events category, select Backwardsolve. As explained earlier, Backwardsolve is opposite in functionality to the forward solve where bones are responsible to drive the controls. So we can use the get set node combo in the opposite manner to create the entire Backwardsolve. Starting with the spine chain, we can either create all the nodes manually or we can copy paste the entire spine chain nodes from the forward solve and paste it in the Backwardsolve. And we can make use of Control Rig's flexible item based node that can be updated on the go. Let's interchange the bones with Controls and vice versa. You'll notice that the title conveniently changes to indicate the current item being used by the node. Now that we are done with the spine chain, let's do the same for all the joints till finger. We are done setting up the Backwardsolve, so let me take you through a few changes that I made along the way. For all the controls that are not snapped to the bone transform like the pole vectors, I'll use a new node called Parent Constraint instead of Get node. This node helps us to maintain that relative offset between the parent bone and the child control. Finally, for the fingers, we are still using the for each node and I've just entered in the item type where I'm getting the transform from the bones and setting the control with these transform. Before we move on to use our Backwardsolve Insequencer, it's always a best practice to debug it inside the Control Rig editor first. To do that, I'll go to Preview Scene Settings tab and change the Preview Controller to use Specific Animation and assign an animation here. Once you do that, you'll see an animation timeline with default playback controls in your viewport. I'll hit Play and you'll notice that nothing is moving. That's because the Event Mode is set to Powered Sol. Let's switch this to Backwardsolve and you'll see this yellow outline around the viewport, indicating that you are in Backwardsolve Mode. One thing to note here is that you won't be able to move your control in this mode as the transform of controls is being driven by the bone transform. We can't scrub through the timeline and you'll notice that all the controls are following the bone perfectly as intended. So if I were to disconnect input transform on the Body Control node under my Backwardsolve Event, you'll see that the Body Control goes to the origin as the input translation is now zero on all three axis. If you see any such control which is not where it is supposed to be, then you know that something is wrong in the Rig Graph. But you'll notice that the animation plays just fine. So if you want to visualize the result that you'll get after baking, then you need to switch the Event Mode to Backwards and Forward. Now the viewport has a blue outline indicating the selected mode and the character has a broken lower body. This is because the current Event Chain first runs the Backwardsolve and then the Forward Sol. If you scrub through the animation, you'll notice that the Body Control is first placed at the origin in the Backwardsolve and then it sets the bone transform of the pelvis using the Body Control in the Forward Sol. So this is a good way to validate if both chains are working together well sequentially. But in order to visualize the exact outcome of baked control rig using Backwards and Forward Sol, make sure that all the bones and controls are backwards and forward solved in the Rig Graph. So I'll go ahead and connect the input transform back. Now that we have completed and verified the functionality for Backwardsolve Event Chain, we can now bake animation onto a control rig in the next module.