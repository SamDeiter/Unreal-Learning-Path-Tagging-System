The final data type that we'll be working with in this project is going to be our float curves or curve floats as they are fitly named. This is going to last to extrapolate just a tiny bit of information, usually float values along that curve and we'll see how we can pull that information, drive some rotation for all of our item. So just a very quick piece of project hiding before we jump into that. One thing we didn't do in the past is double check that the data asset class that we were creating is working with the widget. So we can just confirm this is the data asset pulling the information as expected. We then have our data table information being pulled over here. So what I'm going to do is go back to the view where we've got one of each of the data tables. Although we can see the data assets and tables are working together. Just to keep things nice and simple again, some of the processes that we've taken in this course have been levels of repetition just so that we can get all of the information across. But for the rest of this we'll just work with the data tables so that we only need to worry about one class. We now know that they work with either assets or tables. Logic is all set up if you wanted to test things in both. What I'm going to do is just make things look a little bit more interesting. I'll recreate those extra items by just alt and dragging these out. I'm just going to bring in some cubes. Again, this for visual purposes, this is completely optional. I just want this to look kind of like a stand or something. When the rotation is happening it just looks quite nice. These items will be almost like a prop on display or something. It really doesn't matter. But it's just going to make the level a little bit easier to visually understand as we go around. I'll just set each of them to be a different item. We're going to have the radio, the trophy and the spaceship. Each of these in the data table that we'll be working with to store the curve float and the rotation duration information are all in here. So it's going to be important that we have one of each to try the different curve information. We can double check that by going into the item info table. This is what we'll be working from. We're going to need a class night to fill our curve float slot and we're going to be setting the rotation in our E plus plus code a little bit later. To get started, whilst inside of the data folder, what I'm going to do is we'll right click, as we've done previously, all of the data information or the data assets that we've been working with. You'll notice I'm under the miscellaneous category. We're going to do the same thing again. We'll go to the miscellaneous category. We can see here we have the option for a curve. When we select this, we have a few different types of work with. Once you've seen one, you pretty much get the idea of how to work with the others. So we're going to work with the very basic curve floats. Like I said, this will be a multiple float points over curve. We can have things for linear colors and vectors. So we're going to create the first one here. And I'll just call this one rotation curve linear. We'll make this our linear curve. Also remember when we were importing our CSV and JSON files that we had the option to create curve floats from those. So again, you remember that these can be populated by information you've created outside of the engine if you had that available. These are going to be really simple. So we're just going to work with this internally. And that will give us the functionality to work with at least if we jump into the rotation curve linear. We can see this is fairly similar. If you've worked with curve information before, it's not quite the same as you might have seen in some of the blueprint classes. And to add a new point in the curve rather than shifting left click, we actually just press enter. We can select these. And I'm going to set the first one to be zero with a time of zero. So that should be right over here. And see that lines up with the float values we have here. And then grabbing the second one, I'm going to set this to a time of one. So again, we're going to normalize the information as we go through and program that in the code files. So remember, we're just going to normalize this from zero to one. I'm going to set this to a value of two based on the way that the logic is set to read this information. And we could add extra curves around the center if we wanted. And again, of course, drag these around. And we're going to base the rotation to take in the value along the curve as we go. But I'm naming this one the linear curve. So we're going to keep this nice and simple. And in the next curve that we make, we'll set this to look a little bit different. But with that done, we can save this one. We'll go back into the data folder. I was going to control W to duplicate this. And on the emnest one, rotation curve smooth. We don't need too many different curves. We're just going to jump back into this one. And what we want to do, drag select both of these, right click on one of them, set this to auto. This is going to give us the little handle that we can then add a slight easing on there. So it's going to smoothly interplay between these two points. And that should be enough to show the slightly different results in the animation when we put this in. With those two ready, we could have one per item, but it's going to be interesting to see how with the different rotation durations we're going to have, the rotation curves will look slightly different as well. So if we come into the table as the last step before we go to our code, for each of these, we just want to select one of the curve options. I'm going to set the radio to be linear. I'll try if you can be smooth. And let's set the spaceship to be smooth as well. It's going to look quite interesting, I think, because we've got a very quick rotation rate. So if that done, we can save that act. And that is everything in the editor ready to go. We're then going to want to go to our C++ classes. We want to go to the data actor base. This is all going to be done in the base class. Like I mentioned, although we won't necessarily be working with the data asset class anymore. If you did want to test things in there, we're going to do the rotation logic. But all of that is taken from any of the classes that we work with. So it would work for both. So before programming in some logic, again, this is going to be very kind of generic logic. The logic will be placing in here as basically just going to be the rotation of our actor. So it's something that you should be familiar with already. The important takeaway is going to be how we can access that information from our curve. So there are going to be a couple of things we need to do outside of the actor base. The first is if we do want to update this in the asset and the table classes, we're going to need a variable that they can access from our actor base header. If we navigate to the header class, first of all, in the protected section, I'm just going to create two new variables. The first one is going to be the curve float that we're working with. So this will be a you curve float. We're appointed to that class. And I'm going to call this M float curve just because we're going to have a few things named similarly. I'm going to start separating it out with the member variables here. And I'll make sure to default this to be a null pointer. Then just below this, I'm going to create a new float named M rotation duration and set this to a default of zero. Then for the new property, I'm going to set both of these to be just simply blueprint read right. They don't need to be exposed in any way. We don't really need them to be in a category. But just in case we did want to do that extra work in the blueprints to also fill these in there. So remember, when we're on the construction script, if we're not running the code or accessing the code from our C++, then we might want to have access to the blueprint graph. Finally, there's going to be another variable that we need, which we're just put into a private section, nothing else when you'd access to this. And this quite simply will be a float named current rotation time. And again, I'm going to set this or default this to zero. Okay, so we now have these two important variables are going to be the ones that we really needed to make here. This means that we can I go back into our data asset and data table classes. And if you're only wanted to do this once, you only really need to do it for the data table, because like I've mentioned, this is where we're going to be focusing at the rest of our work. But it's a very simple step. So I'm going to do it in both. Just in case we wanted to play around and test the different classes inside of the data table, then in the code section, after we have all of the information from our row and we filled our generic information, what we want to do. And really, this is just so we don't have to keep calling this kind of longer that's of information. And also because we have slightly different classes that we're working with again, purely for the sake of the course in the base class, we need to potentially account for whether we're working with a table or an asset. And because this information is going to be a little bit more universal, I'm just going to set it here instead. So what we're going to do is we'll say that the M float curve is equal to the generic item info dot curve float. And again, in a similar way, we're going to say that the M rotation duration is equal to the generic information rotation duration. So you can see here, this might be an added unnecessary stage, especially if you're working in a project where you don't have the same information in a kind of generic base class, it could potentially be from two completely different types of information, whether that's an asset or a table. But to save us some extra queries and to make this a little bit more flexible, we're just going to take this code. I'm going to do the same thing in our data actor asset code file. This time just before we break out of the for loop, I'll paste this in. And because of the naming conventions, we have the generic information is what we're filling here anyway. And again, we can just take this from the curve float and the rotation duration. And we'll fill this value here. So again, we're going to set them in the child classes. And it just means that we can ensure that regardless of what we're getting this from, we're just going to use these two bits of information for the rotation logic that we're going through now. So back in the data actor base class, this is the code file that we're going to want. And we have all of the values that we need to work with. We want to be working on our tick event. So ensure that you still have your tick function available. We're basically going to add a rotation offset to our actor if we have some float information to work with. The first thing is obviously it's going to be important to check that we have the float curve to work with that that's been filled. And we have a valid float curve or curve float asset that float information from. Next, we want to start tracking the current rotation time against the total rotation duration that we are getting from our assets. So we'll say that if the current rotation time is less than or equal to our rotation duration or the em at rotation duration, then we'll house all of our logic in here. We can do the else statement very quickly because of course, if that's not the case, then we know that we've completed our rotation cycle of what we want to do. Reset the current rotation time to zero. Then like I've mentioned, we're not going to go too deep into the rotation logic here. Hopefully this is going to be very kind of familiar or understandable to anyone following this process. We mentioned we're looking less at the kind of generic programming logic and what these features are doing more about how we're extrapolating this information from our data classes and data assets. So the first thing we're going to want to do very simply set the current rotation time. So making sure that we're constantly updating this based on the delta time. Then we're going to want that value between zero and one. So a kind of ratio to float create a new constant float named time ratio. And to get this, we'll make this the result of the F math clamp function. We'll be passing into the current rotation time divided by the em at rotation duration. And we'll clamp this to be a minimum of zero and a maximum of one. We'll then just create a new constant float named rotation alpha. And this is going to be equal to the return of a function named get float value that we can find on our M float curve. And quite simply, we want to find out from the normalized value of our float curve, what point of the ratio. I met we're currently at in this curve positions. It would just pass in the time ratio. And to keep things nice and simple or to keep things kind of clean and easy to read, we could just go straight ahead now and set to the actor rotation. We'll take one extra step, which is to create our effortator so that we can pass this in after we've done the modifications to our custom value. So we'll create a new constant effortator named current rotation. This is going to be the result of another F math function library using the Lurp function. And then let's just assume we're starting from a fresh rotation each time that we can use the effortator zero function. We want to rotate to a target rotation. I've just found that sticking with your 180 is going to be the easiest here. This is going to give us because we've set the float curve to two. This is going to give us the full 360 rotation. It was kind of the easiest way to work with things for this demonstration purpose. So we'll create a new effortator. We'll fill the pitch and roll with zero. The in-nual will be that 180. And then the alpha for our Lurp, it's going to be the rotation alpha that we've just created. And that gives us all of the information that we need. So we can now call the set actor rotation function and simply pass in the current rotation. Of that complete, we can test our code, though, we'll make sure that we save and compile that and return to the editor. In the editor, we'll just hit the compile button to ensure that everything again has synced up and we have all of the information, which is programmed. A couple of small things to check is that we probably need to refresh all of these in the editor. So I'm just going to shift select all of our items, make sure that they are, that's a run code. Then we'll un-tick this and tick this again. That should just force the construction script or that. And then we'll force either our construction script or the post-change function we've been working with to process that. The main thing is that's now updated, which curves these are using. Now remember, we also took that extra step of making the variable blueprint read right. So I'm going to go back into the data items blueprint folder, go into the data item table. What I want to do in here, maybe just before we set the static mesh, I'm going to set to the M-loaked curve and the M-retation duration. I just hook these back up. And in fact, this can be done after the static mesh because we have the information that we want to hear inside of the generic item info. What we want to do is tick the curve float and the rotation duration pins. Then we can just hook these in the correct nodes. That's really it. So it's just getting a little bit more information. And again, this is optional. But if you wanted to test things in blueprint as well as your C++, we need to make sure that at some point these two values are also being filled. And the final step is just to check how this information is being pulled apart. So we have these two bits of information. Remember, we're dividing the rotation time by our variable that was throwing here, the rotation duration. And we're just using the curve flight. So we're going to pull some information about the flight value along the certain point of this curve. If we go and press play, if everything synced up, this should now be working. Okay, and then we go. So we can see that with rocket, that is using the linear rotation. So it looks like it's a pretty much unstopping rotation. It looks smooth, but it is just going essentially in a 360 degree circle without any noticeable changes. In comparison, the trophy is using our smooth rotation curve. It doesn't look like it's doing much different until it gets to the end. And you can see it kind of using that ramping curve information to lurped towards the end. And then it's taking a kind of abrupt stop and then starting the next rotation with that kind of easing in based on the curve that we've given it. And in a similar way, the spaceship is doing a very similar thing, but we've set the start only a rotate for a second. So it's a lot more aggressive, but again, you can see there is a kind of break in between each of those 360 degree rotations. So if we were to come back into the editor and change any of these out, you'd see how this will look different. I think the biggest and most obvious one was the spaceship. We can turn this to be the linear curve. And we should nice see that the rotation here is going to be a lot more. This is going to look completely continuous. That, of course, do remember when you're doing this, we need to make sure that this is being updated constantly. None of that information will be pulled unless something here is changed and forces that's update to be made. We're going to un-tick this, we'll watch the curve change. There we go, and that updated to a linear curve. So we've got the right information, and we can try that again. And there you get, you can definitely see that this is using a different rotation at this stage. So we could do that for anything we could keep going through. And this is really the kind of beauty of working with later driven workflows like this is that we can now completely play around with test and trial, different patient speed, durations, types. We could create a few different rotation curves. And we could see what we want these different items to look like when they're on this kind of standard. But it just kind of shows that if we want to choose this as like a museum display or something and you wanted to really refine what the rotation of a certain turn style was going to look like, we can do this very, very easily now by coming into our data table, changing how long we want that turn style to rotate, the type of rotation, mechanisms we want to give it. And all of this can be changed on the fly using our data table. We just need to compile something one that I take all of that information in process that and we can update and test things pretty much immediately. Like I said, this is going to be very, very useful, especially things like the curve floats where if you wanted to make your menu a little bit more dynamic, wanted buttons to have slightly different pops and rotations and things for different menus. You can create this functionality like we've done in our C++ code where we've programmed to that rotation functionality once. And then we can make each of these three different items or in this case I'm talking about like the menu systems, each of the different buttons are elements. Have that completely different animation being applied to them using that one chunk of code once and then just passing in different rotation times, different float curves and things to use as the base of their design. So the final thing, like I've mentioned, I wanted to try and avoid programming too much in this content, in this course, which wasn't specific to the data structures. What has been expected that you have some familiarity with code with creating your in logic and blueprints, which is why we're not going to program the same rotation functionality on the event tick because again we'd have to do this on data table and the data assets if you wanted to work in both, whereas in comparison we've put everything in the parent class so it runs on both. So again, if we had the information available at this stage we could bring in a data asset class. We could set this to be a certain type. And of course, we give it a correct item information asset. At this stage we haven't filled in the rotation information so I just go to this very quickly. Just a very quick demonstration here to get at least one thing working. So we're using the rocket radio here so I'm going to give this the linear curve. Save this. We're going to go back in again. We're just, it can un-tick this, but we now have the curve information. Because this has been done in the parent class, we'll see that this also rotates on the spot. Okay, so that's the reason that we've separated the code out that way again, kind of for the purposes of the course, so that we didn't have to do too much duplication of code to look at the different class types. But what I wanted to say is that we can also come in and we can get that curve information in Blueprints. So if we get the M float curve, like I've mentioned, the rotation logic isn't too important. That was very generic and you'll probably see that in a lot of different places. What you might not be familiar with is to get the information is very simple. So there's only a really a few things to look at in Blueprints. If we type a curve, this will bring up most of the curve at specific maths that we can use. So the one that we're using in RC++ code was the get curve value. So that's going to return the same thing. And again, expects the same thing being plugged in. So what time, though, what time along the curve, are we currently checking and what value is that returning? That would be the only real difference. The logic to handle the rotation would be the same is just knowing how to get the information from our curve asset in Blueprints, which is going to be very simple, just a case of typing in essentially the thing you're looking for. And that is really that topic wrapped up. We've now looked at all of the different internal assets that we can work with as well. There's one kind of bonus topic that I wanted to go into, which is how we can also take some of the information we've been working with, specifically the data table and exporting that.