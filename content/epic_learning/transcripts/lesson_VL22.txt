Being able to create, save, and delete spatial anchors is great, but the power is demonstrated when they are shared with other sessions and devices. In this module, we are going to create a new component that will assist us with finding anchors in our space and creating a mesh in the location for us to identify across devices and sessions. Alright, let's get started by setting up BPPON to identify anchors it is found. First, let's add a watcher to our BPPON to search for spatial anchors. Create an event named Azure, underscore, prepare, watcher. Give it one input and name it start. And set it to type Boole. We will both want to create the watcher as well as remove it as they take up resources to search for the anchors. From start, create a branch, and from true, create a second branch. We'll come back to this in a bit. From true on this second branch, search for create watcher. Drag backwards from in locate criteria and select make Azure spatial anchors locate criteria. Set search near device to true. This will enable our devices to use the map data associated with a spatial anchor to find it. As it's currently set, it will find a maximum of 20 anchors near the device and it will search only 5 meters from the device. When the user needs to search for specific anchors, the cloud identifiers can be provided to the identifiers string array here. For now, we will only search for anchors directly around our device. From out result on the node, check if it is equal to success. And if so, promote out watcher identifier to a variable and rename it to current, Azure spatial anchors watcher. And set its default value to negative 1. We'll use this identifier to both stop the watcher when needed as well as prevent more than one watcher to be created at any given time. Back to that second branch we created. Check if current Azure spatial anchors watcher equals minus 1. From false on the first branch after Azure Prepare Watcher, create another branch. Check again if current Azure spatial anchors watcher equals minus 1. And if false, search for stop watcher. And plug in our current Azure spatial anchors watcher. Then set the value to its default minus 1. Alright, our watcher is made. Pan up to our load local anchors event. Move over the clear node and add one of our use Azure anchors macro right after the event. Plug local anchors into clear. From cloud anchor, call Azure Prepare Watcher and set start to true. We don't want to only wait for the user to request the loading of found anchors. And so let's also call Azure Prepare Watcher right when the Azure session begins. Again set start to true. Pan over a bit to our config session two node. Start backwards from course localization settings and create a make course localization settings. While the HoloLens does not support GPS, it does support Wi-Fi and can use local Bluetooth Beacons UUID to assist in finding spatial anchors quicker. As stated, it will be a course approximation to the original anchor. But as the spatial data becomes more detailed, the spatial anchor will find its correct location. I'll set enable and enable Wi-Fi to help quick in the search. In order for our device to use these capabilities though, they must be set in our project settings. Navigate to the HoloLens settings and turn on Wi-Fi control, proximity, location, and Bluetooth. Alright, we have the watcher setup to be created when a user presses the load button, as well as right when our Azure session is started. Let's handle what should happen when an anchor is found. Add a new Azure Spatial Anchors event component. This component provides us three delegates that we can use in this blueprint. Located delegate will get called when an anchor is found. Let's bind an event to it so we can use the data it finds. Create a new event named Azure underscore prepare to load. Drag in a reference to Azure Spatial Anchors event and search for bind event to ASA anchor located delegate. Call this event after the successful creation of our watcher. This will allow us to immediately begin searching for spatial anchors. Alright, from our bind event, drag backwards from event and create a custom event. Name it Azure underscore spatial anchor found. Check if status equals located and if so, drag from cloud spatial anchor and search for create a our pin around Azure spatial anchor. You can of course place your own pin ID in here that works for your system, but I'll drag again from cloud spatial anchor and get its Azure cloud identifier. From the return value, create a branch. From false, I'd recommend indicating to the user that something is wrong. From true, add the new anchor and its identifier to our loaded AR pins. Call cycle current anchors in toolbox event to update the toolbox. Because our local persistent anchor system was not built to spawn a single anchor on its own when one was found, we need to create that now. Create a spawn actor from class node. Set the class to our BP anchor. If you don't see anchor name at the bottom, right click the node and select refresh nodes. Plug in the cloud identifier into anchor name. Split the struct transform pin and then call attach actor to pin. From return value, get the anchor and plug it into component to pin. Plug the cloud identifier into debug name. Now we have a setup that will discover when an anchor is found and when it is found, it will spawn a BP anchor at its location and set its name to the identifier. Alright, let's first create the anchor and once it is saved, I will close the app completely. We can see in the Azure portal that it was saved. Back to the HoloLens. I'll open the app and walk around a bit. Because the watcher is created right when the session is started, I just have to look around. And there it is. Our anchor was found and it holds the cloud identifier as its name. Also lots of other anchors that were created previously. In this course, we prepared our Azure portal accounts and created a new spatial anchor resource. Using the account information provided, we were able to connect our world builder project to save delete and load created anchors. We integrated cloud anchors into our local anchor mechanics and we set up a new component to search our map for an Azure spatial anchor. When it was found, it called a newly added Azure spatial anchor event component and we were able to turn that spatial anchor into an AR pin and spawn a mesh in its location. If I may, this has been a great experience for me and my team to create this learning path. We all hope it provided lots of helpful information and I want to thank everybody who assisted us. Good luck with your projects and keep exploring.