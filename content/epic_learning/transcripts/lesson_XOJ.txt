In this lesson, we'll be discussing working with UV manipulation. We'll cover these topics. Controlling independent UV directions, offsetting our UVs, and adding motion to UVs. Let's start by talking and working with UVs. There are multiple ways we can control usage of our UVs in our favor, based on optimization and just general preference. You know the basic setup of a texture coordinate, node multiplied by a constivalent to get a general tile going on. This setup allows you to change the tiling of the UVs. Let's just quickly set that up on our material here. If I have a material instance, I'm going to go to our material master. I'm just going to do a texture coordinate multiplied by a single value. I'm going to convert this to a parameter and rename this tiling. I'm going to set this to 1. This is our general basic setup for setting up tiling for any particular material, knowing that this will work based on tiling our texture coordinates of our UVs. To separate them, we would need two separate values to control you and V, or X and Y, if you're thinking in 2D space. Let's go over a few ways we can get this setup. The first would be to combine two values together. We can create another constant one, so if I just created a constant one, convert the parameter. We're going to name this UV tiling X. We're going to name this UV tiling Y. Now, we have two separate values that we're going to use. How do we get those combined into our multiply? What we need to do is create a node called an append node. What this is going to do is essentially create two vectors and add them together. Instead of adding them into a single value, what it's going to do is create a string from them. Basically, we'll have X and Y. Essentially, this is creating a constant two node from the two single values that I'm putting in here. Now, we have these two values. I'm going to set this to 1 as well, too. You can see now it's doing the exact same thing. If I increase this amount, you can see it's going to be tiling one directional. If I decrease this amount, you can see it's tiling in the other direction. We have direct control of each individual tiling based on X and Y as separate values. Another alternative way we can go about doing this is if we create a constant three, convert this to a parameter. Now, this is going to be parameterized so we can use this just as we could with the X and Y values as a hinarium material instance. We're going to name this tiling amount. In here, we have the three values plus the alpha. We have R, G, and B, and A. In this particular setup, we're going to be using R and G as our X and Y are you and V. How do we block it so that basically this value only exports or sends out two, a constant two or two values that we want to use? We can use what's called a component mask. A component mask is basically going to allow us to block certain values or to allow only certain values to pass through this node. Here, by default, it already sets it up to only allow R and G through, which is exactly what we want. We're going to now ramp this into here. We can do the exact same thing we did before. We control the X and Y or the U and V with R and G of this value in our particular material instance. Let's work now on offsetting the values of our UVs. This can be useful for sliding or changing the position of the texture on an actor. One good example might be this example here. So we have four different unreal logos that we have here. We basically want to offset it so each of our material instance is only using one. What we can do is we switch back to our U and V tag here just for simplicity sake so we can see a little bit more easily. We can set one to be 0.5 and this one to be 0.5. What this will do is it will only tile so that we only get one corner of the particular texture that we have. It's only getting this one corner. Now, how do we access these other corners if we only want it to show safer instance the black one? What we can now do is add a value to the UVs to offset that amount. So it basically would add a value to offset the difference of the UV number. So we're actually going to copy this, paste it, and we're going to name this UV tiling add or you can name it UV tiling offset. Whatever one makes it easier for you to remember what this does and offset Y. I want to make sure I set this to use or remember that that's going to be you. So now we add those values. We're going to asset it to 0 by default. So by default it's not going to actually do anything. It's not going to add any values to it. But if we change this value, let's say we add 0.5, you can see now it's offsetting that over so that we're getting to that black value. So if you look at the texture here, you can see it's adding 0.5 so that it shifts the entirety of the UV set over this black value. We can do the same thing here by adding 0.5. And again, it's now shifted it over one to the right and one down. So you can see now we're accessing that particular red area of our particular UV set. Let's do one more control for fun in particular when working with UVs. The pattern node can be quite interesting as it allows essentially the UVs to move within world space or move within their UV space in any direction. So what I could do here is I can add this value or basically take the output of this value into the coordinate of the panor. And then I can use this into our UVs. And let's just go ahead and set this back to 0 for now. And let's just set this back to 2. So we can see it being tile twice. So we can see the effect a little bit easier for our panor. So we're just going to add a simple time node. And with that setup, we're just going to add a bit of speed. So we can decide how fast or how slow it's going to be. So we have x and y. So we can also duplicate this same thing. Ramp this into here. And we're going to set this as our speed. So we're going to set this as speed x and speed y. So if I set this value to say something like 0.1, you can see here it's tiling x or it's making x pan in the direction much faster than y is going up in the direction. So we can change the speed to go a different direction if we use a negative value. So if we did negative 2, you can see now it's going in the opposite direction. Or if we did negative 10, you can see it's going in a completely much faster different direction than it initially was. So a panoron can have a lot of uses as well when manipulating the UVs. Or if we want to get motion with their actual UVs as we're using it on particular material. Say for instance, like running water or something like that that you're trying to get more of a dynamic effect where you can use the material and a simple panor to get much more dynamic interest within that fact. For now, let's go ahead and put this behind a switch so we can save it for later, but let's disable the feature for now. I'm going to use UV features. And I'm going to set it to true and the rest of this to false. So now we have our basic UV tiling back. And if we want anything additional such as offsets or any other additional features, that would just basically be behind the switch so that we're not using it right away. We're going to create a comment from the section, additional UV features.