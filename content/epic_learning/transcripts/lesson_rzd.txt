Today, many architects work in Revit, which produces a 3D building information model or BIM of a building. Even though these models are highly detailed and provide documentation for the construction process, they are able to be brought into Unreal Engine by way of Datasmith plugin. In this video, we'll walk step by step through that process. In the exercise files, you'll find under prerequisites a Revit file called 804 North Cypress. This Revit file was created in Revit 2019, so it will open in 2019 or any newer version of Revit. If you don't have access to Revit, the Datasmith files that we're about to export are available in the Datasmith exports folder. When the file opens, you'll see we have a nice residential model complete with its topography. The first thing we're going to want to do is make this feel a little bit less digital and a little bit more like a physical site model. We'll do this by adding what architects affectionately call Poshet to the base of the model, so that way when it's on a real-life table, it'll feel like it's actually sitting on it. The way we'll do this is by enabling the section box over in the Properties panel, under Extents, Section Box, Apply. From here, we can grab the four outward faces of the section box and pull them in until we're cutting into the site model. If you'd like, you can do the same to the bottom. Now it feels like it could actually be sitting on top of a flat surface. Next, down at the bottom we want to make sure the detail level is set to course. In this case that won't change any element of this project, but if you're using your own Revit model, this will help ensure that Revit is using the least complex versions of every element of the model. Similarly, if you click VD on the keyboard, this will bring up the visibility and graphic varieties. If there are any categories in your own Revit model that you would like to turn off, such as Furniture or Entourage, perhaps some complex plants, once those are off, that will also help simplify the model once it's brought into Unreal Engine. Lastly, curves can have a very high performance cost on a mobile device such as Magic Leap. So while this project is fairly orthogonal, it's still worth checking out to see if there's anything that can be reduced in complexity. If you get close enough to one of the railings, you may see that while the bottom rails are all square, the top railing actually has curvature to it. That's unnecessary. If you go to Edit Type, you'll see that the railing is mostly made up of square elements. Flat elements, those are all things we like. However, if you go to Top Rail, you'll see that the type right now is set to Elliptical. We'll click on the ellipses to see if we have any other options. And we don't right now, so we can duplicate this. And we want to keep the Architects Intention as much as possible, so we're simply going to call this optimized. And the only thing we need to change is the profile. In this Revit model, I've already created a profile that is similar to the current Elliptical one, but it's simply an octagon and therefore much more optimized. If we choose that and press OK, and OK again, you'll see the model change from a very smooth elliptical shape to something a little bit more polygonal. We're now ready to export our data smith scene files. To do that, we'll go up to Addins and find Export 3D View here. However, if we were to export right now, we wouldn't be taking advantage of the fact that we've set up several different design options and we'd be exporting everything on top of itself. So instead, let's see how many design options we have and it's two, and one of them is already turned on. But if we only want to export the base model, we can select everything with either a drag select or control A on the keyboard. And we'll see that the elements that did not get selected are part of the design option. However, within the Revit export, there is not an export selected option, so we'll actually need to make sure that we can't see that. The easiest way to do that is to go to the sun glasses in the bottom, called Temporary Hide Icelate, click that, and we'll choose Isolate Element. Now that that's gone, we can do an export. We'll choose Export 3D View, and then under Data smith exports, which we've already created, this is probably populated already in your case. We can do 804 North Cypress, underscore main model, to denote that this will be the base model that is always on regardless of which design option is on. Once that's complete, we'll reset our Temporary Hide Icelate and go down to our Active Design Option panel and choose Option 1. This will highlight Design Option 1 and gray out everything else, but once again, it's all still on and if we were to export right now, it would all go together. So once again, let's do a drag select or control A, go to our Temporary Hide Icelate, Isolate Element, and then go to Export 3D View. We'll call this one, Option 1. If we go to the drop down again and select Option 2, Option 1 will automatically turn off and all we'll see is Option 2, which is perfect for exporting. We'll call that Option 2, and that's all that we need to do in Revit. Before we import, there's a few elements of this scene that we will not need, so we're actually better off to start with a new level, an empty one. Once we're here, we may as well save this by creating a new folder called Maps, and we'll call this 00 using ML Initial Import. Now let's import our Datasmith Scenes. For our first one, we'll do the main model. Let's create a Datasmith folder by right-clicking and creating a new folder, and in that Datasmith folder, we'll be presented with some options. All that we need from a Revit file is the geometry along with its materials and textures. It did come with a camera, but we don't want that because magically might try to use that as the main camera that we view the scene through, so let's uncheck that, and then lights and animations are not included in the Revit file, so it doesn't matter whether you leave those checked or not. For the static mesh options, this would be the Lightmap Resolution, important if we're going to be baking the lighting in our scene, but we're not, so we'll leave those at their default. Go ahead and hit Import, and we'll see that a new scene file was created, it's called a Datasmith Scene, and this file is essentially what was dropped into the scene, it's that root actor there, and everything has been imported. Now it's very possible that when this happened, you didn't see anything. For example, if I go to Unlit and then Back to Lit, everything disappears, and the reason for that is because there are no lights in the scene. Now on a desktop project, I might at this point bring in a directional light, which would be fine, but lights of all kind are expensive on a mobile platform, so instead of a directional light, I would recommend using an ambient cube map to provide a simple light source. To do that, we'll find a Post-Process Volume, drag that anywhere in the scene, minimize our root actor here so we can better see it, we'll scroll to the bottom of the Post-Process Volume, make sure Enabled is checked as well as Infinite Extence Unbound. That way, no matter where it is or what size it is, it will affect our scene. But if you'd like, you can still reset it to 0000. Back over in Ambient Cube Map, we're going to want to select a cube map that will provide lighting, but if you select that right now, there's nothing to be shown because we don't have any cube maps in our scene. However, if you go to View Options and choose Show Engine Content, you'll be presented with a number of options that come with this version of Unreal Engine. Let's stick with a simple daylight ambient cube map, and you won't see anything until you select its intensity. The screen is a little bit low, let's try 12. If it's a little bit slow to navigate around, remember that you can use the mouse wheel to speed up or slow down how quickly you move through a scene, as well as the camera options in the upper right for your camera speed settings. If you'd like to save a particular view in the editor, use Control and then one of the number keys, and then you can retrieve that view at any time by pressing that number key again. Let's go ahead and import the remaining data smith scenes. Again, in the data smith folder, and there's our design option, number one, and design option two will overlap right on top of it. If you'd like, you could turn one off by simply going to the root actor and doing that, just for the editor's purposes. It would also be useful to make sure that this all moves together. Let's get an empty actor, reset it to 0,000, figure out where it is, press F on the keyboard to frame in on it. That empty actor is by default a movable actor. Let's call that empty actor's site model, and you'll see by the orange circle that it is by default a movable actor, so we won't be able to attach the rest of our site model to it until those are also movable, so select each of those and their first component, and change from static to movable. This is important because we will be moving the model around once we're in our magic leap and trying to locate it in the real world. Select all of the actors and drag them in to the site model. Now that root actor is going to want to be where we're locating the model, so it's important that that be at the level of, say, a table that we're going to be placing the sun. So let's go into an orthogonal viewport, like left, and we see that's a 0,000, so let's move the other actors that are now children of this, and bring it up to a spot where it feels like it could be placed on a table. When that's to your liking, you can go back into the perspective view, double check that that seems to be in a reasonable spot, and it does, and that's it for now. In this video, we explored the process of using the Datasmith pipeline from Revit to Unreal Engine, setting up an architectural site model with two different design options.