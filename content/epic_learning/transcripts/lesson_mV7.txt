And then last, there's a number of miscellaneous combinations that didn't really fit any rails. So we'll take a look at bump offset and what you use it for normal map blending, normal map intensity, and vertex colors and a couple of smaller ones in between that. So here we have a bump offset, which is used not incredibly often, but it is used here and there, and it's a very fun effect. What a bump offset does is that it tries to fake a parallax style effect by offsetting some of the pixels by moving them as if. So if you look at, for example, here, we have a stone texture. Let me disconnect it for a second and just connect a constant. Now I'm not sure how visible this is. You have to try this for yourself. But if you move the camera, the texture seems to move as if it's in the depth, as if it's deeper than it really is. And if you click on bump offset here, you have two numbers, reference plane and height ratio. You can change these numbers a little bit so you can get a bit of feel for what's happening. You can see that it's as if it's deeper. As if I can look into the cube. You can do negative numbers as well, which will be fun. Just play around a little bit with these numbers, you can see that numbers negative there too. And see what happens. Now it's bulge out. This is all kinds of very interesting uses. The more obvious one is that you would use a texture. This one apparently complains what a color. So there's a texture black and white texture assigned here. But it seems to complain. So it actually is interesting. If you get an error, it gives you hint of what it is. It says, sampler type's color should be linear color. So I clicked it and I will do sampler type here, color, linear color. This is the type that it expects. And for some reason, it didn't have the right color anymore. So I put it to linear color. Now we're back here. You can see what kind of strange things you can do with this. And it can change the height ratio again and see that you can come up with. This is probably the best number, but it's a bit subtle. It kind of gives you the impression the stones are coming out of the surface. It's deep. There's depth to the surface. We use this for other things as well. For example, an old tube TV. If you want to give the illusion that the tube TV is a layer of glass with something deeper in it, you would use bump offset. If you would have a light on the ceiling, a ceiling spotlight, and you want to give the illusion, the ceiling spotlight is a layer of glass with something deeper inside, something beyond that layer of glass. You could use bump offset. If you want to have some kind of aquarium fish tank style effects, that could be bump offset and so on and so on. Now parallel execution mapping, I'm going to skip over because it's something we're going to dive into. In one of the next few parts, you got blunt angle corrected normals. Here's an important one. If you want to blend two different normal maps together, so you have normal map A and you have normal map B, do not use a layer, do not multiply it together because all of those are actually incorrect. They might work, but they're not correct. The most correct way of doing it would be to use blunt angle corrected normals, which will correctly blend this normal with that normal. You can also change the intensity of a normal map by just multiplying it, by two to one or similar. The important part is, do not change the last number. The blue channel should always be one. So you can do whatever you want with the first channel, but don't touch the blue channel. It would be like this. Now it's more stronger, so you can kind of tweak the intensity of the normals a bit, but definitely do not multiply it with a single constant value, because that will also affect the blue channel as well. And that would not be correct. See that the blue changes in intensity. There is depth fades, which is a little bit hard to show here, and it might be better to talk about this when we talk about translucency, but this only works on translucent surfaces, but it allows you to detect where the translucent surface intersects with geometry, and then based on that intersection, for example, fades it out so you can get soft edges. But we'll revisit this later. And then lastly, we've got vertex color. We don't have a model here with vertex colors. Let me just quickly connect it so we should add. Connecting that to a misiff. Let's stop the preview there as well. Maybe on. Let's just apply that. Connect to the viewport. If you have a cube here that we had from the previous video. And if I now use the painting tab here, and I'm going to be painting vertex colors. So let's paint some kind of pinkish color here. I can now paint vertex colors on top of the model. I can only do that because this one tells me to get the vertex color that I'm painting and multiply that on top of the texture. Now, since you can do that, you can also do that for two different textures. So here we have broken tiles and non-broken tiles. And they will get blended together based on where we paint vertex color. So it's the same kind of setup I could use that as well. And use vertex colors to switch to a paint broken, basically paint damage into it. Now, I forgot what color it should be. Let's just try a couple of things. You see where I paint black? I started getting broken tiles. If I paint white, I fix the tiles again. So that's not a possibility. So just wrapped up the list of common combinations by taking a look at bump offset and how it creates a simple parallax effect, the normal map blending, and how to change the intensity of a normal map within the Material Editor and vertex color.