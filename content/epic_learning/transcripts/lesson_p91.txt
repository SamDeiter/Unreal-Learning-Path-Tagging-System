Hello and welcome back in this second video in this series we're going to take a look into what happens before the rendering takes place in particular the occlusion process. Let's switch back to my view. Right so before it renders and as mentioned in the intro we have the CPU we actually have a turk 1 2 which is the draw thread. So we have the CPU thread we have a draw thread which is technically mostly on the CPU and we have the GPU thread. They are in sync but here's the thing they're not at the same point in time. Okay so while they move forward at the same you know the same speed when the CPU starts the CPU is one that starts because the CPU has to figure out things that happen before the rendering takes in so we'll get back to this in a second. The CPU will then start processing everything it needs to know about frame A. When it's done with that it will move frame A to the draw thread on the CPU as well which will calculate further things. In the meantime the CPU has started on frame B. So they are in sync they are moving forward at the same intervals at the same speed but by the time the draw thread is done with frame A it gets passed on to the GPU which now starts the rendering. Here's the actual rendering. If you have 30 frames per second that means that actual rendering started 66 milliseconds after the CPU started calculations. That's a latency of 66 milliseconds. Okay so this is a very important thing. It's very important to A understand the CPU is actually ahead in time before what's rendered and secondly they are in sync. So before we can render anything we first need to know where everything is. That's so principal problem. We don't know what we render. We don't know where everything is. We need to know the positions because the positions of everything are changing all the time. It's real time. The viewer could be doing anything at any point in time so you don't know where things will be. That means we need to calculate all logic and all transforms. Now this is a single slide here that I made on this subject. You can talk an entire day just about this because there's a huge amount of work that the CPU is doing to calculate all of this. This is highly complicated but in short it has to simply know where is everything and in order to know where is everything it needs to figure out the animations because the act of animating something will in turn tell the CPU where something ends up. So it's calculating the animations. It's in generally simply calculating the positions of models and objects through who. It's calculating the physics because again the physics determine where something goes. So in order to figure out where something goes we first need to calculate the physics. Likewise for artificial intelligence if there is for example a car driving over the street and the car is AI driven then we need to know how that is responding how it is thinking where it wants to go because that in turn determines where the car will actually be and we need to handle spawning and destroying so things can appear or suddenly disappear from the world. They could be hidden they could be unhidden while things happen. So we need to handle all of that and again this is a very complicated process I grouped in a single slide here because it's about rendering and not a logic that happens behind the scenes but this is what the CPU does do in a very beginning. And therefore the result is I mean now know all the transforms of all the models we simply know model A is a dislocation model B the dislocation model C the dislocation so you know where everything is but it still doesn't really help us. It now gets moved on to frame one so if you go with couple frames up again we just did this we're now here it gets moved on to the draw thread because we have a next problem and that next problem is that we don't know what we can actually see. We don't know what to include in the rendering right we know where everything is but you can't just render everything that would be too slow so we only want to render only what is actually needed only what's actually visible on the image on the frame we're trying to output. Now it's mostly done on CPU some parts of GPU handled as well for the sake of simplicity let's just say it's CPU it's actually more complicated than that that goes in general for most things in this series of videos a lot of things are a lot more complicated than what they are looking you know than how they're presented here but for the sake of making you understand it and you know giving you the overview or trying to keep things simple left and right. So that means that we have an occlusion process the occlusion process builds up a list of all visible models and objects and this happens per object not per polar so we simply figure out can I see this model no can I see that model yes can I see this model no this so you build up a list these and these and these are visible these and these are these are not visible. This is done in four steps you know when we talked in the intro about using a mix of solutions here's the first mix of solutions we're doing four different systems to determine the entire occlusion process. Four different systems each of them good at a particular thing and we're doing four different systems because we can't come up with a single system that is as performance as performance efficient as this mix of four right here. It's done in this order so the first step that we do is distance culling then we'll do frustrum culling pre-computer visibility and finally occlusion culling which is the most accurate one and the one that usually handles most of the work. I'm going to step through these four and as with most things in these videos I'm going to bring up the editor continuously to show you how it's actually happening in editor so let's start with distance culling. Distance culling simply removes any object further than X from the camera. I show you that in the editor. So here we have the example project which is basically it's a it's a some temple example project was originally built for mobile and I'm using it here because it's fairly it's a small area but has outdoor and indoor environments. So distance culling is this the fact that everything you see there is just disappearing because it does a simple check is it further than X if yes don't render don't include in the renderer for the next frame. Very simple to set up because in any object you've got this property here this goes for any visible object not just models goes for particles too that says in the section rendering LOD and it has here current max draw distance it's grayed out I'll get back to it in a second and it also has desired max draw distance if you type zero it doesn't use it if you type 500 for example object with a spear at five and units. Let's see the fact that the other one here is grayed out so it uses both of these desired max draw distance and current max draw distance the grayed out one is controlled by a cold distance volume if you can find it back here a cold distance volume this volume simply assigns settings to everything within itself okay so in rather than having to set this number for each and every object the box the volume simply sets to everything that's within the box gets a certain number set as you see here in the properties and that's it there's height model if too far away the step one step two is frustrum calling and by the way distance scrolling is not set by default okay so you have to set this up if you don't set it up it does not happen frustrum calling does happen this happens all the time in fact I don't think you can even turn it off so frustrum calling is hard to display I'm going to show you a video this is unengine tree in unengine tree was easy to display it it's just a random environment and you can clearly see that on the left this is the viewport as you see this is the image the viewer would see but on the right side we see that we see the frustrum which is basically what the camera sees and we also clearly see that occlusion happens per object it hides object per object not polygon per polygon or other other things like that and the frustrum calling basically means that it checks what the camera can see and everything that's outside the camera's view is automatically removed that's it so much you can do about it happens all the time and it can't turn off then the third one is not enabled by default either it's pre-computed visibility so back to unreal here so pre-computed visibility can be enabled by in world settings enabling pre-computed visibility here and then you need to have a pre-computed visibility volume you can see here there's a box and that box covers the entire playable area everything within that box is going to be populated by pre-computed visibility cells blue boxes if you want to call like that and those boxes are generated by the light building okay so make sure to rebuild lighting we can visualize them it show and then visualize and pre-computed visibility cells right here we have this the size of the cells can be set in world properties the height of the cells by the way can be set in the light mass i and i config file now we have this and you can see here each of these cells remembers if you're here this is what can be seen this one not can cannot be seen so as the camera moves through the environment you enter a different cell so now we enter the cell here the camera then asks the cell okay so what should be occluded what should I be rendering here and what should I not be rendering and the engine will then say it back okay well actually we remembered that at this location this and this and this bit is visible and this and this is this not visible so each cell simply remembers a list of things that could be seen from this position and that's it and you simply query that information as you move through and then last is the actual occlusion calling this accurately checks the visibility state on every single model and because it accurately accurately checks it on every single model it is heavy you can show it at first before going deeper into that to remove these cells again look if we position the camera right here behind the tip of the the wing on the statue now there's a command the command is called freeze rendering which is a bit of a bad command it's not even suggested you can see there's not doesn't even pop up as a command suggestion and freeze rendering as a name is not entirely correct because we're actually freezing occlusion not rendering but we'll if you enter it you will get rendering frozen and now you will see the scene without this process updating so basically it's entire rendering this entire draw thread it's no longer updating everything else is still going on but the draw thread is no longer updating and therefore you can see what it did the moment you entered the command so you can see that if the camera would have been positioned here this is what it included in the renderer at that state at that point in time and that's fairly accurate we can see behind us everything's gone the pillar is logical and explainable and that one too because we did somewhat still see that right things like the sky will almost always render because they're so big and that also explains the rock here because it's so big it always sends a render and also that explains the landscape which is partially rendering so very big things tend to render all the time or most of the time and it's very accurate because it even does it on the tip of a wing you see you can unfreeze rendering and we're back here again this is a heavy the heavy part of it the reason why it's lost if you go back a couple of slides off is because we're trying to offload performance to the easiest thing look checking the distance on things that's very easy it's very easy to calculate so we're doing the cheapest calculation first let's say there's 10,000 objects let's say that the cheapest calculation removes 3000 objects out of those 10,000 now we have 7,000 objects left to consider for rendering out of those 7,000 we then use TrustroomCurling which is also very cheap to remove even more now let's say that we only have 3000 objects left after TrustroomCurling those 3000 are moved on to step three pre-computed visibility then removes say another 1000 models out of those 3000 and then occlusionCurling removes say 1000 more and eventually we figure out we have 1000 models in view and that's what we're rendering if you would have done all 10,000 models using occlusionCurling that would have been relatively heavy not super heavy but it is more expensive than doing it like this that's more complicated to check so basically when we render things and we're in engine and the camera is positioned here for example it has to check on every single model in the environment can I see you okay can I see you can I see you can I see you can I see you including what's behind it so it also has that can I see you can I see you can I see you can I see so every single model is going to be asked should I render this yes no should I render this yes no should I render this yes no and that's going to take time that is why the fourth one occlusion calling it having now freeze rendering you've seen you can also do start in it views. There's a command called start in it views, in it from initialize views and that will give you the overview of the occlusion. Edit views initialize views is the occlusion process. You can see the expense here, how much it costs view visibility the top one here and you can also see how money you're occluding right so process premises it has to consider 809 objects in this angle if I move for example it will eventually now it's 8035 809 objects in this angle. Out of that it's removing 339 because of the thrust from calling right and you can see the rest too for example statically occluded primitives is 55 those blue cells they removed 55 more objects and so on. So you see the breakdown here how it's dividing the work up. As mentioned here if you have more than 10,000 objects it starts to have an impact it could have an impact it depends on the computer on the hardware it depends on a lot of different factors but it could have an impact. If you have more than 3040 50 60,000 or something like that it's probably really going to have an impact so it doesn't even matter if you see all of them or not just the fact that they're in the world at the same time still gives an overhead because it still has to check if it's if it is in view or not. Here's what to keep in mind every time you have a slide like this with this icon at the top this is what you should keep in mind for day to day usage and working. First of all you want to set up distance calling it's not set up by default it's very easy to set up so you always want to set up distance calling especially for outdoor environments but you even want to set it up for indoor environments where you can never see far it doesn't even matter if you can't see far we still have to calculate the occlusion for all the objects for example if you make a large apartment building you can never see far within the building but it still has to check every object in the entire apartment building and considered for rendering so if you can just distance call most of that you will never see a difference but it'll run better. Secondly more than 10,000, 15,000 objects per world per environment might have an impact start being careful and start taking into account if you have more than that many objects at least have it in the back of your mind it's possibility this is mostly CPU bound could have a GPU impactor um large open environments will not accluth well because we simply see everything we can't hide objects if you can see everything right we can't accluth them if everything is in view so take that into account as well if you want to build a large open environment there's a good probability it might run slow because the occlusion can't do much about it you're simply rendering too much so be careful do some tests you know benchmark a profile at anz1 everything that's visible will have occlusion or almost everything that's visible that includes things like particles they also occlude in fact particles occlude by using the bounding box and the bounding boxes updated a few times per second and the updating of the bounding box in itself could also be slow i'm not going to show them just pointing out for people a bit more experience with Unreal look into bounding boxes of particles those could be optimized further um six is that large models will rally occlude and thus increase GPU but also seven combining models tends to lower the CPU that's an interesting one because before we go there let me show you that's briefly and we're going to get back to that later on so i'm only going to show it briefly and then when we get to geometry rendering we're going to revisit this issue because this impacts both things look you might say now okay so having many objects makes it slow because it has to consider every object for occlusion then why was this built with a lot of tiny little objects because now it has to do more occlusion checks and it is correct now it has to do more occlusion checks why didn't we just build one giant room when you want model for the entire room and that's exactly what i did here so here's part of the room at least has a single model and finally loads and it's it's better in some way but it's definitely not uh better and always right i'm going to place it next to it and see it's the entire room or most of the room as a single object the problem is though that if we would be located here and if you see even a single spixel of that model you might not be able to see it but it's an extremely small little corner here on the bottom right if we freeze rendering on this and we look back the whole thing is still rendered that is also why the sky didn't occlude why the landscape there doesn't remove itself if you can see even the smallest bit of it it's going to still render that means that if you do very large objects yes there is less work to occlude them because here a few or things to check to occlusion for but on the other hand there is more to render because now there's more likely that you have to render this at all times if you're here and you look this way it's not rendering what's behind me but if i be here and i look this way it is still rendering the whole entire room behind me so there's that balance and see this the first time we're hitting this issue where it's a consideration between all balances you come to real time rendering perfect neither solution is actually really correct they both have a problem but so what we're trying to do is we're trying to find a balance between both issues and both challenges and the compromise between the two is usually what we end up doing this is not correct neither is this really perfect either again we'll revisit this when we get to geometry rendering because it also massively impacts that as well okay so the result in any case now unlection 4 has a list of models to render it knows for example a b and d are visible c and e are not visible this is then passed on to the GPU and now we start the actual rendering process