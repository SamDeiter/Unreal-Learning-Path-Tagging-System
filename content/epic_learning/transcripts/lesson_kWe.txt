I want to take a moment and talk about something that's pretty important called project settings. We can find our project settings under the edit, project settings, but also under the settings in our tool bar project settings. And if we select project settings, we get a pretty large dialog box that gives us a tremendous amount of power and unreal engine. Now, we see here on the left side, there are a lot of selections we can make, but I want to talk about a couple of key selected elements here, a couple of key menu items that are really important as we understand the fundamentals of unreal engine. The very first one is the maps and modes. If I click on maps and modes, we can see that we are instantly taken to our project maps and modes panel area that defines a couple things that I wanted to point out. The very first is the default game modes and the default game mode selection. Now, we may have touched on this a little bit earlier, but this is where we select what game mode is going to be played. If we create an executable of this particular project we're working with. So in the case here, we are only dealing with default game modes. So if we were to cook this out and turn it into an executable, we would only have the default game modes and the default pawn and the default controller. In a moment, we'll bring in a template, a third person template, and we'll see how we can select a different game mode and cook out our third person pawn. Additionally, the section below deals with the default maps that are chosen when we create an executable or when we start the editor. The very first one is which map, which level loads when we first launch the editor. If you wanted to select a very specific map, we can choose it here. For instance, right now we load the initial table and chairs from our minimal default map, but I can easily change that to the starter map which we see here just by selecting that as the editor loads. If I wanted to create an experience with an executable that launches this map as well, I can choose that in the next section here. Instead of loading a game through an executable that launches the initial tables and chairs, I can also switch to the starter map. And now if I were to create an executable from this experience, they would by default load this map this level instead. Now this is really important because a common error that new users to Unreal Engine make is forgetting to choose this default map when they create an executable. If for instance you were to load a player start or some kind of a menu map that you would create where you would choose your initial experiences, this is where you would load that as well. The next important section to consider as we're beginning to learn Unreal Engine is this input section as well. This input section has an input binding section, which is something we need to learn at the very beginning, which is creates our action mappings and our access mappings. And as you can see here from the description, the action and access mappings provide a mechanism to conveniently map the keys from our input devices to Unreal Engine. That could be anything from a mouse and keyboard, a game controller, a VR controller, or even a mobile device controller like a touch screen. And you can create those just by clicking on the plus key next to the action mapping or access mapping, which immediately gives us the ability to create something like a jump binding, and then assign it to any one of these types of controller inputs like a keyboard that we can then map to a spacebar or add multiple mappings, like also a gamepad, for instance, and we can use the special right or left stick to map that as well. And we can also assign that to Shift, Control, Command, or whatever we choose to. And then we can also create an access mapping and an access mapping. Maybe we can look up and you can see that that mapping is actually bound to a scalar value. And if we choose to use mouse, X or mouse Y to look up, we can invert that value as well. Now, we can also be aware that there are advanced values here that allow us to configure these controllers and give ourselves much more refined tuning so we can tune dead zones and sensitivity and invert those values. And we have a lot of control of how this is all managed. I'm going to do something really quickly. I'm going to jump out of my project settings and I'm going to go ahead and bring in a template. And I can do that by coming down to the green button down here in my content browser, adding a feature content, which I'll bring in a blueprint template, like the third person template. And one of the things that comes in with these templates is those input binding. So by creating this project template and closing this, and I'm going to go ahead and open up and show my source panel, you can see that now I've got a third person blueprint, which has a character, third person blueprint associated to it. So now if I navigate back to my project settings and also navigate back to my input bindings, you can see that all of this has already been brought in with the template. So now I've got to look up, I've got to look right and look forward. And those are already mapped to keyboard, to gamepad bindings and all the scalar values have been tuned positive and negative one as appropriate. Now the last menu item that I wanted to point out here is the rendering menu item. And this is a really important set of features inside of the engine, which allow you to tune your reflection captures at the appropriate resolution, tune your texture streaming settings, decide how your materials behave, decide how your anti-aliasing and post processing behaves, tune your lighting settings, translucency settings, also your bloom and default auto exposure. This can be a really good place to familiarize yourself with because Unreal Engine has multiple renders that are available to it, as well as this is where we can also find things like ray tracing features. And because there are so many menu items available here in the entirety of our project settings, sometimes the best things to do is to just search here. For instance, if I wanted to turn on something like ray tracing, maybe I'll just type in ray tracing or to start typing ray and see that here in the engine rendering section is where I would enable ray tracing. So by clicking on this and being aware that it gives me a tool tip of what I need to do, which is to also turn on supported compute skin cache, is where I enable features like ray tracing. So that is a very quick brief overview of our project settings, but those three initial ones, the game modes and maps, input by knees and some of the rendering settings are really good things to initially familiarize yourself with. Take a look as you become more familiar with Unreal Engine here. You've got lots of settings for your editor. You've got lots of settings for general AI animation, LOD generation, other general settings for management of cooking and packaging your project, and how things generally behave inside of a project.