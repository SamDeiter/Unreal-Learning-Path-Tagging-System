In this module, we will have a look at macro libraries. They aren't as performance-friendly as function libraries, but offer more flexibility in terms of what nodes you can call. Multiple execution inputs, no problem, a delay, no problem. The main difference in terms of performance when it comes down to function and macro libraries is the fact that a macro is basically the nodes inside copy-pasted. We have a bunch of nodes in a certain macro. Once you reference that macro somewhere else, you're not really referencing any logic, you're just copy-pasting that code over there. A function is more of a reference. There's a few lines of code that have been written that execute this function, but they're in a different place and they're executed over there. This means that with a macro, it's copying-pasted, function is a reference. In general, it's not that big of a deal, it's not a huge issue, but it's something to keep in mind. If you use Blueprints with a lot of macros instead of functions, you'll notice that your Blueprint file sizes are bigger and that your compile times will be longer. This becomes more noticeable and important as you start working on BIGA project, but this does not mean that you should be afraid of using macro libraries. With that said, let's get into it. We go to Blueprints, Target, and open up our BP Target spawner. You can see a for loop here. Now, why am I doing this? This is actually a macro that is included when you download Unreal. When you open it up, you can see a lot of macros over here. This is a macro library with standard macros used by Unreal. You can have a look at the for loop, flip-flop. There's a lot of different macros that you can find in here. I'll have a look at to learn a little bit more about macros. Now let us create our own macro libraries. We'll go back to Libraries and let's go to Blueprints, Macro library, and here we have to pick a parent class. Now this is important because this will determine where you can call your macro. We could do Object, which means we'll be able to use it in anything, or we can pick Actor, which means we'll be able to call it in any of the children from the Actor. I like to go with Actor and then have a separate one for widgets, but you can also go with Object and then it will be available both in Actors and in Widgets. And then call that BPML Comparisons. Open it up and you'll see that we already have a macro set up there. I'm going to do int, smaller, equal to int. You may probably think to yourself that we already have this, but I want one that gives me two execution output notes. So I have to add those ourselves as well. I have to look for exec. There we go. Now we have an execution pin. Call this in and we'll add two on the output. One being true, the other being false. But it's already an example, something that you can't do in a function, but you can do in a macro. You cannot have two execution pins in a function. Here we'll just add a branch. True to true, false to false. And here we will check in smaller, equal, another integer. Which is this one. VLU-A, VLU-B. This is fine. And this can be our macro, very simple one, but might be useful down the line. We will be able to use this. The nice thing about macro libraries is that macros, you can give a nice little color. We can make a pink or green. So you can make all your calculation related macros. You can make those green. And now when we go into the level blueprints, look for int smaller, equal. We can see, here we go, can compare VLU-A to be is a smaller or equal to B through or false. We created our first macro library that can be called in any child of the actor class. In the next module, we'll start working on our weapon by creating a parent weapon.