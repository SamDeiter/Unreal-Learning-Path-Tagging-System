We are going to be doing yet another example where we migrate the first person character again. All stuff that you have already covered in previous modules, we are just essentially doing a recap with a real world example. So here we've already started migrating the first person character, but we're going to finish off the job and get everything that's in this event graph over in 2c++. Now the sticking points that I foresee are going to be getting hold of the grabber, because that means that we would have to bring over everything here that we have currently got in the first person character and doing the camera and the grabber as a sub object. And everything there is all going to have to go over in 2c++ unless we expose this as a blueprint implementable event. So that's what we're going to do. I'm going to go over into the header file, and we're going to create a protected function. I'll do this under begin play, which is going to be a U function that is a blueprint implementable event. And it is going to be blueprint pure. And then we want it to return is the U grabber, which is a class that we already have. So let's make sure we include it. Get includes just above the generator dot h. I can't stress that enough. Hash include is going to be to the grabber dot h. So then we can include the type there, U grabber, asterisk, and we're going to call it get grabber funnily enough. And it is going to be const. So we don't need to create an implementation over in c++. That's just compile and make sure we have that implementation in blueprint. So let's go over to the blueprint first person character. Let's dock that tab. It looks like it wasn't docked. So then in functions, I'm going to hit the override drop down and look for our get grabber function. And this is where I'm going to just return the reference to the grabber component that we have instantiated here on our first person character. So now we can get hold of that grabber in a c++. So let's go over to the c++ and have a look at how we're going to hook up this functionality. So first of all, our forward implementation. This should be fairly straightforward. So let's go over to the event graph and have a look at what we have disconnected here. So first of all, we're getting the forward vector of the actor that is pretty much going to be the same and get forward vector get actor forward vector is the function on the current actor. Then we were multiplying it by the axis value. That should again be fairly straightforward, asterisk axis value. Then we are getting the character movement component, which is actually still available here character movement. All we have to do is call the function get character movement. And then we need to have the correct include, which is going to be up at the top. We're going to hash include the game framework forward slash and it's going to be the character movement component. So that allows us to call methods on the character movement component. And what we want is the add input vector. So that's what we're going to call add input vector. Like so. And the thing that we're going to add is the multiplication of the forward vector with the axis value. And that should give us what we need in here. Now I'm just going to go and fast forward through the process of doing the almost exact same thing, but with the right vector for the axis right. And as you can see, that was pretty much a copy and paste and replacing forward with right in a few places. So let's build that and see whether we can move around, go to blue print and delete all the stuff at the top. That is about the forward and the right axis compile save, hit play and see if we can move around yet we can move forward and we can move left and right. So we have successfully moved those across. Next, let's have a look at look up and look right. Now in this case, we don't actually have to create a function because there is already a function in one of the parent classes, the add controller pitch input and the add controller your input. So we're just going to go ahead and bind those directly. It's just going to be a bind axis call play input component bind axis and we're going to do the text macro for as we said over here is the look up is the first one calling on this still this particular instance and then we're going to do the ampersand a porn because that is the class it is on. If you go and have a look over in blue print, you can see the target is porn and then we're going to do colon colon the add controller and this case it is the add controller pitch input which should hook up just fine like that and then we are going to go ahead and do the look right and this one is adding the controller your input instead. Now if you wanted to do anything fancy, of course you would put some function in between that we implement here locally. For singers, we don't need to do anything fancy. This should be fine. Let's go ahead and build and see the result. So after we have built, we can go and delete our look up and look right events in a blueprint and we can go over to the main map hit play and check that the mouse is able to have us look around which it is. So the next thing in our sites is the jump function which again is a function of parent class. In this case, it's the character parent class. So we can go ahead and instead of calling to this jump, we're going to call to a character and in this case, it's actually still called jump and what we want to do is remove the private jump function that we've got on the first person character because that is bogus and we don't need it. Now let's go ahead and build and see what the result actually looks like. Again, we're going to be deleting this jump, extra extraneous stuff in the blueprint, go and hit play, hit spacebar and see that we are able to jump. So the final target in our sites is this grab a grab and release. Now we're going to have to create a function for grab and release both locally because it will not work if you try and bind to something that isn't this, that is something dynamic in here. So we're going to go ahead and create another bind action. So I'm just going to copy this line and we're going to do text. The name of the action is grab and we're going to create two of these. One of these is going to be for the input pressed. Again, its target is this and then the function we haven't yet created. And then the second one is going to be released instead. Let's go over to the header file to create these. We're going to have a void taking no arguments grab function and we're going to have a void release again taking no arguments. Go back to the C++. In fact, we want to implement both of those. So let's copy their signatures. So let's create their definitions and then we go over to our C++ again and we're going to go ahead and bind those. So amp's and grab and then we're going to do the same amp's and release and get our semicolons and our indentation fixed and we should have the bindings that we need. The only thing that remains is for us to actually call that function on the grab. So we're going to have to call to get grab in here arrow and then we want to call the grab function on the get grab. Not forget my parentheses there. Now we haven't actually implemented grab or release over in the grab. But it is relatively straightforward to do seeing as we've already brought across most of the functions. So let's go ahead and do it super speedily. We're going to have a U function that is going to be a blueprint callable over here at the top and it is going to be a void grab function and then we're going to repeat it having U function blueprint callable which is going to be a void release instead and we're going to go ahead and implement both of these over in the C++. Then let's go over into the C++ body and see what the result is and what we're going to actually implement. Now I'm just going to go through this really quickly but you can compare this result against the blueprint to make yourself sure that we're doing the right thing. So first of all we're going to have a variable to store the hit actor as an out parameter and we're going to also have a U primitive component that we store the hit components into. We're then going to do a trace so it's going to be trace for physics bodies. Hit actor is going to be one out parameter and then the hit component is going to be the other. Then in the body of the if so if we do hit anything we're going to call on the hit components what are we going to call we are going to call the set simulate physics and we're going to set that to true as a first port of call. Then we are going to get the physics component of this wrapper and we're going to tell it to grab the component at location with rotation and what are we going to tell it to grab. We're going to tell it to grab the hit component. We're going to give it name none because this is for the bone that we want to attach to. We've got no bones here because we're static meshes. Then we're going to do the hit component and we're going to get the center of mass which is where we should grab it. This is really just translating the blueprint after all and then we're going to set a kind of null rotation with passing in a empty construction of an f rotator and then semicolon at the end of that line we're going to then notify the quest actor with the hit actor that was hit by our grabber. The implementation of release is fast. In fact we're just going to do the get physics component and then we're going to release that component. So now we should be able to go back to the first person character CPP and call those grab and release functions. So again I'm going to do the release is going to be fairly straightforward get grabber and then we call release on that. Let's build and see what the result is. We can see that we now need to delete the grab and release functions in the grabber blueprint and everything in the first person character compiles are just fine and we can go and test it out in the scene. So we're going to do that by right clicking in the center of the village and doing a play from here then going up to the pumpkins and trying our press. Yep we've got a pumpkin so the grabbing mechanism is working and then a release and all of that is going via a binding in C++ and then the grab function that we've implemented in C++ as well. That's been the result of this module. Hopefully you've been able to follow along. It's been rather a whistle stop tour but that should have helped you to solidify a bunch of knowledge about moving things over in to C++.