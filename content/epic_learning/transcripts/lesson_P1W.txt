In this video, we will set about using the Vector and Float variables that we have already created, so as to be able to adjust the box component that we added to our tools visual component. Now to get started, we will want to left-click and drag from the automatically created construction script node that we see here, and then release the mouse button somewhere in the graph area. From the list that pops up, let's search for and add the set box extents node, which should automatically add a reference to our radius visualization component. If it doesn't, we can drag it from the components list and then pipe it into the newly created node's target input. Now this node is what we will use to change the x, y and z scale of a target object, which, as we just stated, is our spawn area component. At this moment in time, nothing different will happen until we take the vector variable that we set up earlier, and then click and drag to pipe it into the in box event input. Now once connected, we can test our script to see if it is working in the way that we expect. And so after clicking on both the compile and save buttons if needed, we can locate our advanced scatter blueprint in the content browser, left click and drag that into our scene. And what we should see is that the perfectly squared box trigger component that we made earlier is no longer perfectly square. And so with the tool selected, let's come over to the details area where we should see the radius option that we made public. Let's adjust that then and make sure that the scale of the spawn area changes accordingly. Now we're probably also good times to make sure that all the other public variables are showing up as well. And though we will be making use of these later in the course, we should currently see them with just the defaults that we set up earlier. We just open up the blueprint again and select all of the nodes that we have created over the last few videos. We can hit the C key to separate them out with a frame, call it spawn area size control and then press return. In fact, from this point on, once we finish a section of code, I will also separate it out as we have done here. Only I will do this off capture so that you don't need to watch me do the same housekeeping work time after time. Now that we know our first bit of blueprint code is working as intended, we are ready to start tackling the more difficult aspects of the blueprint flow.