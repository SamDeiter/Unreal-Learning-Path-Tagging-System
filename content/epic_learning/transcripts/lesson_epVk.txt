Hi everyone, I'm Degu-Ri and I'll be guiding you through this gameplay ability system course. This is the fourth session. Today we'll continue building the gameplay loop by adding functionality using gameplay events. Here are the learning objectives for this session. Implement a gameplay ability that triggers another gameplay ability from an animation notify. Understand the components and sequence over standard gas interaction work flow. Implement the custom hit detection logic for specific actors. Before we begin, I'll show you the result of what we will be working on today. In this session, we use the structure provided by gameplay ability to get information about the actor that was hit. These are key components we cover in this session. Building or what we covered in the last session with the ability tasks, we will now move on to gameplay events. Today's feature completes the role of the gameplay ability within the gas loop. With this session, the gameplay ability is finally complete. To fully complete the alt-axis system we built last time, we need to get information about the actor who was it. While you could implement it manually, gas provides a consistent structure for this using something called a target actor. A target actor is a special actor used to play the gameplay ability targeting. When targeting begins, the ability spawns a target actor to perform a target action that fits the current ability. The target actor processes the result into a format called target data and returns it. When real engine provides several built-in types of target actors for different targeting methods, targeting in gas is generally handled in two ways, instant targeting and user-confirmed targeting. Instant targeting returns a result immediately in the current frame, user-confirmed targeting on the other hand, which for input, like throwing a grenade. For this type, there is also a helper actor called a electrical actor to help with visual feedback. The blue circle in the image on the right is an example of a electrical actor. Now let's look at how a target actor is actually used in a gameplay ability. You can use a target actor by running the weight for target actor ability task. When the task runs, it spawns the target actor and calls its strategy targeting function. If it's an instant type target actor, confirm targeting runs immediately, then confirm targeting continues, finalizes the targeting and passes the data back. For user-confirmed types, it may spawn a radical actor, then calls either confirm or cancer targeting based on input. If confirm targeting is called, confirm targeting and continue follows. Once targeting is complete, the result is passed to the ability task. If confirmed, the data is stored in an app gameplay ability target data structure. This target data can be either a hit result from a raycast or an array of actors. Now let's implement these ourselves by spawning a target actor in a practice project and retrieving the result. For this session, I will prepare the example 3 strategy file in advance. When unziping it, I will rename the folder to example 3. Let's start setting up the project for practice. Right-click the project file while holding SHIFT and choose Generate Visual Studio files to create the solution. Double-click to open the solution. Make sure the start-up project is set to Arena Beta Gas. Then let's build the solution. Once the build is finished, launch the editor. I duplicate the level we created in the last session and saved it as step 3. It's already set as the default level. Today, we'll create an animation notify blueprint to define the exact hit timing for the attack. Go to the character folder in Blueprint. Search for Enem notify. And create a new one called AN hit check using the default class. Open the blueprint. Here, we'll implement two event functions. First, implement get notify name to define the name shown in the editor. Let's just return hit check for simply city. Next, implement the received notify function, which runs when the notify is triggered. For now, let's add a print string node to make sure it's working. Create a print string node and enter AN hit check. Now, we'll apply this animation notify. Open the animation montage and replace the notify at the predefined timing with AN hit check. Click play and see if the hit check appears at the correct moment. Now, we'll create a gameplay ability that detects a hit at the timing. Go to the g a, character folder, and create a new blueprint. Search for gameplay ability and name it BPGA hit check. Instead of triggering this ability with an asset tag, we will set it up to be triggered by an event. In the triggers section, set the trigger source to gameplay event and assign the tag event that character that hit check. To test if it's working, add a print string node inside the ability and enter GA hit check. Since we are signing more default abilities and NPCs will also use this, we'll handle this more efficiently. Open Visual Studio and go to AB base character, the parent class for all characters. We'll declare a variable named initial abilities to hold the list of abilities to assign a startup. Since it's on array, we'll use T array. Inside the array, we'll store class types using T sub class up and you can play a ability. And we'll declare it like this. This lets us set the values anywhere in the editor and change them in blueprint. Now, we'll create a function that assigns these abilities during initialization. Since this isn't a multiplayer network game, we'll do it inside the possessed by function like we did before. In the player character declaration, copy and paste the possessed by function. Then, generate the function body. Inside the function, call the parent logic first. Then, use a range default loop to assign each ability from the initial abilities array to the ASC. When assigning abilities in C++, you need to wrap them in a spec using class info. The spec can also include other values like level and input ID, which we saw in the last lecture. If you look at the declaration of the spec class, you'll see the default values are level 1 and input ID minus 1. So, we'll use the default values and just pass the class info. Now, press Ctrl, Alt, F, and L to compile with live coding. Once it's compiled successfully, the Unreal Editor will launch a game. Go to the character blueprint, BPABPC, and remove the old Gible ability node. Click the gear icon on the variable list and enable show inherited variables. In the gas category, set the initial abilities variable, assign both BPAG-A attack and BPAG-A hit check. Now, both abilities are ready to be used anytime. Go back to the animation notify. Remove the printer string node. Use the send a gameplay event to actor node to send the event. Set the tag to event that character that hit check. Since the notify only has a reference to the mesh component, use get owner to retrieve the actor. In BPABPC, change the logic so the input triggers using the tag. Press play and test it. You should see the attack animation, and the text should now say, G-A hit check. Now, let's move on to the main topic of this session, detecting the NPC using a target actor. Open BPAHit check and use the weight target data or ability task. This task will spawn a target actor. Name the task instance, hit check task. Set the target data type to instant. Now, we need to assign a target actor class. Since the built-in class is, don't shoot our game. We create one from scratch. Click tools, new C++ class, to create a target actor. Search for target actor, and set up parent to the gameplay ability target actor. Name the class, AB target actor, SPIRT race, and create it. Once it's created, go back to Visual Studio. Click reload, to check that the class was added. You'll see the new class there. For this instant type target actor, we'll implement a constructor and two virtual functions. In the parent class, you will find a function named start targeting. Declare start targeting. If you open the parent class, you find a function called start targeting. And right after that, we'll also declare the confirm targeting and continue function. Generate the function bodies. For now, there's nothing to do in the constructor, so we'll skip it. In start targeting, call the parent logic first. We'll use this function to assign the source actor member variable for our trace logic. Get the avatar actor from the ability, and set it as the source actor. Then in confirm targeting and continue, we'll handle the actual detection. The parent implementation, just send empty data. Since we'll return real data, we won't call super. Instead, copy and paste the logic. Now, we need to pass in the actual trace result, so let's do that in blueprint. We'll call it getsPiatraceResert, and we'll return an app-heat result from the source actor. Now, go to the definition. Now, get помpmmt. Go to the definition. We'll get the trace unjust using the function. Confirm attractions, dec redesigning, creating such subsea. We'll get the trace results using the function. Convert the result to single target data and pass it to the target handle. Then, broadcast the delegate with the target handle to finish the task. Press Ctrl, Alt, F, 11 to compile with live coding and test it. Go to the Blueprint character folder, search for A, B, target, and create a Blueprint based on our new C++ class. In this Blueprint, we'll implement the GetSpearTraceReserves function. We'll build our detection logic using the SpearTrace by channel node. Set the Actors position as the trace start. Add 100 units in the forward direction to set the trace end. Set the Radius to 50 for the trace channel, use A, B, action. Set the Radius to 50 for the trace channel, use A, B, action. This channel only responds to the capture profile, I set up a player and NPC. Here is a quick explanation. Go to Project Settings. Open the Collision section. You'll see the Custom Trace channel named A, B, action. Scroll down the preset and check the custom one named A, B, capture. It's set to ignore everything by default. There, A, B, action is set to block, meaning it will respond to it. Go to the Character's Capture component and check the collision preset. It should be set to A, B, capture. Now, go back to TA, SpearTrace. And then, we need to specify which actors to ignore. Of course, we want to ignore the Source Actor. So, we exclude the Source Actor from the trace. Next, we can choose whether to disable the debug drawing or enable it for a short period. Among the inherited variables, under the Targeting section, there is a Debog setting. Using the Debog value, we can plug it into Select node. If it's false, we show nothing. If it's true, we show it for set duration. Finally, we return the trace reserved directly. And that completes the setup for our target actor. Go back to BPGA hit check to wrap it up. In the Weight target data task, set the class to TA SpearTrace. If cancered is triggered, use end-obility to finish the ability. Connect the pin from data to a target data as actor node. If the data doesn't have an actor, just the code end-obility. If it does, get the actor from the target data. Since we only did one, use index zero. Get the actor's display name. Combine it with the text, space, got hit, and show it using print string. Then call end-obility to finish. Click play to test. If you swing the sword in the air and nothing is detected, use the red capsule. If you hit on NPC, it will show a green capsule and display NPC got hit. Once you are done testing in the editor, switch to Visual Studio and do a final build. Now, we've implemented an attack action that detects enemies using gameplay ability. With this setup, we can start interacting with the detected target in the next session. That's it for today's practice. Let's go over what we've learned in this session. Custom target actors must be implemented in C++ to suit specific gameplay needs. Target confirmation can be handled through immediate wake-ass detection, or the user-compomethod which waits for player input. A visual helper called the reticle actor is usually in conjunction with the user-confirm for input-based targeting. Once a target is acquired through a target actor, ASC can be applied to enable further interaction with that target. Try using the provided step 3 level to practice implementing different gameplay abilities with target actors. That wraps up today's session. I hope you're looking forward to the next one as well. I'm Dukuri. Thanks for watching. Bye-bye.