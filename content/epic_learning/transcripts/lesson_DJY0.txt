Our main area of polish will be to smooth out the transition between the two states we've already loosely defined for the AI. Those are patrolling and chasing. We'll begin this work within the AI controller, so navigate to content, 2D size crawler BP, Blueprints, Enemies and open the enemy AI Blueprint. Within the event graph, locate the do behavior custom event. We're going to be adding a bunch of related logic in this area, so let's move the whole thing into its own space and create a comment box to visually separate it from the rest of the graph. Markey select everything that is connected to the do behavior custom event and move it down into some clear space. With all the nodes still selected, press C on the keyboard to create a comment box. I suggest adding the comment behavior to define this section. Extend the top of this comment box to give us a little bit of empty space to add some new nodes. Let's first create an event to run when the AI is starting a patrol. That is to say, moving around without any awareness of the player. Right-click into an empty space on the graph and locate custom event. Name this new custom event start patrol. Within the My Blueprint panel, create a new variable. Ensured this variable is a Boolean and name it patrolling. Alt left-click drag this new variable into the graph to create a set node. Check the box within the node to set the patrolling variable to true. Connect the execution wire between the start patrol custom event and the set node. When the enemy is patrolling, we don't want them to be running around, so let's ensure that they're set to walking speed. Drag a reference to the enemy character into the graph and locate set is running. Leave the box within this set node unchecked and connect the execution wire to the previous set node. With that taken care of, we'll create another behavior modifier we need, chasing the player. Combined with our previous work on the AI systems, this will make the AI run towards the player to begin an attack if they're too far away. Create a new custom event and call it Begin Chase. We can save some time by copying and pasting the two previous set nodes we used for the start patrol custom event. Uncheck patrolling from the set node and check is running from the other set node. Make sure to connect the execution pin from the Begin Chase custom event into the patrolling set node. Moving on, the patrolling Boolean we just added allows us to fine tune the logic in our nearby do behavior custom event. So let's go ahead and make use of it. Locate the do behavior custom event and let's look at the first branch with the condition on the is alerted state. We want to add some additional logic to be processed following the false outcome of this branch. Knit a dis branch, add another branch by holding B and left clicking in the graph. Control left click drag a reference to the patrolling Boolean and connect it to the condition input of this new branch. Connect the true output of this new branch to the execution pin of the existing and movement input function. Alt left click on the false execution pin of the previous branch to disconnect the wire between it and the add movement function. Finally, connect this false input of the branch into the input of our new patrolling branch. Marquis select all of the nodes attached this new branch and reposition them to keep everything neat and tidy. Now we have added our two behavior modifiers but nothing within the event graph ever calls them so they're a bit useless right now. Let's go ahead and fix that, starting by half the enemy begin the game in the patrol mode. Within our large comment box create a new custom event called initialize behavior. Drag from this new custom event and locate start patrol. We do things this way so that we could easily switch out the initial behavior of our AI as we develop it. Now let's make sure this new custom event is called as part of the AI creation process. Within the event graph locate the event on possess. Marquis select the first three nodes here and move them over to the left a little bit just to give ourselves a little bit more room. To keep everything readable resize the existing comment box to encapsulate these nodes. Drag out from the execution wire of the set node for enemy character and locate initialize behavior adding it this way will insert the new node into the execution flow without disconnecting the following nodes. Now this custom event will be called as the AI controller possesses the AI ensuring that they will start in the patrol behavior. While we're in this area of the graph let's make some adjustments. Let's make use of our new patrolling boolean to fine tune some of our existing bound events. Moving along the execution path locate find event to undetected ledge. Moving to the bound path blocked event we can see that the AI will always just attempt to rotate. That's fine if the AI is patrolling but if they're moving through the rest of our new AI logic while alerted and potentially attacking we actually want them to stop moving and stay where they are. We can use our new patrolling variable to handle both cases. Control left click drag a reference to patrolling into the graph. Hold B and left click within the graph to create a new branch and connect a patrolling variable to the condition input. Move the existing rotate node and the connected enemy character reference to the right a little bit to give us a bit more room. Connects a new branch directly to the path blocked custom event. Note that this disconnects the custom event from the rotate function which is what we want. Now connect the true execution wire from the branch into the rotate node. This means that if the AI is currently patrolling then they are allowed to rotate if the path is blocked. But what about the false path of this branch? If they're not patrolling we actually want them to stop moving. Here's where we can lay the groundwork for a similar Boolean to tweak character movement. Create a new Boolean variable called disable movement this frame. As you've probably guessed we'll use this variable to control disabling of movement. All left click drag a reference to this variable into the graph to place a set node. Ensure that the checkbox is checked because we do wish the disabled movement within this frame. Connect this set node to the false output of the existing branch. Reposition all of the nodes and resize the comment box to keep everything neat and tidy. Let's move on and apply the same Boolean as part of our attack event. Locate bind event to uncompleted attack animation in the graph and find the associated event on attack completed. We can see that at the moment the first thing we do is set on attack cooldown to true. And now we also want to ensure that disable movement this frame is set to false. All left click drag a reference to disable movement this frame into the graph and position it before the existing set node. Drag from the execution pin of on attack completed and connect it to the new set node. Connect the output of this set node to the input of the existing set node. Now when our attack animation is completed our AI will return to normal movement behavior as well as entering the attack cooldown phase. We can also take this opportunity to hook up our recently created begin chase custom event to fire at the appropriate time, which is when the enemy first detects the player. Locate the on enemy sense change custom event. Follow the true execution path from the first branch and locate the last node, clear and invalidate timer by handle. Drag out from the execution pin of this node and locate begin chase. Now this event will fire just as the enemy senses the player, allowing them to start running towards the player in certain situations which we've already configured. With those new changes made to our bound events, we're nearly there. We just need to quickly revisit some of the existing logic to make sure we don't end up in any situation where disabled movement is frame remains true for multiple frames. Let's return to our do behavior custom event and locate the branch that currently uses the is attacking variable to drive the condition. Drag a reference to the disabled movement is frame variable into the graph. Drag from the output of this new variable and locate the not node. Note we're already checking here to see if the AI is not currently attacking. But what we need to do is see the result of both of these Booleans to drive the condition for the branch. So alt-left-click on the condition to disconnect it from the existing not node of the is attacking variable. Now drag out from the not node and locate and. Now we can connect the second not node from our new variable into the lower input for the and node and then connect the output from this node back into the branch condition. This minor tweak will ensure that the AI will now pause movement if either of these conditions is true. Let's move on to do something similar for our core behavior custom events. Start patrol and begin chase. In both cases we do not want movement to be disabled as they are either chasing the player or moving around normally as part of the patrol. Locate the start patrol custom event and then alt-left-click drag the disable movement this frame variable into the event graph nearby to create a set node. Leave the box unchecked thus setting this value to false. Connect this new node between the existing nodes. Repeat this process for the begin chase custom event. That's all we need to do with these custom events leaving just one more place where we need to manage this new variable. As a fullback we'll also ensure that this variable is set to false every tick after we have applied any changes of state from the do behavior event. Locate event tick within the graph. Alt-left-click drag disable movement this frame into the graph and connect the node to the outgoing execution pin of the existing do behavior node. And with that we finish with these tweaks. We can compile, save, close the window and return to the main editor window. Before we start a play and edit the session to review our changes let's make a little tweak to the layout so we can see the enemy use the chase mode. Select the enemy within the viewport and move them over to the right hand side of the platform. With that done start a play and edit a session. We can see that the enemy repeatedly tries to attack us because we are within their detection zone. We can see that the cooldown works properly and they'll fire every three seconds. If we move over to the left hand side of the platform we'll see the chase behavior be initiated and they'll run towards us before attacking again. And that's it. That's a huge step forward for our AI systems. With the enemy behavior now all taken care of it's time to deal with the outcome of this projectile hitting the player. But for now we can end the play and edit the session and return to the editor window ready for the next section. In this module we've taken a huge step forward for our AI systems within the game. We began by configuring a basic AI scent system using event dispatches and overlap events from the AI character. We created a basic alert state within the AI depending upon if the player was currently within their detection range. We then worked towards having a functioning attack state carrying out various checks along the way to decide if it was appropriate for the AI to fire their projectile towards the player. This naturally led us to move on to the logic for firing the projectile. Creating a location on the AI character to spawn the projectile, playing the attack animation at the correct time to give the appearance of summoning the projectile rather than it just appearing out of nowhere. We then spent some time working through all of the common edge cases and dealing with them appropriately including facing the wrong direction and being too far away. Finally we polished up the overall look and feel of the AI by adding tweaks to smooth over the transition between idle patrolling and chasing the player character. We then finished up by reviewing our progress in a play in editor session.