Being able to quickly identify any navigation problems in real time is essential for fixing bugs in any AI system. Unreal Engine provides us with a tool for doing all this and more, called the Gameplay Debugger. In this video, we'll activate the Gameplay Debugger and take a closer look at what useful information it can tell us about the AI navigation system. Before we go any further, we should probably make sure that you can actually activate the debug, which would be useful. If you don't have a US keyboard layout, like myself, there's a good chance that the Gameplay Debugger isn't actually bound to a key by default. Open the project settings window and navigate to Engine and then Gameplay Debugger on the left-hand side. Looking at the input rollout, check the activation key is actually bound. I have a UK keyboard layout and I have bound this to a postraphy as I find that useful. Now if you're also using a similar keyboard layout, a postraphy is previously bound to the console, so you'll need to rebind that to a different key. You can do that now by once again looking on the left-hand side and going to Engine, Input and scrolling down until we see the console rollout. In the console rollout, there'll be a single option and you can change this to whatever you'd like from the drop down. With those changes made, we can now close the project settings window and return to the viewport. Now we can run the game by pressing play in the toolbar or pressing alt-p. With the game running and our AI character happily running around the space, let's activate the Gameplay Debugger by pressing whichever key we bound it to previously. Make sure you're looking towards the AI character as you do this. You'll see this puts a lot of information on screen and a lot of it is constantly updating. To make our lives easier for the moment, let's pause the game by either pressing the pause key on the keyboard or the pause button in the toolbar above the viewport. Let's start by looking in the top right-hand corner of this Debug window. This highlights the debug actor that is currently selected. In this case, it's our only AI character. However, as we built more complex scenes in multiple AI, it's good to be able to confirm which character is actually selected. Note that you can also see an icon above the AI character's head in the world. Let's move over to the left-hand side now and we can see we have cheat sheet reminding us of all the different keys we can press to toggle very functionality within the Gameplay Debugger. We reminded which key we have bound to the Gameplay Debugger itself so that we can toggle it on and off and we can use the numpad to cycle through the various categories. We can press Ctrl until day to toggle the HUD on and off and we can press Ctrl and Tab to toggle the debug messages on and off. Below that, we have the most important features within the Gameplay Debugger, navmesh, AI, behavior tree, EQS and perception. We'll return to the short key. For now, let's unpause the game by pressing the pause key once more and pressing Tab to switch to the Spectator view. You'll notice that in the cheat sheet area, the Spectator text is turned green, indicating it's active. Now we can move the camera around in much the same way as the viewport. I'll reposition the camera to get a good view of the AI moving around the level. Note you can also use the mouse wheel to speed up or slow down the movement rate of the camera just like in the normal editor viewport. If you return once more to the various categories, let's begin by toggling the ones we want on or off. For now, we want to focus on navmesh, so I'll press 0 on the numpad and then I'll press 1 to toggle the AI section off and I'll press 2 to disable the behavior tree section. With just the navmesh section toggled on, we can see the area of the navmesh to the AI is currently using in the moment. And as it moves through the various locations, we can see the different sections of the navmesh become active. Now let's disable the navmesh category by pressing 0 on the numpad and enable the AI category by pressing 1 on the numpad. Once again, I'll pause the game by pressing the pause key, so we can look at the information without it rapidly changing. The AI category gives us a bunch of useful information that we can use to verify things are working as we expect. It begins with the controller name, this refers to the AI controller that the AI is using, and we can see that it is indeed using our AIC underscore third person character controller that we created. We can also see which pawn is being controlled, in this case it's our AI underscore third person character as expected. We can also see which movement mode it's using, which mesh component is currently being used, what nav data it's using, and the current status of its path following. Note the sections below will be explored later. In this video, we took off first look at the gameplay debugger. We learned how to activate it and how it can be used to monitor the AI's actions in real time. Finally, we selected the navmesh category using the numpad and observed the information it provided.