Our next step in optimization will be to learn some tips for handling static meshes and LODs. We have built a new blueprint that will assist us in seeing all of this in action. Using an Editor Utility widget, we'll be able to replicate a task over many assets at once. We will end this module by merging the architectural meshes into a single larger one to lessen the number of draw calls required to render it. Each of the decisions we make regarding what goes into the project and how will have its own cost to performance. When we brought in the project assets at the beginning, the decision at the time was to get started with developing an experience for the HoloLens 2 as quickly as possible. Similar to the process of creating a project and getting a little too far down the road before our first optimization effort, we must now look closer at the actors and the blueprints that make up our scenes. Let's get started by quickly learning about LODs. Starting with them will help us see the difference between static meshes, instant static meshes, and hierarchical static meshes. We'll begin inside of the main map. Select one of the wall meshes that make up our architecture and open it. At the top left of the viewport, we can see the stated LOD and current screen size. In LOD or level of detail is a fantastic resource saving method of optimizing our assets by removing the triangles that it is built from as well as the material it is using at each LOD level. The farther back we move from the asset, the lower the LOD used, meaning a smaller amount of resources are required to render the mesh. So as we fly way out here, we can see that the mesh is really small and is using LOD 1, resulting in rendering only 202 triangles as opposed to 404 at LOD 0. To see the changes up close, set LOD auto to its lowest available, which is LOD 3. Now only 50 triangles make up this mesh. When these get set up, it is assumed that the user is too far away to notice any changes. With the HoloLens 2, we have a unique problem. See that it is measuring which LOD to choose based off of screen size and not distance. Testing the amount of screen size to judge which LOD should be set will be important. If you have a tabletop experience where everything is generally pretty small and a user wouldn't be closer than arm's length, you'll require different LOD settings than if there are larger meshes in your scenes. Luckily, we can uniquely set the screen size determinator for each mesh. In the Details panel, under LOD settings, uncheck Auto-Comput LOD distances. I would like this distance to be when it switches to LOD 1, so I'll set the LOD under LOD picker to LOD 1, and type in the value here at current screen size into the screen size input. Let it back to LOD Auto and pan in and out. I'm not seeing any changes which is great and I know how many triangles because of the metrics shown. I'll do the same thing for LOD 2 and 3. Return to the map and change the view mode to mesh LOD coloration. We can see that the Windows mesh already have LOD set for this screen size, but let's zoom out a bit. Now our walls are changing the amount of resources needed as we fly away. Open the console command and enter stat rHI. While we are up close, we are rendering about 100,000 triangles. As we zoom out, the number is about the same, but there are also many more walls on the screen. We have a lot of assets here and it would take some time to do each piece manually. Luckily, we can use editor utilities to do this job for us. For us to use them, we need to enable the plugin editor scripting utilities. In the project files that came with this course, I have done this already and the blueprint is created. To use it, navigate to world builder blueprint editor. These are created by right clicking and under editor utilities, we can create a blueprint or a widget to modify assets in the content browser, which is very nifty. Right click on utilbp LOD and select run editor utility widget. Navigate back up to the world builder folder and add a filter to only show static meshes. Select all of them and press go. This will go through every static mesh and add or modify its LODs for us and now all of our meshes have been updated. Let's move on to our static mesh discussion. Drag in a BP static meshes. This is a new blueprint made to create an array of versions of static meshes. Set the count to 15 and keep the choice at 0. Press refresh. There are now 225 of these static meshes in the scene. We kept the choice at 0 which results in spawning static meshes. We can see that LODs are here and the triangles drawn is approaching 70,000. Under triangles drawn in the stack group RHI counter is draw primitive calls. This is telling us the amount of draw calls needed to render these meshes. Keep in mind that the stats shown here are also showing the details of needing to render the editor. This is one reason why outside profiling on the target hardware is so important. Set the choice to 1 and click refresh. What is now being spawned is 225 instance static meshes. The first thing we need to notice is that the LODs are gone. Instance static meshes will not use the LODs applied to the mesh. So while we are now rendering all the triangles in LOD 0, we are using one draw call to do so. This makes instance static meshes very valuable. But another thing to note about instance static meshes is that if any part of any of the meshes are rendered, then all of them will be rendered. I can adjust my camera to look at the tip of just one of them and the same amount of triangles are drawn. To see this, we can freeze the current render to better understand what was provided to the GPU to render. Open the command line and type freeze rendering. One word. Turning back, we can still see all of the meshes. Call the same console command to resume rendering. When using instance static meshes, it's best to keep them nice and close to each other so we can take advantage of that single draw call while spawning many meshes. Now change the choice to 2 and press refresh. Choice 2 will spawn higher arcacole static meshes. Again, the first thing to notice is that the LODs are being set at the same distance as the original static meshes. The triangles drawn and draw calls are still much, much lower. Notice 2 that when the meshes are not being looked at, they are not rendered. Call freeze rendering while only looking at a couple of the meshes. Alright, zoom out and here are the only actors that were provided to the GPU to render. Again, call freeze rendering to disable. As mentioned, each decision we make regarding what gets put in our scenes will have a cost. Take advantage of the various tools to assist you with staying in your budget. If I hide the BP static meshes and unhide the BP anchor locator, we can see that we are using lots of the same mesh to build this architecture. But this structure has some needs that must be met. First, it's never going to be duplicated in the scene, which means I wouldn't need to instance it, and it has to be movable. BP anchor locator is looking for an anchor to move to and a different anchor to look at. Any child of it will follow and this is how the meshes will always be able to find my bookcase to cover it. The cost of all these individual static meshes, though, is more than it needs to be, so let's merge them all into one mesh. Select all the descendants of BP anchor locator. Right click and select merge actors. Keep the merge type at grouping by materials. The geometry is already where I want it to be, so I will set pivot point at zero to true. I'll make sure it will use all the LED levels and set to replace source actors to true. Select merge actors. Place this asset in world builder assets architecture and name it SM underscore bookcase underscore cover. When it's finished merging, it will place the new actor exactly where it needs to be, and it holds all of the original LEDs that we added. In this module, we discussed the importance and optimization effectiveness of LEDs. We created them inside a single static mesh and then used an editor utility widget to apply them to many assets at one time. We then focused on the differences and use cases of static meshes, instance to static meshes and hierarchical static meshes. Finally, we merged many actors in the scene into one static mesh. In the next module, we will profile our project using unreal insights so we can find additional areas to optimize.