We're going to start looking at some of the components I've already created to accompany the cost and we'll be starting with the homing component as it's probably the easiest to get started with, but it also shows a lot of considerations that went into the types of function calls and setups that needed to be made or this type of component to work properly. So if you haven't seen this ready, you can add a homing component onto any actors I've done here. It is currently visualised by this sphere radius here. So as soon as you enter that sphere radius with the correct setup, that will start the cube moving towards you. Before I show you how to get that setup, we're going to go through the class and see what it includes and some of the function calls that needed to be made together to work properly. Now these are things that I haven't seen covered in many places before. So hopefully this will give you a nice insight of some of the things that you should really be thinking about to do with things like weather certain components. As you'll see in a moment, the homing component also automatically creates the magnetic sphere component. And we need to consider whether that should be cleaned up if the homing component is ever removed from its actor. And I'll be showing you how we can do that. The first thing to mention is of course you can check all of the includes and everything that you need here. You have access to all of this code if you wanted to dissect this, reuse it or take it apart a bit later. But the main things we have I've created a T array, taking in the T subclass of the A actor, this allows us to add absolutely any actor that can be in the world to be attracted to them. I've called that one attract actors. So it doesn't need to be just the player. If you wanted to create complete mayhem, you could add things like falling cubes and it wouldn't move towards falling cubes or other objects that could potentially attract it in the world. We then have the float, the magnetic sphere radius set to 500 units, just a default. So anything that has been added to the T subclass of should hopefully trigger this component to get the actor, the only actor to begin moving towards that attracted actor. And then finally we've got the homing speed suggest a standard variable that will need to allow us to set how fast we want this to home in on the other actor. Now again, I will drop these down, but do remember you have access to these if you wanted copy and paste or just look into the overrides for the on overlap enter and on end overlap. These are just the very standard kind of bindings that we'll be making to the standard overlap events. And then the two things which were very, very important, these are things I hadn't seen anywhere else. It wasn't super clear how to get this set up. So hopefully this is going to provide some useful insight. I've got two things which is one to check when a component is being destroyed. And we can actually do something kind of like on begin play or end play components have an understanding or function set to track when they're about to be destroyed. Before that happens, we can then do something and we'll see why this is going to be very important on this type of class that we'll be making here. And then in a similar way, there's a post edit change property, which can be very handy to visualize things in the constructor without needing to default to setting up sort of copying logic into blueprints. Otherwise, the things that we create in the constructor in our C++ class will not automatically be visualized inside of the editor. And then finally, just some variables below to track whether we have a valid object for our own other thing that we're attached to, the locked actor, the thing that we're trying to move towards whether or not we're in the homing state and then the magnetic sphere, the use fair component that we need to create inside of our constructor. Okay, so the first important thing here is we are beginning this by creating a magnetic sphere. And this is again, using the create default sub object of the use fair component. This one we're just calling it magnet sphere. And this is important because remember, we're creating this from a another scene component rather than an actor class. So what we're doing down here is we're actually setting the set up attachment rather than our self. So remember in the actor example, which was setting up the attachment to be the actor itself instead, we're setting the parent of this component to be the magnetic scene component that we've added in or the homing scene component. Then just here we have our on component begin and end overlap binding. So these are just binding to the homing components on overlap enter and on end overlap. So the overlap so we can get from our sphere component that we've just created. This is optional, but of course it's going to be very useful to know that we have the correct collision responses set up. So I've set this to overlap. So we're going to overlap everything of course when you have a magnetic sphere that you're looking to check for other actors, you don't want it to block them. Just forcing the relative location to be zeroed out. So we don't have any weird offsets when we create the sphere component. And then this is again very important that we're setting the sphere radius to be the magnetic sphere radius that we have full control over in the editor. Finally of course we're setting the owner to be get owner you've seen that before just so that we can track that. Now for the tech component, this is where we're going to be doing all of our checking to see if we do have something to homing on. If we are homing, whether we have a locked actor and that we have a valid reference to our owner because of course we want to avoid trying to move something if we don't have a valid reference to either as the owner or our locked actor that we're trying to get towards. This is very simple though, we're just using a V into up to we're going to get the components location. We can do that because we're in a scene component and then we're lurping towards the locked actors location using the dance time and our homing speed. This of course is drawing a debug sphere. If you wanted to get rid of that, you can just comment this out. This is again very handy during gameplay to just double check that the overlap is being triggered when you walk into it or when you put something into the magnetic sphere radius. Again, I'll just drop this down in case you wanted to see that. This is again, of course, completely optional and just something that is useful for the course so that we can see what's going on. The on overlap enter and on overlap end functions are very, very similar. We're just checking whether we have the certain actors. We go on enter, we're checking if the attract actors, the array that we set up contains the thing we've just overlapped or has just overlapped the component. If it does, we're going to set that to be the current locked actor and set it to be homing. Then kind of the same, but I guess the exact opposite is when that tracked actor is leaving the radius, then we're going to set the homing to false and set the locked actor back to null pointer. This means this is just helping us control what's going on here. If any of those are false, then of course we're going to stop homing, so we shouldn't have any issues with those. Again, these are the very important bits that I really wanted to highlight here. What I want you to do is between the modules come in and remove or comment site these two options here and see what happens when these are not included. I'll do that in the video, but of course it's super handy to get my day of this first hand because without these, it's actually very, very hard to work with this type of component. In order, the first thing we're doing is we're using the on component destroyed function. Of course, like I said, this is a standard component function that you can find. We need to call the parent super override first, so the on component destroyed on the parent version of the class. What I wanted to do, so I just uncomment these quickly. When this is destroyed, the important thing is to also remove the magnet sphere that we've just created inside of this scene component. We're just using the magnet sphere, destroy component function call. If you didn't do this, that's probably quite obvious what's going to happen, but basically the magnet sphere doesn't automatically get taken with the scene component. That is its parent. Instead, it just jumps up one level in the hierarchy and then it is taking the same position in the component hierarchy that the homing component was. What you left with is a magnet sphere, which is essentially not tracing anything for any reason, as it doesn't have the existing parent component to pass that functionality back to, but it's still left in the world. Of course, that can be a little bit annoying and it also makes it very hard to track it down and get rid of it in a similar way in the post edit change property. This is again, just a standard call that we need to also include call to the parent function. This is updating the set sphere radius on the magnet sphere, so again updating the built-in function call for our sphere components and we're setting this constantly to be the magnetix sphere radius. This is what allows us to track changes made in the editor. If you didn't have this, the changes wouldn't actually be accounted for until you enter something like a begin play and you have the call there or on the event tick. Of course, that's not too useful because remember in the constructor, we're setting the sphere radius to be whatever we've defaulted this to here, which means if you were making updates then in the editor because we've exposed this property in the editor so we can change this on the fly. You wouldn't see those changes until, like I said, you update it somewhere else. Adding this function call in just here allows you to drop in all of the things that you want to see changing in the editor and see those live updates. If we go back over to the editor, we can see what this is going to look like. This is what I was just explaining. We can come in, you can change our magnetix sphere radius, so by default it would have been this if we just add a new homing component onto something. We can try that very quickly here. We can see that defaults to 500, which tells us immediately that might be a little bit too large or a little bit too small. Then we can get an idea live in the editor of how big we want this to be. I'm actually just going to delete that. This shows us the next useful thing is that we can delete the homing component and the sphere goes with it. Just to show you how this works, we're going to go and get this homing component working. I'll set this back down to something a bit smaller. Having speed set to 1 should be fine. We're going to add an element to the array and we'll make this the player. The BP underscore player. If I press play, and again, I'm also creating this debugging sphere, so we can also see this in the event tick, but of course this is exactly the same size, so it's taking the same variable to find increased this. Then that debugging sphere is going to be bigger as well. What we should expect is when I press play and walk into this sphere, the object here should start tracking us and homing in. That is working exactly as expected. As soon as we leave, like I said, those references are being set to null and it will stop tracking something that doesn't have access to anymore. That is the homing component. That's how it works. These are the considerations. Like I said, you want to take to make this as workable as possible. Because this is inherited, this is the other problem because the magnet sphere that we've created is inherited. Of course, if this is left over because you haven't done that kind of cleanup call, then we can't manually delete this. I'm just going to go back to the code file and I'll show you what this looks like. Like I said, I think you should definitely go ahead and compile this and make these changes yourself. Play around with things and see the kind of results that you get. Let's see, you get that first hand experience of just how useful and the importance of these kind of implementations can be inside of your project. With that compiled successfully, though, we're just going to come back in. The first thing is we can very easily see whether or not the magnet sphere is going to be updated. Here we go. This is the first problem where I'm scrolling along with the sphere radius. This is being updated, but it's not clear that the actual sphere itself is being changed in size. We can see that is taking the value separately because that isn't being passed across. What would happen is if I set this a lot bigger, I need to come in play and we can see, of course, that's being updated when we press play only. Likewise, if we add this onto another component or another actor, sorry, we're going to get that default 500 radius, which again, if that isn't the ideal size, I mean, that we wouldn't know how close or how big or how small we want to make this until again, we press play and we update the variable somewhere and begin play something. So that's the first problem. If we now get rid of this, we can see that I've just deleted the homing component and we're stuck with a magnetic sphere component on this actor that still follows it around. We still have this connected to this actor when we don't want to. And like I've said, because this is inherited, we now can get rid of it. So you can have to come in, delete the actor and so on. So not the best approach. And like I said, it's something which I haven't seen a lot of information on, but it is a very, very useful habit to really consider as you're going through. Do I also need to do tidy up later on if the designer, the user removes this component at some point? Is there going to be anything left over? Is there anything I can add to make things a little bit easier to visualize things and just to make again, the process and the whole idea of reusing these components and taking this process is that we can make things a lot more. User friendly, reusable and kind of quick to get these iterations of the project going. So taking these extra steps in your code is just going to ease that process up again. Like I said, I think that's quite easy to visualize. There are certain things which I think just take that first hand experience to really appreciate the difference that these two changes can make to the project. So I'm going to compile that back in to make sure that we have that saved and ready to go in future examples. You can play about with that class though. There's definitely a lot more that you can do with the homing class that I've provided here. But that is definitely the main aspect of the class that we've just looked at. So that's how the homing component is implemented. Hopefully this demonstrates the flexibility of being able to expose the class types and the component details to check or not just things related directly to the component you're working on. So the other component would have been the focus of the class creation there, but also any subcomponents you might be adding into your component because you do have that flexibility to create components within components.