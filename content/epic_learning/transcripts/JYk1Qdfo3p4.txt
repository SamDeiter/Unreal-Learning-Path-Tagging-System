[Music] welcome to another unreal Tips &amp; Tricks creating realistic glass materials can be a challenge however unreal provides great material tools that make the job easier I'm going to show you quickly how to create two common effects for glass surfaces frosted glass and tinted glass to get started we're going to need a new material so I'm gonna go ahead and create one here in my content browser we'll name the material M underscore glass and double-click it to open it our first step is to change the blend mode to translucent for a glass material next we'll need to specify the base color using a vector3 leave the base color black rather than use the opacity input of the route material we're going to use the emissive color input this technique will allow the reflection to be consistently bright we're going to need the scene color node and we'll also need a new vector3 to represent the tint color of our glass convert the vector 3 to a parameter and let's call it edge tint color we need to multiply the scene color and the tint color together let's also add an intensity control we'll need a constant parameter which we'll call edge opacity let's get the inverted result using a 1 minus node then we can multiply the two groups together after that we need to plug it all into the emissive color and save be sure to apply our new material to the tabletop you can see that our tinted glass covers the entire top but we want to make it so that it only affects the edges for that we'll use vertex painting go to the paint tab in the modes panel and set the color View mode to RGB channels the table should look all black with a small brush paint the vertices along the edges of the table you'll need good chamfered edges in order for this to work right when you're finished reset the View mode to off and let's jump back into the glass material in order to control where the tinting occurs we're going to use a vertex color expression we simply need to multiply the expressions output by our tinting graph then plug that into the emissive color input after saving we can see that we're getting a lot closer the edges of the tabletop are now tinted green and translucent however the top and bottom are solid black let's fix that now draw a box around the entire tinting graph and copy and paste it rename the new color parameter to top tint color and set it to white we'll also rename the opacity parameter to top opacity in this case we need to get access to the top and bottom vertices and not the edges so we'll need to use a 1 minus expression to invert the vertex color output before plugging the hat into the multiply node finally we need to add the two tinting groups together then plug the ad expression into the emissive input save it and let's have a look now the green tinting is only found on the edges while the top and bottom are clear but it's time to improve the quality of our glass now to do that select the route material node or select a blank spot in the graph and scroll down on the details panel to the translucency section from here we need to change the lighting mode to surface forward shading at the bottom of the drop down this is the highest quality version of translucency that unreal offers but keep in mind that it is also the most expensive to render this should give us access to the roughness input on the master material now let's add a constant parameter to roughness and leave it at a value of zero we'll call the parameter roughness let's also set the material to a two-sided material on the details panel this will allow us to see the edge tint color from both sides of the glass save your progress and let's give our new tinted glass material a world next up we'll look at how to use the spiral blur node in the material editor to create a frosted glass effect on our tabletop