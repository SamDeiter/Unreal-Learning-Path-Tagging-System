Thank you for joining. Uh my name is Luke Thatcher. I'm a principal rendering programmer on the RHI team at Epic. Um and this is our session on the new GP profiler and RHI submission pipeline. Uh so this session will be one that's in kind of two halves really. Uh first section I'm going to talk about what we've done to the new GP profiler in Unreal 5.6 and the improvements that we've made there. Um, and then in the second half I'll be going over like a deep technical dive into uh the RHI submission pipeline. This was something that we shipped in 5.5 uh but we haven't kind of publicly spoken about yet. Um, and just for anyone who who's curious, RHI stands for render hardware interface. Um, it's one of those acronyms that we throw around quite a lot but we don't tend to define very much. Um so yeah the RHI is the is the module of the engine that sits between the render uh the renderer and the GPU kind of um platform API and we implement one of these per platform and uh it's what provides the platform support for graphics. Uh but before we dive into that um let's just have a quick recap on GPQs as this will set like some of the scene for some of the changes that we've made in the profiler. Um, some of this you may already be familiar with, but it's it's probably worth just having a quick refresh. So, for the best part of a decade now, um, GPUs have had multiple cues. Um, these cues you can kind of think of as almost like hyperthreading on a CPU and multiple threads on a CPU in that we have kind of two or two or more independent cues of work that the the application can submit to. Um, and they run concurrently. Um, and they sort of intelligently share the execution resources on the GPU. Uh typically there's always uh a single graphics queue and the graphics Q can access the full traditional uh rasterization pipeline. So depth essential testing, vertex, pixel shaders, all that stuff. Um but it can also run compute jobs. And then you typically have either one or more compute cues that can only run compute shaders. And of course these jobs um the jobs submitted to these cues can run concurrently with each other. But that's not always desirable. we tend to have uh jobs that create data on one pipe and need to be consumed by another pipe. So we need some way of synchronizing these two pipes together and that's where we get fences. So fences are kind of synchronization primitives that are used to prevent one queue from advancing any further until uh the signal has come from the other queue that was producing that data. Uh so in this case um those three compute jobs on the right hand side on the compute Q can't start work until the graphics queue has completed that first piece of work in the upper left and then we get that signal of fence A and then the wait for fence A um to allow everything to advance and then coming back the other way those last three uh jobs on the graphics queue can't start until that signal has passed um on the compute queue. And one thing that's interesting to note about this is that because these weights stall the cues, you can end up with execution kind of pauses. And you can see that kind of like in the uh on the left hand side where there's that gap just before uh the weight for fence B. Um so because the cues are synchronizing with each other, you can end up with these kind of bubbles on the cues. Um and that's something important to to to be aware of when we're looking at the profiler. So, moving on to the actual profile itself and then the improvements that we've made in 56. Uh, hopefully you're all familiar with these tools. Um, they've been around for a while now. Uh, the three main components of the GPU profiler are stat GPU, the profile GPU command, and Unreal Insights. Um, in 56, we've rebuilt these from the ground up to to uh uh be based heavily on our RI breadcrumb system. I'll touch on what our RI breadcrumb system is um further on in this talk. Um, and one of the key things here is that it now all draws from a unified data source. So, uh, all of these, uh, tools now agree with each other. Um, perhaps previously you might have noticed that some of the stats were like off by fractions of milliseconds because they're all drawing from the same, uh, information. Now, they all they all agree with each other. Okay. So, uh, stat GPU, um, this is a, uh, a tool that you can use as to to give you kind of like a live overview of the GPU work that's running in the frame. You can bring this up on console. You can bring this up in the editor. Um, each one of each one of the lines here is a single stat and the stats are defining code. There's an example macro for the base pass there. Um, and the individual uh the individual stats get attached to specific render graph event scopes or RHI breadcrumb scopes and then all of the rendering that happens within those scopes gets attributed to these stats. And something important to note is that uh these stats can appear multiple times in the renderer and they uh so like uh for example I think the post-processing one is an example where there's actually multiple places in the renderer we tag with post-processing and then the stat the data that you see in stat GPU is uh an amalgamation of that kind of we accumulate in some the results. Uh so for the first time we are now multiGPU aware and multiqaware. Um, this was the big change in 56. Uh, previously with stat GPU, um, you'd only get the single graphics pipe of your first GPU. Um, so if you're running in a, uh, if you're running as compute, you'd never see the information from async compute or, uh, if you're running a virtual production uh, scenario with multi multiple GPUs and so on, um, you never got the information about those GPUs. Now, we get all of it. Um, and so each of these stat pages that are highlighted with the brackets, um, they're all actually independent stat pages. It just uses the standard Unreal stat system. Um, and you can bring them up individually uh with the longer format command stat GPU and then the index, the Q type and another index. Um, it's all built so that we can in future support potentially more than one compute Q. Um, but at the moment there's just the one. Um, but we've also repurposed this the standard uh stat GPU command um, which is just like a macro command now. It toggles all of them on and all of them off. So if you've got like muscle memory of of typing stat GPU then it will still work as it did before. Uh but the actual data that we present now has been greatly expanded. Um we now have these three separate columns. The one on the far left is what you used to see before. Uh it's the busy time. It's the time actually spent doing useful work, running command lists, running shaders. Um the middle column on the and the column on the right are the two new ones that we've added. Uh the middle column is the wait time. And going back to that example I gave during the recap, that's the time when one Q is waiting for another Q to signal a fence and therefore isn't running anything useful. Um you may find that uh generally um compute Q you'll see larger nonzero values um because it's not always a win to run something on the compute because it depends on what it's overlapping with on the graphics queue. Portions of the frame on the on the compute Q is just idle waiting for fences to signal waiting for the the data dependencies to be resolved. Um so it's not necessarily a bad thing if you see lots of wait time on the compute Q. However, the graphics queue we tend to try and keep busy all the time. So generally the numbers there should be closer to zero. And then the the the column on the right hand side uh is idle time. We've never had a stat like this in Unreal Engine before. This is really cool uh because we know exactly when the work gets submitted. So in modern APIs like D3G12, submitted to the driver. We know exactly when the GPU is completing the work. We can now see that the GPU has gone idle because it's run out of work from the CPU. Um, so a non-zero value here means that the GPU has actually completed all of its work and has gone idle because the CPU hasn't given it anything further, which is a good indication um that your title is at least partially CPUbound um for that duration of the frame. Um, so if you see non-zero values here, be sure you're running a test build or something, something with good CPF, otherwise it's not very representative. But if you do see non-zero values, that kind of warrants some further investigation with Unreal Insights or or other tools. And one very important thing to to note is that the cues are because the cues are concurrent. Um you can't just add up these numbers across the Q pages. Um that wouldn't make any sense. Um each of the stat pages have a uh Q total stat which is the one at the top. Um which gives you the total amount of work in each of the columns for that frame. Um, but if you're looking for an overall GPU stat time that tells you kind of like the ballpark figure of how long the GPU took for the whole frame across all cues, uh, you need to be using the stat GPU time for that, which is, uh, the one highlighted with the arrow. Um, we compute this stat by taking, uh, a union of all the busy times across all of the all of the cues. Um, and then that goes into uh that final stat time, which is for the frame. Um, and that's the that particular stat is the one that drives systems like dynamic resolution. um because we need like a a single stat to say how expensive the GPU is this frame. Um and then we can scale things up and down uh with that. So moving on to profile GPU. Um you may have already seen this uh this command before. It's a command you can run in in the in-game consoles. Uh it produces a text log that goes out to either the it goes out of the UEI log. So, it's either into the um debugger output window if you've got Visual Studio attached uh or the editor log window um or the log file itself. Um it gives you a table nice view breakdown of absolutely everything that happened in the frame. Um and again, it's driven by RHI breadcrumb system that I'll come to later. Um and this will include all of the individual RDG uh pass names. Um so there's a couple ways you can use this. I use this uh sometimes just to check if a certain pass is even running. is quite a handy command to just throw into the console, see the output log and go, "Oh yeah, this particular postprocess pass is running in this frame." Um, but of course it gives you all of the stat breakdowns. So we have for the first time we now show both uh exclusive and inclusive timings. Um, I think previously we only showed the inclusive timings. Um, and also again as the kind of theme with the changes that we've made here, we're now also multiGPU and multiqaware. So you get one of these tables per GPU per Q. Um, so if you have async compute enabled, you'll get two tables. You get one for graphics, one for compute. Um, but if you're trying to use this tool to focus on a particular uh render pass, a particular shader, um, to kind of optimize and and tune things, um, it might be best for you to turn off async compute with that uh, the CR R. RDG. Async compute zero. Um, the reason for this is because as uh, compute work overlaps with work on the G on the graphics Q, um, they can affect the timings of each other because they're sharing execution resources. So if you're trying to focus on a specific pass, it's it's worth turning that off. When you turn it off, everything will fall back to the graphics queue and then everything kind of lines up. Uh there's a whole the whole chunk of uh uh console variables that you can use to to filter this this table um and and show and hide columns that you don't you don't want to see. Um in this particular the screenshot example I have at the top, I've actually filtered the tree down to just the scene node. Um and you can do that with the r.profilegu.root root uh console variable. It just takes a string and you can put wild cards in and it will show you only the the event names that match that. Um so if you're focusing on like say post-processing that you can just set the route to postprocessing you only get that section of the table visible. Uh one other important thing to note um some text editors don't handle uh unicode fixed width characters very well. So those kind of pretty bars that we draw now uh to show the kind of relative timings, some text editors just just can't handle that and it's all it's all um out of alignment. Um so if you find that I'd either recommend changing your font to one that can handle those characters because then you get the nice the nice table view. Um but otherwise you can set that uh unicode output cart to zero and we'll fall back to just asky characters and then the table should line up properly. Okay, so moving on to Unreal Insights. Um, hopefully you've all been using this tool to profile uh the CPU, but now we've greatly enhanced our uh GPU capabilities in a real uh insights. Again, as as with the theme, we're now multiGPU uh multiGPUware and multiQ aware. So, we have one uh timeline per Q per GPU. Um, and these cues are aligned correctly with the CPU as well. Um, so you can see the the the the motion of work from CPU into GPU and back again and see where the weights are happening if you have bubbles in your pipeline and that kind of thing. And maybe some of you are familiar with the context switch data that you can get in Unreal Insights um which you see on CPU threads that tell you exactly when the thread is actually scheduled and running. We have something similar now on uh on the GPU. Uh so we're able to with the extra information we get from the new profiler draw these regions that highlight exactly when the cues are running and when they're waiting and when they're idle. So the highlighted um regions on the timeline at the top currently are the regions where we're doing the GPU busy work. And this again is the same stats that you get from stat GPU. Um it's literally these regions that we're summing up to produce that data. And then the highlighted regions here are the GPU weight regions where the where the queue is blocked. And you can see here the the lower line is the uh async compute Q. And you can see that there's just more weight work happening there because that's just generally what async compute does. And then finally, we have the kind of grayed out regions that don't have any marker above them. And that's the idle regions, the places where the GPU has stopped because there's nothing further for it to do. The CPU hasn't given it any more work to do. Uh so we've also added uh similarly to the to the tasks channel um on the CPU in Unreal Insights, we now have these fence dependency arrows. Um you can bring these up by just single clicking on one of the markers like the frame marker. So yeah, the screenshot I have here is just single clicking on the frame marker and it shows all of the uh the weight dependencies between the two cues. Um and each one of these is a fence that is getting signaled and weighted upon and it's the thing that is driven by our RHI transition API, not an API that you generally need to deal with because RG deals with all that stuff for you. Um but as RG knows that there's a data dependency between certain uh certain passes, then this is when we get these fences. Uh but of course this is quite a busy busy view to look at. So if we zoom in and just look at a single example of one of these arrows um to get a feel for exactly how they appear on the on the timeline. Um the top line again is the graphics Q. The lower line is the compute Q. And here we have uh some nanite base pass work that's running for several milliseconds and a region where the uh the as compute Q is just waiting for graphics to catch up. So if we zoom in a little further that's this is that same arrow. So we have that region in the top left uh corner where the graphics Q is doing the busy work. And as soon as that busy work completes uh we signal the fence and that's denoted by these uh two matching fence numbers. They appear uh just below the markers on the timeline and these are the indices of the specific fence. Um they just increment along the pipe. So the very first fence ever submitted is index one. Um and it shows you exactly where the fence is signaled and where the fence is resolved because that might not always line up exactly. It could you could signal a fence much earlier and then it gets weighted on much later if just the way things line up. Um so you can see here the fence gets signaled. The compute Q goes from a waiting state it back into an idle state because there's always some amount of latency that's associated with signaling a fence. Um but after a very short period of time you can see the compute uh springs back into life and we get back into a a GPU um busy state. So uh as I mentioned before um since we have the GPU cues aligning correctly now uh with the CPU timeline and insights we can get a lot of information from uh the flow the kind of pipeline of data into the GPU. Um so one thing that's worth mentioning a lot of our platform RHIs now have an RHI submission thread. Um and this is a thread that sits between the RHI thread and uh the GPU itself and the driver. uh primarily it's used for batching work together um to reduce uh GPU overhead and driver overhead. So we're just making fewer calls into the OS. Um and it's also the thread that resolves uh fences. Um so in the example again on the timeline uh the very upper left corner we have the RI thread with uh this RHI submit to GPU marker. That marker is the place where we take the work that we've produced in the in the kind of the CPU threaded side of the renderer and hand it down to the submission thread. The submission thread runs for some amount of time doing this batching work and resolving the fences and then eventually we hit this that blue uh region on the right um which is where we actually crawl into the OS and then you can see the GPU Q springs into life uh immediately afterwards. And similarly to the to the submission thread where work is flowing into the GPU, we have another thread that's monitoring work uh completion on the GPU to then signal the rest of the engine. Um we call this the RI interrupt thread. Uh it's basically just one thread that sits and waits on fences um to see the work that we've submitted to the queue know when it's completed um and then goes and signals the rest of the engine. And uh one thing to note is that we have generally we have one of these threads but it actually handles all of the GPU cues. So you'll see this thread wake up whenever any one of the cues complete some work. Um so you'll see them it kind of springing to life. Um so in the timeline example uh this particular example is occlusion queries. So the very bottom left corner we have the GPU doing occlusion queries for the previous frame. And then in the top right on the foreground worker thread we have uh the the code in the renderer that's doing uh the init views pass the scene visibility pass that wants that data back and it's waiting on the the occlusion query results from that frame. Uh so the GPU completes the work signals fence there's a little fence number you might not be able to see on the slide um but where that arrow is pointing um the fence gets resolved. It wakes up the interrupt thread. The interrupt thread pulls the cues across all of the GPU to know what work has just completed. Sees that it's this particular piece of work that's completed, grabs the event and fires it into into task graph which then wakes up the task graph. Um so with this kind of that this flow of data the arch submission thread the arch interrupt thread and then the alignment um uh of the timelines um you can now use insights to really know where your bottlenecks are where your bubbles are and you can do analysis of the pipeline all the way back from the GPU all the way through to the to the render thread um which is super powerful super useful. So that covers uh the improvements we've made to the new profiler. I'll move on now to talk about our new RHI submission pipeline which uh shipped in 5.5. And uh in this section I'm going to be focusing primarily on the RHI API itself. Uh day-to-day this isn't an API that you'll be uh kind of directly using. Um generally if you're writing renderer features these days you'll be using render graph. Um so sort of like some of the some of the details here are real low-level stuff inside the arch. But it's still very useful to know about this um if you're doing performance analysis and you're looking at insights and trying to work out where your per uh whether where your bottlenecks are, where your threading is going, that kind of thing. So I'll start by just uh defining a few highle types that you'll see in the code. Um we have the FRI command list base type which has most of the implementation for recording RHI command lists and then the three derived types from that which are just sort of mostly adapter classes that give you access to things like the draw call API and the dispatch API and so on. Um, and the special case in there is the R uh RHI command list immediate, that last option there. Um, the immediate command list is a singleton in the engine that's owned by the render thread and is kind of the primary entry point into the pipeline for submitting work to the GPU. Uh, we also then have uh the uh the types that are implemented uh per platform RHI. So there's these base interfaces that are then implemented by each each each RHI module. Um uh the the the main one there being the RHI compute context and RHI command context types. The contexts in the RHI are kind of uh the the objects that are involved in converting the recorded RHI commands from the renderer into the actual GPU commands that run on on the hardware. So it's within the context that we're calling into say D3D12 and actually producing D3D command lists. Um and those produced D3D command lists end up getting wrapped in this IHI platform command list type um which is used just for the management of the submission pipeline. Uh and one thing to be aware of uh we have lots of legacy names around this stuff because this this this system has been around since like the early UE4 days maybe even UEI3. Um so the II command context is specifically for the graphics pipe but it doesn't mention the word graphics in it even though we have a compute context called II compute context. Um, so yeah, I'm sure one day we'll be renaming these, but uh that hasn't happened just yet. So as a general kind of high level overview of how this process works, the renderer and render graph creates instances of FRHI command list. Um, there's one of these per recording thread and the commands that get inserted into this command list happens in CPU order. Um, but a single RHI command list can record work into multiple RHI contexts. You have one of these contexts per pipeline that you're targeting. So either graphics or compute generally you have either one or two contexts per per command list. Um and as the commands are inserted we have these two functions switch pipeline and in Q lambda multipe that is what toggles between uh it directs the the commands towards whichever appropriate context um the commands are for. So say for example when render graph is uh replaying passes and there's a a pass for the graphics queue and then a pass for the compute queue inside render graph there will be a call to switch pipeline from one to the other to direct it over to to async compute. Um all of our RI command lists are submitted via the immediate command list uh with those two functions there the Q async command list submit and immediate flush. Um and all of our command lists are they operate in two phases. they're either um they're initially recorded and then later what we call translated. And translation is that process of replaying uh the recorded commands calling into the RHI context and generating the actual GP platform commands. And so this is exactly what the the switch pipelines looks like. Uh in the center is an example command list with loads of commands in. Um and then you get the kind of interled uh switch pipeline calls that just direct when we do the translate it directs those following commands into the appropriate context. Uh one other thing to note is the uh the context themselves are required lazily. So if you have a command list that only uses graphics, it will only acquire a graphics context and only do the work for the graphics uh for the graphics pipe. Um so it saves you the work of like if you were to grab uh both at the same time, you'd end up with a context that had no work in it and it's just an overhead. So uh so at this point I'll apologize. is I'm going to switch to just reading directly from my speaker notes because this slide is really complicated. Um, and yeah, there's there's there's a lot of information here that I need to make sure that I get right. So, uh, we'll have to bear with me and read just from the from my notes. Uh, so over the next few slides, I'll walk through an example of how RHI command lists are created, recorded, dispatched, translated, and submitted to the GPU. This is a multi-step process with lots of tasks and parallelism. You don't need to take all of the information in on this slide immediately as I'll be stepping through the stages and adding arrows to the slide to show task flow. Solid arrows indicate where one task starts another and dashed arrows indicate where a weight is resolved. The colored boxes represent threads or chains of tasks. They execute left to right on the slide. None of the steps block unless a step is explicitly marked as a weight. To begin, the render thread creates three new RHI command lists which we'll label A, B, and C. It then kicks a set of tasks to the to record work into these command lists. Each recording task runs concurrently. The main source of the tasks in this the main source of these tasks in the renderer today are from render graph parallel pass execution. When the command lists have been fully recorded, the recording tasks will call finish recording. This function signals that the command list is completed and ready for dispatch. Meanwhile, the render thread has continued on and has called QA async command list submit passing the three command lists. The order of ABC is significant as this is the submission order and therefore the order that the work will be sent to the GPU. QAnc command list submit will launch a new task called the dispatch task. This task is responsible for grouping command lists together into translate chains to achieve parallelism. It starts by creating a state structure to track the progress of the submission. After creating the state structure, the dispatch task awaits completion of the first command list in submission order. This weight is resolved once the recording task is called finished recording. At this point, command list A is ready for dispatch. Since this is the first command list in the submission, the dispatch thread starts a new translate chain. This is another task graph task which is responsible for replaying the commands recorded in the RII command lists into RHI contexts. Translation is the process that calls into the platform RHI implementation to generate actual GPU specific command lists. The translate chain acquires the relevant RHI context by calling RHI get command context and then executes the command lists. The contexts acquired are stored on the chain for later use. Command list A is deleted once execution has has completed. While translation of command list A is in flight, the record task for B has finished. The dispatch task has been waiting for B to be completed and once it has dispatches B. Since A and B were relatively short command lists, the dispatch task decided to append B to the first translation chain. The context for the translation chain, excuse me, the context of the translation chain acquired while translating A are reused for B. Again, B is executed and then deleted. As before, the dispatch task has moved forward and is now waiting for C to be completed. However, this time the the dispatch task decides that it should avoid appending C to the first chain to achieve better parallelism. Instead, it issues a close command to chain one. This causes chain one to finalize the context that it acquired once all translations in that chain have completed. Finalizing an an RHI context converts it into an IHI platform command list pointer which contains all the platform specific GPU work generated by that context ready for submission. These pointers are stored on the chain for later. Meanwhile, the second chain is started by a dispatch task. As before, contexts are required, C is executed, and then While all of this has been happening, the render thread has continued on and called immediate flush on the immediate command list. This function flushes all prior work and will eventually result in submission of work to the GPU. It achieves this by appending a submit command to the end of the dispatch task to run after all prior command list dispatches have completed. The submit command closes the last open translate chain. This is the same as before. The acquired contexts are finalized into platform command list pointers. The submit command also incues a task for the RHI thread. The RHI thread waits for the translate chains to be closed, then retrieves the finalized platform command lists. It then forwards these command lists to the platform RHI implementation via RHI submit command lists. Again, the order is important. Work from chain one is submitted before work from chain 2. Finally, the tracking state structure is So there we are. That's that's the full graph. That's quite a lot. Um, as I mentioned that this is like the the full uh run through of everything that happens from the render thread all the way down to the RHI thread. Uh, an interesting thing to note is that this final uh box in the lower right that was the marker that I was referring to when we were talking about Unreal insights earlier. Um, the Ri submit GP marker that occurs on the RHI thread. So that whole flow graph goes up to that point and then after that point the par the platform command lists get um pushed into into the platform RGI and then from that point the submission thread and the GPU is kind of an implementation detail of the particular platform that you're running on. Uh so this is what the translate chains look like if you were to take an insights capture. Um each one of these lines here is a single translate chain. Um and each of the blue markers uh within those lines is a single RI command list that's being translated. We have this RI translate marker above each of them. Um and each of the chains end with an RHI finalize command uh which is the pink marker at the end of each line. Um and one thing uh that we do here is that uh to avoid some task graph overhead as we build these chains we append command lists to the end of the existing chain atomically in a way that the task if the if the command list is has arrived before the task is finished. It sees that there's another command list to continue and it just pops it and just loops back again and runs it again. Um which minimizes the overhead. We used to have a version of this where there was gaps between each because every single command this was a taskcraft task and the overhead was was too much. Um but crucially the question then becomes when exactly do we break the chain? When do we how do we decide how wide we're going? How many how many of these chains we're producing? Um the uh the kind of the short answer is we count the number of commands on the RII command list. We have a a num commands field per RHI command list. every time you call a function on an RII command list that results in an allocated command that just increments that counter. Um, and then by the time the RHI command list arrives at the dispatch thread or the dispatch tasks, um, we know how many commands are coming up. We know so we know kind of roughly how big the command list is. And then we have this car um, r. RHI command.paral translate max commands per translate. Um, and it's just a high watermark. You set that value to like a thousand or something. then uh we'll be build chains up until the next command list will breach that limit and then we'll start a new chain. Um but besides that mechanism, there's also a a handful of reasons why we can't do parallel translate on on some command lists. The full reasoning is the code snippet on there. Um I don't expect you to be able to read that, but that's kind of the function you want to look at if you want the reasons. Um the main one being or the one of the big ones is any use of the immediate command list. That's the one that's the global singleton, the one that's owned by the render thread. um because that thing has been around since very early UE4, there's a lot of systems that were still using it that relied on uh that weren't thread safe. Um so it wasn't possible to uh to run that stuff in parallel. Um so even today any use of the immediate command list to do drawing work will result in a single thread translate and is is a bit of a per hit. Um so generally you should avoid uh unnecessary use. Uh that means like in your render graph passes the type of the command list that you take you can take a base one just like FRI command list um which will allow parallel translate but if you take one that is the immediate command list then you'll be causing this to happen. Uh there's also a mechanism that we call uh the lock fence. Um the name came about because this was originally used in RHI buffers for lock and unlock to update uh buffers on the GPU. Um, but it's used today for anytime there's an RHI command that will mutate the CPU side of an RHI resource. Uh, the reason that buffers use it is because on most of our platforms when we lock and unlock a buffer, um, we're not actually updating the original buffer. We're allocating a brand new buffer in GPU memory to avoid having to stall anything. Um, and then we do a kind of a swap uh uh we call it a rename which is where we uh there's like a pointer to the current buffer in the RHI and at some point we swap out that value for the old one, discard and and and and free the old buffer. Um, but of course if if uh multiple translates are happening in parallel, we want to ensure that all the all the commands that happened before the lock see the old value and all the ones happening after see the new value. So we have to fall back to a single thread translate. And this is what that RHI thread fence with true as the argument um causes to happen. Um but again you won't necessarily run into that yourself. It's just a an implementation detail of the API. There are certain APIs that do these mutates that will have this just kind of built in. Um and then finally we also have this uh additional command um uh console variable r.rmand.p parallel translate combine single and parallel. Um, and so when this is enabled, it means that any uh an RI command list that could have been parallel translated will be a candidate to join a translate chain that is forced down to single thread. Um, so if you turn this off, then it means that any uh any RI command list that could be parallel translated will be parallel translated and it won't be kind of siloed into um running on just the RHI thread uh because of some other uh some other um command list that has to be single thread translated. Um, so that s uh comm uh console variable together with the max commands per translate one are your two primary tunables for uh determining how wide the renderer will go. Excuse me. Um so uh these are the two kind of things you want to experiment with and run a load of Perf captures on if you're targeting hardware that has fewer cores or more cores. Um generally you'll want to go wider if you're on a on a system with multiple calls or go narrower if you're on a on a on a more limited system. Um, and I think I already mentioned, but the single thread translate always runs on the RHI thread. Um, and this was again because of a legacy reasoning. There's certain code that expects to be on the actual physical RI thread. Um, and we achieved this with some task dependencies um between the the the the um the tasks. Uh, and again, so this is what this looks like in insights. Um, so this uh you'll get single thread translate anytime that allow paradigm translate function on the command list returns false. And you can see we have two blocks of parallel work and then we just fall back to single thread for for a duration. Um I think this screenshots's probably about over a year old now. And the reason for this particular single thread um translate was uh some uh rate tracing APIs that was using the media command list. I believe we fix this now although don't hold me to that. Um and yeah, so we're we're we're we're slowly moving towards a world where we're trying to reduce the amount of uh single thread translate that happens because it's just a perfect it's just a bubble in the pipeline. Um doesn't scale very well. Uh, so a few kind of caveats and clarifications about all the stuff I've just said. Um, any thread can create an RHI command list instance, but only the render thread can submit them. Um, a good example of this would be our mesh streaming system. In order to stream a mesh, we're pulling like vertex and index data off the hard disk. And on PC, getting that into the actual GPU memory involves work on the GPU. um the OS like Windows will get you as far as bringing the data into system memory into an upload heap but then the GPU actually has to copy that data into its local uh local memory. Um so as a result if you want to create a buffer in the RHI you need a command list. The function to create a buffer is on the command list type. Um and so what happens in the mesh streamer is the mesh streamer thread that's running in the background creates its own RHI command list. It uses it to create the buffers and fill them in with the initial data. And then the very last step of the mesh streaming system when the buffer gets handed over to the renderer is a task on the render thread gets incued. Um and then the command list that it created gets handed along with that and then submitted through the immediate command list and from that point onwards the buffers are now safe to use. So there's kind of like a handover mechanism that happens. Uh the dispatch task in quotes uh is not isolated to a single submit. Um you could think of this as a thread really. We don't call it a thread because it isn't a physical thread, but it is a continual chain of of dependent tasks. I know in the in the kind of flow graph example I gave earlier, it looks like the dispatch task is only relevant for that particular submit, but actually there's dependencies off either end of that and it's it's just a chain of tasks. Um, so yeah, so like a dispatch for a subsequent job won't run until the private private one uh previous ones have completed in submission order, but generally the dispatch tasks are so so short you don't see them being a bottleneck. uh the recording and the translate chain tasks complete out of order but the dispatch and submission is always in order. Um again this goes back to that the reason why we have the lock fence and so on is because those translate jobs they're just kind of free to run on the task graph and they complete out of order. Um but all of this management logic then has to kind of reorder everything back into the correct order to hit the actual GPQs. Uh and then finally the immediate command list works exactly the same as the parallel ones. You can do drawing on it. You can do all the same stuff that you can with a parallel command list. Um, I'd advise you don't because we should be avoiding the immediate command list. Um, but the only difference is that you don't call finish recording. Um, finish recording was uh a mechanism to get this kind of parallel stuff to work and it doesn't need to happen on the immediate command list. I think it'll actually crash if you do there's an assert in there, I think. Okay, so uh that covers all of our submission pipeline stuff. Um but we'll now just kind of go into RII breadcrumb system and the RI breadcrumb system uh forms the kind of underpinnings of everything that we've done in the new GP profiler. Um and it was kind of it came about as uh there were multiple systems in the engine and in the renderer that were trying to label renderer work for so that you can see what you're working on in like in a pix capture or anything like that. Um and all the systems were kind of competing with each other. uh they weren't correct in all the threading scenarios and there's multiple ways that the engine runs threads across all the different platforms. So we needed a system that um kind of brought everything together and the archite system is is what came out of that. Uh so this is what they look like um in insights. Uh you probably can't read that on the slide, but the the area that's highlighted with the brackets um everything from the frame marker down is a breadcrumb. And the breadcrumbs um we tag certain areas of the renderer with with certain names and then that breadcrumb that same breadcrumb appears everywhere that it can. Um so you'll see them on the render thread, you'll see them on parallel pass RDG execution, you'll see them on the parallel translate, you'll see them on the RHI thread, you'll also see them on the GPU. It's all the same marker all the way through. Um, and this gives us some real good context as to like what what a thread is working on. Previously, you just saw, oh, it's doing render graph stuff, but you actually had no idea what pass it was working on. If something's really expensive, you have to kind of profile with sampling or something. Um, so these are in every build uh that has that macro enabled with RHI breadcrumbs, which in vanilla UE is uh every debug and dev config build, but also builds with the profile GPU command enabled. There's some additional macros that can turn that on in test or shipping. And when you do that, you'll also get the breadcrumb system as part of that because uh profile GPU relies on the breadcrumb system to work. Um and then we also have this console variable rdg.events. Um this uh determines how much detail render graph will emit to the breadcrumb system. Obviously emitting all of the detail all the time is is is more expensive than not. Um so the default value here will only emit the highle scopes things like scene base pass processing that kind of thing. Um but you can turn this all the way up to number three. And when you get to level three, you get all of the scopes, all of the info, all the pass names, including all their parameters and everything. Um, and that's that's actually implicitly what happens when you do a profile GPU is it internally turns that C bar kind of to three. Um, and you get everything in a profile GPU capture. Uh, and so this is what they actually look like in code. Um, and like I said, because this was kind of a drawing together of multiple systems that that work differently, there's multiple scopes, uh, scope macros here that all feed into the same system now. So the implementation of these macros all feed to the to the same place now. Um so around the renderer you'll see the scope drawer events that have been there for for a long time. Um and then in the lower left is the actual is the new breadcrumb macro that this all kind of feeds into. Um and that macro is capable of taking either an RHI command list or an RHI context. So you'll actually see some of these inside platform RHIs. Now um the example on the slide there is uh D3D12's defrag pass that happens at the beginning of the frame. um we can now label stuff that is specific to the platform RHI in the same breadcrumb system and then you'll get the markers everywhere you'll get them in insights you'll get them in pix and so on um and then the ones on the right render graph uh the one at the top the event scopes are the ones that you get by default with the events greater than or equal to one um and then the lower one the pass names are the ones that you'll see when you when you turn the full the full system on so this is a kind of quick overview of how the breadcrumb system is it works like the operation of it. Um, as we're recording RHI command lists, we build a tree of uh RHI breadcrumb nodes that refer to the to the actual breadcrumb names themselves. Um, and all of that code is crossplatform. Um, so it works works on all of our platforms. Um, and since RHI command lists are uh they can be recorded like with the mestreamer, they can be recorded on entirely separate threads that are separate from the render thread. Um each RHI command list itself uh builds a subree of nodes um as it walks in and out of the scopes and then at the dispatch time when uh when the dispatch thread acquires that command list for submission um it gets attached to the root tree which then means that those sub trees appear in the overall kind of overarching tree for the whole frame and we get the full context all the way up to the frame marker. Um and we also do by capture of the format args. Um, so the primary reason for this is that we want to be able to to capture all of the context like the there's a marker at the beginning of the frame which which is frame n where n is an integer. Um, and rather than just calling it frame and not having that that extra context, we capture the like the four bytes for the integer and the pointer to the to the format string. And then it means that we only have to do the formatting when we need the actual name, which uh for the most part if you're not actually running insights, if you're not running uh like a snapshot that frame of profile GPU, we don't do the string formatting. So it's a lot cheaper. Um so there's a handful of functions on the archite command list uh which you don't need to call directly but I'll just mention them here for um uh for completion sake. Um so uh begin and end uh breadcrumb g CPU and begin and end uh breadcrumb GPU. Uh the CPU functions are used for inserting the insights markers. This is how we get them into insights. Um it's also used for CPU crash reporting. This is something really cool now is that uh if you crash like in previous versions of the engine if you crash uh on a thread that is inside a render graph pass just a parallel uh pass generally the call stack doesn't give you much information um because the call stack just takes you back to some execute task that came from the the uh the task graph you have no idea what pass it was or anything like that unless uh it's particularly a problem for compute shader passes because comput shader passes uh there's like a general add shader pass function and you get no context from the from the call stack. Um so now when we crash we can dump into the crash report um the stack of breadcrumbs from the thread that crashed which gives you all that extra extra information. Likewise for the parallel translate if you crash inside the RHR you know where in the frame you came from and the CPU functions uh are the ones that build the tree. They're the ones that kind of do all all of the bookkeeping and everything. It's quite complicated because we have lots of different uh threading modes in the engine. So it might be quite hard to wrap your head around. It certainly took a while to write. Um but that's where all the bookkeeping is. Um and then the GPU functions are the ones that actually go and insert pix markers uh render dot markers and the the insights GPU markers that we see now. Um and we could also uh we also have a new system um as a result of this for GPU crash debugging. And so this is what our new GPU crash debugging system uh looks like. Uh you can turn this on with any of those options at the top there. um - GPU crash debugging- GPU breadcrumbs on the command line or the equivalent CRS which do the same thing R.GPUp crash debugging one R.GP crash debugging breadcrumbs one. Um the first one there is like a a larger features thing that turns on a lot of systems whereas the the lower one is just specifically turning on the system that you see on the slide. Um, and the way this works, uh, as we, uh, encounter breadcrumbs, as we're, as we're replaying them into the into R context to produce the GPU command lists, um, every time we enter a breadcrumb scope and exit a breadcrumb scope, we insert, uh, on D312, for example, we use write buffer immediate to insert, uh, an integer, right, into the command list, uh, stream and those integers get written to a buffer that persists even if the GPU crashes. There's some mechanism in Windows that allows you to get that data back. Um so what we can do is basically the GPU is is marking how far through the frame it got the whole time it's running and then on the RI interrupt thread when we see that the GPU has crashed um we can we get we get the device removed error we jump into the crash handler um and we can pull back those integer values and then look through the list of work that we know was in flight on the GPU that we haven't completed yet find the corresponding range of of markers and then dump that to the log. Um, and again, because we're multiGPU, multiqaware, you'll get one of these, one of these dumps per Q. Um, and so the example here was some particular, it's really handy. When I was putting these slides together, someone had a GPU crash in in in Lumen. Uh, so I just, I just stole the log and then could dump it into into here. Um, we do have a a debugg like test command, but it's a bit of a it's like a kind of a contrived example. You just see it's like GPU crash debug hang. That's the pass that crashed. Surprise, surprise. Um, so in this case, uh, it was something in Lumen that was crashing. Um, and the likely culprits are going to be the ones that are the most indented nodes that are still marked as active, which we we've highlighted in orange here. Um, but the one thing to to be aware of, oh yeah, so the and the the stuff that's highlighted in gray there, um, we know that we've not started that because the GPU hasn't the command processor hasn't emitted that break marker in submission order yet. So we know that's not the culprit. It's something before then. Um, one thing to be aware of is that some uh some hardware can report completion of work even though it's not actually completed. There can be like memory operations that are still in flight that might still cause a page fault on the GPU. Um, so some of those nodes there that are marked as finished, take that with a bit of a pinch of salt. Um, and maybe gather multiple repros and see if there's a pattern of um, whether or not like that that upper bound changes changes as well. Okay, so that's pretty much everything. I hope uh all the information here helps you guys with uh profiling your games and and and resolving all of your horrible uh CPU and GPU per bottlenecks. Uh, thank you very much and I'd like to open the floor to Q&A.