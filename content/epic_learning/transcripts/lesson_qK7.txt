In this video we are going to take a look at adding the seemingly small and yet very important option of a random seed control that will give both ourselves and any other user of the tool, the ability to randomise the results of the scatter tool while still maintaining a certain amount of predictability. Let's drag our Instanced Mesh's variable onto the graph area then choose the get option. What we need to know at this point is how many meshes have been selected for scattering by the user so that we know how many in turn to spawn into the scene. To do this we can drag from the output port, search for the length node and then use this to add a random integer in range from stream node and make sure that we are affecting its max and not its min value. To explain this means that we can now spawn meshes in a range that starts at 0 and goes all the way up to the maximum number that the user has chosen. Let's add our random seed variable then and pipe it into the stream input. We can now drag from the Instanced Mesh's variable again and add a get copy node. Just as a point of housekeeping here we may want to double click on the connection line and add some breaks in order to make how a flow a little neater. You can then make sure that the return value from our random integer node is fed into the float input of the get node. This will essentially select a random mesh from the stored array that can then be used to spawn a mesh in the scene. With our random seed created we have given the user the ability to add variation to the scatter result if it's not quite what they wanted first time around. And so we can move on now and look at creating the final part of our blueprint based scatter tool.