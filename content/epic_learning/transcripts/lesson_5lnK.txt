Now that our enemy character can safely navigate the level, it's time to add some more gameplay fundamentals to our game. In this video, we use Unreal Engine's built in damage system to give the enemy character the ability to kill the player character when they bump into them. Let's begin by setting up collision channels on the player character. What we don't want to happen is that the enemy character be blocked by the player character, as they'll end up pushing each other's capsules around. We're only interested in overlap events. For that reason, we'll adjust the collision presets on the player character collision capsule. In the content browser, navigate to content, 2D sidescrawler BP, Blueprints, and open the My 2D Player character in the Blueprint Editor. Select the capsule component from the Components panel on the left-hand side. In the Details panel on the right-hand side, locate the collision rollout, and then the collision presets drop down. Click the arrow to expand the collision presets rollout. From the drop down that is currently set to pawn, select Custom. We only need to make one change here. In the Object Responsive section, change pawn from Block to Overlap. By doing this, we'll prevent any strange physics collisions occurring in game when they meet, but they'll still generate the overlap event that we want to use. That's all we need to do to the player character, so let's compile, save, and close the Blueprint Editor window. Now, we can go ahead and use that overlap event to drive the rest of our logic. We'll do this by checking for an overlap between the player and enemy collision capsules, and applying damage when this occurs. Begin by opening My 2D Player character and opening the Event Graph. Click on the Captural component in the Components panel on the left-hand side. Navigate to Add Event and select the Event on Component Begin Overlap. This will add the new event to the Event Graph. Reposition this new event to give a space to work. This event will fire when anything overlaps with the capsule, but we're only interested in a specific overlap, an overlap between this object and the Captural component of the enemy character. You can use the other actor output of this event to check if it's the enemy character by attempting to cast to an enemy character. Drag from the other actor pin and locate cast to enemy character. If this cast is successful, it tells us that we hit some part of the enemy character, but this will also include the ledge checking volume we created, which isn't what we want. So let's go ahead and make another check to make sure that we hit the root component of the enemy character, which will be the Captural component. By looking at the details panel of our character, we can see in Hierarchy that the capsule component is the root component, to which all other components are parented. As we created the enemy character from this character, we know the hierarchy is the same. According to our enemy character cast, drag once more from the other actor pin and type get root component and add the node. Drag from the root component output pin of this node and type the equals key to locate the is equal to object node. Now drag from the other comp of the on component begin overlap node and connect it to the empty input pin of the equals node. Four things get too messy, let's add two re-root nodes this connection. Let these nodes and position them so the lines no longer overlap. Now let's use the output of this node to drive some logic. Create a branch node nearby by holding B and left clicking into the graph. Connect the return value from the is equal to node into the branch. We have now created a branch that will run the true execution path if the component we overlap is the root component. Now we just need to go ahead and connect the execution pin of this branch that occurred just before the cast enemy character. Connect the true execution pin into the input of the cast enemy character node. Old and left click on the component begin overlap event to remove the current execution wire. And then connect the execution wire between component begin overlap and un-unilicreated branch. Once again it's worth taking some time to use some re-root nodes to stop the wires overlapping. Now we have created the logic that will test if the player character has overlapped the enemy character and they have overlapped with the root component of that character. That is to say the capsule component. All this left to do now is apply some damage when this occurs and luckily Unreal Engine has built-in functionality we can use for this purpose. Because we want to selectively apply damage depending on the player state we combine the logic we've created on the player character with some new functions on the enemy character to actually apply this damage. For now let's assume that whatever is executed following a successful cast enemy character this location will cause damage to the player. Let's close my 2D player character and return to the main editor window. Locate the enemy character blueprint and double click to open it in the blueprint editor. Now click in the event graph and create a new custom event called Apply Bump Damage. With the Apply Bump Damage custom event still selected, click the plus sign in the inputs rollout of the details panel on the right hand side to add a new parameter. Name this new parameter Player and change the variable type to My2D Player Character. Now anytime this event is fired we can have a reference to the player character. Drag from the execution pin of the event and create an Apply Damage function. Now we can drag from the player output pin of the event and connect it to the damage actor input of the applied damage node. This means that the damage we are about to configure will be applied to the specified player character. Currently we can see in the applied damage function that the base damage is set to zero. We all want to raise that to something more sensible and we also want to make this a variable so we can control the amount of damage. Drag from this base damage pin into the event graph and select Promote to Variable. Name this new variable Bump Damage. We want to change the default value for this variable so compile the blueprint. Now in the details panel on the right hand side in the default value rollout change the bump damage from zero to 50. This is also good time to position the variable in the event graph so it doesn't overlap with the existing lines. Now let's go ahead and specify the event instigator and the damage cause for the applied damage function. Let's start with the event instigator. The event instigator refers to the controller responsible for the damage which in this case will be the AI controller. In order to get that reference right click on the graph and create a get controller function to get a reference to the AI controller. Drag from the return value of this function into the event instigator pin. Now let's do something similar for the damage cause. This refers to the actor that calls the damage. We can easily provide this reference by getting a self reference in the event graph and connecting it to the damage cause input. This worth once again taking a moment to reposition all the nodes so that the lines don't overlap. Once you're happy compile save and return to the my2d player character blueprint. Let's return to the blueprint logic we were working on before specifically the on component begin overlap section. Looking on the right hand side we still have our cast to enemy character mode. If we remember what we want to do here is apply damage to the player character if we can successfully cast to enemy character. In order to do this we're going to want to call the apply bump damage function we just made for the enemy character. We can do this by dragging from the as enemy character pin and locating the apply bump damage. Now we just need to provide the player reference for this function. We can easily do that by dragging from the player pin and locating the get reference to self node. This is also a good opportunity to marquee select the nodes and position them so that they have enough space. This just makes everything a little easier to read. So let's just see what we've done here. When all of the conditions are met and we're able to cast to the enemy character the enemy character will damage the player via the apply bump damage function. With that set up we're now checking for the correct conditions and sending damage to the player character but we're not actually doing anything practical with the information. So let's start with something simple by simply killing and respawning the player character if they take any damage. In the My2D player character event graph right click and create a new custom event and call it respawn. This simplified respawn event will just place the player back to the player start. And on we could use this event to support more complicated interactions so it's a good practice to set up the execution flow now. The first step will be to get the location of the player start within the level. Drag from the custom event and locate the get all actors of class function. In the actor class drop down select player start. This function will return an array of all the actors that match that class. All we know that our level only has a single player start. It's a good idea to get in the habit of confirming things like this before carrying out subsequent actions. For example let's confirm that we actually get a result from the get all actors of class function. Drag from the out actors array and select the length node. Drag from the integer return value and select the integer greater than integer node. We can leave the second value at the default of zero. The Boolean return value of this function will return true if the length of the array is greater than zero. That is to say we actually managed to find at least one actor that matched the actor class we were looking for. As all wants of very our execution depending on the output of this ball let's use a branch node. Hold B on the keyboard and click in the event graph to create it. Before we forget let's hook up the execution pin between get actors of class and the branch. It'll be pretty embarrassing to forget this and wonder why our code isn't working. Next let's connect the Boolean return value to our branch. Now let's think about what this branch is going to do. Anything executed from the true execution pin of this branch can safely assume there is at least one player start in the level. And you know what? That's good enough for now. What we'll want to do is take the first result from the out actors array and then find its location. Returning to the get all actors of class function drag from the out actors array. Look for get a copy. We want to drag this new node out beyond the branch and use some reroot nodes to keep things tidy. Returning to this get node we can leave the integer value as zero meaning we'll always get the first result from the array. So the output of this get will have the actor that we want. Now we just need to get that actors location. So drag any output of the node and locate the get actor location node. This function return value is a vector representing the location of the actor. Now we need to do is teleport or play a character to this location. We can do this by dragging from the return value and locating the set actor location node. You can see that hopefully the default target is self. That means we'll move the player character to this location. Make sure to set the teleport Boolean to true by hitting the checkbox in the set actor location node. We do this to maintain the physics state between the start and end location as we can see in the tooltip. Finally, all that's left to do is connect the true execution pin of the branch into the set actor location node. When everything else is set up, we can use a marquee select and move the other nodes to prevent them from overlapping. And with that, we have our respawn function set up correctly. So let's take this opportunity to compile and save to Blueprint. Now we have the respawn function set up. We just need to call it. For our first pass, let's assume that if we take any damage at all, the player character will be killed and need to respawn. We can always add more complexity to this later. Fortunately for us, Unreal Engine provides the perfect note for this. Right-click on the event graph and create event any damage. This does exactly what you'd expect with a bunch of helpful outputs. However, for right now, let's not worry about those outputs. We can just use the execution pin. Drag for the execution pin of event any damage and locate our respawn function. Now, whenever the character receives any damage, the respawn function will be called and the player will be repositioned at the player start within the level. Let's compile, save, and move over to the main viewport to test this. Before we start a plane editor session, let's position a player character up on the left hand sign the platform so we're not instantly killed. Once that's done, let's start a plane editor session and see what happens. So there we go, I think that's significant progress. While it doesn't look like much in the game, we've hooked up some fairly complicated systems that will be flexible for future use. Player character is using the damage system and when they take any damage, they're reset to the player start. That's progress. In this video, we gave the enemy character the ability to damage the player. We began by configuring the collision capsule on the player capsule to only generate overlap events when colliding with pawns. We then used this overlap event to check if we had overlapped with the enemy collision capsule and if so, apply Unreal Engine's built-in damage system. We then used the any damage event on the player character to trigger a simple respawn function, which would teleport the player back to the player start position within the level. We then confirmed this was all working by starting a play in editor session and observing the results.