Hello everyone, I'm Deguri and I will be your instructor for this gameplay ability system lecture. This is the 7th lecture in the gameplay ability system series. In this session, we will build the foundation of a combat system using meta attributes. Here are the learning objectives for today's lecture. Learn the concept of meta attributes and how to implement them within an attribute set. Create a basic combat system with a modifiable attack stats. Learn how to implement on NPC's counter attack by reusing on existing gameplay ability. Before we begin, let me show you the reserved video for the example we'll be working on today. In this lecture, we'll implement a system where when you attack on NPC, the NPC counter attacks the player. If the player dies from this, the level will restart. Here are the key components we'll be covering in this lecture. We'll enhance the attribute set we covered in the previous session. In this lecture, we will use the cyclical structure we've learned so far to strengthen the system as a whole. Meta attributes are designed adapt flexibly to a variety of situations. Unlike regular attributes, meta attributes are used temporarily and only once. A typical example is the damage attribute. In the previous lecture, we reduced the health via fixed value, but there are actually many ways to reduce health. For example, if the opponent has a shield, you should subtract the remaining damage after the shield value is applied to health. If the opponent is in an invincible state, any amount of damage should be notified. Starting on the relationship between the attacker and the target, meta attributes are useful for implementing these diverse scenarios. Now let's move to our practice project to create a meta attribute. I have prepared the example 6 35 in advance for today's exercise. When unzipping it, I will rename the folder to example 6. Now let's proceed with the basic setup for the exercise. Shift right click on the project file and select the Generate Visual Studio Project Files menu. Generate the solution file, double click to open the solution. Select that a startup project is set to Arena Petal Gas. Build a project. Once the build is finished, run the editor. For this lecture as well, I duplicate the level created in the previous lecture. Name this step 6 and set it as the default level. This time we will configure a DemiG meta attribute in the project to build a more structured combat system. To do this, go to the character attribute set class and add a DemiG attribute. In Visual Studio, open the AB Character Attribute Set.h header. Add the DemiG attribute. Ideally, we should also add a Max DemiG attribute to manage the maximum damage. But since we already did this in the previous lecture, for this example, we will skip it and just declare the DemiG attribute. From now on, we will refactor the system so that the character's health reduction is done through damage. The most suitable function to implement this is post gameplay effect execute, which we discussed in the previous lecture. First, use the macro to declare the basic functions for damage. Then in the definition. Just check whether the attribute triggered by the gameplay effect is damaged. Here, we will implement logic to reduce the health using damage. There can be various situations when applying damage. For example, if I show this active or if the target is invincible, the damage could be nullified. To handle these cases, create a variable, apply the damage and initialize it to zero. This will store the actual damage applied. Conversely, create a remaining damage variable of the damage to be applied and assign the full damage value to it for now. Get the Crunch Health value. Apply the damage to calculate and set the new health value. Since that health changes the base value, the pre-attribute to change function will be called which automatically performs range validation and correction. Finally, store the total applied damage in applied damage variable. Reset the meta attribute value to zero. Since we modified the header, close the editor for safety, build in Visual Studio and rerount the editor. Once the editor is open, create a new gameplay effect to apply damage. Create a blueprint. Name it BPGE Attack Damage. To make editing easier, open the blueprint, close it immediately, then reopen it so the default value editor is larger. Add a modifier. Select a newly declared damage attribute. Set the modifier OP to override. So the assigned value completely replaces the base value and the 40 as the value. Set the finish in the setup, save and close. Now, go to the Attackic gameplay ability. Change the effect class to BPGE Attack Damage. Click Play to test it. You should see that health decreases by 40 each time you attack. Next, let's build the most structured combat system for character battles. Create a new attribute to set C++ class. Name it AB Combat Attribute to Set. Once the class is created and the build is done, go to Visual Studio. Click Reload O. Open the AB Combat Attribute to Set.h header file. This attribute to Set will be used for defined characters' combat related stats. Declare the constructor and add the attributes. First, add the Attack Rate attribute. Then add Attack Range and Attack Radius, which you will use the Boturget Detection in the target actor. Ideally, we would also add Max Attributes for these values, but since we will use the values as is in this lecture, we will keep it simple in the definition. Select the default values to 40, 150 respectively. Since these attribute values will not change during gameplay, we will not add Macros for them. Compile with Light Coding. Once compiled, go to BPG Attack Damage. Change the Damage Value to be linked to the Attack Rate attribute. Change the Scale of Float to Attribute Base. In the Backing Attribute, select Attack Rate. This should result in the same value of 40 being used. For this to work, we must first grant this combat attribute to Set to the Character. In Visual Studio, open ABBASECharacter.h. Similar to Character Attribute to Set, declare a combat attribute to Set. In the definition, include the Head of Fire. Use Create the Fort Sub object to create the Combat Attribute Set object. Then register it to the ASC. Compile again with Light Coding. Once compiled, click Play to check the Research. In the Debog window on the left, you should see the New Combat Attribute Set attribute. You should also see that houses reduce according to the Attack Rate attribute value. Now go to the Target Actor and modify it to the Detection Range is also based on Attributes rather than HardCoded values. First, we need to get the values of 100 from the Combat Attribute to Set. Just get Attribute to Set to retrieve the Combat Attribute Set. Get Attack Radius from it. Click the Struct into 2 values and assign the Current Value. Since the parameters are messy, copy the Parameter node on the right for a Cleaner Setup. Do the same for Attack Radius' Current Value. Oh, this should be Attack Range, not Attack Radius. Fix it. Now, it's implemented via the Attribute to Set instead of HardCoding. Since we set the same initial values, clicking Play will show it works the same way. There's no visible change yet, but now we can freely change the Attack Behavior via Gameplay Effect. Now let's add Functionality so that NPCs can attack the Player. If an NPC takes damage from someone, it will immediately counterattack the target to do this. We'll implement or delegate the Notifies when damage is received. In Visual Studio, open the Character Attribute to Set Class that handles damage. Add or Delegate. Use the Clear Dynamic Multicast Delegate. Since we need to output both the Attacking Actor and the Damage Received, use two Params. Define the Delegate Type as App Damage Applied. And have we take Actor Pointer in Instigator and Float Applied Damage. We clear the OnDemage Applied Delegate of Type App Damage Applied. Use the same U-Proport Settings as before. In the Definition, broadcast the OnDemage Applied Delegate before resetting the Damage. Save the Additor with a Moment and Compile. Once the build succeeds, we open the Editor. First, go to the NPC Blueprint. This is the Logic for Handling Desk. For now, let's move it down a bit. From the Character Attribute to Set, drag out and search for bind. Then use OnDemage Applied bind. Create an Event named OnDemage Applied BP linked to the OnDemage Applied Delegate. Here, display the attacker and damage received. Add a Print String node. Add an Append node and two pins. Enter God and Damage From. Link the Respective Information. By using the Get Display and Function, we can display both the Damage Value we received and the Information about the attacker. Click Play to check the Resert. Then go back to the NPC and add Functionality so it immediately counterattacks when taking damage. Since we already have the Attack Ability, simply activate the Gameplay Ability just like on the Player. Use Try Activate Ability by Tag with Character that Ability that Attack Gameplay Tag. Since our Tags have been forward, rotate the NPC to Face the Player. Use Set X Rotation node to Set the Rotation. Use the Positions of yourself and your attacker. Since Vector B-A, point from A to B and PC should be A and the target B. Once this forward vector is created, connect it to the Make Rotax node to generate a Rotation Value. Here X refers to the Forward direction. This will rotate the NPC toward the Player then Attack. Click Play to Test it. The Attack works, but NPC attacks aren't hitting the Player. This is due to Target Actor Settings. By default, Target Actors only work for the Player Control Character, so we need to adjust the Settings for NPC Target Actor Activation. Go back to the Visual Studio. In the Parent class, there is a Should Produce Target Data on Server, Variable. It is set to Force by default. In the Constructor, set this to True. Now Target Actors can activate 10 Applied Images even for Non-Player Kickters. Compile with Live Coding and Re-Lunch. Now NPC Attacks apply Collected Actors to the Player. Next, let's restart the game when the Player dies. But currently, the Player always wins, so we can't easily test the Player Death. On the Player, in AB Initialize Component, Create an Event bound to the Out-Over-House Delegate. Add the Death Ability to the Player. Activate it. Wait 5 seconds. Then execute the Restart Level command. Now, when the Player dies, the Level will restart. Also, on the Player, Create an Event bound to Own Demigie Applied. Copy the NPC's Print String logic here. Next, Increase the NPC's Attack Power. Go to the G-Eep folder. Create a Self-Applied Effect. Call the BPGE Attack Up. Go to the G-Eep folder. After creating this simple effect, in the NPC's Begin Play Event, use Applied Gameplay Effect Self. And assign BPGE Attack Up. This will increase Attack Power right from the start. Testing should show 100 damage being applied correctly. One thing we missed. On the Player, add the Death Ability to the Initial Ability List. This shows how gameplay effects can be used to set Initial Attribute Values. Player should now fall. Then Restart after 5 seconds. This completes the prepared example. To wrap up, close the Editor. Hit Ripley, then Compile. This concludes the hand-john session for today. Let's summarize what we've learned in today's lecture. Meta Attribute can be used to implement various combat systems. Attribute to Set can be grouped and managed by category. Additional setup is required to trigger target actors from NPCs. Changes to stats from Initial Setup or Leveling can be easily applied to the character using gameplay effects. Try using the provided Step 6 level to practice implementing different meta attributes. That concludes today's lecture. I hope you look forward to the next one. This has been Tuku Rii. Thank you. Bye bye.