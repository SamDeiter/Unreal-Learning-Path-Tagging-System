When we initially load a data Smith file into Unreal Engine using a data prep graph, we haven't actually imported the content into our project yet. We need to execute the graph and commit. Let's walk through how to do that now before we make changes to the data prep recipe. At this point, we've imported the data Smith file into the data prep graph. You may notice that Unreal Engine has automatically begun to compile shaders as indicated here in the bottom right corner of the screen. Again, this panel on the left shows all of the content in the data Smith file, geometry, materials, textures, and so on. And this panel to the right of the viewport shows the hierarchy. All of this content from the data Smith file has not yet been added to the project. If you look at the viewport and content browser for the project, these are still as empty as they were previously. The execute button in the top left of the data prep window will take all the data Smith information, run it through the data prep recipe at the bottom, and show those changes in the viewport and hierarchy panel. Currently, we're not modifying the data Smith file in any way, so if we hit execute, we'll see that nothing actually happens. As soon as we hit commit, all of the information we have processed in this window will be committed to our Unreal Engine project and the assets will be added to the viewport and the content browser. I'll do that now. Depending on how large and complex your data Smith file is, this may take a few minutes. Because of how Unreal Engine imports and overrides information, we're able to iterate and repeatedly re-import content as our project evolves. If our 3DS Max file were to change, we could just re-export the data Smith file and be sure to overwrite the previous one. In the data prep asset, choose import, execute if you have any recipes in the graph, and commit. This will update all of our content in the viewport and the content browser, but keep in mind, your shaders may need to recompile in the background again. Now that we have a good understanding of what a data prep asset is, how to import, commit, and re-import, let's add some functions to our data prep recipe. In the next module, we'll use the recipe to replace materials on imported geometry.