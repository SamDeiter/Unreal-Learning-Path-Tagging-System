In this module, we will create a function library. A function library is an incredibly powerful tool, but you can use to store functions which you need a lot during development or in your project specifically. This performance is friendly if used correctly and can save you a lot of headaches and time. Time is something that we all need more of in development. Let's create our first blueprint function library. It's a library with functions that you can call anywhere in your project. So it's incredibly useful. We're going to go into Blueprints and I'm going to create a new folder and I call that Libraries. I'm going to open that up and I'm going to write mouse button, go to Blueprints and then click to print function library. I call this BPL for Blueprint function library and I'll call these traces because here I'll store the traces that I commonly use. We're going to start off by creating a camera trace. So a trace from the camera. I'm going to be looking for a sphere trace by channel and I prefer to use a sphere trace by channel as opposed to the line trace by channel. A line trace is always going to be a line as sphere. You can say what the radius of the sphere should be so you don't always have to be perfectly precise. You could always tweak that radius a little bit. So I'm going to do a sphere trace by channel. We have our normal sphere trace by channel. We can do that, get play a camera manager again and get the world location. We will get that transform components. Now, I don't know if you remember from last time but we need the forward vector. What direction is the camera looking at? We want to multiply this with a float. We get the direction that we're looking in and we want this trace to go a certain distance that will be that float multiplier. We'll add that to the function. We can just drag it on there and it will add an input function and we'll call this distance. And now we have that forward vector multiplied by distance. We need to add that to the world location to make sure it's done in the right location of the world and this will be our endpoint and the world location of our camera is going to be the starting point. Now the radius is also something that we'll want to tweak so we'll drag in the radius. Let's set some default values for these. The default value for distance, I'll go with 5,000. The default value for radius, we can go with one. We don't need the very wide sphere trace to begin with and something that might be nice to add as well is a draw deep up type so that we can change the deep up when we're calling this event. Now we also want to have an output so I'm going to click the new parameter and drag over the return here and connect the out hit with the return node. It will automatically create that new variable output. I'll delete the boolean. We don't need that. We just need the out hit and we're done. Of course I recommend commenting this. Maybe straighten out some of the lines but basically we have our first function inside of a function library. Now just let's just open up some things. Let's go to generic actor or generic interaction. Now let's look for camera trace. There we go. Camera trace. It's a function that we can call here. We can call in the level blueprint. You can now simply call camera trace anywhere in your project. So if you have a few of these functions that you'll use a lot, I recommend putting them into function libraries but watch out with references. Always be careful with references so that means costing or having an input or an output be a reference to an asset because the more references you have, the bigger the size map of something will be. The more memory has to load when you start your game. References is a bit more often at font stoppick so I won't go too much in depth into this but just watch out. Don't do too many cost in a function in a function library and try not to have very specific actors as either an input or an output in a function library. We just created our first function library with a function that we can now call anywhere in the project. In some cases, you want to create logic that you want to call in multiple places but it won't fit into a function. In this case, you can use a macro library which is what we'll have a look at in the next module.