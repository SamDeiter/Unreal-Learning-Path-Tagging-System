In this module, we'll learn about using an array to keep track of all the possible blocks on the game. By doing so, the player will have a practically infinite number of blocks at their disposal to build with. Our objectives for this module are to use an array to keep track of the available blocks in the game. We'll learn how to access an array correctly, taking precautions to avoid accessing the array with an out-of-bounds index. We'll switch the active block using inputs from the mouse wheel. To get started, go to the BP block player and open it for editing. Click the Add Variable button and change the new variable name to All Blocks. In the Details panel, change the variable type from Boolean to BP underscore Base Block and make sure that you choose Class Reference. Call that a Class Reference keeps track of types of things while an object reference keeps track of references to actual things, or in other words, instances of things. Since we're using an array of BP Base Block, any blocks we create that our children of the BP Base Block can be in this array. The power of inheritance is once again allowing us to write code using general descriptions, Base blocks in this case, that will work seamlessly with the specialized child classes in our hierarchy. Just to the right of the variable type, click the menu and select Array. Then click Compile and Save. An array of class references in this case allows us to add as many elements that we need to the array, all under the name All Blocks. Just to the right of All Blocks, it will tell you how many elements are in the array, right now there's zero. Click the plus sign two or three times and then just below where we added the array elements, you'll see the index is zero, one and two of the elements that we just added. The class references set to none, click that drop down and you'll see that we can now choose all of the BP blocks or the blueprint blocks that we created that are child classes of the BP Base Block. The zero with element, I'm going to choose BP Base Block, but for the first element, I'm going to choose the dirt block and for the next element, I'll choose the grass block and I've got several more elements to add, so I'll just click Add Element and then after the grass block, I'll add the brick block and finally the wood block. The default value gives us a nice picture of the array and I want to point out a couple of attributes of arrays, there are five elements in this array, but the last index in the array is four because arrays are index starting at index zero, so the zero with index is the first index in the array. Click Compile and Save. Going to need some variables to support changing the block using the all blocks index, click the Add Variable button and name this variable Current Block Index. Over in the details, change the type to an integer. Just to the right of the variable type, we'll change that to a single variable, so it's a single integer value and click Compile. The default value of the Current Block Index is zero and that's just what we need. Next, find the Begin Play event and I notice that I set the comment to Begin Player, which is somewhat true, but I want to change that, I'm going to select that, press F2 and Begin Play. So in the Begin Play event, we need to find out is what the last index is of the all blocks array. To do that, get the all blocks array and then drag out of the all blocks array and type in Last Index and that returns an integer that is the last index of the array. We click on the return value of that and promote it to a variable and call that the last block index. And the reason we're making a variable for this is because we'll need it a few times and we can avoid writing this code several times by just making a variable for it. Now, the last index, I'm going to click on all blocks to demonstrate that again. The last index is four in this case, but if we were to add more elements, add more blocks, that number would increase. We won't have to change this code because this will pull the last index from the all blocks array when the event Begin Play starts. Next we need to add inputs so that we can use the mouse wheel to change the Current Block. Select the Edit menu and go to the Project Settings and then select input from the list on the left. Going to add two action mappings, click the plus next to action mappings, two times, go to the first one, you action mappings zero and we're going to name this select next block. And for that, we will search for mouse wheel down. The next one, we'll call that select VBS lock and we will search for mouse wheel up. Close the project settings find an open area on the blueprint to start writing some code. Add the two inputs that we just added, so right click and type in select VBS block and right click and type in select next block. We'll be adjusting the current block index. When we select the previous block, we'll be decimating it or reducing its value by one and when we select the next block, we'll be incrementing it by one. So let's drag out the current block index and get it. We'll start with the select previous block. Drag off of the current block index and type in minus minus for decrement int. We'll reduce the value of the current block index by one. We have to be careful because if it becomes less than zero, that would be an out of bounds per a access. So we need to test and see if it has become less than zero. Drag off of the result of the decrement operator and type in greater than and select greater than or equal to. And it's already got zero for the other operand. Add a branch, hold on the B key, left click, hook that up, hook up the condition. If the current index is not greater than or equal to zero, it's less than zero. So this condition will be false. If that's the case, we have to set the current block index to the last block index. So drag out a set for current block index, hook that into the false wire for the branch. And the value we're going to set the current block index to is the last block index to a get on that and set that. If the current block index is greater than or equal to zero, then the true wire will run and we can set the current block to that index. So to do that, we're going to drag out the current block variable that we previously created. We were changing using the number keys and we're going to drag do a get on the all blocks array and drag off of the all blocks array. Type in yet. And the get. No needs to know which index we're going to get, which is going to be. The current block index. So do a get on that and hook that up. And then set the current block. To the value retrieve from the array and hook that up here. And here. So if it's true, we don't need to adjust the current block index. And if the current block index became less than zero, we set it to the last index. And we should be able to change our current block with the mouse wheel, but it's only going in one direction right now. So the code to go in the other direction is very similar. We'll drag off of the current block index. Type in plus plus. Or increment hint. Normal test. The result of that if it's less than or equal to the last block index. Drag out the last block index. Get that. And that will be our condition whether or not adding one to this has left it less than or equal to the last block index. So we need a branch. The. Up, hook up the condition. So this over to the side a little bit. So if this is false, we want to wrap around to zero. So set the current block index to zero. This condition is false. And if it's true, we're good to go. We're within the range of block indexes. So all we need to do is hook up the true. To set the current block, we're treating the current block index. And if it was false, we set the current block index to zero. And hook that up. Let's compile and save and test it out and see if we can change the current block with our mouse wheel. Oh, that's the BP base block. I'm moving the mouse wheel forward. Not far enough. Here we go. So those are the previous blocks and then going towards me. I've got the dark block. Pull it again, grass block. Just the brick. More brick and back to the wood. Roll it one more time towards me and back to the base block. And when you test this, make sure you test in both directions to ensure that your code is working correctly. In this module, we added an array to keep track of available blocks in the game. We accessed the array correctly and avoided accessing the array with an out of bounds index. We switched the active blocks using inputs from the mouse wheel.