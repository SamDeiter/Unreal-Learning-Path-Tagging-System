Sometimes an event might happen, and many things might want to know about it. For example, we're ending our level, or players died. We could create a section of code in our project that we designate to handle that, and then anything we might want to happen can be put in that one place. This however creates hard connections. We might not want those, as well as having code that should be in one location in another due to this one place handling everything. Rather than doing this, we can use an event dispatcher. It handles when the event happens, and then lets everything else handle that event in a more appropriate place. You can think of this as like a list that can be subscribed to. We have an event, say the player, dying, anything subscribed to that event, or the list, will be notified when it happens, and then those items can take care of those things individually. We're getting near the end of our level, and what we need now is three collectables to be collected, and then the final door open. As we collect those items, we're also going to have a user interface event that's going to update to show us how many are left. Let's go into our GM underscore BP communication. It's our game mode blueprint in our Blueprints folder. This is the data only blueprint, so I'll open it in the full editor. Inside of our game mode, we've got a few things set up already. We have two events. One when the collectable is spawned in, it will register itself, and update our collectables left variable. And another once we collect the collectable, it'll go ahead and remove one from our collectables left. The collectables left is simply an integer variable that will go up or down based on when we register them or collect them. Going back into our starting map, we open up the BP underscore collectable. We can see this is set up in a fairly simple way, using some of the methods we've learned already. We can see here our begin play events after a short delay, enough time to give our world to spawn in. We get our game mode, cast it to the correct type, and then run the collectable register. So for every collectable in our world, we're going to increment that number by one on our overlap event, which is our sphere. When we walk over collectable, we're checking if the other actor is our character class. And if it is, we then get the game mode again, cast it to the correct type, and call the collectable collected. So it will remove one from our collectable count. I'm going to go ahead and close our collectable. Going back into our BP communication, let's see about creating our event dispatcher. We'll create an event dispatcher. We'll call this collectable count updated. Now we want this collectable count updated to be called whenever we register or collect an item. And if we drag this out, we're going to choose call. When this event is ran, anything that is registered or listening to the collectable count updated dispatcher will be notified. We'll go ahead and hook this up. We'll duplicate this node with control D. We'll hook it up to the top one. Now we have our two events set up. Now we actually need to do something when the call collectable count is updated. Let's go ahead and quickly set up a test. Drag out our collectable count updated event dispatcher and we'll do an event. Whenever this is called, we're going to do something. Later we'll use this to check and see if we've collected all of the collectables, but for now we'll simply print out the current value. We'll use a print string. We'll grab our collectables left and get it and plug it in. Now let's go ahead and test. We'll compile and save. Go back to our starting map, move to one of these locations and then hit play. Well nothing showed up in our print string. We can even go and collect an item and nothing's happening. We implemented the event, but why is nothing happening? The reason for that is we haven't actually bound or added ourselves to the list for this event. This is the event we have set up that we want to call whenever collectable count updated is triggered. But we're not actually listening to that. We haven't assigned ourselves to it. We haven't bound it. If we drag out collectable count updated and we look at binding or assigning, binding is what we call and we hook up an event in order to subscribe to the list. If we drag off again and we do assign, you'll notice it basically handles that for us. It gives us our binding and creates our custom event. Let's go ahead and delete this. We need to go ahead and make sure that we set up our binding. Even though this event dispatcher exists in this blueprint, it's still more of like a signature or like your interface. You still need to do something with it. We'll hook up our bind event to our begin play. With this set up, when the blueprint starts, we will bind an event, which is this custom event here we've created, to the collectable count updated event dispatcher. So whenever something calls it, these two items, for example, then this will happen. Let's go ahead and compile and save and then test again. And after one second, it should show one, two and three because our collectables are now registering properly and when we collect them, it's going to go ahead and go down. Now this is good and all, but we still need to be able to actually hook in something else. So let's go to our user interface, go back to our map. In the UMG folder, I have UMG underscore player UI. Now, if we go ahead and take a look at this, it's fairly simple. We have some text showing us the collectables left and inside of our graph, we have nothing. We want to update this variable whenever we have something collected in the game mode. So we will register and listen in, grab our variable and get it. We're going to go ahead and call a set text. We're going to set text text. Now we want this text to be set whenever a collectable is updated and that's in our game mode. So let's go ahead and get the game mode. We'll go ahead and cast that to our correct one. And then we want whenever the collectable update is called to trigger the set text. We can call that event directly, but that's not what we want. We want to listen in whenever it is triggered. So this is where our assigned or our bind is set up. So let's go ahead and assign. You'll see it's gone ahead and created our bind event. And our event that's going to be called. We'll go and move this over and hook this up to our set text, clean this up a little bit. Go back to the beginning and type in construct. And whenever this widget is now created, it's going to go and bind itself to the collectable count updated and then we'll update the text. Now where are we going to get that information from? This is an issue. We have no way of updating this variable. That variable is inside of our game mode. Well, it's not that hard. We'll go back to our event dispatcher and we'll go ahead and add in a new variable. At the top under inputs, we can do a new parameter. We'll call this one the count. It's going to be an integer type and we'll go ahead and compile and save so the signature is updated. And now whenever we use our collectable count updated whenever we call it. So our two events here, we can go ahead and pass along the new value. We can hook it up from our decrement and increment or just dragging our variable. And now the count will be passed along. We'll go back to our user interface inside of our event from our binding. You can now see it's updated with the count. You can drag this into the input text and let it convert. Let's go ahead and compile and save. We're still not actually adding that user interface to our screen. I'm going to go and put it in the game mode to make it simple. After we have bound to this event here, we're going to go ahead and create and display our user interface. So we'll drag off and do create widget. Make sure we set it to our UMG player user interface and then off the return value add to viewport. Let's go ahead and compile and save. And then let's go back into our map and hit play. It shows zero for a short amount of time in the bottom left and then goes up to three as our collectibles register. When we walk over collectible, it updates itself appropriately to two and then one and then if we walk over the last one, we now have zero. Event dispatchers can be very helpful when you want to have items trying to manage themselves rather than having one place to handle it all. Imagine if you had one blueprint that handled all the code that needs to happen on every frame rather than the way it is handled now. The engine letting the individual objects handle their own tick event. Our next step is going to be implementing our final door. We're actually going to see how we'd implement it using all of our previous methods and then you'll be introduced to one last way to find items and initiate communication.