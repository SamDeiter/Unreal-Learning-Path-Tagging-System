Now that our AI character CAN fire a projectile, we should return to our controller logic to decide if they should fire a projectile. Using the content browser, navigate to content, 2D size scroller BP, Blueprints, Enemies, and open the enemy AI Blueprint. Locate the do behavior event using the Event Graph rollout in my Blueprint panel. Let's quickly refresh our memory about what we've set up here. We've set up the false branch. The false branch is to when the AI is not alerted. In this case, they simply continue to move in the direction that they are facing as before. Currently, we don't do anything if they are actually alerted, so that's why we can start to set things up. Once we know that they're alerted, we want to see if they're actually able to make an attack. We should go ahead and clarify exactly what we mean by able to make an attack, by creating a pure function to drive a subsequent branch. Create any function and name it CAN perform attack. With the function open, tick the pure checkbox in the Graph rollout in the Details panel and the right-hand side. While we're open the Details panel, let's set an output using the plus icon in the Outputs rollout and name it SHOULD attack. Make sure that this new output is of the Boolean variable type. When we add the output, this is automatically added to the return node and the two are connected together. However, we'll be adding more logic between these two nodes, so ALT, LEFT-CLICK on either pin to disconnect the wire. Go ahead and move the return node off to the right a little bit in the Graph to give ourselves a bit more space to work. Before carrying out any more advanced checks, we should sign up to check that we still have a valid target at this point. Control drag the player target variable into the Graph from the MyBluePrint panel to get a reference to it. Drag from the output of this node and locate the IsValid node. This time, we want the version with the question mark rather than the pure version as we actually need the execution pins. Now we can connect this into the existing logic. Connect the execution pin of the function into the execution pin of the IsValid node. Connect the IsValid execution pin to the return node. Now, let's handle the other possible outcome of this check. Select the return node of the function and make a copy of it by pressing Ctrl C and Ctrl V to copy paste. Connect this new node to the IsNotValid execution pin. Following the IsValid path, should attack should be set to true, so let's check the box in the return node to set the should-attack Boolean to true. If the player target is not valid, we shouldn't attack. That is to say, the should-attack Boolean should be left set to false. So let's return to when it is valid. While this is a good start, we definitely need to carry out some more check before we can be confident that the AI can attack. But what about when it is valid? We definitely need to carry out some more checks to be confident that the AI should attack. The first check we should carry out is to determine if the AI is currently facing towards the target. If it's not, I'm pretty sure it's going to miss. Let's create another function to determine if the AI is facing the target. Specifically, we'll call this function is facing target. Create the new function and move over to the details panel and ensure that the pure checkbox is ticked in the graph rollout. Additionally, add a Boolean output called should attack. Let's begin by adding the same player target validity check as we've used before. Drag from the execution pin of the function and locate the IsFalid node with the question mark. Call Let's click drag a reference to the player target and connect it to the input object of the node. Copy and paste the return node for the function and connect it to the execution wire from the IsNotFalletPim of the node. Let's focus on the IsFalid path for now. Let's use a quick bit of vector maths to determine if the AI is facing towards the player. The first step will be to get the location of both the player and the enemy character. Control left click drag a reference for the enemy character and player target from the MyPlyPrintPanel into the graph. The first step will be to get the location of both the player and enemy character. Place the player target variable above the enemy character and drag out from the player target variable and locate get actor location. Click out from the return value of this node and locate vector minus vector. Now drag from the enemy character variable and locate get actor location. Connect the output from this node into the lower input of the vector minus vector node. In a 2D game, we only need to care about the x component of the vector, so let's break it open and extract what we need. Drag from the output of the vector minus vector node and locate break vector. Drag from the x output of the break vector node and locate sine float. The return value from this function forms the first half of our comparison. For the other half, we'll need the forward vector of the enemy character. Copy and paste the reference to the enemy character in the graph to get a fresh reference. Drag from the variable and locate get actor forward vector. Drag from the return value of this node and locate break vector. As before, we're only interested in the x output. Drag from the x output and locate sine float. Now we're ready to do our comparison. Drag from the return value of the sine float function and locate the is equal to function. Connect the other sine float return value into this node. And with that, we did it. The output of this function tells us if the AI is facing towards the player. All we need to do now is hook up this return Boolean to the return node. Reposition the return node and connect the output Boolean of the function into the should-attack output of the return node. At this point, I suggest using the marquee select to shift everything over to the right a little bit and then reposition our vector mouse logic to the left hand side of the return node. Compile, save and return to our can perform attack function. We still need to make a few more helper functions, but let's drop our new is facing target function into this graph to remind us that it's ready to go. Control, left click drag the function into the graph and place it somewhere near to the top return node. Our next check will be to see if the player is within a suitable attack range. Create a new function called is in attack range. As with the previous function, make sure that the pure checkbox is ticked. While we're over in the details panel on the right hand side, add a new Boolean output, name this new output inside attack range. Once again, it's a good idea to sanity check that our target is still valid. Drag from the execution pin of the function and locate is valid. Control, left click drag the player target variable from the my blueprint panel into the graph. Connect the output of this variable into the input object of the is valid node. Let's quickly deal with the outcome of the player target no longer being valid. This means that for whatever reason we no longer have a current target, so we definitely can't be within attack range. Make a copy of the return node and connect it to the is not valid execution path. Ensure that the inside attack range box is not checked. While we're here, ensure that it is checked for the top return node when it is valid. With the basics taken care of, let's dive into the other branch when we still do have a valid target. We can keep our range check here very simple because we're relying on multiple small checks to determine if it's a good idea to attack. The first step will be to get the locations of both the enemy character and the player target. Luckily for us, we've already set those up, so let's make use of them to find their locations. Drag references to the enemy character and player target variables into the graph from the my blueprint panel. Drag from the output of the enemy character variable and locate get actor location. Now drag from the return value of this function and locate vector minus vector. Now we'll do the same for the player target variable and connect the return value from its location function into the vector minus vector node. The result of this node can be used to determine the distance between these two actors. But before we get to that, we need to break open the vector to get to the relevant data. We break the vector because we only care about the distance between the actors in the x-axis. Drag from the output of the node and locate break vector. Now in order to compare this to a set range, we need the absolute value from the x-axis. As this is a common operation, Unreal Engine has a node specifically for this purpose. Drag from the x-output of the break vector node and locate absolute float. Now we can compare this float value to another float value which will represent our attack distance. Drag from the absolute node and locate float less than float. The lower value here will be our attack range. So drag from the input and select promote to variable. Name this new variable attack range. We will want to set a default value for this new float variable so compiled a blueprint. In the detail to panel on the right hand side, set the attack range default value to 250. Now the return Boolean value of this node will return true if we are within the attack range and false if we are outside of this range. This is exactly what we created the function for. We can connect the return value Boolean to the inside attack range output of the return node. With everything hooked up, it's worth taking the time to tidy up the nodes and keep everything readable at this point. Do whatever makes sense for you? But I suggest using a re-root node so that we can position this logic above the is-valid execution pin and prevent the wires from crossing over. And with that, we've completed another of the small functions we'll use to decide if the AI should attack or not. Let's compile and return to the can perform attack function to decide our next move. Let's drag a reference to R is in attack range function and place it near the is-facing target function in the graph. With those functions placed within the graph, let's start building the logic that will let the AI know if it is able to perform an attack. At the very least, we want to say if the AI is facing the target and the AI is within attack range, then the AI can attack. Because we set these helper functions up to return Boolean values, this logic is very straightforward to implement. So let's do that now. Now, connect the is-in-attack range output into the second input of the AND node. Now, connect the output of the AND node to the should-attack input of the return node for the function. This is probably a good opportunity to reposition the return node to stop the wires crossing over. So that's a pretty good start. But there's something else we should consider. What if the AI is already attacking? The AI shouldn't be able to start a new attack until the current one is complete. Luckily for us, we've already laid the groundwork to make this sort of check very easy. Drag a reference to the enemy character into the event graph. Drag from the output of the reference and locate get is-attacking. This is a Boolean value we already set up with a fire projectile event to create a new attack. We already set up with a fire projectile event on the enemy character. It's set to true as part of the firing process. We consider this test to be passed however when the AI is not currently attacking. So drag from the is-attacking output and locate the not Boolean node. The output from this node will return true if the input is false, which is what we want. Now we just need to hook it up to our existing Boolean checks. Click the plus icon next to the add-pin text of the and node and connect the output of the not node into this new input. Once again, feel free to reposition the nodes to make everything nice and easy to read. Okay, we're making good progress, but there's still at least one more thing we should add to make this whole thing look and feel better. The quickest way to handle this setup would be to make sure that is-attacking is set to false as soon as the attack animation had completed. However, if we do that, we'll end up with a awkward situation where the enemy will attack over and over instantly without any sort of cooldown. Not only is this not really very fun, it also tends to look pretty bad. Let's fix that by adding a simple cooldown that starts as soon as the AI has finished an attack. Let's return to the enemy character Blueprint, as that's where the animations are being handled, and that makes it the best place to trigger events based upon animation completion. Return to the content browser and open the enemy character in the Blueprint Editor. Locate existing on Flipbook playback complete custom event in the graph. At the moment, this custom event only carries out a single check based upon the animations. It asks, was the animation that just completed called AirTrans animation? And if so, we update the animation to the falling animation. Now, we're going to add something pretty similar for the attack animation. Let's save ourselves some time and copy and paste some nodes used to get the current Flipbook being used and compare it to a specific reference. Use the marquee select tool to grab the sprite reference, get Flipbook function and the is equal to node. And then, copy and paste these nodes into some clear space in the graph. Now's a good time to reposition the execution path from the true side of the branch. I suggest moving it up and using re-root nodes to keep everything neat as we have before. Returning to our new comparison logic, we need a reference to our attack animation, so drag that in from the myplewprint panel. Connect the attack animation to the bottom entry in the is equal to node. Next, add another branch by holding down B on the keyboard and clicking into an empty nearby space on the graph. Connect this branch to the false execution pin of the previous branch. Connect the output from the is equal to node to the condition input of our newest branch. With the basic nodes in place, now's a good time to reposition the nodes relative to the existing branch. Let's begin by using the true execution path of our new branch to do a little housekeeping. Alt left click drag a reference to the is attacking Boolean into the graph. Leave the is attacking Boolean input unchecked, meaning that is attacking will be set to false by this node. Connect this set node to the true output of the branch. Copy and paste a reference to the sprite component of the enemy character and locate set looping. Check the new looping checkbox. We do this because while we only want the attack animation to play once, the default expectation for most of the enemy's animations are to loop over and over. For example, idle and walking. Connect this set looping function to the previous is attacking set node. This quick alteration takes care of the basics and now we should move on to handling the implementation of that cooldown we discussed. We're going to handle this cooldown logic in a similar manner to our basic sense changing logic. We'll do that by binding specific things to the AI controller to a specific event dispatcher we'll create. We'll begin by creating a new event dispatcher on the enemy character and naming it on completed attack animation. Now all that's left to do on this blueprint is to call this new dispatcher. Drag from the execution pin of the set looping function we were just working on and locate call uncompleted attack animation. Compile and save the blueprint and then move over to the enemy AI blueprint. Select the event graph and locate the event on possess event within the graph and note all of our existing bindings. Let's add another binding. Begin by getting a reference to the enemy character and drag from the output and locate bind event to on complete attack animation. Make sure to give us some space in the event graph to work and don't forget to connect this bind event to the previous one otherwise it'll never be executed. Let's create the event to associate with this bind. Drag from the event input and locate add custom event. Name this new custom event on attack completed. Now let's add a Boolean variable to store the current cooldown state. Add a new Boolean variable and call it is on attack cooldown. Alt left click drag this new variable into the graph and check the box on the input to set the Boolean to true. Connect this new set node to the custom event execution pin. Now we'll use a simple timer to control the cooldown length. Drag from the set node execution pin and locate set timer by event. Let's work our way through the input for this function. Drag from the event input and locate add custom event. Name this event remove attack cooldown. Copy paste the is on attack cooldown set node and connect the execution wire to the event we just created. Make sure to uncheck the box within the set node as we want is on attack cooldown to be set to false. That's all this custom event needs to do. So let's return to the set timer by event function. The next input is the time input. This is something we're likely to want to tweak later so let's turn it into a variable. Drag from the time input and locate promote to variable. Name this new variable attack cooldown time. Compile the blueprint so that we can set the default value for this variable. In the details panel in the default value rollout change attack cooldown time to 3. This means it will be 3 seconds before our remove attack cooldown custom event is triggered. Returning to the set timer by event function we can leave the looping option unchecked as we don't want this set timer to repeat. As always I believe it's worth taking the time to reposition the nodes and use reroute nodes to make sure none of the wires crossover and this section remains readable. With that taken care of we can compile save and return to our can perform attack function. We already have our 3 check set up and now we can add our additional cooldown check as well. Drag a reference to is on attack cooldown into the graph. Once again we're looking to check that the AI is not currently on an attack cooldown. So drag from the Boolean and locate the not node. Add another input pin to the and node by hitting the plus in the low right hand corner of the node. Connect the output of our most recent not node into the new input we have created. And there we have it our can perform attack function is complete. This function will only return true if the AI is facing their target, is within an attack range, isn't attacking already and isn't currently on a cooldown from a previous attack. With that taken care of now's a good time to compile and save the blueprint before moving on. Let's leave our can perform attack function for now and return to the main event graph for the enemy AI. Using the my blueprint panel locate the do behavior custom event. At the moment this custom event isn't doing a great deal. All we do is handle the situation where the AI is not alerted. That is to say it doesn't currently have a player target. So let's turn our attention to the true path of this branch when the AI is alerted to the player's presence and needs to decide what to do. The very first thing the AI should do if alerted to the player's presence is decide if they should attack at this moment. Luckily that's what we've just been working on. So control left click drag our can perform attack function into the graph near the true execution pin of this existing branch. Hold down B and left click in the empty space in the graph to add a new branch. Connect the Boolean output of can perform attack into the condition input of the new branch. Before we forget connect the execution pin from the true side of the previous branch into the execution pin of this new branch. Let's continue along the ideal execution path for now. What should happen if should attack is true? Well, which is fire projectile? Drag an enemy character reference into the graph and then drag from this new reference and locate fire projectile. Connect the execution pin from the true side of the branch into the fire projectile node. Let's quickly step through this to identify our best case outcome. With the first branch we start by seeing if the AI is alerted. The alert status is set by having a valid player target. If that's true, we check if the AI should attack. This function as we know checks the AI is facing the target is within an attack range and is not already attacking or within a specified attack cooldown period. If all of that is true, great, the enemy character will fire a projectile. However, real life rarely works out that way and we have to deal with the edge cases. We have to deal with situations where some or all of these conditions for attacking are not met. There are two specific edge cases we're going to deal with for now. Firstly, the AI isn't currently facing the target and secondly, the current target isn't within the attack range. We'll capture these specifically because it's pretty easy to give the AI a simple response to improve their chances of being able to attack in the near future. First though, let's give ourselves some space to work. Marky select the branch we've been working on and it's connected nodes and move it up. Make use of a re-root node to prevent wires crossing over. Let's start with the first reason we suggested. The AI shouldn't attack at the moment because it's already attacking. If that's the case, there's nothing else we could really do so we want to rule that out first. Drag another enemy character reference into the graph. Drag out from this reference and locate get is attacking. Drag from is attacking and locate the not operator. Now we can use this return value to drive another branch. Hold B and left click in an empty space on the graph nearby. Connect the return value from this not node in the graph. Connect the return value from this not node into the condition input of the branch. Don't forget to connect an execution player between the previous false execution path of the branch and unduly created branch. The true execution path from this branch rules out the possibility that the enemy is currently attacking. That takes care of one check, but what about the next one? Are they still facing the target? Drag a reference to our is facing target function into the graph. Using the same method as before, place another branch. Connect the output from the is facing target into the condition input of the branch. Connect the true execution wire from the previous branch into this new one. Now we can look at the false response from this branch. This would mean that the AI is not currently facing their target. And there's a pretty obvious way we can address that. Tell them to turn round. Drag another reference to the enemy character, then drag from this new reference and locate rotate. Connect the false execution pin of the latest branch into this rotate node. Now, if the enemy is facing the opposite direction to the target, they will rotate on the spot in exactly the same method they would to avoid falling off a platform. With that, we're almost there. However, there's one last thing we can address. What if the AI isn't currently attacking is facing the target, but it's just a little bit too far away. Well, in that situation, they should probably just keep moving. Drag a reference to the is in attack range function into the graph just beyond our existing branch. Add a new branch and connect the return from is in attack range into the condition input of the branch. Connect this new branch to the true execution pin of the previous branch. The simple movement logic we want to use here is identical to our non-alerted code, so let's be lazy. Marky select everything in the false branch with the alerted condition, just beyond the do behavior custom event node. Paste this new logic somewhere near our latest branch. Connect the false execution pin of this branch to the add movement input node from the newly pasted section. And with that, we've made a significant improvement to our do behavior custom event. For now, we can save, compile, and close the blueprint window to return to the main editor window. Before progressing any further, it's probably a good idea to check if things are actually working as we expect. Let's begin a play and editor session. Once we start the session, we'll notice a couple of things. The AI will successfully attack the player by firing a projectile. However, we can't actually see the enemy character play the attack animation, even though we definitely set it up correctly. Also, we notice that they never fire again, despite the fact they should fire repeatedly via the cooldown we configured. This is happening because the attacking animation is instantly being overridden by some of our existing animation logic. This is also impacting the cooldown, because the cooldown logic requires the animation to play to completion. Luckily, this is an easy fix, but I think it's important to see how small change to our animation can have a big impact when we're using the animations to determine state. Let's end the play in editor session and open the enemy character blueprint. We want to locate the handle animation custom event within the event graph. As a quick reminder, this custom event is called every tick to make sure the correct animation is playing. Currently, the first branch of this custom event just checks the character movement component and decides if the enemy should be walking or running and sets the appropriate animation. What it certainly doesn't do is check if the enemy is currently attacking. That's important because if we are currently playing the attack animation, we do not want this custom event to make any changes to the currently playing animation. This current setup is what stops our attack animation playing, because it is instantly changed to either a walking or a running animation. Let's resolve that by adding a new branch to check if we're currently playing the attack animation, and give us a par through the logic that will skip any further changes to the playing animation. Fortunately for us, this is pretty easy to do. Thanks for the setup work. We've already done around the attacking. Drag a reference to is attacking into the graph. Drag from the output of is attacking and locate the not node. Create a new branch and connect the output from the not node into the condition input of the branch. Before integrating these new nodes, let's make some space for this new logic. Move our latest nodes off to the side and marquee select everything connected to the handle animation custom event and move them off to the right a little bit. Be sure to resize the comment box if required. Now we can reposition our latest nodes and connect the execution pin from the handle animation custom event into the new branch. Next, go ahead and connect the true execution pin off the new branch into the existing branch that was checking is walking as a condition. Take a moment to reorganize the nodes to keep everything neat and readable. With these changes, we can see that if we are not attacking, the rest of this logic is processed as before. However, if we are attacking, we'll hit the false path of this first branch, which has nothing connected to it. This means that during this tick, no changes will be made to the animation via this custom event. And that's all we need to do, so we can compile, save to blueprint and return to the main editor window. Now let's start another plane editor session and note how the animation plays successfully. We can also see the enemy fire over and over as the cooldown stops and starts. Finally, let's also check the projectile is working as expected. If we walk into the path of the projectile, the player character is killed and reset. And with that, we're just a couple of polish steps away from wrapping up all the AO related work. Let's end this plane editor session and get to work on those polish features.