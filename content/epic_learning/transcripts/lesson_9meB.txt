We have our project built and optimized and now we're ready to finish preparing to submit to the Microsoft Store. In this module, we will package our project in a shipping configuration and a new project version to recognize this as a release. There are a lot of customization options in regards to packaging a project. We'll cover the basic setup for getting the project onto the Microsoft Store. Let's jump straight into the project settings. We'll begin inside of description. Our project version should now be set for a more important release, so I'm going to set it as 1.1.0.0. We're still in version 1 of the project, but have moved on from a 1.0. This of course is all up to you and your team. We also need to change the project named value here to be a value given inside of the product identity within the Microsoft Partner Center. Paste to the value of package identity name here. Moving down to packaging, we will use a pack file to keep all of our content in one single file. Under project, set the build configuration to shipping and set full rebuild and for distribution to true. Moving forward distribution to true, we'll bundle all of the needed assets into one zipped file that we can use when we upload to the store. Return to our main map and under file, select package project and choose HoloLens. Find a place to package it to. While we wait for that, let's prepare the automated test that we need to pass for the store approval. It is the Windows app certification kit or WAC test. The test is built to identify common issues that affect uploading and processing in the store. Navigate to your local disk and open program files x86, Windows kits, 10, app certification kit, open appcertui.exe. Browse for your built project and choose the app x bundle file and press next. The WAC test is going to default to running all tasks that it is capable of running. So if you see a star like here, it means that it cannot run in this environment, which is fine. Press next. Now the test will start. Depending on the size of your app, it may take some time. When it finishes, save the file to where you saved the app for future reference. I like to include the version of the build in the name for easy finding. And the overall results is... Past. Alright. In this module, we finalized our project settings and packaged out a shipping build for the Microsoft store. We certify at our app using a WAC test and saved our successful results. Next, we'll submit our build to the Microsoft store.