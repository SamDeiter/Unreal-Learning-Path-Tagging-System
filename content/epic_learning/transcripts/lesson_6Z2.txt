Manipulating various actors with our hands nearby and or from afar using familiar mechanics can be very beneficial to solve problems, but it is not the only mechanic we have been provided. In this module, we will add a component to several meshes in the scene to be manipulated by adaptive bounce control. Our intention is to interact directly with the floating islands in various ways. We're going to experiment with a few types of affordances to achieve the results that work best for our scene. Let's get started again inside of the hands map. Select one of the islands and first change the mobility to movable. Next, click on add component. Search for UXT bounce control and add it. The first setting is to input which data asset of type bounce control config we want to use. We'll get back to editing and create new versions of this shortly. The next setting is collision box. It is set to none by default, but if we set it to box collision, it will block all hand rays from passing through it. Affordance visibility distance will determine how far away your hand can be before the bounce is visible and the duration will be how long it takes to transition in or out. The next category, affordances, allows us to change the mesh that is representing the center, face, edge and corner. Without changing anything, let's preview and VR. The familiar bars are surrounding the island. We can rotate it a few ways from afar and scale it by grabbing the corner pieces, all no problem. But what if I don't want to allow scaling on this mesh? Let's create a couple new config assets to expand our options. Navigate to our assets folder. Right click and under miscellaneous create a new data asset of type UXT bounce control config. Name it bounce control z rotation. Once opened, we'll need to add each bar as an element to the affordances array. The two balls here are is slate, which will treat bounce using this as a 2D surface, not allowing scaling in the X. And uniform scaling speeds for itself. Press the plus to add a new element. Open the new zero element and notice that we have two options, placement which is an enumeration. Turn it up to see what kind of options we have. We have the option to place a handle at all the edges, all the faces, all the corners, and even the center. Not all will be needed, nor will all of them give us the use case that we are looking for. Keep in mind that the meshes are all going to render at 0, 0, 0 relative rotation. So without updating the rotations here, they will all face the same direction. In that the corners will not face the corner they are representing, or the faces will all face forward on the X. We want to only be able to rotate about the z axis, so we'll need four total affordances, edge front right, edge front left, edge back right, and edge back left. Turn to our floating island and set the config as our new bound control z rotation. Press preview and VR. Now these options make more sense for a floating island. We are now limited to only rotating about the z axis. Duplicate our new bound control z rotation and name it bound control scale rotate. We're still going to want our four rotation affordances, but let's also add some that will allow us to scale the actor. Adding affordances to the top and bottom edges will only allow us further rotation, which we don't want. But we do want to scale the actor, so let's add the eight corners. I'll just go ahead and add them and we'll skip ahead. Select one of the clouds and make sure to set two movable and add the bound control as a component. Set the config as our new bound control scale rotate. Lastly, set the affordance visibility distance to around 30 so we can interact from a distance. Okay, let's preview this in VR. Now that we have these corners here, it is possible to scale the cloud however we need to and we can also rotate it. But look at the corners here and notice that they are facing all the wrong way. This is what that rotation value will help us fix. In this module, we have added bound control components to a couple of actors in the scene and created new data assets to allow us to manipulate the actors in different ways. Next, we'll modify Unreal Engine and our BP pawn so we can map gestures to use as inputs.