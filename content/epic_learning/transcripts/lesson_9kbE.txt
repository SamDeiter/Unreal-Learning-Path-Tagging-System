Although we can always build physical objects in our scenes to help obscure the light, and so create interesting light and shadow patterns in the shot, an approach to creating light distribution that can save us both time and resources in the long run is that of adding light functions. These being a powerful way to control light settings via a material, opening up a whole other set of light controls to us, namely those found inside Unreal's material editor. Now that being the case then let's take a look in this video at how we can set up a very simple light function. After right-clicking in the Content Browser then, from the list that appears, let's click to create a new material, which we will call LF Far Light Function, underscore Spotlight, after which we can double click to open up the material editor. With the material selected we want to change its material domain attribute to light function, which means that we can now use this material to drive a light intensity setting and distribution pattern. Let's switch back to our scene view now, and then select the Spotlight by the stairs. We can remove its IES profile by clicking the small yellow reset to the false button, and then click and drag our material over to the light function material slot. Coming back to the material editor, let's right-click and then search for the time node that allows us to animate our light function over time. To see this in action, let's also right-click and search for the sign node, which naturally enough produces a sign wave for our material. Let's hook the time node up to it, and then pipe the sign node into our material's immersive slot, which should now give us a pulsing white light. Let's make our function a little more interesting at this point though, so that we can really start to see the possibilities that lie behind light function building. And do keep in mind that I am showing you how to set up a simple function here, but we could go on to use textures, math functions, and many other nodes besides to produce a whole array of complex light function effects. Now to create a better slash more interesting pattern here then, let's right-click and search for a multiply node. And then after adding it to the mix, let's pipe our sign node into slot B, and the multiply nodes output into the immersive slot of our material. Next we can either right-click and add, or look in our palette tab for the generated band node. We need to set the width of our light band here, and so holding down the one key, we can click on the graph to create a constant node, which we will set to a value of 0.5. Piping it into the width slot once we are done. Next, I actually want to tile the light function. And so let's right-click, search for a text-coord node, and then pipe that into the input-coord and it's slot. And then finally, let's set that node to a V-tiling value of 8. After piping our generated band node into slot A on the multiply node, we can save our material and then rebate the lighting. If we focus our view using the middle mouse button now, and then press the G key to enable game view, we can clearly see our light function in action. Now although I've produced just a simple strobed light effect using a light pattern here, I'm sure you can see the potential that we have for creating some fairly complex and interesting lighting effects, all made possible by means of light functions in Unreal.