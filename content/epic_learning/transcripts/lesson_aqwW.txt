Now that we understand why performance matters and what we will do to ensure a comfortable experience for our users, let's take the first step by getting our project settings optimized for the HoloLens 2 environment. Unreal Engine comes ready to go with features that will help us increase the performance of our project. In this module, we're going to work through the project settings to review what settings we altered at the very beginning of the learning path, additional rendering parameters, mobile specific settings, disabling a custom pass for post-processing and go through some additional tips. While we haven't used any form of source control in our project, let me quickly touch on that. If you are already taking advantage of a form of source control, that's great. If not, we highly recommend finding one that works for you and using it on all your projects. I bring it up now because we are going to change many consequential settings throughout our assets in this and the following modules that may result in something going wrong or not looking quite right. It would be such a relief to have access to an older version we can revert back to if needed. Okay, let's get started. Open the project settings. Starting in the description category, when we first created the project, we ensured that start in VR was set to true. This is because we want to only run this project on the HoloLens 2. If your project requirements were to start on PC with a main screen and you wanted to only start on the HoloLens when needed, you could keep this off and set up that system separately. This could be the case for in-store demo types of projects or to use one project for use on PC and one on the HoloLens. Move down to target hardware. We have already prepared Unreal Engine to optimize many settings to mobile hardware and render scalable 2D or 3D. This is going to disable many effects that are not supported by the platform for us. Scroll down to collision. While this setting is not required, for our project, we created a new trace channel that we can use to block spatialized sounds from passing through. OK, now to general settings. We need to maintain a constant 60 frames per second to avoid any jitter in our moving holograms. Set use fixed frame to true and set it to 60. Moving on to rendering. When we changed our target hardware to mobile, many of the change settings were located here, but there are still a few we need to change. At the very top, we are disabling vertex fogging in mobile shaders, so we can turn off vertex fogging for a peak inside a forward renderer as we will not be using it. Forward shader is the shader model we have been using since the start. It has a lower cost than the default deferred rendering pipeline, and it will allow us to use only features we need for our project. Because we turned off vertex fogging, Unreal Engine wants us to restart. It's wait until the end. Pan back up a bit to mobile. Reduce the number of shadow maps required down to one. We will keep it here unless we notice a visual quality loss when testing our scenes. Next to calling. Because our scenes are fairly minimal and don't require occlusion calling, we can turn this off. If you find that your project does require your meshes to be cold and not rendered, keep occlusion calling set to false and instead set support software occlusion calling to true. I'll keep it false for our project. Move down to post processing. Disable the custom depth stencil pass. This additional pass that would normally be done is unnecessary as we are not using any post processing effects. We can also disable custom depth with temporal anti aliasing jitter. In VR, turn on instance stereo so we can quickly reduce the amount of draw calls. We won't use any HDR so we can disable mobile HDR and we can now enable mobile multiview. Multiview is an advanced rendering feature for Android mobile devices. Building on to the effect of instance stereo, when our scene is rendered for our left eye buffer, it is then duplicated for our right buffer with all the offsets for the right eye applied. This will greatly reduce the CPU overhead. Our scene doesn't use any movable lights so we can set the max movable spotlights slash point lights down to zero. This will also reduce the number of shaders that Unreal Engine needs to generate for rendering. Before we move on, open the Maps and Modes setting. Set the Editor Startup map and Game Default map to main. We'll be using this map in our packaged version. Now we can go ahead and restart. In this module, we changed the project settings pertinent to helping us create a project that maintains our needed frame rate of 60 frames per second. We removed render features that we will not use and optimize the settings to give us a good base to optimize the assets in our project. In the next module, we will work on optimizing our materials and textures.