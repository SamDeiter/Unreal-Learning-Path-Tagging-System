There we go. All right, little technical difficulty. We're good to go now, though. So, morning, everyone. Thanks for coming. My name is Nick Pennwarden. I'm the Director of Engineering for Unreal. And this morning, I'm going to be talking about some of the work that our rendering team has been up to. This is a presentation that we showed at GDC that Marcus Wassmer gave. And I wanted to present it here to be able to give the same information to you guys. So, over the past year, the rendering team have worked on a large refactor of the mesh drawing pipeline in UE4. And I want to talk about why we did it, what the benefits are, some of the benefits that we've seen. And give you kind of an idea of what you'll need to do to upgrade any rendering modifications that you've made to Unreal when you update to the 422. Talk a little bit about where we're taking things. So, why did we spend a year refactoring the mesh drawing code in UE4? One reason is that I think if you were at the keynote yesterday, I was talking about how more people are making open world games. We want to really focus on allowing people to build ever more complex scenes. So, supporting huge view ranges, higher detail, stuff like that. When you're building a world, we want to make that easier. You tend to build it modularly. You pull an individual meshes. You use that to construct your world. We don't want artists to have to work with the mesh. We don't want to make it easier. We don't want to make it easier. We don't want to make it easier. We don't want to make it easier. We don't want to make it easier. We don't want to make it easier. We don't want artists to have to worry as much about taking individual meshes and going and merging them to reduce draw call counts for optimization purposes. And going forward, dynamic lighting and shadowing are going to become ever more important as games become more and more dynamic. And those additional passes are going to require drawing meshes multiple times for multiple viewpoints. Also, upcoming technologies need more information about the scene during evaluations. So, for instance, take DXR. In DXR, when you shoot a ray, you can hit something in the scene that might not be visible in the primary view. When that happens, you need to be able to evaluate the surface shader for that triangle and return the data back to the shader that casts that ray. That means that we need all the information about the scene on the GPU, even if it wasn't visible that this frame. And traditional rendering pipelines, the way you before used to work the way most game engines work, that information is not usually available on the GPU because each frame based on the view, you only give the GPU the data that it needs to render exactly what's visible. And moving forward, as we want to move more and more of the computation for rendering off of CPU and onto the GPU, that means that the GPU is going to need more and more information about the scene to be able to make the decisions that it needs to to set up all the command buffers needed for rendering. So, how do we get there? We started by looking at a draw call merging. What would we need to do to take multiple batches of draw calls and get it down to reduce the number of API calls that we make per frame. And looking at something like D3D11, the only option for doing that really is instance drawing. And in order for that to work, the only data that you have per draw call that's going to be different for different objects in the scene is the instance ID. So, that means in order to make that work, we can no longer be setting shader parameters for every single draw call. And so therefore, what we need to do is be able to have that data all available on the GPU and be able to access it using only the primitive ID. Another way for us to get to the point where we're doing less work on the CPU as we're rendering is with more aggressive caching. So, if things aren't changing, making sure that we're not doing duplicate work. A lot of the world doesn't change every frame. It's very, it's very temporarily coherent. So, static scene draws, we want to be able to build those when they're added to the scene and then only when they move or update in a meaningful way should we invalidate that data. And by doing more aggressive caching, this allows the platform, the RHI layer as we call it, to pre-build as much as possible, things like the shader binding table entry that I mentioned for DXR and the graphics pipeline state for DX12, for Vulcan, et cetera. And again, the goal here is to remove all of the renderer overhead in building that static geometry every single frame. So, I wanted to start by talking about how mesh drawing works in UE4 in 421 and earlier. Okay, so we start with the primitive scene proxy. And the primitive scene proxy is sort of the rendering threads copy of the primitive component. So, the primitive component, that's what lives on the game thread, that's what game code works with, that's what you move around as objects move in your game. The renderer has this copy of the data for, partly for thread safety purposes and partly so can cache information that only the rendering thread needs to care about. And so every frame, we take that scene proxy and we generate a number of mesh batches from it. And the reason for this, what is a mesh batch, it's basically all of the data, all the high level data that we need to figure out how to make a draw call in a given pass. And it decouples the scene proxy from all of the details of the rendering implementation. The scene proxy doesn't need to know about what passes it's going to render in, it doesn't even need to know what passes potentially exist in the renderer. We don't want it to, this is what gives us the flexibility to implement new passes, new algorithms without having to go and update every single object that potentially wants to render. So now we have this mesh batch. And we now need to go and use something that we call a drawing policy to take that mesh batch and convert it into a set of RHI commands. So this would be, for instance, we have a depth drawing policy for rendering the pre-pass, we have a base pass drawing policy for rendering the G buffer and so on. So this reads things from the mesh batch, like the vertex factory and the material and uses that to make decisions. Finally, the RHI command list is sort of to cross-platform set of commands for rendering that then the RHI implementation, the actual low level API layer will actually make API calls. So our D3D11 implementation will turn these RHI commands into drawing and exprimative calls on D3D11 and corresponding calls on OpenGL on PS4 on Vulkan and so on. So what does this, I want to dig a little bit more into this, this sort of part of the renderer where we're taking mesh batches and we're generating RHI commands. So this works in 421 and earlier is we traverse the static mesh draw list or the dynamic meshes and the first thing that the drawing policy needs to do is figure out what what shader do we do we render with. So we use the material, we use the vertex factory use that to figure out what shader to render with and then we have to actually gather all of the shader bindings. So what are all the parameters we need to draw for it? These are things like the parameters that the the artist has set on a material instance constant, things that are needed for the vertex factory information about the view. So all that stuff we need to we need to bind all those parameters. And so we're about that. So the other thing about the static mesh draw list is that it is prebuilt. So we did some level of caching in 421 and earlier and so at add time when a printer is added to the scene, we would take the mesh batch and we would run it through a part of the drawing policy cash off some of that information and store it in the static mesh draw list. So the static mesh draw list would actually contain all of these semi pre processed mesh patches for the entire frame and then it would sort them based on state so that we could minimize the number of times we're changing shaders and changing other rendering state. So the run time when we actually go to render the match the mesh draw list, it would actually go through and it looks at every single static mesh in the scene and looks up into a bit array for visibility. Basically saying, is this mesh visible? Yes, draw is this mesh visible? No, is this mesh visible? No. So it means that a certain aspect of drawing scaled with the entire size of the scene, not just the number of meshes that were visible. That traversal is relatively fast, so it works okay for certain sizes of scenes, but once you get to a certain point, it doesn't, doesn't scale any longer. Another problem with it is it's an entirely different code path than dynamic drawing. So dynamic mesh batches need to go through the entire drawing policy every frame and that means we can actually sort between the two. So anything that goes down the static path, we can't sort it with things that go down the dynamic path. From a code point of view, it's also kind of, there's a lot of boilerplate to deal with this. The static draw list is templated by that drawing policy and therefore you end up with with a bunch of these drawing lists just sitting on the scene and adding one is kind of kind of extra overhead. So long story short, static mesh draw list prevents us from doing efficient draw merging, partly because the drawing policies are too tightly coupled with static mesh draw list. And partly because the way that we design drawing policies, they're allowed to set shader parameters directly to the RHI per draw call and that's something that we need to avoid if we want to be able to merge draw calls. So let's take a look at the new mesh drawing pipeline that we've designed for 422. So first of all, we're getting rid of this traverse static mesh draw list drawing policy bit and where replacing it with what we call mesh draw commands. So what is a mesh draw command? A mesh draw command stores everything that the RHI needs to know in order to issue a draw call. This is a full standalone low level description. So it has things like the pipeline state object. It has things like shader bindings has things like the index buffer that we need to render with. It's completely stateless. There's no data here that points you back to where this mesh draw command comes from. There's no context. And that is really nice from a data transformation perspective because it means that as we go, we're able to optimize, replace, merge these things without having to worry about the implementation of mesh batch or the implementation of a scene proxy. So there is actually a debug pointer on mesh draw command. So you can see where the draw call originated from, which is really useful for debugging. But we strip it out in in shipping builds. And so you should never actually depend on it for any code that you're writing. So having this sort of stateless description of what the RHI needs to render to issue a draw call allows us to do more aggressive caching, which is what we're looking for. We can build the mesh draw command for static geometry at the time we add to the scene. So I mentioned that static mesh draw list to some amount of caching. This draw command does even more. It's basically the entire execution of what the drawing policy used to do all cashed in a single mesh draw command. And this gets us closer to what we're looking for, being able to have the pipeline state object and all of the shader bindings needed to draw anything in the scene at the time it's added to the scene. So the thing is that because this is purely state, we get robust draw call merging. We can just look at the state and if it's the same across multiple draw calls, we can just combine them into a single instance draw call. But more on that later. So getting back to sort of the lifetime here, how does this actually what is this actually look like? So we have a mesh batch and we need to generate mesh draw commands for it. And to do that, we have this mesh pass processor. So the mesh pass processor its purpose again is to build the mesh draw commands is select a shader collects all the bindings for rendering that draw call. Bindings from the past from the vertex factory from materials and so on. Now, you need to write a mesh pass processor per pass that you want in your renderer. So one of the nice things though is that this is not templated on the shader class. It's it's just a relatively simple class to write. We use the same code path for both dynamic and static draws. So there's no more duplication of rendering code when you're implementing a pass. So what I want to do is take a quick look at an example. We'll take a look at the depth pass. This is the simplest pass in in the renderer probably where we just want to render the depth for every primitive in the scene. So there's a little bit of copy paste boilerplate when you're going to create a new mesh processor. This is about the extent of it. So it's not too bad. This is basically just setting up the things that FMesh pass processor needs. And then the part that you implement, the part that you really care about is this. This is adMeshBatch. And this is called for each mesh batch in the scene that's visible. And the two things you're doing here really is filtering. Do I care about this for this pass and selecting the shader? So for instance, for the depth pass, we're not going to render translucency into the depth pass. So we filter that out. We filter out anything that's not actually going to render in the main pass, et cetera. In terms of selecting the shader, we look at, in this case, whether it's an opaque mesh or not, if it's an opaque surface, then we do some optimizations. In this case, we can render with the depth-only vertex shader, which is a bit faster than otherwise. If it's masked, then we need to evaluate the material so that we can do alpha testing. And so that goes through a slightly different path. But so here's sort of the second part of selecting the shader. And finally, we call this build mesh draw commands function, which will gather the bindings. This is actually code that's shared among all depth pass processors, so you don't have to write that function yourself. And that's it. You have this function, and this one is actually just a convenience template function about choosing whether we're using the depth-only shader or not. So yeah, that's it. The drawing policy in 421 was somewhere around 642 lines of code. The depth-pass-mesh processor is around 100. So it's a lot simpler to set up these new passes, a lot simpler to add them. The base pass also went down from almost 1,000 lines of code to just over 200. So I think all engineers like deleting code, so having simpler code is always a good thing. I want to take a closer look now at that function. I told you you don't need to write this, the build mesh draw commands. So this is the one that actually collects all of the shader bindings that are needed for the draw call. And this is part of where removing a lot of the boilerplate came from, why implementing a new mesh pass is simpler now than it was before. So the main change here is that rather than directly grabbing all of the parameters that we want to set for the shader, and directly asking RHI to set this float for at this offset, instead what we do is we gather them into basically a shader bindings table, and we cache them off. After we've gathered all the mesh draw commands, now we can sort them. And so this, as I was mentioning earlier, we could never do state sorting before between dynamic path meshes and static mesh path, static mesh, or static meshes because they go down different code paths. So now because they are all using the same data, it's really simple for us to just sort all the mesh draw commands. And finally, we submit them. So now we have a list of mesh draw commands, and all we need to do is iterate that visible list and generate the RHI commands for rendering it this frame. So the nice properties of submit mesh draw commands, it scales only with the visible number of primitives, so we no longer have this property where traversing the static mesh draw list would have some costs for the entire set of visible meshes. It's also easier to parallelize than the static mesh draw list was for two reasons. One, it's easier to split it into an even number of tasks. When you're generating parallel tasks, parallel jobs, you want to be able to generate roughly even amounts of work. You don't want one job that takes 10 times longer than another job because that's going to end up being your critical path. So in this case, because it's only the visible set, it's really easy. If there are 5,000 visible meshes, we'll break it into groups of 100 or 200 and send those off to task threads to process. With the static mesh draw list, we couldn't really do that. We had to have some notion of which one's going to look at the first 100 or 200 or 300 meshes in the frame, and then it would go over the bit array and only draw what's visible within those areas. So there was kind of some complicated code that would based on the previous frames visibility, try to figure out how to break up tasks to make them even. And it worked okay in practice, but it was a lot more complicated than just a simple parallel for. Also, all we're doing is taking these mesh draw commands, which are just data and writing into an RHI command list, which is just a data stream. And so it's completely side effect free. We're not touching global state. We're not modifying global state. So there's no worry about race conditions. Another thing that we do in submit mesh draw commands is we do state filtering above the RHI. This reduces the load, the amount of time that we have to spend executing the command list. I think in Fortnite, we've seen maybe a 20% savings by doing this alone. And this also means that by doing it at this level, there's less burden on the per platform implementations to make sure that they're doing very fine-grained state caching. It's also cash coherent. This is just one big flat array of data. And therefore, since everything we need is in this contiguous memory space, we're not hitting a lot of cash misses during rendering anymore. We're able to just run through this flat array. We get a lot of benefit out of pre-fetching from the CPU and everything goes really quickly. So we've done all this work to refactor how we're taking primitives and generating mesh batches and generating mesh draw commands and so on. Now let's get to the caching part of things. Again, the majority of the worlds that we're dealing with are modular static meshes. So we shouldn't be doing all the work every frame. What we want to do is just cache these on the scene and then just select the right ones to draw every frame. This means that anything referenced by mesh draw commands can't change frequently. Because if it does change, then we need to invalidate it. What we needed to do was use a level of indirection to make sure that that was the case. For instance, let's take a look at per view parameters. So these are values that change every single frame. The way that the renderer used to work, every frame we'd gather all of that data, we'd make one uniform buffer, the view uniform buffer. And then as we're issuing draw calls, we'd bind this view uniform buffer that we created uniquely for that frame and issued the draw call. That's not going to work because it means that we would have to invalidate every mesh draw command, every frame to update that binding. So instead, we keep the binding stable. We create a single view uniform buffer that we is always bound to every draw command. And then every frame when we're about to render a view, we update the contents of the view uniform buffer instead. So the indirection remains constant even though the data in the buffer ends up changing. So we're here to support that the RHI now supports a code path for updating the contents of the uniform buffer dynamically, which we did not previously. So taking a look at the type of data that we reference from draw commands and how we organize them into uniform buffers, we have the primitive uniform buffer. So since this is where we store all data that is unique to that primitive things like the local to world transform. Then we have the material uniform buffer. This is where everything unique to the material, things like parameters that artists are setting in material instance constants, color tints and different textures and so on. So we put our collections which are unique to the scene. These are data that can be accessed by a material, but they're global for the entire scene. The pre-computed lighting uniform buffer for accessing data like what light maps we should be referencing, what our offsets are into those light maps, those tend to be per primitive as well. So we have pass uniform buffers. This is any data that a particular pass needs per primitive to render, but is is not shared among different passes, the depth pre pass might need different bindings than the base pass for instance. So once we've moved all that data into those uniform buffers, now we had to make sure that any time that anything that really does need to change, we do invalidate the mesh draw commands. And this is really important because if you screw this up, you're going to get incorrect rendering. Fortunately, most of this was already handled because the caching done in static mesh draw lists, cached sort of the first part of the evaluation that we that we now do to generate mesh draw commands. So all of those cases are still relevant. Whenever we would invalidate a static mesh draw list before, we would have to invalidate mesh draw commands. But it's not quite everything because now we are caching the shader bindings as well, which we were not previously. Therefore, every time that something would change in the scene that would cause the shader bindings to change, we need to remember to invalidate cached mesh draw commands. We've added a validation mode. It adds quite a bit of overhead, so it doesn't run most of the time. But if you're running into a bug that you think is because caching validation is not working correctly, you can enable this and this will assert when it's screwed up, which is really, really useful for finding out what is actually going wrong. We use this a lot as we were bringing up this code path and looking for corner cases and bugs that we were trying to track down. So it's very useful. If you run into this, in your case, you start seeing something like flickering or some unexplained crash in the renderer after modifying it, this would be a good first step forward to bugging. Vertex factories, if you're familiar with those, basically how we generate the vertex data in the vertex shader for drawing and how we get that data from CPU to GPU. Right now, we only support caching for vertex factories that don't need to update their bindings per view. For instance, the local vertex factory. In the local vertex factories, what we use for static meshes, we use it for a couple of other cases. It's by far the most commonly used vertex factory in the engine. Other vertex factories, which do need to change per view, we do allow them to still cache mesh batches. So some of the work is still cash, but not all of it. These are things like landscape that the LOD computations are a bit more complicated than with static meshes and so need to be evaluated per view. BSP, Instancing, stuff like that. And this means we have a couple of different caching code paths in terms of efficiency. So we have the least efficient path, which we call dynamic relevance. This would be cases like cascade particles or Niagara particles. These change every frame, we get new data pushed to the rendering thread from the game thread. So every frame, the scene proxy needs to generate a new set of mesh batches. We need to take those mesh batches, generate a new set of draw commands, and then take those draw commands and finally generate the RHI commands for them. For static primitives, in cases where things are not changing in terms of the game code isn't sending us new data, but rather we need to change how we create those draw commands based on the view. We do a little bit more work. So we cache the mesh batches, but then every frame, we take the mesh batches, generate draw commands, and then those draw commands will get turned into RHI commands. And then we have our most efficient path, the path used by static meshes, for instance, where we have cached the mesh batches and we've cached the mesh draw commands at scene ed time. And then at runtime, we just need to find, we gather all of those visible mesh draw commands and we issue RHI commands for them. So high level overview of a frame with caching enabled, when we add a primitive to the scene, if it's static, we cache the mesh batches. And if the vertex factory doesn't need the view, if the vertex factory supports draw command caching, then we also take those mesh batches and we generate mesh draw commands at that time and cache those. There are a couple places, for instance, like when we change the skylight, that is going to change shader bindings for pretty much everything in the scene. And so we invalidate the cached mesh draw commands. When it comes time to render the scene, we will need to go over all of the cached batches and regenerate those mesh draw commands. Then each frame when we're rendering, we figure out all of the primitives that are visible in the scene. If they're static, we compute their LOD and we add the cached mesh draw commands to a visible list. If they're dynamic, then we'll go ahead, we'll gather their mesh batches, we'll generate draw commands from them, and then we take those draw commands and add them to the same visible list. After that point, after it views every subsequent pass, only needs to look at this one list of visible mesh draw commands and then draw them. Now we have this nice data-oriented layout of mesh draw commands and we set all of this up so we could do draw call merging. So what does that look like? On D3D11, we're using draw instance indirect and that means that the only thing that can change between merge draws is the instance ID. We can't change shaders, we can't change bindings, nothing else. But it does mean that we can do dynamic insensing. It's pretty easy to implement at this point with the architecture that we now have. In the future, on D3D12, for instance, execute indirect allows us to change state in between instance draw calls. That's going to actually allow us to take these big lists of mesh draw commands and make far fewer API calls in order to render them. But let's take a closer look at, I should mention we haven't actually done the D3D12 path yet. That will be a future endeavor that we will embark on. For now, we've done the D3D11 style dynamic insensing. So let's take a closer look at dynamic insensing. Now that we have these mesh draw commands, we can robustly merge them because again, they're just low level RHI state. All we need to do is compare the data that exists on there. And if they're all the same, then we can merge the draw calls and change it into an instance draw call. This has the really nice property of content creators don't have to worry about it. They don't need to go and create a special instance static mesh and try to combine them and worry about what about calling. How do I balance draw calls versus calling efficiency and so on. It just works. There is one downside, of course, assigning those state buckets is slow. Doing all those comparisons does take a good amount of time. And we don't want to do that every frame. So again, let's cache this add to seeing when we generated the draw command to begin with at that point. What we can do is we look up into a cache of state buckets and we see which state bucket does this draw command belong to. And then this lets us group early on which draw commands can be merged together. And then the actual merging operation is just a data transformation on the visible list. So we have this big list of visible draw calls and any command in the same bucket. We just replace that with a single mesh draw command. So we have our sorted list of mesh draw commands. And as we as we go through them, we're looking up what state bucket they're in. And as long as one is in the same state bucket, we just keep it in a single instance draw call. And so the output of that merging path pass is just the smaller list of merged mesh draw commands. So now now that we can merge mesh draw commands, that's great. How do we make sure there is effective as possible? So one of the things that we needed to do was get rid of any sort of per draw bindings that would have broken up draw commands into multiple sub commands. For instance, anything that was going to be bound per pass per primitive, we really need to make sure that doesn't happen. And in order to support this, what we did is we created a pass uniform buffer frequency, like a per pass place to store data. For instance, things like in the base pass, we potentially sample the debuffer textures. We potentially sample the eye adaptation texture, stuff like that. The fog parameters, those things don't change for the entirety of the base pass. So why set them on every single draw call. We just set up a single uniform buffer for the base pass for this frame. And we and we render it. And then we have some remaining per draw call bindings. We have the global constant buffer values. We have the primitive uniform buffer. This is something again, this changes on every single primitive that's in the world. So if we don't change the way we store primitive uniform data in some way, this is trivially going to break apart all of our merged draw calls. We're not going to be able to merge anything pre-computed lighting. So again, this is the light maps that that the primitive need to render with. And the distance call data basically as your distance calling or LOD fading different primitives can be in a different state. And so that can break merging effectiveness. So what we want to do instead is we want to upload all of this data into a single scene wide buffer that we can then look up into based on the instance ID in the shader. That way when we merge the draw calls, it doesn't matter if we render them individually emerged, no matter what we're going to be able to look up and get that data without changing the bindings for the shader. So in order to support this, there were a number of cases where we needed to do something like this. So we implemented sort of a generic, you can give it like a GPU TRA implementation. It's a dynamically resizable array on GPU where when you want to insert data from the CPU, we kind of track add updates and moves. And then before we render, we launch a couple of computer shaders to shuffle data around as needed to make this possible. And this, this means we didn't want to every frame just completely update upload the entire primitive buffer. So this lets us just track deltas and perform only those operations. One, one result of this is when you're accessing the data in a shader, you need to use get primitive data before you can just directly access the primitive uniform buffer. But now we're going through this in direction where we need the primitive ID. And so anything that was going to access the data needs to go through this get primitive data helper function. Again, this is only used by supporting vertex factories and it's abstracted by this get primitive data methods. So it's not too bad. But how do we actually get the primitive ID in the shader? All we have is is the instance ID. So we could do something like have a buffer that we bind per draw call with the primitive primitive IDs and index into that. But adding a single global constant buffer value per draw actually increased the base past drawing time by 20%. So we didn't want to eat that overhead of just having to update a single parameter every, every draw call. Fortunately, the input assembler has a much faster path for it. We can just set stream source with with a dynamic offset. And this lets us set up the primitive ID vertex input at per instance frequency. And then after draw call merging, we build the primitive IDs buffer out of the list of mesh draw commands. So we did all this work to merge draw calls. Let's take a look at the results. What does that actually look like? So I have this map call it GP perf test. It's actually we just grabbed one of the cities from Fortnite saved the world probably a year or two ago and use this as just a static representation that we could use to iterate on and test rendering performance initially primarily intended as GP performance. But it was basically a way for us to have a static profiling set for optimizing for night. So taking a look at this, looking at the depth pass, we were able to reduce the number of draw calls with merging by more than 2x and same thing with base pass around a factor of two. The depth pass ends up being a little more efficient just because there are a lot more cases where you can share the same shader for for mish is that aren't alpha masked and can just use the depth only drawing policy. But that wasn't nearly enough to really tell us that like 2x is good, but we really wanted to see how it scaled. So we did the programmer art version of trying to make a more complex scene. We took the scene and we duplicated it three times. And then we removed distance calling to just render things really far out. And so the results of that we ended up with a depth pass that we ended up basically drawing around you know 7400 draw calls per frame. And we were able to with draw call merging reduce the depth pass by about 10x in terms of draw calls in the base pass by closer to 5x. Now, all that said, let me let me caveat that with this was by far an optimal case. We literally just copy pasted content. So it's pretty much perfect for dynamic instant. But it's sort of proving that if you do make a modularized static scene, then this code can do a really good job of optimizing that on the back end. Some performance gotchas to keep you in mind things that will break draw call merging light maps that make small textures. So basically if you're using static lighting and a lot of your primitives end up in different light maps, then we're not going to be able to merge draw calls where they do not use the same light map. You can tweak the radius that we use to pack primitives into the same light map texture. And that can help with it. It's kind of a trade off between texture streaming efficiency for memory of light maps and draw call efficiency of how much you're able to merge using vertex painting on instances in the world will also break draw call merging because those exist in their own independent vertex buffers that need to be bound separately. The speech tree windowed again if you use a speech tree windowed we do not support merging. There's no good technical reason for that. We just didn't get to it. So that could be improved in the future. And if you're still using the legacy sparse volume lighting samples for lighting dynamic objects with static lighting. That will not work either. However, the newer volumetric light maps work perfectly well with draw call merging. So before we're looking at just draw calls how well did merging work in terms of reducing the number of draw calls. But what does that actually mean in terms of milliseconds because that's what we actually care about at the end of the day. So we tested on PlayStation 4 is is it's just nice to be able to use a console platform where we don't have to worry about the OS getting in the way we don't have to worry about differences in in hardware just a very consistent platform to be testing on. So with the base GPU perf test with the old code in 421 the depth pass was running in about two milliseconds in the base past 3.2 the new mesh draw command pipeline with dynamic instanceing depth drawing took 0.3 milliseconds in base past 0.4 milliseconds so much much faster six to seven times faster. So in our programmer art larger scene. The old path was running at around 15.7 milliseconds for the depth pass and 27.8 milliseconds for the base pass again issuing about 7,400 draw calls. The new path is now running in 1.2 milliseconds for the depth pass and 2.4 for the base pass so massively faster right more than 10 times faster. But let me caveat that in this test case it worked awesome. These are by far best case results for one is a heavily modular scene with very limited mesh and shader variety. So the speed up I was showing that's only the mesh drawing part of the code and that may or may not be on your critical path for a variety of reasons. For instance, you, I'm not talking about and it views which is the visibility part so if visibility is still dominating your draw time then taking the mesh drawing and making that smaller might not help you. If you are if you're bound by another reason we were already parallelizing the renderer fairly heavily so if you have a lot of task threads that aren't doing other useful work we were already using them very effectively and so even though those go away. You still might not see an actual frame rate benefit from it and also you your content as well as ours what we've been shipping in fortnight in all of our demos and other projects is up we're optimized for the previous renderer so our artists were very carefully making sure that they kept draw call counts down and these benefits really start to show up as you're seeing scales up by by factors of three four five of what they are. So some casualties from the change things that no longer work all of the deferred primitive update mechanisms that were in the renderer for efficiency sake those had to go away because they're just incompatible with with the idea of caching all of that data when we create the scene. So this these are things like if a primitive moves but it's not visible we would never bother updating the primitive uniform buffer for that for that primitive we can't do that anymore because we need to update some GPU data so that DXR can trace a ray to it or so that we can build a mesh draw command that can reference it. Also if you have any materials that are using custom expressions. They can pretty much type whatever HLSL code they want and they might be grabbing data from the primitive uniform buffer if they were they need to make sure that you go in and change those custom nodes to use the get primitive data accessor function. So this is something where if you update to 422 and some of your materials are not compiling anymore there's a good chance that this is why so go and look for custom expressions that might be breaking the new rules. The forward render can now only support a single global planar reflection because again we to do multiple global planar reflections we would have had to take into account purview information in this case. And I already talked a little bit about this but current you for projects may or may not see benefits so I just want to caveat it you you might go home you try 422 and like where is that 10x speed up well here's here's why you might not be getting a 10x speed up just by updating 422. But that said we give a couple of testimonials so when we release the preview release we had some developers on Twitter who were talking Joe winter green mentioned that in his project 422 ended up saving about a thousand draw calls. And the shores unknown guys were letting us know they had data seen that was around 18,000 draw calls with 422 now it's around 2.3000 and so they're seeing that was running at 30 FPS is now running at 60 FPS just by updating to 422 so depending on how you built your scene you might get a massive speed up you might not but at the end of the day I think the we've managed to allow the renderer to scale much better with with far with a larger set of draw calls and so going forward you'll be able to build larger more complex scenes. There was one caveat that I did forget to mention around draw call merging as well and that is in 422 we don't support draw call merging on mobile and the there's no technical reason for that we just didn't get to it in time for 422 we will have it in for 423. The simple reason for that is there are different passes in the mobile renderer versus the deferred renderer and we need to go in and do some of the same transformations of pulling out data that we were binding per draw call and make sure that we put them into per pass buffers or put them into like global scene buffers that we can then access per primitive. That's it that is an explanation of the new measuring pipeline in UE4 so thank you for coming out to the talk I hope it was useful I hope I hope you learned a little bit about why we did it and I hope when you update to 422 you do get a massive improvement in the future and if not add more meshes to your scene.