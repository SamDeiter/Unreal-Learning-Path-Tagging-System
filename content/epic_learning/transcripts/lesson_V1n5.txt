Hi everyone, I'm Deguri and I will be guiding you through this lecture on the gameplay ability system. This is the second session in our guest series. Today we will be creating a simple game that incorporates gameplay gimmicks using gas. Let's go over the learning objectives for this session. First, you learn how to activate the necessary plugins and initialize the core component of gas, the ability system component. Next, we look at how to design gameplay abilities and gameplay tags to allow for interaction between actors. Lastly, you apply the core components we've covered to build a level that includes a simple gimmick element. Before we begin, let me show you a quick preview video of the final result you will be building in this session. We will be working through 200 exercises today. The first simple example, based on the third person template, to help you get familiar with the basics. In the second exercise, we work through the core setup for the IPG project, we will be working moving forward and build a basic gimmick level within that context. Here are key components we'll be focusing on in this lecture. The ability system component, gameplay tags and containers, and the basic setup for gameplay abilities. These components each server specific role, with a cyclical structure that the gas framework is designed to support. Let's begin with the core of gas, the ability system component. Since it's a long name, I will reprint it as ASC from here on. The ASC is a component that can only be attached to one actor at a time, and is what enables an actor to use the gas framework. That's because ASC handles much of the internal logic of gas, it's essentially the CPU of the system. I often refer to any actor with an ASC attached as a gas actor. These gas actors can interact with one another through their ASCs. Actors equipped with the ASC can activate gameplay abilities, essentially, they can perform actions. These actions allows the actor to interact with others, and the receiving actor reacts to those actions and feed the research back into the system. It's a two-way process. So think of the ASC as the central component that drives this interaction. Now, let's take a look at how the ability system component is initialized. Inside the ASC, various environmental data, necessary for performing actions, is stored ahead of time. This is encapsulated in something called ability actor info. Ability actor info contains two key pieces of data, the owner and the avatar. The owner is the actor that owns the ASC, why the avatar refers to the physical actor in the game that will actually perform the action. As shown in the illustration, the owner and avatar can be the same actor who was separated. In simple single player games or for NPCs, they are often set to the same actor in multiplayer environments, however, it's common to separate them for performance and clarity. Ability actor info also stores references to the player controller, skillet image, animation instance, and movement component. Everything needed to configure how gameplay abilities behave. Let's now move on to gameplay abilities. Gameplay abilities are what drive the various actions you see in games, attacks, spells, special skills, and so on. With enough experience, you can combine these abilities to create complex situation specific action sequences. Gameplay abilities are designed to be triggered by player input, but they can also respond to in-game events as a reactive triggers. In short, a gameplay ability is a functional unit that tells your avatar actor how to animate and move based on context. This is what powers your character's actions. So how does a gas actor actually activate a gameplay ability? Each ability is created as its own independent class. You assign this class to the ASE, and the system then recognizes that actor has access to that ability. In other words, the actor is now ready to perform that action. This is done using a function called give ability. Once given, the ability is stored internally within the ASE as a structure called spec handle. As long as you keep that handle, you can trigger the ability whenever needed. When the ability is triggered, it is instantiated into gameplay ability instance, which then receives the ASE stored ability actor info. Using this, it can issue animation and movement commands to the avatar. Let's take a look at the final component, gameplay text. Gameplay text is lightweight markers, managed as app name type in unure engine. While similar in function to regular actor or component text, they are bit different in practice. Gameplay text are set up and managed in your project settings, and can be used globally across the entire project. Initially, they are stored in a file called the default gameplay text that I and I, but they can be expanded as needed. What sets gameplay text apart is their hierarchical structure, which allows for clear, organized categorization. For example, if you want to define text for character actions, you could create a top level text like character.action, then define subtext like attack, skill, and so on underneath. This structure can also be used to tag character stage or system trigger the event, making your game logic easier to manage. On the right is a screenshot of the gameplay text manager interface. We will explore it in the hand zone session. To use gameplay text effectively, you should also understand the concept of a gameplay text container. As the name suggests, it's a container that holds multiple gameplay text. Azure system grows more complex, handling everything with a single text becomes impractical, so it's often necessary to group them. That's where containers come in. Some API functions even require a container, so even if you only want to use one tag, you need to wrap it in a container. Here are some examples where multiple text are useful. Let's say your character is currently attacking. You might want to distinguish whether the attack is a physical merely strike, a ranged skill attack, or magical fire based spell. By combining multiple text, you can describe this context precisely. So far, we've explored the core components of gas. Now, let's move into the practical part of today's session and build a simple method that includes a gimmick using what we've learned. Let me work you through the first example we'll be working on. First, create a 30% template project and name it example 111. Once it's loaded, you will see the familiar default map. Our core here is to implement a feature that prevented a player from jumping in a specific area of the map. But instead of doing it the old fashioned way, we are going to use the gameplay ability system to show how clean and efficient this can be. First, go to plugins and enable the gameplay ability plugin. Once that's done, restart the editor to activate it. Once the plugin is active, the first task is to modify the 30% character blueprint. Open the character blueprint and add a key component. The ability system component, which is the core of the gameplay ability system. Let's name this component asc. This sets up the foundation that allows this character to interact with the gameplay ability system. Gas framework. You will notice there is a ready of built-in jump function. Instead of directly calling this built-in jump function, you will convert the jump into gameplay ability and treat it like a skill activation and cancellation process. In other words, we will cast jumping as a skill that can be activated and cancelled. So, we need to turn the jump functionality into a gameplay ability. To do this, we will create a new class based on gameplay ability. Epic Games already provides a prebuilt jump ability in the Unreal Engine source, so we will use that to simplify our implementation. We will create a blueprint based on that class and name it BPGA jump. You might see on error when saving. Don't worry. Just change the instant scene policy to instant per actor. Save it again and you are good to go. Now it's done. This jump ability is a little as a logic built-in, so we don't need to script anything extra. Now, we just need to set up this blueprint to actually use it. Next, we need to assign this jump ability to our character during initialization. Scroll down in the character blueprint and add logic that uses the giveability function from the ASC to assign the BPGA jump ability. Use the giveability node from the ASC to assign BPGA jump. Now, the character has the jump skill, but it's not being used yet. Having the jump skill does minute activates automatically. We need to trigger manually, like when the jump button is pressed. To do that, we need to something that acts as a reference or handle for that ability, and that's where the spec handler comes in. The spec handler is a structure that represents a granted ability, and by storing it, we can use it later to activate or cancel the ability whenever we need. Store that handle in a variable like a jump spec handler. Once stored, go back to the original jump function and replace it. Replace it. Rather than calling jump directory, use the try activated ability node, passing in the stored spec handler. To the same here, for stopping the jump, we will cancel the ability using the handle and the ASC. So, we retrieve the ability spec handler to check if the ability is currently active, and then pass a reference to the ability system component, as is required by the function. To the same for stopping the jump, retrieve the active ability instance using the spec handler and ASC, then call cancel ability to explicitly stop the jump when the input is released. Let's check the result. Press play. You will see that jumping works just like before, but now it's powered by gas. And that's how we replace the original functionality with a gas based jump ability. Now, let's explore the advantages of using gas for this. We will create a trigger volume that develops jumping when the character enters a certain area. Create a new blueprint based on the trigger box, and name it BP blocks on. Placing the trigger box, make it wide enough to fully cover the area. I think this should be enough to flip block jumping in this area. We will implement logic in this one area for now, and expand it later. Open the blueprint. You will use two events, own actor begin overlap, and own actor and overlap. When the character enters the zone, we want to block jumping. When they leave, jumping should be reenabled. Traditionally, this would involve adding a brilliant variable to the character, and sending it over via blueprint communication, which leads to casting and other complex setups. We can avoid that kind of complicated setup, and solve the problem very simply by taking full advantages of the various features of the gameplay ability system or gas. The key to making this work is the gameplay text. Let's go ahead and set that up. Go to project settings. There is a section specifically for gameplay text. In there, you will find the tag manager. Create a new tag that clearly represents what you want, something like a character that block that jump. We will define the tag using a clear, easy to understand hierarchy. Then, we will save it to the default INIF file. Once the tag is created, we will use it in our logic. When an actor enters the trigger zone, we can apply it to the gameplay tag to that actor. To make this flexible, we will set the tag as a variable. Let's call it block tag. Once we compile, we can set its default value. Here, we will assign the character that block that jump tag we just created. When the actor leaves the zone, we will simply remove the tag. We will use the same block tag variable for that. This way, we don't need to create extra variables on the character itself. By using text through the gameplay ability system, we can track the character's current status efficiently. Let's test it. Press the killed key to open the console and enter show debug ability system. When the character enters the zone, you will see the character that block that jump tag added to our character in the default view. What can we do with this? Go to the gameplay ability settings. There is a field called activation block tags. Add the character that block that jump tag there. Now, if our character has this tag, the jump ability cannot be activated. With this single setting, we can disable jumping in the zone. No extra logic required. Imagine expanding this. When players enters the zone, you could prevent them from attacking. Allow only dialogue or enforce any other condition of behavior just by managing tags. This shows the power and flexibility of developing games with the gameplay ability system. Next, I will show one example that builds on this foundation. Let me introduce the simple RPG project we will be working on. First, unzip the provided example 1, 2, start gif file and rename the folder to example 1, 2. Inside, you will find the folder and files necessary to run the project. To use gas properly, we need to build a C++ code module. The easiest way to do this is by using the free visual studio community edition. If you don't have a C++ development environment setup, please download and install it from the site shown in the video. Instructions for installation are available on the Epic Games website. Once the visual studio is ready, shift right click the project file and select generate the visual studio files menu to create a solution file. After the generation complete, double click the slln file to open the solution in visual studio. The project name is a renavated gas. We will report it to sab for short. Make sure it is set as the start of project. If we are not bolded, right click on it and choose setup as start of project. For convenience, we preview the basic functionality on related to gas. Let's work through the code together. All classes use the AB prefix to distinguish them from engine classes. We'll begin with the base character class, AB base character. Open the header file to understand the overall structure. This class includes wizard and weapon components for UI and equipment visuals. It also declares the required ability system component for gas. Now, open the cpp file to see how initialization is handled. The constructor creates the ability system component object, basic meshy and movement setup follows. Weapon and HP bar settings are also configured here. Back in the header, you'll notice that this class implements iability system interface, which is recommended when using gas. As required by the interface, we implement to get ability system component function to simply return our ASC. Next, let's check the AB player character class. Our game will use both player and NPC characters. Players in a from AB player character, NPCs from AB base character. The player character needs proper camera and input configuration. Here's the camera setup, and this part is for input bindings. In the constructor, we configure a top-down camera system. Additional settings are needed for player controls. In begin play, we change the input mode so that inputs are recognized immediately. And then, bind movement related functions to the input component. Let's look at the possessed by function called when the controller possesses the character. This is the best place to initialize the ability system component with owner and avatar info, because controller is confirmed here. Both are set to this to complete initialization. The animation instance and blue prints are also pre-configured for responsible motion. These aren't called to this lecture, so we'll move on. Finally, open the build.cs file and verify module dependencies. We've included UMG, gameplay abilities, and gameplay tags. Go to the menu and build a solution. Wait until the output shows build succeeded. If we fail, close all running on your engine editors and try again. Once successful, run the project using start without debugging. Once the editor launches, you will see two main folders, Arena Battle and Arena Battle gas. Core access are in Arena Battle. All your work will happen in Arena Battle gas. Open the blueprint folder in Arena Battle gas. You will see BPAB PC. This is a blueprint based on ABA player character. It includes logic to display how the UI represents the player's state. It also has a component pre-configured to play visual effect when the game is cleared. Let's add one more feature here. Previously, we used the console command, show debug ability system to debug gas. We'll not automate that. Add an execute console command node. Enter show debug ability system as the command. Close this and return to the game folder. The game model sets this character as the default player character. This is because in the project settings, we've already set the default game mode. So when you press play, the configured character will appear automatically. There are two key actors in the current level. One is gimmick section. The other is gimmick box. Let's open the gimmick section actor to inspect the structure. I've pre-added an ability system component. Beep for to show two doors. Start always open. Access is closed. When the gimmick is solved, we will rotate plus gate by minus 90 degrees to all-low passage. Begin play is declared. We will expand this logic soon. Next, open the BP gimmick box from the item's folder. It has a trigger volume as the root, plus mesh effect and ASC component. This event grab has a simple logic setup already. When the player interacts with the box, the effect plays and the box disappears then. That wraps up the setup. Let's begin implementation. We have two main goals for this lecture. First, one of the three boxes in the first section will clear the gimmick. Second, the box in the second section will clear the game when collected. Open the gimmick box blueprint to begin. When the correct box is collected, it should notify its target gimmick. So we add a target gimmick variable. Set its type to actor reference. Enable instance editable so we can assign it in the editor. When the overlap event fires, we will check validity and apply attack. Use Maker Gameplay tag container from tag to define a tag. Click plus button to add a new tag. Name it event.gimic.cleared for clarity. Assign this tag and save the blueprint. Now, let's move to Bpp gimmick section. This actor needs to detect whether it receives the clear tag. If so, open plus excate. On Begin Play, mind-tack changes. Drag in a ESC, search divine, and mind-tack change event. Use the event.gimic.cleared tag. From the red pin, create a custom event named ongimic.cleared Bpp. Drag in plus excate and set its relative rotation Z to minus 90. Assign gimmick section 1 to the correct box to write most to one in section 1. Play the game. Lapt and middle boxes do nothing. Write box opens the gate. Next, let's make the second box triggers the game clear. Open the gimmick box blueprint and add a game clear, blue and variable. Make it instance editable too. In Begin Play, if game clear is true or assign the player upon reference to the target gimmick. Set the tag to event.gim.cleared instead. Drag from the gameplay text pin and use select to choose between two tags. If first, send the original tag. Duplicate the node with control D. Change it to event.gain.cleared and connect it to. Now go to the player blueprint. Just like the gimmick, mind-tack changes in Begin Play. This time, use event.gain.cleared. Name the event on-game.cleared BP. Here, we trigger a free computer firework effect. And show the hidden celebration message in the how the UI. The UI already has a function called setgame.cleared. Let's briefly look at the function in the UI. The blue text is hidden by default. When setgame.cleared is called, it becomes visible. Go back to the second gimmick box and check game clear. Press play and test it. As expected, the firework score off and the message displays. And that's how gas allows streamline the labate design without complex blueprint communication. This concludes the lecture with this example. Let's summarize what we've covered in this lecture. Actors using gas are controlled through a single ability system component. And this setup enables structured interactions between actors. When on ASC is initialized, it stores the owner and avatar actor references in ability actor info. To perform a specific action, a gas actor needs to be assigned a gameplay ability using the Gibra ability function. Once assigned, the ability is stored in a spec handle, which can later be used to trigger the ability at the appropriate time. GameplayText operates globally across the project, allowing you to share game state without needing custom variables, making your system more efficient and modular. That concludes this lecture. Thanks for sticking with me and I hope you are excited for what's coming next. I'm Deguri and I will see you in the next session. Thanks again.