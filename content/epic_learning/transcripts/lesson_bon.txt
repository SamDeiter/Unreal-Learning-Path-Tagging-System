Let's now take this a step further and demonstrate how you can set up Blueprints to allow your viewer to click on the object and change one of the global material parameters. We'll go to the Blueprints button at the top here and open our level blueprint. Now if you've been following along with the course, you should already have something set up similar to this, which will allow us to show the mouse cursor to click on objects and then to detect which object we've clicked on. If this isn't the case, be sure to go back to the previous chapter on dynamic material instances and follow that to set this up. We only really need to get to the part where we've set up the break hit result and our left mouse button click. So the first thing we're going to need to do is get a reference to the lamp shade in our scene. So I'm going to go ahead and make sure I have that selected in the viewport. And then in our Blueprint graph here, we're going to right click and choose Create a reference to SMWalSconz 001. Now we need to test if the object we've clicked on is in fact this wall sconce. So we'll go ahead and pull off of Hit Actor and we're going to type the Equal sign on the keyboard. And we want to choose Equals Object. From here, we're going to pipe in our Reference Object into the input of that equal node. And then of course, we'll need an if node or branch to find out whether that is true or false. We'll pull off of that and do a search for if. And there we go, we have our branch all set up. From here, we're going to pipe the true output of our first branch here into the true input of our second branch. Now in order to change the color of the object, we're going to need a new node. We're going to right click here and do a search for Parameter. The one we're looking for is going to be found under Rendering Material. And we can see the four here we have Get and Set, Scaler and Vector Parameters. In this case, we're going to be working with the color or the subsurface tinting color. For that, we're going to need a Vector. We don't want to get the Vector. We want to set the Vector Parameter value. So from the true output of our branch, we'll pipe that into the input of our execution flow there. And from the collection drop down, we're going to choose NPC underscore lampshade. From the parameter name drop down, we'll choose SS Color Tent. For the value, we're just going to do something random. So we'll right click and do a search for random. What we want is random unit vector. Now we can pipe this into the input of our parameter value here. And of course, it automatically converts the information to the correct parameter value. Now we can compile. As long as we have a green check, everything should be working. Let's go ahead and try it out now. We'll press the play button at the top and now we can walk over towards the lampshade. And we'll start to click on it. And we can see the tinting of our subsurface color change. So that's a basic look of how to build and use material parameter collections. Material parameter collections are going to allow you to control multiple materials in your scene at the same time using the parameters inside of the material parameter collection window. This way you won't have to go into each material and change them separately. But if you set up your material correctly, you can still tweak them individually based upon the global settings as well. Up next, we're going to take a look at how to customize your material workflow by creating your own custom material functions.