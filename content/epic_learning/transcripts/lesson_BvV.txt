In traditional cell animation, inclines were often used to make the flat images more pronounced. Many films and video games implement an outline effect as a part of their aesthetic style. And this module will create an outline effect with a post-process material. Our outlines post-process material is partly going to rely on drawing from the scene depth G-buffer. In order to visualize what this looks like, if we go to the lit button down to buffer visualization, we can browse down to scene depth. We see that we have a gray scale fog on our scene where dark values are near and light values are far. So now let's create a new post-process material. In the post-process folder, if I right click I can create a new material and call it ppmm underscore outlines. Let's open it and let's set the material domain to post-process and the blendable location to before tone mapping. We're also going to want a scene texture node. Over in the scene texture ID menu, if I expand it, the second option down is scene depth. This is what we're going to want. Now as we see here, the scene depth is gray scale. We don't need red, green and blue values coming from the color output into the emissive. We just want one of the values. So let's do that with a component mask. If I left click and drag off the color pin and release, and I begin to type component mask. Under math, I see I have a second option, component mask. In the details panel, I see I have the ability to either add the red, green, blue and alpha or selectively only add a few. So let's only select the red channel and plug that into our emissive color and save it. Now if I minimize this, making sure I have my post-process volume selected. If I click the plus button under my rendering features, I see I have the option to add a second blendable to the array. So just like we did before, let's choose asset reference, select our material and add it. We're going to want to left click and drag this to the top of the stack because we want our outlines to draw on top of our base color. But we can't see it because we're in a buffer visualization. So let's go back to the lit mode and see what we have. And we can see we need to do some work to the material because the screen is white. So let's go back into the material. If I take my scene depth and I copy it and paste it here, I can now use this scene depths inverse size to drive where this scene depth will render on the screen. The size is exactly what it sounds like. It's the resolution of your image. The inverse size at a high level allows us to do an edge detection or a sobal operation to draw outlines. Screen Unreal Engine in Screen Space UV coordinates are stored in the red and the green vectors. So if I hold 2 and left click, I can create a 2 vector that will give me the red and the green vectors. The red vector contains the u coordinate which is going to be responsible for movement of pixels left to right. The green contains the v coordinate which is going to be responsible for moving pixels up and down. If I multiply the inverse size by a value set in either of these coordinates, I can manipulate where the pixels will be rendered. In order to do that, if I hold M and left click, I can create a multiply node. Now let's plug the inverse size into the A input and the 2 vector into the B. Let's set the green or the value to 1. Now in order for this scene depth to understand that this is in fact a UV coordinate, we need a texture coordinate node added to this shader code. In order to create that, if I right click and I type text, I see under my coordinates section, I have a texture coordinate. Now if I hold A and left click, I can plug the texture coordinate into an add node and then plug the result of the multiply into the same add and then plug the result into the UVs of this scene depth. Now we're still not going to see anything because we have to subtract this from the unaltered scene depth. We've now moved where this scene depth is going to render one pixel up. In order to see it, if we take this scene depth, if we copy and paste, we have to take the mask and copy and paste it as well because we only want grayscale value. And now if I right click and I begin to type subtract, I can see under math, I have a subtract node. So let's plug this into A and let's plug this mask into B and then the result into the emissive. And right away in the shader, we can see that we're shading the top polygon, so let's hit save and look at the result. To demonstrate further, if I go back to my two vector and I want to manipulate the V UV in screen space, if I invert this to negative one and hit save, I now see I'm shading the lower sides of the polygons. Let's set this back to one and hit save. The other thing we're going to need to do is get our base color to render wherever the dark values are. In order to do that, we need to create what's called a linear interpolate node. If I right click and I begin to type linear interpolate under math, we'll see as the second option. A linear interpolate node is a blend node. You can blend between two values A and B with a mask that will be plugged into your alpha. Let's use our current shader code as our mask. Now let's take this scene texture, scene depth material expression. Let's copy it and paste it here. Let's go back to our scene texture ID and let's browse to post process input zero. Now what post process input zero is is the unaltered render after it's gone through the post process pipeline. Before we apply our outlines, our post process is rendering the base color. So in order to get the base color to render under our outlines, we need to set post process zero as our A input into our LERP. Now if we take the output of the LERP and plug it into the emissive color, we also need to mask our post process input zero to be RGB. So let's copy this mask, paste it here, include the green and the blue channels, and plug our post process input zero into this, and then plug the result of that into our LERP. Now if we hit save, we can see that we have the base color rendering underneath the first lines that are happening on the tops of all the polygons. So now that we have shading on the top of our polygons, let's create it on the bottom sides of our polygons as well. We go back into our material and we select all of the shader code that we're using as our current mask to shade the tops of our polygons, and we control C, control V, and then I create another ad node, and I add this line of code with this line of code, and then I go to the two vector of the newly pasted shader code, and I change the green vector or the V coordinate from one to negative one, and then I plug the result of the ad into the alpha, and then I hit save, I can see now that I'm beginning to get some lines on my scene. Now if we go back into our material and we take both lines of code, let's expand this to full frames so we can see a little better, if we take both the top polygons shader code and the bottom polygons shader code, plus the ad, and we copy all of that and paste it again below, we can draw lines on the sides left and right of our polygons. If we change this two vectors value, the green or the V coordinate back to zero, and then we go to the red vector or the U coordinate, and we set this to one, and then we go down to our second two constant, we set the green back to zero, and the U coordinate to negative one, and then we add the result of this to this, let's move our material over here so we can see a little more what's happening, let's move our lurp down, and then we we're plugged the result into our alpha, and now we hit save, we're getting lines on the tops and bottoms and both sides of the polygons, but they're rendering white and we need to be able to affect how they are colored, so back in our material, if we hold three and left click, we can get a three vector, which is going to enable us to assign an RGB value to what the lines should look like, this is going to need to be plugged into the B of our linear interpolate, sometimes when you're creating materials like this, the lines can become a bit confusing because they will intersect other nodes, and in order to organize things better, if you double click anywhere on the line, you can see I've created a little manipulatable point and I can move the line out of the way to get a better organization. So now we technically should have black lines on our scene, so if we save, we see it looks a bit better, but we don't have black lines yet, and the reason is because we need to clamp the result of all of our code that's going in to create the alpha that's defining where the lines are. So let me move over the base node of our material, and in a blank space, if I right click and I begin to type clamp, I can see under math, I have a clamp, and this is going to clamp your values in between a designated constant, and by default, it's going to clamp them in between zero and one, which is what we want. So let's plug this into the alpha, and now when I hit save, I can see that I have black lines as I would expect drawn on my scene. We now have a post process material that renders outlines on the levels polygons. The effect works, but we'll need additional controls to eliminate the flickering that's happening, and the following modules will add outlines to the levels normals, as well as explore some options to polish the outlines.