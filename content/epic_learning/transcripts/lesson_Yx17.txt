While it's now possible to build in the game with a base block, having a wide variety of blocks to build with will make the game much more fun. Let's add a dirt block, a grass block, a brick block, and a wood block to get things rolling. Our objectives for this module are to create several child blueprint classes of the BP base block. We'll add a way to switch between which block we're currently building with, and finally we'll provide a way to remove blocks from the world. To create the new blocks, make sure you're in the Blueprints folder and then find the blocks folder and open it. Going to make child classes of the BP base block. To do that, right click on the BP base block and select Create Child Blueprint class. We'll name the first one, BP underscore dirt block. Then open it for editing. Select the block mesh and find the Materials section. There are three materials. Element 0 is the top material. We'll change that to dirt. In fact, we're going to change all of these materials to dirt. Element 1 is the side materials and we'll change that to dirt as well. And then the element 2 is the bottom. We'll change that to dirt. The dirt block's pretty straightforward. Select Compile and Save and close the dirt block. Right click on the BP base block again. Create Child Blueprint class and name this one BP underscore grass block. The grass blocks a little bit more complicated. Select the block mesh. It will have three different materials assigned to it. So for the top, click on Element 0, type in grass and select MI grass top. For instance, grass top. For Element 1, type in grass again and find the MI grass side. And finally for Element 2, type in grass once more and find the MI grass bottom. Compile and Save. For the Brick block, repeat those steps. Click on the base block, create Child Blueprint class, BP underscore brick block. Open in the Editor, select the block mesh and type in brick. MI brick, we're going to use the same texture for the top, the middle and the bottom. So brick again and brick. For the bottom, and Compile and Save, close that. Right click on the BP base block, create Child Blueprint class, BP underscore wood. Open that up for editing, select the block mesh. Over to the materials, wood, material instance wood. Wood again and wood once more. Now save and close the BP wood block. After you finish the project, you can bring in as many materials as you like. You can make your own materials and bring them in and use them on your own blocks and make as many blocks as you like. Before we get to that though, let's provide a way to switch which block we're currently using. Go to the Blueprints folder and open up the BP block player. Now this is the code we wrote to spawn a new block and we should put a big comment around that. So drag and select the whole piece of Blueprint code and hit C and type in spawn a block. Now let's observe over here at the very end where we spawn actor BP base block, the class BP base block is the class reference that we're spawning. What we'll do is we'll create a variable for the current block and then use that here and switch that variable's value depending on which block we want to use to build. To do that, let's add a new variable, click the Add Variable button and call this Current Block. For the variable type, click the drop down type in BP underscore base block and from the BP underscore base block, we want a class reference, not an object reference, an object reference is a reference to an instance of the class. We want a class reference which is going to tell us which class we'll use to build the block with. Let's click compile and provide a default value for that. Now notice when we click compile, this is currently set to none, that drop down will have all of the classes that are BP base blocks and that's the thing about inheritance. Although a dirt block is a different class, it's a child class of the BP base block. Therefore it is a BP base block as well as being a BP underscore dirt block. So let's set the default block to the BP base block. We'll switch that later. We'll provide a way to switch that. We'll just set that to BP base block for right now. Drag out the current block variable to a get on it, holding down the control key. Sorry, I probably said I wasn't going to mention that again, but I did. Let's drag off of the current block and hook that into the class. Now nothing's changed just yet. We're still making a new actor of type BP base block because the current block has that value. Let's provide a way to change the value of current block. Just find an open area in the event graph, right click and press the one key on your keyboard. And for keyboard event, let's choose number one. And then let's drag out current block and do a set on that. So we'll say when the one key is pressed, we'll set this to BP base block. We click type in to on your keyboard and select keyboard event to. And I'm just going to duplicate this node, select it, control W. And when two is pressed, I'll set current block to the dirt block. And right click and three is pressed, duplicate this control W. And when that is pressed, we'll set this to the grass block. Right click for duplicate. This will be the brick block and right click one more time. Five and duplicate. That will be the wood block. So we're changing the value of the current block variable, depending on which key is pressed. I'll highlight all of those. But a comment. Change the current block. Now let's test it. Compile save back to the map, press play. Now, nothing should have changed. We should be putting down the base block right now. I'm going to tap two on the keyboard. And now I'm placing dirt blocks. And now three should be grass blocks and four. And we've got the brick block and five. Finally, we've got our wood block. So we provide a way to change the current block. Last bit of functionality we'll add for this module is to provide a way to remove a block. Let's find an area near the ad block code. And it's going to look very similar to this. It won't be quite as complicated because we won't be turning towards where we're removing the block. So let's right click and type in remove block to get the action event remove block. And in the same way, we did a timer and an event and a clear timer. We're going to do those nodes as well, similar nodes as well. These will be for removing a block. So we'll drag off of the press set timer by event. And we will right click add custom event. We'll name this remove block from world. And we'll hook up the delegate. Set the time to point to and make sure looping is checked. And finally drag off of that set timer by event node. And call the remove block from world event. So it runs immediately when we start doing that. Let's also come right off of the release tab and clear timer by function name. And again, to get the function name right case where any misspellings here, select the custom event press F2, control C back to function name here, and control V to paste that in. All that's left to do is write the code to actually remove the block. So to do that, we'll call the get crosshair location function. We previously wrote that up that will return the location of the crosshair. We'll call the get clicked on block function that we previously wrote. And that up and use the crosshair location as the target. We'll make a branch hold down the B key left click. And if we successfully clicked on a block. We'll need to check if the block is breakable because we don't want to delete the floor, which is not breakable. And later we might have other blocks that we don't want to break. So we'll drag off of the hit block and type in is breakable to get its is breakable value. And we'll make another branch B left click and make that a condition. And if that's true or if it is breakable, we'll drag off of the hit block and type in the destroy actor there it is right there. We'll destroy that block. And I'm going to use a reroute node double clicking on that. Why are you needing that up a little bit? And that's pretty much it for the remove block functionality. I'm going to highlight that a C at a comment and remove a block. And of course, no code is correct until we test it. So let's compile save come back to the map, click play. And let's try using some more interesting blocks at press three. Use grass blocks and then right click to remove those blocks. It's right click on the floor to test the floor is not going away, which is good. And now we can really start building and modifying our builds by adding blocks and removing blocks. In this module, we created several child classes of the BP base block. We added a method to switch between which block we're building with. And only we added a way to remove the blocks from the world.