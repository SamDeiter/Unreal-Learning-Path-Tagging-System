Welcome to the third coming to the rescue challenge, a module where we will build upon what we learned in this course combined with previous courses to solve a problem. The previous modules have prepared us to solve the problem on our own to help our friend get their treasure. In this problem, we will need to finish building our object spawner mechanics so that we can build a bridge to reunite our friends. We have created a collection of mechanics that we can use to interact with our scene. Our newest being interactable widgets that spawn directly from our tracked hands. These spawned widgets will be used to spawn new actors in front of us so we can once again solve the problem. Open the UI problem map and simulate the scene. We can see that there are now three friends in the scene and they all want to be together again. It is our job to spawn various meshes and build the bridge that they need. Before we start, open the AI friend blueprint. Panned down to the target found event. Add a call to celebrate and set treasure found to true so they can stop searching. Okay, let's finish setting up our spawn mechanic. Inside of the enum's folder within Blueprints, add a new enumeration named eSpawnableType. We'll use this list to name the actors we'll be able to spawn. Create six enumerators. Or ramp, wall, roof in, roof out, roof straight. Using these six meshes, we can pretty much rebuild the map we have here. So there are lots of options. Let's create the blueprint that we will spawn. Inside of the Blueprints folder, create a new blueprint of type actor and name it BP Spawnable. Set our new eSpawnableType as a variable and give it the same name. There will be a few familiar components to add that will allow us to manipulate it. First, add a static mesh and set its scale to our .05 that we've been using in these maps. Create a new function named set and place mesh. In the static mesh component, add a set static mesh node. Drag backwards from new mesh and create a select node. Plug eSpawnable into index. Now we can set which mesh will be spawned based on the value of eSpawnable. Find these meshes in the content browser. Here are in world builder assets, architecture, and each in their own folder. I'll go ahead and quickly set them. When each has their value, call set and place mesh in the construction script and look in the viewport. As we change the value of eSpawnable, see that the mesh is sort of moving all over the place. We need to make sure it stays in the same spot regardless of the mesh's pivot. Come to set and place mesh. From static mesh, create a set relative location node. Keep new location at 0, 0, 0 as we will need to reset its position with each change. Next, create an add world offset node. Get the world location of static mesh and also get component bounds. Subtract the world location by the origin. Then again, subtract the result to the z of box extent. Plug this difference into delta location. Return to the viewport and cycle through the various values of eSpawnable again. This function will now place the new meshes at the middle in the same spot and not above 0 in the z axis. This will work much better to spawn in under our menu. OK, let's finish bpSpawnable. Create another function named cycleMesh. We're giving the user the ability to scroll through the list both up and down. So add a bull and put named up. Create a branch and from true, add another branch. For the condition, drag in a reference to eSpawnable and check if it is greater than or equal to a byte. Plug the result into condition. Right click and search for get number of entries in eSpawnable type. If our enum has 6 entries, then the result of get number of entries will be 6. The enumeration though will start at 0, giving us 5 for the number of values. So we need to subtract get number of entries by 1 and plug it into the greater than or equal to node. From true meaning it is at its max value, set eSpawnable. Drag backwards and convert to a byte, keeping it at 0. This will give us the value in its 0 byte position, which for us is floor. From false, add 1 to eSpawnable and set it. From the starting branch, if false, we will do the opposite and not let the value go under 0. Again, create a branch. Check if the value of eSpawnable is equal to 0. If true, set it to 1 minus the get number of entries. If false, subtract by 1 and set. Return to the event graph and create a custom event named cycle spawnable. Call cycle meshes and then set and place mesh. From up on the cycle meshes function, drag to the event to create the input. And finally, create another event named has been spawned. Drag from the event and search for add uxt generic manipulator component. In the details panel, set two hand transform modes to only translate and rotate. Also set static mesh as the component name. Now we can interact with these meshes once they are spawned. Let's move on to the blueprint and the UI. Starting with the essential piece, open BP object spawner. Create a new event named check spawnable. Give it one input named should spawn and set it to a bull. Add a new variable named not yet spawned. Set it to type BP spawnable. After the event, create a branch from should spawn. From true, check if not yet spawned is valid. From is not valid, create a spawn actor from class node. Set BP spawnable as the class and set the return value to our not yet spawned variable. From false on the branch, check if valid again and if so, destroy not yet spawned. Call check spawnable at the end of both on begin tracking and on end tracking. Set should spawn to true on begin tracking. We need to add an event and a timer to constantly update the location and rotation of not yet spawned. So create a new event named display spawnable. From not yet spawned, create a set actor location and rotation node. Drag in a reference to static mesh and get the world location. Subtract by a vector and set the vector to 0, 0, 10. This will set it right below our UI. Plug the result into new location. Split the new rotation struct. From the new location result, add a find look at rotation. Right click and search for get owner. We can use get owner to represent BP pawn because when we spawned this blueprint, we set self as the owner. From get owner, get the actor location and plug into target. Split this rotation struct as well because we only want the Z value and plug that in. Pan up to the end of on begin tracking. Create a set timer by event node. From event, drag backwards and search for create event. Select the drop down and choose display spawnable. Set the time to 0.01 and looping to true. Let's promote the return value to a variable so we can pause it when our palm is not showing. Name the variable spawnable underscore timer. At the end of on end tracking, drag from spawnable timer and search for pause timer by handle. We just need to create the two events for our UI to interact with. Create the first one named user spawns mesh. When this event is called pause the timer. And from not yet spawned, call the has been spawned event and then set not yet spawned to nothing. Next, a new event named user cycles spawnable. From not yet spawned, call cycle spawnable. And drag the bull input into the event so it shares the input. To get the notification that our spawn button inside of the child actor was pressed, we need to bind an event to it. From begin play, drag in a reference to our child actor. From child actor, get the child actor and cast to uxt pressable button actor. As uxt pressable button actor, get the button component and search for bind event to on button pressed. Drag backwards from event and select add a new custom event. Rename it to spawn button pressed. And immediately call user spawns mesh. Okay, our last bit before we can test all of this. Drag in a reference of widget and search for get user widget object. Cast to our w object spawner. And bind an event to UI button pressed. From event drag backwards and search for create event. Set the signature to user cycles spawnable. Okay, copy the widget and the cast node and paste it after we spawn BP spawnable in our check spawnable event. Paste it after we spawn the new actor and call set name. From not yet spawned, get e spawnable. Convert the enum to a string and plug it into in text. Select all of these new nodes and right click. Select collapse to function and name it set UI mesh name. We can remove the inputs that were given automatically. Open this function and plug in not yet spawned into target. Return to the event graph and call this function at the end of user cycles spawnable. So the name updates with every change. Okay, that was a lot of small steps. Let's finally help our friends reunite. I'll first test this interface. I can successfully cycle through each of the meshes. I can hide my palm and everything is gone. If I reveal it again, it's all back and it works with both hands. Let's just spawn this mesh here and now we can easily move and rotate it and place it wherever we want. There's no need to follow exactly what I do because now you can build the map all on your own. I'm going to add on to this path here with a floor mesh. Maybe another one here. We'll need a ramp to get up to this platform. Move it around a bit. And make sure it's nice and connected and there they go. They have been reunited. I know this was again a lot of work to get this all working. But think about how much we have done to create what has become a very custom experience. In this course, we created a 2D widget from scratch with the intention of altering lighting in our scene using buttons and sliders. We brought the widget into 3D and placed it in our map. We then added functionality to it and used provided MRTK and standalone assets to interact with it. We created a spawnable widget that appears when needed by flattening our hand out to a specific degree. We created a system of communication from hands to blueprints to widgets and back. We combined our 2D widgets with a pressable button actor to spawn interactable meshes into our 3D scene and use those meshes to reunite our friends. We took full advantage of the tools available to us to create a solution to the problem given to us. Creating an expandable system like this will be of great use in future courses and our projects in general. The heavy number of blueprints and nodes we've explored thus far should, by themselves, assist with many varieties of future projects. In the next course, we will discuss sound design for spatial computing and build systems to increase the end-user's immersion into our projects.