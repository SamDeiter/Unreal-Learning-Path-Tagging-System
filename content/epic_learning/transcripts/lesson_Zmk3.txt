Hi everyone, I'm Deguri and I'll be your instructor for the gameplay ability system lecture. This is the eighth lecture on the gameplay ability system. In this session, we'll implement a special visual effect for games using gameplay cues. Here are the longing objectives for this lecture. Learn how to implement a gameplay cue to provide a visual feedback in a way that does not affect gameplay logic. Learn how to set up a gameplay cue tech in a gameplay effect to automatically trigger an associated visual effect. Learn how to create a visual effect and screwing UI for damage at runtime. Before we begin, I will show you a preview video of the example we'll be working on today. In this lecture, we'll implement two special effects linked to a damage gameplay effect. These are the core components we'll be covering in this lecture. In this lecture, we'll focus on the static type among the two elements that make a gameplay cue. In this lecture, we'll build on the cyclic structure system we covered last time to learn how to display visual effects. A gameplay cue is a gas feature that handles additional effects that does not affect gameplay logic. When a gameplay effect is triggered, it modifies the attribute data that makes up the game system. And these changes are usually accompanied by the presentation of special effects. However, because these special effects only need to deliver visual or audio representation on relay to the actual game system, many games separate the two when building their systems. For example, the damage gameplay effect we used in the previous lecture reduces on attribute value called health. But the special effect that shows your character taking damage should be managed separately from the logic that changes the attribute. This is because such effects can be disabled based on user settings. And in a multiplayer network environment, there's no need for the server to play these effects. Guess worse in the same way. Gathering these special effects into a separate system called gameplay cue. These collected special effects can be played using a special gameplay tag that starts with gameplay cue. We'll refer to this as a gameplay cue tag. There are two main ways to trigger a gameplay cue. These visual effects are shown when on actual attribute value changes, which is a handled through a gameplay effect. Therefore, at the bottom of a gameplay effect, there is a section where you can specify your linked gameplay cue. Gameplay cues are divided into two main types, static and actor. Static is a suitable for playing special effects that explode instantly. Actor is a suitable for playing special effects that remain active for certain period of time. If you think about linking with the gameplay effect, the static type works well with the instant type gameplay effect we created in the previous lecture. Although we haven't covered it yet, the actor type works well with the duration-based gameplay effect, which we will discuss in a later lecture. One thing to note, when creating a static type gameplay cue is that, it triggers the on-executive event when activating the effect. You can simply add the relevant special effect to play when the on-executive event is triggered. Now, in our practice project, we will use the on-executive function of a gameplay cue to add visual special effects for attack damage. I will prepare the example 7-starthicified in advance for this hand-drawn practice. When extracting the archive, I will rename the folder to example 7. Now, let's set up the basic configuration for the practice. Shift right-click the project file and select generate visual studio project files. Generate the solution file. Double-click to open the solution. Make sure the start-up project is set to arena-better gas. And then, build a project. Once the build finishes, launch the editor. As in the previous lecture, I duplicated the level we implemented last time, named it step 7, and set it as the default level. In this practice, we work only in blueprint without C++. Since gameplay cues operate independently from gameplay logic, I will manage them inside the folder named GC. Gameplay cues come in two types, static and actor. In this lecture, we only create the static type. Create a folder named static. The first special effect will implement is the damage blink effect. Search for gameplay cue and set static as the parent class. Name it GC and Material Link Damage. Static types run as one-shot, triggering the own executed function. Overwrite this function to implement it. Here, remove the parent logic and check, so that the result comes out as true. Here, we will change the character's material parameters, and I will let it prepare the material for you. Since the NPC uses the 4G character, I will open the material. Among several parameters, they want to note our start time, and duration. Specialize the start time in start time, and set the activation time of the effect in duration. With these two parameter values set, we will pre-configure it, so single-blink effect executes. Get the mesh component. There is also an effect color parameter to change the color, its default is red. The same parameter has also been configured in other character materials. Now, let's return to the gameplay cube blueprint and modify this parameter. Check if the actor is off-cooked time, so get its material. Get the mesh component. Use the set material, the color parameter, node. For the first parameter, enter start time. Use get time seconds to pass the current time. This way, the material change is starting at the moment the gameplay cube triggers. Then, use the same node saturation. Setting it to 0.5 seconds for quick blink. Set the return value to true and finish up. To run this gameplay cube, we use a special gameplay tech starting with the gameplay cube. You can set this in the default values. Click plus create gameplay cube that material that blinked damage and assign it. Compile and save, then link with the damage gameplay effect. Go to the G-E folder, open attack damage, and in the gameplay section at the bottom, link it. Add a gameplay cube and set the gameplay cube tech at the bottom. Gameplay cubes can receive parameters, but since we are only applying a visual effect, we'll skip that here. Save and click play to test. When taking damage, you should see a red blink. But, in the current set of, even if the player is invincible and takes no damage, the blink still plays. So, we'll update the functionality so it only blinks when actual damage is dealt. We'll implement this manually. First, go back to the gameplay effect and remove the gameplay cube. Then, go to the NPC blueprint. Remove the print string node and add a sequence node. Check if the damage is greater than zero. If so, use execute the gameplay cube to call it manually. Set the gameplay cube tech here. Drag from the gameplay cube parameters, link the damage value to low magnitude. Compile and save. Copy this logic and apply it to the player as well. Remove the print string node in the player. Paste the copied logic and connect it. Now, let's move to the gameplay abilities hit check. On check the debug option, so the effect can be clearly seen. When we return to the NPC, the attacker gameplay effect is triggered at the start. Now, we'll use the effect to call it the effect. The attacker gameplay effect is triggered at the start. Now, if the damage is zero, it won't trigger to test, open the attacker effect. Temporarily, set the attack rate to zero and save. Now that the NPC's damage is set to zero, the material effect will not trigger on the player, but only on the NPC. Let's press the play button to check. You can see that only the NPC flickers, while the player remains unaffected. Now, go back to a tag up and set it slightly higher to 50. Next, we'll add a damage scrolling feature. To do this, create an actor that scrolls the damage value on screen. Name it BP scroll damage. Add a widget component to the actor and set it as the root. Set it to screen space and size it to 100 and 300. This will display a UI widget for damage. I will lightly prepare the one in UI folder. Double-click WBP damage to open it. It already has a number set and I will sign the animation. The fade animation comes with a built-in pop-out effect. In the graph, it's pre-configured to play the animation when the widget is created. I will also declare an update damage function to change the text. Back in the scroll-damaged actor. Use create widget to create the widget. The animation will play automatically. Set this as the widget in the widget component. Immediately call update damage to set the damage text. Create a variable named damage. Check instance editable and also check expose on spawn so the value can be passed when spawning the actor. Link this variable to the parameter. So it auto-converged to text. Set the actor's lifespan to 3 seconds. This way it disappears automatically after 3 seconds. Now let's create a gameplay queue to spawn this actor when damage is taken. Name this gameplay queue actor. Choose the actor's lifespan. Choose the actor's lifespan to save. This allows a special gas frame on naming rule matching the gameplay queue tag based on underscores. Go to project settings. In the gameplay ability section, under gameplay queue settings, specify the folder path containing your gameplay queues. Then return to the gameplay queue blueprint. Click plus and create only the tag as gameplay queue. That actor does scroll damage following the rule. After that, close and restart the editor to apply the changes. When you return, as you can see, it is automatically set according to the internal system rules. Even if you try to remove it, it always gets reset to the value. Now create the on execute function. Here, spawn the actor we created earlier. At this point, pass in the damage value that came through the gameplay queue parameters. Use the spawn actor from class node and specify the BP scroll damage class. Then, thanks to the exposure and spawn option, the damage variable appears. Connect the parameter's damage to it. Finally, connect the actor's transform to the node's transform pin. Now move to the NPC and use the same node. To create the gameplay queue, specify both the parameters and the gameplay queue tag. Apply the same process to the player as well. Once all the settings are complete, press play to test the result. You should see the damage scroll displayed collectively. That concludes this hand zone exercise. Let's summarize what we've learned in this lecture. Gameplay queues are designed to operate independently separated from the core game logic. A single gameplay queue asset is matched with a special gameplay tag that begins with gameplay queue. Gameplay queues include the static type that triggers as a one shot. And when activated, it calls the on execute function. In this function, you can create various effects, such as changing materials or spawning a damage scroll actor. Try using the provided step seven level to practice implementing different effects based on gameplay effects. That concludes today's lecture. I hope you're looking forward to the next session as well. This has been Tuku Rii. Thank you.