Now that we have some of our tool in place and working, it's time to finish off adding the basic framework for our blueprint. And although the variables that we will create here have been left until last, they are by no means less important to the working of our scatter tool. Indeed, what we will set up here will let us place and scatter our mesh actors in a random, but still controllable manner. Once again then, finding ourselves inside the Advanced scatter blueprint, let's click the plus icon and then call our newly created variable, spawn, point, and then press return. We can change its variable type to vector in the dropdown. And if needed, also make sure that its container type is set not to array, but rather to the single option. Now vector really contains three sets of numbers that correlate to the x, y and z or z axis 3D space. As this makes it perfect for storing 3D coordinates, it can be used to determine the exact point at which we will create our stored mesh actors. Again, let's create a new variable giving it the name number of meshes and then press return. After setting the variable to be public, we can set it to be an integer variable type. Setting set it to public means we have access to it in our level, so we can click the compile button for our blueprint. And this is so we have access to the default value, which we will set to around about 50 or so. Now we have done this so that when the user selects and adds static mesh actors into the tool, some will immediately spawn and become visible in the viewport by default. Now this hopefully, and finding a situation in which anything that the tool is actually broken, because well, if we don't change this value, nothing will spawn or appear in the level. Now this can happen if the value defaults to zero. And so setting it to 50 here will just help provide this potential problem. And just to elaborate on that, the main reason for this variable is to allow the user to set the number of static mesh actors that will be spawned inside the scattering radius. Let's create the final new variable for our framework then. And this time call it random seed in the variable type drop down we can search for the word random and set it to be a random stream variable. So how would we explain the random stream? Well this simply allows repeatable random numbers to be generated and then used inside of a blueprint. This makes it perfect for the times when we want to create almost random effects, but also have a consistent distribution each time the graph is evaluated. Now this will be our random seed option, which will still use all of the position, scale and number options that we have already created, but then give us a different and random distribution pattern. Giving our users the ability to control the number of meshes to be added to the scatter area, as well as making it possible for them to add large amounts of variation via a seed control should make this quite a flexible tool with which artists can work. Now we can use these new variables to finish off our static mesh selection portion of our blueprint.