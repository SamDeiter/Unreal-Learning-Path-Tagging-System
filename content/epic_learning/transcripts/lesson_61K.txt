Welcome to creating a simple water material. I'm Paul Neal for Epic Games. We'll be having a look at several nodes that will allow us to control the look and feel of the surface of a body of water. To create this, we'll need to explore how to make the water move and be able to rotate in the direction that we desire. Control the height of the waves and mimic how water is more transparent when it is looked at from above compared to a steep angle. Let's take a closer look to get started at the water we'll be creating. In this case, it's a waterfall. Now, there's several tricks that are being used here to make it look like the water is flowing over the edge of the rocks and speeding up as it falls, as well as the ability to have displacements on the water to show the ripples in the form happening. You can see that we have nice reflections, as well as what looks like a quite randomized rippling happening across the water surface. We have a look at the mesh that we're working with by opening the static mesh. To make the water appear as though it's speeding up as it casts cades over the edge, we're simply using a trick of UVs. We inspect the UV channel zero. You can see that the UVs are evenly spaced on the top of the UV map. This is the flat area across the top of the water. As the water cast cades over the edge, you can see that the UVs become crushed together as they come down to the bottom of the waterfall. This gives the illusion that the water is speeding up as it moves over the top of the falls. To create the waves in the water, we're also using displacements. If we press Alt 2 in the keyboard to toggle to the wireframe, you can see that the mesh has this odd circular patterns on it. This is because of the tessellation that's happening on the mesh to increase the resolution of the mesh so that we get nice clean waves. We'll be able to control that as well. Pressing Alt 4 will take you back to the lit viewport so you can see the final result. We'll make some changes to the base shader. In the Details panel, Material Domain is set to Surface, we can leave that as it is. The Blend Mode is set to opaque for solid objects. We'll need to change that to Translucents so that we can see through our water. We scroll down a bit further under the Translucency rollout. You can see the Lighting Mode. There are several different options here. For the volumetric non-directional, the mesh is lit as if we were used to create volumetric effects like Smoke. Only diffuse lighting will be displayed and the normal is not used. This is the cheapest form of lit Translucency. The volumetric directional option, the mesh is lit as if we were used to create volumetric effects like Smoke, but with the normal that affects the lighting. Only diffuse lighting would be displayed. The Surface option is lit and receives a reflection, but the reflection is limited and not as good as an opaque material. It simply picks the nearest reflection caption actor and applies it to the entire object. For our purposes, we're going to choose the Surface Translucency volume. This tends to give the best result I've found for minicking water. Next we'll need to change the Tessilation settings. We'll scroll down to it and see that it's set to no Tessilation. There's two different options, the Flat Tessilation, which testulates the model, but doesn't blend between the existing geometry and the new Tessilated Mesh. The PN triangles blends the new geometry in with the original edges, so you get a smoother result. We're going to choose the PN triangles for the water. To start building up the material, we'll need the two textures that we'll be working with. I'll drag in the Water H from the Texture's folder, which stands for Water Height, and the Water N for Water's Normal. We'll take the normal map and we'll take the output of that into the normal channel of our material. For the Height Map, we're actually going to use it to mix together two blue values so that we can have a little bit darker blue in the bottom of the waves, a little lighter blue on the top of the waves. For that, we're going to need a couple of constant vector three nodes. I'll hold down three on the keyboard and left click and double-click on it, and we'll set it to sort of a mid-tonish blue. Make a copy of this node by holding down Control W, and it will make a copy of the node underneath where the mouse is. The bottom one's going to be a little bit lighter blue, and then we're going to want to mix those together with a Lurp node. Lurp stands for Linear Interpolate. So, hold down L and left click, and take our little bit darker blue into the A channel and the light blue into B channel, and then we'll need a way to mix them together. We'll use the Height Map for that. We'll take the output of the Height Map into the Alpha. So, then anywhere it's black, we're going to get the little bit darker blue, anywhere it's white, we'll get the lighter blue, and then the output of the Lurp node into the base color. I'll turn on live previews so we can see the end result. You can see that we're getting sort of nice watery colors now being mixed together. We'll also control the specular channel of the material with the output of the Height Map. Next, we want to start animating our textures. We'll need two of each of our textures so that we can mix them together on slightly different angles to produce that random water effect. To do that, we'll select the Height texture and press Ctrl W to make a copy of it, and then the normal texture and same thing so that we have two of each. We'll start with the Height textures. We'll want to blend those together with the Lurp node. So, I'll hold down L and left click to create a Lurp, and we'll mix them together 50-50. So, I'll take the output of the RGB into the A and again with the second one into the B, and now that we have them mixed together, we want to replace the connections that we've made to the other nodes. So, I want to take the output of the Lurp into the alpha of the Lurp that is mixing together the colors, and then the output again into the specular channel. The normal maps need to be blended differently. Because they represent angles on the surface, you can't blend them together with a Lurp and end up with a correct result. We're going to use a node called Blend, Angle, Corrected Normals, and this will correctly mix the two together. Once again, we'll take the output of the first and second and input them into the blend angle corrected normals, and then the output back into the normal input for our material. Since we have two Height textures and two normal textures that are the same, it'd be great to be able to change these in the material instance in one place only. To do that, I'm going to right click and type in texture object parameter. Texture object parameters are direct reference to the texture map, and then we'll name this case, the water height. We'll take the output of that into the texture inputs for both of the texture samples. We'll then need to set it to be the correct texture. We can then do the same for the normal map as well, and of course name it and set the texture correctly, and then take the output of the texture object parameter into the text input of the texture samples. To keep everything organized, it would be best to put the nodes into comment boxes to define what those nodes are doing. In this case, I'll grab the ones that are handling our color mix, press C on the keyboard, and type in color, and then for the others that are handling the mixing of our textures, once again, C, and we'll type in textures. We'll do this for the rest of the nodes as we go, so that we come back to the material. We actually can understand it far quicker than trying to sort through what each section of nodes might do. We'll move on to animating the textures using a panor node. I'll right-click and type in panor. And the panor node allows this to be able to animate UV coordinates of a texture. So we'll take the output of the panor into UV coordinates of the first height map and the first normal map as it's going to control both. We'll need two panor nodes so that we control different speeds for the textures. I'll create a copy of it with control W, and once again take the output into the second height map and the second normal map. To control the speed, we're going to have one speed input and then an offset speed so that we can increase the overall speed of both textures, but be able to offset the values between them. For inputs, we're going to need constant vector ones, so we'll hold down one and left-click and create a constant. And I'll make a copy of this with control W, select both, and then we'll convert them to parameters so that we can name them. First one will be called speed, and the second will be called speed offset. Since the panor nodes control the UV coordinates, we'll need to create a UV value input for the speed of the panor so that we're only controlling the texture sliding along its v coordinate. To do this, we'll right-click and type in a pen vector, and then take the speed into the b-channel. The a-channel needs to be set at a value of 0, so we'll create another constant with one and a left-click, take the zero value into the a-channel. We'll need to do the same with the speed offset as well, so make a copy of the pen vector with control W, speed offset will go into the b-channel, and then we'll place the zero value into the a-channel again. The output of the first pen will then go into the speed of the first panor node. However, we want to add the two together to be able to control the second panor node, so I'll create an ad node holding down a and left-click, and we'll add the two appends together, and then take the output of the ad into the speed of the second panor. As defaults, we'll set the speed to a value of 0.5, and we'll set the offset speed to a value of 0.1. Once again, let's make sure that we create a comment for these nodes so that we can identify what they do quickly. Press C on the keyboard, and we'll type in speed for the name. We'll now need a way to be able to rotate the textures into the direction we want them to travel, as well we'll want to be able to offset the second texture, so that it passes over the first at a slightly different angle, giving us that more random feel to the waves. To view this, we're going to use the custom rotator node. I'll right-click, type in custom rotator, and then take the output of it into the coordinates of the first panor. Once again, we'll need two of these because we have two textures to control, so I'll make a copy of that with controlled w, and take the output into the coordinate again of the second panor, and we'll need two input constants again, so hold down one and left-click, and we'll create two constants. Right-click will name these as a parameter again, and the first will be rotation, and the second one will be rotation offset, and we'll do the same as we did for the speed with the panor nodes. We'll add these two together so that we can control the overall rotation at an offset rotation separately. The custom rotator node has an input called rotation angle. It's a value from 0 to 1, or what's referred to as a normalized value. Artists would prefer to work in a value that would go from 0 to 360 degrees for rotation. We'll need to convert our value from 0 to 1 value into a 360 value for the artist. To do this, we're going to divide our input values by 360 degrees so that we can type in 360 and end up only with a value of 1. We'll need a divide node for each, so hold down D and press the left mouse button. We'll take the input of the rotation into the A channel, and then we'll create another constant holding down one and left-clicking, and we'll set this constant to a value of 360, and take the output into the B channel. We can then create another divide for the second rotation, and do the same. Because our rotation offset will be added onto the original rotation that will be setting, we'll need an add node, and we'll add the two together. We can then take the output of the first divide into the rotation angle, and the output of the add into the second rotation angle for the second custom rotator node. We'll also need a text coordinate node to input the UVs into the custom rotators. Hold down U and left-clicking, create one. Take the output into the UVs for both the custom rotators. We'll then select all of the nodes, press C, we'll call this rotation. We should also have some way to tile the textures more or less to be able to increase or decrease the size of the waves. We'll do this after the rotations. We'll need to multiply the UV coordinates by some factor in the U and V channels separately. We'll need two inputs again with constant factor 1, so hold down one and left-click. We'll select them both right-click and convert to parameter, and then we'll name these tile U and tile V. We'll need to append these together to create the UV channels. We'll use the append vector again, and then we'll multiply the outputs from the custom rotators with a multiply node. Hold down M and left-click. Again, we'll need two, and we can multiply those values together and then take the output of the multiply into the coordinate again of the panners, and we'll do it for the offset value as well, and take the output of the multiply into the coordinate. We'll set the default tiling for both the U and V to a value of 1, and then we'll select them all and once again, create a comment for them, and we'll call this tiling. We'll need a way to control the opacity of our water. Water that's viewed directly down is more transparent than its viewed perpendicular. We need to be able to dial this in so that we get a more realistic looking surface. The way we do that is with the Frenel node, a right-click, type in Frenel, and we'll take the output of it into the opacity channel of our water material. You can see that the water surface becomes very translucent in the polygons that are directly facing us on our sphere. On the edges, they become more opaque. We'll need to be able to control this so that we could dial in exactly what we need for our given body of water. I'll add a constant vector 1 by holding down 1 and left-clicking and taking the output into the exponent in, right-click and convert to parameter, and call it opacity. We'll set the default value to 2. The lower the value goes, the more opaque the water becomes. We'll select these nodes, press C, and call this opacity. We'll also be adding displacements to our material so that the water surface isn't just looking like it's rippled, but it's actually being displaced and pushed up and down with the waves. To do this, we're going to use the output from the height textures to control the world displacement. Inputting the value directly into the world displacement channel, however, won't work. Since the world displacement needs to know which direction it's pushing all of the vertices. To do this, we're going to need vertex normal WS node. The WS stands for world space. This returns the direction that all of the vertices are pointing in the model. We'll multiply the output of the height texture with the vertex normal. Hold down M, left-click to create a multiply node, and take the outputs of both, then the output of the multiply into the world displacement. We may also want to control how much displacement is happening, and to do this, we'll simply multiply the height textures so that we can increase or decrease the value output from them. Once again, I'll create a multiply node with M and left-click, and place that in between the texture itself and the multiply that we just generated for the vertex normals. And once again, we'll need an input, constant factor 1, one on the keyboard and left-click, take it into the B channel, right-click, convert to parameter, and we'll call this displacement. We also need to set the Tessilation Multiplier input. This allows us to control how many new vertices are being generated in the mesh. For this, we just need a constant factor 1. We'll set it to a default value of 2. We'll convert it to parameter, and we'll call it Tessilation. We can then select all these nodes, add a comment, and call this displacement. For the specular, we're going to use a multiply, so M and left-click, and we're also going to use an add node, so A and left-click, we'll take the multiply into the A channel of the add, and the add into the specular, and the output of the height textures into the A channel of the multiply, we'll need two constants, so hold down 1 and left-click twice, and we can plug those into the B channels both the multiply and the add. Select them both, convert to parameter, and we'll call the first one, spec amount, and the second one will call spec offset. Select all those, and call that specular. With a simple water example saved, we can step back to our environment, and find the material we created, right-click on it, and say create material instance, we'll replace the underscore inst at the end of the name, and add an MI for material instance as a prefix. We'll drag and drop that onto our waterfall. Right away you can see our waterfall is going backwards, it's not translucent, the water is moving too fast, so we have some adjustments we need to make in the instance, we'll double-click and open the instance, we can then start adjusting some of the parameters. First thing is the water needs to run in the opposite direction, we'll start by rotating the flow in the correct direction, by setting it to 180 degrees, we'll also turn the speed down a little bit so it's not so fast, we'll increase the amount of opacity, add some displacements, and even work with the size of the waves, increase the specular amount, so it becomes a little shinier, we can even go about changing the colors of the waves if we like, adding more blues into them, or make other adjustments to make our water feel as though it fits within our environment. In completing this module, we've learned how to use several nodes that can control the look and feel of a flowing body of water, using the frontal node to control opacity, animating height normal textures using panor nodes, and controlling direction of flow with custom rotator nodes, we've been able to create the illusion of water.