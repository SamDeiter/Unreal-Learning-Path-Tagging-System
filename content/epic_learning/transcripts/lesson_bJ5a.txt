For the final topic, we'll see how to use the ViewTarget with Blend functionality to switch between our active player camera and a static camera. This will be somewhat similar to the classic Resident Evil style cameras, but it will give us a really good opportunity to look at some of what's going on behind the scenes in how the ViewTarget's are being managed. There are also other types of ViewTarget functionalities, but when you know how to use one, you'll very easily be able to switch between the different implementations. So to get started, back inside of the unreleditor, as always, we will be tackling the Blueprint implementation first, but just so that we don't have to implement the same components in a Blueprint and a C++ class. We'll get started by creating our C++ version. There's a given us a lot of the base work that we need and then we can switch back to calling the functions in Blueprint. What we'll be creating is a basic actor class, and I'll be giving this one the name of CF underscore ViewTarget actor. We're going to put this into its own folder, named Actors. This will cause an issue, but we'll fix this as we go through, and I'll just finish by creating the class. So when this is compiled, like I've mentioned, we're expecting to see an error coming up. It will tell us that we need to go into the code. So we're just going to jump over to our IDE and we'll fix up this redirector or linkage issue with the code base. So this is just one of those legacy issues. This has been in the engine for quite a number of versions. I'm just going to close the editor so that once I fix this, I'll do another rebuild and get this launching through the IDE again. But quite simply, all we need to do is in the code file, because we have this nested in a folder, we just need to remove the name of the folder structure. And if we compile this, we can see that it now knows that this class does in fact exist and where to locate it. And it's going to build this again, just so that we're running the project back through the IDE. With that done, we can jump over to our header file and just start adding in those components that I mentioned. Doing a quick class tidy here, just moving things around so that we have just the one public section. Inside of the protected section, I'll start by creating a UBox component named overlap box. I'll provide this a U property of visible anywhere and put it in the category of components. And of course, we'll forward declare this at the top to include the UBox component class. This is quite simply going to be the area that once we enter this, we will transition to another view target and look through a different camera. And when we leave, we'll go back to using our active camera. Then just below this, I'm going to add a few additional properties. So the first is going to be a Boolean, which I'll call B is dynamic. Set this to a default of false and this will just control whether or not we should have a camera, which is kind of going to be that very pseudo dynamic camera setup that I've mentioned. It will rapidly follow where the player is moving, nothing super fancy, but it'll give us an idea of a difference between a static active and dynamic camera. Then below this, I'll create an A actor named a view target and set this to be a null pointer. This will just be the thing that we want to look through. And we'll also want a float, which our name at blend time, set this to a default of one. And this will be an exposed variable to allow us to very quickly and easily update how long we want it to take when using the view target with blend function to switch between one camera and the other. All of these will have the same U property. So we'll give this a U property of edit anywhere. These values will be super useful for testing. So we're going to make sure that we have these in the editor will make these blueprint read right just in case we did want to update any of this in our blueprint class. We'll put this in a category of view target. The variables in we're also going to need two different functions that will be binding. So these are built in functions that will be binding to the box components. We want the on overlap begin and the on overlap end is really important that we put the U functions here. They don't need any meta properties, but to leverage the dynamic binding that will be using a bit later, we will need to include these. And I won't go through all of the properties and overrides. Just pause the video here if you need to and make sure that you have the same properties included. Of course, you can jump to the documentation or the location in the source code for the overlap end and the overlap begin functions. If you wanted to safely copy and paste the required parameters as these can change and be updated between engine versions. And it's going to be very, very important. Of course, that these match up with the functions that will be binding them to. With that done, a couple of final things that we need that we can nest inside of a private section, nothing we need access to these apart from the code will be running a little bit later. That's going to be access to the player controller. So the a player controller that I've just named PC and we need a Boolean just to check and keep track of whether or not the player is still being overlapped by the box components. We'll just call this one B is overlap and set this as a default to false. Jumping over to our code file, the first thing we want to do is to actually create our box component. This is one of the components that we will need in both Blueprint and C++. So we'll just do it here once and say that the overlap box is equal to the create default sub object of the new box component type and will name this one box. We will of course need to jump up to the top and include the components forward slash box component dot h. And whilst we're here, we'll also need to create the definitions for our on overlap begin and on overlap end. So I'll just be using Visual Studio's also complete option for this under the quick actions. Of course, as I've mentioned, hopefully you know your way around the IDE that you're using. So if you don't have these options, we just need to implement these manually. But once you have that done, that's pretty much everything that we need to do at this current time in our C++ side of things. So again, I'm just going to compile this and make sure that the project is being loaded through our IDE and then jump back on over to the editor. And we can finally look at getting the view target with blend setup and working inside of our Blueprint version. So the first thing is I'm just going to jump into the Blueprints folder, create a new folder named view target, then heading down to the Actors folder, which is now being created inside of the C++ camera frameworks directory. I right click on our new C++ class. I'll put this inside of the view target folder we've just created and rename this one to BP underscore view target actor. Then inside of the view target actor Blueprint class, I'm just going to do a few kind of housekeeping setups here. So for the box, I'm going to set the line thickness to three and untick hidden game under the rendering category. Just so that this will be much easier to find and run into in our level when we're testing. Then we'll start with doing our Blueprint versions of what we'll get to a little bit later. We want to bind the default on overlap begin and the on overlap end to some functionality in our Blueprint to the overlap box. So the first thing we want to do is when we begin overlapping, we want to find if we have a valid view target, we'll set this up in the editor in just a moment. If we do have a view target, so another actor, a thing to look through, then we're going to get our player controller. And from here, we can call the set view target with blend function. So with blend in this case, just means that it will kind of use a set blend function and it will essentially lurp over time to the new actor. So this gives us a nice kind of smooth effect. The view target will of course be the view target property we have exposed and we can set the blend time to also use our exposed customizable variable. Then for the overlap end, so when the player leaves the overlap box, what we want to do is we want to reset our view target back to the active camera. So we're going to, in this case, I'll just duplicate our set view target with blend function and the player controller. We need to call the same thing to revert this. We'll get the current player pawn. It doesn't matter. We don't need to know the specific class type. We just need to find what the player is currently controlling. And we'll set the view target with blend to look back through our character. This will default to using the camera inside of the character class. And we'll be back with controller over our active camera. And that will be what we're looking through again. And that's really all we need to do to get our view targets working. So we can also see there's a few other options we've got here that we won't be playing around with, but we can change things like the blend function, which will just change the type of interpolation that will be used. So we can make, we're currently going with the linear blend, which is the kind of constant flow from start to finish. We can have it speed up towards the end or curve faster towards beginning things like that are all available through our view target with blend function. With that, we'll set, we can jump back into our level and we can start implementing. We can bring in a version of our BP underscore view target actor into the world. I begin by just changing the dimensions of the box a little. So we're going to make this a little bit wider and a little bit longer. The dimensions here really don't matter. Just something that's big enough for us to walk into clearly and also a little bit of space to walk around for the camera to stick on us at whilst we're there. And if we select the class that we've just dropped in, we also want to update the view target. So we want to give this a camera to look through the way that we can do this is we use our drop down and search for camera for the new actor class. We'll drag in the default camera actor class. We'll make this look somewhere in the direction of our overlap box. Grab our overlap box again and we'll set this new camera to be the view target that we want to view through. And this should now immediately work. So if we press play, we can run around and we can go into our box and we can see that as soon as we walk into the box, the view target is swapping to the camera that we've just set up and as soon as we leave the box. It's being told to go back and possess the default character that we've set up. Now, some of this can feel a little bit weird. You may want to play around with the direction the camera is facing because of course the input isn't based on where we're looking. So this can feel a little bit counter-intuitive with how to control things. So do feel free to play around with that. But again, this isn't so much of the topic of these videos, but it's just something that's worth keeping in mind. That's really as complex as it is to set up a view target with blend change inside of blueprints. Like I've mentioned, you have other options like the standard set view target function. It does exactly the same thing essentially, but it just snaps directly to the new view target there's no lurping in between. I will return very quickly to the blueprint version and I'm just going to unhook the overlap functions so we know for certain that that will be the version which is taking effect. So with the blueprint topic covered, what we can do is start looking at getting this functionality implemented.