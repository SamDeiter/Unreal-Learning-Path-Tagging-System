We are going to be going over an example that hopefully brings through some of the concepts we've been covering in this course And that is to migrate the complete quest function of the quest manager over to C++ at least part of it So that's what we're gonna do. Let's go ahead and have a look at our quest manager and see what we're dealing with here So this is the complete quest and it is not too big It's not too bad But it's going to be much shorter and easier to read in C++ at least that's what I think So let's make a little bit of a plan how we're going to move this over in C++ well What can we tackle? What can't we tackle get quest index now? That is a call that has some complexity we could move it into C++ I'm going to show you how we can do it without moving this into C++ just have this as a blueprint Implementable event there if we go back to complete quest We also see that we have this call to the complete quest event and we don't know how to do events yet We are going to do that in a later module, but for right now We don't quite know how to handle this so what we're going to do is we're going to replace the bulk of this function with a call to a C++ parent class And then we're just going to drop out to this call complete quest at the end and you notice that it does take the index that comes out of this function So we probably will need to get hold of that index as well So first of all we're going to create a blueprint native event called complete quest and I'll show you how we can do that when we've already got an implementation so if we go to our quest folder the quest manager dot h We want to create a public function because this is a public function called complete quest So it's going to be a U function and we want to make it blueprint callable and We want to make it a blueprint native event so that we can call up to the implementation and C++ Now as much as possible we want to try and match the signature of this function So it's not pure if I drop down here. It's also not const It is public we put it in the public section so that's correct It has no outputs so that's going to be a void return type It is called complete quest and we can do that with camel case complete quest and then we have two arguments We have the quest ID and complete whole quest, which is a Boolean Neither of these are passed by reference you can see this by expanding them out There's no pass by reference so we're going to try and match that and not pass by reference So we're going to have an f name called quest ID and then we are going to have a complete whole quest I'm just going to copy that which is going to be a bull complete whole quest like so so once that's been written I'm going to go ahead and copy the signature in to the bottom of our implementation file again remembering that it has to be suffixed with underscore implementation when we are dealing with blueprint native events That's how it works so we can go ahead and compile that and see what's going to happen over in the blueprint Hopefully the compilation has given you know errors Let's go over to the blueprint and see what the errors say here So the errors are saying that the complete quest is already in use So we can't just go and swap out the base function for an existing function in blueprint So what we can do is we can copy the body of this function cut it out and Delete our current complete quest. Don't worry. We'll bring it back We go to override and then look for complete quest in here and click on that now notice something really funny It's happened. It's gone and added it as an event node in our event graph instead of a function over on the left here And the reason for that is that if you have a blueprint native event or blueprint implementable event that is not Conced or does not have a return value then it will be put in the event graph If it does have a return value or is constant it will show up as a function bit of a weird quirk However, if you do override something as a function and then you remove its Consonus or you remove its return value It will stay over written as a function So if you really want to have complete quest over in the functions Then you could make it const for example and then override it and so on But that's a little bit complicated and we're not going to do that. We're just going to deal with it right here So what I can do is I can right click on here and add a call to the parent function We're going to need that later But for right now we're going to paste in the function body that we are going to be migrating away from So that we can deal with that So let's have a go at writing this over in the C++ So the first thing we're doing is getting this quest index. Okay, that already presents a problem because get quest index is a Function that exists only in blueprint. So we're going to repeat the same process get quest index I believe is a private function. So let's go and add a private section just below our tick in the C++ header file And we're going to create a U function that is a blueprint Callable and it is going to be a blueprint Implementable event because we do not need to have a C++ Implementation here and then we have the input name is going to be the quest ID So what's the name of our function? It returns an index. So this is slightly problematic because the return value is called index And that's not going to match up with the return value from C++, which is going to be an int 32 So by default the way we can handle this is to just rename this return value of index to return value And that is a special name that will match up with the return value of a C++ So now otherwise we know that this is a pure function. It's not constant private So what we're going to do is we're going to try and match that as best we can we're going to make it a get quest index And it is taking not by reference an F name, which is called quest ID So that is trying to match it as best we can match the signature and we are going to have to Reimplement it again, but it's trying to keep the signature the same so that other places that are calling get quest index Will update and use the C++ version Now you'll notice that I'm trying to match it to be a private function But actually a private function cannot be a blueprint implementable event So we're going to have to make this protected which is going to change the signature slightly But I think isn't going to matter for any of the calling functions And we're going to have to re-implement the body anyway It's not just going to slide in and use the implementation that already exists there in blueprint So let's go back to blueprint C what errors we are getting here It is saying that get quest index is already in use So as I said, it's not going to just slide in under that implementation and be happy The X is specifiers also change cannot order return parameters something about that Well, let's do the same process here We're going to cut the body and delete our get quest index and override get quest index And notice that because it has a return value It shows up in the list of functions instead of being in the event graph And we don't need this call to the parent function because we actually don't have a parent function in this case And let's paste in the body and hook up the nodes as they were hooked up before So the quest id went here You could take a screenshot if you're unsure and don't want to lose any work And we don't need this return node because we're going to use these return nodes And they have come unhinged So we're going to get the array index there is that return value and minus one for when we don't find anything in the array So let's compile and save that And one thing that I forgot to do here is to make this blueprint pure So as well as blueprint, callable, blueprint, implementable If I change blueprint, callable to blueprint, pure That also implies that it is blueprint, callable So if we go ahead and compile that That should hopefully fix the last few errors that we have here about calling to get quest index So let's try and compile that And finally these last places where we're getting an error If we right click on them and do refresh node You can see it just has to update there We'll do it with the other error place So right click, refresh node and reconnect And finally now we can go back to our event graph And try and move this stuff over into c++ So the first thing is we're getting a quest index So let's go over to c++ and implement that So we want int32 quest index Which we're going to do by doing get quest index And we're passing in a quest id Then we are going to get from a quest list via the quest index So we're going to get particularly a quest So it's going to be f quest info Which is a quest, we're getting that from the quest list And we're doing that with the quest index Then we have an if statement that is conditional on the complete whole quest So if complete whole quest Then what we are going to do is we're going to go ahead And in one case we're going to update the member With the progress set to the total So that's if we complete whole quest So let's do that So that's going to be quest list quest index Dot progress is going to be equal to the quest dot progress total Then on the other hand we are incrementing progress Now this is a function but we can easily expand this out here Because it's not nearly so verbose in c++ So this is the else And in the else we are going to do the quest list progress again Is going to be equal to the quest dot progress Plus one But notice that here we want to make sure that we aren't making it greater than the progress total So we want to do a min operation And a min operation is a mathematical operation We hover over it says it's using the kismet math library And we often know that we can use f math instead of the kismet math library So we can do if f math min like so And we're going to do the minimum of the progress plus one And the quest dot progress total So that we can't overrun the progress total by incrementing progress here So if we go back to the event graph Is there anything left to implement? No not really that is pretty much all of that We still have a call complete quest to call So let's hook up our call to parent first of all So doing the call to parents Hooking up variable inputs We're then hooking up the execution node to call complete quest And then what index should we be using Well we can use get quest index to find out Hooking up the return value to call complete quest And we've moved part or the most important part of that function Over in to c++ Now we can just go ahead and play and check whether things are working out If for you they aren't working out Or for whatever reason You are getting a blueprint build error You may have to go ahead and launch up the blueprints that are using the functions we have changed So for example you can right click on the complete quest And do find references And then down at the bottom If you click on the little goggles You can find in all blueprints And you can see which classes are using this For example cutable tree is one if I click on there We open that blueprint and if you hit compile Sometimes that fixes any errors that might have been occurring there And would have prevented this from running So I'm going to go ahead and do that as a precaution And I will just speed up through that And once that is done You should be able to go ahead and play And see that all the quest markers are working as they should If I go through them They update the display and they make themselves disappear So we've successfully migrated that function over into c++