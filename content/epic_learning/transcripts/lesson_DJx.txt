Hi, welcome back. Not all we've gone over the how the draw calls work and how the geometry rendering is done. We're going to take it close to look at what it actually means for you and your workflow and performance, how to organize, you know, building up a scene and the environment and so on. Right. So the cost to rendering many polymers is as mentioned, often lower than the draw call expense. So draw calls bigger cost than polymers. 50,000 triangles can run worse than five, 50 million, dependent on how exactly you've implemented and set it up. Draw calls have a basic expense. So optimizing very low poly, super low poly models may make zero difference. That's an interesting one. And before we go even deeper into that, let's actually take a look at this. So in and real, let's visualize the whole draw call thing entirely on the scene that we got right here. Since for I think for 16 is on the star, there is a plugin build in and that is called render doc. So if you enable that plugin, you're going to have to restart the editor. You'll get a little button on top right of the viewport that will capture the current image, the current frame to render doc. Now, I've already pressed that. That will take usually it takes about five to 10 seconds. And then it will open up render. Here's render doc as a program. It's a free program. You can download. And this is the frame we had in the editor output. In fact, it's the first view you get. You see this, the editor view, everything there. If you double click that, it will open it up in these different tabs. And if you go to, for example, pipeline states, we'll get back to this later on. We see the steps it goes through. This is all we're currently doing. We're currently still here in the beginning. And also you can do a texture view. Now, texture view is showing you the G buffer, which we'll get back to later on. Right now, we're just going to use this to visualize the draw calls. And on the left side, this one here, that is the list of everything it does. If you expand scene, this is the rendering in order. And you can see it's roughly comes across this to the pre-pass. It does the first thing. And we're currently at the base pass. The base pass is a big one. That's just the rendering of geometry. You can see the steps it takes. Right. This is what we're going through. We're not going to go over every single one of them. For example, HZB is hierarchical Z-base occlusion. It's a different way of doing occlusions related to that. So all of this is kind of tied in. It's doing some preparation here and there for shadow depths and all that. Light grid is related to it and so on. But still, most of this hasn't done that much base pass. It's a really big one. Now, if we expand base pass, we see we're doing these in three steps. We're doing stat two types of static. And we have a dynamic one. It's all kind of the same thing. We just organized things three groups. I think the second one is where most of the models are. And if you expand the second one, you can see here. These are the models in the environment. Everything that we can see in that particular frame. And they're sorted by name. It looks like they're sorted by name. They're actually sorted by material, as mentioned before. But because most things with the same material tend to have this, you know, the same name is just an arc and a copy of an arch and a copy of that again and so on. It looks as if it's grouped by name. Now, if you go through each one of these is the draw call. What we're going to do now is set the G buffer here to. I think that one's without lighting and I'm going to highlight the draw call. So overlay via frame. Imagine you can see this pillar is right there. And here's the image being built up live in front of you. Draw call per draw call. You can see a render's object per object in the builds of the scene by taking object per object into account. Each of these is a draw call. So this is all the work it's doing to do a single frame. You can see why draw calls are slow. If you have 10,000 draw calls and you got 60 frames per second, that is 60, that is 60 times 10,000 objects. Okay, that's 600,000 different draw calls per second that is processing. You can see that's not that particularly fast. So this is how it's building up and you can even see the time. There's a small button here that says time duration. You now see the times in microseconds. You can see how long it takes to complete each one of these. There is some variation in that's why these walls which are essentially the same model are not exactly the same number every time. In fact, every time you recapture this, you'll get slightly different numbers. Okay, so it's not the exact science. Depends what a computer in the hardware is doing at that time. But you still get a pretty good idea because the heaviest thing we have in here, is the statue in the middle. Let's try to find the statue. It's probably at the very end. You see at the very end, we get the statue. In fact, we can look at the statue. Here is the statue. It's fairly high poly. I forgot the poly count. Let's just say it's 20,000. I'll just put a number on it. And then we can look for something that's extremely low poly. And I'm not sure what maybe this one that's basically as you can see there. That's basically just a box. That box took five microseconds. The box is, say, 12 triangles or something like that. The statue is, say, 20,000 and only took 28. So you can see that number is not in relation to the poly count. Not entirely. In fact, you will not find a single number in here that is lower than 4.096, which is the lowest I've ever found. That is because every draw call has a basic expense. So at some point, it doesn't matter anymore if you make something a thousand polygons or 2,000 polygons because the expense is almost exactly the same render wise anyway. Exactly where the limit is. I'm actually not entirely certain about it. Depends on how hard it is. Well, it depends on everything else you're doing. But be aware that at some point probably around a thousand, two, three, four thousand triangles, it won't make that much of a difference anymore. If you make it even lower poly at least not for this. It will fall apart the dream. It will for, for example, dynamic shadows, but not for this part of the rendering. Next up is understand that components are also draw calls. So if you take make a blueprints and you add a lot of different static measures into that blueprint, each of those static measures within the blueprint is still a draw call. Does not make a difference. In fact, they also include one by one and render one by one. Absolutely no difference. Whether you put something together in a blueprint or you have a separate object in the world, zero difference. So merging single act usually makes a difference. Next up to lower the draw calls, it's usually better to use fewer, to use fewer, it's usually better to use fewer larger models than many small ones. Right. But if you do that too much, you get all kinds of other problems. It's a worst of occlusion. It's worse for light mapping and it's worse for collisions and it's worse for memory. Let's take a look at that in the editor. Because you have two main things you can do. You can either do everything as a modular measures, which means modular as in it's basically Lego. This is how we commonly build a lot of things. We build these individual pieces, modular pieces. They're aligned to the grid. And if you double the catom, we can easily like Lego, continue building walls. So we mix isolated objects, import them and then start copypacing them around. This is a model that a modular workflow. So on the other hand, you could of course, as we've shown before, just model the entire thing and import it as is. This however is worse for occlusion. As mentioned before, I will actually, in fact, occlusion is faster in itself, but it won't be able to do a good enough job at it. So it has fewer objects to check occlusion for, but it has a lower chance of actually occluding anything. Therefore, your polycount might actually go up. Now, we'll just say polycount doesn't matter too much, but it does matter for something to later on. So this could still have an impact. It's worse for light mapping. We'll get back to light mapping several videos in, but light mapping has a limited amount of space. You only have the texture, the light map texture. That is your maximum amount of space, whatever resolution it has, it has an upper resolution limit as well. A 4K, for example, 4,096. It's already pretty huge for a light map. If you make very, very large models, eventually they simply run out of UV space. There is a problem. Collision is another thing. Collision is something not many people consider, but everything in the world has collision. In fact, we can use a simpler one like this. It's just boxes. Preferably, we have boxes. The collision in the world is often different than what you see. So we're having a separate model that is collision, but invisible. And the things that we see, those themselves, those polygons don't actually have collision most of the time. You see that here too. See, in the collision view, the environment is very, very basic. Now, if a ball would bounce through the environment, so it's bouncing on the floor, hits the floor, and again, and again. And eventually it hits this pillar right here. The moment it hits the pillar, it has to query the collision information from the pillar in order to determine where it hits and what to do next. So it receives, retrieves this box, very low poly model from the pillar, and that's what it uses. But if this would happen over here, and this same ball would hit this pillar, then it gets this. It suddenly gets all the collision data just because it hit the wall. There's an impact there too. An impact is complicated. The impact is not extremely big unless you overdo it, but it's still an impact. And a memory by the way as well. Again, this does not take much memory. In fact, this takes 410 kilobytes of memory. The wall is probably a bit less I'm guessing. This takes 377 kilobytes of memory, but that giant model would take 7.5 megabytes. So by making large unique models, you will get more memory. It's going to take more memory because you have to remember all those large and unique models. Compared to the small models, you only have to load this one. It only remembers one wall, no matter how many times you copy paste it. So the memory consumption is going to be higher. That in turn will impact streaming where it has to bring data into the game, into the experience in real time, and so on. So again, bottom line here is, yes, it is technically better to use fewer, larger models, but you have these other problems. As I mentioned, a common technique is modular measures. This saves you work time, a work time, and memory. Lower memory has been just explained. It's easy to place, it's easy to tweak. It gets better lighting because the light maps can be higher resolution piece by piece. It's a better occlusion, collision, and loading, but you have a higher draw count. So the truth is usually in the middle again. So we have these two things, big models or a lot of small ones. The truth is usually in the middle because both have their own problems. So what we tend to do is we tend to build everything as separate pieces because it's just easier. We think it saves time. It's easy to work with, it's easy to tweak. It's easy to just be in the environment and say, hmm, what would it be like if I just move this wall and just immediately move it without having to import an export? But then once we've done that in order to optimize this, we would merge it. So you can always easily merge things in real. Let's select the things you want to combine, right click, and then you can do merge actors right there. It says do you want to do these things? Yes, let's just do that. Yeah, just put it there. And it's going to replace this. It would replace these four with a single model. So here's a single model it made, which is essentially the same thing that had selected but as a single model. Now it does have three materials. And it reminds me, make sure this is preferably this should be just a single material. But you can then place this one instead and therefore lower the different number of models in the environment. That's what we usually do build up separately. But at the end of production, at the end of when we're done with the environment, we start combining things. Not everything. Just once that are in the areas where it's heavier, heaviest, where we really need to do it the best candidates. Once merged, you cannot easily go back by the way. So be careful. Now which one should you merge? We have this kind of rules set. The more common and mesh and the lower poly, the better. Can use the statistics window to figure it out. The window statistics. And you can see how many times each mesh is used. So what I typically do is I go through and I see, okay, so we have 164 trim walls and they are 268 polyhands each only. So all of them together are only 43,000, 44,000 polyhands. So this is a pretty good candidate. This isn't even better one. Trim wall without you. We've got 45 of them, but there are only 125 triangles. If you would combine all of them, it's only 6000 triangles. So you try to find the ones that are used a lot, but they are low polyhands. And if you combine them, you don't get a super high poly model. You still get a pretty reasonable polygon count as with all of them grouped together. Next up, they should only merge things that are within the same area. So if you have six rooms, don't merge something that's in room one with something that's in room six. It does not help occlusion. For the reason of occlusion and collision, it's worse. So it should be within the same room or area. Only merge things that share the same material, because otherwise you're not really gaining much. Because every material is a draw call, so you're still having the same number of draw calls. Mashes with no or simple collision are better for merging for collision reasons as shown. Very small meshes or meshes receiving only dynamic lighting are better candidates. Because if they're so small, we don't have to care about the light maps. If they only receive dynamic lighting and they probably don't have collision or simplified or you know, it's easier to work with. So smaller meshes tend to be easy or distant geometry is also a good candidate and then you can merge a lot. We don't have a good example here, but just imagine that somewhere on top of this hill, for example, there was a small village built with a couple of houses. Well, those things you can probably combine very, very easily. Because the occlusion doesn't really work well in the distance because it's usually always in view or it's so far away that even though it's many objects, it's a large object. The large objects will appear small. Therefore, it doesn't really matter too much for collision. So distant objects are very good candidates for just grouping them together. You can also merge things by exporting them, by the way, I did not show that, but I'm not going to show the whole workflow, but you can always just to this select things, file, export selected and just export it as FBX, bring it back into 3D Studio Max, Blender, Maya, Moto, whatever you're using and continue on it there. For example, combine the models there or do further changes to it. And how much to merge and exactly how is greatly dependent on a lot of factors and often there's no need to merge anything. Okay, so keep that in mind. Even though the merging would make it more efficient, you get up to a point where the amount of time it would take you to do that doesn't balance out to the performance gain you might get. For example, if your target frame rate is 30 frames per second and currently the environment runs at 25 frames per second, it's still above the target frame rate. Now, you could go through and make it everything a lot more efficient. You could spend another week on doing that and you might end up with 45 frames per second, but it doesn't really matter because you still above your target frame rate. And in fact, your week might have been better, spend somewhere else in the project to doing some other type of work or other optimizations elsewhere. So balance it out. Do you really need to make it more efficient? Yes, you can make things more efficient, but perhaps you don't need it. In this level, we never did that because it ran good enough. So we just kept it separate. It is not a must to do this merging in every environment only when you hit problems. On very low and hard work, so you probably end up merging almost everything. If you have to do VR, especially VR on mobile, right now that's still very hard to do. You'll probably end up merging almost everything. So with everything, a balance between all extremes is usually the best way forward, some modeler content, some merge content and some unique models. Now you can also do one more thing because now you might ask, okay, that's all, so nice. But why are you even rendering this pillar and this pillar separately? Why is this a draw call? That a second and that a turn. Why not just figure out that this pillar is the same as that one and the same as that one and render all of the pillars at the same time. Then you only have one draw call. That exists. That's called the instant static mesh rendering. So by default, in Unreal, meshes are instant in memory, but not instant in rendering. Difference there. The instant in memory means that if a model is imported, for example here, it exists only once in memory. It's 422 kilobytes in memory. I place it in the level twice. It's still only 422 kilobytes in memory, but it's rendered twice. Instance in memory, not instant in rendering. But if you do instance rendering, it's instant in bold places. Instance rendering has been done for these pillars here. Good set it up for these where basically the pillars will be added together in an instant static mesh component and rendered as a group. There's simply rendered as if there are four objects, a single model of four objects big. But in fact, they're still using only one model. If you would look it up in the content browser, we'd end up looking like one model. The user interface of that is a little bit hard to use. So it's not easy to do it on a daily basis for everything. And there's a reason also why you can't do this for everything. And that is also the reason why this is not enabled by default. Why we're not just doing that behavior on everything automatically. Look if we have to know if this thing is the same as that, then we have to compare. Before we render, we would first ask the renderer the question, but hold on, wait a minute, is this thing maybe the same as that? That means that for every object we render, let's say we're rendering the pillar, we'd have to check. Actually, is this one the same? No. Is this one the same? No. Is this one the same? Yes. Cool. Okay. So that's the same as these two. And you can see how quickly you get a huge list of comparisons. So that is heavier. That's going to take more performance actually running all those comparisons that to simply render it as is. That is why we're not doing it by default. And that is why you have to use an InstaStaticMesh component because then you tell the engine is shortlist. You basically tell us, yes, please do run these expensive comparisons, but don't run it on everything because it's too heavy. Here's a list for you. These are the things you need to consider because these are probably all the same anyway. So you give them a short list and that makes it go faster. That's what we use for foliage and vegetation. So if you look at foliage outside, this is rendering because it's InstaStaticMeshRending. That's the only reason it's fast. If you're InstaStaticMeshRending, you can have a huge number of objects rendering at almost no performance loss. But you can't use it for isolated cases. So these four pillars here would actually be a bad example because it's too little use. You need a few dozen or a few hundred meshes combined for it to have a proper effect. So vegetation, lots and lots of papers on the ground, anything like that. But not for just a few things left or right. Finally, lastly, there is also a lot of level of detail, LOD. I can show you directly in the editor. The statue has it over here. Now, you're not going to see it, but if you move back, it actually is a different model right now than it is over here. Level of detail means we simply replace one model for another model based on distance. You can see up close is this polygon count. If you move away, it simplifies. See, it is literally a different model altogether. It replaces it. But because they both look the same, you don't see the switch happening. So that's very straightforward. Game engines have had that for a long time. Unreal has that as well. There's a couple of rules there. Make sure that every LOD is roughly 50% of the polygon count, the mod before it or more. The difference between the steps has to be big enough to offset the extra memory you take up with having the LOD and so on. But it generally is pretty straightforward. But we have another one. We have something called HLOD. Hierarchical LOD. And Hierarchical LOD goes further because the problem with the normal LOD is, which is, so it's nice to work with a normal LOD. But it only reduced the polygon count. It's still one draw call. In fact, these things here are four draw calls. And whether I look at it from here or I'm over here, it's still four draw calls. So what HLOD does is it groups things together and replaces groups of objects with a single model from a distance. So up close, you would still have your four individual models. But in a distance, these four separate models are replaced by a single combined lower poly model. So the draw calls and the poly count is reduced. That you can set up using the Hierarchical LOD Outliner and the other tools available. It would generate clusters and then within those clusters, it would automatically create meshes to replace models within the cluster. That's what I, you should definitely use that for outer environments. Large open environments need LOD in. A distance calling and you need probably HLOD. And that brings us to the end of this section. And we'll continue in a bit in the next video with verdict shaders and related subjects.