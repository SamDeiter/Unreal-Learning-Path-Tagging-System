[Music] welcome back to unreal Tips &amp; Tricks we'll be wrapping up our two-part look into creating high quality glass effects by taking a look at the spiral blur material function before we look at adding a blurred or frosted effect let's create a material instance that we can use to tweak various properties of the glass material right-click the tinted glass material we created in their last episode and choose create material instance name the new instance m i underscore glass with the table selected drag the new material instance onto element 1 in a details panel to assign it now we can open up the material instance to play with the values we exposed in the last Tips &amp; Tricks episode the first parameter is edge tint color enable it using the check box and then feel free to tweak the color using the color wheel and sliders you can also use the text entry fields below to enter RGB a or HSV values hit OK when you're happy next up we have the top tint color parameter this lets us do the same thing to the top and bottom parts of the glass again feel free to play with the color values until you get something interesting below these parameters are the scalar parameters including edge opacity roughness and top opacity playing with the roughness value will adjust how sharp or diffuse the glasses specular reflection is using top opacity allows us to make the glass more or less opaque and of course edge opacity works the same way for tinted edges all right now that we've spent some time tweaking the glass parameters we're ready to add some frosting using the spiral blur material function reopen the parent material M underscore glass we'll be getting rid of the scene color node in order to make use of spiral blurring on the top and bottom of the glass we'll be multiplying the top tint color parameter by a spiral blur function instead right clicking in the material graph brings up our search box in here search for spiral blur scene texture and create the material function now we can delete the scene color node and connect the result output of our spiral blur node up to the B input of the multiply we also need to take the scene color clamp to zero out and plug that into the opacity input of our material result node there are a lot of inputs on the spiral blur function but we only really need to worry about the top input distance to get a good-looking frosted effect create a new scalar node by pressing 1 while left clicking and then wire it up to distance now right-click on the scalar and turn it into a parameter will call this parameter spiral blur distance that's all there is to it we can save and compile our material now and then reopen the glass material instance so we can tweak the blur effect I like to unmask Imai Zammit irreal instance window and then press f11 to get a better look at the table in the viewport now we can enable the spiral blur distance parameter it's weak the value you'll notice that you can still tweak the color and opacity parameters as well thanks for watching this two-part episode on creating high quality tinted and frosted glass we hope to see you next time for another unreal Tips &amp; Tricks