Welcome to the second coming to the rescue challenge, a module where we will build upon what we learned in this course combined with previous courses to solve a problem. The previous modules have prepared us to solve the problem on our own to help our friend get their treasure. With a similar goal to the first challenge, this one however needs to be solved by using both methods of interaction we've covered, gaze tracking and hand tracking. We're going to expand upon what we built in the previous module from the touchable volume, the pressable buttons, the transfer of knowledge from AI to AI and the teleporting of our friend. Let's get started in BP motion sensor. Select UXT touchable volume and in the details panel, there are many events we can access that are called from this component. On event begin and end focus, we'll call once when the user focuses on the volume and once when the user focuses away from it. Event update focus will call every frame that the volume is in focus. This is similar with begin, update and end poke. With the motion sensor, we want to slowly animate out the ramp that our friend will use to get to the teleporter so we want to update this event every frame. If the calls from these updates stop coming, the ramp has been built to detect that change and quickly animate shut. We'll see how this has been set up shortly. Create a new event for on update poke. Again, this will be called every frame that the hand is within the volume of the mesh. Create a new variable named interactwith and set it to type actor. We are continuously setting these variables to actor for two main reasons. We can set this value to anything that has been placed in the scene and we can call an event from our blueprint interface without casting to the value of interactwith. This allows us to create our own modular tools. Be sure to set this to instance editable so we can select which actor in the scene to interact with. Inside of the map set the value to BP ramp. With BP ramp as the value, we can call the function interactwith object which has been defined in our BPI HoloLens. But first let's check if the actor is implementing the interface. From interactwith search for does implement interface and set the interface to our BPI HoloLens. Connect to a branch and from true call interactwith object and plug interactwith into the target. There is a boolean put named right hand but in this case it is irrelevant. So if that was needed information, you can retrieve the near cursor component within the UXT hand interaction components that we spawned in our BP pawn. We used a very similar technique to retrieve the far cursor with both hands but this motion sensor will work regardless of the hand that enters it. Open BP ramp. The first thing we need to do is implement our interface BPI HoloLens. Alright, right click and create an event interactwith object. Call MoveRamp and compile. Let's quickly walk through what this blueprint does. When MoveRamp is called the world location of the scene component named mover is compared to the world location of the actor. If it is not about equal, move it slowly in towards the actor location. This retriggerable delay is being reset every point one seconds. If the user's hands leave the volume, a timer will be set that calls close ramp once every point zero two seconds and loops until it is back to its original offset. Let's test this in the editor here. I'll quickly fly over and when my hand enters the volume, we can see that the ramp is slowly coming out. Because the nav mesh is being dynamically built, it will update with each of these moves. If I exit the volume, the ramp closes fast. So we're going to need to stay over here if we want our friend to be able to go up this ramp. Let's move on to the teleporter. We're going to need to add two new functions to a BPI HoloLens. The first one, name it teleport character. Name the first input teleporter ID and set it to type integer. And one more input named incoming AI and set it to type character. Create another new function named toggle gate. It needs only one bull variable named active. Okay, compile and close. Open BP teleport gate. This is a child instance of BP master teleporter. So let's also open that. Like the ramp, we need to set this blueprint to implement our BPI HoloLens interface. Okay, let's quickly walk through how this blueprint works. When an AI character overlaps with the box collision and the gate is active, get all the actors implementing the interface and call teleport character. Add the ID of the teleporter it wants to send to and a reference of the incoming AI. Pant down a bit and let's add our interface function to finish this line. Add event teleport character. Plug teleporter ID into the compare node and incoming AI into the object of the cast node. So if the compare is true, cast to AI friend. Then turn off the collision of the box and set world location of the AI to the world location of the arrow component named out. Tell the AI to search again, then set the collision back to query after two seconds. Compile and save. Return to BP teleport gate. Now we're going to use that toggle gate function we added to the interface. So search for event toggle gate. From active set active and call the function toggle active. Open the toggle active function. When called, it checks if m gate is valid. M gate is a material instance which allows us to change a parameter that was set in the material itself. In this case, we want to change the color from blue as in active to red as in in active. Then if active set the nav modifier to default, which will allow the AI to include the space taken up by this mesh inside of its path. If the gate is inactive, remove the path possibilities. Return to the map to see this in action. If navigation is not showing, press P to turn it on. See how it changes depending on the value of active. All right, we only have one quick blueprint to finish and we can try this in the HoloLens. Let's set up the button. Open player button BP. We have already set much of it up in the previous module. Now we only need to set up the events. Just like UXT touchable volume, UXT pressable button has event bindings for focus, poked and pressed. Here we will only use on button pressed and on button released. Both of these are only going to call one time when the status changes. So from on button pressed, call get all actors with interface. Because there are now two actors in the scene that need to be updated when the button is pressed. Create a 4-Each loop and from array element, call toggle gate, setting active to true. Repeat this line for on button released. But set active to false of course. So if we let go of the button, the gate turns off. That was a lot of setup. Let's finally try this inside the HoloLens. You can see our friend is already out here looking for the treasure. I'll place my hand in the volume and the ramp is coming out. You can see when I remove it, the ramp closes really fast. So I'll put it back in and at the same time I need to press this button to get the gates activated. You can see that they've changed color. So I know that they're active. Everything should be ready. We need to tell our guest that we are indeed ready and they can transfer the information. Using gaze and dwell, I'll stare at them for a bit to give a not so subtle hint that we are ready. And there they go. And they're talking and are AI celebrated. So now they're on the run. They can successfully move up the ramp and they're teleported over here. All right. And they got their treasure. In this course, we did a lot. We prepared our pawn to spawn hand interactions for both hands. We set up a system to track and render our hands and use tools to interact with holograms around our scene. We prepared Unreal Engine to handle new input calls using gestures and created bounds around manipulatable actors. We used modular components and added them to various blueprints to allow us to alter the scene. As I said, we got a lot done. But there's still lots to do. And in the next course, we will complement our scenes with UI and interactable menus using Unreal Motion Graphics. See you there.