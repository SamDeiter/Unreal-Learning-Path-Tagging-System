Moving on to data asset classes, we're going to need a couple of extra classes set up before we can create our first actual data asset class. There are a couple of ways that we can create the required assets using either Blueprint or C++ and again I'll be detailing both approaches. As we're hopefully already in the editor, we'll start with the Blueprint-based approach. If we take a quick look at what we need to create a data asset class, this will give us a small insight into what we're currently missing from our project. We could do this by simply right-clicking anywhere in the content browser, doesn't really matter as we'll be removing this very soon. Then navigate to the miscellaneous section and select the data asset option. From the list of options, I'll simply select the first one as we can see that none of our custom structs or assets that we've already created appear here. Then if we open our data asset, we can see a small struct of information in the Details panel, in this case relating to the AR Candidate image. I'll click on the AR Candidate image parent class in the top right to quickly open this in the ID and we can take a quick look at what we're going to be working with. By navigating to the class declaration details, we can see that the class is actually a child of the U Data Asset type and the Candidate image information is all being set below. We want to do something similar to this in our own setup. Back in the editor, I'm going to delete this data asset that we've just created as I've mentioned, we won't be using this. For the Blueprint approach, we're going to want to create something to represent that U Data Asset. We want to right-click inside of the data folder inside of our Blueprint hierarchy, select to create a new Blueprint and search for Data Asset. Click on the primary Data Asset option, press the Select button and name the new class to something like BP underscore item data assets. If you've worked with data, only Blueprints in the past, this is going to look fairly familiar, is lacking some of the default areas such as the construction script or functions to call like the event tick in the Event Graph. We only really need to do one thing here and that is inside of this class we're going to create a new variable named ItemInfo. I'm going to set this to be the generic item info, a struct type that we've declared in our Data Library. Keep the class open, compile and save this and return to the data folder. We'll back here in just a moment. Once again, we're going to right-click inside of the data folder, navigate back to miscellaneous and now look for the data asset. You can see from the options here we have the BP underscore item data asset to base this on in the list. This time select that and click the Select button, name the something related to the type so following the convention of item info table, our name is one item info asset. If we open our new data asset we can see that we now have our item info struct with all of the information that we've been familiar with working with in the past. The one limiting factor that you might notice here is that we only have the option of storing information about a single item per data asset in its current form. By default, data assets are essentially just representing a single row that would otherwise be found in something like a data table, making them much more lightweight. We'll look more at these comparisons a little bit later in the course. We can easily address this limitation by going back to the BP underscore item data asset, selecting the item info and changing this from a single variable or a single value to an array. Back in the data asset we now have an array of items that we can create by pressing this small plus button and a quick tip if you're not aware is that we can hold shift and left click on the small drop down arrow to drop or hide an entire array or collection in Mongo. Just compared to otherwise clicking individually like so to hide things. So we're not going to spend too much time filling these details out at the moment, but that's an insight to creating the data foundation using only blueprints, setting the details from here would be the same as our approach in the data table. So we don't really need to do that at the moment. So that's the blueprint approach. We're going to quickly look at the C++ alternative if you wanted a fully C++ hierarchy based project. So we already know that the thing we're looking for is the you data asset to be the base of where the information is going to be held. So navigate to the data folder under the project name where the other C++ classes are held. We're going to right click in here select to create a new C++ class. This time we can search for and select the data asset type. Click next, name this one something like item data asset and then click to create the class. Again, when this is compiled, we'll be created with our persistent compilation error warning. Click now and we can head over to the IDE and fix this. Inside of the item data asset code file, we just need to remove the folder name and the forward slash as before compile this if you wanted to get rid of those errors. Otherwise, we can just head straight over to our header file and implement the variable that we need here. So below the generated body, let's define a public section and we'll create our new f generic item info variable. We already know from our blueprint experiment that a default variable here isn't going to be ideal. So we'll turn this into a type to ray and name this one item info array. That way we're going to have our drop down option. We can add multiple elements to this for the different items that we want to store. Then I'll add a fairly standard but flexible you property here. And I'll place this into the same category as I've been using in the past, which is item info. With that new variable declared, you may have noticed that because I'm using rider, it did pop up and say that a new library has been added when I added the f generic item info. It's been nested under here automatically. So if I drop this down, you'll just need to make sure that you also include the data library.h. If you haven't already done that, it's automated. Like I said, because I'm using rider for Unreal. So just go ahead and make sure you include that. With that or done though, we can go ahead again and we can compile and go back on over to the editor when that's done. When that has finished and we're back in the editor, you create our final data asset class. I'm just going to delete the item info asset and we'll recreate a new one from the miscellaneous category. And this time we're going to base it on the item data asset C++ class. Again, I'm just going to rename this to the same thing. So I'm still going to call it item info asset. And if we open this again, we can see that this looks exactly the same as our blueprint approach after we've taken that step of turning the variable into an array. The only difference is that this is now inheriting from the item data asset class as we can see in the top right hand side. And then there's going to be one final step that we need to take for our logic to browse through our data asset and get the information. And this is specific to the data asset type. So let's jump back into our C++ data actor asset header one more time. Just below that we'll be creating a new U data asset pointer. We'll name this one item data assets. And then for the U property, we'll just make this edit anywhere. Blueprint read only. And I'll put it back in a category of the item info. That's pretty much it. So we can save, compile that. And we'll then be ready for the next topics where we begin looking at using this information. So for the next video, we just need to head back on over to the editor. So you might see what's happening here. This is very similar to the variable that we've exposed in our data actor table class so that we can use this in both our blueprint and our C++ logic to pick which data asset we're going to be looking through. So again, just some of that extra flexibility. If you wanted to make a few different type of data assets and test between them.