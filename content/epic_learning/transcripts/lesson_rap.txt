In this module, I'll be finalizing the parameters in my master material, adding a few extras to give myself more artistic control in each of my material instances. So there's still a couple parameters that I'd like to put inside of our master material. The first one is going to be a roughness parameter that's similar to the parameter that we created earlier with the Lurp node in our roughness channel. So I'm just going to hold the L and create a Lurp node. And what we want is we want to take our roughness and have a couple of parameters where we can tweak the values on it using min and max values in our A and B. So I'm just going to plug this directly into my roughness channel here and I'm going to take my roughness switch value right here. I'm just going to plug that directly into my alpha. So as soon as I do that, you get this issue right here where it's trying to convert a float three, a constant vector three, which is basically all three channels, your RGB, trying to convert that to a single channel in your alpha of your load. Remember the way that we fix this, we take our RGB and we take a single channel. What you could also do is you could create what's called a component mask. So if I right click here, I say component mask and what this does is it filters the channels. So if I just plug this in here and then plug this directly to false, what this does, if I switch that back to RGB is if I put RG, CEO it says RG, and you can select different combinations here. If I say just select R, it's only going to take the red channel. So it's basically the equivalent of that. Whereas let's say I put RG, then it's basically going to be both the RG channel. But what we want is we want just the red channel. So I'm just going to disconnect that green right there. So that's good. And as soon as we do that, the error fades from our alert and we're just going to create two different parameter values, two different constants, hold one left click twice. And I'm just going to name this. I'm going to convert these two to parameters, graphs getting a little bit tight here. So just going to blow it a little bit. So I'm just going to name these two parameters with the top one, which coordinates with the black areas of our roughness alpha mask. And that's going to be roughness min. And this one's going to be roughness max. And let's plug max into B and min into A. And let's change roughness max to one. So roughness min correlates to all of our black values inside of our alpha mask. And roughness max coordinates all of our white values inside of our alpha mask. So now that we have our roughness parameters, let's click save and let's minimize that window and also go to our test channel mask, one a material instance file. And let's apply that directly onto our mesh, just to see what's going on. And I want to see what's going to happen when I change these roughness parameters. So I'm just going to go to my G buffer and my buffer visualization mode by clicking lit and go back in there and click roughness. And now that I click that, I can see everything that's going on in my roughness channel. And you know, it'd be nice if I could just play around with these values a little bit and kind of get something that has a bit more contrast in it. So some of those values will really pop out a little bit more. So that's looking pretty good. I think it could use some extra stuff in the actual shader to make the moss look better, but that's for the next section of the course. And now that we have our roughness parameters, let's see if we can add another parameter specifically to alter the UVs and add some tiling parameters in there. And remember that anytime you want to alter the UVs, UV sets or change the tiling of your textures, you have to create what's called a texture coordinate node. And let's take that, plug it directly into all of our textures. And remember that if you want to tile it, you can change the tiling in here. But this texture coordinate node, it's not a parameter. So what we would do if we actually wanted to be able to tweak those UVs and that tiling per material instance is we would want to take this texture coordinate node and create a multiply node, hold the M left click and drag this, hold control, hold left click, drag let go. Move this. Connect that texture coordinate node directly to the A input and create a constant node, hold one left click. Connect that to the B and create a parameter with this constant node and let's name it UV tiling. And by default, this value should always be one because you always want it to be the default UV tiling of the 3D model that you're applying the material on to. And now if we click save and go directly to our viewport and select our material instance, can now see that we have a parameter named UV tiling. So we change that, can change the overall tiling of all of your textures on your material instance. And by now, you should have a fully functioning master material that has a bunch of different parameters in it that will give you ultimate control over the material instances that you create with it.