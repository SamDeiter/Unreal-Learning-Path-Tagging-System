Kind: captions Language: zh-Hans &gt;&gt;Nick Penwarden：好了 好的 遇到一点技术障碍 不过现在没问题了 大家早上好 谢谢参加演讲 我是Nick Penwarden 虚幻引擎的工程主管 今天上午我要讲的是 渲染团队最近完成的一些工作 这原本是Marcus Wassmer在GDC上 做的一个演讲 我想在这里 再重复一遍 在过去一年里 渲染团队投入了大量时间 来重构UE4的网格体绘制管线 我想谈谈我们为什么这么做 好处是什么 目前已经显现的一些成果 并告诉你调整引擎渲染功能时 以及在升级到4.22版本时 应该要注意些什么 然后再讲讲我们的开发动态 为什么我们花了一年时间 来重构UE4中的网格体绘制代码？ 其中一个原因是 如果你们参加了昨天的Keynote 我当时谈到有越来越多的人 正在制作开放世界游戏 我们希望专注于让人们 能够构建更加复杂的场景 支持更大范围的视野 更丰富的细节等等 如果你要创建一个世界场景 我们想让这个过程变得更加简单 你可能会采用模块化的构建方式 逐个添加网格体 用它们来丰富你的场景 我们不希望美术师花太多时间 在合并网格体上 以便降低绘制调用次数来进行优化 此外 动态光照 和阴影将变得越来越重要 因为游戏的动态资源越来越多 这些额外的通道需要 为多个视点多次绘制网格体 此外 今后的技术需要更多 有关场景的技术 以便进行评估 比如 在DXR中 如果你发出一条射线 你会在场景中命中某个对象 这个对象在主视图中不一定可见 发生这种情况时 你需要评估 三角形的表面着色器 并将数据返回给发出射线的着色器 这意味着 我们需要GPU中 所有和场景有关的信息 哪怕它在当前画面中看不见 在传统渲染管线中 也就是UE4以往的工作方式 或者说大多数游戏引擎的工作方式中 这些信息通常无法通过GPU获取 因为在渲染每一帧画面时 你只会将GPU需要的数据提供给它 让它只渲染 可见内容 接着 随着我们将越来越多的渲染计算工作 从CPU转移到GPU上 这就意味着 GPU将需要 越来越多与场景有关的信息 以便能够做出决定 来设置 渲染所需的所有命令缓冲区 我们如何实现这点？ 我们从合并绘制调用开始看起 怎样才能将多个批处理的绘制调用 合并起来 减少每一帧画面的 API调用次数？ 比如在D3D11中 实现这点唯一的方法就是实例绘制 为了实现这点 每次绘制调用时 场景中不同对象之间唯一不同的数据 就是实例ID 这意味着 为了实现这点 我们不再需要为每次绘制调用 单独设置参数 因此 我们需要做的 就是将所有数据都存放在GPU上 并且只能通过图元ID访问数据 另外一种 能够在渲染时降低CPU负载的方法是 更加频繁地进行缓存 内容没有发生变化 确保我们不会进行重复工作 场景中有不少内容不会每帧更新 它们在时序上非常一致 静态场景绘制 我们应该在对象添加到场景中时 才去绘制它们 然后 只有当它们发生实质性的移动或更新时 我们才应该使那些数据无效 借助更加频繁的缓存 我们的平台 也就是我们所说的RHI层 能够尽可能地进行预先构建 比如我提到的DXR中的 着色器绑定表中的条目 以及用于DX12、Vulkan等的图形管线状态 所以 我们的目标是 删除每一帧中构建静态几何体时 产生的所有渲染器开销 我想先谈谈在UE44.21或更早版本中 网格体绘制是如何工作的 我们从图元场景代理入手 图元场景代理是一种渲染线程副本 源于图元组件 图元组件 位于游戏线程上 也就是游戏代码所在的位置 也就是游戏中对象的移动逻辑所在的位置 渲染器拥有这些数据副本 部分是出于线程安全目的 部分是它可以缓存 只有渲染线程需要关心的信息 每一帧中 我们都会获取场景代理 然后通过它生成许多网格体批处理 原因在于 什么是网格体批处理？ 基本上就是所有的高级数据 能让我们解决 如何在特定通道中实现绘制调用 它能将场景代理与 所有的渲染实现细节解耦 场景代理无需知道它会在哪个通道中 进行渲染 它甚至不需要知道 渲染器中可能会有哪些通道 我们不需要它知道 这就让我们能够灵活 实现新的通道 新的算法 而不必更新每一个 可能需要渲染的对象 现在我们有了网格体批处理 我们还需要使用 所谓的绘制策略 将网格体批处理 转换成一组RHI命令 这样就能 比如 我们有一个深度绘制策略 用来渲染预通道 我们有一个基本通道绘制策略 用于渲染G缓冲区等等 它会从网格体批处理中读取数据 比如顶点工厂和材质 然后使用这些数据制定决策 最后 RHI命令列表是一种 跨平台的渲染命令集 然后在RHI的实现部分 具体的底层API层将负责具体的API调用 我们的D3D11实现会将这些RHI命令 转换成D3D11相关的绘制索引图元调用 以及对应的OpenGL调用 以便用于PS4 用于Vulkan等等 我想再深入介绍一下 在渲染器中我们是如何 获取网格体批处理 以及如何生成RHI命令的 在4.21以及更早的版本中 我们会遍历静态网格体绘制列表 或者动态网格体 绘制策略做的第一件事就是 搞清楚用什么着色器渲染？ 我们使用材质 使用顶点工厂 我们用它们来确定用什么着色器渲染 然后我们必须收集所有的着色器绑定 需要哪些参数来进行绘制？ 这些包括美术师 在材质中设置的参数和常量 用于顶点工厂的参数 和视图有关的信息 我们需要绑定所有这些参数 关于静态网格体绘制列表 另一点是 它是预先构建的 我们在4.21或更早版本中尝试了缓存 所以在添加参数时 在将某个参数添加到场景中时 我们会获取网格体批处理 按照部分绘制策略执行它 缓存一些信息 然后将它保存在静态网格体绘制列表中 静态网格体绘制列表实际上包含了 所有经过半预处理的网格体批处理 以便用于整副画面 然后再根据状态对它们进行排序 这样我们就能最小化 着色器和其他渲染状态的更改次数 在运行时 在我们实际 渲染网格体绘制列表时 它实际上会 遍历场景中的每一个静态网格体 然后查找一个位数组来确定可见性 也就是确定 这个网格体是否应该可见？ 是 绘制它 网格体可见吗？ 不是 这个网格体可见吗？不 这意味着 绘制的某些方面 会按照整个场景的大小缩放 而不仅仅是根据可见网格体的数量缩放 这个遍历相对比较快 对于某些大小的场景来说没有问题 但一旦超过了某个程度 就不能再扩大了 另一个问题是 这是一个完全不同的动态绘制代码路径 所以动态网格体批处理需要每一帧 都遍历整个绘制策略 这意味着我们无法在两者之间进行排序 任何在静态路径的内容 都无法与动态路径中的内容进行排序 从代码的角度来看 这可能意味着 有很多模板内容要处理 静态绘制列表通过绘制策略 采用模板实现 因此 你最后会在场景中得到一堆 这样的绘制列表 添加列表就会产生额外的开销 简而言之 静态网格体绘制列表 不利于高效地进行绘制合并 部分原因在于绘制策略 与静态网格体绘制列表的耦合度太高了 还有部分原因在于绘制策略在设计上 可以直接在每一次的绘制调用中 为RHI设置着色器参数 如果我们要合并绘制调用 就要避免这点 让我们来看看我们为4.22设计的 新的网格体绘制管线 首先 我们不再 遍历静态网格体绘制列表 不再使用绘制策略位 而是替换成 所谓的网格体绘制命令 什么是网格体绘制命令？ 网格体绘制命令保存着RHI 在执行绘制调用时需要的一切数据 它是一种完整、独立的底层描述 拥有管线状态对象 拥有着色器绑定 还包含索引缓冲区这类内容 供我们渲染 它完全是无状态的 没有数据会告诉你 网格体绘制命令的来源 也没有上下文 从数据转换的角度来看 这非常好 因为这意味着我们可以优化 替换、合并它们 而不必去实现 网格体批处理 或者去实现场景代理 实际上 网格体绘制命令 有一个调试指针 以便你了解绘制调用的来源 这对调试非常有用 但是我们发布的版本中将它剥离了 所以你不应该依赖它 编写任何代码 通过将这种无状态描述 应用到RHI的绘制调用上 能让我们更主动地进行缓存 而这正是我们想要的效果 我们可以在将静态几何体添加到场景中时 建立网格体绘制命令 我提到过 静态网格体绘制列表 会做一些缓存 网格体绘制命令缓存得更多 基本上就是将绘制策略 此前的整个执行部分 都缓存在一个网格体绘制命令中 这能让我们更接近目标 能够让我们获取 在场景中添加内容 并在场景中绘制内容时所需的管线状态 以及所有着色器绑定 另一点是 因为这单纯就是状态 我们能得到非常高效的绘制调用合并 只需查看状态 如果多次调用是同一种状态 就可以将它们合并成一个单独的绘制调用 稍后会具体介绍这点 回到生命周期这个问题 它到底是如何 它看上去到底是什么样子呢？ 我们有一个网格体批处理 我们需要为它生成网格体绘制命令 为此 我们用到了网格体通道处理器 网格体通道处理器的作用是 它会选择一个着色器 收集所有用于 渲染绘制调用的绑定 收集来自通道 顶点工厂、材质的绑定信息 现在你需要为渲染器中的每一个通道 编写一个网格体通道处理器 其中一点好处在于 它不需要依赖着色类模板 只是一个相对简单的类 我们在动态和静态绘制中使用了相同的代码路径 所以你在实现代码时 就无需再重复 复制渲染代码了 让我快速演示一个示例 让我们来看看深度通道 这可能是渲染器中最简单的通道 它只负责渲染 场景中所有图元的深度 当你新建网格体处理器时 会用到一些重复的模板代码 内容差不多就是这些 所以不是太复杂 基本上就是设置 FMeshPassProcessor所需要的内容 然后在实现部分 真正你需关心的部分都在这里 这是AddMeshBatch函数 场景中每个可见网格体批处理 都会调用它 你在这里需要做两件事 一个是过滤 是否需要用到这个通道 以及选择着色器 例如 关于深度通道 我们不会在深度通道 渲染半透明信息 我们会过滤掉它 我们会过滤掉任何在主通道不需要 渲染的内容 诸如此类 在选择着色器时 例如这里 我们会查看 是否为不透明网格体 如果是不透明表面 那么我们会做一些优化 比如这里 渲染时只用了仅有深度信息的顶点着色器 这比其他方法要快一点 如果涉及遮罩 那么我们就需要评估材质 以便进行阿尔法检测 这样逻辑就会稍微有些不同 这是选择着色器的第二部分 最终 我们会调用这个BuildMesh DrawCommands函数 它会收集绑定 这其实就是 所有深度处理通道处理器都会用到的代码 这样你就不用再自己编写函数 就是这样 这个函数 这实际上是个方便的模板函数 用于选择 是否使用深度着色器 就是这样 4.21中的深度绘制策略 大约有642行代码 深度网格体通道处理器大约有100行 设置这些新通道要简单得多 添加起来也更简单 基础通道的代码 也从近1000行减少为200多行代码 我相信所有工程师都喜欢删繁就简 所以减少代码量总是件好事 我想详细介绍下这个函数 我说过 你不需要写这个 BuildMeshDrawCommands函数 它实际上用于收集 绘制调用所需的所有着色器绑定 同时也是导致模板代码 变少的原因 也是为什么实现新网格体通道 比以前更简单的原因 这里的主要变化是 不再直接获取 我们想为着色器设置的所有参数 并且直接让RHI设置它们 也就是直接设置这个 Float4偏移值 相反我们的办法是 将它们收集到一个着色器绑定表中 再将它们缓存起来 在收集了所有网格体绘制命令后 就可以对它们进行排序 正如我之前提到的 我们此前在动态网格体和静态网格体之间 无法实现状态排序 因为它们沿着不同的代码路径 现在 由于它们全都使用相同的数据 对我们来说 为所有的网格体绘制命令排序就变得非常简单了 最后 我们会提交命令 现在我们有一张网格体绘制命令列表 我们要做的就是迭代那个可见对象列表 生成RHI命令 以便在这一帧中渲染它 SubmitMeshDraw Commands函数的优点 它只和可见图元数量有关 所以不再会出现 因为遍历静态网格体绘制列表 而导致 整个可见网格体出现开销 此外 它比静态网格体绘制列表更容易并行化 有两个原因 首先 分成偶数个任务更容易 当你生成并行任务时 你应该大致生成偶数个的任务 你不应该让某个任务 比其他任务要多花10倍的时间 因为它最终会成为你的关键路径 在这种情况下 由于它是一个可被整除的集合 所以会非常简单 如果有5000个可见网格体 我们会把它分成每组100个或200个 并将其发送给任务线程进行处理 使用静态网格体绘制列表时 我们并不能做到这点 我们必须知道哪一个会查看 这一帧中前100、200或300个网格体 然后它会遍历位数组 只绘制在这些区域中可见的对象 其中会有一些很复杂的代码 它们会根据前一帧的可见性 试着确定如何把任务分成偶数个 在实际使用中 效果还不错 但是比起ParallelFor 还是要复杂不少 另外 我们还会获取这些网格体绘制命令 也就是一些数据 然后写入到RHI命令列表中 它只是一个数据流 没有任何不利影响 我们没有触及全局状态 也没有修改全局状态 不用担心竞争条件 我们在SubmitMeshDraw Commands中做的另一件事是 我们会对RHI命令列表执行状态过滤 这能减少加载量 还能减少 执行命令列表所需的时间 我记得在《堡垒之夜》中 我们发现 仅仅凭借这点就节省了20%的加载量 这也意味着 如果做到这样 每个平台的实现负担就会减少 以确保它们能够实现非常精细化的状态缓存 同时在缓存上又是连贯的 这只是一个很庞大的扁平化数据数组 因此 由于我们需要的一切数据 都位于这个连续的内存空间中 我们在渲染时就不会再遇到大量的 缓存遗漏情况 我们可以遍历这个扁平化数组 CPU的预读取给了我们很大帮助 它让一切都变得非常快 好的 我们已经介绍了如何重构 如何获取图元并生成网格体批处理 如何生成网格体绘制命令等等 现在让我们介绍缓存部分 同样 场景中的大多数 处理内容都是模块化的静态网格体 所以我们不应该每一帧都重复所有工作 我们应该在场景中缓存它们 然后在每一帧中选择合适的来绘制 这意味着任何被网格体绘制命令所引用的内容 都不能经常改变 因为只要发生了改变 我们就需要让它们失效 我们要做的就是通过间接层 来实现这点 例如 让我们逐个看下视图参数 这些参数每帧都会改变 渲染器此前的工作方式是 每一帧 我们都会收集所有数据 我们会创建一个统一缓冲区 视图统一缓冲区 当我们执行绘制调用时 我们会绑定单独为那一帧 创建的视图统一缓冲区 并执行绘制调用 但现在不行 因为这样我们就必须让所有 网格体绘制命令都失效 让所有帧都失效 以便更新绑定 所以 我们不去动绑定 我们会单独创建一个视图统一缓冲区 它始终会绑定到每个绘制命令 然后在每一帧中 当我们需要渲染视图时 我们会更新视图统一缓冲区的内容 间接层会保持不变 哪怕缓冲区数据最终改变 也是如此 为了支持这点 RHI现在会支持一个 能够动态更新统一缓冲区内容的代码路径 以前不会这样 来看看我们从绘制命令中 引用的数据类型 以及我们如何将它们组织成统一缓冲区 例如 我们有图元统一缓冲区 这是我们存储所有 与图元唯一相关的数据的地方 例如本地到世界坐标的变换 然后是材质统一缓冲区 保存所有与材质唯一相关的内容 例如美术师 在材质实例常量中设置的参数 着色和不同的纹理等等 还有材质参数集 是一些场景特定内容 这些是材质可以访问的数据 但在整个场景都可以访问 预先计算光照统一缓冲区 用于访问数据 例如 应该引用哪些光照贴图 光照贴图的偏移量是多少 这些一般都视图元而定 然后是通道统一缓冲区 这些是特定通道在渲染各个图元时 所需要的数据 但不会在不同通道中共享 深度预通道使用的绑定 可能和基础通道不同 一旦我们把所有数据都移到统一缓冲区之后 我们就必须确保在任何时候 只要需要发生变动 我们就要让网格体绘制命令失效 这很重要 因为如果你搞错这一步 渲染就会不正确 所幸 大部分问题都已经替你处理好了 因为静态网格体绘制列表在缓存时 会缓存评估的第一部分内容 这部分内容被我们现在用来生成网格体绘制命令 这样所有这些情况都仍然会保持相关 每当我们要让静态网格体绘制列表失效 我们就必须让网格体绘制命令失效 但并非全部 因为现在我们也缓存了着色器绑定 而此前并没有 因此 每次 场景中发生变化时 都会导致着色器绑定发生变化 我们要记得让缓存的 网格体绘制命令失效 我们添加了一个验证模式 它会增加很多开销 所以大多数时候都不会运行 但是如果你遇到了一个错误 并且你觉得 有可能是因为缓存失效错误导致的 你就可以启用它 当出现问题时 它就会进行断言 这对于找出错误 十分有用 我们在查看代码路径时 会经常用它 查看旮旯角落 用来追踪 要找的错误 非常有用 如果你遇到这种情况 比如你发现画面有些闪烁 或渲染器在修改后出现了无法解释的崩溃 你可以用它来进行第一步调试 顶点工厂 如果你熟悉它的话 就知道它是用来生成顶点数据的 以便让顶点着色器进行绘制 并让我们将数据从CPU传输到GPU中 目前 我们只支持对那些不需要在每个视图中 更新绑定的顶点工厂进行缓存 例如 局部顶点工厂 我们将局部顶点工厂用于静态网格体 我们还把它用于许多其他一些情况 它目前是引擎中最常用的 顶点工厂 至于其他需要每个视图进行更新的顶点工厂 我们仍允许它们缓存网格体批处理 所以部分内容仍会被缓存 但不是全部 其中包括那些LOD计算 比静态网格体要复杂一些的地形 因此需要每个视图都进行评估 还包括BSP 实例化对象 诸如此类 这意味着我们的缓存代码路径 在执行效率上会有一些不同 有一条效率最低的路径 我们称之为动态关联 比如Cascade粒子 或Niagara粒子 它们每帧都会改变 我们会将新数据 从游戏线程传入渲染线程 在每一帧中 场景代理都要生成 一组新的网格体批处理 我们会获取那些网格体批处理 生成一组新的绘制命令 然后获取绘制命令 并最终为它们生成RHI命令 至于静态图元 如果没有发生变化 也就是游戏代码没有发送新的数据 但我们需要改变基于视图 创建绘制命令的方式 我们就要多做点工作 我们需要缓存网格体批处理 然后每一帧中 获取网格体批处理 生成绘制命令 然后这些绘制命令 会转化成RHI命令 然后获得最有效的路径 用于静态网格体的路径 例如 我们会在添加场景时 缓存网格体绘制命令 然后在运行时 我们只需找到 只需收集所有可见网格体绘制命令 向它们发出RHI命令 来看下启用了缓存功能的高级帧 当我们向场景添加图元时 如果是静态图元 我们就缓存网格体批处理 如果顶点工厂不需要视图 如果顶点工厂支持绘制命令缓存 那么我们也会获取那些网格体批处理 并在那时生成网格体绘制命令 然后缓存它们 但也有若干种情况 例如 我们更换天空球时 这可能会为场景中所有内容 改变着色器绑定 我们会让缓存网格体绘制命令失效 当需要渲染场景时 我们会检查所有缓存批处理 并重新生成这些网格体绘制命令 然后在每一帧中 当我们在渲染时 我们会计算出场景中 所有可见的图元 如果它们是静态的 我们就计算它们的LOD 然后将缓存网格体绘制命令添加到可见列表中 如果是动态的 我们就继续 收集它们的网格体批处理 从中生成绘制命令 然后使用这些绘制命令 将它们添加到同一个可见列表中 之后 在初始化视图后 每个后续通道只需要查看这个 可见网格体绘制命令列表 然后绘制内容 好了 现在我们有了一个非常不错的 面向数据的网格体绘制命令框架 我们把这些都设置妥当 以便合并绘制调用 它到底是什么样的呢？ 在D3D11中 我们会间接使用绘制实例 这意味着在合并绘制时 唯一需要改变的是实例ID 我们不能改变着色器 不能改变绑定 仅此而已 但这意味着我们可以实现动态实例化 目前为止 借助我们现有的框架 实现这点很容易 将来在D3D12中 ExecuteIndirect方法 将允许我们改变实例绘制调用之间的状态 这就能让我们 获取这些网格体绘制命令的总列表 并调用更少的接口来渲染它们 好 让我们来具体看看 我应该提一下 我们实际上还没有实现 D3D12的路径 这将是我们未来的努力方向 现在我们已经在D3D11中完成了动态实例化 让我们来了解一下动态实例化 现在我们已经有了这些网格体绘制命令 我们可以放心地合并它们 因为它们都处于底层RHI状态 我们需要做的就是比较那里的数据 如果是一样的 就可以合并绘制调用 改为实例绘制调用 好处就是 内容开发人员无需再担心它 他们无需再创建一个特殊的实例静态网格体 试着把它们合并起来 然后担心如何进行剔除 如何平衡绘制调用和剔除效率 等等 只要用就行了 当然 也有不利的一面 分配这些状态桶会比较慢 执行这些比较也会花很多时间 我们不应该每一帧都比较 让我们先缓存以便添加进场景 当我们生成绘制命令时 首先 此时我们能做的是 查找状态桶的缓存 然后看这个绘制命令属于 哪个状态桶 这样就能尽早分组 知道哪些绘制命令可以合并在一起 然后 具体的合并操作 只是在可见对象列表上进行数据转换 在这个可视对象绘制调用总列表中 任何在同一个状态桶中的命令 都用一个网格体绘制命令来代替 我们排列了网格体绘制命令列表 当我们遍历它们时 我们会查看它们位于哪个状态桶之中 只要处于相同的状态桶中 就合并到同一个实例绘制调用中 合并通道的输出只是一张 合并了网格体绘制命令后的缩小列表 现在我们可以合并网格体绘制命令了 这非常棒 我们如何确保它们尽可能高效？ 我们要做的其中一件事是 消除每一次绘制中任何 会把绘制命令拆分为 多个子命令的绑定 例如 任何需要 每个通道 每个图元绑定的内容 都需要避免发生这点 为了做到这点 我们的办法是 创建了一个通道统一缓冲区频率 例如一个存储数据的通道 例如 在基础通道中 我们可能会对D缓冲区纹理进行取样 可能会对眼部适应进行取样 纹理、雾参数 诸如此类 这些内容不会因为整个基础通道改变 为什么每次绘制调用都设置它们呢 只要为这一帧的基础通道设置一个统一缓冲区 然后渲染它们 此时会有一些剩余的绘制调用绑定 这里有全局常量缓冲数值 有图元统一缓冲区 这会根据场景中的每一个图元进行改变 如果我们不改变保存图元统一数据的方式 在某种程度上 会很容易拆分 我们所有的合并绘制调用 我们将无法合并任何绘制调用 预先计算光照 这是图元需要渲染 的光照贴图 距离调用数据 当你使用距离剔除或LOD优化时 不同的图元可能处于不同的状态 这样就会破坏合并的有效性 我们的想法是 将所有这些数据上传到 一个场景大小的单独缓冲区中 供我们随后 基于着色器中的实例ID进行查看 这样 当我们合并绘制调用时 无论是单独渲染还是合并渲染 都没有关系 无论如何 我们都能查看 并获取数据 而不必为着色器改变绑定 为此 在很多情况下 我们都要做到这点 我们实现了一个通用的 请试着把它想象成一种GPUT TArray的实现 一种可以在GPU上动态调整大小的数组 当你需要从CPU插入数据时 我们会跟踪 添加更新 然后删除 然后在渲染前 我们会启动一些计算着色器 根据需要打乱数据 以便实现目的 这意味着我们无需在每一帧中 上传整个图元缓冲区 我们可以跟踪改变量 并只执行这些操作 这样做的一个结果是 当你在着色器中 访问数据时 你需要使用 GetPrimitiveData 之前 你可以直接访问图元统一缓冲区 但现在 我们需要间接访问 并且需要用到图元ID 任何试图访问数据的操作都必须通过 这个GetPrimitiveData辅助函数实现 当然 这只用于支持顶点工厂 并且被这个GetPrimitiveData方法 抽象了 所以还算不错 但我们究竟该如何在着色器中获取图元ID呢？ 我们只有实例ID 我们可以创建一个缓冲区 让每个绘制调用都和 图元ID绑定 并建立索引 而每次绘制调用都添加一个 全局常量缓冲值 实际上这会将基础通道的 绘制时间增加20% 我们不希望仅仅因为每次绘制调用时 需要更新一个参数就增加那么多开销 所幸 输入汇编程序有一个更快的办法 我们可以将动态偏移 传入SetStreamSource 这让我们可以按照实例频率设置 图元ID的顶点输入 然后在绘制调用合并之后 我们会通过网格体绘制命令列表 构建图元ID的缓冲区 好的 我们完成合并绘制调用所需的所有工作 让我们来看看 结果到底如何？ 这有一张地图 叫做“GPU性能测试” 这其实是从《堡垒之夜》拯救世界模式中 找来了的一张地图 大概是在一两年前 用来作为静态地图 以便用于迭代并测试渲染性能 起初 主要用于测试GPU性能 但最后成为了我们 用来优化《堡垒之夜》的静态分析场景 再看一下这个 看看深度通道 通过合并 我们将绘制调用的次数 降低了2倍多 在基础通道中 同样降低了2倍多 深度通道的效率更高了 因为有更多机会 共享同一个着色器 以便用于 没有使用透明遮罩的网格体 并且可以只使用深度绘制策略 但这还不足以让我们确信 要知道降低2倍很不错 但我们还想看看它的潜力有多大 我们创建了程序员版本的美术场景 试着让场景更复杂 我们将场景复制了三次 然后我们删除了距离剔除功能 以便渲染远处的场景 结果是 我们最后得到的深度通道 大概每帧 调用了7400次绘制调用 通过合并绘制调用 我们得以 将深度通道的绘制调用次数降低了约10倍 基础通道则是降低了5倍 话虽如此 我还是想提个醒 目前为止 这是最理想的情况 我们只是复制粘贴内容 所以十分适合动态实例化 但这在某种程度上证明了 如果你创建一个模块化的静态场景 那么这段代码就可以 在后端优化上做得很好 还有一些性能问题需要关注 那些会妨碍绘制调用合并的内容 创建小型纹理的光照贴图 基本上 如果你使用静态光照 并且很多图元都出现在不同的光照贴图中 那么就不能合并绘制调用了 因为它们用的是不同的光照贴图 你可以调整半径 以便将图元 打包到同一张光照贴图中 这样会有所帮助 你需要进行权衡 究竟是侧重纹理流送效率以及光照贴图占内存 还是侧重绘制调用效率和合并次数 为场景中的实例使用顶点绘制 也会破坏绘制调用的合并 因为它们存在于各自独立的顶点缓冲区中 需要分别绑定 SpeedTreeWind节点 注意 如果你使用SpeedTreeWind节点 就不能使用合并 没有什么技术上的原因 就是还不支持而已 未来可能会得到改善 如果你还在使用旧版本中的稀疏体积光照取样 用于为带有静态光照的动态对象 设置光照效果 合并也无法奏效 然而 新的体积光照贴图 能完美支持绘制调用 好的 之前 我们只看了绘制调用 了解了合并如何能够 降低绘制调用的次数 但具体就毫秒层面而言 究竟有多大改善呢 因为说到底 我们关心的 是具体节省的时间 我们在PS4上进行了测试 使用主机平台的好处就是 无需担心操作系统的干扰 也无需担心硬件的不统一 这是一个非常稳定的测试平台 在测试GPUPerfTest这张地图时 使用4.21的旧代码时 深度通道大约需要2毫秒 基础通道需要3.2毫秒 使用新的网格体绘制命令管线 和动态实例化后 深度绘制只需要0.3毫秒 基础通道只需要0.4毫秒 变得快多了 快了6到7倍 在程序员版本的美术场景中 使用老版本代码时 深度通道大约需要15.7毫秒 基础通道是27.8毫秒 大概有7400次绘制调用 使用新的代码路径后 深度通道只需要1.2毫秒 基础通道只要2.4毫秒 快多了不是吗？ 快了10倍还不止 但说明一下 在这个测试用例中 效果非常棒 是迄今为止最好的结果 首先 这是一个高度模块化的场景 网格体和着色器的变化非常有限 此外 我展示的速度加快效果 只和网格体绘制有关 出于多个原因 可能不会位于 关键代码路径中 例如 我说的不是初始化视图 也就是可见性部分 如果可见性仍然占据绘制时间的大头 那么针对网格体绘制 降低批处理数量可能不会管用 如果你 哦 另一个原因 我们已经让渲染器尽可能采用了并行处理 如果你有很多任务线程 并且没有用于其它用途 我们可能已经利用了它们 即使这些压力消失了 你仍然可能看不到 帧率有明显的提升 此外 你的游戏内容和我们的游戏内容 也就是我们在《堡垒之夜》中发布的内容 以及所有的演示内容和其他项目内容 都针对前一个渲染器进行了优化 所以我们的美术师非常谨慎 一直在确保降低绘制调用的次数 当你的场景放大到原来的三倍 四倍 五倍时 这些好处 会真的开始显现出来 改动后的不利因素 会有一些内容失效 渲染器中所有涉及到效率的 延迟图元更新机制 都会失效 因为它们无法与在创建场景时 缓存所有数据保持兼容 比如 如果一个图元移动了 但它是不可见的 我们永远不会再为那个图元更新图元统一缓冲区 没有必要再这么做 因为我们需要更新一些GPU数据 这样DXR就可以向图元发出射线 或者让我们创建一个网格体绘制命令 以便引用它 此外 如果你有任何材质 用到了自定义表达式 它们几乎可以输入任何想要的HLSL代码 并且还可以从图元统一缓冲区 获取数据 如果它们使用了 你就要确保 将这些自定义模式改为使用 GetPrimitiveData这个访问函数 如果你更新到了4.22版本 发现有些材质无法再编译了 很可能就是这个原因 去查看下自定义表达式 很可能是它出了问题 正向渲染器现在只支持 单一全局平面反射 因为如果要实现多个全局平面反射 在这种情况下 我们就必须考虑到 每个视图的信息 这我之前也提到过 但是当前的UE4项目不一定那么好用 我想提醒一下 你们可以回家试一下4.22版本 你可能感受不到速度变快了10倍 这就是为什么只升级到4.22并不会让你 获得10倍的加速 但尽管如此 我们还是收到了很好的反馈 在我们发布预览版后 一些开发者在推特上对此进行了讨论 Joe Wintergreen表示在他的项目中 4.22大约降低了1000次绘制调用 名为“Shores Unknown” 的用户告诉我们 他们有一个场景 绘制调用 原本是18000次 使用4.22后大约只需2300次 仅仅是更新到4.22 他们的场景帧率 就从30提升到了60 所以帧率可能会有很大的提升 也可能不会 这取决于你构建场景的方式 但无论如何 我想我们已经成功让渲染器 得以更好地处理大量绘制调用的情况 所以今后 你将能够构建更为庞大和复杂的场景 关于绘制调用合并 有一点我忘了提醒了 在4.22版本中 我们不支持在移动设备上合并绘制调用 不是因为技术原因 仅仅是因为我们来不及在4.22中实现这个功能 我们将在4.23中实现这个功能 原因就是 移动设备渲染器的有些通道 和延迟渲染器中的通道并不一样 我们需要做一些相同的转换 来提取我们在每次绘制调用中绑定的数据 并确保将它们传入每个缓冲区 或把它们传入全局场景缓冲区中 然后每个图元就都能访问了 就这样 以上就是 UE4全新网格体绘制管线的介绍 谢谢大家来听讲座 希望对你们有所帮助 我希望你们能了解我们这么做的原因 我希望当你更新到4.22时 帧率能得到明显的改善 如果没有 等到场景网格体变多就能感受到了 谢谢 [鼓掌] ♫ 虚幻引擎标志音乐 ♫