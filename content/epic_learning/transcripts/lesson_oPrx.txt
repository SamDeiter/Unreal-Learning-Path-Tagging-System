In this video we'll have the AI controller make a decision on whether or not to attack the player based upon information it receives via the event dispatches we created in the previous video. We'll start by binding events to our new event dispatches before moving on to carry out additional checks to decide if the AI should attack the player. Finally, we'll move on to setting up the attack state ready to actually fire the projectile. Let's move over to the AI controller to handle the event dispatcher we just created. While we use this new event dispatcher to create a basic alert status for the AI, representing its awareness of the player character as a viable target. From the content browser, open enemy AI from content to G-size scroller BP, blueprints, enemies. Locate the existing event on possess. We can see here that we're already binding to a few event dispatches, specifically undetected ledge and on hit obstacle. Let's bind our new event to our newly created event dispatcher just after these. Drag from the execution pin of bind event to on hit obstacle and locate bind event to on enemy sense change. Make sure to set the target in put on this bind node to the enemy character. We can do that quickly now by copying and pasting it from the previous node. Now let's create a new custom event to be bound to this dispatcher. Drag from the event input and locate custom event. Name this new custom event on enemy sense change. Note that unlike our previously bound events, this event has the player and detected outputs we can configure as part of the signature of the dispatcher. We can now use these outputs to branch the AI logic to either patrol back and forth as before or attempt to attack the player. Begin by creating a branch near the new custom event by holding B and left clicking onto the graph. Connect the execution wire to the event and the detected Boolean output to the condition input of the branch. Let's start by handling if the player is detected first. If that happens, we want to set the player as a target by storing a reference to them on the controller itself. Let's do that by dragging from the player output of the custom event and selecting Promote to variable. Name this variable player target. For reasons that will become clear later, we only want to update this player target reference immediately when the player character is detected. So position this set node after the branch and connect it to the true execution path of the branch. I'd suggest using some re-root nodes here to minimize the wire crossover. We'll add some more logic to this particular branch later, but for now, let's move over to the false branch. This will be executed at the moment when the player ends overlap with the sense volume. We could just set a blank reference as the player target immediately, but it's more natural to include a slight delay, and this also allows the AI to continue to respond to the player's recent presence rather than just instantly giving up which looks unnatural. For that reason, we'll use a timer to fire an event after a short configurable delay. Drag from the false execution pin of the branch and locate set timer by event. Drag from the event input and create a new custom event. Call this custom event disable alert. Alt-left-click drag the player target variable from the variables roll out of the My Blueprint panel into the graph to place a set node. Connect this node to the custom event we just created. Now, when this event is fired, we'll remove our player target reference and put the AI in a non-alert state as they have nothing to attack. Returning to the set timer by event function, we should look at the time input. The current value for this is 0, meaning the event will fire instantly, which isn't what we want. Drag from the time input of the set timer by event node and select Promote to variable. Name this variable disable alert delay. Compile the Blueprint so that we can set a default value for this variable. In the Details panel, set the default value to 1. Before moving on, let's reposition and tidy up our nodes to prevent the overlapping too much. This basic logic will get us most of the way, but there's one more thing we should do right away to prevent problems. Currently, this timer we created will persist even if the player becomes visible again, meaning that we could end up in a situation where the player is with in-the-detection volume, but the disable alert event still fires, causing the player target to become invalid, while the player is still actually a viable target. Luckily, this is easy to fix, and we'll do that by clearing the timer if the player becomes a valid target again, which will prevent that event from ever firing. To do that, we'll need a reference to the timer itself, and luckily for us, the Set Timer by Event function provides us with that. Drag from the return value of the Set Timer by Event node, select Promote to variable. Name this variable disable alert delay handle. Control left click drag this new variable from the My Blueprint panel into the Event graph near the logic for the true execution path of the branch. Drag from the output of this variable, and locate clear and invalidate timer by handle. Connect this new function to the execution pin of the Set node for the player target. We'll return to this logic again soon, but for now, we're done here. This set up a basic alert status we can use to influence AI behavior determined by whether or not the AI has a valid player target. Speaking of player behavior, it's time to upgrade our AI's core functionality to support more and just walking forwards and backwards. Let's start by reviewing the logic we already have, as it won't take very long. Locate the event tick and view the connected node add movement input. What we're doing here, is pretty simple. Every tick we simply add movement input in a facing direction of the AI character, and that's it. Now going forward, we want the AI to attack the player as well, which means multiple states and transitions driven by the alert status we just created. Unreal Engine has some really great AI tools including behavior trees, but for now, let's keep things simple and just build two basic states using Blueprint logic. We'll consider these to be patrolling and attacking. As we're going to dramatically expand this logic, it makes sense to move it into its own event and then call that event from event tick. So let's do that. Right-click in an empty space on the event graph and locate add custom event. Name this new custom event do behavior. Move this new event further into the graph to give ourselves a bit more space. Now we have our new behavior event. We want to branch this behavior dependent upon whether the enemy is alerted or not. So let's make a branch by pressing B and left-clicking in an empty space on the graph. Connect the execution wire from the do behavior custom event into the branch. As we already know, branches work on Boolean operations, but currently we're deciding if the AI is alerted by if we have a valid player target variable. In order to get this as a Boolean value, we'll make a quick function that returns true or false depending on if the AI has a valid target or not. In my Blueprint panel, click the plus icon next to the functions rollout to add a new function. Call this new function is alerted. Let's begin by setting up the return Boolean variable first. In the details panel on the right hand side, click the plus next to the output rollout. Ensure that the output is of the Boolean type and name this output alerted. Note that this return node will automatically be hooked up in our function. Now we just need to return true or false depending on if the player target variable we set up earlier is valid. Luckily for us, there's a built-in function for this comment task called, imaginatively enough, is valid. Control left click the player target variable from the my Blueprint panel into the graph to get a reference to it. Drag from the output of this node and locate the is valid function. Note that the return value from this function is a Boolean, which is exactly what we need. Connect this return value to the alerted input of the return node for the function. Take the time to reposition the nodes, everything is neat and tidy and readable from left to right. Just one more thing. With the function selected in the my Blueprint tab, move over to the details panel on the right hand side. Locate the pure checkbox in the graph rollout and ensure that it's ticked. The full details of how pure functions work are beyond the scope of this course. But for now, all we need to know is that pure functions promise not to modify the state of the members in the class in any way and most importantly for our uses allow us to use them without using an execution pin. Compile this Blueprint and return to the Event Graph tab. Time to use our new function to drive the condition input of the branch we just created. Drag the function into the Event Graph from the my Blueprint panel. Connect the alerted output into the condition input of the branch. Let's start with the easiest part of the branch first, the false path. If the enemy isn't alerted, we want them to patrol, which in our case just means keep moving in the direction they are facing as before. As you may have guessed, we can use exactly the same logic to do this as we did previously. Locate the Add Movement input function from earlier and Alt left click to disconnect it from the Event Tick execution pin. Marky select the nodes and bring them towards the false path of the branch. Now connect the false execution path of the branch into this function. Now when the enemy is not alert, they will walk backwards and forwards just as before. Before we forget, there's something quite important we should do. We should make sure that this new do behavior custom event is actually called from the Event Tick. Otherwise, we might get further into the process and wonder why none of this is working. Locate the Event Tick event and drag from the execution pin and locate do behavior. That's all we need to do at this stage so we can compile, save and close the Blueprint Editor. Before we can expand the AI controller logic any further, we'll need to give the AI character the ability to actually fire the projectile. We'll be setting all of that up from the enemy character Blueprint. Go ahead and open this Blueprint from Content 2D Size Grull of PP, Blueprints, Enemies in the Content Browser. Move to the Viewport tab so that we can visualize the changes we're about to make. With the Capture Component in the Components tab selected, hit the Add Component button and locate the Scene Component. Name this Component Projectile Source. With a new component added, let's position it where we want our projectile to start from. With the Projectile Source still selected, move to the Location section of the Transform rollout. Set the Location to be 20, 0, minus 10. If we look at the Viewport, we can see this places the Source Location roughly above the character's fist. With the Component Setup complete, let's go ahead and create an event that will actually fire this projectile. Move to the Event Graph tab and right-click an empty space in the Graph and locate Custom Event. Name this new Custom Event Fire Projectile. Most of the animations for the AI are looping, but we only want this attack animation to play once. For that reason, our first step will be to disable the looping variable for the AI character sprite. Control, left-click drag the sprite component from the Components tab into the Event Graph to get a reference to that sprite component. Now, drag from the output of the sprite variable and locate Set looping. Note that the fault value of the new looping Boolean is false. This means the animation will no longer loop, which is exactly what we want. Connect the execution wire between the Fire Projectile Custom Event and the Set looping function. Now it's time to play the Attack animation. Control, left-click drag the Attack animation variable into the Graph from the animations roll out of the Variable Troller within the My Blueprint panel. Drag from the output of this variable and locate Update Flipbook Animation. Connect this node to the previous Set looping function. In the same way that we have created a Boolean variable to track running and moving direction, we also need to create a Boolean to track the attacking state to power logic we'll be creating soon. Create a new variable, ensure it is of the Boolean type, and name it is Attacking. Hold down Alt and left-click drag this new variable into the Graph in order to set the variable. Connect the execution pin to the previous node and make sure to check the Is Attacking box, setting the Boolean variable to true. It's almost time to fire the actual projectile. I promise. Before we do that, let's take a quick look at the Attack animation itself. The quickest way to do that is to select the Attack animation variable in the My Blueprint panel, and then double-click the Attack animation in the Details panel on the right-hand side to be taken to the Flipbook Editor. Note how there's a degree of wind up to this animation. If the projectile fired as soon as the animation started playing, it would look strange. For that reason, let's have a short delay before spawning the projectile. Close the Flipbook Editor tab and return to the enemy character event graph. Drag from the execution pin of the Set node for Is Attacking and locate the Delay node. Set the duration to 0.4. Finally, let's spawn our projectile. Drag from the execution pin of the Delay node and locate Spawn Actor from Class. Select the class dropdown and locate Enemy projectile. With the class selected, now we need to provide the transform for this object to spawn. Selects use our projectile source component to do that. Control left-click drag to projectile source component from the Components panel into the Event Graph. Drag from the projectile source and locate GetWildTransform. Connect the Return value from dysfunction into the spawn transform input of the spawn actor node. We can happily leave the collision handling as is as we're not doing anything too clever here for the time being. However, we do need to set up the instigator. As we can see, this is the Pawn Responsible for Damage and other gameplay events caused by this actor. To provide that, right-click in an empty space in the Event Graph and locate GetReference to Self. Connect the Self Reference to the instigator input of the Spawn Actor Enemy projectile. Before moving on, it's worth taking the time to reposition the nodes to prevent any wires from overlapping and keep everything nice and readable. Once that's done, we've done it. We've built the logic that will actually fire the projectile. So for now, we can compile, save and close the Blueprint Editor window.