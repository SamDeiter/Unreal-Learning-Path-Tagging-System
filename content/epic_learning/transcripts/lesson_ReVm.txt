With the Control Rig asset all set up in the forward solve, let's debug the functionality and performance of the RIG logic using some very useful inbuilt tools in the Control Rig editor. Let's start with the Visual Debug. I'll use a basic IK node to demonstrate the use case. Now if you right click on any node pin, at the very bottom you'll see this option Add Visual Debug. Just select this, you'll notice that the pin IKIN now looks like a bug. Also there is now a red box in the viewport. This is a visual representation of Debug node and you can modify the properties of this node via the details panel here. But this appears at the origin and that because the space is not set to any bone. So I can set it to lower RML and it snaps to that bone location. And it's not useful to me as a box as I want to debug the orientation of the elbow. So I'll change the mode from point to a vector, update the thickness and change scale for a proper visualization. Now you can see that the primary access value is correct as it is aligned along the length of the item. I keep referring to Visual Debug as node because it's actually a node that is hidden right now. You can right click on the pin with the bug icon and select eject last node. This displays the visual debug as a node with all its properties. Now I can change the value and this will directly reflect on the viewport. Next couple of debugging tools might sound very familiar to anyone coming from a blueprint background. These are watches and breakpoints. Watches will let you watch any pin value of a node. For example, if I go to the for each node that I use for fingers, right click on the element pin and watch this value. It displays all the regaliments like these finger bones running through this node. Similarly, I can watch values on this concat node and you will see all the finger controls listed here. Also if I want to get rid of these, I can right click and select stop watching this value to disable the watches. But I'll keep it as it is for now to showcase the breakpoints in action. Breakpoints allow us to add breaks at different points in the execution flow. So I can right click on any node to add a breakpoint and you'll see the graph focusing on this node when it's executed. Breakpoints are highlighted by these red dots over the node and this huge arrow over the node indicates the current breakpoint being executed. Since this is where the breakpoint has paused the execution, you can see that the watches have been reduced to just one value each inside the loop. These indicate the current values from each node. Also take a note that this current node with the huge arrow on the top is not executed yet. I can use these debug buttons up here in the toolbar. If I click on this button with right arrow icon, it will make me go to the next node. So I can keep clicking on it to move one node at a time or I can just hit this green resume button to move to the next breakpoint. Now you'll see that your watches have been incremented with one value. So I can go ahead and remove the second breakpoint by right clicking and selecting remove breakpoint. Now if I keep hitting that resume button, you'll see that the loop keeps incrementing with new values. This will go on till the end of the loop. To get out of it, I can just hit the last button to step out of the function. This brings the loop back to its initial stage with a single value. Remember that breakpoints will only get triggered when the node is being executed. So if I disconnect the execution pin, you'll notice that we just see the red dot over the node indicating a breakpoint that is not triggered. Let me connect it back and I'll remove this breakpoint. So that's how you can debug your reg nodes at each step of the execution using breakpoints. For the next two tools, we'll use the class settings menu in the toolbar. First one is VM profiler. If you look under the VM category, also shot for virtual machine, you'll see VM runtime settings. Expand this and there is an option called enable profiling. Let's enable this and you'll immediately notice all these nodes flashing with different colors, ranging from green to red. This indicates how much time a node takes to execute. So green means the node is faster and red means the node is taking more time to execute. This is a good indicator to check the performance of individual nodes in the reg graph. Looks like the basic IK node is moving towards red. You'll see that it goes in the range of 8 to 17 microseconds. Going back to the class settings under the user interface section, I'll expand the reg graph display setting. You'll see the colors to indicate the node duration. I mentioned here. You can change these colors, but green and red make more sense for the purpose. They'll leave it as it is. Also, if you see your nodes exceeding the current time range, you can turn off this auto determined range and set your own. VM profiling also helps in quickly identifying the execution flow. If I add a branch here with false input and connect the execution to true pin, then you'll see that these nodes stop executing and they're not considered for profiling. I'll switch it back to true and it immediately starts getting profiled. Next tool is node run count. In the reg graph display settings under class settings, you'll see the toggle called show node run counts. If I enable this, it'll display the node count which indicates how often that particular node has been run. But if I check the other nodes like getters and IK nodes here, you'll see that there is no node count displayed for them. This is because the lower bound is set to one right now and you can check the same under the enable node count parameter. So if I change it to zero, you'll see that the count is now displayed for all the nodes that are being executed. Then there is node run limit which is set to 64 by default. So if any node exceeds this count, you'll see a warning message saying that this instruction has hit the node run limit. In that case, you can increase this value and the warning will go away. But this is to inform you that a certain node is exceeding the node count mostly in case of a heavier rig like mid-cat for example. So along with the reg graph, node count is also displayed in the execution stack tab. This brings us to the final debugging tool in control regritor that is execution tab. As you can see here under the execution stack, it displays all the nodes in the sequence of the execution along with the node count on the right as that's turned on under the class settings. On the left, it displays the total number of execution that's taking place in the rig graph. One thing to note here is that this doesn't reflect the total number of nodes in the graph as it displays different executions like copy execution and various array iterations along with the nodes. I can quickly navigate through these executions using up and down arrow keys on the keyboard and press F to focus on a current selection in the rig graph. So you can use this and all the other tools mentioned in this module to quickly debug your control rig. Since there is no glaring bugs in our setup, which means we are all ready to use this control rig to animate our character in the next module.