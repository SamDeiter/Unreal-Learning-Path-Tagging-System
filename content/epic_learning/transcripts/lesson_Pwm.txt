Welcome to this material workflow class. In this chapter on material instances we're going to cover what a material instance is, how to make one and why and when you would want to use them. We are in the level workflow underscore start, which is in materials slash workflow slash start assets, one right there, and we have three different doors here. We'd like to make three different door materials with some slight differences between each material. For example, one door should be red, the other door should be green and so on. We have a material over here called instances underscore m underscore door underscore master. This is the material we're going to use. And we're going to use material instances to create a different versions for the door. A material instance is a modification of an existing material. Essentially, it allows you to create a new version of a material that inherits the functionality of the parent. We often call the parent material a master material. That is why this one is called the master material. And in fact, if you'd like to create an instance, all you have to do is just right click a material and right click the door material we have here. And we're going to pick create material instance. You can give it a name and I'll just leave the name at the default. And you can see it's darker green compared to the light green regular material. This is an instance material. If you open it up, you will notice that it has a different user interface. You can see the material previewing here, but there's almost no properties available. And there's definitely no graphical network and a shading network you can put together. That's an instance. It uses a simplified interface and it only allows you to change the properties that were designated in the material to be allowed to be changed in the instance. Now, we haven't said anything to be allowed to change to be changed. So you don't see any properties here. So what we're going to do now is we're going to let's move the instance to a site for a second. We can open up the master material and maximize that in the master material. We can see a setup is very similar to what we did a couple of chapters back for the door. It's the same door setup as we had it. And we're going to make sure that we can modify some of these properties within the instance. In fact, for example, let's say we would like to have different types of colors. Right. So let's add a multiply here and let's add a constant tree vector and something like so. And then let's say by default it's red. So we have a red door. But we'd like to create a second version. A second version that's maybe green. So we'd like to have this thing, this expression, to be accessible in the instance. The way you do that is by turning them into parameters. So anything that is a parameter becomes exposed to material instance. In fact, if you in the right click menu look for parameter, you will see there's quite a long list of things that are parameters. All of these are exposed. Everything else is not. Now rather than using this list, what I usually do is I simply right click the thing that I'd like to make into a parameter. So I'm right clicking the constant tree vector here. And then it says convert to parameter. So it automatically converts it to the parameter version of the same thing. Because it looks the same. It's still a color. But now it has a name. It says num. In fact, here also it says parameter name num. So I'm going to call this something color. You can by the way also set groups and sort priority. If you'd like to, if you have very long list of parameters, you can use these to organize it a little bit. But right now the most important thing is simply the name. Now if you approve it as is to press apply and then go back to the instance. Now you see color here. In fact, I can change the color here. I can enable the color override. So enable the override and change the color to something else. Say green. And if you take a look in a content browser, now you see the red door and the green door. You now have two different versions. You have the master material and the green version that drives from it. It uses the same functionality. If I would do something in the master material, it would also automatically be applied to the instance. And you can continue like this. You can do multiple additional things. Let's say you would like to modify the textures. Which texture is assigned to the material instance should be accessible? Well, right click the texture sample. Convert to parameter. Then parameter name, give it a name. We're going to call this base color texture or similar name. And essentially you would continue like this. You can take the normal map, for example, right click convert parameter, normal map. So and essentially most things that you think you might want to change at some point, you turn all of them into parameters. There are other additional things you can do too. For example, one of the parameters I use a lot is a switch parameter, static switch parameter. Because a static switch parameter allows you to turn part of the functionality in the material off. So that's very, very powerful. So for example, you can say, you know, in some scenarios, the material should look wet. So it's true. In other scenarios, it should not look wet. So it's false. So you have this property you can enable or disable. We're going to do this for detailed normals, which is something we haven't covered before, but it's something it's pretty common. So we're going to make use of the opportunity here to also quickly bring up detail normals. We're going to add a new texture sample to do so. And that texture sample is going to use a texture called t underscore detail normal. So t underscore detail normal. It's essentially just a norm map of some fine detail. And then we're going to blend this fine detail normal map with the actual normal map. So I'm going to blend these two together by saying blend angle corrected normals. So that's the base normal. The one we just added, the new one, the detail norm is going to be the additional normal. And that will go to true. If it's false, it's simply used the normal as is. And then the switch is going to be called use detail normals. So now essentially this by the way connects to normal. Now you can basically essentially turn this layer off, only use this normal map or use a combination of the two if this gets enabled to true. And you can choose between the two. Now just to wrap this little setup up, we're also going to make sure you can change the scale between material instances. So if we add a texture coordinate and we hook the texture coordinate up to the UVs, then normally what you would do is you would enter the scale of the texture here. But this number is inaccessible to the material instance. In fact, if you right-click it, you also can't convert this one to a parameter. So here we have a type that is not available as a parameter. There's multiple of those types in those kind of scenarios. So what you can always do then is to simply take the number that comes out and add a multiplier in this case and multiply it together with a parameter. And that will still give you the same effect. So we've added a constant one vector here, just a constant. That gets connected to the multiplier. I right-click that again, convert the parameter. I'm going to call it detail scale. But detail scale is good enough. And make sure that the default value is not zero when I call it say one. And that will be it. Setup we have now is this. And if you apply this and take a look at the instance, you can see we have a number of additional properties here. We could override the texture. Just take something random here. I'll just take some random texture. And you can see now we have a door that looks like this, which is not super realistic. In fact, let's just assign that to the level as well for a second. So we can see what it looks like. Here's the red door. And here's the green door with a tree bark. Let's reset that. That's a little bit strange. But we'd like to take a look at use detail normals. So here's the true false switch. So we're going to say yes, it is actually true in this case. What we're getting now is an additional property that joined in detail scale. And now we can change the scale of that overlay normal map. And that's essentially a material instance. Now in terms of why and when you would like to use this first of all, I'd like to point out that the example we've built here is only a very simple example. What we are doing here is essentially using a material instance to create different versions of what is really the same object. We're simply creating different versions of a door. But you can go another step higher and say that you create a master material for anything that is wood inside a building. So you would create one master material for anything wooden for doors for tables for floors for anything. And then you create a large number of instances, maybe several dozens of instances that all derive from that same master material. That is in fact the most common way of using material instances. To create an instance that is used through who for a large number of usually widely different uses. Material instances really help with collaboration in a team because you can have one team member work on the master material, the parent material, while everyone else simply uses the instances. This allows multiple people to work on the same assets at the same time. Again, just like functions, it also allows the more experienced team members to work on the master materials, which is more complicated work because you have to build up the network of expressions. Whereas all the other people in the team will simply use the material instances, which use a simplified interface. They simply have to check the properties they would like to use. And lastly, material instances are also slightly better for performance and it's also easier to tweak them because if you change something here, you don't have to recompile a material. It doesn't have to be applied. It instantly changes even in the level. As you can see. So it makes it much easier to tweak and iterate on content. Material instances are probably the most commonly used additional feature in a material pipeline in your real engine. So we've taken a look at what a material instance is, how to make one, and why and when you want to use them.