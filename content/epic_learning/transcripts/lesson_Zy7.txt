When creating a new landscape or terrain for ourselves, one of the options that we used was called a component. And in order to understand what kind of resolution settings we are to use when creating said landscape, we also need to understand just what a component is. To do that, let's come to the Managed tab of the terrain tools. And after clicking on the selection tool, we can click to create a new terrain, just as we did earlier. Then take a look at what we have. Although we will just quickly set up these at position of this terrain to a value of 200, just to stop the co-plane aflicker that we are getting here. These components are in reals way of not only visually representing the terrain, but also of calculating collisions, knowing how many are created then is going to be important. Now each of these components uses a texture that stores height data, and this is why power of two component sizes need to be used. Now each component can be split into sections as we saw earlier when selecting our options. So either one section or four. Now it's good to keep in mind that this is done via the sections per component option, and will again increase the demand on computing resources when using the higher numbers. So let's just show this in action. Let's set this to a value of 2x2. Now this gives us the same size height map as using four times as many components with the option set to 1x1 that is. But because we are using fewer components, this generally gives us better performance, and so it's something that we will need to keep in mind. This constant balance of components versus amount of sections is something that we will need to keep an eye on on a per-project basis. Is he having some background information regarding the settings that we are using? We'll of course help us make informed decisions when it comes to both performance and visual fidelity or our landscapes, both of which are very important.