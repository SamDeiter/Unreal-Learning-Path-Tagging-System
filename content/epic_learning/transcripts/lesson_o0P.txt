Over the next few modules, we'll be locating our model in relation to the real world, utilizing both Magic Leap's controller, as well as an external image marker. We'll start by only using the controller's touchpad to push and pull the model across a level plane. After that, we'll parent the model to the controller in order to move that model freely. Lastly, we'll explore how an image marker can be used to snap a model to a fixed location and rotation configuration. This module will be learning how to use Magic Leap's core peripheral, its controller, and using its touchpad to move the model across the X and Y axes of the real world. Now, to begin to use the touchpad on the Magic Leap controller, we'll actually have to create an XR pawn. This will be our first blueprint, so we should create a folder called Blueprints. Inside the Blueprints folder, we can right-click and say that we're creating a new basic asset, a blueprint class. This class will be a pawn as it will be our character controller. We can call this XR pawn touchpad. Let's open up our XR pawn and set up a few elements of it. First of all, we will want this to auto-possess Player0, considering this is a single player experience, Player0 will be the only one used. Next, let's add component and type in Scene. This will be our XR transform guiding all the actors that come under it. Inside the XR transform, we want to place a camera, and another component called Touchpad gestures, which has the Magic Leap header. Once that's set up, we're ready to go over into the Event Graph tab, and we're going to be adding a new event. And if we click on Touchpad gestures, over an events over here, we want on Touchpad gestures start. Hit the plus, there it is, and the first thing we're going to need to figure out is when someone touches the touchpad, what is happening, so we can type Break, and Break Magic Leap Touchpad gesture appears. We'll hit the drop down, and as you can see, there's quite a bit of information that can be gleaned from this node, including what hand is being used, what direction a swipe is happening, the position and force of it, etc. All we care about right now is whether someone is swiping left, right, up, or down, which of course will be direction. So if we pull out from there, we'll type in Switch, and there's only one option under Flow Control, we're going to switch to see which direction is being swiped. And we can see here with the drop down, there's none up, down, left, right. In and out is only for the mobile device, which is not part of this course, and then clockwise and counterclockwise. So let's start by connecting these nodes to set them up, and then we're going to decide what happens when we swipe up, down, left, and right. Well, we're going to move the model, so we need a model, so let's create that as a variable. We'll call it Site Model, and make sure you change the variable type to Actor, and then we can also make it an instance editable actor, so in our map we can actually decide exactly which object in that map that will be, and of course it will be our Site Model Root. So here we can get our Site Model, and decide what's going to happen to it. So if we swipe up, we're going to do a local offset, make sure the target is the Site Model, and we're going to move it 20 centimeters up, which will be on its X axis. And as you might imagine, down will be negative 20, left will be on the Y axis, right will also be on the Y axis, in and out, again, not applicable to this. And if you wanted to, you could do clockwise and counterclockwise to play with rotation. I'll leave that as a stretch goal for you. And let's add a comment box to remind us what we're doing here, as this pun will become more robust as the course progresses. So I'm saying for the comment, move model along X and Y with Touchpad. Additionally, we might want to add some debug information, so we can add a print string to each of these. This one will say, up, down, left and right. And if you're bothered by this being slightly offset, we can select them all, and do Alt Shift S to align them to their center. With that, let's compile, bring this into our scene. Note that its location will be affected by the player start location, unless we delete that. Because the camera here is going to be much more visible to us, I'd say let's delete the player start, so we don't get confused. And let's save this as our next scene. Let's go 0-1, underscore using ML, underscore Touchpad movement. Let's save all. And because we're only moving on the X and Y axis, the height needs to be assumed from the start. So let's say you've measured the table you want this to go up on. And in my case, the table I want this to sit on is 76 centimeters off the ground. So right now, I'm going to raise this, 76 centimeters. Similarly, I may wish to start my player pond and its camera at ground level. That way, when I pick up the magic leap device from the ground, it will be at the correct height to view the site model on a 76 centimeter table. Let's go ahead and start our magic leap remote with the device, and then go to VR preview. If the model is too high, we simply need to reset head pose. We can do this by placing the headset on the ground, opening up a command prompt, and typing MLDB reset head pose. And there's the model. But before we start swiping, let's open up our Unreal editor to have in the background. And now we can try swiping up, down, right, and left. And of course, we see each of those being logged here in the output log. In this video, we used the X and Y axes of the magic leap controllers Touchpad to simply locate our model in the world.