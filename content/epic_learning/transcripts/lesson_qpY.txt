In this module, we'll go over the basic BP generic interaction actor that's already in the project. We'll add an event dispatcher to it and use that event to start spawning the targets in the BP target spawner. At the moment, we have a begin play that starts spawning the targets. We want to replace the begin play with a custom event and then call that from a different actor. I'm going to go ahead and disconnect this, move this out of here, I'm going to go add a custom event, we'll call this start spawning. You could call it start spawning targets and I'll rename the begin play to start spawning. I'll save and I think we can get out of here. If we go to content and then blueprints, you'll find that there's a generic interaction. This is just a little button. If we drag it out, you can see there's E above it, text, there's a sphere around it, which will do the interaction. Yeah, let's have a look inside, actually. Now that we look in here, we can see a few things already done. As you can see, under class default, the tick is disabled by default, which is a very nice thing to do by default, just disable the tick. Even if you're not using the tick and this is not disabled, it can still have a very minimal impact on performance. But if you have thousands of actors in your level and they have to start with tick enabled on, it can have an impact, even if it's small, it's a good thing to disable this by default. Which is something that you can also do in the project settings, have the tick disabled by default, but we won't go into that for now. You can just look that up, it's pretty straight for it. From begin play, we set a text render to hidden, which is that E button for the children we can use, interacted. This is a hard coded interaction input, so instead of the properly set up input in the project settings, what happens? When we interact with it, we check if the actors recently rendered to ensure that the player wants to interact with this actor. Now we call the interactive event, which is the one we have here, play a little sound, and the animate the button. Here's the overlap, which enables the tick, and then the tick makes sure that the text is rotated to the player camera, which this logic we already also did in the target spawner. Now for me, this is missing something at the moment, is missing an event dispatcher. So we'll add an event dispatcher, and then we'll start setting that up. An event dispatcher, very much like casting or interfaces, is a way of communicating between actors. I'll try to explain how to use an event dispatcher by showing you this example. I'm going to skill this down a little, drag this out, and we will create our event dispatcher over here under my Blueprints, you'll see event dispatcher. We'll create a new event dispatcher, and call this dispatch, interact, or interact. Now if you drag this out, you can see a whole bunch of settings here, call bind, unbind, unbind, all event sign. We're going to do a call. When the player interacts with this button, the event dispatcher is called. It also has this little open mill icon next to it. It basically sends out a message. It dispatches the message. Now we need to set up a binding for this somewhere. That's where the level blueprint comes into play. If you go to Blueprints and an open level blueprints, this is a blueprint that is specific to your level. So everything you do in here, you cannot call in another level. But the nice thing about the level blueprint is that you can reference actors in the level. Say we have this button here, we can, with that selected, we can go into our level blueprint, write mouse button, and we can already see create a reference to BP generic interaction. So this is a direct reference to this actor. We want to do the same for our spawner. So with our spawner selected, go back and create a reference to our spawner. Now just like any other blueprints, you can do a tick in here and you can do a big in play in here and the big in play is what we need. So on big in play. Now we're coming back to the dispatcher again. We can create a binding bind event to dispatch interact. We can associate an event that's bound to the dispatch interact that we just created. Create a custom event or add custom event and we'll call this start spawning target. And with a reference to our BP target spawner, we can now call start spawning. Connect these up. This already works. Now the nice thing about dispatchers is maybe we have 20 more different actors that will start doing things. Maybe we have a music actor that we want to start spawning music or we have a particle actor that's in control of all the particles in our level and we want to have that spawn particles. The moment we interact with that actor, you can bind as many events as many things as you want to that one dispatcher. So you can do another bind to dispatch event and add a new custom event for that and you can do that as many times you want over. This one little dispatcher just goes for everyone. There's no overrides. There's just unlimited amount of connections that you can have here. So it's a really cool way, a really easy way of associating events in different actors of communicating between actors without having to use a cost or without having to use an interface. So now when we go to our aim training and we press play, as you can see there are no targets. We walk up to here, you press E and they start spawning. It's as simple as that. All we had to do was add one dispatch event, which is now there forever. So we could add more buttons and set up different things for those and we now have our target spawning on the press of a button. If I went over the generic interaction actor a little bit too fast for you, take your time, go over everything, see if it makes sense. We're now able to push a button that will start spawning the targets for us. We had to look at defend dispatchers, which can help save us time in the future. In the next part, we'll have a look at the damage interface that will allow us to destroy our targets.