Compression and memory. Textures are almost always compressed. You're going to take a look at why that's happening, when that's happening, how to change that, how to possibly turn it off if you would like to either temporarily or permanently. There is BC or DXEC compression. So what is it? Difference is the different types of compression there. Normal maps are special case. You can't just compress them with a regular BC or DXEC compression. You will get issues. So why are they different in what's being done behind the scenes and tracking the size of textures and the impact they might have on performance? So textures are compressed on import. They're always compressed by default. The compression they get differs per platform. BC or DXEC, by its old name, is used for direct X on PC. So BC stands for block compression. DXEC stands for direct X texture compression. Sometimes you also see DXD without a C. That's the exact same thing we just left off on a letter. So that's the standard format because that's what direct X on PC uses. The compression name is followed by a number to identify the compression type. So each of these formats have many different types of compression within it. So you would have BC1, BC3, BC5, or BC7 are common ones. So the number identifies a type of PC compression. Similarly, the same thing happens with DXEC. You could have DXEC1, 3 or 5 are common ones there. This could be disabled if needed or temporarily or permanently. So if you take any texture, let's just take the door here that we used in the previous part. You can see that I have this currently set to vector displacement map. The default is actually default as the name implies. So let's set it back to default. This is what you would have in simply import a texture. It means it gets to default compression and the default compression is, as you see here, DXD1 or 5 or BC1 or 3 on direct X11. So the names are interchange. It picks between 1 and 5 or 1 and 3. BC1 is equal to DXDC1 and DXDC5 is equal to 3. The difference is exactly we'll get back to in a second. For now, just focus on the size. At the top you see here, it says resource size 2731 kilobytes. So let's say around 2.7 megabyte. It is format DXEC1. So the compression settings you can set here. Let's set that to, for example, vector displacement map, which is what I had a second ago. And you can see now the format is a code. The code stands for this. The code is actually uncompressed. So vector displacement map is uncompressed. It's going to take a bit of practice to figure out which code stands for what, but if you see something like this, it means it's not compressed. It's completely uncompressed. But the resource size, went up to 21 megabyte. So that's a huge difference. So this controls the compression, but the names in this list are pretty bad, I think personally. And this is very confusing, because the reason is simply an item that says uncompressed. There's all very particular settings here, and many of them might be very misleading. For example, there is gray scale. And I've seen the Velper Zoodis in the past day, I think I have a gray scale texture. For example, you have this one here, the roughness texture. It's obviously a gray scale texture. So I'm going to save some space instead of having it on the fault. Let's put it to gray scale, because that's probably cheaper, right? It's only gray scale. So you put it to gray scale, but without looking at what happens over here. Resource size is now 21 megabyte. In fact, the default settings had it at only 2.7 megabytes. So it became a lot bigger by having it on gray scale. So putting it to gray scale doesn't help at all. In fact, putting it to gray scale makes it the same size 21 megabyte, as putting this one with color, red, green and blue colors to vector displacement maps. So gray scale doesn't really do much at all in that sense. So again, this list is confusing. Please pay close attention to what you're doing and look at the resource size, because that's the best indication of what you're doing. It's the best indication if it's compressed or not, and if it's actually helping what you're doing, or if it's actually making it worse, like putting this to gray scale is making it worse. Now, that's the permanent compression. This is permanent. You also have the fur compression here, which is a more temporary change. If you enable the fur compression, then you literally defer the compression until I believe you safe. So it makes important fuss, because when you import a texture, the moment you import a texture, it also automatically compresses it, but the act of compressing it could especially on a very large texture. Let's say you have an 8192 texture, it could take a very long time to compress, and it might make it very annoying and difficult to work if you have to be waiting for this compression all the time. So enabling the fur compression will disable the outer compress on import, and only compress it the moment you save it, and that could be helpful. Now, what the number means, we'll get back to now. So BC has many different compression settings not exposed by on the N4. So that's also important to note. And the number BC3 are textures with alpha, but BC1 are textures without alpha, an equivalent for Dxc. So Dxc5 are textures with alpha, Dxc1 textures without alpha. That's identifies a difference in number that it picks, and it picks this number automatically. So if you were to import a texture in and real, without alpha, there's no alpha channel, let's just take this one. You see it takes Dxc1 or BC3, or one actually, sorry, but if it has alpha, let me try to find an alpha texture somewhere in here. Remember where I have this, I'm just gross somewhere. For example, this is a gross texture called T field gross, 0 1 D, you want to look it up in a content browser. It has an alpha channel in here. In fact, the alpha channel contains the mask for the gross. Having this pushes it to Dxc5. That's the difference there. And it's very interesting to note what that does, because the alpha channel is nearly uncompressed. Dxc5 means to compress red, green and blue, and include an alpha, but don't really compress it. So that alpha is equal in file size as these three together. And you can see that here too. This resource is 1365 kilovide big. But if I say here, compress without alpha, which is going to change that Dxc5 to Dxc1, and this will obviously remove the alpha. So it wouldn't work for the gross necessarily. Let's just do it for a second. You can see the texture is now 680,3 kilovide is half of it. And it's now Dxc1. So the difference is having or not having alpha, and you're going to have to manage that carefully. We'll get back to our second how you manage that. Let's first go over some other important details. There is a plugin you can get over here from Intel. It's called the Intel Texture Works plugin for Photoshop. And this allows you to expose some of these settings and preview some of the compression settings directly in Photoshop as you can see here. Unfortunately, at this point Unreal does not allow you to import these already compressed textures from Photoshop. Unreal insists on compressing it internally so you cannot import an already compressed texture. But you can use this tool to in Photoshop see what would happen to it. So here's a little preview image of the project we're using. It's very low resolution, but I will actually help us see how the compression will work out. I have this plugin installed. I'm going to do save as. And I'm going to save it as Intel Texture Works DDS. DDS is the compressed format. It's directly draw surface if I'm correct. I'm not 100% certain. But I believe that's what it stands for. Then you get a compression settings and you can see the different types of BC compressions we can use. And that's more than Unreal offers by itself. But we can also do preview and in preview you can zoom in. You can see the left one is the uncompressed, the right one is the compression. You can see the compression, especially on gradients you do see the difference. You can see this is 108 kilobytes. It's now 18 kilobytes off the compression. It is running BC 1 and you can switch it to BC 7 which is the best quality. And you can see at BC 7 there's a bit of slight slight difference here, but it's nearly identical and this is 108 kilobytes, 26 kilobytes in BC 7. Unreal doesn't use BC 7 by default but it is possible to enable it if you would choose. So there is a setting here BC 7. Again most things are going to be BC 1 or 3D XC 1 or 5 and that's it. Now normal maps are a special case and you should be aware of that as well. It's very important that normal maps are configured correctly. In fact you cannot just import a texture, a normal map texture, then not compress it, not change any of the settings and just import it as is because that's not going to work well. Even if it looks as if it's working that is not correct. Normal maps use a special BC 5 compression. Because the problem with BC compression and the XC as well is that it's actually really weak with the exact color combination that a normal map is. It has difficulties compressing the ways a normal map blends the different colors together. It's a weak point in the system. So we can't compress normal maps with a regular compression and we designed a new type specifically made to handle the type of colors we have in a normal map. And then the next really important thing and a real fun aspect of this is that a normal map doesn't actually have any blue. You can see here I have a normal map of a couple of plants. The blue does not exist. Blue isn't really here. You import it with blue. It has blue in the original image. But unreal sees this. It only seems red and green. It doesn't see this. This is actually removed. So what the compression does is it removes and strips out blue. And then it only stores red and green. But because it removes 30% of the image it can store more data here. It can do less harsh compression on red and green because it actually removes part of the color. In fact you can see this happen in material editor as well. You can reconstruct this. Because if you have a normal map here you can do mask RNG red and green. So essentially get something like this. This is not entirely correct. It's too dark here. Just focus on this for a second. And then you have a function called an expression called derive normal z which based on where red and green is figures out where blue should be. And that actually restores your rich non-normal image. This is what happens internally. You don't have to use this. This is happening internally if you use the right compression type. And you can see in render lock it's well which shows us what's happening on the GPU. You can see here we're actually rendering a scene out here. But you can see on the inputs these are the textures it receives. Right so it receives a couple of textures. There's for example a stone diffuse base color texture. This is the normal map. There is no blue in the normal map when the normal map is on the GPU. So again, pay close attention to that. If you import a normal map it is set to compression type normal map. Very very important. If you don't set it to that if for any other reason you need to have a normal map 100% uncompressed. Set it to for example vector displacement map which is very neutral one. It doesn't do anything just import that as is. But then pay attention to the fact that it's going to expect something to happen in the blue channel. Possibly remove it you could do that. And you're going to have to use a setup in the material editor to recalculate a range from minus one to one. It's not something I have covered in this class but point being pay attention to that be aware that it happens and looked it up online and see how we do that. Now the reason why we compress is because we have limited memory and bandwidth. So we have to compress which simply cannot have everything uncompressed. In particular, a very large project. It's very difficult to do obviously. The texture resolution affects memory and bandwidth but not rendering performance. If something goes wrong with the amount of textures you use and the size of the textures you're using it's going to impact the bandwidth and memory and it may cause lack. It may cause freezes and hitches but not frame rate loss. So having a low frame rate is usually not because there is a problem with the textures. If there's a problem with the textures what you're going to see is you're going to see very low resolution textures appearing, low resolution then what you import into mass. You're going to see lag and freezes, hitches, those kind of things but not low frame rate. So that's how you can differentiate the performance problems you might be seeing. If any of those things happen it's usually related to loading and it's usually related to textures. Now the statistics browser in the editor show the amount taken up in the editor and the project and start memory in particular shows it in game. So the couple of tools you can use to figure out how much memory you're actually using. If you're using not much memory you could get away with simply uncompressing all the textures in your project. Especially if it's not a game. If you're building something that's not a game you're controlling the hardware that would be acceptable. But if it's the game it's almost never ever done to disable the compression. You only disable the compression in extremely specific cases where it's really important it's not compressed because you see some artifacts you're doing some kind of weird you know more stranger setups and so on. Now in the editor in window statistics you get a window that shows you the primitive stats by default. So here's the button. Then instead of the primitive stats make sure it's set to the texture stats and this shows you all of the textures that are currently in memory in the editor which is not necessarily the same as in game. When the project is running this is what the editor has and the editor tends to load more than it should but it gives you an indication already because you can for example store it by resolution you can see oh I have a very large texture there let's find that one. In fact you can click on actor you can see okay so that's that model right there that's using a very large texture. Is there anything I can do about that? So you can try to find textures that are much bigger than they should be or smaller. You can see the compression type here as well you can see these are all the ones uncompressed. If it says B, A, G, A, R, A, A, A that is uncompressed so you can again find them and once more these are actually editor icons so it's not really that important. You can see the size they take and so once again you can use this list to try to work out what stands out what's a lot bigger or a lot smaller. In the texture stats you also got cooker stats instead of to cooker stats now you don't have to select the platform and the problem is I don't have any platform set here so this is empty but what this does is it takes you last cook to last time you package to project and it takes the values from that packaging process and it will allow you to open it up over here and see that so in other words if you would have done file a package project then wait until it's done then go back to this window then you will have an entry here that says select platform you can then select the one that you just packaged for and then you're going to see the exact same list as this but for what you package to instead in other words you're going to see the exact list of textures as it was packaged into the final project itself rather than what the editor sees and that's a more accurate list. Likewise in the editor in game you cook run the game and I'm going to do this in editor now to make it easy but you cook run the game and type start memory in the console and you would get this window in game and then you can see again the texture memory pool is this big you know the texture streaming texture pool that's right that's right so you can kind of see how much you're using and get a bit of an idea for it. So I just thought about the textures that I usually compress are almost always compressed the different compression types and formats for PC that is BC and DXCC normal maps there is special case why is that so that's what something we looked at and tracking the size of textures how can you keep all of this under control we'll continue with textures in the next part of this class as well.