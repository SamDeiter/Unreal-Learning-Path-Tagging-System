In this video, we're going to create our characters first, Blendspace. Blendspace allows us to blend animations in real time based on one or two parameters, depending on our requirements. A normal Blendspace uses two parameters to drive a two-dimensional graph, whereas a Blendspace one D only uses one. We're going to set up a one-dimensional Blendspace to begin with to get our character moving. To create our Blendspace, I'm first going to create a new folder. I'm going to call this one Animation. And inside, I'm going to right-click and go to Animation Blendspace one D. I'm going to select the bot, and call this BS one D underscore idle run. I'm going to open it up. And then in the horizontal axis settings, I'm going to set the name to Speed. The minimum axis value to 0, and the maximum axis value to 500. This is the maximum speed we're going to set the character to be able to walk later on. And you set the grid divisions to 1, because we only have two animations that we're going to use at this point, one for idle and one for running. First, we're going to drag the idle animation. And so, from the asset browser on the right-hand side, we're going to grab A underscore bot underscore idle, and drag it to Speed equals 0. You should see the character pop into an idle animation in the viewport. For 500, we can use A underscore bot underscore run, and just drop that in place. Now, I want to hold Control and drag my mouse along the graph. You'll see the character begin to run. This is our preview point, and it's quite handy to check that we've set up our animations correctly. Now we have the first blend space created. We can use it instead of the idle animation in the animation blueprint. In the next video, we're going to use our run blend space to get the character moving.