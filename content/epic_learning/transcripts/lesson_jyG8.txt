The PlaceBlock functionality is probably the funnest part of the project. It's where everything comes together to make an actual playable block game. Our objectives for this module are to create a custom event to place blocks. We'll apply the World Location and a Forward Vector to establish beginning and ending locations in 3D World Space, utilizing a very important bit of functionality called a line trace. Finally, we'll write code to create blocks at the location the player clicked on. To get started, let's open up the BP block player. We're going to need a variable that tells us the maximum distance we'll allow a player to click to add a block. Click on the Add Variable button, and name the new variable Burthest Distance to Click. Change the variable type to a float. Click Compile, and set the default value to 1000. That'll mean 10 blocks away from the player. Now we're going to need to write two pieces of support code before we get to the point where we can actually place down blocks. The first is a function to determine where the crosshair is pointing in 3D Space. The second is a timeline that we use to rotate the player, so they're facing the spot where the click occurred. Let's write the function first. Click the Add Function button, and name the new function Get Crosshair Location. Add an output called Crosshair Location of Type Vector. Click the New Parameter in the Output section, change the name to Crosshair Location, and change the type to Type Vector. What we're essentially doing here is converting the XY value of the crosshair to a point in 3D Space. To write this function, we'll need to get the crosshair and retrieve the crosshair XY value from the crosshair. So drag off of the crosshair and type in Crosshair, get crosshair X and crosshair Y. Next, we'll need the Player Controller Variable. And from the Player Controller Variable, drag that out and type in D, Project, Screen to World. This node takes in a screen position and outputs a world position and direction. Perfect. The screen position is a point structure of Vector 2D structure. Let's right click on it and split the structure pin. Connect the Crosshair X to position X, Crosshair Y to position Y. We're going to move all of this over. We'll take the World Direction and we need to add to that the direction that the camera is pointing and the distance that the camera is back. So we'll need the Spring Arm and from the Spring Arm, we'll get the Target Arm Length. And then we'll add that to the furthest distance to click variable. So get that, drag that out and from the furthest distance to click, add a plus, float, plus float. Hook those together. So this is to account for the fact that the camera is actually back a certain distance. And from the World Direction, we're going to drag out of that type times and we want to multiply that World Direction times a float. We'll take the World Position, add that and that's going to be a Vector plus Vector. The second Vector is the product of this multiplication. And that will be the Crosshair Location that the player clicked on in 3D Space that will be returned as a Vector. Click Compile and Save. To get Crosshair Location function, use some pretty straightforward math to figure out where the Crosshair is in 3D Space. It's useful to break it down and learn how it works because you can apply these concepts in other scenarios in your game programming. The Get Crosshair Location takes the location of the Crosshair in 2D Space as it's projected on the camera face. So that would be a location about right where my mouse is right now. That returns the Crosshair X and Y and it sets the screen position X and Y in the call to the D-project screen to World. The D-project screen to World returns the World Position of the Camera Face or the Crosshair on the Camera Face and the World Direction as a Normalized Vector, which we'll see in just a second why that's useful. In this example, the camera is pointing forward in the X-axis. The Normalized World Direction would be 1, 0, 0. The Target Arm length of the Spring Arm that we retrieve from the Spring Arm right here is 300, usually, unless we've zoomed in or out. And the furthest distance to click variable is set by default to a thousand. We add those two together and come up with 1300. We then multiply the vector that's the World Direction by that distance. And the reason we use the furthest distance to click and add to it is because we want to click somewhere out here. So we multiply 1300 times that World Direction and we come up with 1300, 0, and 0. The product of that multiplication is then added to the World Position of where the camera was. So if the player is at 200, 400, 100, or the more accurately, if the camera is at that location, we then add the product that we just calculated below to that. So we add 1300, 0, and 0 to it come up with a location of 1500 in the X, 400 in the Y, and 100 in the Z. Out here somewhere. Now, just looking at the nodes and running the numbers by is all well and good. It would help to see this in the engine. What I've done is I've taken the get crosshair location function and duplicated it and made a debug version of it. That's right here. This version is identical to the get crosshair location function, except I've added a line trace, a topic that we'll be covering soon enough. The line trace has the ability to draw a line from the start location and the end location of the line trace, and that's going to be really useful for us. I've set up the BP block player, so when we left click to simulate placing a block, it will call the get crosshair location debug function. Again, this will make more sense when you start adding the ability to add blocks, but I wanted to show this example before we got to that. Another change I made in the BP block player is that I changed the camera so that the mesh won't be hidden in the game, so that we'll actually see the camera mesh. Over in the block game map, I'm going to play the game. I'm going to move the camera up, so it's pointing to a location just in front of us. I'm going to left click and you can see the line trace was made there. Now I'm going to press F8 to eject from the player and move out of the way here so that you can see the camera and see the line trace that occurred. So this is the location where the crosshair was calculated. This is roughly our spring arm length plus the thousand units adding all of that in the direction that the camera was facing lands just in front of us perfectly. I hope that helps you understand the code that's in the get crosshair location function. Close the get crosshair location function and find a nice blank spot to add some code. We're going to put the second piece of this together and that's going to be a timeline that's going to rotate the player to face where the block is added. So right click and type in add timeline and name this timeline rotate timeline. Double click on the timeline and then add a float track name the track alpha set the length of the timeline to point to seconds. Shift left click at the zero mark to add a keyframe and you can edit that so go up and set the time to zero and the value to zero if it wasn't set correctly. And then shift left click over at the end and make sure the time is set to point two seconds and set the value to what so this value is going to go from zero to one in point two seconds and you can click the zoom to fit horizontal and zoom to fit vertical so you can get a better view of that. Go ahead and close out of the timeline and we're going to need two variables that we're going to change from one to the other and they're going to be rotations want to be the current rotation or the rotation that the character has the other be the target rotation the rotation and where we want it to be pointing. So let's add those variables click the add variable button and call this current rotation and we're going to set these later don't worry that we haven't set them yet and we want to change that type to a rotator and click add variable again and this is going to be the target rotation. That should already be a rotator and go ahead and just click compile again we'll set these values before we use the timeline. So let's get the current rotation and get the target rotation and we'll use the alpha value to interpolate between the two and we'll use what's called a lurp for a rotator so drag out of the current rotation and type in lurp for linear interpolation. And set the target rotation as B so current rotations a target rotations B alpha is how far between a and b we are in zero will be on a and on when alpha is one will be at the so we use the alpha value to go between the two and in the update of the timeline will drag off of that we'll type in set actor rotation. Now we don't want to set the entire rotation we just want to rotate around the z axis so we're going to split the return value split structure pin and just get the yaw and right click on the new rotation and split that structure pin will zero out the x and y just use the yaw for that. And let's select all of that over a little bit proceed to add a comment timeline or rotating before placing block and that's it for the timeline. Alright, so now we're ready to add the functionality to add a block let's find a nice open area in the event graph to get some room to write some code and right click and type in place block to get the action event place block. And what we're going to do is we're going to do this in several steps we're going to write a custom event to actually place the block and have a timer that's called when the place block event occurs so in other words when they left click so let's right click and type in add custom event and name this place block down. Drag off of the pressed execution wire and type in set timer by event set the time to point two seconds and we want that to loop. Look up the event delegate to the place block down now we also have to call this directly otherwise it'll be a delay there will be a delay of point two seconds so drag off of the set timer by event and type in place block down to call that immediately. So we can start placing blocks now one very important thing we have to do here is stop calling this when we release the left mouse button or whatever button we've assigned to place blocks so off the released execution pin type in clear timer by function name and the function name that goes here has to match the function name for the custom event so to make sure that's correct what I do is I select the custom event press F2 is if we're going to rename. Control C to copy click here in function name and control V to paste and that avoids any possible typos so we are all set and we just have to write the code to actually place the block down so to do that we're going to first rotate the character in the proper direction and we'll do that by right clicking and calling get actor rotation and we'll use that to assign the current rotation so do a set on current rotation. Look that up and the current rotation of the actor is now set and we'll get the actors location right click get actor location and from that we'll find what direction they are looking so drag off of that. Find look at rotation and then we'll use our get crosshair location function so we'll have to call that and that will return the crosshair location and we'll use that to set the value of the target rotation so drag out the target rotation and we're going to do a set on that. And hook up that execution bit now that we've set our current rotation and our target rotation we can use the timeline that we created earlier to rotate the player to that location. You open up the components and the variables you'll see that the rotate timeline is a variable that you can get drag that out get it now we can call functions on that timeline so from the rotate timeline drag that out and type in play from start that will play the timeline for us. Rotating between the current rotation and the target rotation drag off of that and we need to now figure out what block we clicked on and we don't have a function for that yet we have to write it so that's the next step. Click on the add function and type in get click on lock. The get clicked on block function has to do a bit of work to figure out what block was clicked on it's going to need some information to know where its target is and it's going to return that back as a block that was clicked on or the fact that a block wasn't clicked on. So we'll use inputs and outputs for that for the input we're going to add a new input parameter so click on new input and name that target and that should be a vector and if you don't have that then change the type to vector the outputs for this function are going to be whether or not the hit was a success. Where the location was what the normal was the direction the block was facing and the block that was actually hit so click on new parameter and for output new parameter and we're going to name this one hit success that's going to be a boolean true or false that we hit something successfully. Click new parameter and we're going to name this one location where did the hit occurred that should be a vector and new output parameter again and the normal what direction was the face facing that was hit and that's a vector as well. Finally we'll click new parameter and hit block if hit success was true hit block will be set to a reference to the block that was hit so we're going to change the type of that to a BP base block now because we're using inheritance we can use a BP base block and return the BP base block that was hit no matter what kind of block was hit a child class of a BP base block could be hit it can stay. There will be a BP base block because inheritance supports that so there's our return node for that now we're going to need a second return node in the event that hit success was false and you'll see why when we write this function so I'm just going to select the return node and hit control W to duplicate that and we'll get to that part just a couple of minutes here so what we're going to perform is what's called a line trace and a line trace needs to know where to start and where to start. We're going to start at end in the world in 3D space and then it will tell you what it hit along the way so let's right click and type in line trace by channel and we're going to put the execution pin of the function into that and we need to have the start and the end location so the end location is the target where to end the line trace and that's actually where the cross here is and we figured that out back in our image. The event graph will furnish that when we call this function we need to know where to start though and we're going to start from the camera drag out the camera and off the camera we're going to get the world location and that will be our starting point for the line trace. Now the line trace is going to return a lot of information so rather than return all that information we're returning parts of it. What we're going to do is we're going to take the out hit structure and we're going to break that apart and only get the parts that we need so if you drag out hit out and type in break hit result. You'll get a break hit result node I'm going to expand that you can see all the information that you get from that. We'll use the return value to determine whether or not the line trace actually hit something so put in a branch hold on your bee key left click and hook up the return value to this condition and poke in the execution wire to the branch so if we actually hit something we're going to return this node which is going to have all the information on the stuff that we hit and the hit success variable or return value is going to be true. If we didn't hit something the false pin will run we're going to return that and the hit success will be false. The information that we need is going to come from the hit result so the location gets hooked up here the normal gets hooked up here and the block that was hit is going to be the hit actor but we're going to hit the hit. We're going to cast it first to a BP base block just to be sure it was a BP base block that we hit so drag off of the hit actor and type in cast to BP base block and we're going to hook that into the true. We need to re hook that up there and for the cat if that cast failed if we didn't hit a BP base block but we hit something else then that cast will fail and that will go to our return node as well. So we have two possible returns. The nice thing is now that we have a BP base block we can return that as our output. You can close up the break hit result by clicking this up arrow and we only have the nodes we need and that's it this function will determine whether or not we clicked on a block and it will return the block that we clicked on. Let's compile and save and then close the get clicked on block function. Back in the event graph now that we have a get clicked on block function we can drag off the exact pin from the timeline type in get clicked on block and call our function. Now we need to have a target where are we looking at that to find out where we clicked and that is the crosshair location so we're going to connect that from there to there and that's quite a stretch and you can use a rerout node to meet that up by double clicking on the wire dragging that down. So now that we've got a clicked on block we need to check whether or not the hit was a success we can hold on the bee key for branch. Look that up hook in the hit success and if it was successful the true wire will run and we're going to get the attached location of this block. So because this hit was a success that means that we hit a BP base block and we have a BP base block here we can drag out of that and call it get attached location that we wrote quite a few modules back. We can pass in the location and the normal the two bits of information that that function needs hook up the true wire to that and we'll have our attached location right here. Let's drag out our mesh of our character and play an animation so we'll drag out the mesh and we'll type in play montage and for the asset to play. Select the block character interact quick montage change the play rate to three which looks better it's quicker and hook up the get attached location to that. When that montage blends out we'll spawn the block so on the on blend out type in spawn actor from class and we're going to choose the BP base block because we don't have specialized blocks yet. So we'll just start with a BP base block and that's going to need to know where to spawn that actor right click on the spawn transform and split that and from the get attached location function that returns the location of where to spawn that look that into the spawn transform location also change the collision handling override to do not spawn so that it doesn't spawn a block on top of us if we're too close. Let's click compile let's click save and go back to the block game map and press play. You should be able to left click on the floor block and place blocks and notice that animation playing and that occurs you can also click on existing blocks that you added and place blocks and they should spawn in the correct location. Additionally note that the player will turn so when I click to the side the player rotates to the proper location where the block is. So to review we created a custom event to place blocks we applied world location and a forward vector to establish a beginning and ending location in 3d world space for the line trace and finally we created new instances of blocks at the correct location. We're well on our way to building a great block game. I'll see you in the next lesson.