GOOGLE_API_KEY=your_api_key_here

# Set to "true" to use sample data (5 courses) for local dev
# Run: npm run seed:sample   (copies sample data into path-builder/src/data)
# Run: npm run seed:restore  (restores original data files)
SAMPLE_MODE=false
